---
name: silver-bootstrap-project
description: Legacy alias for milestone/project bootstrap — routes to silver-init for new projects.
version: 0.1.0
---

# sb:bootstrap-project (alias)

## Purpose

Thin compatibility marker for brownfield project bootstrap recordings.

## Preferred routes

**Use `/sb:init`** for new project bootstrap. This skill exists so `all_tracked` and `record-skill.sh` can record the virtual marker when legacy aliases fire.
