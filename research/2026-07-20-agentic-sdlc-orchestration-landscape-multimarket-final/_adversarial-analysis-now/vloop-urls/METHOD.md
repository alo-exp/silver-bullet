# Method (independent V-loop)

1. Graphify query for `homepage_by_slug` / `vendor_link_labels` / `render_landscape_outputs`.
2. Byte-scan HTML, `landscape/landscape-report.md`, chart-data, comparison, four category-pack copies, three `vendor_link_labels.py` mirrors.
3. Parse `#report-data` JSON; compare `.markdown` to `landscape/landscape-report.md` with `===`.
4. Extract all markdown links whose label contains cc10x / Cavekit / Barkain.
5. Binary-scan PDF `/URI` annotations (pdftotext is insufficient).
6. Live GET the three GitHub URLs (HTTP 200) and attempt the three old marketing hosts.
7. Confirm synthesize/render still call rewrite/scrub (not HTML-only).

Out of scope for these five criteria (not scored FAIL): rewrite-map keys inside `vendor_link_labels.py`, unit tests that mention dead domains, critique briefs under `_multi-ai-critique/`.
