---
project: Silver Bullet
period: 2026-08
type: knowledge
---

# Project Knowledge — 2026-08

## Architecture Patterns

2026-08-08 — Current operational guidance and historical evidence are separate governance classes. `docs/doc-scheme.json` inventories live guidance; audits, research, sessions, completed plans, forensics, and superseded analyses remain preserved but are not per-task guidance obligations.

## Known Gotchas

2026-08-08 — Release identity appears in package and plugin manifests, the local agent-plugin catalog, the README current-release block, the public security support line, and the Codex `config_version`. Manifest-only parity checks do not catch all release drift.

2026-08-08 — Cross-host tests must unset inherited Codex/Claude/Cursor detection markers before testing path-based inference. Archive-install tests must exercise the working tree rather than committed `HEAD`, and macOS identity checks must normalize duplicate separators plus `/private/var` aliases.

## Key Decisions

2026-08-08 — Silver Bullet's current architecture is standalone and SB-owned. Documents describing GSD as lifecycle authority are historical unless explicitly labeled as comparisons of optional usage.

## Recurring Patterns

2026-08-08 — Preserve point-in-time records verbatim and add a prominent superseded header with links to current authorities instead of rewriting history.

## Open Questions

*(none)*

> To add: append `YYYY-MM-DD — <note>` under the relevant category.
> To resolve an open question: append `[RESOLVED YYYY-MM-DD]: <resolution>` below the original.
