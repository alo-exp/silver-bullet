# GSD Legacy Alias Sunset — Scheduled 2026-09-01

**Status:** BLOCKED removal until sunset date (see `.planning/REDUNDANCY-AUDIT.md` §8).

Silver Bullet still ships backward-compatible `gsd-*` → `silver-*` normalization in `hooks/lib/legacy-skill-alias.sh` and retains 19 `tests/skill-scenarios/gsd-*` scenarios until **2026-09-01**.

## Do not remove before sunset

| Asset | Reason |
|-------|--------|
| `hooks/lib/legacy-skill-alias.sh` | Existing projects invoke `gsd:*` routes |
| `tests/skill-scenarios/gsd-*` (19 files) | Regression coverage for alias normalization |
| `silver-orient` skill | Records `gsd-scan` / `gsd-map-codebase` virtual markers — deprecated; use `silver-scan` |

## Enforcement

- `SB_LEGACY_ALIAS_SUNSET_DATE="2026-09-01"` in `legacy-skill-alias.sh`
- CI: `tests/scripts/test-legacy-alias-sunset.sh` asserts date and pre-sunset alias behavior

## Post-sunset (Phase 3 breaking)

1. Remove `legacy-skill-alias.sh` mappings (or no-op with warning)
2. Point `gsd-scan` directly to `silver-scan` instead of `silver-orient`
3. Retire `gsd-*` skill scenarios
4. Remove `silver-orient` skill file
