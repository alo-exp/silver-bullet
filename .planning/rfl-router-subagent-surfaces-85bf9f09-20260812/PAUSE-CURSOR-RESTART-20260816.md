# RFL ladder 3 — PAUSED for Cursor restart (2026-08-16)

**Status:** paused. Do not launch rungs until the user pings this chat after Cursor restart.

**Frozen SHA-256 (both plan copies):** `db8cec806a33cfea991316c3ba9034281a229efe7424194625a51ce9527c06ed`

**On resume (user-directed):** launch **Opus 5 High** as a **Cursor Task** (`sb-opus-5-high` / Claude Opus 5 thinking High — host slug the Task tool accepts). REVIEW ONLY. Stay on `main`. Do not start Extra High or Max until High returns CLEAN (or ACCEPT then Extra High).

**Why Cursor:** Claude CLI monthly spend limit QUOTA-ABORT on `claude --print`. User explicitly overrode “no Cursor Task substitute” for this High relaunch only.

**Do not:** git checkout/switch; commit; start Extra High/Max; relaunch Claude CLI High unless the user says the spend cap is raised.
