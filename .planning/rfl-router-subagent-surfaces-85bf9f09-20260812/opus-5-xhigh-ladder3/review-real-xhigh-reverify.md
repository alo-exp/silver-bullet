# RFL Ladder 3 — Opus 5 Extra High — RE-VERIFY REVIEW ONLY (real xhigh; saved by ACCEPT worker)

**Reviewer:** Opus 5 Extra High ([`sb-opus-5-xhigh`](8b1c0e9f-6bd4-413c-a0ae-2016dd5fd31b); not delegated; no Fast; Max not started)
**Date:** 2026-08-16
**Branch:** `main` (no switch; this file saved by the ACCEPT worker after parent verified the findings)
**Read order:** [SB-PRODUCT-OVERVIEW-FOR-REVIEWERS.md](../SB-PRODUCT-OVERVIEW-FOR-REVIEWERS.md) → [router_subagent_surfaces_85bf9f09.plan.md](../../router_subagent_surfaces_85bf9f09.plan.md) → [router_subagent_surfaces_85bf9f09-CLARIFY-260717-143757.md](../../router_subagent_surfaces_85bf9f09-CLARIFY-260717-143757.md)
**Frozen SHA-256 at review time (both copies matched):** `d9d452eee1d4ed2307cdc9c195d42afaf03889ab18923e52cc48ca8e72bdd172`
**Extra High ACCEPT SHA-256 (H-1–H-5 / M-1–M-4 landed; both copies matched before Round-18):** `3b980a2006e8c05448ed02a38e879d6e47cfa2bec8e20ecfb66cb3e772e15fcc`
**Round-18 ACCEPT SHA-256 (output-compliance lock; both copies matched):** `65fde3d6f4521e7a8aed89ceb07efc6bcfdbf1b5518e3d1ce0e921c43ac7fd8c`

Prior Extra High remains in [`review-real-xhigh.md`](review-real-xhigh.md). Body below is the reviewer's verbatim Extra High re-verify output (not invented).

SHA verified before review: both copies matched `d9d452ee…`, so the spec was frozen throughout.

```
VERDICT: NOT CLEAN
```

Hash gate passed before review: both copies of [`router_subagent_surfaces_85bf9f09.plan.md`](/Users/shafqat/projects/silver-bullet/repo/.planning/router_subagent_surfaces_85bf9f09.plan.md) and [`~/.cursor/plans/router_subagent_surfaces_85bf9f09.plan.md`](/Users/shafqat/.cursor/plans/router_subagent_surfaces_85bf9f09.plan.md) are byte-identical at `d9d452eee1d4ed2307cdc9c195d42afaf03889ab18923e52cc48ca8e72bdd172` = frozen SHA. Stayed on `main`.

**Prior Extra High findings: all ten landed.** H-1 row 35 dashboard-only/non-gating (row 35 + identity fixture in `VAL/TST-RFL-621`); H-2 `.sb/` present in all 14 ledger-omit enumerations (lines 3, 15, 48×2, 96, 453, 475, 530, 578, 624, 723, 756, 782, 846) including the row-33 discriminator; H-3 FAST has no Job step-1 pre-read node; H-4 `blocked_fast_leaf` terminal at row 36; H-5 `AF-agent-delegate` / `sb:agent-wrap` in WS1 APO. M-1–M-5 all landed (the only "amends" string left is inside the round-16 revision-history cell at line 80, in the same cell as its own M-1 correction — acceptable history, not residue).

## Blockers

None.

## Highs

**H-1 — `/sb:agent-*` prose still mints the wrapping Workflow at the Orchestrator (line 535 vs line 118 / row 39).** Line 118 is correct post-round-16: "Cold invoke: Orchestrator does **not** compose a user Work Plan and does **not** invent a WF; it **dispatches** wrapping catalog AF `AF-agent-delegate` (WF id `sb:agent-wrap` already in catalog)". Line 535 still reads: "A cold `/sb:agent-*` invoke with no Process in flight **mints a Process wrap (same as `/sb`) that always creates wrapping Workflow `sb:agent-wrap`**… when a Process is already in flight, a direct user `/sb:agent-*` invoke **mints nested Workflow `sb:agent-wrap` under the current Process**". `wf_mint` is a *named, Authorizer-gated operation* in this plan (rows 37/39/40), so "Orchestrator mints/creates Workflow" in the normative shared-state section is the exact string row 39 exists to block. Two secondary conflicts in the same sentence: line 535 makes Advisor-first (`advisor_planning` → `plan_handed_off`) **unconditional** for cold `/sb:agent-*`, while line 118 makes it conditional ("If the user intent is a Job with no validated plan yet, it enters the Advisor-composed path first") — and a classified-trivial cold `/sb:agent-*` Q&A must skip Advisor entirely per the FAST carve-out. Reword 535 to "dispatches the already-existing catalog wrapping WF" and make the Advisor clause conditional.

**H-2 — HINST-01 B4 asserts parent-proxy is "only" at `remaining_depth` 0, contradicting the canonical rule in three places.** Line 400: "Parent-proxy is **only** when host `remaining_depth` is 0 (HNEST-01)". Line 421: "Any in-flight request that needs a **new** projector write… MUST use parent-proxy jsonl + requester yield **even when remaining depth is greater than zero**". Line 425 heading condition: "when remaining depth is 0 and more nesting is needed, **or when an in-flight child needs a new projector write at any remaining depth**". Line 786: "Parent-proxy is specified (for remaining depth 0, **and for any in-flight new projector write at remaining depth > 0**)". The bare "only" is echoed twice more in shorthand (line 730 `VAL/TST-RFL-624`, line 848 HINST-01 traceability). Followed literally on the `/sb:agent-*` spawn path, an in-flight Executor at depth > 0 would nested-Task an agent-* grandchild with no on-disk work-spec — the precise split line 421 fail-closes ("An in-flight child must not nested-Task a grandchild that still lacks an on-disk work-spec"). B4's intent is clear (proxy is not a substitute for a missing install); the wording needs to be scoped to that, e.g. "parent-proxy is not an install substitute; the depth-0 and new-projector-write parent-proxy rules of §Dispatch still apply unchanged."

**H-3 — New rows 41–42 are unreachable under the table's own first-match rule.** Line 620: "Every failure classifies to exactly one canonical `blocked_*` by the **first matching row** of this ordered table." Row 22 `blocked_child_unavailable` still reads "residual required child cannot be launched, **when rows 11–21 do not match**" — an exclusion list never updated for rows 36–42. Both new host rows are launch-prevention conditions: row 41 fires on "`/sb:agent-opencode` or `/sb:agent-pi` spawn when **no** Cursor/Codex/Claude SB install dir exists"; row 42 on "SB install/repair failed **before** `/sb:agent-cursor|codex|claude` spawn". Both match row 22 first, so the round-16 IDs never classify and the operator gets "Relaunch child / refresh capabilities" instead of "Install SB on at least one of Cursor, Codex, or Claude". Row 20 `blocked_unsupported_capability` ("Required capability not proven on the selected adapter") is a second earlier competitor for a present-but-SB-less host. Rows 37 and 40 have the same latent exposure when the refused mint is expressed as a child launch. Fix is the convention the plan already uses elsewhere: extend row 22 to "when rows 11–21 and 36–42 do not match" (and carve row 20 against 41/42).

**H-4 — The round-14/15/16 role gate has no `VAL/TST-RFL` fixture and no traceability ID.** Test IDs stop at `VAL/TST-RFL-624`; the traceability ID column ends `… WBS-01 HNEST-01 HINST-01 GST-01 POA-01 …` with no `WFM`/role-gate entry. So `blocked_wf_mint_unauthorized` (37), `blocked_orchestrator_wf_mint` (39), and `blocked_executor_wf_out_of_plan` (40) — plus the in-plan `wf_mint` happy path and mid-AF-step nested insert — are unowned, while the two same-round host locks each got an ID and both are named in MVP acceptance (line 776). Coverage today is partial and indirect only: line 760 asserts the composition-Val fail returns to Advisor "not Executor and not `/sb` as a mint site", and `VAL/TST-RFL-617` WBS-01 requires "in-plan NWs must appear (omit → `blocked_progress_viz`)". Neither asserts a denial. Given MVP acceptance is ID-driven, the newest structural lock in the plan is unenforceable at acceptance.

**H-5 — Five-tool selected-runtime probe vs the OpenCode/Pi instruction-only path is undecided.** Line 568: five-tool after opt-in is "mandatory on every **selected** runtime (`host_native` and each `sb:agent-*` that is actually selected)", and "**Do not accept Pi or any other runtime into prefs if Graphify and the rest of the opted-in stack cannot run there yet**"; a failing probe means "unselect that runtime… five-tool unselect fail-closes **five-tool gates** for that runtime". B6 (line 417) says the opposite for the same runtimes: OpenCode/Pi "**cannot** natively run hooks, MCP Graphify, recommended-tool gates" yet remain dispatchable because "Parent-proxy **must** persist work-spec, WBS, GST if Job, AM-save, and `graphify update` after their edits". So in a five-tool-opted-in project (SB's own default posture) it is unresolved whether `/sb:agent-opencode` / `/sb:agent-pi` are selectable at all, and if they are, whether proxied Graphify/agentmemory counts as passing probe. No blocker row covers "dispatch to a five-tool-unselected runtime", so the failure mode is unclassified. HINST-01 needs an explicit carve-out: instruction-only runtimes are probe-exempt and satisfied by ancestor proxy, or they are unselectable and row 41's whole path is dead under opt-in.

## Mediums

**M-1 — Depth unit is never stated normatively, and the HNEST table mixes conventions.** Cursor: "**2 Task hops** below the main agent (3 agent layers: main → child → grandchild)". Claude: "**3 subagent layers** below the main conversation". Line 362: "Remaining depth is that configured maximum minus current depth"; line 421: "configured host maximum minus that child's host nesting depth after this spawn". Nothing says whether "configured host maximum" is hops-below-main or layers-including-main, and the Cursor cell supplies both 2 and 3 for the same host. One sentence fixing the unit to hops-below-main (Cursor 2, Claude 3) removes an off-by-one in every `remaining_depth` stamp and in `blocked_depth_unsupported`.

**M-2 — Codex has no defined `remaining_depth` maximum to stamp.** The table says "**No documented official numeric max**… Adapt via `remaining_depth` + parent-proxy only" and forbids writing `max_depth`. But `remaining_depth` is a required integer in the envelope and the spawn-proxy record, computed from a "configured maximum" that does not exist for Codex. Plan should say what Init/Doctor records for Codex (treat as unbounded and rely on host refusal, or a conservative SB-side default), since silence means implementers invent one — which HNEST-01 explicitly forbids ("do not invent a fourth max if undocumented").

**M-3 — Line 112 clause (3) prohibits WF mint by Validator / Authorizer / Mentorship / Ver-loop / Val-loop, but no blocker row classifies it.** Rows 37/40 are Executor-scoped, row 39 is Orchestrator-scoped. Under "exactly one canonical `blocked_*`", a clause-(3) violation — most plausibly the Authorizer, which unlike the deny-all Ver/Val leaves *can* spawn — has no ID. Cheapest fix: widen row 37 from "Executor `wf_mint`" to "any non-Advisor role `wf_mint`… without Authorizer admit", or add a row.

**M-4 — Risks section has no HNEST-01 / HINST-01 entry.** Lines 780–789 cover shared WBS, five-tool, parent-proxy, and Authorizer key isolation, but not the newest and most externally-dependent workstream: vendor doc drift (Cursor's 2-hop limit and Claude's default 3 are product facts that can change under SB), cross-host writes into `~/.codex` / `~/.claude` from a Cursor parent, and the honest fact that instruction-only hosts have no physical rail for the "expected writes/effects" the Authorizer binds at launch (line 429) — B6 contemplates them editing product files with no PreToolUse/PostToolUse gate and no post-hoc reconciliation owner.

**KEEP REJECT — nearly re-raised, deliberately not reopened:** GST rows 34 **and** 35 non-gating with `gst_stale` and Job-continues (verified landed, not a defect); FAST not a Job / not GST / quality-order exemption / classify-not-mint (my H-1 flags only the Advisor-first wording in line 535, not the exemption); Authorizer admits but does not mint (my M-3 asks for a classification ID, not a role change); `sb:agent-wrap` as dispatch envelope and the retracted invent-ban (my H-1 is vocabulary at line 535 only); B1 schema unchanged; `.sb/` enum (landed); board-of-one unifier still launches.

## Required extra section — per-host nested-subagent numeric max, as the plan states

Quoted from the HNEST-01 table (plan lines 368–372); the round-14 clarify table at lines 638–642 agrees on all three numbers.

| Host | Numeric max — plan's words | Config SB writes | Source URLs + doc date/version — plan's words | Residual uncertainty — plan's words |
|---|---|---|---|---|
| **Cursor** | "**2 Task hops** below the main agent (3 agent layers: main → child → grandchild). Since Cursor 2.5 the main agent and its direct subagents can launch subagents; a subagent launched by another subagent cannot launch further ones." | "**None.** No documented `~/.cursor/` settings / CLI / Task flag raises this hard product limit." SB "records the hard max and stamps envelope `remaining_depth` from it (`remaining_depth` is SB's field, not a Cursor-documented setting)." | [cursor.com/docs/subagents](https://cursor.com/docs/subagents) FAQ "Can subagents launch other subagents?"; [cursor.com/docs/agent/subagents](https://cursor.com/docs/agent/subagents); [cursor.com/docs/sdk/typescript](https://cursor.com/docs/sdk/typescript) Nested subagents. "Fetched **2026-08-16**. Cursor **2.5**." | "**No documented writable max-nesting setting.** Adapt via `remaining_depth` + parent-proxy only." |
| **Codex** | "**No documented official numeric max** for nested subagent depth. Official `[agents]` table documents concurrency (`max_concurrent_threads_per_session`), not nesting." | "If `agents.enabled` is unset or `false`, set `agents.enabled = true` in user `~/.codex/config.toml` or project `.codex/config.toml`… **Do not write `max_depth`.**" Schema `max_depth` ("Maximum nesting depth for V1 agent threads. Ignored by V2"; no published default) is "**not** an official product knob — do not write it." | [developers.openai.com/codex/multi-agent](https://developers.openai.com/codex/multi-agent) Global settings; [developers.openai.com/codex/config-file/config-basic](https://developers.openai.com/codex/config-file/config-basic); schema note only: [codex-rs/core/config.schema.json](https://raw.githubusercontent.com/openai/codex/main/codex-rs/core/config.schema.json) `AgentsToml.max_depth`. "Fetched **2026-08-16**." | "**No official nesting-depth knob.** Adapt via `remaining_depth` + parent-proxy only. Concurrent-thread cap is not nesting depth." (Plus M-2: no maximum defined to stamp.) |
| **Claude Code** | "**3 subagent layers** below the main conversation (documented default). A subagent at the third layer cannot spawn further. Set `1` to disable nesting." | "Write `env.CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` = `\"3\"` in user `~/.claude/settings.json` and/or project `.claude/settings.json` if unset or below 3." Must not touch `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`, maxTurns, permissions, plugins, model, telemetry. | [code.claude.com/docs/en/env-vars](https://code.claude.com/docs/en/env-vars) `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` (default 3); [code.claude.com/docs/en/sub-agents](https://code.claude.com/docs/en/sub-agents); [code.claude.com/docs/en/settings](https://code.claude.com/docs/en/settings); [code.claude.com/docs/en/agent-sdk/subagents](https://code.claude.com/docs/en/agent-sdk/subagents). "Fetched **2026-08-16**." | "Docs say the limit can be 'adjusted or disabled' but **do not publish a numeric ceiling above 3**. SB writes the documented default **3**, not an invented unlimited/disable token." |

**Evidence-backed vs guessed:** all three cells carry source URLs, a fetch date, and a version/doc anchor; the one unsourced number that could have been invented (Codex) is explicitly declared absent rather than filled in, and the schema-only `max_depth` is quarantined as "not an official product knob". **No High on missing sources.** Residual risk is vendor drift (M-4) and the unit ambiguity (M-1).

## Hosts / five-tool / control plane / consistency notes

- **MVP scope reconciles correctly.** Line 558: "The MVP SB **host** is Cursor. Claude Code, Codex, and OpenCode are current-product install and `/sb:agent-*` surfaces in this ship; Orchestrator-as-parent adapters for those hosts sequence after MVP", and line 366 permits the Claude `settings.json` env write and Codex `agents.enabled` now as "trivial user-settings patch". HINST-01 shipping cross-host install ensure at MVP while Codex/Claude *parent* adapters are post-MVP is therefore stated, not a contradiction.
- **HINST-01 detect matrix vs spawn path is internally consistent** apart from H-2: present = "CLI **or** config dir" with `${CURSOR_HOME:-$HOME/.cursor}` / `${CODEX_HOME:-$HOME/.codex}` / `${CLAUDE_HOME:-$HOME/.claude}`; absent → `host_absent` warn, parent Job continues; sibling failure warns; parent unrepairable → row 42; B4 verify-then-install-then-spawn with no instruction-bundle fallback for installable hosts. Row 42's recovery text correctly re-states the sibling-vs-parent asymmetry.
- **Instruction-only rails are honestly scoped but one-directional.** B5/B6 name the reference dirs (resolving `current` to the versioned cache), always add `$primary_checkout/silver-bullet.md` / `skills/` / `contracts/`, state "no SB hooks/MCP on this host", and route durable writes through parent-proxy. What is unstated: who Authorizer-admits an OpenCode/Pi in-plan `wf_mint` (B6 says only that "the mint **record** is written by parent-proxy"), and who reconciles the launch-bound "expected writes/effects" for a host with no PostToolUse. See H-5 and M-4.
- **B7/B8 are content-free cross-references.** Line 419: "**B7** is the product-facts lock in B5. **B8** is the role gate: agent-\* nested Executors mint WFs in-plan only; they do not become Advisors." B7 has no body of its own — harmless, but the numbering implies a missing subsection to a reader diffing against B1–B6.
- **Blocker table has no duplicates among the new rows.** 36/37/38/39/40/41/42 are distinct conditions with distinct recoveries, and row 37 vs 40 precedence is deterministic (unauthorized wins over out-of-plan). The only ordering defect is against row 22/20 (H-3).
- **Document integrity holds:** ten frontmatter todos, `.sb/` propagation complete, clarify banner SHA (`d9d452ee…`) matches the plan and both file copies, round-15 correctly self-marks "SHA not frozen", round-16 correctly invalidates `18ac07bb…`.

Files touched: none. No commit, no checkout/switch, no `SetActiveBranch`. agentmemory writes skipped per brief. Graphify query used for orientation; native Read for overview, plan, and clarify rounds 13–16.
