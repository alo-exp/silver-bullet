# RFL Ladder 3 — Opus 5 Extra High — RE-VERIFY REVIEW ONLY (real xhigh; SHA `65fde3d6`; saved by ACCEPT worker)

**Reviewer:** Opus 5 Extra High ([`sb-opus-5-xhigh`](bc5b2af0-21d0-4a36-bd30-1dbd4b71eafc); not delegated; no Fast; Max not started)
**Date:** 2026-08-16
**Branch:** `main` (no switch; this file saved by the ACCEPT worker after parent verified the findings)
**Read order:** [SB-PRODUCT-OVERVIEW-FOR-REVIEWERS.md](../SB-PRODUCT-OVERVIEW-FOR-REVIEWERS.md) → [router_subagent_surfaces_85bf9f09.plan.md](../../router_subagent_surfaces_85bf9f09.plan.md) → [router_subagent_surfaces_85bf9f09-CLARIFY-260717-143757.md](../../router_subagent_surfaces_85bf9f09-CLARIFY-260717-143757.md)
**Frozen SHA-256 at review time (both copies matched):** `65fde3d6f4521e7a8aed89ceb07efc6bcfdbf1b5518e3d1ce0e921c43ac7fd8c`
**Round-19 ACCEPT SHA-256 (H-1–H-2 / M-1–M-2 landed; both copies matched):** `b673de8cc11bb46dfb12ed8bd48fa1d9ecc642f9e4ea4b9047d46b8e9c240c18`

Prior Extra High remains in [`review-real-xhigh.md`](review-real-xhigh.md) and [`review-real-xhigh-reverify.md`](review-real-xhigh-reverify.md). Body below is the reviewer's verbatim Extra High re-verify output (not invented).

SHA verified before review: both copies matched `65fde3d6…c7fd8c`, so the spec was frozen throughout.

```
VERDICT: NOT CLEAN
```

Hash gate: both copies byte-identical at `65fde3d6…c7fd8c`, branch `main`, read-only throughout.

**Landing check (no re-raise).** All ten prior Extra High items are landed: line 537 is dispatch-not-mint with conditional Advisor-first and the FAST skip; B4 (L402/L732/L853) states depth 0 **and** in-flight projector write at depth > 0; row 22 excludes rows 11–21 **and** 36–42 and row 20 carves 41/42/37/40; `VAL/TST-RFL-625` / WFM-01 exists and is named in MVP acceptance (L779) and traceability (L854); OpenCode/Pi are five-tool probe-exempt (L419/L570); hops-below-main is the normative unit (L364); Codex stamps the `unbounded` sentinel; row 37 is any non-Advisor mint with Orchestrator staying at 39; HNEST/HINST risks are in the risks section. The four new Round-18 items are also landed: change surfaces plus golden fixture in WFM-01/625 (L120/L733/L747), the no-AF-only-tree requirement, "authoring session is a Job", and the FAST-or-Job artifact shape. No leftover Orchestrator-mint in the control-plane prose — L118, L122, L251, L537 all read as catalog dispatch. KEEP REJECT items were not re-litigated.

## Highs (new)

**H-1 — Control plane / consistency: the `/silver:new-workflow` authoring session is declared a Job but never given a role contract.** L120 specifies the *emitted artifact* in seven clauses, and the today→after table maps "role-gate today Orchestrator queue-builder mint path" onto "the generated WF's runtime contract is (2)". That resolves the artifact and leaves the session's own composition path — retained as "Create/Convert is an Orchestrator **queue builder** (clarify → scan → plan → RFL → execute → verify → validate → ensure-docs)" — unretired. Five consequences: (a) a Job whose plan is composed by the Orchestrator is row 39 by its own text ("Orchestrator … composes a Work Plan"); (b) L120 says the session "**may** use Advisor internally", while a non-trivial Job mandates Orchestrator work-spec + Advisor compose + composition-Val + plan-time Val (L46/L92/L171) — permissive where the Job contract is mandatory; (c) nothing says the authoring Job mints `original_intent_hash`, takes a GST row, or emits WBS, though the lock makes it a Job; (d) `WF-SILVER-NEW-WORKFLOW`'s own precomposed record stays AF-only while clause (1) requires NW-capable compositions, and the plan never says whether that record is in scope; (e) WFM-01/625's golden fixture covers only the emitted WF, and WS2 lists `.silver-bullet/orchestrator-workers/NEW-WORKFLOW.md` only "so authored examples and launch templates stamp … the role-gate contract", so acceptance proves nothing about the session. This is not the rejected "only invokes Advisor" recast and not a mint-site rename — the gap is a stated role contract for the authoring session, plus whether the authoring WF's own record is in or out of scope.

**H-2 — Hosts / five-tool: rows 41/42 are shadowed by rows 11/12, and the surviving recovery is the one the plan forbids.** L622 makes the table strictly first-match, and Round-17 carved row 20 and row 22 only. Rows 11/12 (`blocked_executor_unavailable` / `blocked_verification_unavailable`, L636/L637) sit earlier with no carve, and "recorded Executor tuple unavailable" is the natural reading of both a `/sb:agent-cursor|codex|claude` tuple whose SB install/repair failed (row 42) and a `/sb:agent-opencode|pi` tuple with no reference install dir (row 41). Row 11's resume via L209 is "closest replacement … if that replacement changes `runtime`, probe the new runtime first" — which performs exactly what row 42 forbids ("Do not fall back to the instruction-bundle path for an installable host"), and because the Round-17 H-5 landing makes OpenCode/Pi probe-exempt (L419/L570), the probe filter cannot reject those candidates. Net effect is a silent downgrade to an instruction-only host instead of surfacing install repair, and `VAL/TST-RFL-624`'s 41/42 fixtures are not provable against a conforming first-match implementation. Fix shape mirrors row 20: carve rows 11/12 so HINST-01 install/reference failures classify 41/42, and exclude instruction-only runtimes from the replacement set for an installable-host tuple.

## Mediums (new)

**M-1 — Hosts / control plane: the Codex `unbounded` sentinel has no consumer semantics.** The sentinel is locked (L364, L373, rows 1 and 4), but every normative parent-proxy trigger is an integer comparison: L362 and L423 ("remaining depth greater than zero", "at remaining depth 0 parent-proxy is mandatory") and L427 ("parent-proxy even at remaining depth > 0"). `unbounded` never satisfies `== 0`, so depth-exhaustion parent-proxy is unreachable on Codex, and the actual mechanism — "Spawn until the host refuses, then parent-proxy" — appears only inside the HNEST host-table cells at L373, with no refusal-detection state, no ledger field, no blocker row other than row 19 (which requires parent-proxy itself to be impossible), and no `VAL/TST-RFL-623` fixture. L6 and L701 also describe `remaining_depth` only as "the launched child's post-launch host-API remaining depth", so nothing obliges consumers to accept a non-integer token. Medium because Codex-as-parent is post-MVP.

**M-2 — Five-tool / consistency: the replacement-ladder "probe" is undefined.** L209 (echoed at L12) gates any runtime switch on "probe the new runtime first; if the probe does not pass, do not switch", but the only probe the plan defines is the five-tool init probe (L570), which is opt-in-conditional and now exempts OpenCode/Pi. When five-tool is not opted in, or when the candidate is instruction-only, the ladder's safety filter has no content. State whether that probe is the five-tool probe, an adapter capability probe (row 20), or the HINST-01 install-ensure pass, and what a probe-exempt runtime must still prove before replacing an installable-host tuple. This is the mechanism that makes H-2 reachable.

Blockers: none. Files touched: none.
