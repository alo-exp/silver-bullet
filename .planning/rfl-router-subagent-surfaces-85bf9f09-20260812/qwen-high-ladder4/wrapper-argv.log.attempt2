wrapper.argv 2026-08-16T02:16:37Z
run --variant high --dir /Users/shafqat/projects/silver-bullet/repo -m opencode-go/qwen3.8-max --auto --auto # RFL architecture review — Qwen 3.8 High (REVIEW ONLY) — ladder 4

This is **ladder 4, Qwen 3.8 High** (same High+ order as ladder 3). Do a full High review from the required files. Do not copy ladder-3 reviews verbatim. **Do not rubber-stamp ladder-3 CLEAN.**

You are OpenCode CLI (`opencode-go/qwen3.8-max`, `--variant high`) running as a **REVIEW-ONLY** worker under `/silver:agent-opencode` / `sb:agent-opencode`. You are **not** a Cursor Task. You are **not** the parent orchestrator. Do not spawn Task subagents. Do not implement. Do not triage. Do not edit the plan copies. Do not commit. Stay on branch `main`.

Put the complete review in your **final assistant message** and then stop. Do not write files. Do not start a workflow queue. The harness captures only your printed final text.

This is the **High** thinking rung (`--variant high` / `reasoningEffort: high`) on live catalog model `opencode-go/qwen3.8-max`. **Not** Fast. **Not** medium. **Not** max effort. Extra High **does not exist** in catalog (`high` / `max` / `minimal`). Skip Medium. Do not start Max. Do not start DeepSeek, MiniMax, Composer, GLM, Gemini, Kimi, Grok, GPT, or Opus. **Not** MiMo. **Not** `ocg-qwen3.7-*`.

## Frozen SHA

Plan SHA-256 (both copies must stay byte-identical; do not edit): `c9511f2daa336ef34f30271348085c885e19903eb8243ddb53832980279aaddf`.

Canonical: `/Users/shafqat/projects/silver-bullet/repo/.planning/router_subagent_surfaces_85bf9f09.plan.md`
Cursor mirror: `/Users/shafqat/.cursor/plans/router_subagent_surfaces_85bf9f09.plan.md`

## Mandatory read order (do this first, in this order)

**Graphify first** before any codebase exploration: `graphify query "<question>"` (not `lctx_graph`). Then:

1. Overview in full:
   `/Users/shafqat/projects/silver-bullet/repo/.planning/rfl-router-subagent-surfaces-85bf9f09-20260812/SB-PRODUCT-OVERVIEW-FOR-REVIEWERS.md`
   Then preamble: `/Users/shafqat/projects/silver-bullet/repo/.planning/rfl-router-subagent-surfaces-85bf9f09-20260812/REVIEW-PROMPT-PREAMBLE.md`

2. Plan (canonical repo copy):
   `/Users/shafqat/projects/silver-bullet/repo/.planning/router_subagent_surfaces_85bf9f09.plan.md`

3. Clarify brief **through round-21** (latest addendum is round-21 Max CLEAN ACCEPT):
   `/Users/shafqat/projects/silver-bullet/repo/.planning/router_subagent_surfaces_85bf9f09-CLARIFY-260717-143757.md`

Do **not** grep the whole tree as a substitute for reading those files.

## KEEP REJECT (do not reopen)

From the latest Clarify Decision Addendum (round-21). Do not amend KEEP / KEEP REJECT. Do not reopen:

- Orchestrator work-spec + Advisor compose (no `/sb` WF mint)
- FAST is **not** a Job (classify-not-mint; not GST)
- GST degrade 34+35
- Public route is `/sb:new-workflow` (not `/silver:new-workflow`)
- Authoring session **is** a Job (Advisor compose; queue-builder retired)
- HINST 3+2
- HNEST: Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3
- `agent-*` in-plan Executor mint
- B1 schema unchanged
- Also unchanged: ESC-02 no A; Authorizer not Approver; `process_v_verified`; BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first; current-month cap; AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier

## Quota

If you hit a usage / spend / rate limit, **stop immediately**. Print a single line `QUOTA` plus the error, then stop. Do not retry. Do not switch models. Do not fall back to Cursor yourself. Parent will Cursor-fallback — **this family has no Cursor slug**. Report that honestly.

## Acceptance criteria

- [ ] Graphify used before codebase exploration
- [ ] Overview → plan → clarify through round-21
- [ ] REVIEW ONLY: zero file edits, zero commits, no branch switch
- [ ] End with exactly one line: `VERDICT: CLEAN` or `VERDICT: NEEDS_FIXES`
- [ ] Findings grouped: **Blockers**, **Highs**, **Mediums** (Lows optional; do not pad)
- [ ] Each finding cites plan/clarify/overview section or obligation IDs
- [ ] Do **not** rubber-stamp ladder-3 CLEAN
- [ ] Do **not** reopen KEEP REJECT
