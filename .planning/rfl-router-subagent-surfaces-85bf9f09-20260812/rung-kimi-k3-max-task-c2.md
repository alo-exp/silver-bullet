# RFL Rung 3 — Kimi K3 Max — Adversarial Architecture Review (Cycle 2)

**Plan under review:** `.planning/router_subagent_surfaces_85bf9f09.plan.md` (HEAD `e5c58009`; working tree clean for plan + brief; 441 lines)
**Model/launch:** `kimi-k3-max` via native Cursor Task, cycle 2 (cycle 1: `rung-kimi-k3-max-task-c1.md` → CLEAN)
**Read order followed:** SB-PRODUCT-OVERVIEW-FOR-REVIEWERS.md → REVIEW-PROMPT-PREAMBLE.md → plan (full, lines 1–441 in four sequential reads) → cycle-1 report (as claims to falsify, not premises) → clarify brief (full, incl. Q12–Q22 appendix, round-2/3/4 addenda, 10 RFL incorporate notes) → independent git/diff/grep cross-checks.
**Stance:** independent re-verification. Every cycle-1 closure assertion was re-derived from current bytes with my own commands; cycle-1's CLEAN was treated as a hypothesis. Fresh adversarial angles beyond c1's enumerated probes were tested (§3 below).

## 1. Mechanical cross-checks (independent, current bytes)

- **Byte-parity lock (Q2):** `diff -q` repo plan vs `~/.cursor/plans/router_subagent_surfaces_85bf9f09.plan.md` → **identical**. `git log` confirms `e5c58009` ("enumerate AF/Workflow validator and A-loop advisor launch edges") is the latest plan commit; `git status --porcelain` on plan + brief is empty.
- **Structural integrity:** exactly 10 frontmatter todo IDs, all `status: pending`; exactly one `## Locked decisions` (line 40); exactly one each of `## 1`–`## 9` (lines 121, 140, 159, 203, 211, 257, 267, 294, 331). Zero `TBD`/`FIXME`/`XXX`/`PLACEHOLDER` hits. Zero `Broker` residue (Q9a rename complete).
- **Blocker enum:** my own `grep -oE 'blocked_[a-z_]+' | sort -u` yields **exactly 28** unique IDs, set-identical to the canonical list at line 253. Non-blocker receipts (`migration_not_activated`, `terminated_iterate_ceiling_reconciled`, `stale_projection_after_seal`, `stale_contract_binding`, `a_loop_not_applicable`, `val_loop_not_applicable`, `kl_post_write_no_insights`) correctly carry no `blocked_` prefix.
- **Traceability:** matrix yields 66 `| [A-Z]` lines minus 1 header = **65 data rows**, no duplicate keys (7 CAT + 18 CORR + 5 PREV + 6 FIX + 5 NEW + 6 CUR + 18 singletons). Validator IDs present: 001–007, 101–118, 201–205, 301–306, 401–405, 501–506, 601–618 (pair shorthand), matching preserve list line 322 exactly; meta evidence 900 + BOOT-RFL-001 outside the matrix per line 412. §8 obligation paragraphs 601–618 all present (lines 304–321), including 612/ALP-01, 613/KLW-01, 614/VLP-01, 615/VALP-01, 616/LPS-01, 617/WBS-01, 618/POA-01. LPS-01/WBS-01/POA-01 anchors (§3/§6/§8/Locked; §1/§6/§9/Locked; §3/§5/§8/Locked) each resolve to real text I read.
- **Retired ceremony:** `verify_1`/`verify_2`/charter-signal/PM-filing appear only inside the line-432 prohibition. All 11 `review-fix-ladder` mentions (lines 33, 45, 62, 134, 138, 298, 337, 419, 421, 423, 431) are hard-cut / migration-mapping / historical-filename / checklist contexts — no surviving public route, alias, or ceremony (Q7).
- **Charter + rungs:** nine exact `fitness_charter` field names verified verbatim at line 68; four canonical rung slot IDs in exact order at line 69 (`verification_medium`, `verification_high`, `planning_validation_medium`, `planning_validation_high`).
- **Migration sextuple (Q17=A):** exact six states in order at lines 82 and 274 (`freeze_new_source → project_pre_freeze_events → seal_drain_watermark → drain_old_epoch → producer_stopped → cutover`); barrier substates explicitly non-reordering (line 82); one-CAS `seal_drain_watermark` immediately-before/after authority boundary (line 280) with Doctor diagnosing any separate revocation prerequisite as corruption (line 328).
- **Ordinary SM (Q18):** all 16 states verified present at line 222: `pre_read_pending`, `poa_draft`/`poa_advisor_review`/`poa_satisfied`, `i_running`/`i_two_clean`, `a_running`/`a_two_clean`, `v_running`/`v_two_clean`/`v_verified`, `val_running`/`val_two_clean`/`val_validated`, `kl_post_write_pending`, `scope_complete`.

## 2. The `e5c58009` launch↔deny fix — independently re-derived

1. **Per-scope validator edges — PRESENT.** §1 line 127 enumerates "Workflow-scope Validation-loop validator" and "AF-scope Validation-loop validator" request edges; §3 line 163 mirrors both and adds "Process-final Validation-loop validator" to the Process launch set. Matches Q21=B / Q22=B (lines 50, 53, 221; brief lines 103–104, 123–124).
2. **A-loop Mentor edges — PRESENT.** "A-loop advisor (Mentor)" for Workflow and "AF/leaf A-loop advisor (Mentor; leaf Step A-gate)" at §1 line 127; mirrored §3 line 163; Q14 leaf-dedup semantics at line 220 (leaf Step A-once satisfies AF A-gate; Step yields at `a_two_clean`; AF owns V→Val→K/L).
3. **Deny-generation fence-out — CLOSED.** §3 line 163 binds the generator: "§6 deny-generation MUST include these edges so generated denies never fence out mandatory control-plane children"; line 164 leaf classes read "Final, triage, advisor (P-loop / A-loop Mentor / on-demand consult), verifier, validator, defect-escalation, and Iterate controllers" — matching Locked line 86.
4. **Trigger edges are real SM states.** The symmetric Authorizer launch rule binds `i_two_clean → a_*`, `v_verified → val_*`, `poa_advisor_review` (lines 127, 163); all three exist verbatim in the locked ordinary SM (line 222). `v_verified` is explicitly V's two-clean terminal (line 222 parenthetical), so the validator-admission edge cannot fire before V two-clean — consistent with "Val never starts before V two-clean" (line 223). No invented transition names; no dangling edge.
5. **Verifier admission needs no additive instruction.** Verifier request edges ("composition verifier", "AF verifier", "final verifier") survive at lines 127/163, and verifiers are deny-all Authorizer-handoff leaves admitted under the universal launch_intent rule (line 169). The fix correctly did not name `a_two_clean → v_*`.

## 3. Fresh adversarial challenges (beyond c1's enumerated probes)

1. **"final-validation" vs "Val two-clean" terminology (strongest fresh probe — resolved non-material).** Lines 65, 75, 247, 272 require "final validation" receipts alongside V-loop receipts, and line 222 binds "scope I/A/V/Val two-clean **+ final-validation** + K/L post-verify receipts" — superficially suggesting a sixth receipt type beyond `val_validated`. Resolution: no sixth gate exists anywhere — the SM (line 222) runs `val_validated → kl_post_write_pending → scope_complete` with nothing between; line 222's own parenthetical ("`val_validated` is Val's two-clean terminal") equates the terminal receipt; line 53's "Process final completion" phrasing shows "final" is scope-position vocabulary. "Final-validation receipt" = the scope's `val_validated` artifact; the line-222 formulation is emphatic redundancy, not a distinct obligation. No state-machine hole, no deadlock path, no locked-decision conflict. Recorded as observation O7; one synonym clause ("final-validation (the `val_validated` receipt)") would remove the redundancy.
2. **Scope-uniformity of P-loop (resolved).** Line 50 locks canonical order "per governing scope (AF, Workflow, and Process)" while line 86 binds "every implementation worker/executor". Composition-owning Workflow/Process executors perform work (launch/join) and are not deny-all control-plane leaves (line 127), so they fall under line 86; deny-all leaves are explicitly exempt (lines 86, 218). The two formulations compose without conflict — P-loop is required wherever an I-loop runs, and I/A/V/Val run at AF/Workflow/Process (lines 220–223). Advisor-launch multiplication across scopes is a cost profile, not an executability hole.
3. **Strongest-executor Advisor deadlock (resolved as locked design).** If a scope executor is already the strongest available model, "no eligible Advisor strictly stronger" yields `blocked_advisor_state` (line 156) — a hard block, never a live skip. This is the deliberate locked fail-closed semantic (Advisor strictly stronger; brief "Recommended defaults"), with defined blocker and profile-refresh wait path. Intentional product behavior, not a gap.
4. **Levels 0–3 repair return omits "Val" by shorthand (resolved).** Line 239 second clause returns repair to owner "I-loop → A-loop → V-loop" without naming Val; brief incorporate note 3 uses the same shorthand. The Val-always-after-V lock (Q21/Q22; lines 52–53, 221, 223) makes re-Val follow mechanically whenever V re-runs, and line 244 invalidates affected final validation on material contract change. No bypass path: line 223's "accepted Val findings return to I (re-A/re-V as needed) before re-Val" governs.
5. **P-loop Advisor admission path (resolved).** Line 127's trigger binding includes "P-loop Advisor at `poa_advisor_review`" under the symmetric Authorizer launch rule; line 86 makes the P-loop Advisor deny-all except Authorizer handoff + review packet; line 85's prompt+work-spec gate applies to every host spawn including Advisor children (line 87 confirms for consults). No unfenced launch path.
6. **Doctor as WBS surface vs inspect-only (resolved).** Line 88 lists "Doctor progress view" as a mandatory WBS surface; lines 255/326 keep Doctor inspect-only. Rendering derived ASCII from durable state is inspection, not mutation. Consistent.
7. **Iterate↔ordinary separation — no circularity (re-derived).** Ordinary completion binds I/A/V/Val two-clean + final-validation + K/L receipts only and never requires Iterate fields (line 222); Iterate baseline consumes ordinary A/V/Val receipts and Iterate evidence can never substitute for them (line 65); Iterate is never auto-launched by Process (line 164) and activates only via `explicit_user` / in-repo hash-bound `critical_policy` (line 63; Q6).
8. **All 10 RFL incorporate notes (brief lines 184–194) live in plan — spot-re-verified:** implementation-only P-loop + leaf exemption (86, 218); migration-only `a_loop_not_applicable`/`val_loop_not_applicable` never satisfying live gates (272); Levels 0–3 repair return I→A→V (239); `local/default/<repo_dir_hash>` fallback (205); KLW write-or-`kl_post_write_no_insights` (223); `contracts/work-spec.schema.json` as Row-1 reviewed source + 612–618 paragraphs (418, 315–321); work-spec immutability per `launch_id` + re-launch (87, 170, 180); control-plane P + recursive I/A/V/Val exemption (86, 218); `<<<SB_LAUNCH_PROMPT>>>` envelope (171–179); leaf Step terminal at `a_two_clean` with AF owning V→Val→K/L (220).
9. **Four-fence consistency (re-derived).** "Four fences" (lines 75, 314, 421) = cancellation/write/effect/callback (line 71). Contract-binding tuple + whole-tuple hash + monotonic generation + `callback_acceptance_fence` + "no member is inferred" (line 70) consistent with CAS-only Authorizer publication (71) and deterministic replacement-activation / repair-rebind / revalidation-cycle identities (72, 249, 74).
10. **Product fit (overview §8):** single `/silver` router + complete Workflow/AF `silver:<route>` catalog, no second Process router; parent-never-implements preserved (control-plane nodes "propose and validate but do not perform work", line 127; Work Skills execute only inside AF); host realism via capability-gated adapter proof (154), `blocked_unsupported_capability` fail-closed paths (182, 190), and the single-string-prompt envelope addressing the real Cursor `Task.prompt` constraint (171–179); RFL hard cut with one-for-one Iterate replacement (45, 62); migration without authority resurrection (209, 291–292); Doctor inspect-only (255, 326–329); single coordinated release with site/release gates matching repo policy (46, 338, 424, 433, 441).

## 4. Non-material observations (recorded, not blocking)

- **O7 (fresh this cycle, strongest):** "final-validation" receipt terminology is redundant with Val two-clean (`val_validated`) at lines 65/75/222/247/272; line 222's parenthetical resolves the equivalence, but one synonym clause would remove the apparent sixth receipt type (see §3.1).
- **O1–O6 (c1's, independently re-confirmed non-material):** O1 line-239 Iterate-only `awaiting_baseline_revalidation` vocabulary in the generic Levels 0–3 subsection — ordinary closure fully specified by the second clause + lines 51/244, Iterate closure by 78/249, and line 222 explicitly disclaims baseline/`authority_status` for ordinary delivery; O2 P-loop/K-L applicability to Iterate rung executors unstated — both readings compose (hooks enforce at write/return time; charter+baseline subsume POA otherwise), and line 54's "execution/iteration" already sweeps rungs into pre-read; O3 I-loop disposition→dirty mapping undefined — backstopped by A/V/Val and receipt audit, no deadlock since V-gating is on two-clean not disposition type; O4 brief line 120 historical Q18 text lacks `p_*` — superseded by brief line 100 appendix + round-4 addendum + plan, brief self-defers (line 90); O5 carry-overs (A-loop knowledge-candidate routing, LPS cross-host JSON canonicalization — harmless within one adapter since retries reuse identical bytes and migration never re-admits in-flight launches across hosts, POA re-satisfaction on pure approach pivot, P-loop Advisor-unavailability mapping to `blocked_advisor_state` vs `blocked_plan_of_action_review` — both fail closed); O6 cosmetic wording variances ("Advisor-owned" gate ownership, §1/§3 Mentor parenthetical asymmetry, floor-wording) — no semantic divergence.
- **O8 (fresh, cosmetic):** plan line 88 marks the blocked marker `[!]` "optional" while brief round-3 line 154 lists it without "optional"; the mandatory element (the viz itself, fail-closed `blocked_progress_viz`) is identical in both.

## 5. Conclusion

Cycle 1's CLEAN was independently re-derived, not rubber-stamped: mirror byte-parity, structural integrity, the 28-blocker enum, 65-row traceability with full validator coverage, retired-ceremony confinement, the migration sextuple, the 16-state ordinary SM, nine charter fields, four rung slots, and the `e5c58009` launch-edge fix all verified from current bytes with my own git/diff/grep passes. My fresh probes beyond c1 surfaced no contradiction, state-machine hole, executability break, or traceability orphan — the strongest (O7 final-validation terminology, §3.2 scope-uniform P-loop, §3.3 strongest-executor Advisor block) resolve to redundancy, composed readings, or locked fail-closed design. Locked decisions Q1–Q22 and all four clarify addenda are faithfully incorporated. Second consecutive Max-ladder CLEAN.

VERDICT: CLEAN
