# RFL Rung 3 — Kimi K3 Max / XHigh — Adversarial Architecture Review (Cycle 1)

**Plan under review:** `.planning/router_subagent_surfaces_85bf9f09.plan.md` (HEAD `e5c58009`; working tree clean for this file; 441 lines)
**Model/launch:** `kimi-k3-max` via native Cursor Task (fresh cycle-1 after two `sb-kimi-k3-xhigh` launches died `resource_exhausted` pre-review — see `rung-kimi-k3-max-task-c1-attempt{1,2}.meta`)
**Read order followed:** SB-PRODUCT-OVERVIEW-FOR-REVIEWERS.md → REVIEW-PROMPT-PREAMBLE.md → plan (full, two passes) → clarify brief (full, incl. round-2/3/4 addenda + 10 RFL incorporate notes) → prior rung reports (Kimi High c1–c3, GLM Max c2, Gemini c3 log) consulted **only** as claims to falsify → independent grep/diff/git cross-checks.
**Stance:** independent Max-effort adversarial pass. Kimi High's two consecutive CLEANs and GLM Max's two CLEANs were treated as hypotheses to falsify, not premises. Every closure assertion was re-derived from current bytes, every mechanical claim re-checked with my own greps, and fresh adversarial angles not enumerated by prior cycles were probed (§3 below). Gemini c3's nine findings were individually re-checked against current bytes and confirmed **stale** (all target pre-fix text: F2→lines 86/218, F3→lines 171–179, F4→line 222, F5→lines 88–101, F6→line 68, F7→line 220, F8→line 421, F9→licensed by notation line 342, F1→lines 87/170/180).

## 1. Launch↔deny coherence (the `e5c58009` fix) — independently re-derived

Cycle-1 (Kimi High) Finding 1 had three sub-gaps; closure re-verified from current bytes without deferring to c2/c3:

1. **AF/Workflow Validation-loop validator edges — PRESENT.** §1 line 127 enumerates "Workflow-scope Validation-loop validator" and "AF-scope Validation-loop validator" request edges; §3 line 163 mirrors them and adds "Process-final Validation-loop validator" to the Process launch set. Matches locked Q21=B/Q22=B (lines 50, 53, 221; brief lines 103–104, 123–124).
2. **AF/leaf-Step A-loop advisor edge — PRESENT.** "AF/leaf A-loop advisor (Mentor; leaf Step A-gate)" at §1 line 127; "(Mentor)" at §3 line 163; Q14 dedup semantics at §5 line 220 (leaf Step A-once satisfies AF A-gate). On-demand `i_running` consults explicitly separated from mandatory Mentors (line 87, §3 line 164).
3. **§6 deny-generation fence-out — CLOSED.** §3 line 163 binds the generator: "§6 deny-generation MUST include these edges so generated denies never fence out mandatory control-plane children"; §3 line 164 leaf classes now read "Final, triage, advisor (P-loop / A-loop Mentor / on-demand consult), verifier, validator, defect-escalation, and Iterate controllers" — matching Locked line 86. The symmetric Authorizer launch rule binds triggers to real SM edges (`i_two_clean → a_*`, `v_verified → val_*`, `poa_advisor_review`); all three exist verbatim in the locked ordinary SM (line 222). No invented transition names; no dangling edge.

Verifier admission needs no additive instruction: verifier request edges ("composition verifier", "AF verifier", "final verifier") were enumerated pre-fix and survive, and verifiers are deny-all Authorizer-handoff leaves (lines 86, 164) admitted under the universal launch_intent rule (line 169). The fix neither weakened nor needed to name `a_two_clean → v_*`.

## 2. Mechanical cross-checks (independent greps, current bytes)

- **Byte-parity lock (Q2; addenda lines 105, 113, 118):** `diff -q` repo plan vs `~/.cursor/plans/router_subagent_surfaces_85bf9f09.plan.md` → **identical**. `git log` confirms `e5c58009` is the latest plan commit; `git status --porcelain` on plan + brief is empty.
- **Structural integrity:** exactly 10 frontmatter todos, all `status: pending`; exactly one `## Locked decisions` (line 40); exactly one each of `## 1`–`## 9` (lines 121, 140, 159, 203, 211, 257, 267, 294, 331). No placeholders/TBD/FIXME. No "Broker" residue; the only "two levels" hit is the explicit unlimited-nesting lock (line 47).
- **Blocker enum:** independent `grep -o 'blocked_[a-z_]*' | sort -u` yields exactly the 28 canonical IDs of line 253; every one resolves to a point-of-use or a line-254 predicate (spot-verified all 28, including `blocked_advisor_state` line 156, `blocked_validation_state` line 254, `blocked_iterate_contract_mapping_unresolved` line 248, `blocked_depth_unsupported` line 200). Non-blocker receipts (`migration_not_activated`, `terminated_iterate_ceiling_reconciled`, `stale_projection_after_seal`, `stale_contract_binding`, `a_loop_not_applicable`, `val_loop_not_applicable`, `kl_post_write_no_insights`) correctly excluded.
- **Traceability:** 65 matrix data rows (7 CAT + 18 CORR + 5 PREV + 6 FIX + 5 NEW + 6 CUR + 18 singletons; GLM c2's "63 keys" was its own arithmetic slip — the plan's set is complete). Validator IDs present: 001–007, 101–118, 201–205, 301–306, 401–405, 501–506, 601–618 (pair shorthand), 900 — exactly matching preserve list line 322. §8 obligation paragraphs 601–618 all present (lines 304–321). LPS-01/WBS-01/POA-01 anchors (§3/§6/§8/Locked; §1/§6/§9/Locked; §3/§5/§8/Locked) all resolve to real text. `VAL-RFL-N` vs `VAL/TST-RFL-N` mixing licensed by the line-342 notation definition.
- **Retired ceremony:** `verify_1`/`verify_2`/charter-signal/PM-filing appear only inside the line-432 prohibition. All 11 `review-fix-ladder`/`Review Fix Ladder` mentions (lines 33, 45, 62, 134, 138, 298, 337, 419, 421, 423, 431) are hard-cut/migration/manifest-filename/checklist contexts — no surviving public route, alias, or ceremony.

## 3. Fresh adversarial challenges (not enumerated by prior cycles)

1. **Levels 0–3 repair closure references an Iterate-only state in the generic subsection (strongest probe — resolved non-material).** Line 239: "Successful repair closes repair authority into `awaiting_baseline_revalidation` **and** returns to the original owner's I-loop → A-loop → V-loop sequence." Levels 0–3 is generic ordinary-delivery machinery (line 61: "not an Iterate Ladder rung"; line 216 `contract_defect` disposition; line 223 "unresolved contract defects block the governing scope"), yet `awaiting_baseline_revalidation` does not exist in the ordinary SM (line 222 explicitly disclaims baseline admission/`authority_status` for ordinary completion). Resolved: the sentence's **second** clause plus line 51 (scope reset) and line 244 (defect invalidates verification → owner I→A→V) fully specify the ordinary path, while lines 78/249 fully specify the Iterate closure — so both contexts are executable and the first clause is simply Iterate-scoped vocabulary in a generic paragraph. No deadlock, no missing edge, no locked-decision conflict. One subordinate clause ("in Iterate, …; in ordinary delivery, repair authority closes and the affected scope resets") would remove the conflation. Recorded as observation O1; not a material hole.
2. **P-loop / K/L gate applicability to Iterate rung executors (resolved non-material).** Line 86 ("every implementation worker/executor") and line 54 ("before execution/iteration") are universal in phrasing, while the Iterate SM and rung-completion evidence (line 247) name no `poa_*` states or K/L receipts. Both readings compose mechanically: if rung executors are covered, the gates enforce at write-time (line 180) and return-time (line 54) via hooks without needing ladder SM states, and the rung's `launch_id`-bound work-spec gives the POA its binding target; if exempt, the reviewed hash-bound charter + baseline admission subsume the POA's function. No contradiction either way; line 54's "execution/iteration" phrasing in fact already sweeps rungs into the K/L pre-read. Under-specification, not a hole — observation O2.
3. **I-loop disposition → clean/dirty semantics unspecified (resolved non-material).** Line 216 lists `accept_fix`/`reject_wont_fix`/`defer`/`needs_verification`/`contract_defect` but never states which dispositions make a round "dirty" for two-clean and Levels 0–3 counting (line 236 counts "completed dirty rounds"). Natural reading (dirty = round produced an accepted fix; reject/defer are receipted non-changes; `needs_verification` defers the check to the independent V-loop, which cannot deadlock because V-gating is on I/A two-clean, not on disposition type) is implementable, and receipted dispositions are audited by the A-loop Mentor, V, and Val backstops, so self-triage gaming fails closed downstream. Observation O3.
4. **Triage pass launcher unnamed (resolved).** Line 163's "exactly one Planning/Validation triage pass" on nonempty V batches does not name its launcher, but triage is a deny-all leaf class (line 164), never fixes (line 223), and is not an implementation executor, so P-loop/recursive-loop obligations do not attach and deny-generation recognizes the class. No fence-out path.
5. **`active` transient in `inactive → active → awaiting_baseline_admission` (resolved).** Line 227 consumes exactly one provenance receipt and lands in `awaiting_baseline_admission` + `provenance_pending_admission`; the consumption is the linearization point, replay is idempotent under the plan's put-if-absent discipline, and line 64 reads as one activation action. GLM c2 probed the same angle; I concur independently — not a hole.
6. **Iterate↔ordinary separation — no circularity.** Ordinary completion binds I/A/V/Val two-clean + final-validation + K/L receipts only and never requires Iterate fields (line 222); Iterate baseline consumes ordinary A/V/Val receipts and Iterate evidence can never substitute for them (line 65); Iterate is never auto-launched by Process (line 164) and activates only via `explicit_user` / in-repo hash-bound `critical_policy` (line 63; Q6).
7. **All 10 RFL incorporate notes (brief lines 184–194) live in the plan:** implementation-only P-loop + leaf exemption (86, 218); migration-only `a_loop_not_applicable`/`val_loop_not_applicable` never satisfying live gates (272); Levels 0–3 repair return I→A→V (239, second clause); `local/default/<repo_dir_hash>` fallback (205); KLW write-or-`kl_post_write_no_insights` (223); `contracts/work-spec.schema.json` Row-1 + 612–618 paragraphs (418, 304–321); work-spec immutability per `launch_id` + re-launch (87, 170, 180); control-plane P + recursive I/A/V/Val exemption (86, 218); `<<<SB_LAUNCH_PROMPT>>>` envelope (171–179); leaf Step terminal at `a_two_clean` with AF owning V→Val→K/L (220).
8. **Migration sextuple and substates re-audited:** six ingress states verbatim at lines 82/274 (Q17=A); barrier substates explicitly non-reordering; `drain_only` scoped inside `drain_old_epoch` (282–283) though absent from the line-82 illustrative parenthetical (non-issue, as Kimi c1 noted); one-CAS `seal_drain_watermark` immediately-before/after authority boundary (280) with Doctor diagnosing any separate revocation prerequisite as corruption (328); cutover's nine substates (290) are intra-`cutover`, not ingress states.
9. **Four-fence consistency:** "four fences" (lines 75, 314, 421) = cancellation/write/effect/callback (line 71). `contract_binding` tuple, whole-tuple hash, monotonic generation + `callback_acceptance_fence`, and "no member is inferred" (line 70) consistent with CAS-only Authorizer publication (line 71) and deterministic replacement-activation / repair-rebind identities (lines 72, 249).
10. **Product fit (overview §8):** single `/silver` router + complete Workflow/AF `silver:<route>` catalog, no second Process router; parent-never-implements preserved with fenced workers and non-recursing deny-all leaves; host realism via capability-gated adapter proof (line 154), ineligibility/`blocked_unsupported_capability` fail-closed paths (182, 190), and the single-string-prompt envelope addressing the real Cursor `Task.prompt` constraint; RFL hard cut with one-for-one Iterate replacement; migration without authority resurrection; Doctor inspect-only (255, 326–329); single coordinated release with site/release gates matching repo policy (338, 424, 441).

## 4. Non-material observations (recorded, not blocking)

- **O1 (strongest):** line 239's repair-closure clause uses Iterate-only `awaiting_baseline_revalidation` in the generic Levels 0–3 subsection; ordinary-delivery closure is fully specified by the sentence's second clause + lines 51/244, but one scoping clause would remove the conflation (see §3.1).
- **O2:** P-loop and K/L post-write applicability to Iterate rung executors is unstated; both readings compose (see §3.2). One clause either way closes it.
- **O3:** I-loop disposition → clean/dirty mapping undefined; backstopped by A/V/Val and receipts (see §3.3).
- **O4 (fourth recording, brief hygiene):** brief line 120 (round-2 historical record, section marked COMPLETE) still shows Q18 as `pre_read_pending → i_* → …` without `p_*`. Authoritative appendix (line 100), round-4 addendum (line 175), canonical order (line 41, fixed in `e5c58009`), and the plan all carry the correct SM; the brief self-defers to the plan (line 90). One parenthetical closes it; not a plan defect.
- **O5 (carry-over, re-verified still non-material):** Kimi c1's residuals — A-loop `knowledge/learning candidates` disposition routing undefined (line 220); LPS cross-host hash equality lacks a JSON canonicalization rule (line 179; harmless within one adapter since retries reuse identical bytes); POA re-satisfaction trigger on a pure approach pivot undefined (work-spec immutability covers scope/outcome change only); P-loop Advisor-unavailability classification plausibly maps to either `blocked_advisor_state` (line 156's "no eligible Advisor strictly stronger" reads naturally onto P-loop) or `blocked_plan_of_action_review` — both fail closed.
- **O6 (cosmetic):** line 218 "P-loop is Advisor-owned" vs line 86's executor-drafts mechanics — "owned" reads as gate ownership (Advisor satisfaction terminates the loop), parallel to A-loop usage; §1's "(Mentor; leaf Step A-gate)" vs §3's "(Mentor)" asymmetry with full Q14 semantics at line 220; GLM c2's "Minimum … boundaries" floor-wording. No semantic divergence anywhere.

## 5. Conclusion

The `e5c58009` launch-edge fix holds on independent re-derivation (per-scope validator/Mentor edges enumerated, symmetric Authorizer launch rule bound to real SM edges, §6 deny-generation instructed, leaf classes complete); mirror byte-parity holds; blocker enum, traceability, migration sextuple, authority axis, and structural integrity all re-verified from current bytes with my own greps. My fresh probes beyond prior cycles surfaced no contradiction, state-machine hole, executability break, or traceability orphan — the two strongest (O1 repair-closure vocabulary, O2 rung-scope P-loop/KL applicability) resolve to non-material under-specifications with complete executable paths in both readings. Gemini c3's findings are confirmed stale against current bytes. This is an independent Max-effort CLEAN, not a deferral.

VERDICT: CLEAN
