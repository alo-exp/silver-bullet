# Security Review — v0.26.0

## Scope

Files reviewed (all security-touching changes in v0.26.0):
- `hooks/spec-session-record.sh` — YAML frontmatter extraction → JSON output
- `hooks/uat-gate.sh` — version string extraction → deny/allow gate
- `hooks/roadmap-freshness.sh` — ROADMAP.md content → block message
- `hooks/session-log-init.sh` — process management, file locking
- `hooks/dev-cycle-check.sh` — bash command validation, allowlist checks
- `hooks/timeout-check.sh` — command parsing
- `.github/workflows/ci.yml` — CI hardening (new jq assertion steps)

## Findings

| ID | File | Issue | Severity | Status |
|----|------|-------|----------|--------|
| SEC-01 | spec-session-record.sh | `spec_version` and `jira_id` extracted from SPEC.md without allowlist validation before JSON interpolation | HIGH | ✅ FIXED — allowlist regex `^[0-9]+(\.[0-9]+)*$` and `^[A-Z][A-Z0-9_]*-[0-9]+$` added; jq-mediated output encoding applied |
| SEC-02 | uat-gate.sh | `uat_version` and `spec_version` extracted from YAML frontmatter without allowlist validation before use in block message | HIGH | ✅ FIXED — allowlist regex `^[0-9]+(\.[0-9]+)*$` applied to both; invalid values cleared to `""` (safely skips check) |
| SEC-03 | roadmap-freshness.sh | `phase_title` from raw ROADMAP.md content used in block message without stripping control chars | HIGH | ✅ FIXED — `tr -dc 'a-zA-Z0-9 .:,_-'` strip applied before message construction |

All findings identified by SENTINEL v2.3 adversarial audit (Round 1). Rounds 2 and 3 both CLEAR.

## Security Controls Verified

| Control | Evidence |
|---------|----------|
| Input validation (allowlist-based) | All 3 H-1/H-2/H-3 fixes confirmed present in files |
| JSON output encoding | `jq -Rs '.'` used for all deny/advisory messages in modified hooks |
| ERR trap (fail-open) | All 6 hooks confirm `trap '...' ERR` — no hook can block Claude on unexpected failure |
| Symlink write guard | `sb_guard_nofollow` confirmed in spec-session-record.sh (5 refs) and session-log-init.sh (14 refs) |
| No hardcoded secrets | Grep confirmed: only `old_token` UUID variable (part of TOCTOU fix) — not a credential |
| CI security | jq for safe JSON parsing; shellcheck enforced; no environment secrets exposed in logs |
| SENTINEL adversarial audit | Round 2: CLEAR, Round 3: CLEAR — 0 High/Critical findings confirmed |

## Gate: PASS

All CRITICAL/HIGH findings resolved. No new vulnerabilities introduced by v0.26.0 changes. Security posture confirmed by SENTINEL v2.3 × 2 consecutive clean passes.
