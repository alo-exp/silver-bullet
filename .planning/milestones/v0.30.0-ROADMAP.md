# Roadmap: v0.30.0 — Open-Issue Sweep

Five phases, atomic commits per issue.

## Phase 76: Stop / completion-audit hook bug-fix bundle (#85, #86, #87, #88)

Four regression-tested atomic fixes. Each plan: red test → green fix → atomic commit → close issue.

- **Plan 76.1** — `skipped` terminal status (#86) — `hooks/lib/workflow-utils.sh` + 3 inline fallbacks.
- **Plan 76.2** — HOOK-14 transient-artifact allowlist (#88) — `hooks/stop-check.sh`, configurable via `.silver-bullet.json`.
- **Plan 76.3** — SessionStart benign-event safety (#87) — `hooks/session-start`: skip mutations on `compact`, defensive empty-branch handling, write-verify.
- **Plan 76.4** — Stop hook scope-appropriate gating (#85) — `hooks/stop-check.sh` uses `required_planning` not `required_deploy`.

## Phase 77: Release/SDK gating audit (#48, #50, #71)

- **Plan 77.1 (#50)** — Hook `silver-create-release` skill behind explicit code-review state check; require Layers A/B/C of FLOW 9 to have produced "review-loop-pass" markers before allowing the release skill to run. Surface in `silver-create-release/SKILL.md` Step 0.
- **Plan 77.2 (#48)** — Document the Agent-SDK / claude.ai/code limitation in `silver-bullet.md` §11 (a new "Runtime Compatibility" section). Add a runtime-detection probe in `silver-init` that warns when hooks won't fire (no Claude Code CLI hooks API available). Cannot make hooks fire in environments that don't support them — document + warn is the actionable bound.
- **Plan 77.3 (#71)** — Stop hook false-positive audit. Catalogue every code path in `stop-check.sh` and `completion-audit.sh` that can return `block` and verify each has a clear escape hatch. Land any narrow fixes inline. Producing artifact: `docs/internal/stop-hook-fp-audit-v0.30.md`.

## Phase 78: `silver:init` UX & boundary verification (#64, #69, #72)

- **Plan 78.1 (#64)** — `silver:init` instructions explicitly invoke `claude --dangerously-skip-permissions` notice so the user knows the re-prompt is a Claude Code limitation, not SB. Add explainer to skill body.
- **Plan 78.2 (#69)** — `silver:init` interactive conflict resolution. When CLAUDE.md exists, present three options (merge / overwrite / abort) with diff preview. Lands as additional Step 4a in `skills/silver-init/SKILL.md`.
- **Plan 78.3 (#72)** — Add a PostToolUse/Skill-tracker that records when `verification-before-completion` was last invoked. New hook `boundary-verify-check.sh` warns (informational, non-blocking) when intermediate `git push` happens and verification hasn't been run since the last code-mutating skill.

## Phase 79: Plant seeds for design-only items (#67, #68, #75)

These are roadmap-scale design problems. Filing as seeds in `.planning/seeds/` per SB convention so they surface at the right future milestone.

- **Plan 79.1** — `SEED-001-skill-gap-check.md` (#68) — trigger: when SB adds a hook that depends on a not-yet-installed skill.
- **Plan 79.2** — `SEED-002-sdlc-coverage-roadmap.md` (#67) — trigger: when starting a v0.40+ planning session focused on enforcement coverage.
- **Plan 79.3** — `SEED-003-path-layer-parallelism.md` (#75) — trigger: when a future milestone surfaces FLOW execution latency as a measured pain point.

Issues stay open with seed reference; closed only when the seed is converted to a phase.

## Phase 80: Documentation sweep (#59, #70, #73, #74)

- **Plan 80.1 (#59)** — Template numbering fix. Re-number §10a–§10e as §10.1–§10.5 (or fold under §10 with sub-bullets) in `templates/silver-bullet.md.base` and live `silver-bullet.md`.
- **Plan 80.2 (#73)** — Create `docs/gsd-vs-silver-bullet.md` — when to use what, dependency direction, install matrix.
- **Plan 80.3 (#74)** — Add SB-only install section to `README.md` and the homepage (`docs/index.md` if present, else README equivalent), explicit "without GSD" footprint.
- **Plan 80.4 (#70)** — Cross-surface docs audit pass: README, CONTRIBUTING, help center, homepage. Verify version numbers + skill counts current. (Scope-bounded: surface a punch-list, fix anything mechanical, file follow-ups for editorial calls.)

## Success criteria

- All 17 issues either closed or downgraded to seed-with-issue-ref.
- Test suite green.
- Pre-release-quality-gate Stage 1–4 each clean for two consecutive rounds.
- v0.30.0 tagged + released.
