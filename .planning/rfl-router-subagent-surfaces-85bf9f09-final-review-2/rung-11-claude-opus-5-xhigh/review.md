# Pi claude/claude-opus-5-xhigh

RFL round 2 — rung 11/11 (`final-review-2`), last named reviewer. Phase: **REVIEW-ONLY** (no APPLY, no triage, no Policy C, no verify, no product implementation, no model remap). Named model kept: `claude/claude-opus-5-xhigh` via `/silver:agent-pi` (OmniRoute).

## 0. Freeze gate (independent `hashlib.sha256`, run at review start and again immediately before writing this file)

| # | Copy | SHA-256 | Bytes | Result |
|---|------|---------|-------|--------|
| 1 | `/Users/shafqat/projects/silver-bullet/repo/.planning/router_subagent_surfaces_85bf9f09.plan.md` | `1e3c9866d47de094ba8815fadac95c9cc4ff47062cc0fa9c9a6b24326ed27b13` | 653189 | **MATCH** |
| 2 | `/Users/shafqat/.cursor/plans/router_subagent_surfaces_85bf9f09.plan.md` | `1e3c9866d47de094ba8815fadac95c9cc4ff47062cc0fa9c9a6b24326ed27b13` | 653189 | **MATCH** |
| 3 | `git show HEAD:.planning/router_subagent_surfaces_85bf9f09.plan.md` | `1e3c9866d47de094ba8815fadac95c9cc4ff47062cc0fa9c9a6b24326ed27b13` | 653189 | **MATCH** |

All three copies byte-identical to the expected post-`783e9fce` value `1e3c9866…` / **653189**. No split. Git blob OID `024d305b39a53a9c4e92725897b75a3c1e505a70`. No freeze copy was edited, written, restored, or checked out by this rung. `review-grok-substitute.md` in this rung folder is untouched.

**HEAD observation (non-blocking, recorded not filed):** the brief names HEAD `81560474`. Live HEAD is `2562d11c` (`memory: auto-snapshot 2026-08-28T13:50:15Z`), a descendant of `81560474` → `783e9fce` → `49fa0423`. The snapshot commit does **not** touch the freeze path; the freeze blob at HEAD is still `1e3c9866…` / 653189. Lineage is forward-only, not a rollback. Reviewed the **live working-tree** freeze as instructed. Obsolete SHAs `088a18a6…`/648963, `63680e37…`, `e48a524b…`/644327, `564c94ab…`/646464 were not read.

Freeze read method: all quotes and line cites below come from a `sed`/Python dump of the hashlib-verified on-disk bytes (4404 lines, 327 headings), not from a compressed Read. No compression markers are reproduced here.

---

## 1. Bird's-eye: is this a shippable process spec?

**Overall: yes, with two MED specification defects, both residue of the panel public-name purge.** The document is a coherent, dense, self-consistent process freeze. Structure holds end-to-end: frontmatter (35 todos, all `pending`) → How-to-read → Glossary → TOC (175 entries) → §1 Document control → §2 PRD incl. §2.7 LS-* MUST catalog → §3 Analysis incl. §3.3 KEEP REJECT → §4 Architecture (§4.0–§4.8) → §5 Design (§5.1 behaviors + 43 failure rows, §5.2 ship sequence, §5.3 WS0–WS8, §5.4 coverage) → §6 Risks/rollout/Q1–Q3 → §7 Appendices A–F.

**TOC walk (mechanical, GFM single-hyphen slug rule as locked).** 175 TOC entries; every TOC anchor resolves to **exactly one** heading, and every TOC indent depth matches that heading's level (`- ` → `##`, 2-space → `###`, 4-space → `####`). Across the whole file there are **295 internal anchor references (197 distinct)** and **0 unresolved** under strip-punctuation-then-single-hyphen. `ws0--ws0b` occurrence count = **0** (GFM lock intact; the required `#52-ship-sequence-ws0-ws0b-ws17-ws8-docs-release` single-hyphen slug is used at 4 call sites and resolves). No `--` demand is made anywhere in this review; L1-style GFM `--` findings are correctly out of scope.

**Heading hygiene.** Exactly one `#` title (L126), one `## How to read this document` (L130), one `## Table of contents` (L172), matching Appendix F's integrity checklist (L4396–L4404). No truncated headings; the two longest (L4077, L4084) are long but complete sentences with resolving anchors. Mermaid blocks: **1** (L1498–L1556), matching L1558/L1698/L2171 which all assert a single Process quality-order mermaid and no second copy — consistent with Appendix F "no duplicate mermaid block".

**YAML todos vs claimed ship.** 35 ids, all `status: pending` (35 in-frontmatter occurrences; the 36th match at L4270 is the prose restatement "All 35 YAML todos remain `status: pending`"). Appendix B maps **all 35 → named test → WS → Part**, with zero missing and zero extra (set-equal against frontmatter). Appendix F's "exactly 35 YAML todos (all `pending`)" holds. Nothing is marked completed anywhere, matching "planning freeze only — do not execute WS0" (L128) and L4270 "None may be marked completed in this planning rewrite." No claimed ship contradicts a pending todo.

**Ship sequence.** §5.2 heading (L3349) is `WS0 → WS0b → WS1–7 → WS8 → docs-release` with **no** `ap10-partial-emit` in the heading — rung-10 N1 REJECT-as-wrong is respected and is **not** re-filed. Body (L3353, L3369–L3374), `LS-ship-sequence` (L650–L662), WS8 (L3828), Appendix B ap10 row (L4268), and frontmatter overview all place `ap10-partial-emit` **after** docs-release, as install/bootstrap generators + WS7 docs, "not a new numbered WS", "not Part A". Part A / Part B split is stated in LS-ship-sequence, §5.2 Part A/B, §5.3 preamble, WS1/WS2/WS3/WS4 headers, and every Appendix B `Part` cell. Internally consistent.

**Workstreams WS0–WS8.** All nine present as `###` headings with matching TOC entries; each names its red tests and its regenerate command; WS0/WS0b carry the KR-ws0-preserve-evidence lock; WS8 → docs-release → ap10 chain closes. WS ownership boundaries are restated consistently (Q2: WS1 emit / WS4 runtime / WS7 docs — L4193–L4198, L3552, L3685, L3795).

**Failure-mode rows.** The table (L2999–L3040) is contiguous **1…43**, one blocker per row, with `blocked_panel_end` as row 43. The "historical IDs" preamble (L2994) enumerates the non-classifying/scoped rows including row 43 as panel-end-scoped. Row 22's exclusion band was updated to "rows 11–21 **and 36–43**" (L3186) — correctly widened for row 43. Every row 1–43 has a `####` detail block. (Charter says "rows 1–42"; the live freeze has 43 and is internally consistent — recorded, not filed.)

**Appendix D.** The 44-row public surface inventory is **byte-identical** (as a set and line-for-line) to the §2.3 inventory (L467–L510 vs L4340–L4383). No drift between the two catalogs — this is the strongest structural signal that the rename purge was applied uniformly.

**Q1–Q3.** L4174–L4206: closed, decided, "Do not reopen", with the single named Q1 amendment to `KR-fast-overlay`. No A/B/C is reopened anywhere; §6 has no new open decisions; Deferred post-MVP (L4211–L4218) is explicitly "not this-ship MUSTs", not unresolved rejects.

**Control-plane roles.** Six roles / five preference keys / Authorizer-not-a-key are stated in §4.1 (L1131–L1141) and restated in FR-03, NFR-03, glossary, KR-authorizer-not-pref, and every Board passage. Board of Advisors (ABU-01), generalized Boards on the other four keys, unifier-leaf-for-Board-of-one, and "Authorizer MUST NOT gain a preference key or Board" (L1295) are consistent.

---

## 2. Ant's-eye: mandated surfaces, PASS/FAIL

### 1. Executor Trivial / Regular / Complex — **PASS** (with L3)
Definitions L1170 (`**Trivial** … → FAST path (classified-trivial, **not a Job**, `/sb:fast` required). **Regular** (moderate) and **Complex** (high) are Job Executor thinking-levels`); dispatch/classification contract L1171 (`Inputs = user intent, requested route (/sb:fast vs Job), durable-write vs read-only, and file-touch/scope. Uncertainty or mixed signals → **Regular Job** (not FAST)`) with worked examples (read-only Q&A → Trivial FAST; one-file durable edit → Regular Job; multi-file/architecture → Complex Job). Job vs non-Job is unambiguous: Trivial→FAST is not a Job; Regular/Complex are Jobs. Overlap with FAST is carved at L1170, L1333 (`**Executor tier Trivial** maps here`), glossary L148, and YAML `fast-short-quality-order` (L41). Tuple mapping L1199 (`thinking-level` = `effort`; runtime shared unless per-tier) and L1213. **Unspecified default is host built-in — Cursor Grok 4.6 High, explicitly not XHigh** at L1170, L1213, and the runtime table row L1217 (`high` (Grok 4.6 High; not XHigh as unspecified default)`), plus host-defaults table L1306 (`Grok 4.6 High (host_native)`). `Fast remains forbidden unless the user explicitly says Fast` at L743 and L1213. Rung-5's removed "highest available" sentence has **not** returned: the two surviving `highest available` hits (L2681, L2705) are Iterate-Ladder Verifier/Validator rungs, not unspecified-Executor.

### 2. Public panel trio (`KR-panel-public-trio-only`) — **PASS with M1 + M2**
Live public panel surface is exactly `/sb:panel` | `/sb:panel-start` | `/sb:panel-end`, with `/sb:ladder` as the fourth first-class public Job. Verified by full-file token scan: `/sb:fusion` count **0**; no `/sb:parallel` or `/sb:council` appears as a public route — their only 7 occurrences are inside forbidden-token test assertions (L981, L3379, L3552, L3687, L3795, L4304). `/sb:multi-ai-task` appears only as retired/forbidden (LS-retire-multi-ai L768–L781, inventory rows L486/L4359, and "do not invent" guards). Mapping is replace-not-dual-run: `/sb:panel` = one-off fuse-and-done, Consolidator unifies then **ends member sessions** (L488, L746, L748, L759, L4361); `/sb:panel-start` = sitting body, sessions stay live (L749, L490, L4363); `/sb:panel-end` ends the **current live panel-start only**, **no-op after one-off `/sb:panel` with receipt**, **fail-closed without receipt**, does not mint a Job, **not Ladder** (L166 glossary, L489, L750, L3326–L3333, L4362) — four independent sites, all in agreement including the end-twice and partial-shutdown forks. Help line `/sb:panel` is **not** a room; `-start` is present at L166, L356, L749, L2778. Quality-order default remains **Ladder**: L740 (`Inside quality-order, if the user specifies no mode, **Ladder is the default**`), L1293, L2412 (`run **Ladder** (default; do not default to /sb:panel)`), L3722. Compose legality: `ladder XOR panel XOR panel-start`, nested pairings fail-closed, `/sb:fast` not a legal `<route>` (L764, L762, L166). Catalog rows: §2.3 ≡ Appendix D. KEEP REJECT id is `KR-panel-public-trio-only` at L979–L981, referenced from KR-kr-13 (L977), §3.3 compact pointer (L925), Appendix B (L4264), Appendix C (L4304); zero references to the retired `KR-no-public-fusion` id remain. **Two defects remain inside this surface — see M1 and M2.**

### 3. AP 1.0 partial emit — **PASS**
§3.4 feasibility (L1008–L1044) reaches an explicit "**No. Decision: partial — not yet a 1:1 replace**" with a per-surface portability table and a named blast-radius list; §4.8 (L2762–L2778) declares it "**Not a fourth control plane**" and an "optional generated package shape beside the three host adapters" with SoT-vs-generated table and dual-publish compatibility window; §5.2 slice (L3369–L3379) fixes the owner (install/bootstrap generators + WS7 docs), the position (**after docs-release**), rollback, and coverage (`test-ap10-plugin-emit.sh`). YAML `ap10-partial-emit` (L120–L123) and Appendix B (L4268) agree. It is **not** a numbered WS and does not reorder the ship sequence.

### 4. Doctor expansion — **PASS** (with L5)
Post-freeze Doctor scope is WS7-owned and enumerated: L3795 (help/site/route-set assertions), L3796 (Omni **setup + health + diagnosis + troubleshooting/`--fix`**, upstream-doc consultation requirement, opted-out → `disabled`), L3862–L3874 (the long inspect/report enumeration incl. blockers, WBS, receipts, bind-path resolution, OVERRIDE audit log), L2986–L2992 and L3860 (inspect-only scoped to **unrelated** IDE prefs; HNEST-01/HINST-01 writes explicitly permitted — rung-24 carve intact), Appendix D `/sb:doctor` row, YAML `omni-agent-doctor` (L98–L101), Appendix B row (L4262). Doctor is required to state the public trio and to assert the installed catalog/lock route set contains no extra one-off alias / `/sb:parallel` / `/sb:council`.

### 5. KEEP REJECT drift — **PASS with M1**
19 KR-* entries under §3.3, which is declared "the **only canonical KEEP REJECT catalog**" (L923) with the rung-5 **qualification** intact at L925 ("listed in full below as KR-* entries **or as compact pointers**") — the unqualified completeness claim has not returned. The §3.3 compact-pointer line carries the panel lock as `public trio only / no extra one-off alias ([KR-panel-public-trio-only](#kr-panel-public-trio-only))` — the rung-10 APPLY is present, not regressed. No closed lock is reopened; no later prose contradicts a KR (spot-checked: catalog-generated vs WS1 "back-port then regen then `--check`"; projector-exclusive vs `orchestrator-admission.sh` **requests** the projector at L1050/L1626/L3612; row-40 vs rows 37/39 three-fixture split at L4010; L598 no-abandonment vs row-1 limb (b) narrowing at L1608–L1615). `sb:agent-wrap` is FORBIDDEN in the inventory and forbidden again in KR themes; no public `/sb:agent-omni` (L167, L2926, L3367, L3746); OmniRoute routing-only; public `/sb` with no dual `/silver`.

### 6. Q1–Q3 — **PASS**
Locked and decided (L4174–L4206). Q1 FAST redefinition, Q2 workstream owner (A), Q3 deep research. Not reopened as product questions anywhere; the "Clarify decisions (locked)" preamble re-asserts every adjacent lock (no dual `/silver`, no `sb:agent-wrap`, no `/sb:multi-ai-task` alias, evolution general-not-per-user, contribute fail-closed, catalog generated).

### 7. FAST not a Job — **PASS**
`FAST = classified-trivial`, one concept, **not a Job**, `/sb:fast` **required**, short order **Executor → Verifier → Validator** — stated at LS-fast-short-order (L797–L811), KR-fast-overlay (L931–L933), glossary L148, §4.2 FAST-vs-Job (L1333), §4.5 (L1593+, L2436–L2444), Part A (L3357), Q1 (L4180–L4191), autonomous-E2E step 5 (L858), YAML L41. Not GST, no `original_intent_hash`, must not mint a Job WBS. Not a legal compose `<route>` (L762, L166, L148). The rung-8 H1 qualification is intact in the Executor role bullet (L1165: "no Advisor A-loop and no Job Process-final Val; FAST **does** run Executor → Verifier → Validator") — the unqualified "no A/V/Val" wording has **not** returned. FAST reclassify path (durable-edit misclassify → Advisor-composed Job that does hit GST) is stated at every FAST site.

### 8. Catalog / WS ship order — **PASS**
Catalog is **generated** from APO via `scripts/generate-apo-catalog.py` + named builders; no JSON-edit as SoT (KR-catalog-generated L927–L929, §4.2 L1622, WS1 L3428–L3436, WS4 L3687). Ship order `WS0 → WS0b → WS1–7 → WS8 → docs-release then ap10-partial-emit` is stated identically in LS-ship-sequence, §5.2, §5.3, WS8, Appendix B, and the L4270 execution-order sentence.

**Additional audits requested:** YAML todos pending vs claimed — no claim of completion anywhere (PASS). Broken refs — 0 of 295 (PASS). Truncated headings — none (PASS). TOC-GFM `ws0--ws0b` = 0 (PASS). Mermaid count = 1 (PASS). Executor producer for post-Val K/L is **Executor**, with `knowledge_postwrite` explicitly *not* the producer (LS-post-val-kl L784–L795, Orchestrator/Advisor/Executor role bullets, spine step 23, WS4 L3696) (PASS). FAST short order `Executor → Verifier → Validator` (PASS).

---

## 3. Findings

### HIGH — none

No defect in the live freeze prevents an implementer from executing WS0 → WS0b → WS1–7 → WS8 → docs-release → `ap10-partial-emit`, and no locked product decision is contradicted or silently reopened.

### MED

**M1 — `KR-panel-public-trio-only` contains a dangling "Not a live command." whose only available antecedent is now `/sb:panel-start` (L981; mirrored risk at L979–L982).**
Verbatim on-disk bytes, L981:

> `KEEP REJECT — public trio only / no extra one-off alias. Public first-class Jobs are `/sb:ladder`, `/sb:panel`, and `/sb:panel-start` (+ terminator `/sb:panel-end`). `/sb:panel` is one-off fuse-and-done; `/sb:panel-start` is the sitting body. Not a live command. Named tests must **fail** if a pre-panel one-off public alias or …`

Before the panel-rename APPLY this sentence sat in `KR-no-public-fusion`, whose subject (the retired one-off id) was named in the heading and the first clause, so "Not a live command." bound to the retired id. After the purge the subject was removed from both the heading and the opening clause, and the sentence now immediately follows "`/sb:panel-start` is the sitting body." Read left-to-right, the canonical KEEP REJECT for the public-name lock literally asserts that the sitting body is not a live command — the exact inverse of the trio lock it is meant to enforce, and the inverse of L490 / L749 / L4363 / FR-13, which all make `/sb:panel-start` a first-class public Job.

This is a **new contradiction remaining after the rung-10 APPLY / `783e9fce` purge**, not a re-file of the purge itself and not a re-litigation of rung-10 M1/L2/L4/N2. It is MED rather than HIGH because every other site in the freeze (four independent panel-end sites, two inventory tables, FR-13, glossary, §4.8, WS1/WS4/WS7) unambiguously makes the trio live, so an implementer reading more than one site recovers the intent — but this is the *canonical* lock, the one §3.3, KR-kr-13, Appendix B and Appendix C all point at, so it is the site most likely to be read alone. Note the same sentence at L480 and L4353 is **correct** there, because those rows' subject *is* the retired alias.

**M2 — the `KR-panel-public-trio-only` named-test obligation is specified as a denylist against a token the freeze deliberately never names, so `tests/scripts/test-panel-public-trio-only.sh` cannot be written as stated (L981; same defect at L3552, L3687, L3795, L4304).**
The lock requires: "Named tests must **fail** if **a pre-panel one-off public alias** or `/sb:parallel` / `/sb:council` still appear as public routes after regen", and gates ship on it ("Ship is blocked until that map is complete and green"). `/sb:parallel` and `/sb:council` are concrete literals an assertion can grep for. "A pre-panel one-off public alias" is a *description*, not a token — and by design of this very lock the retired id appears nowhere in the freeze (verified: 0 occurrences). The same unnamable-token phrasing propagates to WS1 regen ("Regen MUST NOT emit a pre-panel one-off public alias"), WS4 ("Named tests MUST fail if a pre-panel one-off public alias … remain as public routes"), WS7 Doctor ("the installed catalog/lock route set contains no extra one-off public alias"), and the Appendix C test note.

The gap is one of specification detail, not of product: the freeze already owns the machinery to state this positively (a fixed public route set — trio + `/sb:ladder` + the Appendix D inventory), and stating the assertion as an **exact-route-set / allowlist** check ("the emitted public one-off route set is exactly `/sb:panel`; the public panel surface is exactly the trio") would be implementable without ever naming the retired id and would subsume the `/sb:parallel` / `/sb:council` denylist. As written, an implementer must either invent the retired token (violating the lock) or invent the allowlist unilaterally (unspecified). MED because it sits on a ship-blocking coverage obligation cited from four workstreams plus Appendix B and C. No product fork is proposed and no retired name is reintroduced here.

### LOW

**L1 — verbatim duplicated normative sentence in WS7 (L3795).** The clause `/sb:doctor` and `tests/scripts/test-router-doctor-report.sh` MUST also assert the installed catalog/lock route set contains no extra one-off public alias, `/sb:parallel`, or `/sb:council` id (not help-text-only).` appears **twice, back to back, byte-identical** in the same bullet. Provenance check: 0 occurrences at `49fa0423~1`, 2 occurrences at `49fa0423`, 2 at `783e9fce`, 2 at HEAD — i.e. the duplication was introduced by the rung-10 APPLY and survived the purge. Harmless semantically (a MUST asserted twice is the same MUST) but it is a live editing artifact in a normative WS7 bullet.

**L2 — WS7 (L3795) and its Appendix E mirror (L4390) have drifted apart.** Appendix E reproduces the WS7 "Update `scripts/sb-doctor.sh`, …" bullet, but the App E copy carries **only** the "Help/`/sb:doctor` MUST state the public trio …" sentence and lacks both the catalog/lock route-set assertion MUST (which is the *machine-checkable* half of the lock) and the AP 1.0 partial-emit docs sentence. The AP-sentence divergence pre-dates rung 10; the route-set-assertion divergence is new. Since Appendix E is presented as the traceability restatement of that same WS7 obligation, the two are now unequal restatements of one requirement — a reader working from Appendix E gets a help-text-only obligation, which is precisely what WS7 says is insufficient ("not help-text-only").

**L3 — "thinking-level" is used for two different value spaces (L1170 vs L1199 / L1213).** L1199 fixes `thinking-level` = `effort`, and L1213 fixes the canonical effort domain as `low | medium | high | xhigh` (+ `max` where documented). But L1170 says "**Regular** (moderate) and **Complex** (high) are Job Executor **thinking-levels** (names **Regular** / **Complex**)" — which makes `Regular`/`Complex` members of the thinking-level space, alongside `high`/`xhigh`. The parentheticals `(moderate)` / `(high)` deepen the collision: `(high)` looks like an effort value while `moderate` is not in the canonical domain at all. The rung-8 M1 contract is present and the surrounding text (tiers carry a `{model, thinking-level}`) is recoverable, but the sentence as written invites an implementer to encode `effort: "Regular"`. A one-word repair ("tiers", not "thinking-levels") is derivable from L1199 without reopening product.

**L4 — `/sb:panel-end` issued while a one-off `/sb:panel` is still *in flight* is not enumerated, and is not in the mandated `test-sb-panel-end.sh` fixture list (L750, L489, L3328, L4362).** All four panel-end sites enumerate exactly three empty-current-panel forks: (1) no live panel-start **and** no last-panel receipt → fail-closed; (2) last-panel receipt says last completion was one-off `/sb:panel` → idempotent no-op success; (3) receipt says panel-start already ended → no-op success. An in-flight (not yet completed) one-off `/sb:panel` has produced no last-panel receipt yet — the receipt is minted at "`/sb:panel` completion" per L750 — so it falls into limb (1) and fail-closes as row 43. That is almost certainly the intended behaviour, but the freeze never states it, and the required fixture list ("mint, persist, last-panel fork, unmatched fail-closed, and recovery receipt") has no in-flight-one-off case. Given that `/sb:panel` is a *concurrent multi-member* run, an operator typing `/sb:panel-end` mid-panel is a foreseeable path that a ship-blocking coverage map does not name.

**L5 — the WS7/Doctor route-set assertion is absence-only; nothing asserts *presence* of the trio + `/sb:panel-end` in the installed catalog/lock route set (L3795, L3862–L3874).** WS7 requires Doctor and `test-router-doctor-report.sh` to assert that no extra one-off alias / `/sb:parallel` / `/sb:council` id is present, and requires help text to *state* the trio — but the only positive emission requirement for `/sb:panel-end` lives in WS1 (L3552, L765). A regen that silently drops `/sb:panel-end` from the catalog therefore passes every Doctor assertion the freeze names, while the terminator is the one surface whose absence is unrecoverable at runtime (a live `panel-start` could not be ended). Absence-only checking on a rename-heavy ship is a coverage asymmetry.

### NIT

**N1 — `*(pre-panel one-off public alias)*` occupies an identifier column as prose (L480, L4353).** In both inventory tables the "Surface" column otherwise holds literal ids in backticks; this row holds an italic description, and it sits at the retired id's former alphabetical slot (between `/sb:fast` and `/sb:improve`) rather than at a position the placeholder text would sort to. Deliberate consequence of the purge and harmless; recorded for accuracy of the two tables as machine-readable inventories.

**N2 — L1558 labels the single live mermaid as "The Proposed-architecture mermaid".** The block lives inside §4.2, whose live title is `Process router `/sb`, catalog generation, FAST vs Job` (all six §4.2 label sites verified current; the rung-4 label APPLY has **not** regressed, and the surviving `Proposed architecture` string at L4228 is Appendix A SHA-lineage receipt text, correctly out of scope). L1558 is descriptive prose, not a section reference or a receipt, so it is a legacy diagram label rather than a stale cross-reference. Non-blocking; noted only because it is the one remaining live-prose use of the retired name.

**N3 — pointer-only KR entries with zero inbound anchors.** `KR-kr-18` states its own justification as "Duplicate heading from a prior split; **kept so existing pointers resolve**" (L1002), yet the file contains **0** `(#kr-kr-18)` references. Same zero-inbound condition for `kr-kr-13`, `kr-kr-15`, `kr-kr-17`, `kr-catalog-generated`, `kr-evolution-not-custom`, `kr-off-01-post-mvp`, `kr-l598-no-abandon`, `kr-contribute-fail-closed`, `kr-row-40`. For the canonical entries that is expected (they are destinations, cited by name in prose); for `KR-kr-18` specifically the stated rationale is now self-falsifying.

**N4 — duplicate heading slugs beyond the F-2 HOLD (observation only, no fix requested).** Three slugs repeat: `blocked_corrupt_state-row-1` at L1605 / L2264 / L4058, `blocked_launch_prompt_spec-row-4` at L2207 / L3055, and `blocked_advisor_state-row-14` at L3131 / L3337 — **the last is the intentional F-2 HOLD and is explicitly not filed; neither heading should be deleted.** Under GFM only the first instance of each slug is addressable. Checked the consequence: the sole inbound reference `#blocked_launch_prompt_spec-row-4` (L3607, WS3 `VAL/TST-RFL-626` pointer, annotated "(architecture / LPS-01)") resolves to L2207, which *is* the architecture site — the rung-7 heading APPLY and the rung-8 L2 retarget both land correctly. `blocked_corrupt_state-row-1` has zero inbound references. So the uniform-heading APPLYs of rungs 4/7 are intact and cause no broken navigation; recorded for completeness only.

---

## 4. Regression check against rungs 4–10 (nothing re-filed)

- **Rung 4** — §4.2 current label present at all 6 sites; `blocked_corrupt_state` (row 1) headings at the worktree-merge / remint / specified-risks sites (L1605, L2264, L4058) intact; `blocked_launch_prompt_spec` (row 4) uniform architecture heading (L2207) intact. Not re-filed.
- **Rung 5** — "highest available" for unspecified Executor is gone; Cursor Grok 4.6 High is the unspecified default at three sites; §3.3 qualification present. Not re-filed.
- **Rung 7** — §5.1 row-4 heading is `#### \`blocked_launch_prompt_spec\` (row 4)` (L3055); `VAL/TST-RFL-626` remains a body bullet as expected. Not re-filed.
- **Rung 8** — H1 FAST hop qualification intact (L1165); M1 tier/effort contract present (see L3 for residual wording only); M2 live names audited independently and correct; L2 WS3 pointer retargeted (L3607); L1 GFM `--` **not** re-filed.
- **Rung 9** — CLEAN, nothing reopened.
- **Rung 10** — M1 (KR-kr-13 trio + new `KR-panel-public-trio-only` + §3.3 compact pointer), M2 (panel-end fail-closed/no-op forks), L2 (absorb names), L4 (compose parenthetical incl. Ladder sequential / Panel fuse-and-done / Panel-start sitting cycle at L757), N2 (`/sb:fast` public form) all **present and intact**. N1 correctly **not** re-filed (§5.2 heading has no `ap10-partial-emit`). The panel public-name purge itself is **not** re-filed: leftover public-id count is 0 and the KEEP REJECT id is `KR-panel-public-trio-only`. M1 and M2 above are *residue defects the purge left behind*, i.e. new contradictions/gaps remaining after those edits — explicitly in scope.
- **F-2 HOLD** — duplicate `#### \`blocked_advisor_state\` (row 14)` at L3131 and L3337 observed and **not filed**; no deletion proposed.
- **GFM lock** — `ws0--ws0b` = 0; no double-hyphen slug demanded.

---

## 5. Verdict

Buckets: **HIGH: none. MED: M1, M2. LOW: L1, L2, L3, L4, L5. NIT: N1, N2, N3, N4.**

Two MED findings, both confined to the panel public-name lock introduced/reshaped by the most recent APPLY: a dangling "Not a live command." that now reads against `/sb:panel-start` inside the canonical KEEP REJECT (M1), and a ship-blocking named-test obligation phrased as a denylist against a token the freeze deliberately never names (M2). Everything else — TOC/anchors, YAML-todo ledger, KEEP REJECT catalog, live-spec MUST catalog, control-plane roles, ship sequence, WS0–WS8, failure rows 1–43, Appendix D parity, Q1–Q3, and all eight mandated surfaces — audits clean.

**NOT CLEAN**

*(Review-only. No APPLY, no triage, no ACCEPT/REJECT classification, no Policy C, no freeze edit, no commit, no verify, no model remap. Parent runs Policy C; Verify is Cursor Grok 4.5 High, not this rung. OpenCode rungs 1–3 remain SKIP.)*
