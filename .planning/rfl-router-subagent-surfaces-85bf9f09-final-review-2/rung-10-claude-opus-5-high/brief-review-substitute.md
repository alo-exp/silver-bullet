You are the **hang×2 substitute reviewer** for RFL round-2 rung 10. Named Pi `claude/claude-opus-5-high` hung twice (EXIT 124 hard-timeout 900s then 3600s; stubs only; no `--continue`). You are **native Cursor Task Grok 4.6 High** (`cursor-grok-4.6-high`). Never Extra High / XHigh. Never Fast.

Read and follow [brief-review.md](brief-review.md) charter, locked product, F-2 HOLD, GFM lock, and already-applied notes **except**:

- First line of `./review.md` MUST be exactly: `# Cursor grok-4.6-high (Pi hang×2 substitute)`
- Do **not** pretend you are Claude / Pi.
- Independently reread the **new** freeze SHA `564c94ab56734e7bbb0e49ef009cfcce2edc2edafc5c42835e4ce481dfd114f4` / **646464** bytes. If hashlib of all three copies is not that SHA, STOP.
- Public `/sb:fusion` is **retired** (no alias). Trio: `/sb:ladder` | `/sb:panel` | `/sb:panel-start` (+ `/sb:panel-end`).
- Do not APPLY, Policy C, verify, start rung 11, edit freeze, or switch branches.

Graphify first (`graphify query`). agentmemory `memory_save`. ctx_* for large analysis. Native Read before writing `review.md`. After writing: `graphify update .` then memory_save.

Write `./review.md` ≥2500 bytes, HIGH/MED/LOW/NIT IDs or explicit **none**, CLEAN or NOT CLEAN.
