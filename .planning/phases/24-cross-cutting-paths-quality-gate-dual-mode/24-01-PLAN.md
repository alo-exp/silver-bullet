---
phase: 24-cross-cutting-paths-quality-gate-dual-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/silver-feature/SKILL.md
autonomous: true
requirements:
  - CROSS-01
  - CROSS-02
  - CROSS-03
  - CROSS-04
  - CROSS-05
  - CROSS-06
must_haves:
  truths:
    - "PATH 9 REVIEW section exists with three parallel review layers (A/B/C) plus conditional Layer D, iterating until 2 consecutive clean passes"
    - "PATH 10 SECURE section exists with SENTINEL, gsd-secure-phase, gsd-validate-phase, ai-llm-safety steps"
    - "PATH 12 QUALITY GATE section exists and appears TWICE — pre-plan (after CONTEXT.md) and pre-ship (after PATH 11)"
    - "PATH 14 DEBUG section exists with dynamic insertion semantics and resume-from-interrupted-path logic"
    - "PATH 16 DOCUMENT section replaces Step 16 with full skill chain including gsd-docs-update, engineering:documentation, engineering:tech-debt, episodic-memory"
    - "PATH 17 RELEASE section replaces Step 17 with gsd-audit-uat, gsd-audit-milestone, PATH 15 insertion point, create-release, gsd-complete-milestone"
    - "Old flat steps (9a-9d, 10-12, 13, Step 16, Step 17) are fully removed and replaced by the new PATH sections"
  artifacts:
    - path: "skills/silver-feature/SKILL.md"
      provides: "Cross-cutting path sections for silver-feature workflow"
      contains: "## PATH 9: REVIEW"
  key_links:
    - from: "skills/silver-feature/SKILL.md PATH 12 pre-plan"
      to: "skills/silver-feature/SKILL.md PATH 12 pre-ship"
      via: "Dual placement with mode detection"
      pattern: "PATH 12.*QUALITY GATE"
---

<objective>
Add the 6 cross-cutting path sections (PATH 9, 10, 12, 14, 16, 17) to silver-feature/SKILL.md, replacing the existing flat steps (9a-9d, 10-12, 13, Step 16, Step 17).

Purpose: These paths complete the composable paths architecture in silver-feature — after this, every path 0-17 has a structured section with prerequisites, steps, review cycle, and exit condition.

Output: Updated skills/silver-feature/SKILL.md with all 18 paths represented as structured sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/composable-paths-contracts.md
@.planning/phases/24-cross-cutting-paths-quality-gate-dual-mode/24-CONTEXT.md

<interfaces>
<!-- Established pattern from Phase 22-23 PATH sections in silver-feature/SKILL.md -->
<!-- Each PATH section follows this structure: -->

## PATH N: NAME — Brief description

### Prerequisite Check
PATH X completed: [artifact] must exist. If not, STOP and run PATH X first.
```bash
[verification command]
```

### Steps
1. `skill-name` (Always/As-needed — purpose)
2. ...

### Review Cycle
[If applicable]

### Exit Condition
[What makes this path complete]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace flat review/security/quality-gate steps with PATH 9, 10, 12 sections</name>
  <files>skills/silver-feature/SKILL.md</files>
  <read_first>
    - skills/silver-feature/SKILL.md (full file — understand current structure)
    - docs/composable-paths-contracts.md (PATH 9, 10, 12 contracts)
    - .planning/phases/24-cross-cutting-paths-quality-gate-dual-mode/24-CONTEXT.md (D-01 through D-11)
  </read_first>
  <action>
Replace the flat steps section between PATH 8 and PATH 11 in silver-feature/SKILL.md. Specifically:

1. **Remove** the entire block from `<!-- Review, Security, and Quality Gate steps -->` through `### Step 13: Pre-Ship Quality Gates` (Steps 9a, 9b, 9c, 9d, 10, 11, 12, 13). Also remove the `### Step 3: Pre-Plan Quality Gates` reference if it exists as a flat step.

2. **Add PATH 9: REVIEW** section (per D-01 through D-04):
   - Prerequisite: PATH 7 completed
   - Trigger: always for any composition with PATH 7
   - Three review layers, each independent:
     - Layer A: `gsd-code-review` → `superpowers:receiving-code-review` → `gsd-code-review-fix` (automated)
     - Layer B: `superpowers:requesting-code-review` → `superpowers:receiving-code-review` → `gsd-code-review-fix` (re-review)
     - Layer C: `engineering:code-review` → `superpowers:receiving-code-review` → `gsd-code-review-fix` (engineering)
     - Layer D (As-needed): `gsd-review --multi-ai` → `superpowers:receiving-code-review` → `gsd-code-review-fix` (cross-AI)
   - Each layer runs independently. After all layers complete, the entire cycle iterates until 2 consecutive clean passes across all layers.
   - Produces: REVIEW.md
   - Exit condition: 2 consecutive clean passes across all layers

3. **Add PATH 10: SECURE** section (per D-05 through D-07):
   - Prerequisite: PATH 9 completed
   - Steps: 1. security/SENTINEL (As-needed — AI plugins/skills), 2. gsd-secure-phase (Always), 3. gsd-validate-phase (Always), 4. ai-llm-safety (As-needed — LLM agents/prompts)
   - Produces: SECURITY.md
   - Review cycle: 2 consecutive clean passes
   - Exit condition: Security findings resolved

4. **Add PATH 12: QUALITY GATE** section (per D-08 through D-11):
   - This path appears TWICE in the workflow. Add the PATH 12 section definition ONCE (after PATH 11), but with TWO placement notes:
     - **Pre-plan placement:** Insert a `## PATH 12 (pre-plan): QUALITY GATE` reference BETWEEN PATH 5 and PATH 6 (after PLAN.md exists, before DESIGN CONTRACT). Prerequisite: CONTEXT.md exists. Mode: design-time checklist.
     - **Pre-ship placement:** Insert `## PATH 12 (pre-ship): QUALITY GATE` BETWEEN PATH 11 and PATH 13 (after VERIFY, before SHIP). Prerequisite: PATH 11 completed. Mode: adversarial audit.
   - Steps: quality-gates (9 dimensions) for standard OR devops-quality-gates (7 dimensions) for IaC/infra. Individual dimension deep-dive as-needed.
   - Gate itself is the review — no separate review cycle. All dimensions must pass. Non-skippable.
   - 4-state disambiguation: if PLAN.md does NOT exist → design-time checklist. If PATH 11 completed → adversarial audit.
   - Exit condition: all dimensions pass

Follow the exact pattern established by PATH 0-8 and PATH 11/13 already in the file. Use `## PATH N: NAME` headers, `### Prerequisite Check`, `### Steps`, `### Review Cycle` (if applicable), `### Exit Condition` subsections.

Ensure the PATH ordering in the file follows composition order: PATH 0, 1, 2, 3, 4, 5, **12 pre-plan**, 6, 7, 8, **9**, **10**, 11, **12 pre-ship**, 13.
  </action>
  <verify>
    <automated>grep -c "## PATH 9: REVIEW" skills/silver-feature/SKILL.md && grep -c "## PATH 10: SECURE" skills/silver-feature/SKILL.md && grep -c "PATH 12.*QUALITY GATE" skills/silver-feature/SKILL.md && ! grep -q "Step 9a:" skills/silver-feature/SKILL.md && ! grep -q "Step 10:" skills/silver-feature/SKILL.md && ! grep -q "Step 13:" skills/silver-feature/SKILL.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "## PATH 9: REVIEW" skills/silver-feature/SKILL.md returns 1
    - grep "## PATH 10: SECURE" skills/silver-feature/SKILL.md returns 1
    - grep -c "PATH 12.*QUALITY GATE" skills/silver-feature/SKILL.md returns 2 (pre-plan + pre-ship)
    - grep "Layer A:" skills/silver-feature/SKILL.md returns match (three-layer structure)
    - grep "Layer B:" skills/silver-feature/SKILL.md returns match
    - grep "Layer C:" skills/silver-feature/SKILL.md returns match
    - grep "2 consecutive clean passes" skills/silver-feature/SKILL.md returns matches for PATH 9 and PATH 10
    - grep "Step 9a:" skills/silver-feature/SKILL.md returns NO match (old flat steps removed)
    - grep "Step 10:" skills/silver-feature/SKILL.md returns NO match
    - grep "Step 13:" skills/silver-feature/SKILL.md returns NO match
    - grep "gsd-secure-phase" skills/silver-feature/SKILL.md returns match
    - grep "gsd-validate-phase" skills/silver-feature/SKILL.md returns match
    - grep "design-time checklist" skills/silver-feature/SKILL.md returns match
    - grep "adversarial audit" skills/silver-feature/SKILL.md returns match
  </acceptance_criteria>
  <done>PATH 9, 10, and 12 sections exist with full structure. Old flat steps 9a-9d, 10-12, 13 are removed. PATH 12 appears twice (pre-plan and pre-ship) with 4-state disambiguation.</done>
</task>

<task type="auto">
  <name>Task 2: Add PATH 14 DEBUG, PATH 16 DOCUMENT, PATH 17 RELEASE sections</name>
  <files>skills/silver-feature/SKILL.md</files>
  <read_first>
    - skills/silver-feature/SKILL.md (re-read after Task 1 modifications)
    - docs/composable-paths-contracts.md (PATH 14, 16, 17 contracts)
    - .planning/phases/24-cross-cutting-paths-quality-gate-dual-mode/24-CONTEXT.md (D-12 through D-23)
  </read_first>
  <action>
1. **Add PATH 14: DEBUG** section (per D-12 through D-15):
   - Place it after PATH 13 but clearly marked as a DYNAMIC INSERTION path (not part of normal sequence).
   - Prerequisite: None — inserted on failure at any point
   - Trigger: execution failure, CI red, verification failure, unknown error
   - Steps: 1. superpowers:systematic-debugging (Always), 2. gsd-debug (Always), 3. engineering:debug (As-needed), 4. forensics (As-needed — unknown root cause), 5. gsd-forensics (As-needed — failed GSD workflow), 6. engineering:incident-response (As-needed — production incident)
   - Resume semantics: after PATH 14 completes, execution resumes from the interrupted path. Fix plan validated before re-entering. Fixes route through gsd-execute-phase --gaps-only.
   - Produces: fix plan, root cause analysis
   - Exit condition: root cause identified, fix plan validated

2. **Update PATH 7's failure path** to reference PATH 14:
   - Replace the existing "Failure Path" text in PATH 7 that says "Route to silver:bugfix" with: "If execution fails mid-wave, do NOT mark the phase complete. Insert PATH 14 (DEBUG) dynamically. Return to PATH 7 only after PATH 14 confirms the root cause is resolved and fix plan validated."
   - Remove the "(PATH 14 DEBUG dynamic insertion will be implemented in Phase 24, per D-10.)" placeholder if present.

3. **Replace Step 16 with PATH 16: DOCUMENT** (per D-16 through D-18):
   - Remove existing `## Step 16: Episodic Memory` section
   - Add PATH 16 after PATH 13 (and after PATH 14 definition):
   - Prerequisite: PATH 13 completed
   - Trigger: always post-ship
   - Steps: 1. gsd-docs-update (Always), 2. engineering:documentation (Always), 3. engineering:tech-debt (Always), 4. gsd-milestone-summary (As-needed — milestone narrative), 5. episodic-memory:remembering-conversations (Always), 6. gsd-session-report (As-needed)
   - Produces: updated docs/, session log
   - Exit condition: docs/ updated, session log completed

4. **Replace Step 17 with PATH 17: RELEASE** (per D-19 through D-23):
   - Remove existing `## Step 17: Milestone Completion` section and ALL its sub-steps (17.0, 17.0a, 17.0b, numbered list 1-4)
   - Add PATH 17 after PATH 16:
   - Prerequisite: all phases shipped
   - Trigger: user signals milestone complete or last phase shipped
   - Steps: 1. gsd-audit-uat (Always), 2. gsd-audit-milestone (Always), 3. PATH 15 DESIGN HANDOFF (As-needed — inserted between steps 2 and 4 if milestone has UI phases), 4. gsd-plan-milestone-gaps (As-needed — gaps found), 5. create-release (Always), 6. gsd-complete-milestone (Always)
   - Review cycle: cross-artifact review → artifact-review-assessor → fix → pass (before create-release). Gap closure: Claude-suggested, user-decided depth.
   - Exit condition: GitHub Release created, milestone archived
   - Per D-23: note that PATH 17 also exists in silver-release/SKILL.md (primary location). This silver-feature version handles milestone completion at end of feature workflow.
  </action>
  <verify>
    <automated>grep -c "## PATH 14: DEBUG" skills/silver-feature/SKILL.md && grep -c "## PATH 16: DOCUMENT" skills/silver-feature/SKILL.md && grep -c "## PATH 17: RELEASE" skills/silver-feature/SKILL.md && ! grep -q "## Step 16:" skills/silver-feature/SKILL.md && ! grep -q "## Step 17:" skills/silver-feature/SKILL.md && grep -q "Insert PATH 14" skills/silver-feature/SKILL.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "## PATH 14: DEBUG" skills/silver-feature/SKILL.md returns 1
    - grep "## PATH 16: DOCUMENT" skills/silver-feature/SKILL.md returns 1
    - grep "## PATH 17: RELEASE" skills/silver-feature/SKILL.md returns 1
    - grep "Step 16:" skills/silver-feature/SKILL.md returns NO match (old step removed)
    - grep "Step 17:" skills/silver-feature/SKILL.md returns NO match (old step removed)
    - grep "Step 17.0:" skills/silver-feature/SKILL.md returns NO match
    - grep "systematic-debugging" skills/silver-feature/SKILL.md returns match
    - grep "gsd-forensics" skills/silver-feature/SKILL.md returns match
    - grep "resume.*interrupted path" skills/silver-feature/SKILL.md returns match (resume semantics)
    - grep "gaps-only" skills/silver-feature/SKILL.md returns match (fix routing)
    - grep "gsd-docs-update" skills/silver-feature/SKILL.md returns match
    - grep "engineering:tech-debt" skills/silver-feature/SKILL.md returns match
    - grep "gsd-audit-uat" skills/silver-feature/SKILL.md returns match
    - grep "create-release" skills/silver-feature/SKILL.md returns match
    - grep "gsd-complete-milestone" skills/silver-feature/SKILL.md returns match
    - grep "PATH 15 DESIGN HANDOFF" skills/silver-feature/SKILL.md returns match (insertion point in PATH 17)
    - grep "Insert PATH 14" skills/silver-feature/SKILL.md returns match (PATH 7 failure path updated)
    - grep "silver:bugfix.*triage" skills/silver-feature/SKILL.md returns NO match (old failure path removed)
  </acceptance_criteria>
  <done>PATH 14, 16, 17 sections exist with full structure. Old Step 16, Step 17 (and sub-steps) are removed. PATH 7 failure path references PATH 14 instead of silver:bugfix. PATH 17 includes PATH 15 insertion point for UI milestones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill file → Claude executor | SKILL.md instructions are followed by Claude — malformed instructions could cause wrong workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | SKILL.md path ordering | accept | File is version-controlled, reviewed before merge |
| T-24-02 | Denial of Service | PATH 9 infinite iteration | mitigate | "2 consecutive clean passes" exit condition prevents infinite loops |
| T-24-03 | Elevation | PATH 14 bypassing quality gates | mitigate | Resume semantics re-enter interrupted path, not skip ahead |
</threat_model>

<verification>
All 6 cross-cutting paths exist as structured sections in silver-feature/SKILL.md. Old flat steps are fully removed. PATH ordering matches composition order.
</verification>

<success_criteria>
- silver-feature/SKILL.md contains PATH 0 through PATH 17 (with PATH 12 appearing twice)
- No flat steps (9a-9d, 10-12, 13, Step 16, Step 17) remain
- Each new PATH section has prerequisite check, steps, and exit condition
- PATH 7 failure path references PATH 14
</success_criteria>

<output>
After completion, create `.planning/phases/24-cross-cutting-paths-quality-gate-dual-mode/24-01-SUMMARY.md`
</output>
