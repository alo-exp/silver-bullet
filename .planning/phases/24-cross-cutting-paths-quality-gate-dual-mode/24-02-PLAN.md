---
phase: 24-cross-cutting-paths-quality-gate-dual-mode
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/quality-gates/SKILL.md
autonomous: true
requirements:
  - CROSS-07
must_haves:
  truths:
    - "quality-gates/SKILL.md detects mode (design-time vs adversarial) from artifact state before running dimensions"
    - "Pre-plan mode runs lighter checklist focused on design decisions"
    - "Pre-ship mode runs full adversarial audit focused on implementation quality"
  artifacts:
    - path: "skills/quality-gates/SKILL.md"
      provides: "Dual-mode quality gate orchestrator"
      contains: "Mode Detection"
  key_links:
    - from: "skills/quality-gates/SKILL.md mode detection"
      to: "PLAN.md existence check"
      via: "Artifact state inspection"
      pattern: "PLAN.md.*design-time"
---

<objective>
Add dual-mode detection to quality-gates/SKILL.md so it operates as design-time checklist (pre-plan) or adversarial audit (pre-ship) based on artifact state.

Purpose: The quality gate runs twice in every composition. Without mode detection, both invocations would run identically. Dual-mode ensures appropriate depth at each point.

Output: Updated skills/quality-gates/SKILL.md with mode detection section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cross-cutting-paths-quality-gate-dual-mode/24-CONTEXT.md
@docs/composable-paths-contracts.md

<interfaces>
<!-- From quality-gates/SKILL.md — current structure -->
<!-- Step 1: Load all quality dimension skills (9 files) -->
<!-- Step 2: Apply each dimension — run Planning Checklist -->
<!-- Step 3: Produce consolidated report -->
<!-- Step 4: Gate enforcement -->

<!-- The dual-mode detection goes BEFORE Step 1, determining which checklist depth to use -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dual-mode detection to quality-gates orchestrator</name>
  <files>skills/quality-gates/SKILL.md</files>
  <read_first>
    - skills/quality-gates/SKILL.md (full file — understand current structure)
    - .planning/phases/24-cross-cutting-paths-quality-gate-dual-mode/24-CONTEXT.md (D-24 through D-26)
    - docs/composable-paths-contracts.md (PATH 12 contract)
  </read_first>
  <action>
Add a new "Step 0: Mode Detection" section BEFORE the existing Step 1 in quality-gates/SKILL.md. Per D-24 through D-26:

**Step 0: Mode Detection**

Detect operating mode from artifact state using this disambiguation table:

| PLAN.md exists? | VERIFICATION.md with status: passed? | Mode |
|-----------------|--------------------------------------|------|
| No | No | **design-time** (pre-plan quality gate) |
| No | Yes | Invalid state — STOP with error |
| Yes | No | **design-time** (mid-execution, treat as pre-plan) |
| Yes | Yes | **adversarial** (pre-ship quality gate) |

Detection commands:
```bash
PLAN_EXISTS=$(ls .planning/phases/{phase}/*-PLAN.md 2>/dev/null | head -1)
VERIFY_PASSED=$(grep -l "status: passed" .planning/VERIFICATION.md 2>/dev/null)
```

**Mode effects on Step 2:**

- **design-time mode:** For each dimension, run the **Planning Checklist** (lighter). Focus on design decisions, architectural alignment, and upfront risk identification. N/A is acceptable for implementation-specific items.
- **adversarial mode:** For each dimension, run the **Full Audit** (comprehensive). Focus on implementation quality, edge cases, security gaps, and production readiness. N/A requires strong justification.

Update the Step 2 instructions to reference the detected mode:
- Change "run its Planning Checklist" to "run its Planning Checklist (design-time mode) or Full Audit (adversarial mode) as determined in Step 0"

Update the Step 4 gate enforcement message:
- design-time: "Quality gates passed (design-time). Proceed to planning."
- adversarial: "Quality gates passed (pre-ship). Proceed to shipping."

Also update the skill description in the frontmatter to reflect dual-mode: change "Use before /gsd:plan-phase in the dev cycle" to "Dual-mode: design-time checklist (pre-plan) or adversarial audit (pre-ship). Mode auto-detected from artifact state."

Update the intro paragraph after the heading to mention dual-mode operation instead of only referencing pre-plan usage.
  </action>
  <verify>
    <automated>grep -c "Mode Detection" skills/quality-gates/SKILL.md && grep -q "design-time" skills/quality-gates/SKILL.md && grep -q "adversarial" skills/quality-gates/SKILL.md && grep -q "PLAN.md" skills/quality-gates/SKILL.md && grep -q "VERIFICATION.md" skills/quality-gates/SKILL.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Step 0: Mode Detection" skills/quality-gates/SKILL.md returns match
    - grep "design-time" skills/quality-gates/SKILL.md returns match
    - grep "adversarial" skills/quality-gates/SKILL.md returns match
    - grep "PLAN.md" skills/quality-gates/SKILL.md returns match (disambiguation check)
    - grep "VERIFICATION.md" skills/quality-gates/SKILL.md returns match (disambiguation check)
    - grep "Full Audit" skills/quality-gates/SKILL.md returns match (adversarial mode uses full audit)
    - grep "Planning Checklist" skills/quality-gates/SKILL.md returns match (design-time mode uses planning checklist)
    - grep "Dual-mode" skills/quality-gates/SKILL.md returns match (updated frontmatter description)
    - grep "pre-ship" skills/quality-gates/SKILL.md returns match (gate enforcement message)
  </acceptance_criteria>
  <done>quality-gates/SKILL.md has Step 0 Mode Detection with 4-state disambiguation table. Design-time mode runs Planning Checklist, adversarial mode runs Full Audit. Frontmatter and intro updated to reflect dual-mode operation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artifact state → mode detection | Mode is derived from file existence — could be spoofed by stale artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Spoofing | Mode detection via stale VERIFICATION.md | accept | Low risk — worst case runs adversarial mode unnecessarily (safer default) |
| T-24-05 | Information Disclosure | Quality gate findings | accept | Findings are internal planning artifacts, not shipped |
</threat_model>

<verification>
quality-gates/SKILL.md auto-detects mode and adjusts dimension evaluation depth accordingly.
</verification>

<success_criteria>
- quality-gates/SKILL.md has Step 0 before Step 1
- 4-state disambiguation table present
- Two distinct checklist depths documented (Planning Checklist vs Full Audit)
- Gate enforcement messages differ by mode
</success_criteria>

<output>
After completion, create `.planning/phases/24-cross-cutting-paths-quality-gate-dual-mode/24-02-SUMMARY.md`
</output>
