---
phase: 17-formalization-workflow-wiring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/silver-bullet.md.base
  - silver-bullet.md
autonomous: true
requirements:
  - WFIN-04
  - WFIN-05
  - WFIN-06
  - WFIN-07
must_haves:
  truths:
    - "silver-bullet.md.base instructs gsd-new-milestone to invoke review-roadmap after roadmap creation"
    - "silver-bullet.md.base instructs gsd-new-milestone to invoke review-requirements after requirements definition"
    - "silver-bullet.md.base instructs gsd-discuss-phase to invoke review-context after CONTEXT.md capture"
    - "silver-bullet.md.base instructs gsd-plan-phase researcher to invoke review-research after RESEARCH.md creation"
  artifacts:
    - path: "templates/silver-bullet.md.base"
      provides: "Workflow wiring instructions for new-milestone, discuss-phase, plan-phase"
      contains: "review-roadmap"
    - path: "silver-bullet.md"
      provides: "Live instance matching template"
      contains: "review-roadmap"
  key_links:
    - from: "templates/silver-bullet.md.base"
      to: "skills/artifact-reviewer/SKILL.md"
      via: "instruction-based invocation reference"
      pattern: "review-roadmap.*review-requirements.*review-context.*review-research"
---

<objective>
Add instruction-based review round wiring for GSD workflows (new-milestone, discuss-phase, plan-phase) into silver-bullet.md.base. These are GSD commands that SB cannot modify directly (section 8 boundary), so SB instructs Claude via silver-bullet.md to invoke reviewers after those commands produce artifacts.

Purpose: Complete the workflow wiring so every artifact-producing GSD step is review-gated.
Output: Updated silver-bullet.md.base and synced silver-bullet.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/artifact-reviewer/SKILL.md

<interfaces>
From silver-bullet.md.base section 2b — GSD commands that produce artifacts needing review:

| Command | Produces | Reviewer needed |
|---------|----------|----------------|
| /gsd:new-milestone | ROADMAP.md, REQUIREMENTS.md | review-roadmap, review-requirements |
| /gsd:discuss-phase | CONTEXT.md | review-context |
| /gsd:plan-phase (researcher step) | RESEARCH.md | review-research |

These are GSD-owned commands. SB cannot modify GSD plugin files (section 8 boundary).
SB MUST instruct Claude via silver-bullet.md to add review rounds after these commands complete their artifact-producing steps.

The pattern: "After /gsd:{command} produces {artifact}, invoke /artifact-reviewer {artifact-path} --reviewer {reviewer-name} via the Skill tool before proceeding."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire review rounds for new-milestone (WFIN-04/05) — ROADMAP.md and REQUIREMENTS.md</name>
  <files>templates/silver-bullet.md.base</files>
  <read_first>
    - templates/silver-bullet.md.base (lines 185-215 — section 2b GSD process knowledge)
    - templates/silver-bullet.md.base (lines 495-515 — section 3b/3c area for placement context)
  </read_first>
  <action>
Add a new subsection "### 3a-i. Post-Command Review Gates" in section 3a of templates/silver-bullet.md.base, immediately after the "### Per-Reviewer 2-Pass Requirements" subsection added by Plan 17-01 (and before section 3b).

Content for the new subsection:

```markdown
### 3a-i. Post-Command Review Gates

GSD commands that produce reviewable artifacts MUST be followed by a review round. Since GSD plugin files cannot be modified (section 8 boundary), these gates are enforced here as post-command instructions.

**After /gsd:new-milestone completes:**

1. **ROADMAP.md review (WFIN-04):** Invoke `/artifact-reviewer .planning/ROADMAP.md --reviewer review-roadmap` via the Skill tool. Do NOT commit the roadmap until /artifact-reviewer reports 2 consecutive clean passes. If issues are found, apply fixes to ROADMAP.md and re-review automatically.

2. **REQUIREMENTS.md review (WFIN-05):** Invoke `/artifact-reviewer .planning/REQUIREMENTS.md --reviewer review-requirements` via the Skill tool. Do NOT commit requirements until /artifact-reviewer reports 2 consecutive clean passes. If issues are found, apply fixes to REQUIREMENTS.md and re-review automatically.

Run these reviews in sequence (ROADMAP first, then REQUIREMENTS) since requirements reference the roadmap.
```

Do NOT modify any existing content in section 2b or any other section. This is an additive insertion only.
  </action>
  <acceptance_criteria>
    - grep "3a-i. Post-Command Review Gates" templates/silver-bullet.md.base returns a match
    - grep "WFIN-04" templates/silver-bullet.md.base returns a match
    - grep "WFIN-05" templates/silver-bullet.md.base returns a match
    - grep "/artifact-reviewer .planning/ROADMAP.md --reviewer review-roadmap" templates/silver-bullet.md.base returns a match
    - grep "/artifact-reviewer .planning/REQUIREMENTS.md --reviewer review-requirements" templates/silver-bullet.md.base returns a match
  </acceptance_criteria>
  <verify>
    <automated>grep -q "WFIN-04" templates/silver-bullet.md.base && grep -q "WFIN-05" templates/silver-bullet.md.base && grep -q "Post-Command Review Gates" templates/silver-bullet.md.base && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>new-milestone workflow has post-command review gates for ROADMAP.md and REQUIREMENTS.md</done>
</task>

<task type="auto">
  <name>Task 2: Wire review rounds for discuss-phase and plan-phase (WFIN-06/07) + sync silver-bullet.md</name>
  <files>templates/silver-bullet.md.base, silver-bullet.md</files>
  <read_first>
    - templates/silver-bullet.md.base (the "### 3a-i. Post-Command Review Gates" subsection just added by Task 1)
  </read_first>
  <action>
Append to the "### 3a-i. Post-Command Review Gates" subsection in templates/silver-bullet.md.base (after the new-milestone gates):

```markdown

**After /gsd:discuss-phase completes:**

3. **CONTEXT.md review (WFIN-06):** Invoke `/artifact-reviewer .planning/phases/{phase}/{phase}-CONTEXT.md --reviewer review-context` via the Skill tool. Do NOT commit the context until /artifact-reviewer reports 2 consecutive clean passes. If issues are found, apply fixes to CONTEXT.md and re-review automatically.

**After /gsd:plan-phase researcher step completes (before planning begins):**

4. **RESEARCH.md review (WFIN-07):** Invoke `/artifact-reviewer .planning/phases/{phase}/{phase}-RESEARCH.md --reviewer review-research` via the Skill tool. Do NOT commit the research until /artifact-reviewer reports 2 consecutive clean passes. If issues are found, apply fixes to RESEARCH.md and re-review automatically.

> **Note:** The `{phase}` placeholder refers to the current phase directory (e.g., `12-spec-foundation`). The artifact-reviewer resolves the absolute path internally.
```

After updating templates/silver-bullet.md.base, regenerate silver-bullet.md by running:
```bash
node src/commands/update.cjs
```
If that command is not available, manually copy the updated section 3a (including 3a-i) from templates/silver-bullet.md.base into silver-bullet.md.

Verify silver-bullet.md contains all 4 WFIN post-command review gates.
  </action>
  <acceptance_criteria>
    - grep "WFIN-06" templates/silver-bullet.md.base returns a match
    - grep "WFIN-07" templates/silver-bullet.md.base returns a match
    - grep "/artifact-reviewer.*review-context" templates/silver-bullet.md.base returns a match
    - grep "/artifact-reviewer.*review-research" templates/silver-bullet.md.base returns a match
    - grep "Post-Command Review Gates" silver-bullet.md returns a match (synced)
    - grep "WFIN-06" silver-bullet.md returns a match (synced)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "WFIN-06" templates/silver-bullet.md.base && grep -q "WFIN-07" templates/silver-bullet.md.base && grep -q "Post-Command Review Gates" silver-bullet.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>discuss-phase and plan-phase have post-command review gates, silver-bullet.md is synced with template</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SB instructions to GSD behavior | SB instructs Claude to add review steps after GSD commands; GSD files are NOT modified |
| Template to live config | silver-bullet.md.base generates silver-bullet.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Elevation of Privilege | GSD command bypass | mitigate | Post-command review gates enforce review even when GSD commands don't have built-in review; section 3a enforcement prevents skipping |
| T-17-06 | Tampering | silver-bullet.md.base | accept | Git-tracked, developer-reviewed |
</threat_model>

<verification>
1. Section 3a-i exists with 4 numbered post-command review gates
2. Each gate references the correct reviewer name and artifact path
3. silver-bullet.md is synced and contains all gates
4. No GSD plugin files were modified (section 8 boundary respected)
</verification>

<success_criteria>
- 4 post-command review gates (WFIN-04/05/06/07) in section 3a-i
- silver-bullet.md matches template
- Zero modifications to any file outside templates/silver-bullet.md.base and silver-bullet.md
</success_criteria>

<output>
After completion, create `.planning/phases/17-formalization-workflow-wiring/17-03-SUMMARY.md`
</output>
