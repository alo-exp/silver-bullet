---
phase: 17-formalization-workflow-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/silver-spec/SKILL.md
  - skills/silver-ingest/SKILL.md
  - skills/silver-feature/SKILL.md
autonomous: true
requirements:
  - WFIN-01
  - WFIN-02
  - WFIN-03
  - WFIN-08
  - WFIN-09
must_haves:
  truths:
    - "silver-spec Step 7 invokes review-spec via /artifact-reviewer after writing SPEC.md"
    - "silver-spec Step 8 invokes review-requirements via /artifact-reviewer after writing REQUIREMENTS.md"
    - "silver-spec Step 9 invokes review-design via /artifact-reviewer after writing DESIGN.md"
    - "silver-ingest Step 7 invokes review-ingestion-manifest via /artifact-reviewer after writing INGESTION_MANIFEST.md"
    - "silver-feature Step 17.0 invokes review-uat via /artifact-reviewer after generating UAT.md"
  artifacts:
    - path: "skills/silver-spec/SKILL.md"
      provides: "Review round wiring at Steps 7, 8, 9"
      contains: "/artifact-reviewer"
    - path: "skills/silver-ingest/SKILL.md"
      provides: "Review round wiring at Step 7"
      contains: "/artifact-reviewer"
    - path: "skills/silver-feature/SKILL.md"
      provides: "Review round wiring at Step 17.0"
      contains: "/artifact-reviewer"
  key_links:
    - from: "skills/silver-spec/SKILL.md"
      to: "skills/artifact-reviewer/SKILL.md"
      via: "Skill tool invocation"
      pattern: "/artifact-reviewer.*review-spec"
    - from: "skills/silver-ingest/SKILL.md"
      to: "skills/artifact-reviewer/SKILL.md"
      via: "Skill tool invocation"
      pattern: "/artifact-reviewer.*review-ingestion-manifest"
---

<objective>
Wire review rounds into silver-spec (Steps 7/8/9), silver-ingest (Step 7), and silver-feature (Step 17.0) so each artifact-producing step invokes its dedicated reviewer via /artifact-reviewer and blocks until 2 consecutive clean passes.

Purpose: Every SB-produced artifact is quality-gated before commit.
Output: Updated SKILL.md files for silver-spec, silver-ingest, silver-feature
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/artifact-reviewer/SKILL.md
@skills/artifact-reviewer/rules/review-loop.md

<interfaces>
The review loop invocation pattern (from review-loop.md):
- Invoke `/artifact-reviewer <artifact-path> --reviewer <reviewer-skill-name>` via the Skill tool
- The artifact-reviewer orchestrates the 2-consecutive-pass loop internally
- The calling workflow MUST NOT proceed until /artifact-reviewer returns completion
- On failure after 5 rounds, /artifact-reviewer surfaces to user — the calling workflow waits

Standard invocation block to insert:
```
Invoke `/artifact-reviewer .planning/{ARTIFACT}.md --reviewer {reviewer-name}` via the Skill tool.
Do NOT proceed to the next step until /artifact-reviewer reports 2 consecutive clean passes.
If /artifact-reviewer surfaces an unresolvable issue, STOP and present it to the user.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire review rounds into silver-spec Steps 7, 8, 9 (WFIN-01/02/03)</name>
  <files>skills/silver-spec/SKILL.md</files>
  <read_first>
    - skills/silver-spec/SKILL.md (full file — already loaded, 238 lines)
  </read_first>
  <action>
Add review round invocations to silver-spec SKILL.md at three points:

**WFIN-01 — After Step 7 (Write SPEC.md), before Step 8:**
Insert a new subsection "### Step 7a: Review SPEC.md" immediately after Step 7's content (after line that writes SPEC.md). Content:

```markdown
### Step 7a: Review SPEC.md

**NON-SKIPPABLE GATE.**

Invoke `/artifact-reviewer .planning/SPEC.md --reviewer review-spec` via the Skill tool.

Do NOT proceed to Step 8 until /artifact-reviewer reports 2 consecutive clean passes. If issues are found, /artifact-reviewer will apply fixes and re-review automatically. If /artifact-reviewer surfaces an unresolvable issue after 5 rounds, STOP and present it to the user.
```

**WFIN-03 — After Step 8 (Write REQUIREMENTS.md), before Step 9:**
Insert "### Step 8a: Review REQUIREMENTS.md" immediately after Step 8's content:

```markdown
### Step 8a: Review REQUIREMENTS.md

**NON-SKIPPABLE GATE.**

Invoke `/artifact-reviewer .planning/REQUIREMENTS.md --reviewer review-requirements` via the Skill tool.

Do NOT proceed to Step 9 until /artifact-reviewer reports 2 consecutive clean passes. If issues are found, /artifact-reviewer will apply fixes and re-review automatically. If /artifact-reviewer surfaces an unresolvable issue after 5 rounds, STOP and present it to the user.
```

**WFIN-02 — After Step 9 (Write DESIGN.md), before Step 10:**
Insert "### Step 9a: Review DESIGN.md (conditional)" immediately after Step 9's content. This is conditional because Step 9 itself is conditional:

```markdown
### Step 9a: Review DESIGN.md (conditional)

**Only if Step 9 produced a DESIGN.md.**

Invoke `/artifact-reviewer .planning/DESIGN.md --reviewer review-design` via the Skill tool.

Do NOT proceed to Step 10 until /artifact-reviewer reports 2 consecutive clean passes. If issues are found, /artifact-reviewer will apply fixes and re-review automatically. If /artifact-reviewer surfaces an unresolvable issue after 5 rounds, STOP and present it to the user.
```

Also add "Step 7a", "Step 8a", "Step 9a" to the Non-skippable gates list in the Step-Skip Protocol section (around line 39). Update the line to read:
`**Non-skippable gates:** Step 3: Socratic Elicitation, Step 5: Assumption Consolidation, Step 7: Write SPEC.md, Step 7a: Review SPEC.md, Step 8a: Review REQUIREMENTS.md, Step 9a: Review DESIGN.md. Refuse skip requests for these regardless of section 10.`
  </action>
  <acceptance_criteria>
    - grep "Step 7a: Review SPEC.md" skills/silver-spec/SKILL.md returns a match
    - grep "Step 8a: Review REQUIREMENTS.md" skills/silver-spec/SKILL.md returns a match
    - grep "Step 9a: Review DESIGN.md" skills/silver-spec/SKILL.md returns a match
    - grep -c "/artifact-reviewer" skills/silver-spec/SKILL.md returns 3
    - grep "review-spec\|review-requirements\|review-design" skills/silver-spec/SKILL.md returns 3 matches
  </acceptance_criteria>
  <verify>
    <automated>count=$(grep -c "/artifact-reviewer" skills/silver-spec/SKILL.md); test "$count" -eq 3 && echo "PASS: 3 review round invocations in silver-spec" || echo "FAIL: count=$count"</automated>
  </verify>
  <done>silver-spec Steps 7a, 8a, 9a invoke their respective reviewers as non-skippable gates</done>
</task>

<task type="auto">
  <name>Task 2: Wire review rounds into silver-ingest Step 7 and silver-feature Step 17.0 (WFIN-08/09)</name>
  <files>skills/silver-ingest/SKILL.md, skills/silver-feature/SKILL.md</files>
  <read_first>
    - skills/silver-ingest/SKILL.md (lines 355-398 — Step 7 and Step 8 area)
    - skills/silver-feature/SKILL.md (lines 227-242 — Step 17.0 area)
  </read_first>
  <action>
**WFIN-08 — silver-ingest, after Step 7 (Write INGESTION_MANIFEST.md), before Step 8:**

Insert "### Step 7a: Review INGESTION_MANIFEST.md" immediately after Step 7's content (after the manifest format section, before Step 8: Commit):

```markdown
### Step 7a: Review INGESTION_MANIFEST.md

**NON-SKIPPABLE GATE.**

Invoke `/artifact-reviewer .planning/INGESTION_MANIFEST.md --reviewer review-ingestion-manifest` via the Skill tool.

Do NOT proceed to Step 8 until /artifact-reviewer reports 2 consecutive clean passes. If issues are found, /artifact-reviewer will apply fixes and re-review automatically. If /artifact-reviewer surfaces an unresolvable issue after 5 rounds, STOP and present it to the user.
```

Also update the Non-skippable gates line in silver-ingest's Step-Skip Protocol to include "Step 7a: Review INGESTION_MANIFEST.md":
`**Non-skippable gates:** Step 6: Assemble SPEC.md Draft, Step 7: Write INGESTION_MANIFEST.md, Step 7a: Review INGESTION_MANIFEST.md. Refuse skip requests for these regardless of section 10.`

**WFIN-09 — silver-feature, after Step 17.0 (Generate UAT.md), before gsd-audit-uat invocation:**

In Step 17.0, after the line "Write `.planning/UAT.md` using the Write tool. Then proceed to invoke gsd-audit-uat which fills in results.", insert a review gate BEFORE the gsd-audit-uat invocation:

```markdown
### Step 17.0a: Review UAT.md

Invoke `/artifact-reviewer .planning/UAT.md --reviewer review-uat` via the Skill tool.

Do NOT proceed to gsd-audit-uat until /artifact-reviewer reports 2 consecutive clean passes. If issues are found, /artifact-reviewer will apply fixes and re-review automatically. If /artifact-reviewer surfaces an unresolvable issue after 5 rounds, STOP and present it to the user.
```

Reorder the Step 17 sub-steps so the sequence is: 17.0 (generate UAT.md) -> 17.0a (review UAT.md) -> gsd-audit-uat -> gsd-audit-milestone -> gap closure -> gsd-complete-milestone.
  </action>
  <acceptance_criteria>
    - grep "Step 7a: Review INGESTION_MANIFEST.md" skills/silver-ingest/SKILL.md returns a match
    - grep "/artifact-reviewer.*review-ingestion-manifest" skills/silver-ingest/SKILL.md returns a match
    - grep "Step 17.0a: Review UAT.md" skills/silver-feature/SKILL.md returns a match
    - grep "/artifact-reviewer.*review-uat" skills/silver-feature/SKILL.md returns a match
  </acceptance_criteria>
  <verify>
    <automated>grep -q "/artifact-reviewer.*review-ingestion-manifest" skills/silver-ingest/SKILL.md && grep -q "/artifact-reviewer.*review-uat" skills/silver-feature/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>silver-ingest Step 7a and silver-feature Step 17.0a invoke their respective reviewers</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill files to Claude behavior | SKILL.md files are instruction-only; Claude interprets them at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | SKILL.md files | accept | Git-tracked, developer-reviewed; no runtime code execution |
| T-17-04 | Elevation of Privilege | Review skip | mitigate | Review steps marked NON-SKIPPABLE GATE; Step-Skip Protocol refuses skip requests |
</threat_model>

<verification>
1. silver-spec has 3 review round invocations (Steps 7a, 8a, 9a)
2. silver-ingest has 1 review round invocation (Step 7a)
3. silver-feature has 1 review round invocation (Step 17.0a)
4. All review steps are marked NON-SKIPPABLE
</verification>

<success_criteria>
- 5 total /artifact-reviewer invocations across the 3 skill files
- Each invocation specifies the correct reviewer name
- Each invocation blocks until 2 consecutive clean passes
- All review steps are in the non-skippable gates list
</success_criteria>

<output>
After completion, create `.planning/phases/17-formalization-workflow-wiring/17-02-SUMMARY.md`
</output>
