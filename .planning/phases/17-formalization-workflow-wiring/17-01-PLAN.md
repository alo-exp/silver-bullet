---
phase: 17-formalization-workflow-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/silver-bullet.md.base
  - silver-bullet.md
autonomous: true
requirements:
  - EXRV-01
  - EXRV-02
  - EXRV-03
  - EXRV-04
  - WFIN-10
must_haves:
  truths:
    - "Section 3a mapping table lists all 12+ artifact types with their reviewer skill names"
    - "Existing GSD reviewers (plan-checker, code-reviewer, verifier, security-auditor) appear in the mapping table with 2-pass requirement"
    - "silver-bullet.md matches silver-bullet.md.base after template regeneration"
  artifacts:
    - path: "templates/silver-bullet.md.base"
      provides: "Updated section 3a with complete mapping table and EXRV formalization"
      contains: "review-spec"
    - path: "silver-bullet.md"
      provides: "Live instance matching template"
      contains: "review-spec"
  key_links:
    - from: "templates/silver-bullet.md.base"
      to: "silver-bullet.md"
      via: "template regeneration"
      pattern: "review-spec.*review-design.*review-requirements.*review-roadmap"
---

<objective>
Formalize the 4 existing GSD reviewers into the 2-consecutive-pass framework and update the section 3a mapping table with all 12+ artifact-to-reviewer mappings.

Purpose: Establishes the complete enforcement reference so all producing workflows know which reviewer to invoke and that 2 consecutive clean passes are required.
Output: Updated silver-bullet.md.base section 3a + synced silver-bullet.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/artifact-reviewer/SKILL.md
@skills/artifact-reviewer/rules/review-loop.md

<interfaces>
From skills/artifact-reviewer/SKILL.md — the mapping table to expand:

| Artifact Pattern | Reviewer Skill | Notes |
|-----------------|---------------|-------|
| *-PLAN.md | gsd-plan-checker | Existing GSD reviewer |
| Code changes | gsd-code-reviewer | Existing GSD reviewer |
| VERIFICATION.md | gsd-verifier | Existing GSD reviewer |
| Security findings | silver:security | Existing SB reviewer |
| SPEC.md | review-spec | New SB reviewer |
| DESIGN.md | review-design | New SB reviewer |
| REQUIREMENTS.md | review-requirements | New SB reviewer |
| ROADMAP.md | review-roadmap | New SB reviewer |
| CONTEXT.md | review-context | New SB reviewer |
| RESEARCH.md | review-research | New SB reviewer |
| INGESTION_MANIFEST.md | review-ingestion-manifest | New SB reviewer |
| UAT.md | review-uat | New SB reviewer |

Current silver-bullet.md.base section 3a (lines 413-454) has only the 4 existing GSD reviewers in the table.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize existing reviewers with 2-pass instructions (EXRV-01/02/03/04)</name>
  <files>templates/silver-bullet.md.base</files>
  <read_first>
    - templates/silver-bullet.md.base (lines 413-454 — current section 3a)
  </read_first>
  <action>
Update section 3a in templates/silver-bullet.md.base to formalize all 4 existing GSD reviewers into the 2-consecutive-pass framework. For each reviewer, add explicit instructions below the mapping table:

**EXRV-01 (plan-checker):** After the existing table row for plan-checker, add instruction text: "After /gsd:plan-phase creates a PLAN.md, invoke /gsd:plan-checker iteratively. If issues are found, fix and re-run. The plan is NOT approved until 2 consecutive clean passes. Do not commit the plan until the second consecutive clean pass completes."

**EXRV-02 (code-reviewer):** Add instruction: "After /gsd:execute-phase completes code changes, invoke /gsd:code-reviewer iteratively. If ISSUE findings are returned, apply fixes via /gsd:code-review-fix and re-run the review. Code is NOT considered reviewed until 2 consecutive clean passes. Do not proceed to verification until the second consecutive clean pass completes."

**EXRV-03 (verifier):** Add instruction: "After /gsd:verify-work produces VERIFICATION.md, run verification a second consecutive time to confirm results. If the second pass surfaces new issues (e.g., flaky tests that passed first time), fix and restart the 2-pass count. Verification is NOT complete until 2 consecutive clean passes."

**EXRV-04 (security-auditor):** Add instruction: "After /silver:security produces security findings, run the audit a second consecutive time to validate mitigations applied during the first pass. If the second pass finds new or unresolved issues, fix and restart. Security review is NOT complete until 2 consecutive clean passes."

Place these instructions in a new subsection "### Per-Reviewer 2-Pass Requirements" immediately after the current "### Recording Review Loop Progress" subsection (around line 453), BEFORE section 3b starts.

Do NOT modify any other sections. Preserve all existing content exactly.
  </action>
  <acceptance_criteria>
    - grep "EXRV-01\|plan-checker.*iteratively\|plan is NOT approved until 2 consecutive" templates/silver-bullet.md.base returns matches
    - grep "EXRV-02\|code-reviewer.*iteratively\|Code is NOT considered reviewed until 2 consecutive" templates/silver-bullet.md.base returns matches
    - grep "EXRV-03\|verifier.*second consecutive\|Verification is NOT complete until 2 consecutive" templates/silver-bullet.md.base returns matches
    - grep "EXRV-04\|security-auditor.*second consecutive\|Security review is NOT complete until 2 consecutive" templates/silver-bullet.md.base returns matches
  </acceptance_criteria>
  <verify>
    <automated>grep -c "NOT.*until 2 consecutive" templates/silver-bullet.md.base | grep -q "4" && echo "PASS: all 4 EXRV instructions present" || echo "FAIL"</automated>
  </verify>
  <done>All 4 existing reviewers have explicit 2-consecutive-pass instructions in section 3a</done>
</task>

<task type="auto">
  <name>Task 2: Expand section 3a mapping table to all 12+ artifact types (WFIN-10) and sync silver-bullet.md</name>
  <files>templates/silver-bullet.md.base, silver-bullet.md</files>
  <read_first>
    - templates/silver-bullet.md.base (lines 417-425 — current mapping table)
    - silver-bullet.md (section 3a — find via grep "## 3a")
  </read_first>
  <action>
Replace the existing 4-row mapping table in section 3a of templates/silver-bullet.md.base with the complete 12-row table:

| Step | Artifact | Reviewer | Two-Pass Required | Producing Workflow |
|------|----------|----------|-------------------|--------------------|
| Plan creation | {phase}-NN-PLAN.md | /gsd:plan-checker | YES | /gsd:plan-phase |
| Execution | Code changes + SUMMARY.md | /gsd:code-reviewer | YES | /gsd:execute-phase |
| Verification | VERIFICATION.md | /gsd:verify-work | YES | /gsd:verify-work |
| Security check | Security findings | /silver:security | YES | /silver:security |
| Spec elicitation | SPEC.md | /artifact-reviewer --reviewer review-spec | YES | /silver:spec Step 7 |
| Design capture | DESIGN.md | /artifact-reviewer --reviewer review-design | YES | /silver:spec Step 9 |
| Requirements derivation | REQUIREMENTS.md | /artifact-reviewer --reviewer review-requirements | YES | /silver:spec Step 8, /gsd:new-milestone |
| Roadmap creation | ROADMAP.md | /artifact-reviewer --reviewer review-roadmap | YES | /gsd:new-milestone |
| Context capture | CONTEXT.md | /artifact-reviewer --reviewer review-context | YES | /gsd:discuss-phase |
| Research | RESEARCH.md | /artifact-reviewer --reviewer review-research | YES | /gsd:plan-phase (researcher) |
| Ingestion | INGESTION_MANIFEST.md | /artifact-reviewer --reviewer review-ingestion-manifest | YES | /silver:ingest Step 7 |
| UAT generation | UAT.md | /artifact-reviewer --reviewer review-uat | YES | /silver:feature Step 17.0 |

Update the paragraph above the table to say: "This rule applies to ALL artifact-producing review steps. Any step that produces an artifact listed below MUST invoke the mapped reviewer and achieve 2 consecutive clean passes before the artifact is committed."

After updating templates/silver-bullet.md.base, regenerate silver-bullet.md by running:
```bash
node src/commands/update.cjs
```
If that command is not available, manually copy the updated section 3a content from templates/silver-bullet.md.base into silver-bullet.md at the equivalent location.

Verify silver-bullet.md section 3a matches the template.
  </action>
  <acceptance_criteria>
    - grep -c "review-spec\|review-design\|review-requirements\|review-roadmap\|review-context\|review-research\|review-ingestion-manifest\|review-uat" templates/silver-bullet.md.base returns 8
    - grep "Producing Workflow" templates/silver-bullet.md.base returns a match (new column exists)
    - grep "review-spec" silver-bullet.md returns a match (synced)
  </acceptance_criteria>
  <verify>
    <automated>count=$(grep -c "review-" templates/silver-bullet.md.base); test "$count" -ge 8 && grep -q "review-spec" silver-bullet.md && echo "PASS: mapping table complete and synced" || echo "FAIL: count=$count"</automated>
  </verify>
  <done>Section 3a has all 12 artifact-reviewer mappings with Producing Workflow column, and silver-bullet.md is synced</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template to live config | silver-bullet.md.base generates silver-bullet.md — content integrity matters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | silver-bullet.md.base | accept | File is developer-authored, git-tracked; tampering is caught by diff review |
| T-17-02 | Information Disclosure | Mapping table | accept | No secrets — table maps artifact names to reviewer names, all public |
</threat_model>

<verification>
1. Section 3a in templates/silver-bullet.md.base contains 12 rows in the mapping table
2. All 4 EXRV per-reviewer instructions are present
3. silver-bullet.md section 3a matches the template
</verification>

<success_criteria>
- All 12 artifact types mapped to their reviewers in section 3a
- All 4 existing GSD reviewers have 2-consecutive-pass instructions
- silver-bullet.md is synced with the template
</success_criteria>

<output>
After completion, create `.planning/phases/17-formalization-workflow-wiring/17-01-SUMMARY.md`
</output>
