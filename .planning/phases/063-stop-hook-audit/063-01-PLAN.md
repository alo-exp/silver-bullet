---
phase: 063-stop-hook-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/internal/stop-hook-audit.md
  - hooks/stop-check.sh
  - tests/hooks/test-stop-check.sh
  - silver-bullet.md
autonomous: true
requirements: [HK-01]

must_haves:
  truths:
    - "docs/internal/stop-hook-audit.md exists and contains >= 7 labelled scenario entries"
    - "Each scenario entry has: name, trigger condition, reproduction steps, severity, and disposition"
    - "silver-bullet.md §1 (Automated Enforcement) contains a link or reference to docs/internal/stop-hook-audit.md"
    - "Any code changes to stop-check.sh are covered by passing tests in test-stop-check.sh"
    - "bash tests/hooks/test-stop-check.sh exits 0 after all changes"
  artifacts:
    - path: "docs/internal/stop-hook-audit.md"
      provides: "False-positive scenario enumeration with reproduction steps and dispositions"
      min_lines: 80
    - path: "hooks/stop-check.sh"
      provides: "stop-check hook with any confirmed false-positive fixes applied"
    - path: "tests/hooks/test-stop-check.sh"
      provides: "Test coverage for fixed scenarios"
    - path: "silver-bullet.md"
      provides: "§1 Automated Enforcement section with audit link"
      contains: "stop-hook-audit.md"
  key_links:
    - from: "silver-bullet.md §1"
      to: "docs/internal/stop-hook-audit.md"
      via: "markdown link"
      pattern: "stop-hook-audit"
    - from: "tests/hooks/test-stop-check.sh"
      to: "hooks/stop-check.sh"
      via: "bash invocation"
      pattern: 'bash.*stop-check\.sh'
---

<objective>
Audit stop-check.sh exhaustively for false-positive scenarios, produce a written record at docs/internal/stop-hook-audit.md with >= 7 scenario entries (name, trigger condition, reproduction steps, severity, disposition), fix any confirmed false-positives in the hook and cover them with tests, then link the audit document from silver-bullet.md §1.

Purpose: Satisfies HK-01. Gives maintainers a single document to consult when the Stop hook unexpectedly blocks a session — reproduction steps prevent guessing and dispositions record why each scenario is fixed, accepted, or deferred.

Output:
- docs/internal/stop-hook-audit.md (new file, >= 7 scenarios)
- hooks/stop-check.sh (patched for any confirmed false-positives found during audit)
- tests/hooks/test-stop-check.sh (new tests for any patched scenarios)
- silver-bullet.md (§1 gains one-line audit reference link)
</objective>

<execution_context>
@/Users/shafqat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shafqat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/shafqat/Documents/Projects/silver-bullet/.planning/PROJECT.md
@/Users/shafqat/Documents/Projects/silver-bullet/.planning/ROADMAP.md
@/Users/shafqat/Documents/Projects/silver-bullet/.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From hooks/stop-check.sh — firing order (comments in the file):
  1. jq-missing warning                    (line 26-31)  → exit 0, visible warning
  2. ERR trap                              (line 40)     → exit 0, visible warning
  3. No .silver-bullet.json found          (line 58)     → exit 0 silent
  4. Trivial bypass (lib/trivial-bypass.sh)(line 103-107)→ exit 0 if trivial file exists and is not a symlink
  5. HOOK-14 read-only gate                (line 125-190)→ exit 0 on clean-tree scenarios
     5a. clean tree + cmp_ref + 0-ahead       → exit 0
     5b. clean tree + no origin + named branch → exit 0
     5c. upstream_broken=true                  → fall through (fail-closed)
     5d. detached HEAD + clean tree            → fall through (fail-closed)
  6. HOOK-04 empty state                   (line 198)    → exit 0
  7. Branch-scope mismatch                 (line 216-219)→ exit 0
  8. Required-skills check → block or exit 0

From tests/hooks/test-stop-check.sh — existing test coverage (14 tests):
  Test 1:  No config file → silent exit
  Test 2:  All skills present → no block
  Test 3:  Missing skills + dirty tree → block
  Test 4:  Trivial bypass → no block
  Test 5:  On main, finishing-a-development-branch excluded → no block
  Test 6:  Empty state file → no block
  Test 7:  Clean tree + no origin → no block (HOOK-14)
  Test 7b: Clean tree + origin/main at HEAD → no block (HOOK-14 zero-ahead)
  Test 8:  Dirty tree + missing skills → block
  Test 9:  Clean tree + commits ahead of origin → still blocks
  Test 11: Stale upstream ref → fail-closed → block
  Test 12: Gitignored untracked file → block
  Test 13: Local main not used as anchor → skip (no anchor)
  Test 14: Cross-branch stale state → no block

From silver-bullet.md §1 (lines 70-110):
  ## 1. Automated Enforcement
  Lists 11 enforcement layers; item 7 is "Stop hook (Stop/SubagentStop)".
  The audit link must be placed here — after item 7 or after the §1 body.

From docs/internal/ (existing files):
  pre-release-quality-gate.md, site-qa-report.md, CICD.md — confirm naming convention
  for internal docs: plain kebab-case, markdown, full prose.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate all false-positive scenarios via code and test analysis</name>
  <files>
    hooks/stop-check.sh
    tests/hooks/test-stop-check.sh
  </files>
  <read_first>
    - hooks/stop-check.sh — read in full; trace every bail-out path (exit 0) and every
      fall-through that reaches enforcement
    - tests/hooks/test-stop-check.sh — identify which paths already have tests and
      which lack coverage; note test numbers for cross-reference in the audit doc
    - hooks/lib/trivial-bypass.sh — confirm the trivial-bypass guard (exits 0 when
      trivial file is present and is NOT a symlink)
    - hooks/lib/required-skills.sh — confirm DEFAULT_REQUIRED and DEVOPS_DEFAULT_REQUIRED
      variable names used by stop-check.sh
  </read_first>
  <action>
Read hooks/stop-check.sh end-to-end and trace every code path that results in exit 0
(a "pass") when the session MIGHT warrant enforcement. For each path record:
  - The exact lines in stop-check.sh
  - The preconditions that trigger it
  - Whether it is explicitly marked "fail-open by design" with a comment
  - Whether an existing test exercises that path (map to Test N in test-stop-check.sh)
  - Whether it is a true false-positive scenario or a correct fail-open

Produce an internal working-notes table (in memory only; will be written to the audit
doc in Task 2) covering at minimum these 10 scenario candidates:

  S-01  jq missing — hook exits 0 with warning; no enforcement possible
  S-02  No .silver-bullet.json — project not using SB; correct fail-open
  S-03  Trivial bypass file present — admin/read-only session; correct fail-open
  S-04  HOOK-14: clean tree + 0 commits ahead of cmp_ref — conversational session
  S-05  HOOK-14: clean tree + no origin + named branch — no remote, nothing to deploy
  S-06  HOOK-14: detached HEAD + clean tree — no named branch; falls through (fail-closed)
  S-07  HOOK-14: upstream_broken=true — configured upstream doesn't resolve; fail-closed
  S-08  HOOK-04: empty state file — no skills recorded; correct fail-open
  S-09  Branch-scope mismatch — state from a different branch; correct fail-open
  S-10  Subagent sessions — session-start creates trivial file; stop-check reads it

For S-10 (subagent sessions): confirm whether session-start creates the trivial file
for subagent contexts by reading the SessionStart hook command in hooks/hooks.json.
If it does, trivial bypass fires before skills are checked — this is NOT a false-positive
in practice. Document the mechanism clearly regardless.

Also check for one additional candidate:
  S-11  devops-cycle active_workflow with a different DEFAULT_REQUIRED — verify the
        substitution (lines 234-236 in stop-check.sh) does not leave a skill in
        DEFAULT_REQUIRED that should have been removed for devops-cycle sessions.

For EACH scenario determine:
  severity: high (blocks valid work), medium (blocks but recoverable), low (cosmetic/informational), none (working as intended)
  disposition: accepted (correct behaviour), fixed (bug fixed in Task 3), documented (known limitation, deferred with rationale)

This task produces no file output — the findings feed directly into Task 2.
  </action>
  <verify>
    <automated>bash -n /Users/shafqat/Documents/Projects/silver-bullet/hooks/stop-check.sh && echo "syntax OK"</automated>
  </verify>
  <done>
    All 10+ bail-out paths in stop-check.sh are traced and classified with
    severity + disposition. At least 7 distinct scenario entries are ready
    to be written to the audit document. Any confirmed false-positives
    (severity >= medium, disposition = fixed) have a concrete patch plan.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write docs/internal/stop-hook-audit.md</name>
  <files>
    docs/internal/stop-hook-audit.md
  </files>
  <read_first>
    - docs/internal/pre-release-quality-gate.md — study format/heading style used in
      other internal docs; match heading depth and table conventions
    - hooks/stop-check.sh — keep open for exact line references in reproduction steps
    - tests/hooks/test-stop-check.sh — cite Test N numbers where coverage already exists
  </read_first>
  <action>
Create docs/internal/stop-hook-audit.md. Use the following exact structure:

---
# Stop Hook False-Positive Audit

**Hook:** `hooks/stop-check.sh`
**Scope:** All `exit 0` (pass) paths that could allow unintended bypass OR all blocking paths
  that could fire when they should not.
**False-positive definition:** stop-check returns `decision:block` when it SHOULD NOT block.
**Audit date:** 2026-04-26
**Auditor:** Phase 63 executor (automated analysis + test-harness verification)

---

## Overview

[2–3 sentence description of what stop-check.sh does and the firing order comment
reproduced from lines 16–20 of the hook.]

## Scenario Catalogue

For EACH of the >= 7 scenarios write a subsection using this exact template:

### S-NN — [Scenario Name]

| Field | Value |
|---|---|
| **Trigger condition** | [exact precondition] |
| **Hook lines** | [line range in stop-check.sh] |
| **Test coverage** | Test N (test-stop-check.sh) / None |
| **Severity** | high / medium / low / none |
| **Disposition** | accepted / fixed / documented |

**Reproduction steps:**
1. [exact steps to trigger this path]
2. ...

**Rationale:**
[Why this disposition was chosen. If "accepted": why it is correct behaviour.
 If "fixed": what was changed and why. If "documented": why fixing is deferred.]

---

Mandatory scenarios to include (minimum 7; include all 11 if findings support them):

S-01  jq missing                              severity=none   disposition=accepted
S-02  No .silver-bullet.json                  severity=none   disposition=accepted
S-03  Trivial bypass file present             severity=none   disposition=accepted
S-04  HOOK-14 clean tree + 0-ahead of origin  severity=none   disposition=accepted
S-05  HOOK-14 clean tree + no origin anchor   severity=none   disposition=accepted
S-06  HOOK-14 detached HEAD (fail-closed)     severity=medium disposition=accepted (or documented)
S-07  HOOK-14 upstream_broken fail-closed     severity=medium disposition=accepted (or documented)
S-08  HOOK-04 empty state file                severity=none   disposition=accepted
S-09  Branch-scope mismatch                   severity=none   disposition=accepted
S-10  Subagent sessions (trivial bypass path) severity=none   disposition=accepted (or documented)
S-11  devops-cycle required_deploy override   severity=none   disposition=accepted

For S-06 (detached HEAD): the hook currently falls through to enforcement when
HEAD is detached and the tree is clean. This could block a user on a detached HEAD
who has no dirty files and has not run any deploy skills. Analyse whether this is a
genuine false-positive (clean tree + detached HEAD = nothing to deploy) or correct
behaviour (detached HEAD = we cannot prove read-only). Write the rationale clearly.
If it is a genuine false-positive, mark disposition=fixed and implement the fix in Task 3.
If it is acceptable, mark disposition=documented.

Append at the end of the file:

## Summary Table

| ID | Name | Severity | Disposition | Test Coverage | Fixed In |
|---|---|---|---|---|---|
| S-01 | ... | none | accepted | Test 1 / Test 10 | — |
| ... |

## References
- `hooks/stop-check.sh` — the audited hook
- `tests/hooks/test-stop-check.sh` — test harness
- GitHub issues: #14 (HOOK-14), #17 (HOOK-06), #71 (HK-01 audit request)
---

Verify the file has at least 7 scenario subsections (grep for "^### S-") and
contains a summary table.
  </action>
  <acceptance_criteria>
    - `test -f /Users/shafqat/Documents/Projects/silver-bullet/docs/internal/stop-hook-audit.md`
    - `grep -c "^### S-" /Users/shafqat/Documents/Projects/silver-bullet/docs/internal/stop-hook-audit.md` outputs 7 or more
    - `grep -q "^## Summary Table" /Users/shafqat/Documents/Projects/silver-bullet/docs/internal/stop-hook-audit.md`
    - `grep -q "Trigger condition" /Users/shafqat/Documents/Projects/silver-bullet/docs/internal/stop-hook-audit.md`
    - `grep -q "Reproduction steps" /Users/shafqat/Documents/Projects/silver-bullet/docs/internal/stop-hook-audit.md`
    - `grep -q "Disposition" /Users/shafqat/Documents/Projects/silver-bullet/docs/internal/stop-hook-audit.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^### S-" /Users/shafqat/Documents/Projects/silver-bullet/docs/internal/stop-hook-audit.md</automated>
  </verify>
  <done>
    docs/internal/stop-hook-audit.md exists with >= 7 scenario subsections,
    each containing trigger condition, reproduction steps, severity, and disposition.
    Summary table is present. File is >= 80 lines.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix confirmed false-positives in stop-check.sh and add tests</name>
  <files>
    hooks/stop-check.sh
    tests/hooks/test-stop-check.sh
  </files>
  <read_first>
    - docs/internal/stop-hook-audit.md — read the just-written audit to identify which
      scenarios have disposition=fixed; those are the only changes to make here
    - hooks/stop-check.sh — read current state before any edits
    - tests/hooks/test-stop-check.sh — read existing tests (Tests 1–14) to determine
      correct numbering for new tests and avoid duplicate setup patterns
  </read_first>
  <action>
Apply ONLY the changes identified as disposition=fixed in stop-hook-audit.md.

**If S-06 (detached HEAD + clean tree) is disposition=fixed:**
  In stop-check.sh, find the comment block starting at approximately line 186:
    "# Otherwise (cmp_ref empty AND branch empty/detached AND upstream not broken):
    #  detached-HEAD state. Fall through to enforcement..."
  Change the fall-through to an exit 0 with a visible informational message (not a
  silent exit — the comment explains why we know there is nothing to deploy):
    printf '{"hookSpecificOutput":{"message":"ℹ️  stop-check: detached HEAD with clean tree — no deployment target, skipping enforcement"}}' ; exit 0
  This ONLY fires when ALL of: cmp_ref is empty, current_branch is empty/detached,
  upstream_broken is false, AND tree is clean — a state where there is genuinely
  nothing to deploy.

  Add a new test after Test 14 in test-stop-check.sh:
    Test 15: HOOK-14 detached HEAD + clean tree → no block
    Setup: git checkout --detach HEAD in the test repo; write missing skills to state.
    Assert: assert_passes "detached HEAD + clean tree + no origin -> no block"

**If no scenarios are disposition=fixed:**
  Make no changes to hooks/stop-check.sh.
  Add a comment at the top of tests/hooks/test-stop-check.sh (after the initial header
  block) noting Phase 63 audit was run and all scenarios were disposition=accepted or
  documented. This acts as a dated maintenance marker:
    # Phase 63 audit (2026-04-26): all false-positive scenarios reviewed; no fixes needed.
    # See docs/internal/stop-hook-audit.md for the full scenario catalogue.

**After any edits to stop-check.sh:**
  Run syntax check: bash -n hooks/stop-check.sh
  Run shellcheck if available: shellcheck hooks/stop-check.sh || true
  Run full test suite: bash tests/hooks/test-stop-check.sh

The test suite must exit 0 before this task is considered complete.
  </action>
  <acceptance_criteria>
    - `bash -n /Users/shafqat/Documents/Projects/silver-bullet/hooks/stop-check.sh` exits 0
    - `bash /Users/shafqat/Documents/Projects/silver-bullet/tests/hooks/test-stop-check.sh 2>&1 | grep "^Results:"` shows 0 failed
    - If S-06 was fixed: `grep -q "detached HEAD" /Users/shafqat/Documents/Projects/silver-bullet/hooks/stop-check.sh`
    - If no fixes: `grep -q "Phase 63 audit" /Users/shafqat/Documents/Projects/silver-bullet/tests/hooks/test-stop-check.sh`
  </acceptance_criteria>
  <verify>
    <automated>bash /Users/shafqat/Documents/Projects/silver-bullet/tests/hooks/test-stop-check.sh 2>&1 | tail -3</automated>
  </verify>
  <done>
    stop-check.sh syntax is valid. All fixes from the audit (if any) are applied.
    bash tests/hooks/test-stop-check.sh exits 0 with 0 failures. Each fixed
    scenario has a corresponding test that passes.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add audit reference link to silver-bullet.md §1</name>
  <files>
    silver-bullet.md
  </files>
  <read_first>
    - silver-bullet.md lines 70–115 — read §1 Automated Enforcement section in full
      to find the exact insertion point; the Stop hook is item 7 in the numbered list
    - docs/internal/stop-hook-audit.md — confirm filename for the link target
  </read_first>
  <action>
Open silver-bullet.md. Locate §1 Automated Enforcement. Item 7 in the numbered list reads:
  "7. **Stop hook** (Stop/SubagentStop) — Blocks task-complete declaration if required_deploy skills are missing"

Append a reference line immediately after the §1 body (after the final `---` separator
that closes §1, OR after the "Redundant instructions + anti-rationalization" bullet,
whichever comes last before the `---`). Do NOT insert in the middle of the numbered list.

The reference line to add (insert as a new line just before the `---` that ends §1):

> **Stop hook false-positive audit**: For a full catalogue of known bail-out scenarios,
> reproduction steps, and dispositions, see [`docs/internal/stop-hook-audit.md`](docs/internal/stop-hook-audit.md).

Exact insertion point search: find the line that contains "Redundant instructions" within §1,
then find the `---` that follows it, and insert the blockquote between them.

After inserting, verify:
  grep -n "stop-hook-audit" silver-bullet.md  → must return >= 1 result
  grep -n "stop-hook-audit.md" silver-bullet.md  → link target must be present

Also update templates/silver-bullet.md.base with the identical addition in the same location
(critical invariant from CLAUDE.md: changes to silver-bullet.md must be mirrored in the template).
  </action>
  <acceptance_criteria>
    - `grep -q "stop-hook-audit.md" /Users/shafqat/Documents/Projects/silver-bullet/silver-bullet.md`
    - `grep -q "stop-hook-audit.md" /Users/shafqat/Documents/Projects/silver-bullet/templates/silver-bullet.md.base`
    - `grep -q "Stop hook false-positive audit" /Users/shafqat/Documents/Projects/silver-bullet/silver-bullet.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -n "stop-hook-audit" /Users/shafqat/Documents/Projects/silver-bullet/silver-bullet.md /Users/shafqat/Documents/Projects/silver-bullet/templates/silver-bullet.md.base</automated>
  </verify>
  <done>
    silver-bullet.md §1 contains a reference link to docs/internal/stop-hook-audit.md.
    templates/silver-bullet.md.base contains the same addition.
    Both grep checks return results.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → hook scripts | Tests invoke hooks via bash; test state files are written to ~/.claude/ |
| hook → git CLI | stop-check.sh calls git commands; output is parsed by the hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-063-01 | Tampering | docs/internal/stop-hook-audit.md | accept | Internal doc; no executable code; low-value target |
| T-063-02 | Tampering | hooks/stop-check.sh edits | mitigate | bash -n syntax check + full test suite run required before commit; ERR trap preserved |
| T-063-03 | Spoofing | test state files under ~/.claude/ | accept | Tests already use path-validated SILVER_BULLET_STATE_FILE; security pattern unchanged |
</threat_model>

<verification>
After all four tasks are complete, run these checks:

```bash
# 1. Audit document exists and has >= 7 scenarios
grep -c "^### S-" /Users/shafqat/Documents/Projects/silver-bullet/docs/internal/stop-hook-audit.md

# 2. Audit document has summary table
grep -q "^## Summary Table" /Users/shafqat/Documents/Projects/silver-bullet/docs/internal/stop-hook-audit.md && echo "summary table present"

# 3. silver-bullet.md and template both reference the audit
grep -n "stop-hook-audit" \
  /Users/shafqat/Documents/Projects/silver-bullet/silver-bullet.md \
  /Users/shafqat/Documents/Projects/silver-bullet/templates/silver-bullet.md.base

# 4. Hook syntax is clean
bash -n /Users/shafqat/Documents/Projects/silver-bullet/hooks/stop-check.sh && echo "hook syntax OK"

# 5. Full test suite passes
bash /Users/shafqat/Documents/Projects/silver-bullet/tests/hooks/test-stop-check.sh

# 6. Full project test suite
bash /Users/shafqat/Documents/Projects/silver-bullet/tests/run-all-tests.sh 2>&1 | tail -5
```
</verification>

<success_criteria>
- docs/internal/stop-hook-audit.md exists with >= 7 scenario entries, each with name, trigger condition, reproduction steps, severity, and disposition
- Summary table present in stop-hook-audit.md
- silver-bullet.md §1 contains a link or reference to docs/internal/stop-hook-audit.md
- templates/silver-bullet.md.base mirrors the same addition (critical invariant)
- hooks/stop-check.sh passes bash -n syntax check
- bash tests/hooks/test-stop-check.sh exits 0 with "0 failed"
- bash tests/run-all-tests.sh exits 0 (no regressions)
</success_criteria>

<output>
After completion, create `/Users/shafqat/Documents/Projects/silver-bullet/.planning/phases/063-stop-hook-audit/063-01-SUMMARY.md` using the summary template at @/Users/shafqat/.claude/get-shit-done/templates/summary.md.
</output>
