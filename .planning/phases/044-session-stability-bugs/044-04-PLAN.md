---
phase: 044-session-stability-bugs
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - PR-01
  - PR-02

must_haves:
  truths:
    - "PR #37 is merged to main — forge/skills/silver-feature/SKILL.md contains PATH 10b doc-scheme gate"
    - "PR #38 is merged to main — skills/silver-ui/SKILL.md contains Step 13b doc-scheme gate"
    - "Both PRs have passing CI checks at the time of merge"
  artifacts:
    - path: "forge/skills/silver-feature/SKILL.md"
      provides: "PATH 10b doc-scheme compliance gate between PATH 10 and PATH 11"
      contains: "PATH 10b"
    - path: "forge/skills/writing-plans/SKILL.md"
      provides: "## Documentation section in PLAN.md template"
    - path: "skills/silver-ui/SKILL.md"
      provides: "Step 13b doc-scheme compliance gate between Step 13 and Step 14"
      contains: "Step 13b"
    - path: "forge/skills/silver-ui/SKILL.md"
      provides: "PATH 10b doc-scheme compliance gate between Step 9 and Step 10"
      contains: "PATH 10b"
  key_links:
    - from: "PR #37 branch claude/elated-feynman-f535c4"
      to: "main"
      via: "gh pr merge --squash #37"
      pattern: ""
    - from: "PR #38 branch claude/busy-swirles-49e74d"
      to: "main"
      via: "gh pr merge --squash #38"
      pattern: ""
---

<objective>
Merge the two open doc-scheme compliance PRs (#37 and #38) into main.

Purpose: PR #37 forward-ports the doc-scheme gate to forge/skills/ variants (silver-feature and writing-plans). PR #38 adds the doc-scheme gate to both silver-ui variants (CC and Forge). Both PRs are already in MERGEABLE state with passing CI. Merging them closes PR-01 and PR-02 requirements.

Output: No new files created. Both PRs merged to main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CI green on both PRs then merge PR #37 (forge doc-scheme gate)</name>
  <files></files>
  <read_first>
    - Run: `/opt/homebrew/bin/gh pr checks 37 --repo alo-exp/silver-bullet` to confirm all checks pass
    - Run: `/opt/homebrew/bin/gh pr view 37 --repo alo-exp/silver-bullet --json mergeable,state` to confirm MERGEABLE
  </read_first>
  <action>
1. Verify PR #37 CI is green:
   `/opt/homebrew/bin/gh pr checks 37 --repo alo-exp/silver-bullet`
   All checks must show `pass`. If any check shows `fail` or `pending`, STOP and report.

2. Verify PR #37 is in MERGEABLE state:
   `/opt/homebrew/bin/gh pr view 37 --repo alo-exp/silver-bullet --json mergeable,state`
   Must return `"mergeable":"MERGEABLE"` and `"state":"OPEN"`.

3. Merge PR #37 using squash merge:
   `/opt/homebrew/bin/gh pr merge 37 --repo alo-exp/silver-bullet --squash --subject "fix(forge-sb): forward-port doc-scheme compliance gate to forge/skills/"`

4. After merge, verify the changes are present on main:
   `git fetch origin && git log origin/main --oneline -3`
   The merge commit should appear in the log.

5. Verify the doc-scheme gate is present in the merged file:
   `git show origin/main:forge/skills/silver-feature/SKILL.md | grep -c "PATH 10b"`
   Must output a number greater than 0.
  </action>
  <verify>
    <automated>/opt/homebrew/bin/gh pr view 37 --repo alo-exp/silver-bullet --json state | jq -r .state | grep -q 'MERGED' && echo "PASS: PR #37 merged" || echo "FAIL: PR #37 not merged"</automated>
    <automated>git fetch origin 2>/dev/null; git show origin/main:forge/skills/silver-feature/SKILL.md 2>/dev/null | grep -q "PATH 10b" && echo "PASS: PATH 10b present on main" || echo "FAIL: PATH 10b not found"</automated>
  </verify>
  <acceptance_criteria>
    - `/opt/homebrew/bin/gh pr view 37 --repo alo-exp/silver-bullet --json state | jq -r .state` outputs `MERGED`
    - `git show origin/main:forge/skills/silver-feature/SKILL.md | grep -c "PATH 10b"` outputs `1` or more
    - `git show origin/main:forge/skills/writing-plans/SKILL.md | grep -q "## Documentation"` exits 0
  </acceptance_criteria>
  <done>PR #37 merged to main; forge/skills/silver-feature/SKILL.md contains PATH 10b doc-scheme gate and forge/skills/writing-plans/SKILL.md contains Documentation section.</done>
</task>

<task type="auto">
  <name>Task 2: Verify CI green on PR #38 then merge (silver-ui doc-scheme gate)</name>
  <files></files>
  <read_first>
    - Run: `/opt/homebrew/bin/gh pr checks 38 --repo alo-exp/silver-bullet` to confirm all checks pass
    - Run: `/opt/homebrew/bin/gh pr view 38 --repo alo-exp/silver-bullet --json mergeable,state` to confirm MERGEABLE
  </read_first>
  <action>
1. Verify PR #38 CI is green:
   `/opt/homebrew/bin/gh pr checks 38 --repo alo-exp/silver-bullet`
   All checks must show `pass`. If any check shows `fail` or `pending`, STOP and report.

2. Verify PR #38 is in MERGEABLE state:
   `/opt/homebrew/bin/gh pr view 38 --repo alo-exp/silver-bullet --json mergeable,state`
   Must return `"mergeable":"MERGEABLE"` and `"state":"OPEN"`. Note: at planning time PR #38 reported `"mergeable":"UNKNOWN"` — this may be transient. If it still shows UNKNOWN, wait 30 seconds and re-check. If it shows CONFLICTING, investigate and resolve conflicts.

3. Merge PR #38 using squash merge:
   `/opt/homebrew/bin/gh pr merge 38 --repo alo-exp/silver-bullet --squash --subject "fix(skills): add doc-scheme compliance gate to both silver-ui variants"`

4. After merge, verify the changes are present on main:
   `git fetch origin && git log origin/main --oneline -3`

5. Verify the doc-scheme gate is present in the merged files:
   `git show origin/main:skills/silver-ui/SKILL.md | grep -c "Step 13b"`
   `git show origin/main:forge/skills/silver-ui/SKILL.md | grep -c "PATH 10b"`
   Both must output a number greater than 0.
  </action>
  <verify>
    <automated>/opt/homebrew/bin/gh pr view 38 --repo alo-exp/silver-bullet --json state | jq -r .state | grep -q 'MERGED' && echo "PASS: PR #38 merged" || echo "FAIL: PR #38 not merged"</automated>
    <automated>git fetch origin 2>/dev/null; git show origin/main:skills/silver-ui/SKILL.md 2>/dev/null | grep -q "Step 13b" && echo "PASS: Step 13b present on main" || echo "FAIL: Step 13b not found"</automated>
    <automated>git fetch origin 2>/dev/null; git show origin/main:forge/skills/silver-ui/SKILL.md 2>/dev/null | grep -q "PATH 10b" && echo "PASS: PATH 10b present in forge silver-ui on main" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `/opt/homebrew/bin/gh pr view 38 --repo alo-exp/silver-bullet --json state | jq -r .state` outputs `MERGED`
    - `git show origin/main:skills/silver-ui/SKILL.md | grep -c "Step 13b"` outputs `1` or more
    - `git show origin/main:forge/skills/silver-ui/SKILL.md | grep -c "PATH 10b"` outputs `1` or more
  </acceptance_criteria>
  <done>PR #38 merged to main; skills/silver-ui/SKILL.md contains Step 13b doc-scheme gate and forge/skills/silver-ui/SKILL.md contains PATH 10b doc-scheme gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Both PR #37 and PR #38 have been merged to main. The doc-scheme compliance gate is now present in all four skill files: forge/skills/silver-feature/SKILL.md (PATH 10b), forge/skills/writing-plans/SKILL.md (## Documentation), skills/silver-ui/SKILL.md (Step 13b), forge/skills/silver-ui/SKILL.md (PATH 10b).</what-built>
  <how-to-verify>
    1. Visit https://github.com/alo-exp/silver-bullet/pulls to confirm both PRs show as MERGED (purple "Merged" badge)
    2. Visit https://github.com/alo-exp/silver-bullet/commits/main and confirm the two merge commits appear at the top
    3. Optionally: run `git pull origin main && grep "Step 13b" skills/silver-ui/SKILL.md` to confirm locally
  </how-to-verify>
  <resume-signal>Type "approved" once both PRs show as Merged on GitHub, or describe any issue encountered</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR branch → main | Both branches have passing CI; squash merge creates a clean commit history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-044-07 | Tampering | Merging open PRs | mitigate | CI must be verified green before merge; mergeable state checked before gh pr merge command runs |
</threat_model>

<verification>
After both PRs are merged:
1. `git pull origin main && grep -l "Step 13b\|PATH 10b" skills/silver-ui/SKILL.md forge/skills/silver-ui/SKILL.md forge/skills/silver-feature/SKILL.md`
   All three files should appear in the output.
2. Confirm both PRs closed: `/opt/homebrew/bin/gh pr list --repo alo-exp/silver-bullet --state open`
   Neither PR #37 nor #38 should appear.
</verification>

<success_criteria>
- PR #37 state = MERGED on GitHub
- PR #38 state = MERGED on GitHub
- `git show origin/main:skills/silver-ui/SKILL.md | grep "Step 13b"` exits 0
- `git show origin/main:forge/skills/silver-feature/SKILL.md | grep "PATH 10b"` exits 0
- `git show origin/main:forge/skills/silver-ui/SKILL.md | grep "PATH 10b"` exits 0
- No open PRs remaining for these changes
</success_criteria>

<output>
After completion, create `.planning/phases/044-session-stability-bugs/044-04-SUMMARY.md`
</output>
