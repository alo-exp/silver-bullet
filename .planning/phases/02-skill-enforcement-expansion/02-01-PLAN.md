---
phase: 02-skill-enforcement-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/workflows/full-dev-cycle.md
  - templates/workflows/full-dev-cycle.md
  - .silver-bullet.json
autonomous: true
requirements: [SB-R2]

must_haves:
  truths:
    - "full-dev-cycle DISCUSS step contains `/accessibility-review` in the UI work conditional with REQUIRED when UI work marker"
    - "full-dev-cycle EXECUTE step 6 contains a `/test-driven-development` sub-step with REQUIRED marker before wave dispatch"
    - "full-dev-cycle FINALIZATION step 14 invokes `/tech-debt` skill (not inline manual prose) with REQUIRED marker"
    - ".silver-bullet.json all_tracked contains test-driven-development, tech-debt, accessibility-review, incident-response"
    - ".silver-bullet.json required_deploy contains test-driven-development and tech-debt"
    - "templates/workflows/full-dev-cycle.md is identical to docs/workflows/full-dev-cycle.md"
  artifacts:
    - path: "docs/workflows/full-dev-cycle.md"
      provides: "Updated full dev cycle workflow with 3 new skill gates"
      contains: "/test-driven-development"
    - path: "templates/workflows/full-dev-cycle.md"
      provides: "Template mirror of docs/ full-dev-cycle"
      contains: "/test-driven-development"
    - path: ".silver-bullet.json"
      provides: "Updated enforcement config with 4 new skills"
      contains: "test-driven-development"
  key_links:
    - from: "docs/workflows/full-dev-cycle.md"
      to: ".silver-bullet.json"
      via: "all_tracked enforcement"
      pattern: "test-driven-development"
    - from: "templates/workflows/full-dev-cycle.md"
      to: "docs/workflows/full-dev-cycle.md"
      via: "must be identical mirror"
      pattern: "test-driven-development"
---

<objective>
Apply three skill enforcement additions to the full-dev-cycle workflow (docs + template mirror) and update .silver-bullet.json to track and enforce all four new skills.

Purpose: Close the enforcement gap for test-driven-development (EXECUTE), tech-debt (FINALIZATION), and accessibility-review (DISCUSS UI conditional) in the full dev cycle. Update .silver-bullet.json so completion-audit.sh can block commits when TDD or tech-debt are skipped.

Output: Updated docs/workflows/full-dev-cycle.md, templates/workflows/full-dev-cycle.md, and .silver-bullet.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-skill-enforcement-expansion/02-CONTEXT.md
@docs/specs/2026-04-05-skill-enforcement-expansion-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update full-dev-cycle.md — three skill insertions</name>
  <files>docs/workflows/full-dev-cycle.md</files>

  <read_first>
    - docs/workflows/full-dev-cycle.md (full file — must see current state before editing)
    - .planning/phases/02-skill-enforcement-expansion/02-CONTEXT.md (locked decisions)
    - docs/specs/2026-04-05-skill-enforcement-expansion-design.md (exact text spec)
  </read_first>

  <action>
Make three targeted edits to docs/workflows/full-dev-cycle.md:

**Edit 1 — DISCUSS, UI conditional (step 3)**

Find this exact line:
```
   - If this phase involves **UI work**: `/design-system` + `/ux-copy`
```

Replace it with these two lines:
```
   - If this phase involves **UI work**: `/design-system` + `/ux-copy` + `/accessibility-review`
     (WCAG 2.1 AA audit against the phase's UI deliverables)   **REQUIRED when UI work** ← DO NOT SKIP
```

Note: The indentation on the second line is 5 spaces (to align under the open paren), consistent with existing continuation lines in the file.

**Edit 2 — EXECUTE (step 6)**

Find this exact block:
```
6. `/gsd:execute-phase` — Wave-based parallel execution with atomic commit per task. **REQUIRED** ← DO NOT SKIP
   TDD principles apply per task within GSD execution.
   → Produces: atomic git commits (one per task), `.planning/{phase}-{N}-SUMMARY.md`
```

Replace it with:
```
6. `/gsd:execute-phase` — Wave-based parallel execution with atomic commit per task. **REQUIRED** ← DO NOT SKIP

   `/test-driven-development` — Before writing any implementation code: establish      **REQUIRED** ← DO NOT SKIP
   red-green-refactor discipline. Write the failing test first, make it pass,
   then refactor. TDD applies per task within each GSD wave.

   → Produces: atomic git commits (one per task), `.planning/{phase}-{N}-SUMMARY.md`
```

Note: The `/test-driven-development` sub-step is indented 3 spaces (matching existing sub-step indentation in the file). The `**REQUIRED** ← DO NOT SKIP` marker is right-aligned to approximately column 80 using spaces, consistent with existing required markers. Remove the old "TDD principles apply per task within GSD execution." prose line — it is replaced by the skill invocation.

**Edit 3 — FINALIZATION (step 14)**

Find this exact block:
```
14. **Tech-debt notes** (inline) — Append identified debt to `docs/tech-debt.md`.
    Format: `| Item | Severity | Effort | Phase introduced |`. Create file if needed.
```

Replace it with:
```
14. `/tech-debt` — Identify, categorize, and prioritize technical debt introduced    **REQUIRED** ← DO NOT SKIP
    or surfaced during this work. Append structured items to `docs/tech-debt.md`.
    Format: `| Item | Severity | Effort | Phase introduced |`. Create file if needed.
```

Note: Step numbering (14) stays the same. The `**REQUIRED** ← DO NOT SKIP` marker is right-aligned to approximately column 80.
  </action>

  <verify>
    <automated>grep -n '/accessibility-review' /Users/shafqat/Documents/Projects/silver-bullet/docs/workflows/full-dev-cycle.md && grep -n '/test-driven-development' /Users/shafqat/Documents/Projects/silver-bullet/docs/workflows/full-dev-cycle.md && grep -n '/tech-debt' /Users/shafqat/Documents/Projects/silver-bullet/docs/workflows/full-dev-cycle.md</automated>
  </verify>

  <acceptance_criteria>
    - docs/workflows/full-dev-cycle.md contains the string `/accessibility-review`
    - docs/workflows/full-dev-cycle.md contains the string `REQUIRED when UI work`
    - docs/workflows/full-dev-cycle.md contains the string `/test-driven-development`
    - docs/workflows/full-dev-cycle.md contains the string `red-green-refactor discipline`
    - docs/workflows/full-dev-cycle.md contains the string `/tech-debt`
    - docs/workflows/full-dev-cycle.md does NOT contain `**Tech-debt notes** (inline)`
    - docs/workflows/full-dev-cycle.md does NOT contain `TDD principles apply per task within GSD execution.`
  </acceptance_criteria>

  <done>Three skill gates present in docs/workflows/full-dev-cycle.md. Old inline prose removed. REQUIRED markers on TDD and tech-debt.</done>
</task>

<task type="auto">
  <name>Task 2: Update .silver-bullet.json — add 4 skills to all_tracked, 2 to required_deploy</name>
  <files>.silver-bullet.json</files>

  <read_first>
    - .silver-bullet.json (full file — must see current arrays before editing)
    - .planning/phases/02-skill-enforcement-expansion/02-CONTEXT.md (locked decisions for required_deploy)
  </read_first>

  <action>
Make two targeted edits to .silver-bullet.json:

**Edit 1 — required_deploy array**

Find the current `required_deploy` array (ends with `"verification-before-completion"`). Add `"test-driven-development"` and `"tech-debt"` to it.

The updated array must be:
```json
"required_deploy": [
  "quality-gates",
  "code-review", "requesting-code-review", "receiving-code-review",
  "testing-strategy", "documentation",
  "finishing-a-development-branch", "deploy-checklist",
  "create-release",
  "verification-before-completion",
  "test-driven-development", "tech-debt"
],
```

**Edit 2 — all_tracked array**

Find the current `all_tracked` array (ends with `"verification-before-completion"`). Add the four new skills at the end.

The new entries to append (before the closing `]`):
```json
"test-driven-development", "tech-debt", "accessibility-review", "incident-response"
```

So the last few entries of `all_tracked` will be:
```json
      "forensics", "using-silver-bullet",
      "verification-before-completion",
      "test-driven-development", "tech-debt", "accessibility-review", "incident-response"
```

Preserve all existing formatting, indentation, and whitespace. Do not reformat the file.
  </action>

  <verify>
    <automated>node -e "const j=require('/Users/shafqat/Documents/Projects/silver-bullet/.silver-bullet.json'); console.log('all_tracked:', j.skills.all_tracked.includes('test-driven-development'), j.skills.all_tracked.includes('tech-debt'), j.skills.all_tracked.includes('accessibility-review'), j.skills.all_tracked.includes('incident-response')); console.log('required_deploy:', j.skills.required_deploy.includes('test-driven-development'), j.skills.required_deploy.includes('tech-debt'))"</automated>
  </verify>

  <acceptance_criteria>
    - .silver-bullet.json is valid JSON (node -e "require('./.silver-bullet.json')" exits 0)
    - .silver-bullet.json skills.all_tracked contains "test-driven-development"
    - .silver-bullet.json skills.all_tracked contains "tech-debt"
    - .silver-bullet.json skills.all_tracked contains "accessibility-review"
    - .silver-bullet.json skills.all_tracked contains "incident-response"
    - .silver-bullet.json skills.required_deploy contains "test-driven-development"
    - .silver-bullet.json skills.required_deploy contains "tech-debt"
    - .silver-bullet.json skills.required_deploy does NOT contain "accessibility-review"
    - .silver-bullet.json skills.required_deploy does NOT contain "incident-response"
  </acceptance_criteria>

  <done>All 4 new skills in all_tracked; test-driven-development and tech-debt in required_deploy; JSON valid; accessibility-review and incident-response absent from required_deploy (conditional skills).</done>
</task>

<task type="auto">
  <name>Task 3: Mirror full-dev-cycle changes to templates/workflows/full-dev-cycle.md</name>
  <files>templates/workflows/full-dev-cycle.md</files>

  <read_first>
    - templates/workflows/full-dev-cycle.md (full file — confirm it is the same as docs/ before editing)
    - docs/workflows/full-dev-cycle.md (already updated in Task 1 — use as source of truth)
  </read_first>

  <action>
Apply the identical three edits from Task 1 to templates/workflows/full-dev-cycle.md.

Verify first that the template is currently identical to docs/workflows/full-dev-cycle.md (it should be — both were read and confirmed identical). If any diff exists beyond the three changes, do NOT overwrite silently — document the discrepancy in the task summary.

**Edit 1 — DISCUSS, UI conditional (step 3):** Same as Task 1 Edit 1.

**Edit 2 — EXECUTE (step 6):** Same as Task 1 Edit 2.

**Edit 3 — FINALIZATION (step 14):** Same as Task 1 Edit 3.

After applying all three edits, the template must be byte-for-byte identical to docs/workflows/full-dev-cycle.md.
  </action>

  <verify>
    <automated>diff /Users/shafqat/Documents/Projects/silver-bullet/docs/workflows/full-dev-cycle.md /Users/shafqat/Documents/Projects/silver-bullet/templates/workflows/full-dev-cycle.md && echo "IDENTICAL"</automated>
  </verify>

  <acceptance_criteria>
    - templates/workflows/full-dev-cycle.md contains `/accessibility-review`
    - templates/workflows/full-dev-cycle.md contains `REQUIRED when UI work`
    - templates/workflows/full-dev-cycle.md contains `/test-driven-development`
    - templates/workflows/full-dev-cycle.md contains `/tech-debt`
    - templates/workflows/full-dev-cycle.md does NOT contain `**Tech-debt notes** (inline)`
    - diff docs/workflows/full-dev-cycle.md templates/workflows/full-dev-cycle.md exits 0 (files are identical)
  </acceptance_criteria>

  <done>templates/workflows/full-dev-cycle.md is identical to docs/workflows/full-dev-cycle.md with all three skill gates present.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `grep -n '/accessibility-review\|REQUIRED when UI work\|/test-driven-development\|/tech-debt' docs/workflows/full-dev-cycle.md` — must show all 4 patterns
2. `diff docs/workflows/full-dev-cycle.md templates/workflows/full-dev-cycle.md` — must exit 0 (no diff)
3. `node -e "const j=require('./.silver-bullet.json'); console.log(j.skills.all_tracked.slice(-4), j.skills.required_deploy.slice(-2))"` — must show the 4 new tracked + 2 required_deploy entries
4. `node -e "require('./.silver-bullet.json')"` — must exit 0 (valid JSON)
</verification>

<success_criteria>
- docs/workflows/full-dev-cycle.md: `/accessibility-review` in DISCUSS UI conditional with `REQUIRED when UI work` marker; `/test-driven-development` sub-step in EXECUTE with `REQUIRED` marker; `/tech-debt` in FINALIZATION step 14 replacing inline prose, with `REQUIRED` marker
- templates/workflows/full-dev-cycle.md: byte-for-byte identical to docs/ counterpart
- .silver-bullet.json: 4 new skills in all_tracked, test-driven-development and tech-debt in required_deploy, JSON valid
</success_criteria>

<output>
After completion, create `.planning/phases/02-skill-enforcement-expansion/02-01-SUMMARY.md`
</output>
