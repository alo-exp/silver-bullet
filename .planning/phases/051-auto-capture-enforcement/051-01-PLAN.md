---
phase: 051-auto-capture-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - silver-bullet.md
  - templates/silver-bullet.md.base
autonomous: true
requirements:
  - CAPT-01
  - CAPT-03

must_haves:
  truths:
    - "silver-bullet.md §3b contains subsection '### 3b-i. Deferred-Item Capture' with a /silver-add invocation instruction and a classification rubric (issue vs backlog, default=backlog, minimum bar)"
    - "silver-bullet.md §3b contains subsection '### 3b-ii. Knowledge and Lessons Capture' with a /silver-rem invocation instruction and routing guidance (knowledge vs lessons, default=knowledge)"
    - "templates/silver-bullet.md.base §3b is byte-for-byte identical to silver-bullet.md §3b — both files are updated in ONE git commit"
  artifacts:
    - path: "silver-bullet.md"
      provides: "Updated §3b with 3b-i and 3b-ii subsections for deferred-item and knowledge/lessons capture"
      contains: "3b-i. Deferred-Item Capture"
    - path: "templates/silver-bullet.md.base"
      provides: "Template §3b identical to live file — same 3b-i and 3b-ii subsections"
      contains: "3b-i. Deferred-Item Capture"
  key_links:
    - from: "silver-bullet.md §3b-i"
      to: "skills/silver-add/SKILL.md"
      via: "Skill(skill=\"silver-add\", args=\"<description of deferred item>\")"
      pattern: "silver-add"
    - from: "silver-bullet.md §3b-ii"
      to: "skills/silver-rem/SKILL.md"
      via: "Skill(skill=\"silver-rem\", args=\"<insight or lesson text>\")"
      pattern: "silver-rem"
---

<objective>
Add two mandatory capture instruction subsections to §3b in both silver-bullet.md and templates/silver-bullet.md.base in ONE atomic git commit.

§3b-i (CAPT-01): instructs the coding agent to call /silver-add for every deferred, skipped, or identified work item during execution, including a complete classification rubric (issue vs backlog, default=backlog, minimum bar).

§3b-ii (CAPT-03): instructs the coding agent to call /silver-rem for every architectural insight, key decision, or portable lesson observed during execution.

NON-NEGOTIABLE: Both files must be staged and committed together. This is enforced by the §3 "must-not" violations list in silver-bullet.md which prohibits updating silver-bullet.md without updating the template atomically.
</objective>

<execution_context>
@/Users/shafqat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shafqat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/shafqat/Documents/Projects/silver-bullet/.planning/PROJECT.md
@/Users/shafqat/Documents/Projects/silver-bullet/.planning/ROADMAP.md
@/Users/shafqat/Documents/Projects/silver-bullet/.planning/STATE.md

<interfaces>
<!-- Exact anchor text in silver-bullet.md §3b (verified line 528-530): -->

Current §3b ending:
```
> **Anti-Skip:** You are violating this rule if you invoke a GSD command outside the Skill tool. Markers are recorded only by the PostToolUse:Skill hook — there is no other recording mechanism, and manual state writes are blocked.

## 3c. Completion Claim Verification
```

The new §3b-i and §3b-ii blocks go BETWEEN the Anti-Skip blockquote and the `## 3c.` heading.

Content to insert (VERBATIM — do not alter wording, formatting, or code fences):

```markdown

### 3b-i. Deferred-Item Capture (mandatory, all sessions)

During execution, any item that is skipped, descoped, deferred, or identified for future work MUST be filed via `/silver-add` **immediately** — not at session end:

```
Skill(skill="silver-add", args="<description of deferred item>")
```

**Classification rubric:**
- **Issue** — broken behavior, crash, regression, test failure, blocking open question, unfinished work left in broken/incomplete state, verification failure
- **Backlog** — feature request deferred to future milestone, tech debt (known shortcut, hardcoded value, missing abstraction), housekeeping, informational open question, advisory review finding not addressed now

**Default when ambiguous:** classify as backlog — do not over-alarm with issues.
**Minimum bar:** item must have distinct user-visible impact OR block future work OR represent a conscious deferred decision. Do not file transient exploration notes or items already addressed in this session.

> **Anti-Skip:** You are violating this rule if you identify a deferred item and do not invoke `/silver-add` before moving to the next task.

### 3b-ii. Knowledge and Lessons Capture (mandatory, all sessions)

During execution, any architectural insight, key decision, project-local gotcha, recurring pattern, or portable lesson observed MUST be captured via `/silver-rem`:

```
Skill(skill="silver-rem", args="<insight or lesson text>")
```

**Route:**
- Insight references THIS project (architectural decision, project-local gotcha, key decision, recurring pattern, open question for this project) → **knowledge**
- Insight is portable across projects (stack behavior, good practice, anti-pattern, process insight) → **lessons**

**Default when ambiguous:** classify as knowledge.

> **Anti-Skip:** You are violating this rule if you observe a valuable insight during execution and do not invoke `/silver-rem` before the session ends.

```

The template (templates/silver-bullet.md.base) has the SAME §3b structure with the same anchor text. Apply the identical insertion.

ATOMIC COMMIT requirement: After both edits are written, stage BOTH files (`git add silver-bullet.md templates/silver-bullet.md.base`) and commit them in a SINGLE commit before any other file changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add §3b-i and §3b-ii to silver-bullet.md</name>
  <read_first>
    - silver-bullet.md lines 508-535 (verify §3b anchor text and §3c heading position before editing)
    - .planning/REQUIREMENTS.md lines 38-43 (CAPT-01 and CAPT-03 requirement text)
  </read_first>
  <files>silver-bullet.md</files>
  <action>
Read silver-bullet.md around lines 508-535 to confirm the exact anchor text. The edit inserts the §3b-i and §3b-ii content block (from the interfaces section above) between the existing Anti-Skip blockquote (ending `...no other recording mechanism, and manual state writes are blocked.`) and the `## 3c. Completion Claim Verification` heading.

Use the Edit tool. Replace the exact text:
```
...and manual state writes are blocked.

## 3c. Completion Claim Verification
```

With:
```
...and manual state writes are blocked.

### 3b-i. Deferred-Item Capture (mandatory, all sessions)

During execution, any item that is skipped, descoped, deferred, or identified for future work MUST be filed via `/silver-add` **immediately** — not at session end:

```
Skill(skill="silver-add", args="<description of deferred item>")
```

**Classification rubric:**
- **Issue** — broken behavior, crash, regression, test failure, blocking open question, unfinished work left in broken/incomplete state, verification failure
- **Backlog** — feature request deferred to future milestone, tech debt (known shortcut, hardcoded value, missing abstraction), housekeeping, informational open question, advisory review finding not addressed now

**Default when ambiguous:** classify as backlog — do not over-alarm with issues.
**Minimum bar:** item must have distinct user-visible impact OR block future work OR represent a conscious deferred decision. Do not file transient exploration notes or items already addressed in this session.

> **Anti-Skip:** You are violating this rule if you identify a deferred item and do not invoke `/silver-add` before moving to the next task.

### 3b-ii. Knowledge and Lessons Capture (mandatory, all sessions)

During execution, any architectural insight, key decision, project-local gotcha, recurring pattern, or portable lesson observed MUST be captured via `/silver-rem`:

```
Skill(skill="silver-rem", args="<insight or lesson text>")
```

**Route:**
- Insight references THIS project (architectural decision, project-local gotcha, key decision, recurring pattern, open question for this project) → **knowledge**
- Insight is portable across projects (stack behavior, good practice, anti-pattern, process insight) → **lessons**

**Default when ambiguous:** classify as knowledge.

> **Anti-Skip:** You are violating this rule if you observe a valuable insight during execution and do not invoke `/silver-rem` before the session ends.

## 3c. Completion Claim Verification
```

After the edit, verify the file contains both `### 3b-i.` and `### 3b-ii.` strings.
  </action>
  <verify>
    <automated>grep -q "3b-i. Deferred-Item Capture" silver-bullet.md && grep -q "3b-ii. Knowledge and Lessons Capture" silver-bullet.md && grep -q "silver-add" silver-bullet.md && grep -q "silver-rem" silver-bullet.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "3b-i. Deferred-Item Capture" silver-bullet.md` exits 0
    - `grep -q "3b-ii. Knowledge and Lessons Capture" silver-bullet.md` exits 0
    - `grep -q "Skill(skill=\"silver-add\"" silver-bullet.md` exits 0
    - `grep -q "Skill(skill=\"silver-rem\"" silver-bullet.md` exits 0
    - `grep -q "classify as backlog" silver-bullet.md` exits 0
    - `grep -q "classify as knowledge" silver-bullet.md` exits 0
    - `grep -q "Anti-Skip.*silver-add" silver-bullet.md` exits 0
    - `grep -q "Anti-Skip.*silver-rem" silver-bullet.md` exits 0
    - `grep -q "## 3c. Completion Claim Verification" silver-bullet.md` exits 0 (§3c still present)
    - `grep -q "## 3b. GSD Command Tracking" silver-bullet.md` exits 0 (existing §3b header still present)
  </acceptance_criteria>
  <done>silver-bullet.md §3b now contains both 3b-i (deferred-item capture with /silver-add and classification rubric) and 3b-ii (knowledge/lessons capture with /silver-rem and routing guidance). The existing GSD Command Tracking table and Anti-Skip note are intact. §3c is still present immediately after.</done>
</task>

<task type="auto">
  <name>Task 2: Apply identical edit to templates/silver-bullet.md.base and commit both atomically</name>
  <read_first>
    - templates/silver-bullet.md.base lines 505-535 (verify §3b anchor text — same content as live file, confirmed by research)
  </read_first>
  <files>templates/silver-bullet.md.base</files>
  <action>
Apply the IDENTICAL edit to templates/silver-bullet.md.base. The anchor text is the same as silver-bullet.md (confirmed by RESEARCH.md — §3b is verbatim identical in both files).

Use the Edit tool. Replace the same anchor text (Anti-Skip blockquote ending + `## 3c.` heading) with the same expanded content block containing §3b-i and §3b-ii.

After both Task 1 and Task 2 edits are complete, commit BOTH files in ONE atomic git commit:

```bash
cd /Users/shafqat/Documents/Projects/silver-bullet
git add silver-bullet.md templates/silver-bullet.md.base
git commit -m "feat(enforcement): add §3b-i deferred-item and §3b-ii knowledge capture instructions

CAPT-01: /silver-add invocation with classification rubric (issue vs backlog, default=backlog)
CAPT-03: /silver-rem invocation with knowledge/lessons routing (default=knowledge)

Both files updated atomically per §3 template-parity constraint."
```

CRITICAL: Do NOT commit silver-bullet.md alone. Both files must appear in the same commit. Verify with `git show --stat HEAD` that both files appear.
  </action>
  <verify>
    <automated>grep -q "3b-i. Deferred-Item Capture" templates/silver-bullet.md.base && grep -q "3b-ii. Knowledge and Lessons Capture" templates/silver-bullet.md.base && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "3b-i. Deferred-Item Capture" templates/silver-bullet.md.base` exits 0
    - `grep -q "3b-ii. Knowledge and Lessons Capture" templates/silver-bullet.md.base` exits 0
    - `grep -q "Skill(skill=\"silver-add\"" templates/silver-bullet.md.base` exits 0
    - `grep -q "Skill(skill=\"silver-rem\"" templates/silver-bullet.md.base` exits 0
    - `grep -q "## 3b. GSD Command Tracking" templates/silver-bullet.md.base` exits 0 (existing header intact)
    - `git show --stat HEAD` includes both "silver-bullet.md" and "templates/silver-bullet.md.base" (atomic commit verified)
  </acceptance_criteria>
  <done>templates/silver-bullet.md.base §3b contains the same 3b-i and 3b-ii subsections as the live file. Both files committed in one atomic commit. Template parity constraint satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit tool → markdown files | §3b edit is prose only — no shell-executable content added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-051-01 | Tampering | Template drift | mitigate | Atomic commit enforced — both files staged together |
| T-051-02 | Tampering | §3b-i code fence confuses Claude | accept | Code fence content is a Skill() invocation string — not a shell command; clearly labelled |
</threat_model>

<verification>
After plan execution:
1. `grep -q "3b-i. Deferred-Item Capture" silver-bullet.md` — CAPT-01 present in live file
2. `grep -q "3b-ii. Knowledge and Lessons Capture" silver-bullet.md` — CAPT-03 present in live file
3. `grep -q "3b-i. Deferred-Item Capture" templates/silver-bullet.md.base` — CAPT-01 present in template
4. `grep -q "3b-ii. Knowledge and Lessons Capture" templates/silver-bullet.md.base` — CAPT-03 present in template
5. `git show --stat HEAD | grep -c "silver-bullet.md\|silver-bullet.md.base"` — equals 2 (atomic commit)
6. `grep -q "## 3b. GSD Command Tracking" silver-bullet.md` — existing §3b title intact
7. `grep -q "## 3c. Completion Claim Verification" silver-bullet.md` — §3c not displaced
</verification>

<success_criteria>
- silver-bullet.md §3b contains 3b-i with /silver-add instruction, classification rubric (issue/backlog/ambiguous=backlog), minimum bar, and Anti-Skip note
- silver-bullet.md §3b contains 3b-ii with /silver-rem instruction, knowledge/lessons routing (ambiguous=knowledge), and Anti-Skip note
- templates/silver-bullet.md.base §3b contains identical 3b-i and 3b-ii content
- Both files appear in one atomic git commit (template-parity constraint satisfied)
- No existing §3b content (GSD Command Tracking table, original Anti-Skip note) was removed
- CAPT-01 satisfied: coding agent has explicit /silver-add instruction with rubric
- CAPT-03 satisfied: coding agent has explicit /silver-rem instruction with routing
</success_criteria>

<output>
After completion, create `.planning/phases/051-auto-capture-enforcement/051-01-SUMMARY.md` using the summary template at @/Users/shafqat/.claude/get-shit-done/templates/summary.md
</output>
