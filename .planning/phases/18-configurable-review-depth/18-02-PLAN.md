---
phase: 18-configurable-review-depth
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - skills/artifact-reviewer/rules/reviewer-interface.md
  - skills/artifact-reviewer/SKILL.md
autonomous: true
requirements: [ARVW-11d, ARVW-11f]

must_haves:
  truths:
    - "Reviewer interface defines the check_mode parameter and structural-only behavior"
    - "Reviewers receiving check_mode=structural only validate section presence and format, skipping content quality checks"
    - "SKILL.md documents the review_depth configuration and depth modes"
  artifacts:
    - path: "skills/artifact-reviewer/rules/reviewer-interface.md"
      provides: "check_mode parameter contract for structural vs full review"
      contains: "check_mode"
    - path: "skills/artifact-reviewer/SKILL.md"
      provides: "Updated orchestrator documentation with depth configuration"
      contains: "review_depth"
  key_links:
    - from: "skills/artifact-reviewer/rules/reviewer-interface.md"
      to: "skills/artifact-reviewer/rules/review-loop.md"
      via: "check_mode parameter passed from loop to reviewer"
      pattern: "check_mode"
---

<objective>
Update the reviewer interface contract to support the check_mode parameter (structural vs full) and update the artifact-reviewer SKILL.md to document depth configuration.

Purpose: Reviewers need to know when they are in structural-only mode so they skip content quality checks. The orchestrator skill needs documentation so users know how to configure review depth.
Output: Updated reviewer-interface.md with check_mode contract, updated SKILL.md with depth documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/artifact-reviewer/rules/reviewer-interface.md
@skills/artifact-reviewer/rules/review-loop.md
@skills/artifact-reviewer/SKILL.md
@.planning/phases/18-configurable-review-depth/18-01-SUMMARY.md
</context>

<interfaces>
<!-- From review-loop.md (after Plan 01 updates): -->
<!-- invoke_reviewer(artifact_path, source_inputs, check_mode) -->
<!-- check_mode: "full" | "structural" -->
<!-- resolve_depth(reviewer_skill_name) returns "deep" | "standard" | "quick" -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add check_mode to reviewer interface contract</name>
  <files>skills/artifact-reviewer/rules/reviewer-interface.md</files>
  <action>
Update reviewer-interface.md to include the check_mode parameter and define structural-only review behavior:

1. **Update Input Contract** -- add `check_mode` as a new input field:
   ```
   check_mode: "full" | "structural"  — review depth mode (REQUIRED, defaults to "full" if not provided)
   ```
   Add it after the existing `review_context` field.

2. **Add a new section after "Output Contract"**: `## Check Mode Behavior`

   Content:
   ```markdown
   ## Check Mode Behavior

   The `check_mode` parameter controls which quality criteria a reviewer evaluates.

   ### full (default)
   Run ALL quality criteria defined by the reviewer. This is the standard behavior -- every QC check fires.

   ### structural
   Run ONLY structural quality criteria. Structural checks validate:
   - Required sections exist (correct headings present)
   - Document format is valid (frontmatter parseable, markdown well-formed)
   - Required fields are non-empty (not blank placeholders)

   Structural checks do NOT validate:
   - Content quality (e.g., "overview contains a real problem statement" -- QC-2 in review-spec)
   - Cross-reference consistency (e.g., "AC IDs match spec" -- unless it is a format check)
   - Semantic correctness (e.g., "user stories have all three parts")
   - Depth of coverage (e.g., "at least N items present")

   ### Reviewer Implementation Rule

   Each reviewer skill MUST tag its QC checks as either `structural` or `content`:
   - `structural`: Section presence, format validity, non-empty required fields
   - `content`: Quality, consistency, semantics, coverage depth

   When `check_mode == "structural"`, the reviewer MUST skip all `content`-tagged QC checks and only evaluate `structural`-tagged checks. A PASS in structural mode means "the artifact has correct structure" -- it does NOT assert content quality.
   ```

3. **Update Reviewer Responsibilities** -- add a bullet:
   - "Respect the `check_mode` parameter -- when `structural`, skip content quality checks and only validate structure and format"

4. Do NOT change the Output Contract or Finding Structure -- those remain the same regardless of check_mode. Findings from structural checks use the same PASS/ISSUES_FOUND/Finding schema.
  </action>
  <verify>
    <automated>grep -c "check_mode\|structural\|content.*tagged" skills/artifact-reviewer/rules/reviewer-interface.md | xargs test 3 -le</automated>
  </verify>
  <done>reviewer-interface.md defines check_mode input parameter with full/structural modes, tagging rules for QC checks, and skip behavior for structural mode</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md with depth configuration documentation</name>
  <files>skills/artifact-reviewer/SKILL.md</files>
  <action>
Update skills/artifact-reviewer/SKILL.md to document the review_depth configuration:

1. **Add a new section after "Orchestration Steps"**: `## Review Depth Configuration`

   Content:
   ```markdown
   ## Review Depth Configuration

   Review depth is configured per artifact type in `.planning/config.json`:

   ```json
   {
     "review_depth": {
       "review-spec": "deep",
       "review-roadmap": "quick",
       "gsd-plan-checker": "standard"
     }
   }
   ```

   ### Depth Levels

   | Depth | QC Checks | Required Clean Passes | Use When |
   |-------|-----------|----------------------|----------|
   | deep | Full (all QC) | 2 consecutive | High-stakes artifacts (SPEC, REQUIREMENTS) |
   | standard | Full (all QC) | 1 | Default -- good balance of rigor and speed |
   | quick | Structural only | 1 | Speed-critical reviews (CONTEXT, RESEARCH) |

   ### Defaults

   - If `review_depth` is absent from config.json: all artifacts use `standard`
   - If `review_depth` exists but has no entry for a reviewer: that reviewer uses `standard`
   - An empty `review_depth: {}` is equivalent to "all standard"
   ```

2. **Update "Orchestration Steps"** step 4 to mention depth:
   - Change step 4 from: "Run the review loop (defined in rules/review-loop.md)"
   - To: "Resolve review depth from `.planning/config.json` and run the review loop (defined in rules/review-loop.md) with the resolved depth"

3. **Update step 6** to be depth-aware:
   - Change from: "On 2 consecutive clean passes: clear state, report completion"
   - To: "On required consecutive clean passes (depth-dependent): clear state, report completion"
  </action>
  <verify>
    <automated>grep -c "review_depth\|deep\|standard\|quick\|Depth Levels" skills/artifact-reviewer/SKILL.md | xargs test 4 -le</automated>
  </verify>
  <done>SKILL.md documents review_depth config schema, depth levels table, default behavior, and updated orchestration steps</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer-interface -> individual reviewers | Contract change must be adopted by all reviewers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | T (Tampering) | check_mode parameter | mitigate | check_mode defaults to "full" if not provided -- reviewers cannot accidentally skip checks |
| T-18-04 | R (Repudiation) | structural-only review | mitigate | Audit trail records depth and check_mode per round so structural-only passes are traceable |
</threat_model>

<verification>
1. Read reviewer-interface.md and confirm check_mode is in the input contract
2. Confirm structural mode definition: section presence + format only, no content quality
3. Read SKILL.md and confirm depth levels table with correct pass counts
4. Confirm default depth documentation says "standard" when absent
</verification>

<success_criteria>
- reviewer-interface.md has check_mode parameter with full/structural behavior defined
- Each reviewer's QC checks are tagged as structural or content (tagging rule established)
- SKILL.md documents review_depth config with depth levels table and defaults
- Orchestration steps reference depth resolution
</success_criteria>

<output>
After completion, create `.planning/phases/18-configurable-review-depth/18-02-SUMMARY.md`
</output>
