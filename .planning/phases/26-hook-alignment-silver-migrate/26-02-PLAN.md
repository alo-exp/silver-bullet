---
phase: 26-hook-alignment-silver-migrate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/silver-migrate/SKILL.md
autonomous: true
requirements:
  - HOOK-06

must_haves:
  truths:
    - "silver:migrate scans STATE.md and .planning/ artifacts to infer path completion"
    - "silver:migrate generates WORKFLOW.md from workflow.md.base with pre-filled path statuses"
    - "silver:migrate presents the generated WORKFLOW.md for user confirmation before writing"
    - "silver:migrate commits WORKFLOW.md after user approval"
  artifacts:
    - path: "skills/silver-migrate/SKILL.md"
      provides: "Migration skill from legacy to composable paths"
      contains: "WORKFLOW.md"
      min_lines: 50
  key_links:
    - from: "skills/silver-migrate/SKILL.md"
      to: ".planning/STATE.md"
      via: "reads state to infer path completion"
      pattern: "STATE.md"
    - from: "skills/silver-migrate/SKILL.md"
      to: "templates/workflow.md.base"
      via: "uses template to generate WORKFLOW.md"
      pattern: "workflow.md.base"
---

<objective>
Create the silver:migrate skill that generates WORKFLOW.md for existing mid-milestone projects.

Purpose: Projects that started a milestone before composable paths was introduced need a migration path. silver:migrate infers which paths have been completed from existing artifacts, generates WORKFLOW.md, and lets the user confirm before applying.

Output: New skill file at skills/silver-migrate/SKILL.md.
</objective>

<execution_context>
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/workflows/execute-plan.md
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-hook-alignment-silver-migrate/26-CONTEXT.md
@templates/workflow.md.base
@docs/composable-paths-contracts.md
</context>

<interfaces>
<!-- Existing skill structure pattern (from any skill in skills/) -->
<!-- Skills are markdown files with structured sections that Claude reads as instructions -->
<!-- Key sections: title, when to use, prerequisites, steps, produces -->

From templates/workflow.md.base:
- Path Log table: `| # | Path | Status | Artifacts Produced | Exit Condition Met |`
- Heartbeat section: `Last-path:`, `Last-beat:`
- Next Path section
- Composition section: `Intent:`, `Composed:`, `Composer:`, `Mode:`

Path-to-artifact mapping (from docs/composable-paths-contracts.md):
- PATH 0 (BOOTSTRAP): produces PROJECT.md, ROADMAP.md, REQUIREMENTS.md, STATE.md
- PATH 1 (ORIENT): produces intel files in .planning/intel/
- PATH 2 (EXPLORE): produces scope/problem documentation
- PATH 3 (IDEATE): produces ADR, design-system, brainstorming docs
- PATH 4 (SPECIFY): produces SPEC.md
- PATH 5 (PLAN): produces CONTEXT.md, RESEARCH.md, PLAN.md
- PATH 6 (DESIGN CONTRACT): produces DESIGN.md, UI-SPEC.md
- PATH 7 (EXECUTE): produces SUMMARY.md files
- PATH 8 (UI QUALITY): produces UI-REVIEW.md
- PATH 9 (REVIEW): produces REVIEW.md files
- PATH 10 (SECURE): produces SECURITY.md
- PATH 11 (VERIFY): produces VERIFICATION.md
- PATH 12 (QUALITY GATE): produces quality-gate results
- PATH 13 (SHIP): produces PR/release
- PATH 14 (DEBUG): produces debug artifacts
- PATH 15 (DESIGN HANDOFF): produces design handoff docs
- PATH 16 (DOCUMENT): produces documentation updates
- PATH 17 (RELEASE): produces release artifacts
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create silver-migrate skill with artifact-to-path inference</name>
  <files>skills/silver-migrate/SKILL.md</files>
  <read_first>
    - skills/quality-gates/SKILL.md (example skill structure — follow same format)
    - templates/workflow.md.base (WORKFLOW.md template structure)
    - docs/composable-paths-contracts.md (path contracts for artifact mapping — read first 200 lines for PATH 0-7 contracts)
  </read_first>
  <action>
Per D-10, D-11, D-12, D-13: Create `skills/silver-migrate/SKILL.md` following existing skill file patterns.

**Skill structure:**

```markdown
# silver:migrate

> Migrates an existing mid-milestone project to composable paths by generating WORKFLOW.md from current artifact state.

## When to Use
- Project started a milestone before composable paths architecture (v0.20.0)
- .planning/ directory exists with STATE.md but no WORKFLOW.md
- User explicitly runs /silver:migrate

## Prerequisites
- .planning/STATE.md must exist
- .planning/WORKFLOW.md must NOT exist (if it exists, migration is unnecessary — inform user and exit)

## Steps

### Step 1: Scan Existing Artifacts
Read .planning/STATE.md to understand current phase and milestone state.

Scan for artifacts that indicate path completion. Use this artifact-to-path mapping:

| Path | Artifacts to Check | Inference Rule |
|------|-------------------|----------------|
| PATH 0 (BOOTSTRAP) | .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md | Complete if ALL four exist |
| PATH 1 (ORIENT) | .planning/intel/*.md, .planning/codebase/*.md | Complete if any intel or codebase file exists |
| PATH 2 (EXPLORE) | (no dedicated artifact) | Mark as "skipped" — cannot infer |
| PATH 3 (IDEATE) | docs/superpowers/specs/*.md, .planning/ADR-*.md | Complete if any matching file exists |
| PATH 4 (SPECIFY) | .planning/SPEC.md | Complete if file exists |
| PATH 5 (PLAN) | .planning/phases/*/CONTEXT.md, .planning/phases/*/PLAN.md | Complete if current phase has PLAN.md files |
| PATH 6 (DESIGN CONTRACT) | .planning/DESIGN.md, .planning/UI-SPEC.md | Complete if either exists |
| PATH 7 (EXECUTE) | .planning/phases/*/*-SUMMARY.md | Complete if current phase has SUMMARY.md files |
| PATH 8 (UI QUALITY) | .planning/UI-REVIEW.md | Complete if file exists |
| PATH 9 (REVIEW) | .planning/REVIEW.md, .planning/phases/*/REVIEW.md | Complete if any exists |
| PATH 10 (SECURE) | .planning/SECURITY.md | Complete if file exists |
| PATH 11 (VERIFY) | .planning/VERIFICATION.md | Complete if file exists |
| PATH 12 (QUALITY GATE) | (checked via state file) | Complete if quality-gates skill is in state file |
| PATH 13 (SHIP) | (checked via state file) | Complete if deploy-checklist or create-release in state file |
| PATH 14 (DEBUG) | (no dedicated artifact) | Mark as "not applicable" |
| PATH 15 (DESIGN HANDOFF) | (no dedicated artifact) | Mark as "not applicable" unless UI milestone |
| PATH 16 (DOCUMENT) | (checked via state file) | Complete if documentation skill in state file |
| PATH 17 (RELEASE) | (checked via state file) | Complete if create-release in state file |

### Step 2: Determine Composition
Based on the artifacts found, infer which paths are part of this project's composition:
- Always include: PATH 0, 1, 5, 7, 11, 13
- Include if artifacts exist: PATH 2, 3, 4, 6, 8, 9, 10, 14, 15, 16, 17
- Exclude paths with no artifacts and no state file markers (these were intentionally skipped)

### Step 3: Generate WORKFLOW.md
Read `templates/workflow.md.base` as the template.

Fill in:
- **Composition section:** Intent = "Migrated from legacy workflow", Composed = current ISO timestamp, Composer = "/silver:migrate", Mode = "interactive"
- **Path Log table:** For each path in the determined composition, add a row with inferred status ("complete", "in-progress", or "pending"). Use artifact existence from Step 1.
- **Heartbeat:** Set Last-path to the highest completed path number, Last-beat to current timestamp.
- **Next Path:** Set to the first path with status "pending" or "in-progress".

### Step 4: Present for Confirmation
Display the generated WORKFLOW.md content to the user. Highlight:
- Which paths were inferred as complete (and what artifacts proved it)
- Which paths were marked pending
- Which paths were excluded from the composition

Ask the user to confirm or adjust before writing.

### Step 5: Write and Commit
After user confirmation:
1. Write WORKFLOW.md to `.planning/WORKFLOW.md`
2. Commit with message: `docs(workflow): migrate to composable paths via silver:migrate`

## Produces
- `.planning/WORKFLOW.md` — populated with inferred path completion state

## Notes
- This is a one-time migration skill. Once WORKFLOW.md exists, the /silver composer manages it.
- If user disagrees with inferred statuses, they can adjust before confirmation.
- Paths marked "skipped" in the composition indicate they were intentionally not part of this project's workflow.
```
  </action>
  <verify>
    <automated>test -f skills/silver-migrate/SKILL.md && wc -l < skills/silver-migrate/SKILL.md | xargs test 50 -le && grep -q 'WORKFLOW.md' skills/silver-migrate/SKILL.md && grep -q 'STATE.md' skills/silver-migrate/SKILL.md && grep -q 'PATH 0' skills/silver-migrate/SKILL.md && grep -q 'confirm' skills/silver-migrate/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - test -f skills/silver-migrate/SKILL.md
    - wc -l skills/silver-migrate/SKILL.md shows >= 50 lines
    - grep -q 'silver:migrate' skills/silver-migrate/SKILL.md
    - grep -q 'WORKFLOW.md' skills/silver-migrate/SKILL.md
    - grep -q 'STATE.md' skills/silver-migrate/SKILL.md
    - grep -q 'workflow.md.base' skills/silver-migrate/SKILL.md
    - grep -q 'confirm' skills/silver-migrate/SKILL.md (user confirmation step)
    - grep -q 'PATH 0\|BOOTSTRAP' skills/silver-migrate/SKILL.md (artifact mapping)
    - grep -q 'PATH 7\|EXECUTE' skills/silver-migrate/SKILL.md (artifact mapping)
    - grep -q 'commit' skills/silver-migrate/SKILL.md (commit step)
  </acceptance_criteria>
  <done>silver:migrate skill exists with complete artifact-to-path mapping, WORKFLOW.md generation from template, user confirmation flow, and commit step. Skill file is >= 50 lines and references all key artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artifact scanning | Reads .planning/ files to infer state — files could be manually created |
| User confirmation | User reviews generated WORKFLOW.md before it is written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Spoofing | Artifact presence | accept | An attacker who can create files in .planning/ already has write access to the project. User confirmation step catches incorrect inferences. |
| T-26-05 | Information Disclosure | STATE.md reading | accept | STATE.md is a project-local file, no sensitive data beyond workflow state. |
</threat_model>

<verification>
- skills/silver-migrate/SKILL.md exists with >= 50 lines
- Skill contains artifact-to-path mapping for all 18 paths
- Skill includes user confirmation before writing WORKFLOW.md
- Skill references workflow.md.base template
</verification>

<success_criteria>
- silver:migrate skill created with full artifact scanning, WORKFLOW.md generation, user confirmation, and commit workflow
</success_criteria>

<output>
After completion, create `.planning/phases/26-hook-alignment-silver-migrate/26-02-SUMMARY.md`
</output>
