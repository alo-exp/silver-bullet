---
phase: 26-hook-alignment-silver-migrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/dev-cycle-check.sh
  - hooks/completion-audit.sh
  - hooks/compliance-status.sh
  - hooks/prompt-reminder.sh
  - hooks/spec-floor-check.sh
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
  - HOOK-03
  - HOOK-04
  - HOOK-05

must_haves:
  truths:
    - "dev-cycle-check.sh checks WORKFLOW.md path completion before falling back to legacy skill markers"
    - "completion-audit.sh checks WORKFLOW.md path completion before falling back to legacy skill markers"
    - "compliance-status.sh shows PATH X/Y progress when WORKFLOW.md exists"
    - "compliance-status.sh shows PATH: N/A (legacy mode) when WORKFLOW.md absent"
    - "prompt-reminder.sh includes current path name and next path in additionalContext when WORKFLOW.md exists"
    - "spec-floor-check.sh downgrades to advisory when WORKFLOW.md exists and PATH 4 is not in the composition"
  artifacts:
    - path: "hooks/dev-cycle-check.sh"
      provides: "WORKFLOW.md-aware dev cycle gate with legacy fallback"
      contains: "WORKFLOW.md"
    - path: "hooks/completion-audit.sh"
      provides: "WORKFLOW.md-aware completion audit with legacy fallback"
      contains: "WORKFLOW.md"
    - path: "hooks/compliance-status.sh"
      provides: "Path progress display"
      contains: "PATH"
    - path: "hooks/prompt-reminder.sh"
      provides: "WORKFLOW.md position in prompt context"
      contains: "WORKFLOW.md"
    - path: "hooks/spec-floor-check.sh"
      provides: "Advisory downgrade when PATH 4 excluded"
      contains: "WORKFLOW.md"
  key_links:
    - from: "hooks/dev-cycle-check.sh"
      to: ".planning/WORKFLOW.md"
      via: "file read and path log parsing"
      pattern: "WORKFLOW.md"
    - from: "hooks/compliance-status.sh"
      to: ".planning/WORKFLOW.md"
      via: "path progress count"
      pattern: "PATH.*/"
---

<objective>
Make all 5 hook scripts WORKFLOW.md-aware with legacy fallback.

Purpose: Hooks currently enforce workflow compliance via skill invocation markers in the state file. With composable paths, WORKFLOW.md path completion becomes the primary gate. Hooks must check WORKFLOW.md first, falling back to legacy markers when WORKFLOW.md is absent.

Output: 5 modified hook scripts that support both composable paths (WORKFLOW.md) and legacy (state file) enforcement.
</objective>

<execution_context>
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/workflows/execute-plan.md
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-hook-alignment-silver-migrate/26-CONTEXT.md
@templates/workflow.md.base
@docs/composable-paths-contracts.md
@hooks/dev-cycle-check.sh
@hooks/completion-audit.sh
@hooks/compliance-status.sh
@hooks/prompt-reminder.sh
@hooks/spec-floor-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WORKFLOW.md awareness to dev-cycle-check.sh and completion-audit.sh</name>
  <files>hooks/dev-cycle-check.sh, hooks/completion-audit.sh</files>
  <read_first>
    - hooks/dev-cycle-check.sh (full file — understand current gate logic)
    - hooks/completion-audit.sh (full file — understand two-tier enforcement)
    - templates/workflow.md.base (understand WORKFLOW.md structure: Path Log table format)
  </read_first>
  <action>
Per D-01, D-02, D-03: Add WORKFLOW.md path completion check as PRIMARY gate with legacy fallback.

**For dev-cycle-check.sh:**
After resolving config and state file (around line 165), add a WORKFLOW.md check block BEFORE the existing "Read state file and determine stage" section:

1. Resolve WORKFLOW.md path: `workflow_file="$PWD/.planning/WORKFLOW.md"` (same pattern as other .planning/ file reads).
2. If WORKFLOW.md exists and is a regular file (not symlink):
   - Parse the `## Path Log` section table. Count rows where Status column = "complete" (`path_complete_count`). Count total rows (`path_total_count`).
   - Use grep/awk to parse: `grep -E '^\|[^|]+\|[^|]+\| complete' "$workflow_file"` for completed, `grep -cE '^\| [0-9]' "$workflow_file"` for total.
   - If `path_complete_count == path_total_count` AND `path_total_count > 0`: all paths done, emit success message "All workflow paths complete. Proceed freely." and exit 0.
   - If `path_complete_count < path_total_count`: emit info message showing progress "PATH {complete}/{total} — composable paths enforcement active." and allow the existing code to continue (do NOT exit — let legacy gate still apply for the current source edit check).
3. If WORKFLOW.md does not exist: fall through to existing legacy logic unchanged (per D-03).

**For completion-audit.sh:**
Same pattern, inserted after the trivial bypass check (around line 133) and before the branch detection:

1. Resolve `workflow_file="$PWD/.planning/WORKFLOW.md"`.
2. If WORKFLOW.md exists:
   - For `is_intermediate`: parse Path Log, check if PATH 5 (PLAN) or PATH 7 (EXECUTE) are in the log. If any path is present with status "complete", allow intermediate commits. Exit 0 with success.
   - For `is_completion`: parse Path Log, require ALL paths in the composition to show "complete". If not all complete, emit_block with "WORKFLOW INCOMPLETE — {complete}/{total} paths done. Complete all paths before final delivery."
   - If all paths complete, exit 0 with success, skipping the legacy skill check entirely.
3. If WORKFLOW.md does not exist: fall through to existing legacy logic unchanged.

**IMPORTANT:** Do NOT modify any existing legacy logic. Only ADD a new conditional block that checks WORKFLOW.md first and either exits early (paths complete) or falls through (WORKFLOW.md absent).

Error handling: wrap WORKFLOW.md parsing in a subshell or conditional — if parsing fails (malformed file), emit a warning and fall through to legacy logic.
  </action>
  <verify>
    <automated>bash -n hooks/dev-cycle-check.sh && bash -n hooks/completion-audit.sh && grep -q 'WORKFLOW.md' hooks/dev-cycle-check.sh && grep -q 'WORKFLOW.md' hooks/completion-audit.sh && grep -q 'legacy' hooks/dev-cycle-check.sh</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'WORKFLOW.md' hooks/dev-cycle-check.sh returns >= 2
    - grep -c 'WORKFLOW.md' hooks/completion-audit.sh returns >= 2
    - grep -q 'path_complete_count\|complete.*count\|paths.*complete' hooks/dev-cycle-check.sh
    - grep -q 'path_complete_count\|complete.*count\|paths.*complete' hooks/completion-audit.sh
    - bash -n hooks/dev-cycle-check.sh exits 0
    - bash -n hooks/completion-audit.sh exits 0
    - All existing legacy logic remains unchanged (no lines removed from original gate sections)
  </acceptance_criteria>
  <done>dev-cycle-check.sh and completion-audit.sh check WORKFLOW.md Path Log first for path completion status, falling back to legacy skill markers when WORKFLOW.md is absent. Both scripts pass bash -n syntax check.</done>
</task>

<task type="auto">
  <name>Task 2: Add WORKFLOW.md awareness to compliance-status.sh, prompt-reminder.sh, and spec-floor-check.sh</name>
  <files>hooks/compliance-status.sh, hooks/prompt-reminder.sh, hooks/spec-floor-check.sh</files>
  <read_first>
    - hooks/compliance-status.sh (full file — understand status output format)
    - hooks/prompt-reminder.sh (full file — understand additionalContext format)
    - hooks/spec-floor-check.sh (full file — understand blocking vs advisory logic)
    - templates/workflow.md.base (Path Log + Next Path sections)
  </read_first>
  <action>
**compliance-status.sh (per D-04, D-05):**
After reading the state file (around line 124), add WORKFLOW.md path progress:

1. `workflow_file="$PWD/.planning/WORKFLOW.md"`
2. If WORKFLOW.md exists: parse Path Log table. Count completed paths and total paths. Set `path_progress="PATH ${completed}/${total}"`.
3. If WORKFLOW.md absent: set `path_progress="PATH: N/A (legacy mode)"`.
4. In the output message (line ~211), insert `path_progress` after the Mode field: `"Silver Bullet: ${total_steps} steps | Mode: ${mode} | ${path_progress} | GSD ${gsd_done}/${gsd_total} | ..."`.

**prompt-reminder.sh (per D-06, D-07):**
After reading state file (around line 75), add WORKFLOW.md position:

1. `workflow_file="$PWD/.planning/WORKFLOW.md"`
2. If WORKFLOW.md exists:
   - Extract current path: parse `## Heartbeat` section, read `Last-path:` value.
   - Extract next path: parse `## Next Path` section, read the line after the heading.
   - Extract mode from `## Composition` section, read `Mode:` value.
   - Build position string: `"Composable path: currently at PATH ${last_path}, next: ${next_path} (${comp_mode} mode)"`.
3. If WORKFLOW.md absent: set position string to empty (omit the line entirely per D-07).
4. Append position string to `skill_status` before emitting additionalContext. Only append if non-empty.

**spec-floor-check.sh (per D-08, D-09):**
After checking `is_plan_phase` and `is_fast` (around line 56), add WORKFLOW.md composition check:

1. `workflow_file="$PWD/.planning/WORKFLOW.md"`
2. If WORKFLOW.md exists AND `is_plan_phase == true`:
   - Check if PATH 4 appears in the Path Log table: `grep -qE '^\|[^|]*\| (SPECIFY|PATH 4)' "$workflow_file"`.
   - If PATH 4 is NOT in the Path Log (intentionally excluded from composition): downgrade the spec floor check from blocking to advisory. Instead of calling `emit_block`, emit a warning message: `"SPEC FLOOR ADVISORY: PATH 4 (SPECIFY) excluded from composition — spec floor downgraded to advisory."` and exit 0.
   - If PATH 4 IS in the Path Log: continue with existing blocking logic unchanged.
3. If WORKFLOW.md absent: continue with existing blocking logic unchanged.
  </action>
  <verify>
    <automated>bash -n hooks/compliance-status.sh && bash -n hooks/prompt-reminder.sh && bash -n hooks/spec-floor-check.sh && grep -q 'WORKFLOW.md' hooks/compliance-status.sh && grep -q 'WORKFLOW.md' hooks/prompt-reminder.sh && grep -q 'WORKFLOW.md' hooks/spec-floor-check.sh && grep -q 'legacy mode' hooks/compliance-status.sh && grep -q 'SPECIFY\|PATH 4' hooks/spec-floor-check.sh</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'WORKFLOW.md' hooks/compliance-status.sh
    - grep -q 'legacy mode' hooks/compliance-status.sh
    - grep -q 'PATH.*/' hooks/compliance-status.sh (path progress format)
    - grep -q 'WORKFLOW.md' hooks/prompt-reminder.sh
    - grep -q 'Last-path\|Heartbeat\|Next Path' hooks/prompt-reminder.sh
    - grep -q 'WORKFLOW.md' hooks/spec-floor-check.sh
    - grep -q 'SPECIFY\|PATH 4' hooks/spec-floor-check.sh
    - grep -q 'advisory\|ADVISORY' hooks/spec-floor-check.sh (downgrade logic)
    - bash -n hooks/compliance-status.sh exits 0
    - bash -n hooks/prompt-reminder.sh exits 0
    - bash -n hooks/spec-floor-check.sh exits 0
  </acceptance_criteria>
  <done>compliance-status.sh shows PATH X/Y or "N/A (legacy mode)". prompt-reminder.sh includes WORKFLOW.md position when present. spec-floor-check.sh downgrades to advisory when PATH 4 excluded from composition. All three pass bash -n syntax check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WORKFLOW.md content | File in project directory, writable by user and Claude — could be malformed |
| Path Log parsing | Table parsing via grep/awk — injection via crafted status strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | WORKFLOW.md | accept | WORKFLOW.md is in .planning/ which is user-owned; tampering is self-harm. Hooks degrade gracefully on malformed input. |
| T-26-02 | Denial of Service | Path Log parsing | mitigate | Wrap parsing in conditional with fallback to legacy — malformed WORKFLOW.md never blocks the hook from running. |
| T-26-03 | Elevation of Privilege | spec-floor downgrade | mitigate | Only downgrade when WORKFLOW.md explicitly excludes PATH 4 — absence of WORKFLOW.md does NOT downgrade (falls through to existing blocking logic). |
</threat_model>

<verification>
- All 5 hook scripts pass `bash -n` syntax validation
- Each hook has WORKFLOW.md check logic with legacy fallback
- No existing legacy logic is removed or modified
- compliance-status.sh output includes path progress
- spec-floor-check.sh advisory downgrade only triggers when PATH 4 absent from WORKFLOW.md composition
</verification>

<success_criteria>
- 5 hooks modified with WORKFLOW.md awareness
- Legacy fallback preserved in all hooks
- All hooks pass bash syntax check
</success_criteria>

<output>
After completion, create `.planning/phases/26-hook-alignment-silver-migrate/26-01-SUMMARY.md`
</output>
