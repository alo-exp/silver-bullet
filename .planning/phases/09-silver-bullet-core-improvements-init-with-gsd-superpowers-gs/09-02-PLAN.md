---
phase: 09-silver-bullet-core-improvements-init-with-gsd-superpowers-gs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - silver-bullet.md
autonomous: true
requirements:
  - REQ-2
  - REQ-3
  - REQ-4

must_haves:
  truths:
    - "Claude reads GSD STATE.md at step boundaries to determine current position instead of SB state"
    - "Interactive mode shows a big-picture progress banner at each workflow transition"
    - "Autonomous mode outputs structured commentary before each major step"
    - "Session mode selection uses lettered A/B options via AskUserQuestion"
    - "Permission mode selection uses lettered A/B/C options"
  artifacts:
    - path: "silver-bullet.md"
      provides: "GSD state delegation instruction, progress banner template, autonomous commentary format, lettered options"
      contains: "Read .planning/STATE.md"
  key_links:
    - from: "silver-bullet.md"
      to: ".planning/STATE.md"
      via: "Instruction to read at step boundaries"
      pattern: "STATE\\.md"
    - from: "silver-bullet.md"
      to: ".planning/ROADMAP.md"
      via: "Instruction to read for phase context"
      pattern: "ROADMAP\\.md"
---

<objective>
Add GSD state delegation, rich guided UX with progress banners, autonomous commentary, and lettered options to silver-bullet.md.

Purpose: REQ-2 ensures SB derives position from GSD state. REQ-3 ensures users always know where they are. REQ-4 ensures consistent lettered option format.
Output: Updated silver-bullet.md with new guidance sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-silver-bullet-core-improvements-init-with-gsd-superpowers-gs/09-RESEARCH.md

@silver-bullet.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GSD state delegation and progress banner to silver-bullet.md Section 2</name>
  <files>silver-bullet.md</files>
  <read_first>
    - silver-bullet.md
    - .planning/phases/09-silver-bullet-core-improvements-init-with-gsd-superpowers-gs/09-RESEARCH.md
  </read_first>
  <action>
Add a new subsection "### 2d. Position Awareness (GSD State Delegation)" immediately after the existing "### 2c. Utility Command Awareness" section (which ends before "## 3. NON-NEGOTIABLE RULES"). Insert the following content:

```markdown
### 2d. Position Awareness (GSD State Delegation)

**Rule:** SB does NOT maintain its own phase-progress tracking. At every workflow
transition and step boundary, derive the user's current position from GSD's authoritative
state — never from the SB state file.

**At each step boundary, read:**
1. `.planning/STATE.md` — parse YAML front matter for `current_plan`, `status`, `stopped_at`, `progress.total_phases`, `progress.completed_phases`, `progress.total_plans`, `progress.completed_plans`, `progress.percent`
2. `.planning/ROADMAP.md` — identify current phase name, its goal, and how many plans it contains

**SB state file (`~/.claude/.silver-bullet/state`) is ONLY for:**
- Quality gate stage markers (`quality-gate-stage-1` through `quality-gate-stage-4`)
- Skill invocation markers (recorded by `record-skill.sh`)
- Session mode (`~/.claude/.silver-bullet/mode`)
- Session init sentinel (`~/.claude/.silver-bullet/session-init`)

These are SB-specific with no GSD equivalent. Never use the SB state file to determine
which phase or plan the user is on — that information lives in GSD's STATE.md.

### 2e. Progress Banner (Interactive Mode)

At every workflow transition (the transitions listed in the Hand-Holding table above),
display a progress banner BEFORE the transition narration:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 PROGRESS: Phase {N} of {total} — {phase_name}
 Plan {M} of {plans_in_phase} | Overall: {percent}% complete
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Values come from STATE.md (`progress.*`) and ROADMAP.md (phase name, plan count).

**Within-phase narration:** When inside a phase (between PLAN and VERIFY transitions),
narrate at each plan boundary:

> Now executing Plan {M} of {N}: {plan_objective_from_PLAN.md}
> This plan produces: {files_modified summary}
> After this: {what comes next — next plan, or VERIFY if last plan}

### 2f. Autonomous Commentary

In autonomous mode (when `~/.claude/.silver-bullet/mode` contains `autonomous`),
do NOT ask questions or pause, but DO output structured commentary at each major step:

**Before each GSD command invocation:**
```
— [{timestamp}] Running: {command} | Phase {N}, Plan {M} of {total} —
```

**After each GSD command completes:**
```
— [{timestamp}] Done: {command} | Result: {one-line summary} —
```

**At phase completion:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 PHASE {N} COMPLETE — {phase_name}
 {completed_phases}/{total_phases} phases done | {percent}%
 Next: {next_phase_name or "FINALIZE + SHIP"}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

This commentary replaces the silence of autonomous mode with structured narration
so the user can follow along without being asked to act.
```

Important: Do NOT remove or modify any existing content in sections 2, 2a, 2b, or 2c. The new subsections 2d, 2e, 2f are purely additive, inserted between 2c and section 3.
  </action>
  <verify>
    <automated>grep -c "2d. Position Awareness" silver-bullet.md && grep -c "2e. Progress Banner" silver-bullet.md && grep -c "2f. Autonomous Commentary" silver-bullet.md && grep "STATE.md" silver-bullet.md | grep -c "planning"</automated>
  </verify>
  <acceptance_criteria>
    - grep "### 2d. Position Awareness (GSD State Delegation)" silver-bullet.md returns 1 match
    - grep "### 2e. Progress Banner (Interactive Mode)" silver-bullet.md returns 1 match
    - grep "### 2f. Autonomous Commentary" silver-bullet.md returns 1 match
    - grep "Read .planning/STATE.md" silver-bullet.md returns at least 1 match (in 2d or nearby)
    - grep "ROADMAP.md" silver-bullet.md returns at least 1 match in section 2d
    - grep "SB does NOT maintain its own phase-progress tracking" silver-bullet.md returns 1 match
    - grep "PROGRESS: Phase" silver-bullet.md returns 1 match (the banner template)
    - grep "Autonomous Commentary" silver-bullet.md returns 1 match
    - Section 2c (Utility Command Awareness) content is unchanged
    - Section 3 (NON-NEGOTIABLE RULES) content is unchanged
    - New sections appear between 2c and 3 in the file
  </acceptance_criteria>
  <done>silver-bullet.md has GSD state delegation instruction (2d), interactive progress banner (2e), and autonomous commentary format (2f). Position is derived from STATE.md, not SB state.</done>
</task>

<task type="auto">
  <name>Task 2: Convert silver-bullet.md user choices to lettered options</name>
  <files>silver-bullet.md</files>
  <read_first>
    - silver-bullet.md
  </read_first>
  <action>
Make two targeted edits in silver-bullet.md to convert user choice points to lettered A/B/C options:

**Edit 1 — Section 4 (Session Mode), the mode selection prompt:**

Find the block (around line 253-257):
```
> Run this session **interactively** or **autonomously**?
> - **Interactive** (default) — I pause at decision points and phase gates
> - **Autonomous** — I drive start to finish and surface blockers at the end
```

Replace with:
```
Use AskUserQuestion:
- Question: "Run this session interactively or autonomously?"
- Options:
  - "A. Interactive (default) — pause at decision points and phase gates"
  - "B. Autonomous — drive start to finish, surface blockers at the end"
```

**Edit 2 — Section 2.6 (Permission mode), referenced from silver:init but the numbering appears in silver-bullet.md if present:**

Search for the permission mode numbered list in silver-bullet.md. If section 2.6 or any permission mode selection with numbered 1/2/3 options exists in silver-bullet.md (not just in silver:init SKILL.md), convert to:
```
Use AskUserQuestion:
- Question: "Choose permission mode:"
- Options:
  - "A. auto (recommended) — auto-approves most tool calls"
  - "B. bypassPermissions — approves everything (isolated environments only)"
  - "C. Skip — keep current settings"
```

If permission mode selection only exists in silver:init/SKILL.md (already handled by Plan 01), no edit is needed here for that specific item.

**Important:** Only modify the specific choice-point blocks listed above. Do not change any narration text, banners, or non-choice content.
  </action>
  <verify>
    <automated>grep "A\. Interactive" silver-bullet.md && grep "B\. Autonomous" silver-bullet.md && grep "AskUserQuestion" silver-bullet.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "A. Interactive" silver-bullet.md returns 1 match
    - grep "B. Autonomous" silver-bullet.md returns 1 match
    - grep "AskUserQuestion" silver-bullet.md returns at least 1 match in section 4
    - The old format "> - **Interactive**" no longer appears as a user choice prompt
    - Section 4 autonomous mode behavior rules (bypass-permissions detection, anti-stall, etc.) are unchanged
  </acceptance_criteria>
  <done>Session mode selection and any other user choices in silver-bullet.md use lettered A/B/C AskUserQuestion format.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GSD STATE.md -> SB guidance | SB reads STATE.md authored by GSD; if STATE.md is corrupt, guidance narration may be wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | Progress banner shows phase/plan counts | accept | Phase counts are not sensitive; visible in ROADMAP.md already |
| T-09-05 | Tampering | STATE.md could be manually edited to show false progress | accept | GSD owns STATE.md; SB reads only, does not validate |
</threat_model>

<verification>
- silver-bullet.md contains sections 2d, 2e, 2f with GSD state delegation, progress banner, autonomous commentary
- All user choice points in silver-bullet.md use lettered AskUserQuestion format
- No existing content in sections 0-9 was removed or altered (only additions and targeted choice-point rewrites)
- STATE.md and ROADMAP.md are referenced as the source of truth for position
</verification>

<success_criteria>
- Claude derives position from GSD STATE.md at step boundaries (instruction in 2d)
- Interactive users see a progress banner with phase/plan numbers at every transition
- Autonomous mode outputs structured commentary before and after each GSD command
- Session mode selection uses "A. Interactive" / "B. Autonomous" lettered format
</success_criteria>

<output>
After completion, create `.planning/phases/09-silver-bullet-core-improvements-init-with-gsd-superpowers-gs/09-02-SUMMARY.md`
</output>
