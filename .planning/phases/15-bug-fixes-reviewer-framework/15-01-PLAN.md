---
phase: 15-bug-fixes-reviewer-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/silver-ingest/SKILL.md
  - hooks/pr-traceability.sh
  - templates/silver-bullet.md.base
autonomous: true
requirements: [BFIX-01, BFIX-02, BFIX-03, BFIX-04]

must_haves:
  truths:
    - "silver-ingest --source-url with shell metacharacters in owner/repo is rejected before any shell substitution"
    - "pr-traceability.sh heredoc cannot inject commands via WARN finding text"
    - "Confluence failure in silver-ingest produces [ARTIFACT MISSING] block in the relevant SPEC.md section"
    - "Version mismatch block in silver-bullet.md.base shows content diff not just version numbers"
  artifacts:
    - path: "skills/silver-ingest/SKILL.md"
      provides: "Input validation for --source-url and Confluence failure path fix"
      contains: "^[a-zA-Z0-9._-]+/[a-zA-Z0-9._-]+$"
    - path: "hooks/pr-traceability.sh"
      provides: "Safe warn_items handling via printf"
      contains: "printf '%s'"
    - path: "templates/silver-bullet.md.base"
      provides: "Content diff in version mismatch block"
      contains: "diff"
  key_links:
    - from: "skills/silver-ingest/SKILL.md"
      to: "gh api repos/{owner}/{repo}"
      via: "regex validation guard"
      pattern: "a-zA-Z0-9._-"
---

<objective>
Fix four critical v0.14.0 bugs: shell injection in silver-ingest cross-repo fetch (BFIX-01), command injection in pr-traceability.sh heredoc (BFIX-02), Confluence failure path producing wrong output (BFIX-03), and version mismatch display showing only version numbers instead of content diff (BFIX-04).

Purpose: Close security vulnerabilities and fix user-facing output quality before building the reviewer framework on top of these artifacts.
Output: Patched SKILL.md, pr-traceability.sh, and silver-bullet.md.base
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@skills/silver-ingest/SKILL.md
@hooks/pr-traceability.sh
@templates/silver-bullet.md.base
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix shell injection in silver-ingest and command injection in pr-traceability.sh (BFIX-01, BFIX-02)</name>
  <files>skills/silver-ingest/SKILL.md, hooks/pr-traceability.sh</files>
  <behavior>
    - BFIX-01: silver-ingest Step 5 rejects `--source-url https://github.com/foo;rm -rf/bar` — the owner/repo extraction must validate against `^[a-zA-Z0-9._-]+/[a-zA-Z0-9._-]+$` BEFORE any `gh api` or `curl` shell substitution
    - BFIX-02: pr-traceability.sh with warn_items containing backticks, $(), and double quotes does not execute them — heredoc expansion is replaced with printf-based safe concatenation
  </behavior>
  <action>
**BFIX-01 — silver-ingest/SKILL.md Step 5:**

After the line "Extract `{owner}` and `{repo}` from the URL." (around line 243), add an explicit input validation block:

```
**Input validation (BFIX-01 — shell injection prevention):**

After extracting `{owner}/{repo}` from the URL, validate the combined string against:
```bash
if ! printf '%s' "{owner}/{repo}" | grep -qE '^[a-zA-Z0-9._-]+/[a-zA-Z0-9._-]+$'; then
  echo "ERROR: Invalid repository identifier. Must match owner/repo with alphanumeric characters, dots, hyphens, underscores only."
  # Record failed status and skip to Step 7
fi
```

Do NOT pass `{owner}` or `{repo}` to any shell command (`gh api`, `curl`) until this validation passes. If validation fails, record `status: failed` with error "Invalid repository identifier" in the in-memory artifact list and skip to Step 7 (manifest write).
```

**BFIX-02 — hooks/pr-traceability.sh:**

Replace lines 60-71 (the heredoc-based traceability_block construction) with printf-based safe string building:

1. Replace the `cat <<TRACE` heredoc block (lines 60-71) with:
```bash
# Build traceability block safely — no heredoc expansion (BFIX-02)
traceability_block=$(printf '\n---\n## Spec Traceability (auto-generated by Silver Bullet)\n- Spec: .planning/SPEC.md (v%s)\n- JIRA: %s\n- Requirements covered: see SPEC.md ## Acceptance Criteria\n\n### Deferred items (WARN findings from silver-validate)\n%s' "${spec_version:-unknown}" "${jira_id:-n/a}" "${warn_items:-None}")
```

2. This eliminates heredoc expansion where `$warn_items` content could contain backticks, `$()`, or other shell metacharacters that would be interpreted during heredoc construction.
  </action>
  <verify>
    <automated>grep -c 'a-zA-Z0-9._-' skills/silver-ingest/SKILL.md | grep -q '[1-9]' && grep -c "printf '%s'" hooks/pr-traceability.sh | grep -q '[1-9]' && ! grep -c 'cat <<TRACE' hooks/pr-traceability.sh | grep -q '[1-9]' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>silver-ingest Step 5 contains regex validation guard before shell substitution; pr-traceability.sh uses printf instead of heredoc for traceability block construction; no heredoc expansion of user-controlled content remains</done>
</task>

<task type="auto">
  <name>Task 2: Fix Confluence failure path and version mismatch display (BFIX-03, BFIX-04)</name>
  <files>skills/silver-ingest/SKILL.md, templates/silver-bullet.md.base</files>
  <action>
**BFIX-03 — silver-ingest/SKILL.md Step 1, Confluence page resolution section:**

Currently the Confluence failure handling says (in the "Failure Handling Summary" table at the bottom, line ~424): `confluence_get_page | Continue per page | Skip page content; note in Assumptions`.

Fix TWO locations:

1. In Step 1 "Confluence page resolution" section (around line 116-118), after "call `confluence_get_page` to fetch the page content", add explicit failure handling:

```
**On Confluence page fetch failure:**

Record `status: failed` for this Confluence page in the in-memory artifact list. Insert `[ARTIFACT MISSING: Confluence page fetch failed — {error}]` in the SPEC.md section that references this Confluence page (typically Overview or UX Flows). Do NOT bury the failure in the Assumptions section — it must appear inline at the point where the content was expected.
```

2. In the "Failure Handling Summary" table (around line 424), change the Confluence row from:
   `Atlassian MCP / confluence_get_page | Continue per page | Skip page content; note in Assumptions`
   to:
   `Atlassian MCP / confluence_get_page | Continue per page | [ARTIFACT MISSING: Confluence page fetch failed — {error}] in relevant SPEC.md section`

**BFIX-04 — templates/silver-bullet.md.base §0/5.5 version mismatch block:**

In the version mismatch section (around line 79-87), replace the simple mismatch display with a content diff. Change the Mismatch block from:

```
SPEC VERSION MISMATCH
Local (SPEC.main.md):  v{local}
Remote ({repo}):       v{remote}

Run /silver:ingest --source-url {repo-url} to refresh before proceeding.
```

To:

```
      - **Mismatch:** BLOCK the session. Fetch the full remote SPEC.md content and produce a side-by-side summary:
        ```bash
        # Fetch remote SPEC.md for diff
        /opt/homebrew/bin/gh api repos/{owner}/{repo}/contents/.planning/SPEC.md --jq '.content' | base64 -d > /tmp/spec-remote-diff.md
        diff --unified=3 .planning/SPEC.main.md /tmp/spec-remote-diff.md || true
        rm -f /tmp/spec-remote-diff.md
        ```
        Display:
        ```
        SPEC VERSION MISMATCH
        Local (SPEC.main.md):  v{local}
        Remote ({repo}):       v{remote}

        Content changes:
        {unified diff output from above — show added/removed/changed lines}

        Run /silver:ingest --source-url {repo-url} to refresh before proceeding.
        ```
        Do NOT proceed with any implementation work until the user refreshes.
```

Ensure the `{owner}` and `{repo}` values used in the gh api call here are also subject to the same validation pattern from BFIX-01 (already enforced at ingest time, but the values stored in the READ-ONLY header should be re-validated before shell use).
  </action>
  <verify>
    <automated>grep -c 'ARTIFACT MISSING.*Confluence' skills/silver-ingest/SKILL.md | grep -q '[1-9]' && grep -c 'diff --unified' templates/silver-bullet.md.base | grep -q '[1-9]' && grep -c 'Content changes' templates/silver-bullet.md.base | grep -q '[1-9]' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Confluence failure produces [ARTIFACT MISSING] block in relevant SPEC.md section (not Assumptions); version mismatch block shows unified diff of content changes between local and remote SPEC.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> shell | --source-url argument passed to gh/curl commands |
| VALIDATION.md content -> heredoc | WARN findings inserted into PR body via shell expansion |
| Remote repo -> local display | Fetched spec content displayed in mismatch block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | silver-ingest Step 5 shell commands | mitigate | Validate owner/repo against `^[a-zA-Z0-9._-]+/[a-zA-Z0-9._-]+$` before any shell substitution (BFIX-01) |
| T-15-02 | Tampering | pr-traceability.sh heredoc | mitigate | Replace heredoc expansion with printf '%s' for all user-controlled content (BFIX-02) |
| T-15-03 | Information Disclosure | version mismatch diff | accept | Diff shows spec content the user already has access to (same repo or authorized cross-repo) |
</threat_model>

<verification>
1. `grep -c 'a-zA-Z0-9._-' skills/silver-ingest/SKILL.md` returns >= 1
2. `grep -c "printf" hooks/pr-traceability.sh` returns >= 2 (existing + new)
3. `grep -c 'cat <<TRACE' hooks/pr-traceability.sh` returns 0
4. `grep -c 'ARTIFACT MISSING.*Confluence' skills/silver-ingest/SKILL.md` returns >= 1
5. `grep -c 'diff --unified' templates/silver-bullet.md.base` returns >= 1
</verification>

<success_criteria>
- Shell metacharacters in --source-url are rejected before reaching gh/curl commands
- pr-traceability.sh constructs traceability block without heredoc expansion of user content
- Confluence failures produce [ARTIFACT MISSING] blocks inline, not notes in Assumptions
- Version mismatch displays unified diff of content changes
</success_criteria>

<output>
After completion, create `.planning/phases/15-bug-fixes-reviewer-framework/15-01-SUMMARY.md`
</output>
