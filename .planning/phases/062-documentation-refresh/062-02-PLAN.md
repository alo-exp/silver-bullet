---
phase: 062-documentation-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - site/help/getting-started/index.html
  - site/help/reference/index.html
  - site/help/concepts/verification.html
  - site/help/workflows/silver-ui.html
  - site/help/workflows/silver-research.html
  - site/help/workflows/silver-release.html
autonomous: true
requirements:
  - DOC-03

must_haves:
  truths:
    - "The getting-started page uses the correct install command (/plugin install alo-labs/silver-bullet) and has no stale v0.14.0-v0.22.0 era version references"
    - "The reference page lists /silver-quality-gates as 9-dimension (not 8), removes the invented accessibility-review step from /silver:ui descriptions, uses gsd-ship (hyphen) not gsd:ship (colon), and removes the invented post-release plugin version check from /silver:release"
    - "The verification.html page has correct step numbers (gsd-verify-work is Step 8 in silver:feature, not Step 9), accurate silver:ui verify description (no invented accessibility check), and corrects the silver:release exemption to say verification runs embedded rather than replaced"
    - "silver-ui.html includes Step 16 (Milestone Completion lifecycle)"
    - "silver-research.html includes the 4th MultAI auto-offer trigger (Change affects public API or data model)"
    - "silver-release.html uses gsd-ship (hyphen) not gsd:ship (colon)"
  artifacts:
    - path: "site/help/getting-started/index.html"
      provides: "Updated install command + no stale version refs"
      contains: "/plugin install alo-labs/silver-bullet"
    - path: "site/help/reference/index.html"
      provides: "Fixed skill counts, fixed invented steps, fixed gsd-ship naming"
      contains: "9-dimension"
    - path: "site/help/concepts/verification.html"
      provides: "Corrected step numbers and descriptions"
    - path: "site/help/workflows/silver-ui.html"
      provides: "Step 16 added"
    - path: "site/help/workflows/silver-research.html"
      provides: "4th MultAI trigger added"
    - path: "site/help/workflows/silver-release.html"
      provides: "gsd-ship hyphen fix"
  key_links:
    - from: "site/help/getting-started/index.html"
      to: "README.md"
      via: "install command must match README.md §Install"
    - from: "site/help/reference/index.html"
      to: "skills/silver-quality-gates/SKILL.md"
      via: "dimension count must match SKILL.md"
    - from: "site/help/workflows/silver-ui.html"
      to: "skills/silver-ui/SKILL.md"
      via: "Step 16 content sourced from SKILL.md"
---

<objective>
Audit and fix all known accuracy and staleness issues in the Silver Bullet website and help
center pages. This closes DOC-03: all version numbers current, feature descriptions accurate,
installation instructions match marketplace install, no stale era references.

The issues are fully enumerated in docs/internal/site-qa-report.md (12 issues across 5 pages)
plus two additional issues discovered during planning:
- getting-started/index.html uses wrong SB install command (/plugin install silver-bullet@alo-labs
  instead of /plugin install alo-labs/silver-bullet)
- getting-started/index.html contains stale v0.14.0-v0.22.0 era description text

Purpose: The site-qa-report.md from April 2026 is actionable — it lists the exact line numbers,
the wrong content, and the correct content from SKILL.md sources. Execute all 12 issues plus
the 2 install-command issues.

Output: 6 corrected HTML files with zero invented content and accurate version references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/internal/site-qa-report.md
@skills/silver-ui/SKILL.md
@skills/silver-research/SKILL.md
@skills/silver-release/SKILL.md
@skills/silver-quality-gates/SKILL.md

<interfaces>
<!-- Precise fix instructions for each file — no exploration needed -->

FILE 1: site/help/getting-started/index.html
  Fix A — Wrong install command (line 286):
    WRONG:  /plugin install silver-bullet@alo-labs
    CORRECT: /plugin install alo-labs/silver-bullet
    (per README.md §Install — authoritative source)

  Fix B — Stale v0.14.0–v0.22.0 era description (line 199):
    The paragraph starting "Silver Bullet is a Claude Code plugin that combines..."
    contains "With v0.14.0–v0.22.0, Silver Bullet also adds spec-driven development...
    The plugin ships with 61 skills and 18 enforcement hooks."
    Remove/replace the "With v0.14.0–v0.22.0" parenthetical. Update skill count if
    61 is no longer accurate (current count: 61 skills confirmed from ls skills/).
    Update hook count if 18 is no longer accurate (current: 18 hooks confirmed).
    Remove the version range from the text — state current capabilities without
    tying them to a past release range.

FILE 2: site/help/reference/index.html
  Fix C — "8-dimension" quality-gates count (site-qa-report Issue 13-D):
    Find: "8-dimension quality evaluation"
    Replace with: "9-dimension quality evaluation"
    Source: silver-quality-gates/SKILL.md states all 9 dimensions.

  Fix D — /silver:ui inserts non-existent accessibility-review step (Issues 13-A, 13-B):
    Two locations on this page:
    Location 1 (sb-skills section, ~line context for /silver:ui description):
      Remove "accessibility-review →" from the chain description.
      Correct chain: intel → product-brainstorm → silver:brainstorm → quality-gates → gsd-ui-phase
    Location 2 (Orchestration Workflows table for /silver:ui first step):
      Remove "accessibility-review" from the first step description.
      Correct first step: silver:intel → [silver:explore if fuzzy] → product-brainstorming → silver:brainstorm

  Fix E — /silver:release description invents "post-release plugin version check" (Issue 13-C):
    Find the /silver:release row in the Silver Bullet Skills table.
    Remove "post-release plugin version check" from the description.
    The workflow ends at gsd-complete-milestone (Step 8) — no such check exists.

  Fix F — gsd:ship colon to gsd-ship hyphen in ship disambiguation table (Issue 13-E):
    Find all occurrences of "gsd:ship" in the reference page.
    Replace with "gsd-ship" (hyphen throughout — this is the skill invocation name,
    not a slash command).

  Fix G — Stale "v0.19+" composable flows reference (line 528):
    The paragraph "Silver Bullet v0.19+ uses composable flows architecture..." is a
    migration note from ~v0.19. It is now the default architecture, not a version-gated
    feature. Remove or rewrite as a plain statement: "Silver Bullet uses composable
    flows architecture. Three new artifacts and reference concepts are introduced."
    (drop the version qualifier)

FILE 3: site/help/concepts/verification.html
  Fix H — Wrong step number for gsd-verify-work in silver:feature (Issue 10-A):
    Find: "Step 9: gsd-verify-work" (in the "When It Fires" section)
    Replace with: "Step 8: gsd-verify-work"
    Source: silver-feature/SKILL.md — gsd-verify-work is at Step 8.

  Fix I — silver:ui verify description invents accessibility check (Issue 10-B):
    Find the silver:ui entry in the verification table/section.
    Remove "accessibility check" from the description.
    The silver-ui SKILL.md does not include an accessibility check in the verify step;
    WCAG coverage is in gsd-ui-review (Step 9), not the verify step.

  Fix J — silver:release exemption says verification is "replaced" (Issue 10-C):
    Find: verification for silver:release is "replaced by the full quality gates suite..."
    Replace with: "silver:release still runs gsd-verify-work, embedded within
    gsd-audit-milestone. It is not skipped — it is part of the milestone audit step."

FILE 4: site/help/workflows/silver-ui.html
  Fix K — Missing Step 16 Milestone Completion (Issue 3-A):
    The page currently ends at Steps 14-15 (Finishing branch / ship).
    Add Step 16: Milestone Completion.
    Source content from silver-ui/SKILL.md Step 16:
    - Condition: last phase only
    - Lifecycle: gsd-audit-uat → gsd-audit-milestone → [gap closure, max 2 iterations] → gsd-complete-milestone
    Follow the same HTML structure as the other step sections on this page.
    Update the sidebar nav to include a "Step 16" link with the correct anchor id.
    Update the page-nav-bottom if it references the step count.

FILE 5: site/help/workflows/silver-research.html
  Fix L — Missing 4th MultAI auto-offer trigger (Issue 5-A):
    Find the "When MultAI is auto-offered" section (3 conditions listed).
    Add the missing 4th condition: "Change affects public API or data model fundamentally."
    Source: silver/SKILL.md §Step 1d auto-trigger signals and silver-bullet.md §2h MultAI auto-offer table.

FILE 6: site/help/workflows/silver-release.html
  Fix M — gsd:ship colon to gsd-ship hyphen in ship disambiguation table (Issue 6-A):
    Find: "gsd:ship" in the ship disambiguation table.
    Replace with: "gsd-ship" (hyphen). This is the skill invocation name, not a slash command.

VALIDATION AFTER EACH FILE:
  After editing each file, verify the HTML is well-formed. Do not change any CSS,
  JavaScript, navigation, breadcrumbs, or structural elements beyond the specific
  content fixes above. Do not add new sections not listed in the fixes above.
  Do not rewrite prose beyond the targeted changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix getting-started/index.html and reference/index.html</name>
  <files>
    site/help/getting-started/index.html
    site/help/reference/index.html
  </files>
  <action>
Apply the targeted content fixes to both files. Read each file first, then apply only the
specific changes described in the interfaces section above. No structural changes.

For site/help/getting-started/index.html:
- Fix A: Change /plugin install silver-bullet@alo-labs → /plugin install alo-labs/silver-bullet
  (line ~286 based on grep; confirm before editing)
- Fix B: Remove the "With v0.14.0-v0.22.0" parenthetical from the feature description paragraph
  at line ~199. The sentence structure should state current capabilities without anchoring them
  to a past version range. Skill count (61) and hook count (18) are current and accurate — keep them.

For site/help/reference/index.html:
- Fix C: "8-dimension" → "9-dimension" for quality-gates entry
- Fix D: Remove "accessibility-review →" from the two /silver:ui locations (skills table
  description + orchestration workflows table first-step column)
- Fix E: Remove the invented "post-release plugin version check" from /silver:release description
- Fix F: Replace all "gsd:ship" (colon) with "gsd-ship" (hyphen) throughout this file
- Fix G: Remove the "v0.19+" version qualifier from the composable flows architecture statement
  (rewrite as current-state prose, not version-gated)

Read both files fully before editing. Make all changes in a single Edit pass per file.
  </action>
  <verify>
    <automated>grep "silver-bullet@alo-labs" /Users/shafqat/Documents/Projects/silver-bullet/site/help/getting-started/index.html; echo "Exit: $? (0=FAIL found stale, 1=OK not found)" && grep "alo-labs/silver-bullet" /Users/shafqat/Documents/Projects/silver-bullet/site/help/getting-started/index.html | head -3</automated>
  </verify>
  <done>
    getting-started/index.html: install command is /plugin install alo-labs/silver-bullet;
    v0.14.0-v0.22.0 era parenthetical removed.
    reference/index.html: "9-dimension" quality-gates; no "accessibility-review" in /silver:ui
    descriptions; no "post-release plugin version check" in /silver:release; "gsd-ship"
    hyphen throughout; no "v0.19+" version qualifier on composable flows statement.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix verification.html, silver-ui.html, silver-research.html, silver-release.html</name>
  <files>
    site/help/concepts/verification.html
    site/help/workflows/silver-ui.html
    site/help/workflows/silver-research.html
    site/help/workflows/silver-release.html
  </files>
  <action>
Read each file first, then apply only the targeted fixes. No structural changes beyond
what is listed. Follow the same HTML patterns already present in each file.

For site/help/concepts/verification.html:
- Fix H: Change step number for gsd-verify-work in silver:feature from "Step 9" to "Step 8"
- Fix I: Remove invented "accessibility check" from the silver:ui verification description
- Fix J: Correct the silver:release exemption text from "replaced by" to "still runs embedded
  within gsd-audit-milestone" (see interfaces section for exact replacement text)

For site/help/workflows/silver-ui.html:
- Fix K: Add Step 16 (Milestone Completion) content after Steps 14-15.
  Read skills/silver-ui/SKILL.md to get the exact Step 16 content before writing HTML.
  Match the step section HTML structure used by existing steps on this page.
  Add a sidebar link for Step 16 matching the existing sidebar link pattern.

For site/help/workflows/silver-research.html:
- Fix L: Add the 4th MultAI auto-offer condition ("Change affects public API or data model
  fundamentally") to the MultAI auto-offer list. Read the current list, append the 4th item
  using the same HTML list element pattern.

For site/help/workflows/silver-release.html:
- Fix M: Change "gsd:ship" to "gsd-ship" in the ship disambiguation table.
  Confirm how many occurrences exist (may be 1-2) and fix all of them.

Read silver-ui/SKILL.md step 16 before writing the HTML for Fix K to ensure accurate content.
  </action>
  <verify>
    <automated>grep -n "gsd:ship\|Step 9.*gsd-verify\|accessibility check\|replaced by.*quality gates\|accessibility-review" /Users/shafqat/Documents/Projects/silver-bullet/site/help/concepts/verification.html /Users/shafqat/Documents/Projects/silver-bullet/site/help/workflows/silver-release.html 2>/dev/null; echo "Above lines should be empty (all fixed). Exit 1 = OK (not found)."</automated>
  </verify>
  <done>
    verification.html: Step 8 for gsd-verify-work in silver:feature; no "accessibility check"
    in silver:ui verification; silver:release exemption says embedded not replaced.
    silver-ui.html: Step 16 section and sidebar link present with correct lifecycle
    (gsd-audit-uat → gsd-audit-milestone → gap closure → gsd-complete-milestone).
    silver-research.html: 4th MultAI trigger present.
    silver-release.html: "gsd-ship" hyphen in ship disambiguation table.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML file write | Claude Code modifies existing HTML pages in site/help/; no untrusted input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-062-02-01 | Tampering | HTML content accuracy | mitigate | Every fix sourced from site-qa-report.md + SKILL.md; executor must not invent new content |
| T-062-02-02 | Information Disclosure | Install command | mitigate | Use only /plugin install alo-labs/silver-bullet (per README.md) — incorrect commands mislead users |
</threat_model>

<verification>
After both tasks complete:

1. Install command correct in getting-started:
   grep "alo-labs/silver-bullet" site/help/getting-started/index.html  # must find it
   grep "silver-bullet@alo-labs" site/help/getting-started/index.html  # must return nothing

2. Quality gates count correct in reference:
   grep "9-dimension" site/help/reference/index.html  # must find it
   grep "8-dimension" site/help/reference/index.html  # must return nothing

3. No invented accessibility-review step in reference:
   grep "accessibility-review" site/help/reference/index.html  # must return nothing

4. gsd-ship hyphen used throughout (not gsd:ship colon):
   grep "gsd:ship" site/help/reference/index.html site/help/workflows/silver-release.html  # must return nothing

5. Step 16 present in silver-ui.html:
   grep -i "step 16\|milestone completion" site/help/workflows/silver-ui.html  # must find it

6. 4th MultAI trigger present in silver-research.html:
   grep -i "public api\|data model" site/help/workflows/silver-research.html  # must find it

7. Verification page step number correct:
   grep "Step 8.*gsd-verify\|gsd-verify.*Step 8" site/help/concepts/verification.html  # must find it
   grep "Step 9.*gsd-verify\|gsd-verify.*Step 9" site/help/concepts/verification.html  # must return nothing
</verification>

<success_criteria>
- All 12 issues from docs/internal/site-qa-report.md resolved
- Install command /plugin install alo-labs/silver-bullet used consistently in getting-started
- No stale version-range qualifiers (v0.14.0–v0.22.0, v0.19+) in site pages
- HTML structure of each file unchanged except for the targeted content fixes
- Every correction sourced from README.md, silver-bullet.md, or SKILL.md — no invented content
</success_criteria>

<output>
After completion, create `.planning/phases/062-documentation-refresh/062-02-SUMMARY.md`
</output>
