---
phase: 27-silver-fast-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/silver-fast/SKILL.md
autonomous: true
requirements:
  - FAST-01
  - FAST-02
  - FAST-03

must_haves:
  truths:
    - "Trivial changes (<=3 files, no logic) route to gsd-fast"
    - "Medium changes (4-10 files OR logic in <=3 files) route to gsd-quick with detected flags"
    - "Complex changes (>10 files OR cross-cutting OR schema) escalate to silver-feature"
    - "gsd-quick flags are composed from signal detection on change description"
    - "Scope expansion during execution triggers autonomous escalation with banner"
  artifacts:
    - path: "skills/silver-fast/SKILL.md"
      provides: "3-tier complexity triage with gsd-quick routing and autonomous escalation"
      contains: "Tier 1|Tier 2|Tier 3"
  key_links:
    - from: "skills/silver-fast/SKILL.md"
      to: "gsd-fast"
      via: "Skill tool invocation for Tier 1"
      pattern: "gsd-fast"
    - from: "skills/silver-fast/SKILL.md"
      to: "gsd-quick"
      via: "Skill tool invocation for Tier 2"
      pattern: "gsd-quick"
    - from: "skills/silver-fast/SKILL.md"
      to: "silver-feature"
      via: "Skill tool invocation for Tier 3 escalation"
      pattern: "silver.feature"
---

<objective>
Rewrite skills/silver-fast/SKILL.md to implement 3-tier complexity triage with intelligent gsd-quick flag composition and autonomous escalation.

Purpose: Replace the current 2-tier system (trivial vs escalate) with a medium tier that routes to gsd-quick with appropriate flags, keeping trivial and complex tiers intact.
Output: Rewritten SKILL.md with all three tiers, flag detection, and autonomous escalation logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-silver-fast-redesign/27-CONTEXT.md
@skills/silver-fast/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md with 3-tier triage and gsd-quick flag composition</name>
  <files>skills/silver-fast/SKILL.md</files>
  <read_first>
    - skills/silver-fast/SKILL.md (current implementation to understand structure)
    - .planning/phases/27-silver-fast-redesign/27-CONTEXT.md (all decisions)
  </read_first>
  <action>
Rewrite skills/silver-fast/SKILL.md completely. Keep the existing frontmatter format and banner pattern. The new structure:

**Frontmatter**: Keep `name: silver-fast`. Update description to: "3-tier complexity triage: trivial → gsd-fast, medium → gsd-quick with flags, complex → silver-feature escalation."

**Preamble**: Describe the 3 tiers briefly. State this skill does NOT read §10 prefs or create WORKFLOW.md (per D-09, D-10).

**Pre-flight Banner**: Keep the existing banner pattern:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 SILVER BULLET ► FAST PATH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Change: {$ARGUMENTS or "(not specified)"}
```

**Step 0: Complexity Triage** (per D-01, D-02, D-03):

Analyze $ARGUMENTS to classify into one of three tiers. Do NOT use AskUserQuestion for triage — classify autonomously based on the change description:

- **Tier 1 (Trivial)**: <=3 files AND no logic changes. Indicators: typo, config value, rename, comment, one-liner. Proceed to Step 1.
- **Tier 2 (Medium)**: 4-10 files OR logic change in <=3 files OR dependency update. Proceed to Step 2.
- **Tier 3 (Complex)**: >10 files OR cross-cutting concern OR schema change OR new capability. Proceed to Step 3.

If ambiguous between Tier 1 and Tier 2, classify as Tier 2 (safer). If ambiguous between Tier 2 and Tier 3, classify as Tier 3.

Display classification:
```
Classification: Tier {N} ({Trivial|Medium|Complex})
Routing to: {gsd-fast|gsd-quick|silver-feature}
```

**Step 1: Tier 1 — Execute via gsd-fast** (per D-01):

Invoke `gsd-fast` via the Skill tool. Pass $ARGUMENTS as the change description.

After gsd-fast completes, run scope expansion check (Step 4).

If scope remained <=3 files, display completion banner:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 SILVER BULLET ► FAST PATH COMPLETE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Change: {$ARGUMENTS}
Files modified: {count} (confirmed ≤3)
Status: committed
```

**Step 2: Tier 2 — Detect flags and route to gsd-quick** (per D-04, D-05):

Before invoking gsd-quick, detect which flags to apply by scanning $ARGUMENTS for signals:

- **--discuss**: Ambiguity signals — words like "not sure", "unclear", "multiple approaches", "options", "decide", "which", "should we", "trade-off"
- **--research**: Novel domain signals — "new library", "unfamiliar", "investigate", "evaluate", "compare", "never used", "first time", "unknown"
- **--validate**: Production code signals — change modifies src/, app/, or lib/ directories with logic changes (not just config/comments)
- **--full**: All three signals detected

Flag composition rules:
- Any combination is valid (e.g., `--discuss --validate` without `--research`)
- If no signals detected, invoke bare `gsd-quick` (no flags)
- If all three signals detected, use `--full` instead of listing all three

Display detected signals:
```
Detected signals:
  Ambiguity: {yes/no} {reason if yes}
  Novel domain: {yes/no} {reason if yes}
  Production code: {yes/no} {reason if yes}
Flags: {--discuss --research --validate | --full | (none)}
```

Invoke `gsd-quick` via the Skill tool with the composed flags and $ARGUMENTS.

After gsd-quick completes, run scope expansion check (Step 4).

**Step 3: Tier 3 — Escalate to silver-feature** (per D-03):

Display:
```
Change exceeds fast-path complexity. Routing to silver-feature.
Reason: {specific reason — e.g., "touches >10 files", "cross-cutting concern", "schema change"}
```

Invoke `silver:feature` via the Skill tool with $ARGUMENTS. Exit silver:fast.

**Step 4: Scope Expansion Check** (per D-06, D-07, D-08):

After Tier 1 or Tier 2 execution completes, check if scope expanded beyond the current tier:

- **During Tier 1**: If files modified > 3, check if Tier 2 applies (4-10 files). If yes, display escalation banner and re-route to gsd-quick (Step 2). If files > 10, escalate to silver-feature (Step 3).
- **During Tier 2**: If files modified > 10, escalate to silver-feature (Step 3).

Escalation is AUTONOMOUS — no AskUserQuestion (per D-07). Display banner (per D-08):
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 FAST PATH ESCALATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Reason: Scope expanded from {original tier} to {new tier}
Files affected: {count}
Routing to: {gsd-quick|silver-feature}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Then invoke the target workflow. On escalation to silver-feature, re-run /silver classification with expanded scope description (per D-07) by invoking `silver:feature` with the updated scope.
  </action>
  <verify>
    <automated>grep -c "Tier 1\|Tier 2\|Tier 3" skills/silver-fast/SKILL.md | xargs test 3 -le</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md frontmatter has `name: silver-fast` and updated description mentioning 3-tier
    - Step 0 classifies into exactly 3 tiers with the exact criteria from D-01, D-02, D-03
    - Step 2 has flag detection logic with signal words for --discuss, --research, --validate per D-04
    - Step 2 states flags are composable per D-05
    - Step 4 has autonomous escalation (NO AskUserQuestion) per D-07
    - Step 4 displays FAST PATH ESCALATION banner per D-08
    - No Composition Proposal section per D-09
    - No §10 preference loading per D-10
    - gsd-fast invoked for Tier 1, gsd-quick invoked for Tier 2, silver:feature invoked for Tier 3
  </acceptance_criteria>
  <done>SKILL.md rewritten with 3-tier triage, gsd-quick flag composition, and autonomous escalation. All 12 decisions (D-01 through D-12) implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this is a skill definition file (markdown instructions), not executable code. No user input processing, no API calls, no data persistence.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | E (Elevation) | Tier classification | accept | Misclassification routes to more thorough workflow (safe direction); ambiguity rules bias toward higher tier |
</threat_model>

<verification>
1. `grep -c "Tier 1" skills/silver-fast/SKILL.md` returns >= 1
2. `grep -c "Tier 2" skills/silver-fast/SKILL.md` returns >= 1
3. `grep -c "Tier 3" skills/silver-fast/SKILL.md` returns >= 1
4. `grep -c "gsd-quick" skills/silver-fast/SKILL.md` returns >= 1
5. `grep -c "gsd-fast" skills/silver-fast/SKILL.md` returns >= 1
6. `grep -c "FAST PATH ESCALATION" skills/silver-fast/SKILL.md` returns >= 1
7. `grep -c "AskUserQuestion" skills/silver-fast/SKILL.md` returns 0 (no user prompts for triage or escalation)
8. `grep -c "§10" skills/silver-fast/SKILL.md` returns >= 1 (mentions NOT reading it)
9. `grep -c "WORKFLOW.md" skills/silver-fast/SKILL.md` returns 0 OR mentions NOT creating it
</verification>

<success_criteria>
- skills/silver-fast/SKILL.md implements 3-tier complexity triage per D-01/D-02/D-03
- gsd-quick flag composition works per D-04/D-05
- Autonomous escalation with banner per D-06/D-07/D-08
- No composition proposal per D-09, no §10 prefs per D-10
- Only SKILL.md modified per D-11/D-12
</success_criteria>

<output>
After completion, create `.planning/phases/27-silver-fast-redesign/27-01-SUMMARY.md`
</output>
