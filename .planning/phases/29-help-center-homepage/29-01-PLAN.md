---
phase: 29-help-center-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/index.html
  - site/help/concepts/composable-paths.html
  - site/help/concepts/artifact-review-assessor.html
  - site/help/concepts/routing-logic.html
  - site/help/concepts/verification.html
  - site/help/concepts/documentation.html
  - site/help/concepts/index.html
autonomous: true
requirements: [SITE-01, SITE-02]

must_haves:
  truths:
    - "Homepage meta tags reference composable paths, not fixed pipeline"
    - "Hero section describes 18 composable paths and dynamic composition"
    - "Workflow section shows path composition concept"
    - "Feature cards mention WORKFLOW.md tracking, dual-mode quality gates, 3-tier silver-fast"
    - "composable-paths.html concept page exists and explains the 18-path architecture"
    - "artifact-review-assessor.html concept page exists and explains assessor triage"
    - "routing-logic.html updated for composable path composition"
    - "verification.html mentions PATH 11 VERIFY and dual-mode quality gates"
    - "documentation.html mentions PATH 16 DOCUMENT and WORKFLOW.md"
    - "concepts/index.html links to the 2 new concept pages"
  artifacts:
    - path: "site/index.html"
      provides: "Updated homepage"
      contains: "composable paths"
    - path: "site/help/concepts/composable-paths.html"
      provides: "New concept page for composable paths architecture"
    - path: "site/help/concepts/artifact-review-assessor.html"
      provides: "New concept page for assessor"
    - path: "site/help/concepts/index.html"
      provides: "Updated index with links to new pages"
  key_links:
    - from: "site/help/concepts/index.html"
      to: "composable-paths.html"
      via: "href link"
      pattern: "composable-paths\\.html"
    - from: "site/help/concepts/index.html"
      to: "artifact-review-assessor.html"
      via: "href link"
      pattern: "artifact-review-assessor\\.html"
---

<objective>
Update the homepage and create/update help center concept pages to reflect the composable paths architecture.

Purpose: New and existing users see composable paths messaging on the homepage and can learn the architecture through dedicated concept pages.
Output: Updated site/index.html, 2 new concept pages, 3 updated concept pages, updated concepts index.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-help-center-homepage/29-CONTEXT.md
@docs/composable-paths-contracts.md
@site/index.html
@site/help/concepts/routing-logic.html
@site/help/concepts/verification.html
@site/help/concepts/documentation.html
@site/help/concepts/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update homepage for composable paths architecture</name>
  <files>site/index.html</files>
  <read_first>
    - site/index.html (full file — 1910 lines)
    - docs/composable-paths-contracts.md (source of truth for 18 paths)
    - .planning/phases/29-help-center-homepage/29-CONTEXT.md
  </read_first>
  <action>
Update site/index.html per D-01 through D-04:

**D-01 — Meta tags:** Find the existing `<title>` and `<meta name="description">` and `og:` tags. Replace references to "fixed pipeline", "20-step workflow", or similar with "composable paths architecture" / "18 composable paths". The title should include "Composable Paths". The description should mention dynamic composition from 18 paths.

**D-02 — Hero section:** Find the hero section (look for class="hero" or similar hero container). Update the headline and subheadline to describe composable paths — emphasize "18 composable paths" and "dynamic composition" as key differentiators. Replace any "20-step" or "fixed pipeline" language. Keep the existing HTML structure and CSS classes.

**D-03 — Workflow section:** Find the workflow/how-it-works section (look for class containing "workflow" or "steps" or "how-it-works"). Update to show path composition concept — /silver composes a chain of paths from the 18-path catalog based on context classification. Replace linear step descriptions with composition-based description. Keep existing card/step HTML structure.

**D-04 — Feature cards:** Find the feature cards section. Update card text to reflect: (1) composable architecture with 18 paths, (2) WORKFLOW.md real-time tracking, (3) dual-mode quality gates (design-time checklist + adversarial audit), (4) 3-tier silver-fast triage. Keep existing card HTML structure, just update text content.

Do NOT change CSS, layout structure, or JavaScript. Only update text content within existing HTML elements. Per D-16, follow existing patterns.
  </action>
  <verify>
    <automated>grep -c "composable" site/index.html | xargs test 3 -le</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "composable paths" site/index.html` returns matches in meta tags, hero, and body
    - `grep -i "20-step\|fixed pipeline" site/index.html` returns 0 matches (old language removed)
    - `grep -i "18.*path\|18 composable" site/index.html` returns at least 1 match
    - `grep -i "WORKFLOW.md\|dual.mode\|3-tier" site/index.html` returns matches in feature cards
    - HTML structure unchanged — same number of major sections
  </acceptance_criteria>
  <done>Homepage meta tags, hero, workflow section, and feature cards all reference composable paths architecture. No references to fixed pipeline or 20-step workflow remain.</done>
</task>

<task type="auto">
  <name>Task 2: Create new concept pages and update existing ones</name>
  <files>
    site/help/concepts/composable-paths.html
    site/help/concepts/artifact-review-assessor.html
    site/help/concepts/routing-logic.html
    site/help/concepts/verification.html
    site/help/concepts/documentation.html
    site/help/concepts/index.html
  </files>
  <read_first>
    - site/help/concepts/routing-logic.html (full file — use as HTML template)
    - site/help/concepts/verification.html (full file — to update)
    - site/help/concepts/documentation.html (full file — to update)
    - site/help/concepts/index.html (full file — to add links)
    - docs/composable-paths-contracts.md (source of truth)
    - skills/artifact-review-assessor/SKILL.md (assessor details)
    - templates/workflow.md.base (WORKFLOW.md template)
  </read_first>
  <action>
**D-05 — Create composable-paths.html:** Clone the HTML template from routing-logic.html (same doctype, head, nav, page-hero, doc-layout, sidebar, footer structure). Title: "Composable Paths Architecture — Silver Bullet Help". Content sections:
- What are Composable Paths? — The 18-path catalog replaces fixed pipelines. /silver composes chains dynamically.
- The 18 Paths — List all 18 paths from docs/composable-paths-contracts.md with name, one-line description, and trigger. Present as a table or definition list.
- How Composition Works — /silver classifies context, selects paths, orders chain, proposes to user.
- WORKFLOW.md Tracking — Real-time state tracking, path log, dynamic insertions, deferred improvements.
- Supervision Loop — Per-path execution with exit verification, stall detection, progress reporting.
Breadcrumb: Help > Core Concepts > Composable Paths Architecture. Per D-17, use relative links.

**D-06 — Create artifact-review-assessor.html:** Same template. Title: "Artifact Review Assessor — Silver Bullet Help". Content:
- What is the Assessor? — Triages reviewer findings into MUST-FIX / NICE-TO-HAVE / DISMISS based on artifact contract.
- How It Works — Judges against artifact CONTRACT only, not subjective quality. No self-review loop.
- The Cycle — Reviewer produces findings -> Assessor triages -> Fix MUST-FIX items -> Reviewer re-checks.
- When It Runs — Part of PATH 9 (REVIEW) in compositions.
Breadcrumb: Help > Core Concepts > Artifact Review Assessor.

**D-07 — Update routing-logic.html:** Find the main content sections describing routing. Add/update content to describe composable path composition: /silver classifies context into workflow type, then composes a path chain. Replace any "fixed routing" or "20-step" language. Keep existing sections that are still accurate, add new section on path composition.

**D-08 — Update verification.html:** Add a section or update existing content to mention PATH 11 (VERIFY) and dual-mode quality gates (design-time checklist pre-plan + adversarial audit pre-ship). Reference the composable-paths.html concept page.

**D-09 — Update documentation.html:** Add a section or update existing content to mention PATH 16 (DOCUMENT) and WORKFLOW.md as the real-time state tracking artifact. Reference the composable-paths.html concept page.

**D-33 — Update concepts/index.html:** Add cards/links for the 2 new concept pages (composable-paths.html, artifact-review-assessor.html) in the same style as existing concept cards. Place composable-paths first (it's the primary educational resource).

Per D-16, follow existing HTML patterns exactly — same CSS classes, same layout structure, same nav/footer. Per D-17, all links are relative.
  </action>
  <verify>
    <automated>test -f site/help/concepts/composable-paths.html && test -f site/help/concepts/artifact-review-assessor.html && grep -l "composable-paths.html" site/help/concepts/index.html && grep -l "artifact-review-assessor.html" site/help/concepts/index.html && grep -l "PATH 11\|VERIFY" site/help/concepts/verification.html && grep -l "PATH 16\|DOCUMENT" site/help/concepts/documentation.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - site/help/concepts/composable-paths.html exists, contains "18" and "composable" and lists all path names
    - site/help/concepts/artifact-review-assessor.html exists, contains "MUST-FIX" and "NICE-TO-HAVE" and "DISMISS"
    - site/help/concepts/routing-logic.html contains "composable" or "path composition"
    - site/help/concepts/verification.html contains "PATH 11" or "VERIFY" and "dual-mode" or "dual mode"
    - site/help/concepts/documentation.html contains "PATH 16" or "DOCUMENT" and "WORKFLOW.md"
    - site/help/concepts/index.html contains href to "composable-paths.html" and "artifact-review-assessor.html"
    - All new pages use same HTML template structure as routing-logic.html (nav, page-hero, doc-layout, footer)
  </acceptance_criteria>
  <done>2 new concept pages created, 3 existing concept pages updated with composable paths content, concepts index links to new pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static site | No server-side processing, all content is static HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | I (Information Disclosure) | Static HTML | accept | Public documentation site, no sensitive data |
</threat_model>

<verification>
- All 7 files exist and contain composable paths terminology
- Homepage has no remaining "fixed pipeline" or "20-step" references
- New concept pages follow same HTML template as existing pages
- concepts/index.html links to both new pages
</verification>

<success_criteria>
Homepage reflects composable paths architecture. Two new concept pages (composable-paths, artifact-review-assessor) educate users on the new architecture. Three existing concept pages (routing-logic, verification, documentation) updated with composable paths context. Concepts index links to all pages.
</success_criteria>

<output>
After completion, create `.planning/phases/29-help-center-homepage/29-01-SUMMARY.md`
</output>
