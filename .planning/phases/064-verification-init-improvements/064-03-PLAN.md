---
phase: 064-verification-init-improvements
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/silver-init/SKILL.md
autonomous: true
requirements:
  - INIT-01

must_haves:
  truths:
    - "skills/silver-init/SKILL.md step 3.1b and step 3.1c are updated so that when CLAUDE.md already exists, the skill diffs ALL sections comprehensively (not just model-routing/execution overrides), and offers the user an explicit per-section choice: Keep / Replace / Merge"
    - "No CLAUDE.md section is silently removed or overwritten without explicit user confirmation — this guarantee is stated in the skill text"
    - "The non-destructive guarantee at the top of the skill file is consistent with the updated 3.1b/3.1c behavior"
  artifacts:
    - path: "skills/silver-init/SKILL.md"
      provides: "Updated CLAUDE.md conflict resolution with comprehensive diff and per-section user choice"
      contains: "no silent override, per-section, Keep / Replace / Merge"
  key_links:
    - from: "skills/silver-init/SKILL.md step 3.1b"
      to: "CLAUDE.md conflict detection"
      via: "step reference to 3.1c"
    - from: "skills/silver-init/SKILL.md step 3.1c"
      to: "AskUserQuestion per-section choice"
      via: "explicit per-section enumeration"
---

<objective>
Update skills/silver-init/SKILL.md steps 3.1b and 3.1c to implement comprehensive CLAUDE.md conflict detection with explicit per-section user choice (Keep / Replace / Merge). No section may be silently removed or overwritten.

Purpose: Satisfy INIT-01 (GitHub #69). This is a SKILL.md text change — the skill itself IS the implementation.
Output: skills/silver-init/SKILL.md with updated steps 3.1b and 3.1c
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read silver-init/SKILL.md thoroughly to understand current 3.1b/3.1c behavior</name>
  <files>skills/silver-init/SKILL.md</files>
  <read_first>
    - skills/silver-init/SKILL.md — read the ENTIRE file, especially:
      - Non-Destructive Guarantee section (top of file)
      - Step 3.1b Handle CLAUDE.md (around line 553)
      - Step 3.1c Conflict detection (around line 554)
      - Update mode section (around line 534–546) — note how conflict detection (step 5) is referenced there
    - skills/silver-init/references/scaffold-steps.md — may contain detailed conflict detection procedure (3.1c reference target)
  </read_first>
  <action>
Read skills/silver-init/SKILL.md completely. Note:
1. The exact current text of step 3.1b (Handle CLAUDE.md) and step 3.1c (Conflict detection)
2. What sections/overrides the current conflict detection scans for (currently: "model-routing / execution / review-loop / workflow / session-mode overrides")
3. The AskUserQuestion call pattern (Remove / Keep / Skip-all options in current 3.1c)
4. The Non-Destructive Guarantee wording

Then check if skills/silver-init/references/scaffold-steps.md exists and read it for the full 3.1c procedure.

This task produces no file output — it is a read-and-understand step before the rewrite in Task 2.
  </action>
  <verify>
    <automated>test -f /Users/shafqat/Documents/Projects/silver-bullet/skills/silver-init/SKILL.md && echo "FILE_EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - Executor has read skills/silver-init/SKILL.md and understands current 3.1b/3.1c text before proceeding to Task 2
    - Executor has noted the exact lines of steps 3.1b and 3.1c to be replaced
  </acceptance_criteria>
  <done>Executor understands the current conflict detection scope ("model-routing / execution" only) and the current options (Remove / Keep / Skip-all) — ready to write the replacement in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite steps 3.1b and 3.1c for comprehensive per-section conflict resolution</name>
  <files>skills/silver-init/SKILL.md</files>
  <read_first>
    - skills/silver-init/SKILL.md — already read in Task 1; re-read the exact 3.1b and 3.1c text immediately before editing
  </read_first>
  <action>
Edit skills/silver-init/SKILL.md to replace the current steps 3.1b and 3.1c with the following improved versions.

**Important constraints:**
- Preserve all surrounding steps (3.1a, 3.2, 3.2.5, etc.) unchanged
- Preserve the Non-Destructive Guarantee section at the top (it already covers "Backup before overwrite" and "Never overwrite existing docs" — keep it as-is)
- Only replace the text of steps 3.1b and 3.1c within the "### Fresh setup" section

**New step 3.1b text:**

```
- **3.1b Handle `CLAUDE.md`**: if absent, write from template (`{{PROJECT_NAME}}`, `{{TECH_STACK}}`, `{{GIT_REPO}}`). If present, do NOT overwrite silently — proceed to step 3.1c for comprehensive conflict resolution.
```

**New step 3.1c text (replace the single-line summary with a full procedure):**

```
- **3.1c Conflict resolution** (only when existing `CLAUDE.md` is present — no silent override guarantee):

  **3.1c-1 Build the section inventory.** Parse the existing CLAUDE.md into named sections. A "section" is any `##` or `###` heading and its content. Also treat the preamble (text before the first heading) as a section named "Preamble". For each section, check whether Silver Bullet's template CLAUDE.md (from `templates/CLAUDE.md.base`) contains a corresponding section with the same heading.

  **3.1c-2 Categorize each section:**
  - **SB-owned** (same heading exists in both existing and template): potential conflict — needs user decision.
  - **User-owned** (heading exists only in the existing CLAUDE.md, not in template): preserve unconditionally — no user prompt needed.
  - **New from template** (heading exists only in the template, not in existing CLAUDE.md): add automatically — no conflict.

  **3.1c-3 For each SB-owned section that differs**, use AskUserQuestion with three options:

  > Section: **{section-heading}**
  >
  > Existing content (first 200 chars): {existing_excerpt}
  > Template content (first 200 chars): {template_excerpt}
  >
  > What would you like to do with this section?
  > A. Keep — preserve your existing version unchanged
  > B. Replace — overwrite with the Silver Bullet template version
  > C. Merge — show both versions and let you edit the result

  Wait for the user's response before processing the next conflicting section.

  **3.1c-4 Apply decisions in order:**
  - Keep: leave the existing section unchanged.
  - Replace: substitute the existing section content with the template version.
  - Merge: display both versions side-by-side; ask the user to provide the merged text; write their input.

  **3.1c-5 Append user-owned sections** (identified in step 3.1c-2) at the end of the resolved CLAUDE.md, after all SB-owned sections. These sections are never removed.

  **3.1c-6 Ensure the reference line** `> **Always adhere strictly to this file and silver-bullet.md — they override all defaults.**` is present at the top of the final CLAUDE.md. If absent, prepend it. Do not duplicate it if already present.

  **Non-destructive guarantee**: Steps 3.1c-3 through 3.1c-5 together ensure that no CLAUDE.md section is silently removed or overwritten without explicit user confirmation. User-owned sections (step 3.1c-2) are always preserved without prompting.
```

After applying the edit, verify the file is well-formed by checking:
- The Non-Destructive Guarantee section at the top is intact
- Steps 3.1a, 3.2, 3.2.5, etc. are unchanged
- The new 3.1c text contains all six sub-steps (3.1c-1 through 3.1c-6)
- The phrases "no silent override" and "per-section" and "Keep / Replace / Merge" appear in the file
  </action>
  <verify>
    <automated>grep -q "no silent override" /Users/shafqat/Documents/Projects/silver-bullet/skills/silver-init/SKILL.md && grep -q "Keep.*Replace.*Merge\|Keep / Replace / Merge" /Users/shafqat/Documents/Projects/silver-bullet/skills/silver-init/SKILL.md && grep -q "3.1c-1\|section inventory" /Users/shafqat/Documents/Projects/silver-bullet/skills/silver-init/SKILL.md && grep -q "3.1c-5\|User-owned sections" /Users/shafqat/Documents/Projects/silver-bullet/skills/silver-init/SKILL.md && grep -q "Non-Destructive Guarantee" /Users/shafqat/Documents/Projects/silver-bullet/skills/silver-init/SKILL.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "no silent override" skills/silver-init/SKILL.md
    - grep -q "Keep.*Replace.*Merge\|Keep / Replace / Merge" skills/silver-init/SKILL.md (per-section choice options present)
    - grep -q "3.1c-1\|section inventory" skills/silver-init/SKILL.md (section inventory step present)
    - grep -q "User-owned\|user-owned" skills/silver-init/SKILL.md (user-owned section preservation stated)
    - grep -q "3.1c-6\|reference line\|Always adhere" skills/silver-init/SKILL.md (reference line step present)
    - grep -q "Non-Destructive Guarantee" skills/silver-init/SKILL.md (guarantee section still present)
    - The "model-routing / execution / review-loop" narrow scope from the OLD 3.1c is REPLACED (the new version covers all sections); check that the old narrowly-scoped language is gone: the new text applies to ALL sections, not just overrides
  </acceptance_criteria>
  <done>skills/silver-init/SKILL.md steps 3.1b and 3.1c updated: 3.1b redirects to 3.1c instead of silently overwriting; 3.1c has a 6-sub-step procedure covering section inventory, categorization, per-section AskUserQuestion (Keep/Replace/Merge), decision application, user-owned section preservation, and reference line guarantee. The phrase "no silent override" appears explicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing CLAUDE.md content → SB-controlled output | User's custom CLAUDE.md sections must not be overwritten without consent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-064-03-01 | Tampering | CLAUDE.md overwrite via /silver:init | mitigate | New 3.1c-2 categorization explicitly preserves User-owned sections; 3.1c-3 requires AskUserQuestion before replacing any SB-owned section |
| T-064-03-02 | Information Disclosure | AskUserQuestion shows content excerpts | accept | Excerpts are from the user's own CLAUDE.md; no external data exposure |
</threat_model>

<verification>
1. grep -q "no silent override" skills/silver-init/SKILL.md
2. grep -q "Keep / Replace / Merge\|Keep.*Replace.*Merge" skills/silver-init/SKILL.md
3. grep -q "3.1c-1\|section inventory" skills/silver-init/SKILL.md
4. grep -q "User-owned" skills/silver-init/SKILL.md
5. grep -q "Non-Destructive Guarantee" skills/silver-init/SKILL.md (guarantee section unchanged)
6. The old narrow-scope text ("model-routing / execution / review-loop / workflow / session-mode overrides; ask per-hit via AskUserQuestion (Remove / Keep / Skip-all)") is replaced by the new comprehensive procedure
</verification>

<success_criteria>
- INIT-01: skills/silver-init/SKILL.md step 3.1b and 3.1c updated with no-silent-override guarantee; new 3.1c offers explicit per-section choice (Keep / Replace / Merge) for all conflicting sections; user-owned sections preserved unconditionally; reference line step included
</success_criteria>

<output>
After completion, create `.planning/phases/064-verification-init-improvements/064-03-SUMMARY.md`
</output>
