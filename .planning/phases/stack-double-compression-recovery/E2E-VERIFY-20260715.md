# E2E Verification: Stack Double-Compression Recovery

**Date:** 2026-07-15  
**Repo:** `alo-exp/silver-bullet` @ `main` (`d6073bb6`)  
**Plugin version:** `0.51.7` (post-v0.51.4; PR #243 symbols present)  
**LeanCTX version:** `lean-ctx 3.9.9` (official) — matches `min_version: 3.9.9`

## Executive Verdict

**RESOLVED** — The `sb_stack_double_compression` mutex wedge is fixed. False-positive detection on `install-leanctx-sb.sh` paths no longer dirties the mutex; real violations still block; compliant routed MCP (`ctx_search`) clears the mutex; five-tool routing remains stable with LeanCTX 3.9.9.

One **non-blocking nuance**: `sb-doctor.sh --fix` on a minimally scaffolded temp project may not reach D20 in a single pass when other doctor checks fail first (`doctor_apply_fixes` fixes one check per invocation). Lib-level `sb_stack_clear_mutex_violations` and compliant MCP self-heal are verified.

---

## Automated Test Matrix

| Suite | Result | Count |
|-------|--------|-------|
| `tests/hooks/test-stack-compression-coordinator.sh` | **PASS** | 20 passed, 0 failed |
| `tests/hooks/test-five-tool-mutual-exclusion.sh` | **PASS** | 22 passed, 0 failed |
| `tests/hooks/test-agentmemory-gate.sh` | **PASS** | 8 passed, 0 failed |
| `tests/hooks/test-agentmemory-gate-lib.sh` | **PASS** | 9 passed, 0 failed |
| `tests/scripts/test-silver-doctor.sh` | **PASS** | 43 passed, 0 failed |
| `tests/scripts/test-optimize-five-tool-stack.sh` | **PASS** | 13 passed, 0 failed |
| **Total** | **PASS** | **115 / 115** |

---

## Behavioral Probe Matrix

| Probe | Description | Result |
|-------|-------------|--------|
| **A** | `install-leanctx-sb.sh` path false positive | **PASS** |
| **B** | Violation → block → `ctx_search` recovery → Read allowed | **PASS** |
| **C** | Doctor D20 detect / `--fix` on bare temp | Detect **PASS**; single-pass `--fix` **PARTIAL** |
| **D** | `silver-clear-stack-state` skill + paths | **PASS** |
| **E** | LeanCTX 3.9.9 compatibility | **PASS** |

---

## LeanCTX Upgrade — No Regressions

- `lctx_*` namespace unchanged; coordinator deny rules intact
- `LEANCTX_DISABLE_SHELL_MCP=1` in `scripts/install-leanctx-sb.sh`
- Five-tool routing table matches `optimization_profiles.five_tool_routed`

---

## Recommended Follow-ups (low priority)

1. Add `test-silver-doctor.sh` case: `--fix` clears D20 when it is the only failing check.
2. Document that `--fix` processes one failed check per run.
