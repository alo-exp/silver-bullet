---
phase: 28-documentation-update
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/doc-scheme.md
  - templates/doc-scheme.md.base
  - docs/ENFORCEMENT.md
  - docs/workflows/full-dev-cycle.md
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03
  - DOCS-04

must_haves:
  truths:
    - "doc-scheme.md lists WORKFLOW.md, VALIDATION.md, UI-SPEC.md, UI-REVIEW.md, SECURITY.md as planning artifacts"
    - "ENFORCEMENT.md describes WORKFLOW.md-first enforcement pattern for all 5 hooks"
    - "full-dev-cycle.md has a demotion notice indicating it is an example composition"
    - "templates/doc-scheme.md.base matches docs/doc-scheme.md"
  artifacts:
    - path: "docs/doc-scheme.md"
      provides: "Updated artifact documentation"
      contains: "WORKFLOW.md"
    - path: "docs/ENFORCEMENT.md"
      provides: "Hook enforcement with WORKFLOW.md awareness"
      contains: "WORKFLOW.md-first"
    - path: "docs/workflows/full-dev-cycle.md"
      provides: "Demoted dev cycle reference"
      contains: "example composition"
    - path: "templates/doc-scheme.md.base"
      provides: "Template matching doc-scheme.md"
      contains: "WORKFLOW.md"
  key_links:
    - from: "docs/ENFORCEMENT.md"
      to: "hooks/dev-cycle-check.sh"
      via: "describes enforcement behavior"
      pattern: "WORKFLOW.md"
---

<objective>
Update doc-scheme.md with new artifacts, ENFORCEMENT.md with Phase 26 hook changes, and demote full-dev-cycle.md.

Purpose: Three documentation files need updates to reflect composable paths architecture changes.
Output: Updated doc-scheme.md(.base), ENFORCEMENT.md, and full-dev-cycle.md.
</objective>

<execution_context>
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/workflows/execute-plan.md
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-documentation-update/28-CONTEXT.md
@docs/doc-scheme.md
@templates/doc-scheme.md.base
@docs/ENFORCEMENT.md
@docs/workflows/full-dev-cycle.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update doc-scheme.md and template with new planning artifacts</name>
  <files>docs/doc-scheme.md, templates/doc-scheme.md.base</files>
  <read_first>
    - docs/doc-scheme.md (full file — 105 lines)
    - templates/doc-scheme.md.base (full file)
    - templates/workflow.md.base (to understand WORKFLOW.md structure for description)
  </read_first>
  <action>
Per D-06 and D-07: Update the `.planning/` ephemeral planning artifacts table in doc-scheme.md to include these new artifacts (if not already present):

| Artifact | Created by | Purpose |
|----------|-----------|---------|
| `WORKFLOW.md` | `/silver` composer | Composition state — path log, dynamic insertions, next path |
| `VALIDATION.md` | `silver-validate` | Pre-build validation results |
| `UI-SPEC.md` | `gsd-ui-phase` | UI specification — layout, components, interactions |
| `UI-REVIEW.md` | `gsd-ui-review` | UI review findings — 6-pillar assessment |
| `SECURITY.md` | `silver:security` | Security audit findings — SENTINEL results |

Add these rows to the existing table in the "`.planning/` — Ephemeral Planning Artifacts" section. Keep existing rows (PROJECT.md, ROADMAP.md, STATE.md, REQUIREMENTS.md, Phase dirs). SECURITY.md already appears in the optional docs/ files table — the new entry is for .planning/SECURITY.md (different location, different purpose).

Also add a non-redundancy rule 6: "WORKFLOW.md is the composition execution log — silver-bullet.md §2h describes the architecture, WORKFLOW.md tracks the instance"

After updating doc-scheme.md, copy the entire file content to templates/doc-scheme.md.base so they match exactly.
  </action>
  <verify>
    <automated>grep -c "WORKFLOW.md" docs/doc-scheme.md | grep -q "[1-9]" && grep -c "UI-SPEC.md" docs/doc-scheme.md | grep -q "[1-9]" && diff docs/doc-scheme.md templates/doc-scheme.md.base && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - doc-scheme.md planning artifacts table includes WORKFLOW.md, VALIDATION.md, UI-SPEC.md, UI-REVIEW.md, SECURITY.md
    - Non-redundancy rule 6 present
    - templates/doc-scheme.md.base is identical to docs/doc-scheme.md
  </acceptance_criteria>
  <done>doc-scheme.md and template updated with all new planning artifacts</done>
</task>

<task type="auto">
  <name>Task 2: Update ENFORCEMENT.md with WORKFLOW.md-first pattern</name>
  <files>docs/ENFORCEMENT.md</files>
  <read_first>
    - docs/ENFORCEMENT.md (full file — 62 lines)
    - hooks/dev-cycle-check.sh (grep for WORKFLOW.md patterns — understand the primary gate logic)
    - hooks/completion-audit.sh (grep for WORKFLOW.md patterns)
    - hooks/compliance-status.sh (grep for path progress display)
    - hooks/prompt-reminder.sh (grep for WORKFLOW.md position)
    - hooks/spec-floor-check.sh (grep for advisory downgrade)
  </read_first>
  <action>
Per D-08 and D-09: Update ENFORCEMENT.md to reflect Phase 26 hook modifications.

1. In the "The 7 Layers" table, update descriptions for affected hooks:
   - Layer 2 (Dev Cycle Gate): Add note that it uses WORKFLOW.md Path Log as primary gate with legacy fallback
   - Layer 3 (Completion Audit): Same WORKFLOW.md-first pattern
   - Layer 5 (Compliance Score): Now shows path progress (e.g., PATH 5/12) alongside skill count

2. Update the "Dev Cycle Gate (4-Stage)" section to note: "When WORKFLOW.md exists, the dev-cycle gate checks Path Log completion as the primary signal. The 4-stage skill-based check is the legacy fallback for projects without WORKFLOW.md."

3. Add a NEW section after "Dev Cycle Gate (4-Stage)" titled "## WORKFLOW.md-First Enforcement Pattern" (per D-09):

```
## WORKFLOW.md-First Enforcement Pattern

Phase 26 introduced dual-mode enforcement. All hooks check WORKFLOW.md first, falling back to legacy skill markers when WORKFLOW.md is absent.

| Hook | WORKFLOW.md Mode | Legacy Fallback |
|------|-----------------|-----------------|
| `dev-cycle-check.sh` | Path Log shows planning paths complete | 4-stage skill marker check |
| `completion-audit.sh` | Path Log shows all required paths complete | `required_deploy` skill list check |
| `compliance-status.sh` | Shows path progress (PATH N/M) | Shows skill count only |
| `prompt-reminder.sh` | Includes current WORKFLOW.md position | Omits composition context |
| `spec-floor-check.sh` | Advisory when PATH 4 excluded from composition | Hard gate always |

Detection: hooks check for `.planning/WORKFLOW.md` existence. Present = new mode, absent = legacy mode.
```

4. Update the "Scalability" section to note: "Dual-mode hooks support both composable-paths projects (WORKFLOW.md) and legacy projects (skill markers)."

Keep the file concise — ENFORCEMENT.md should stay under 100 lines.
  </action>
  <verify>
    <automated>grep -c "WORKFLOW.md-First" docs/ENFORCEMENT.md | grep -q "[1-9]" && grep -c "Legacy Fallback" docs/ENFORCEMENT.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - "WORKFLOW.md-First Enforcement Pattern" section exists with 5-row hook table
    - Dev Cycle Gate section mentions WORKFLOW.md as primary gate
    - Compliance Score description mentions path progress
    - File stays under 100 lines
  </acceptance_criteria>
  <done>ENFORCEMENT.md reflects all Phase 26 hook modifications</done>
</task>

<task type="auto">
  <name>Task 3: Add demotion notice to full-dev-cycle.md</name>
  <files>docs/workflows/full-dev-cycle.md</files>
  <read_first>
    - docs/workflows/full-dev-cycle.md (first 15 lines — current header)
  </read_first>
  <action>
Per D-10 and D-11: Add a demotion notice at the top of full-dev-cycle.md, immediately after the "# Full Dev Cycle Workflow" heading and before the existing blockquote.

Insert this notice:

```
> **NOTE:** This document shows a representative path composition for feature development.
> The actual workflow is dynamically composed by `/silver` based on context.
> See `silver-bullet.md` §2h for the composable paths architecture and
> `docs/composable-paths-contracts.md` for the full path catalog.
```

Do NOT delete or modify any other content in the file (per D-11). The existing enforced blockquote and all subsequent content stays as-is.
  </action>
  <verify>
    <automated>head -10 docs/workflows/full-dev-cycle.md | grep -q "representative path composition" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Demotion notice appears between heading and existing blockquote
    - Notice mentions "representative path composition", "/silver", "silver-bullet.md §2h", and "composable-paths-contracts.md"
    - Rest of file is unchanged
  </acceptance_criteria>
  <done>full-dev-cycle.md demoted to example composition with clear notice</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — documentation-only changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | T (Tampering) | docs/*.md | accept | Documentation files — no runtime impact, version controlled |
</threat_model>

<verification>
- doc-scheme.md lists all 5 new planning artifacts and template matches
- ENFORCEMENT.md has WORKFLOW.md-first section with dual-mode hook table
- full-dev-cycle.md has demotion notice at top without other modifications
</verification>

<success_criteria>
doc-scheme.md finalized with new artifacts, ENFORCEMENT.md reflects Phase 26 hooks, full-dev-cycle.md demoted to example.
</success_criteria>

<output>
After completion, create `.planning/phases/28-documentation-update/28-02-SUMMARY.md`
</output>
