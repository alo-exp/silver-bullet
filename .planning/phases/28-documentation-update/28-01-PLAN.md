---
phase: 28-documentation-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - silver-bullet.md
  - templates/silver-bullet.md.base
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "silver-bullet.md §2h describes composable paths architecture with 18-path catalog"
    - "Workflow table still present with silver:fast updated for 3-tier triage"
    - "templates/silver-bullet.md.base §2h matches silver-bullet.md §2h exactly"
  artifacts:
    - path: "silver-bullet.md"
      provides: "§2h with composable paths architecture"
      contains: "Composable Paths"
    - path: "templates/silver-bullet.md.base"
      provides: "Template matching silver-bullet.md §2h"
      contains: "Composable Paths"
  key_links:
    - from: "silver-bullet.md"
      to: "docs/composable-paths-contracts.md"
      via: "cross-reference in §2h"
      pattern: "composable-paths-contracts"
---

<objective>
Rewrite silver-bullet.md §2h to describe composable paths architecture, and sync templates/silver-bullet.md.base.

Purpose: §2h is the primary reference for how Silver Bullet workflows operate — it must reflect the new composable paths architecture.
Output: Updated §2h in both silver-bullet.md and templates/silver-bullet.md.base.
</objective>

<execution_context>
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/workflows/execute-plan.md
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-documentation-update/28-CONTEXT.md
@docs/composable-paths-contracts.md
@silver-bullet.md
@templates/silver-bullet.md.base
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite silver-bullet.md §2h with composable paths architecture</name>
  <files>silver-bullet.md</files>
  <read_first>
    - silver-bullet.md (lines 294-331 — current §2h)
    - docs/composable-paths-contracts.md (all 18 path contracts — source of truth for path names and descriptions)
    - skills/silver-fast/SKILL.md (3-tier triage details for D-03)
  </read_first>
  <action>
Rewrite §2h (lines 294-331 of silver-bullet.md). Keep the section heading "### 2h. SB Orchestrated Workflows" and the "---" delimiter at the end.

The new §2h must contain these subsections IN ORDER (per D-01 through D-04):

1. **Composable paths overview paragraph** (per D-02): Before the workflow table, add a paragraph explaining: Silver Bullet workflows are composed from a catalog of 18 reusable paths (PATH 0-17). Each path is a self-contained building block with defined prerequisites, trigger conditions, steps, and exit conditions. The /silver router classifies context and composes an ordered chain of paths. WORKFLOW.md tracks execution state — which paths have run, which are next, and any dynamic insertions.

2. **Workflow table** (per D-01): Keep the existing 8-workflow table format (Workflow | Entry triggers | First step) but update the silver:fast row (per D-03) to: `silver:fast` | "trivial", "quick fix", "typo", "one-liner", "config value" | 3-tier complexity triage: Tier 1 (trivial) → gsd-fast, Tier 2 (medium) → gsd-quick with flags, Tier 3 (complex) → escalate to silver-feature

3. **Workflow enforcement rules** block: Keep as-is (no changes needed).

4. **Composable Paths subsection** (per D-04): After the workflow enforcement rules and step-skip protocol, add a new subsection:

```
#### Composable Paths Catalog

Each workflow composes from these 18 paths. See `docs/composable-paths-contracts.md` for full contracts.

| Path | Name | Purpose |
|------|------|---------|
| PATH 0 | BOOTSTRAP | Project setup — PROJECT.md, ROADMAP.md, REQUIREMENTS.md, STATE.md |
| PATH 1 | ORIENT | Codebase intelligence — gsd-intel, gsd-scan, gsd-map-codebase |
| PATH 2 | EXPLORE | Research spike — silver:explore, MultAI research |
| PATH 3 | IDEATE | Product brainstorming — silver:brainstorm, product-brainstorming |
| PATH 4 | SPECIFY | Spec creation — silver-ingest, write-spec, silver-spec, silver-validate |
| PATH 5 | PLAN | Phase planning — discuss-phase, writing-plans, gsd-plan-phase |
| PATH 6 | DESIGN CONTRACT | UI/UX design — design-system, ux-copy, gsd-ui-phase |
| PATH 7 | EXECUTE | Implementation — gsd-execute-phase with TDD as-needed |
| PATH 8 | UI QUALITY | UI review — design-critique, gsd-ui-review, accessibility-review |
| PATH 9 | REVIEW | Artifact review — 3 parallel review layers with triage + fix |
| PATH 10 | SECURE | Security audit — SENTINEL, threat modeling |
| PATH 11 | VERIFY | Verification — gsd-verify-work, verification-before-completion |
| PATH 12 | QUALITY GATE | Quality dimensions — 9-dimension check with 4-state disambiguation |
| PATH 13 | SHIP | Phase shipping — gsd-ship, PR creation |
| PATH 14 | DEBUG | Debugging — systematic-debugging, gsd-debug with resume semantics |
| PATH 15 | DESIGN HANDOFF | Design-to-dev handoff — runs inside PATH 17 only |
| PATH 16 | DOCUMENT | Documentation — changelog, knowledge, lessons |
| PATH 17 | RELEASE | Milestone release — quality gates, UAT, version bump |
```

Do NOT change anything outside §2h. The section starts at "### 2h." and ends at the "---" line before "## Spec Lifecycle".
  </action>
  <verify>
    <automated>grep -c "Composable Paths" silver-bullet.md | grep -q "[1-9]" && grep -c "PATH 17" silver-bullet.md | grep -q "[1-9]" && grep "silver:fast" silver-bullet.md | grep -q "3-tier" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - §2h contains "Composable Paths" overview paragraph before workflow table
    - Workflow table has 8 rows with silver:fast mentioning "3-tier complexity triage"
    - Composable Paths Catalog subsection lists all 18 paths (PATH 0 through PATH 17)
    - Cross-reference to docs/composable-paths-contracts.md present
    - No changes outside §2h boundaries
  </acceptance_criteria>
  <done>silver-bullet.md §2h fully rewritten with composable paths architecture</done>
</task>

<task type="auto">
  <name>Task 2: Sync templates/silver-bullet.md.base §2h to match</name>
  <files>templates/silver-bullet.md.base</files>
  <read_first>
    - silver-bullet.md (the just-updated §2h section)
    - templates/silver-bullet.md.base (find current §2h location — may differ in line numbers)
  </read_first>
  <action>
Per D-05: Copy the exact §2h content from silver-bullet.md into templates/silver-bullet.md.base, replacing the existing §2h section. The section starts at "### 2h. SB Orchestrated Workflows" and ends at the "---" before the next section.

The template may have slightly different surrounding content (e.g., placeholder §10 overrides) but §2h must be identical word-for-word between the two files.

After updating, run a diff between the two files' §2h sections to confirm they match.
  </action>
  <verify>
    <automated>diff <(sed -n '/### 2h\./,/^---$/p' silver-bullet.md) <(sed -n '/### 2h\./,/^---$/p' templates/silver-bullet.md.base) && echo "MATCH" || echo "MISMATCH"</automated>
  </verify>
  <acceptance_criteria>
    - §2h in templates/silver-bullet.md.base is identical to §2h in silver-bullet.md
    - No other sections of templates/silver-bullet.md.base are modified
  </acceptance_criteria>
  <done>templates/silver-bullet.md.base §2h matches silver-bullet.md §2h exactly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — documentation-only changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | T (Tampering) | silver-bullet.md | accept | Documentation file — no runtime impact, version controlled |
</threat_model>

<verification>
- §2h in silver-bullet.md contains composable paths overview, workflow table, and 18-path catalog
- §2h in templates/silver-bullet.md.base matches silver-bullet.md exactly
- No content outside §2h was modified in either file
</verification>

<success_criteria>
silver-bullet.md §2h describes composable paths architecture instead of fixed pipeline, and template is in sync.
</success_criteria>

<output>
After completion, create `.planning/phases/28-documentation-update/28-01-SUMMARY.md`
</output>
