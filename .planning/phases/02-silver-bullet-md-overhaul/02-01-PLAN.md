---
phase: 02-silver-bullet-md-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - silver-bullet.md
  - templates/silver-bullet.md.base
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-03
  - INST-04

must_haves:
  truths:
    - "Claude can explain what any GSD workflow step does using only silver-bullet.md (no GSD docs needed at runtime)"
    - "At each workflow transition (DISCUSS->PLAN, EXECUTE->VERIFY, etc.), Claude proactively tells the user what completed, what comes next, and what to watch for"
    - "Claude suggests utility commands based on context -- /gsd:debug on failure, /gsd:quick for small tasks, /gsd:resume-work after session breaks"
    - "All existing enforcement rules (sections 0-9) remain intact and functional"
    - "templates/silver-bullet.md.base matches silver-bullet.md structure with only placeholder substitutions differing"
  artifacts:
    - path: "silver-bullet.md"
      provides: "Complete enforcement instructions with GSD process knowledge, hand-holding, and utility awareness"
      contains: "GSD Process Knowledge"
    - path: "templates/silver-bullet.md.base"
      provides: "Template with placeholders matching silver-bullet.md structure"
      contains: "GSD Process Knowledge"
  key_links:
    - from: "silver-bullet.md section 2"
      to: "docs/workflows/full-dev-cycle.md"
      via: "Active workflow reference with transition narration"
      pattern: "docs/workflows/"
    - from: "silver-bullet.md GSD Process Knowledge"
      to: "GSD commands"
      via: "Concise lookup table of what each command does"
      pattern: "/gsd:"
    - from: "templates/silver-bullet.md.base"
      to: "silver-bullet.md"
      via: "Structure parity with placeholder substitutions"
      pattern: "\\{\\{PROJECT_NAME\\}\\}"
---

<objective>
Overhaul silver-bullet.md to add GSD process knowledge, hand-holding instructions, and
utility command awareness so Claude can guide users through every workflow transition without
reading GSD's own documentation. Update templates/silver-bullet.md.base to match.

Purpose: After this plan, Claude reads silver-bullet.md at session start and immediately
understands what every GSD step does, when to suggest utility commands, and what to tell
the user at each transition -- eliminating dependency on GSD docs at runtime.

Output: Updated silver-bullet.md (target under 500 lines) and matching template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-silver-bullet-md-overhaul/02-CONTEXT.md

@silver-bullet.md
@templates/silver-bullet.md.base

<!-- Source material for GSD process knowledge extraction -->
@docs/workflows/full-dev-cycle.md
@docs/workflows/devops-cycle.md

<interfaces>
<!-- Current silver-bullet.md structure (367 lines, 10 sections): -->
<!-- S0: Session Startup (lines 10-19) -->
<!-- S1: Automated Enforcement (lines 23-44) -->
<!-- S2: Active Workflow (lines 48-58) -->
<!-- S3: NON-NEGOTIABLE RULES (lines 62-94) -->
<!-- S3a: Review Loop Enforcement (lines 96-107) -->
<!-- S3b: GSD Command Tracking (lines 109-126) -->
<!-- S4: Session Mode (lines 130-192) -->
<!-- S5: Model Routing (lines 196-214) -->
<!-- S6: GSD/Superpowers Ownership Rules (lines 218-238) -->
<!-- S7: File Safety Rules (lines 242-253) -->
<!-- S8: Third-Party Plugin Boundary (lines 257-271) -->
<!-- S9: Pre-Release Quality Gate (lines 275-367) -->

<!-- Template placeholders in .base file: -->
<!-- {{PROJECT_NAME}} - in heading (line 4) -->
<!-- {{ACTIVE_WORKFLOW}} - in S2 active workflow path (line 52) -->

<!-- Utility commands from workflow files (for D-03 content): -->
<!-- /gsd:debug - parallel debug agents, diagnose root causes -->
<!-- /gsd:quick - small ad-hoc tasks with atomic commits -->
<!-- /gsd:fast - trivial inline tasks, no subagent -->
<!-- /gsd:resume-work - restore context from STATE.md + HANDOFF.json -->
<!-- /gsd:pause-work - create handoff files for clean resume -->
<!-- /gsd:progress - rich progress report with next actions -->
<!-- /gsd:next - auto-advance to next logical step -->
<!-- /gsd:add-phase - append phase to roadmap -->
<!-- /gsd:insert-phase - insert decimal phase between existing -->
<!-- /gsd:review - cross-AI peer review (Gemini, Codex, CodeRabbit) -->
<!-- /gsd:audit-milestone - aggregate verifications, check integration -->
<!-- /gsd:autonomous - drive remaining phases without manual intervention -->
<!-- /gsd:complete-milestone - mark milestone done, archive, tag -->
<!-- /gsd:map-codebase - analyze codebase into 7 structured docs -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GSD process knowledge, hand-holding, utility awareness, and transition guidance to silver-bullet.md; sync template</name>
  <files>silver-bullet.md, templates/silver-bullet.md.base</files>
  <action>

**READ FIRST:** Read silver-bullet.md (367 lines) and templates/silver-bullet.md.base (364 lines) in full. Read docs/workflows/full-dev-cycle.md lines 127-380 (per-phase loop with all GSD steps) and lines 586-643 (transition + utility table). Read docs/workflows/devops-cycle.md lines 704-750 (transition back + utility table).

**STRUCTURE:** The final file keeps all existing sections (S0-S9) intact. Three new subsections are inserted. The final structure is:

```
S0: Session Startup (UNCHANGED per D-05)
S1: Automated Enforcement (UNCHANGED per D-06)
S2: Active Workflow (EXPANDED per D-02, D-16)
  S2a: Workflow Transitions (NEW per D-04)
  S2b: GSD Process Knowledge (NEW per D-01)
  S2c: Utility Command Awareness (NEW per D-03)
S3: NON-NEGOTIABLE RULES (UNCHANGED per D-07)
S3a: Review Loop Enforcement (UNCHANGED per D-08)
S3b: GSD Command Tracking (UNCHANGED per D-09)
S4: Session Mode (UNCHANGED per D-10)
S5: Model Routing (UNCHANGED per D-11)
S6: GSD/Superpowers Ownership Rules (MINOR UPDATE per D-17)
S7: File Safety Rules (UNCHANGED per D-13)
S8: Third-Party Plugin Boundary (UNCHANGED per D-14)
S9: Pre-Release Quality Gate (UNCHANGED per D-15)
```

**CHANGES TO MAKE:**

1. **Expand S2 (Active Workflow) -- per D-02, D-16:**

   After the existing S2 content (active workflow path, skill-not-found rule, anti-skip), add hand-holding guidance. This is directive ("say X to the user"), not descriptive. Example structure:

   ```markdown
   ### Hand-Holding at Transitions

   At each workflow transition, proactively narrate to the user:

   | Transition | What to say |
   |------------|-------------|
   | Session start -> DISCUSS | "Starting the planning phase. I'll ask questions to understand what you want to build before any code is written." |
   | DISCUSS -> QUALITY GATES | "Discussion complete -- CONTEXT.md captured your decisions. Running quality gates next to validate the approach before planning." |
   | QUALITY GATES -> PLAN | "Quality gates passed. Now creating execution plans -- these break your phase into concrete tasks with verification criteria." |
   | PLAN -> EXECUTE | "Plans created. Executing now -- each task produces atomic commits. You'll see progress as files are created/modified." |
   | EXECUTE -> VERIFY | "Execution complete. Running verification to confirm everything works end-to-end against the phase requirements." |
   | VERIFY -> REVIEW | "Verification passed. Final review checks for issues before we move to the next phase or finalize." |
   | Last phase VERIFY -> FINALIZE | "All phases complete. Moving to finalization -- testing, documentation, and deployment preparation." |
   ```

   Keep it to a concise table. 7-8 rows max. The executor should not add verbose prose.

2. **Add S2a (Workflow Transitions) -- per D-04:**

   New subsection immediately after the hand-holding table. Covers dev-to-DevOps and DevOps-to-dev transitions. Content distilled from STEP 7 of full-dev-cycle.md (lines 586-619) and STEP 8 of devops-cycle.md (lines 704-727). Keep it brief -- 10-15 lines:

   ```markdown
   ### 2a. Workflow Transitions

   Two workflows exist: `full-dev-cycle` (application development) and `devops-cycle`
   (infrastructure). Transitions happen after RELEASE:

   **Dev -> DevOps:** After shipping an application release, if IaC files are present,
   deploy checklist flagged gaps, or user requests it -- offer to switch `active_workflow`
   in `.silver-bullet.json` to `devops-cycle`.

   **DevOps -> Dev:** After deploying infrastructure, offer to switch back to
   `full-dev-cycle` for the next milestone of feature development.

   **What is preserved:** Everything -- `.planning/` artifacts, `.silver-bullet.json`
   config, state, git history. Only `active_workflow` changes.
   ```

3. **Add S2b (GSD Process Knowledge) -- per D-01:**

   New subsection. This is the core INST-01 requirement -- Claude's internalized knowledge of what each GSD command does. Concise lookup table format (per CONTEXT.md specifics: "concise lookup table, not verbose guide"). Include ALL 20 guided commands mentioned in ORCH-03.

   Structure as TWO tables -- one for core workflow commands (the main loop), one for project lifecycle commands:

   ```markdown
   ### 2b. GSD Process Knowledge

   Claude reads this once at session start and can explain any step to the user
   without consulting GSD workflow files.

   **Core Workflow Commands (per-phase loop):**

   | Command | What it does | Produces |
   |---------|-------------|----------|
   | `/gsd:new-project` | Deep questioning about vision, optional research, requirements scoping, roadmap generation | PROJECT.md, REQUIREMENTS.md, ROADMAP.md |
   | `/gsd:new-milestone` | Loads previous context, gathers goals for new milestone, defines scoped requirements | Fresh ROADMAP.md carrying forward accumulated context |
   | `/gsd:discuss-phase` | Conversational requirements gathering for current phase -- asks questions, captures decisions | CONTEXT.md with locked decisions (D-01, D-02...) |
   | `/gsd:plan-phase` | Decomposes phase into parallel-optimized plans with 2-3 tasks each, dependency graphs, verification criteria | PLAN.md files with wave structure |
   | `/gsd:execute-phase` | Wave-based execution -- spawns subagents per plan, atomic commits per task, auto-resumes incomplete plans | Committed code + SUMMARY.md per plan |
   | `/gsd:verify-work` | Checks must-haves, runs automated tests, validates artifacts exist and connect correctly | VERIFICATION.md with pass/fail per truth |
   | `/gsd:ship` | Runs deployment checklist, pushes to remote, confirms CI green | Deployed, CI-green codebase |

   **Project Lifecycle Commands:**

   | Command | What it does | When to use |
   |---------|-------------|-------------|
   | `/gsd:map-codebase` | Analyzes existing codebase into 7 structured docs (stack, architecture, patterns, etc.) | Brownfield projects before /gsd:new-project |
   | `/gsd:autonomous` | Drives remaining phases end-to-end (discuss, plan, execute per phase) | Multi-phase autonomous execution |
   | `/gsd:audit-milestone` | Aggregates phase verifications, checks cross-phase integration, requirements coverage | End of milestone before completing |
   | `/gsd:complete-milestone` | Marks milestone done, creates MILESTONES.md record, archives artifacts, tags release | After all phases verified and shipped |
   | `/gsd:add-phase` | Appends new phase to end of current milestone roadmap | Discovered work not in original roadmap |
   | `/gsd:insert-phase` | Inserts decimal phase between existing (e.g., 3.1 between 3 and 4) | Urgent fix before next planned phase |
   | `/gsd:review` | Cross-AI peer review -- invokes Gemini, Codex, CodeRabbit independently | Adversarial review before execution |
   | `/gsd:next` | Detects current state, auto-advances to next logical step | Unsure what comes next |
   ```

   The descriptions must be precise (taken from workflow files) but concise (one sentence each). Do NOT pad with explanatory prose between or after the tables.

4. **Add S2c (Utility Command Awareness) -- per D-03:**

   New subsection with context-based triggers. These are directives -- "when X happens, suggest Y". Structure as a trigger table:

   ```markdown
   ### 2c. Utility Command Awareness

   Suggest these commands based on context -- do not wait for the user to ask.

   | Context trigger | Suggest | Why |
   |----------------|---------|-----|
   | Execution fails, tests break, unexpected error | `/gsd:debug` | Spawns parallel agents to diagnose root cause |
   | User mentions a small change outside the current phase | `/gsd:quick` | Handles ad-hoc tasks with atomic commits + state tracking |
   | Change is truly trivial (typo, config value, 3 files max) | `/gsd:fast` | Inline execution, no subagent overhead |
   | New session on existing project | `/gsd:resume-work` | Restores full context from STATE.md + HANDOFF.json |
   | User wants to stop mid-work | `/gsd:pause-work` | Creates handoff files for clean session resume |
   | User asks "where are we?" or "what's left?" | `/gsd:progress` | Rich progress report with next actions |
   | User seems unsure what step is next | `/gsd:next` | Auto-advances to the next logical step |
   ```

   7 rows. Concise. No additional prose.

5. **Minor update to S6 -- per D-17:**

   Current S6 opening says: "GSD is the authoritative execution orchestrator."

   Add one clarifying sentence after the opening paragraph to reflect the richer orchestration relationship:

   "Silver Bullet orchestrates the user experience and delegates execution to GSD. Silver Bullet owns what to do and when; GSD owns how."

   Do NOT change the hard rules, the anti-skip block, or any other S6 content.

6. **All other sections (S0, S1, S3, S3a, S3b, S4, S5, S7, S8, S9) -- per D-05 through D-15:**

   Copy these sections EXACTLY. Byte-identical. No rewording, no reordering, no "improvements."

7. **Update templates/silver-bullet.md.base -- per D-18:**

   After silver-bullet.md is finalized, create the matching .base template:
   - Copy the entire finalized silver-bullet.md
   - Replace the heading project name ("silver-bullet") with `{{PROJECT_NAME}}`
   - Replace the active workflow path value with `{{ACTIVE_WORKFLOW}}`
   - Keep everything else identical

   Verify: the only differences between silver-bullet.md and the .base file should be `{{PROJECT_NAME}}` vs "silver-bullet" and `{{ACTIVE_WORKFLOW}}` vs "full-dev-cycle".

**LINE BUDGET:** The current file is 367 lines. The new content adds approximately:
- Hand-holding table: ~15 lines
- S2a transitions: ~15 lines
- S2b process knowledge tables: ~40 lines
- S2c utility awareness table: ~15 lines
- S6 addition: ~2 lines

Estimated total: ~454 lines. Well under 500.

**WHAT TO AVOID and WHY:**
- Do NOT reorganize or renumber existing sections (S0-S9 numbering is referenced by hooks and other files)
- Do NOT add verbose explanatory prose between sections (the file is read by Claude at session start -- concise = less context consumed)
- Do NOT duplicate content already in the workflow files (silver-bullet.md is a lookup reference, not a replacement for the workflow)
- Do NOT add new anti-skip blocks for the new subsections (they are informational, not enforcement rules)
- Do NOT change any existing anti-skip text (hooks depend on exact phrasing)

  </action>
  <verify>
    <automated>
# Line count under 500
LINE_COUNT=$(wc -l < silver-bullet.md)
test "$LINE_COUNT" -le 500 && echo "PASS: $LINE_COUNT lines (under 500)" || echo "FAIL: $LINE_COUNT lines (over 500)"

# All original sections present
for section in "## 0. Session Startup" "## 1. Automated Enforcement" "## 2. Active Workflow" "## 3. NON-NEGOTIABLE RULES" "## 3a. Review Loop" "## 3b. GSD Command Tracking" "## 4. Session Mode" "## 5. Model Routing" "## 6. GSD / Superpowers" "## 7. File Safety" "## 8. Third-Party Plugin" "## 9. Pre-Release Quality"; do
  grep -q "$section" silver-bullet.md && echo "PASS: Found '$section'" || echo "FAIL: Missing '$section'"
done

# New sections present
grep -q "GSD Process Knowledge" silver-bullet.md && echo "PASS: GSD Process Knowledge section" || echo "FAIL: Missing GSD Process Knowledge"
grep -q "Utility Command Awareness" silver-bullet.md && echo "PASS: Utility Command Awareness section" || echo "FAIL: Missing Utility Command Awareness"
grep -q "Workflow Transitions" silver-bullet.md && echo "PASS: Workflow Transitions section" || echo "FAIL: Missing Workflow Transitions"
grep -q "Hand-Holding" silver-bullet.md && echo "PASS: Hand-Holding section" || echo "FAIL: Missing Hand-Holding"

# Template has placeholders
grep -q "{{PROJECT_NAME}}" templates/silver-bullet.md.base && echo "PASS: Template has PROJECT_NAME placeholder" || echo "FAIL: Template missing PROJECT_NAME"
grep -q "{{ACTIVE_WORKFLOW}}" templates/silver-bullet.md.base && echo "PASS: Template has ACTIVE_WORKFLOW placeholder" || echo "FAIL: Template missing ACTIVE_WORKFLOW"

# Template and silver-bullet.md differ only in placeholders
diff <(sed 's/silver-bullet/{{PROJECT_NAME}}/;s/full-dev-cycle/{{ACTIVE_WORKFLOW}}/' silver-bullet.md) templates/silver-bullet.md.base | head -20
echo "PASS if diff above is empty or minimal placeholder differences only"
    </automated>
  </verify>
  <done>
- silver-bullet.md contains GSD Process Knowledge section with all 20 guided commands (INST-01)
- silver-bullet.md contains hand-holding transition table telling Claude what to say at each step boundary (INST-02)
- silver-bullet.md contains utility command awareness table with context-based triggers (INST-03)
- All original sections S0-S9 are present and unchanged (INST-04)
- silver-bullet.md contains workflow transition subsection covering dev-to-DevOps and back
- S6 has minor update reflecting SB orchestration relationship
- templates/silver-bullet.md.base matches with only placeholder substitutions
- Total line count is under 500
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| silver-bullet.md -> Claude behavior | Instructions in this file direct Claude's actions -- accuracy is critical |
| template -> project instances | Template generates per-project files -- placeholder substitution must be exact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | silver-bullet.md enforcement sections | mitigate | All S0-S9 content copied byte-identical; verify with diff against git HEAD for unchanged sections |
| T-02-02 | Information Disclosure | GSD process knowledge | accept | Knowledge section describes command purposes (public info), not secrets or credentials |
| T-02-03 | Tampering | template placeholder substitution | mitigate | Verify diff between silver-bullet.md and .base shows only PROJECT_NAME and ACTIVE_WORKFLOW differences |
</threat_model>

<verification>
1. `wc -l silver-bullet.md` returns a number <= 500
2. `grep -c "^## "` shows same section count as original (10 original headings) plus new subsections
3. `diff` between unchanged sections and git HEAD shows zero differences
4. `diff` between silver-bullet.md (with placeholder substitution) and templates/silver-bullet.md.base shows zero differences
5. All 20 GSD commands from ORCH-03 appear in the GSD Process Knowledge section
6. All 7 utility commands from D-03 appear in the Utility Command Awareness section
</verification>

<success_criteria>
- INST-01: Claude can look up any GSD command in silver-bullet.md and explain it without reading GSD docs
- INST-02: Claude has explicit guidance on what to say at every workflow transition
- INST-03: Claude knows which utility command to suggest for 7 distinct context triggers
- INST-04: Every enforcement rule (S0-S9) is intact -- grep confirms all section headers and anti-skip blocks present
- D-18: Template parity verified by automated diff
</success_criteria>

<output>
After completion, create `.planning/phases/02-silver-bullet-md-overhaul/02-01-SUMMARY.md`
</output>
