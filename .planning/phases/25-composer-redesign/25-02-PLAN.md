---
phase: 25-composer-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - skills/silver-ui/SKILL.md
  - skills/silver-bugfix/SKILL.md
  - skills/silver-devops/SKILL.md
  - skills/silver-research/SKILL.md
  - skills/silver-release/SKILL.md
  - skills/silver/SKILL.md
autonomous: true
requirements:
  - COMP-02
  - COMP-01

must_haves:
  truths:
    - "All 5 remaining silver-* workflows have Composition Proposal sections"
    - "Each workflow proposes a path chain appropriate to its purpose"
    - "/silver router works with composer-enabled workflows"
    - "Composition templates exist for silver-feature, silver-ui, silver-bugfix, silver-devops, silver-research, silver-release"
  artifacts:
    - path: "skills/silver-ui/SKILL.md"
      provides: "UI composition proposal"
      contains: "## Composition Proposal"
    - path: "skills/silver-bugfix/SKILL.md"
      provides: "Bugfix composition proposal"
      contains: "## Composition Proposal"
    - path: "skills/silver-devops/SKILL.md"
      provides: "DevOps composition proposal"
      contains: "## Composition Proposal"
    - path: "skills/silver-research/SKILL.md"
      provides: "Research composition proposal"
      contains: "## Composition Proposal"
    - path: "skills/silver-release/SKILL.md"
      provides: "Release composition proposal"
      contains: "## Composition Proposal"
    - path: "skills/silver/SKILL.md"
      provides: "Router compatibility with composer"
      contains: "composer"
  key_links:
    - from: "skills/silver/SKILL.md"
      to: "skills/silver-feature/SKILL.md"
      via: "routing table"
      pattern: "silver:feature"
---

<objective>
Add Composition Proposal sections to all 5 remaining silver-* workflows (silver-ui, silver-bugfix, silver-devops, silver-research, silver-release) and verify /silver router compatibility.

Purpose: Every silver-* workflow becomes a composition template with context-aware path selection, completing the composer pattern across all workflow types.
Output: 5 updated workflow files + verified router.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-composer-redesign/25-CONTEXT.md
@.planning/phases/25-composer-redesign/25-01-SUMMARY.md
@docs/composable-paths-contracts.md
</context>

<interfaces>
<!-- From Plan 01: the Composition Proposal pattern established in silver-feature -->
<!-- Each workflow gets a similar section adapted to its path chain -->

Pattern from silver-feature/SKILL.md (after Plan 01):
- "## Composition Proposal" section after Pre-flight, before Step-Skip Protocol
- Reads existing artifacts to determine context
- Builds proposed path chain from relevant subset of 18 paths
- Displays proposal with approve prompt (auto-confirms in autonomous mode)
- Creates WORKFLOW.md if needed
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Composition Proposal to silver-ui, silver-bugfix, silver-devops, silver-research, silver-release</name>
  <files>skills/silver-ui/SKILL.md, skills/silver-bugfix/SKILL.md, skills/silver-devops/SKILL.md, skills/silver-research/SKILL.md, skills/silver-release/SKILL.md</files>
  <read_first>
    - skills/silver-feature/SKILL.md (reference implementation from Plan 01)
    - skills/silver-ui/SKILL.md
    - skills/silver-bugfix/SKILL.md
    - skills/silver-devops/SKILL.md
    - skills/silver-research/SKILL.md
    - skills/silver-release/SKILL.md
    - .planning/phases/25-composer-redesign/25-01-SUMMARY.md
    - docs/composable-paths-contracts.md
  </read_first>
  <action>
For each of the 5 workflow files, add a "## Composition Proposal" section following the same pattern established in silver-feature (Plan 01). Insert AFTER Pre-flight/preferences loading, BEFORE the first Step section in each file.

Each workflow's composition proposal is tailored to its purpose per D-04, D-05, D-06:

**silver-ui/SKILL.md** — UI-focused chain. Default paths: PATH 0 (BOOTSTRAP), PATH 1 (ORIENT), PATH 6 (DESIGN CONTRACT), PATH 5 (PLAN), PATH 7 (EXECUTE), PATH 8 (UI QUALITY), PATH 9 (REVIEW), PATH 12 (QUALITY GATE), PATH 13 (SHIP). Always includes PATH 6 and PATH 8. Skips PATH 4 if SPEC.md exists. Include per-phase loop logic for phases with UI work.

**silver-bugfix/SKILL.md** — Triage chain. Default paths: PATH 1 (ORIENT), PATH 14 (DEBUG), PATH 5 (PLAN), PATH 7 (EXECUTE), PATH 11 (VERIFY), PATH 13 (SHIP). Typically single-phase, no per-phase loop. Skips PATH 0 if .planning/ exists. Always includes PATH 14 (DEBUG) since this is a bugfix.

**silver-devops/SKILL.md** — Infra chain. Default paths: PATH 0 (BOOTSTRAP), PATH 1 (ORIENT), PATH 5 (PLAN), PATH 7 (EXECUTE), PATH 10 (SECURE), PATH 11 (VERIFY), PATH 13 (SHIP). Always includes PATH 10 (SECURE) for infra work. Skips PATH 6/8 (no UI).

**silver-research/SKILL.md** — Exploration chain. Default paths: PATH 2 (EXPLORE), PATH 3 (IDEATE), PATH 4 (SPECIFY). Short chain — research produces artifacts, not shipped code. No per-phase loop. No PATH 7/11/13.

**silver-release/SKILL.md** — Milestone chain. Default paths: PATH 12 (QUALITY GATE), PATH 15 (DESIGN HANDOFF — if UI milestone), PATH 16 (DOCUMENT), PATH 17 (RELEASE). Short chain focused on milestone completion. Includes PATH 15 only if UI milestone detected.

Each Composition Proposal section must:
1. Read existing artifacts to determine which paths to include/skip
2. Display the proposed chain in the same format as silver-feature:
   ```
   ┌─ COMPOSITION PROPOSAL ─────────────────────────
   │ Paths: PATH 1 (ORIENT) → PATH 14 (DEBUG) → ...
   │ Skipped: PATH 0 (BOOTSTRAP) — .planning/ exists
   └────────────────────────────────────────────────
   Approve composition? [Y/n]
   ```
3. Auto-confirm in autonomous mode
4. Create/update WORKFLOW.md if it doesn't exist

Note: These 5 workflows do NOT get the full Supervision Loop or Anti-Stall sections — those are only in silver-feature (the full workflow). These workflows are shortcut compositions (per D-04) that propose and execute their focused path chains. They DO update WORKFLOW.md path log after each path completes (simple inline: write status to Path Log table).
  </action>
  <verify>
    <automated>for f in skills/silver-ui/SKILL.md skills/silver-bugfix/SKILL.md skills/silver-devops/SKILL.md skills/silver-research/SKILL.md skills/silver-release/SKILL.md; do grep -q "## Composition Proposal" "$f" || echo "MISSING in $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - grep "## Composition Proposal" skills/silver-ui/SKILL.md returns a match
    - grep "## Composition Proposal" skills/silver-bugfix/SKILL.md returns a match
    - grep "## Composition Proposal" skills/silver-devops/SKILL.md returns a match
    - grep "## Composition Proposal" skills/silver-research/SKILL.md returns a match
    - grep "## Composition Proposal" skills/silver-release/SKILL.md returns a match
    - grep "COMPOSITION PROPOSAL" skills/silver-ui/SKILL.md returns a match (display banner)
    - grep "PATH 6.*DESIGN CONTRACT\|PATH 8.*UI QUALITY" skills/silver-ui/SKILL.md returns a match
    - grep "PATH 14.*DEBUG" skills/silver-bugfix/SKILL.md returns a match
    - grep "PATH 10.*SECURE" skills/silver-devops/SKILL.md returns a match
    - grep "PATH 2.*EXPLORE\|PATH 3.*IDEATE" skills/silver-research/SKILL.md returns a match
    - grep "PATH 17.*RELEASE\|PATH 15.*DESIGN HANDOFF" skills/silver-release/SKILL.md returns a match
    - grep "WORKFLOW.md" skills/silver-ui/SKILL.md returns a match
    - grep "Approve composition\|auto-confirm" skills/silver-bugfix/SKILL.md returns a match
  </acceptance_criteria>
  <done>All 5 silver-* workflows have Composition Proposal sections with workflow-appropriate path chains, context-aware skipping, user approval, and WORKFLOW.md integration</done>
</task>

<task type="auto">
  <name>Task 2: Verify and update /silver router for composer compatibility</name>
  <files>skills/silver/SKILL.md</files>
  <read_first>
    - skills/silver/SKILL.md
    - .planning/phases/25-composer-redesign/25-CONTEXT.md
  </read_first>
  <action>
Per D-03 and D-22, the /silver router stays as the entry point. Verify it works with the composer-enabled workflows and make minimal updates:

1. In Step 2's routing table description, add a note after the table explaining that each silver-* workflow now acts as a composition template:
   ```
   **Composer note:** Each silver-* workflow is a composition template. After routing,
   the workflow proposes a path composition to the user before executing. The router
   does not need to know about paths — it routes to the right workflow, which handles
   composition internally.
   ```

2. No changes to the routing table itself — routing logic is unchanged per D-03.

3. Verify the "Ship disambiguation" and other existing sections still make sense with composer. No changes expected — the router's job is classification and routing, not composition.
  </action>
  <verify>
    <automated>grep -q "Composer note\|composition template" skills/silver/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Composer note\|composition template" skills/silver/SKILL.md returns a match
    - grep "silver:feature\|silver:ui\|silver:bugfix" skills/silver/SKILL.md returns matches (routing table intact)
    - The routing table in Step 2 is unchanged (same routes as before)
  </acceptance_criteria>
  <done>/silver router has composer compatibility note and routing table is verified intact</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router → Workflow | /silver routes to silver-* which now composes paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | S (Spoofing) | Composition proposal | accept | Proposals shown to user for approval — no blind execution |
| T-25-05 | T (Tampering) | Workflow path skipping | accept | User explicitly approves which paths to skip in proposal |
</threat_model>

<verification>
- All 6 silver-* workflows have Composition Proposal sections
- Each workflow proposes paths appropriate to its purpose
- /silver router unchanged except for composer compatibility note
- WORKFLOW.md integration present in all workflows
</verification>

<success_criteria>
All 6 silver-* workflow files are composition templates (per COMP-02). /silver router is verified compatible (per COMP-01). Each template proposes a context-aware path chain appropriate to its workflow type.
</success_criteria>

<output>
After completion, create `.planning/phases/25-composer-redesign/25-02-SUMMARY.md`
</output>
