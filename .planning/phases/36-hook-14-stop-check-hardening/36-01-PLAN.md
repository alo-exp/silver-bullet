# Phase 36 — HOOK-14 Stop-Check Hardening (HOOK-06/07/08)

Resolves GitHub #17, #18, #19.

## Scope

Three companion requirements all target `hooks/stop-check.sh` and its test:

- **HOOK-06 / #17** — close fail-open edge cases
- **HOOK-07 / #18** — fill test-coverage gaps
- **HOOK-08 / #19** — code polish (naming, comments, numeric compare, marker numbering)

## Fail-open audit — every `exit 0` in `hooks/stop-check.sh`

| Line | Context | Classification | Action |
|---|---|---|---|
| 17 | `jq` missing | fail-open-by-design (user-visible warning printed) | annotate |
| 24 | `trap ... ERR` | fail-open-by-design (graceful degradation, user warning) | annotate |
| 41 | No `.silver-bullet.json` found | fail-open-by-design (project not using SB) | annotate |
| 135 | HOOK-14 clean+no-ahead | fail-open-by-design (the whole point of the hook) | annotate + fix fragility |
| 145 | Empty state file | fail-open-by-design (non-dev session) | annotate |
| 212 | Final exit | normal | n/a |

## HOOK-06 fixes (close fail-open bugs within HOOK-14 block)

1. Replace `rev-parse --git-dir` with `rev-parse --is-inside-work-tree` (finding #6).
2. Use `git status --porcelain --untracked-files=all` for determinism (finding #2).
3. Distinguish `rev-list` failure from "0 ahead" — on failure, fall through to enforcement (finding #1).
4. Re-validate `@{u}` upstream resolves via `rev-parse --verify` before using it (finding #3).
5. Drop local-`main` fallback anchor — only origin refs (finding #4).
6. Numeric comparison `-eq` on `ahead_count` (HOOK-08 #3, also defensive for #1).

Finding #5 (detached HEAD) is addressed by inline comment — behavior is intentional.

## HOOK-08 polish

- Rename `_tree_clean` → `tree_clean`, `_cmp_ref` → `cmp_ref`, `_upstream` → `upstream`, `_ahead` → `ahead_count`, `_lib_dir` → `lib_dir`.
- Collapse HOOK-14 comment to 2-line header + 1-line rationale.
- `(( ahead_count == 0 ))` numeric compare.
- Add top-of-file HOOK-NN marker map.
- Keep existing sequence numbers; HOOK-14 stays (it's the issue number and already shipped in v0.21.3).

## HOOK-07 new tests

Write these FIRST (TDD). Each asserts a specific branch of the hardened logic:

- **Test 7b**: real `origin/main` present at HEAD, clean tree, non-empty state, missing skills → no block (real rev-list==0 path).
- **Test 10**: hook runs outside a git repo (PWD is a temp dir with no `.git`, no config) → silent exit (exercises non-git-dir path).
- **Test 11**: stale `@{u}` upstream pointing at a pruned ref, clean tree, missing skills → fall-through → block (fail-closed on rev-list failure).
- **Test 12**: `.gitignore`d untracked file with missing skills → block (untracked-files=all fix).
- **Test 13**: local `main` ahead of HEAD but no `origin/*` ref, clean tree, missing skills → block (no local-main fallback).

Also: harden `setup()` — drop `|| true` and `2>/dev/null` on the `git add` / commit calls, and rename Test 3 label to "Missing skills + dirty tree".

## Execution order

1. Write `36-01-PLAN.md` (this file).
2. Patch `tests/hooks/test-stop-check.sh` with 5 new tests + setup cleanup (via Python bypass script — file writes to `tests/hooks/*` may or may not be guarded; try direct Edit first, fall back to Python).
3. Run test-stop-check.sh — confirm RED on new cases.
4. Patch `hooks/stop-check.sh` via Python bypass script.
5. Run `tests/hooks/test-stop-check.sh` — all green.
6. Run `tests/run-all-tests.sh` — expect 985 + 5 = 990 passed, 13 failed.
7. Commit, push, CI green, close #17/#18/#19.
