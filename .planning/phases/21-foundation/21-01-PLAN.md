---
phase: 21-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/composable-paths-contracts.md
  - templates/workflow.md.base
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
must_haves:
  truths:
    - "A summary contracts document exists listing all 17 paths with their 7 contract fields"
    - "A WORKFLOW.md template exists with all 6 sections from the design spec"
    - "The template documents the 100-line cap and FIFO truncation strategy"
    - "GSD isolation rule is documented in the template"
  artifacts:
    - path: "docs/composable-paths-contracts.md"
      provides: "Quick-lookup reference for all 17 path contracts"
      contains: "PATH 0: BOOTSTRAP"
    - path: "templates/workflow.md.base"
      provides: "WORKFLOW.md template for /silver composer"
      contains: "## Path Log"
  key_links:
    - from: "docs/composable-paths-contracts.md"
      to: "docs/superpowers/specs/2026-04-14-composable-paths-design.md"
      via: "derived from design spec section 5"
      pattern: "Derived from.*composable-paths-design"
    - from: "templates/workflow.md.base"
      to: "docs/superpowers/specs/2026-04-14-composable-paths-design.md"
      via: "implements spec section 4"
      pattern: "## Path Log"
---

<objective>
Create the path contracts summary document and WORKFLOW.md template -- the two specification artifacts that all subsequent phases reference.

Purpose: Phases 22-24 need a quick-lookup contracts reference, and Phase 25 needs the WORKFLOW.md template to implement the composer.
Output: docs/composable-paths-contracts.md, templates/workflow.md.base
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-foundation/21-CONTEXT.md
@docs/superpowers/specs/2026-04-14-composable-paths-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create composable-paths-contracts.md summary reference</name>
  <files>docs/composable-paths-contracts.md</files>
  <read_first>
    docs/superpowers/specs/2026-04-14-composable-paths-design.md
    .planning/phases/21-foundation/21-CONTEXT.md
  </read_first>
  <action>
Create docs/composable-paths-contracts.md as a derived quick-lookup reference document (per D-03). This is NOT the source of truth -- the design spec is.

Structure:
1. Title: "# Composable Paths — Contract Reference"
2. Note: "Derived from [design spec](superpowers/specs/2026-04-14-composable-paths-design.md). The spec is the source of truth."
3. Section "## Contract Schema" showing the 7 required fields (per D-02): Prerequisites, Trigger, Steps, Produces, Review Cycle, GSD Impact, Exit Condition
4. For each of the 17 paths (PATH 0 through PATH 17, skipping no numbers), create a section "### PATH {N}: {NAME}" with a table containing all 7 contract fields extracted from the design spec section 5.

Key details to extract per path:
- PATH 0 BOOTSTRAP: prereq=None, trigger=no .planning/ or milestone complete, produces=PROJECT.md+ROADMAP.md+REQUIREMENTS.md+STATE.md, exit=STATE.md exists
- PATH 1 ORIENT: prereq=PATH 0, trigger=always non-trivial, produces=intel files, exit=intel files exist
- PATH 2 EXPLORE: prereq=PATH 1, trigger=fuzzy intent, produces=scope summary, exit=problem space clarified
- PATH 3 IDEATE: prereq=PATH 2, trigger=complex work, produces=ADR/design/brainstorm, exit=direction chosen
- PATH 4 SPECIFY: prereq=PATH 3 or external spec, trigger=no SPEC.md, produces=SPEC.md+REQUIREMENTS.md, exit=zero BLOCK findings
- PATH 5 PLAN: prereq=ROADMAP+REQUIREMENTS+phase dir, trigger=always, produces=CONTEXT.md+RESEARCH.md+PLAN.md, exit=PLAN.md with 2 clean passes
- PATH 6 DESIGN CONTRACT: prereq=PATH 5, trigger=UI phase, produces=UI-SPEC.md, exit=user accepts
- PATH 7 EXECUTE: prereq=PLAN.md+STATE.md, trigger=always, produces=SUMMARY.md, exit=all plans have SUMMARY.md
- PATH 8 UI QUALITY: prereq=PATH 7 with UI, trigger=PATH 6 in composition, produces=UI-REVIEW.md, exit=no critical findings
- PATH 9 REVIEW: prereq=PATH 7, trigger=always with PATH 7, produces=REVIEW.md, exit=2 consecutive clean passes
- PATH 10 SECURE: prereq=PATH 9, trigger=always, produces=SECURITY.md, exit=2 clean passes
- PATH 11 VERIFY: prereq=PATH 7, trigger=always NON-SKIPPABLE, produces=UAT.md+VERIFICATION.md, exit=VERIFICATION.md passed
- PATH 12 QUALITY GATE: prereq=varies (pre-plan vs pre-ship), trigger=always twice, produces=quality assessment, exit=all dimensions pass
- PATH 13 SHIP: prereq=PATH 12+PATH 11, trigger=always, produces=PR, exit=PR created CI green
- PATH 14 DEBUG: prereq=none (dynamic), trigger=failure, produces=fix plan, exit=root cause identified
- PATH 15 DESIGN HANDOFF: prereq=all UI phases verified, trigger=UI milestone in release, produces=handoff package, exit=package produced. Note: runs inside PATH 17 only
- PATH 16 DOCUMENT: prereq=PATH 13, trigger=always post-ship, produces=docs updates, exit=docs updated
- PATH 17 RELEASE: prereq=all phases shipped, trigger=milestone complete, produces=GitHub Release, exit=release created

Include the Steps column listing skill invocations (mandatory vs as-needed) for each path.
Include the Review Cycle for paths that have one (PATH 0, 4, 5, 8, 11 have explicit review cycles; others state "None" or describe their pattern).

End with a note: "This document is generated for quick reference. For full details including GSD compatibility guarantees, anti-stall tiers, and composition templates, see the design spec."
  </action>
  <verify>
    <automated>grep -c "### PATH" docs/composable-paths-contracts.md | grep -q "17" && grep -q "Prerequisites" docs/composable-paths-contracts.md && grep -q "Exit Condition" docs/composable-paths-contracts.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "### PATH" docs/composable-paths-contracts.md returns exactly 17 lines (PATH 0 through PATH 17, no PATH 18)
    - grep "Prerequisites" docs/composable-paths-contracts.md returns at least 17 matches (one per path table)
    - grep "Trigger" docs/composable-paths-contracts.md returns at least 17 matches
    - grep "Exit Condition" docs/composable-paths-contracts.md returns at least 17 matches
    - grep "Derived from" docs/composable-paths-contracts.md returns 1 match (source-of-truth note)
    - grep "## Contract Schema" docs/composable-paths-contracts.md returns 1 match
  </acceptance_criteria>
  <done>All 17 path contracts documented with 7 fields each; file references design spec as source of truth</done>
</task>

<task type="auto">
  <name>Task 2: Create WORKFLOW.md template</name>
  <files>templates/workflow.md.base</files>
  <read_first>
    docs/superpowers/specs/2026-04-14-composable-paths-design.md
    .planning/phases/21-foundation/21-CONTEXT.md
    templates/doc-scheme.md.base
  </read_first>
  <action>
Create templates/workflow.md.base following the existing .base template pattern (per D-04).

The template implements the WORKFLOW.md specification from design spec section 4. It must contain these exact sections with placeholder content:

```markdown
# Workflow Manifest

> Composition state for the active milestone. Created by /silver composer, updated by supervision loop.
> **Size cap:** 100 lines. Truncation: FIFO on completed paths — oldest completed entries collapse to summary line.
> **GSD isolation:** GSD workflows never read this file. SB orchestration never writes STATE.md directly.

## Composition
Intent: "{user's original request}"
Composed: {ISO timestamp}
Composer: /silver

## Path Log
| # | Path | Status | Artifacts Produced | Exit Condition Met |
|---|------|--------|-------------------|--------------------|

## Phase Iterations
| Phase | Paths 5-13 Status |
|-------|-------------------|

## Dynamic Insertions
| After | Inserted | Reason |
|-------|----------|--------|

## Autonomous Decisions
| Timestamp | Decision | Rationale |
|-----------|----------|-----------|

## Deferred Improvements
| Source Path | Finding | Classification |
|-------------|---------|----------------|

## Next Path
{next path name and step}
```

Add a comment block at the top (before the title) explaining template usage:
```
<!-- Template: workflow.md.base
     Scaffolded to: .planning/WORKFLOW.md
     Created by: /silver composer (Phase 25)
     Phase 21 defines the spec only — no runtime creation logic (per D-06)
     
     Truncation strategy (per D-05):
     When approaching 100 lines, oldest completed Path Log entries collapse:
     | 1-3 | BOOTSTRAP→ORIENT→PLAN | complete | (see phase dirs) | — |
     
     GSD isolation (per D-07):
     - GSD never reads WORKFLOW.md
     - SB never writes STATE.md directly
     - Bridge: /silver:migrate reads STATE.md to generate WORKFLOW.md
-->
```
  </action>
  <verify>
    <automated>grep -c "^## " templates/workflow.md.base | grep -q "7" && grep -q "100 lines" templates/workflow.md.base && grep -q "GSD isolation" templates/workflow.md.base && grep -q "FIFO" templates/workflow.md.base && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "^## " templates/workflow.md.base returns exactly 7 lines (Composition, Path Log, Phase Iterations, Dynamic Insertions, Autonomous Decisions, Deferred Improvements, Next Path)
    - grep "100 lines" templates/workflow.md.base returns at least 1 match
    - grep "FIFO" templates/workflow.md.base returns at least 1 match
    - grep "GSD isolation" templates/workflow.md.base returns at least 1 match
    - grep "GSD workflows never read this file" templates/workflow.md.base returns 1 match
    - grep "SB orchestration never writes STATE.md" templates/workflow.md.base returns 1 match
    - grep "workflow.md.base" templates/workflow.md.base returns at least 1 match (template self-reference)
    - File follows .base template pattern consistent with templates/doc-scheme.md.base
  </acceptance_criteria>
  <done>WORKFLOW.md template exists with all 7 sections, 100-line cap documented, FIFO truncation strategy specified, GSD isolation rule enforced</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates documentation/specification files only — no executable code, no user input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | WORKFLOW.md template | accept | Template contains placeholder tokens only, no secrets. Runtime instances in .planning/ are gitignored per project convention. |
| T-21-02 | Tampering | composable-paths-contracts.md | accept | Derived reference doc — design spec is source of truth. Tampering has no runtime effect. |
</threat_model>

<verification>
1. docs/composable-paths-contracts.md exists with 17 path contract sections
2. templates/workflow.md.base exists with 7 sections matching design spec section 4
3. Both files reference the design spec as authoritative source
4. GSD isolation rule documented in WORKFLOW.md template
5. 100-line cap and FIFO truncation strategy documented
</verification>

<success_criteria>
- All 17 path contracts extractable from summary doc for quick lookup
- WORKFLOW.md template ready for Phase 25 composer implementation
- No runtime logic created (per D-06 — Phase 21 is spec/template only)
</success_criteria>

<output>
After completion, create `.planning/phases/21-foundation/21-01-SUMMARY.md`
</output>
