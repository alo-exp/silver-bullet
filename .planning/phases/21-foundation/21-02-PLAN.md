---
phase: 21-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/artifact-review-assessor/SKILL.md
  - templates/doc-scheme.md.base
autonomous: true
requirements:
  - FOUND-03
  - FOUND-04
must_haves:
  truths:
    - "artifact-review-assessor skill exists as a standalone skill separate from artifact-reviewer"
    - "Assessor classifies findings into exactly 3 categories: MUST-FIX, NICE-TO-HAVE, DISMISS"
    - "Assessor judges against artifact CONTRACT, not subjective quality"
    - "No review loop on the assessor itself"
    - "doc-scheme.md.base includes WORKFLOW.md, VALIDATION.md, UI-SPEC.md, UI-REVIEW.md, SECURITY.md"
    - "Non-redundancy rule 6 exists for WORKFLOW.md vs STATE.md separation"
  artifacts:
    - path: "skills/artifact-review-assessor/SKILL.md"
      provides: "Artifact review assessor skill definition"
      contains: "MUST-FIX"
    - path: "templates/doc-scheme.md.base"
      provides: "Updated artifact documentation scheme"
      contains: "WORKFLOW.md"
  key_links:
    - from: "skills/artifact-review-assessor/SKILL.md"
      to: "skills/artifact-reviewer/SKILL.md"
      via: "assessor receives reviewer output as input"
      pattern: "artifact-reviewer.*artifact-review-assessor"
    - from: "templates/doc-scheme.md.base"
      to: "templates/workflow.md.base"
      via: "doc-scheme documents WORKFLOW.md artifact"
      pattern: "WORKFLOW.md"
---

<objective>
Create the artifact-review-assessor skill and update doc-scheme.md.base with new artifacts and non-redundancy rule 6.

Purpose: The assessor prevents over-zealous reviews by triaging findings against contracts. The doc-scheme update ensures all new artifacts are documented.
Output: skills/artifact-review-assessor/SKILL.md, updated templates/doc-scheme.md.base
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-foundation/21-CONTEXT.md
@docs/superpowers/specs/2026-04-14-composable-paths-design.md
@skills/artifact-reviewer/SKILL.md
@skills/artifact-reviewer/rules/reviewer-interface.md
@templates/doc-scheme.md.base
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create artifact-review-assessor skill</name>
  <files>skills/artifact-review-assessor/SKILL.md</files>
  <read_first>
    skills/artifact-reviewer/SKILL.md
    skills/artifact-reviewer/rules/reviewer-interface.md
    docs/superpowers/specs/2026-04-14-composable-paths-design.md
    .planning/phases/21-foundation/21-CONTEXT.md
  </read_first>
  <action>
Create skills/artifact-review-assessor/ directory and SKILL.md (per D-08 -- standalone, NOT inside artifact-reviewer/).

The SKILL.md must include YAML frontmatter with: name: artifact-review-assessor, description: "Triages artifact reviewer findings into MUST-FIX / NICE-TO-HAVE / DISMISS based on artifact contract -- prevents over-zealous reviews", argument-hint: "&lt;review-findings&gt; &lt;artifact-path&gt; [--contract-source &lt;path&gt;]"

Content sections:

1. Title and purpose: "# artifact-review-assessor" -- Reality-check artifact reviewer findings before fixing. Prevents over-zealous reviews from causing unnecessary work. (per D-10)

2. Usage: Show invocation pattern: artifact-reviewer -> artifact-review-assessor -> fix MUST-FIX only -> re-review -> repeat until 2 consecutive clean passes (per D-09)

3. No self-review rule: Explicit statement: "No review loop on the assessor itself. Assessor triages ONCE per reviewer invocation. The cycle is: Reviewer -> Assessor -> fix MUST-FIX -> Reviewer again (NOT Assessor again)." (per D-11)

4. Input/Output (per D-12):
   - Input: reviewer findings (markdown), artifact being reviewed (path), artifact contract source (path or inline)
   - Output: classified findings list with 3 categories

5. Classification criteria (per D-10) -- exact table from design spec section 6:

| Classification | Criterion |
|---|---|
| MUST-FIX | Contract violation: required section missing, factual inconsistency, untraceable criterion, security/correctness issue |
| NICE-TO-HAVE | Genuine improvement: clarity, detail, structure -- logged in WORKFLOW.md "Deferred Improvements" but does not block 2-pass gate |
| DISMISS | Extraneous: stylistic preference, "could be more detailed" without specific gap, contradicts locked CONTEXT.md decision, duplicate |

6. Contract sources table -- the 11-artifact mapping from design spec section 6:

| Artifact | Contract defined by |
|---|---|
| SPEC.md | silver-spec SKILL.md step 7 template |
| REQUIREMENTS.md | REQ-XX format rules in silver-spec SKILL.md step 8 |
| CONTEXT.md | Locked decisions format in gsd-discuss-phase workflow |
| PLAN.md | Wave structure + task format in gsd-plan-phase workflow |
| RESEARCH.md | Evidence + confidence format in gsd-phase-researcher agent |
| DESIGN.md | SB design template in silver-spec SKILL.md step 9 |
| UI-SPEC.md | Design contract format in gsd-ui-phase workflow |
| REVIEW.md | Code quality finding format in gsd-code-reviewer agent |
| UAT.md | Criterion + Result + Evidence format in gsd-verify-work workflow |
| INGESTION_MANIFEST.md | Source artifact listing in silver-ingest SKILL.md step 7 |
| SECURITY.md | Threat model format in gsd-secure-phase workflow |

7. Triage steps:
   1. Load the artifact contract from the contract sources table
   2. For each reviewer finding, compare against the contract
   3. Classify: Does it violate a contract requirement? -> MUST-FIX. Is it a genuine improvement not in contract? -> NICE-TO-HAVE. Is it stylistic/subjective/duplicate? -> DISMISS
   4. Output classified list
   5. NICE-TO-HAVE items get logged to WORKFLOW.md "Deferred Improvements" section (if WORKFLOW.md exists)

8. Output format section showing markdown template with three subsections: "### MUST-FIX (N)" with checkbox items showing Finding/Contract/Source, "### NICE-TO-HAVE (N)" with items showing Finding/Improvement/Deferred-to, "### DISMISS (N)" with items showing Finding/Reason.
  </action>
  <verify>
    <automated>test -f skills/artifact-review-assessor/SKILL.md && grep -q "MUST-FIX" skills/artifact-review-assessor/SKILL.md && grep -q "NICE-TO-HAVE" skills/artifact-review-assessor/SKILL.md && grep -q "DISMISS" skills/artifact-review-assessor/SKILL.md && grep -q "No review loop" skills/artifact-review-assessor/SKILL.md && grep -q "artifact-review-assessor" skills/artifact-review-assessor/SKILL.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at skills/artifact-review-assessor/SKILL.md
    - grep "name: artifact-review-assessor" skills/artifact-review-assessor/SKILL.md returns 1 match
    - grep "MUST-FIX" skills/artifact-review-assessor/SKILL.md returns at least 3 matches
    - grep "NICE-TO-HAVE" skills/artifact-review-assessor/SKILL.md returns at least 3 matches
    - grep "DISMISS" skills/artifact-review-assessor/SKILL.md returns at least 3 matches
    - grep "No review loop" skills/artifact-review-assessor/SKILL.md returns at least 1 match
    - grep "Contract defined by" skills/artifact-review-assessor/SKILL.md returns 1 match
    - All 11 artifacts present in contract sources table (SPEC.md, REQUIREMENTS.md, CONTEXT.md, PLAN.md, RESEARCH.md, DESIGN.md, UI-SPEC.md, REVIEW.md, UAT.md, INGESTION_MANIFEST.md, SECURITY.md)
    - Directory is skills/artifact-review-assessor/ (NOT inside skills/artifact-reviewer/)
  </acceptance_criteria>
  <done>Standalone assessor skill exists with 3 classifications, 11 contract sources, no-self-review rule, and triage steps</done>
</task>

<task type="auto">
  <name>Task 2: Update doc-scheme.md.base with new artifacts and rule 6</name>
  <files>templates/doc-scheme.md.base</files>
  <read_first>
    templates/doc-scheme.md.base
    .planning/phases/21-foundation/21-CONTEXT.md
    docs/superpowers/specs/2026-04-14-composable-paths-design.md
  </read_first>
  <action>
Update templates/doc-scheme.md.base with additive changes only (per D-15). Three modifications:

Modification 1 (per D-13): Add 5 new artifacts to the .planning/ ephemeral artifacts table (the table under "## .planning/ -- Ephemeral Planning Artifacts"). Add these rows after the existing "Phase dirs" row:

| WORKFLOW.md | /silver composer | Composition state -- path log, iterations, dynamic insertions, decisions |
| VALIDATION.md | silver-validate | Gap analysis findings from spec validation |
| UI-SPEC.md | gsd-ui-phase | Design contract -- component specs, tokens, responsive rules |
| UI-REVIEW.md | gsd-ui-review | 6-pillar UI audit findings |
| SECURITY.md | gsd-secure-phase | Threat model, STRIDE register, security findings |

Modification 2 (per D-14): Add non-redundancy rule 6 to the "## Non-redundancy rules" section. After existing rule 5, add:

6. WORKFLOW.md tracks composition state (path ordering, dynamic insertions, deferred improvements). STATE.md tracks GSD execution state (current phase, plan position, velocity). Neither file writes to the other directly -- /silver:migrate is the only bridge.

Modification 3: Add WORKFLOW.md to the Size Caps table:

| WORKFLOW.md | 100 lines | FIFO truncation of completed path entries |

Do NOT restructure existing tables or modify existing rows (per D-15). Only ADD new rows and the new rule.
  </action>
  <verify>
    <automated>grep -q "WORKFLOW.md" templates/doc-scheme.md.base && grep -q "VALIDATION.md" templates/doc-scheme.md.base && grep -q "UI-SPEC.md" templates/doc-scheme.md.base && grep -q "UI-REVIEW.md" templates/doc-scheme.md.base && grep -q "SECURITY.md" templates/doc-scheme.md.base && grep -q "writes to the other directly" templates/doc-scheme.md.base && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "WORKFLOW.md" templates/doc-scheme.md.base returns at least 2 matches (artifact table + size cap + rule 6)
    - grep "VALIDATION.md" templates/doc-scheme.md.base returns at least 1 match
    - grep "UI-SPEC.md" templates/doc-scheme.md.base returns at least 1 match
    - grep "UI-REVIEW.md" templates/doc-scheme.md.base returns at least 1 match
    - grep "SECURITY.md" templates/doc-scheme.md.base returns at least 1 match in planning artifacts table
    - grep "100 lines" templates/doc-scheme.md.base returns at least 1 match
    - grep -c "^[0-9]\." templates/doc-scheme.md.base returns 6 (rules 1-6)
    - Existing rows unchanged (diff shows only additions)
  </acceptance_criteria>
  <done>doc-scheme.md.base has 5 new artifacts in planning table, WORKFLOW.md in size caps, and non-redundancy rule 6</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewer to Assessor | Assessor receives reviewer findings as untrusted input -- must validate against contract, not trust reviewer classifications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | Assessor classification | accept | Assessor is a skill definition (prompt), not executable code. Classification logic runs in Claude's context. No external input injection path. |
| T-21-04 | Elevation of Privilege | Assessor bypassing review | mitigate | Explicit no-self-review rule prevents assessor from dismissing its own findings. Invocation chain enforced: reviewer -> assessor -> fix -> reviewer (not assessor -> assessor). |
| T-21-05 | Information Disclosure | doc-scheme.md.base | accept | Template file, no secrets. Documents artifact structure only. |
</threat_model>

<verification>
1. skills/artifact-review-assessor/SKILL.md exists as standalone skill
2. Assessor has 3 classifications: MUST-FIX, NICE-TO-HAVE, DISMISS
3. Contract sources table lists all 11 artifacts
4. No-self-review rule explicitly stated
5. doc-scheme.md.base has 5 new artifacts added
6. Non-redundancy rule 6 exists
7. WORKFLOW.md appears in size caps table with 100-line limit
</verification>

<success_criteria>
- Assessor skill ready for integration into review loops in Phase 22+
- doc-scheme.md.base reflects all new artifacts introduced by composable paths architecture
- No existing content modified in doc-scheme (additive only per D-15)
</success_criteria>

<output>
After completion, create `.planning/phases/21-foundation/21-02-SUMMARY.md`
</output>
