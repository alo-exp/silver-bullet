---
phase: 052-silver-forensics-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 052-01
files_modified:
  - skills/silver-forensics/SKILL.md
  - .planning/052-FORENSICS-AUDIT.md
autonomous: true
requirements:
  - FORN-02

must_haves:
  truths:
    - "Every gap listed in the 052-FORENSICS-AUDIT.md ## Gaps Found table is fixed in skills/silver-forensics/SKILL.md"
    - "The audit report is updated to record which gaps were found and what was changed"
    - "silver-forensics covers the scope-drift and test-regression detection signals from gsd-forensics Step 3"
    - "silver-forensics Step 2b issues all evidence-gathering commands present in gsd-forensics Step 2 (git diff --stat, git worktree list, git log with timestamps, phase artifact completeness)"
    - "silver-forensics post-mortem report schema includes the artifact completeness matrix and anomaly confidence levels"
    - "silver-forensics security boundary section covers path and credential redaction from report output"
  artifacts:
    - path: "skills/silver-forensics/SKILL.md"
      provides: "Gap-fixed forensics skill with 100% functional equivalence to gsd-forensics"
      min_lines: 260
    - path: ".planning/052-FORENSICS-AUDIT.md"
      provides: "Updated audit report with fix log"
      contains: "## Fix Log"
  key_links:
    - from: ".planning/052-FORENSICS-AUDIT.md"
      to: "skills/silver-forensics/SKILL.md"
      via: "gap list drives each edit"
      pattern: "G-0[0-9]"
---

<objective>
Apply every fix identified in the 052-FORENSICS-AUDIT.md gap list to `skills/silver-forensics/SKILL.md`, then append a `## Fix Log` section to the audit report documenting what was changed for each gap.

Purpose: Satisfy FORN-02 — all gaps are fixed before Phase 54 (silver-scan) begins. The audit report with the fix log is the evidence artifact.
Output: Updated `skills/silver-forensics/SKILL.md` (gap-free) + `## Fix Log` appended to `.planning/052-FORENSICS-AUDIT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/shafqat/Documents/Projects/silver-bullet/.planning/PROJECT.md
@/Users/shafqat/Documents/Projects/silver-bullet/.planning/ROADMAP.md
@/Users/shafqat/Documents/Projects/silver-bullet/.planning/STATE.md
@/Users/shafqat/Documents/Projects/silver-bullet/.planning/phases/052-silver-forensics-audit/052-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read audit report gap list and current SKILL.md</name>
  <read_first>
    - /Users/shafqat/Documents/Projects/silver-bullet/.planning/052-FORENSICS-AUDIT.md (the gap list from Plan 052-01)
    - /Users/shafqat/Documents/Projects/silver-bullet/skills/silver-forensics/SKILL.md (current state to be edited)
    - /Users/shafqat/.claude/get-shit-done/workflows/forensics.md (reference for exact wording/structure to copy from)
  </read_first>
  <files>skills/silver-forensics/SKILL.md</files>
  <action>
Read the audit report at `.planning/052-FORENSICS-AUDIT.md` and identify all items in the `## Gaps Found` table. Then read `skills/silver-forensics/SKILL.md` fully. Apply every gap fix to SKILL.md using the Edit tool.

The fixes below reflect the gaps identified during planning. The audit report (Plan 052-01 output) is authoritative — if the audit found additional gaps or no gap for an item below, follow the audit report.

**Expected gap fixes (apply each that the audit report confirms):**

**G-01 — Evidence-gathering: git log depth and timestamps (Dimension 2)**
In Step 2b, the current `git log --oneline -10` only fetches 10 commits. Expand to `git log --oneline -30` (matching gsd-forensics Step 2a). Additionally add `git log --format="%H %ai %s" -30` as a second parallel call so the executor has timestamps for gap analysis. These should still be listed as simultaneous tool calls.

**G-02 — Evidence-gathering: file-frequency analysis (Dimension 2)**
In Step 2b, add a third parallel call: `git log --name-only --format="" -20 | sort | uniq -c | sort -rn | head -20` to surface files committed repeatedly (stuck-loop signal). Reference this as "most-edited files" in the evidence notes.

**G-03 — Evidence-gathering: git status and diff (Dimension 2)**
In Step 2b, add `git status --short` and `git diff --stat` as parallel calls. These detect uncommitted work (crash/interruption indicator). silver-forensics currently checks only the sentinel file for stall detection; these commands are the direct equivalent of gsd-forensics Step 2a uncommitted-work detection.

**G-04 — Evidence-gathering: phase artifact completeness (Dimension 2)**
In Step 2b, add a parallel check: `ls .planning/phases/*/` — enumerate phase directories and note which phases lack a SUMMARY.md (completion evidence). This maps to gsd-forensics Step 2c. Add a brief note in the step: "For each phase that should be complete, note if PLAN.md, SUMMARY.md, or VERIFICATION.md is missing."

**G-05 — Evidence-gathering: git worktree list (Dimension 2)**
In Step 2b, add `git worktree list` as a parallel call. Orphaned worktrees (more than one listed) are a crash/interruption signal. Note: "More than one worktree indicates a crashed agent or interrupted session."

**G-06 — Anomaly detection: scope drift and test regression (Dimension 1)**
After Step 2c (classification), or within Path 2 (task-level), add explicit anomaly-detection guidance for:
- Scope drift: compare files in recent commits against the current phase PLAN.md `files_modified` list — flag files outside expected scope
- Test regression: `git log --oneline -20 | grep -iE "fix test|revert|broken|regression|fail"` — commit message signal for repeated test failures
These patterns exist in gsd-forensics Step 3 and are absent from silver-forensics.

**G-07 — Post-mortem report: artifact completeness matrix (Dimension 5)**
In the Post-mortem Report template (the markdown block), add an "Artifact Completeness" subsection under "Evidence Gathered" with this table format (per gsd-forensics Step 4 report schema):
```
### Artifact Completeness
| Phase | PLAN | CONTEXT | RESEARCH | SUMMARY | VERIFICATION |
|-------|------|---------|----------|---------|-------------|
| {phase name} | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ |
```

**G-08 — Post-mortem report: anomaly confidence levels (Dimension 5)**
In the "Evidence Gathered" or "Contributing Factors" section of the report template, add confidence annotation: each finding should carry `— Confidence: HIGH / MEDIUM / LOW`. silver-forensics already has confidence in the ROOT CAUSE line but not on individual evidence items. The Contributing Factors list items should become:
`- <finding> — Confidence: HIGH | MEDIUM | LOW`

**G-09 — Security boundary: redaction rules (Dimension 6)**
In the "## Security Boundary" section (or as a new "## Report Redaction" subsection before the Post-mortem Report section), add explicit redaction rules matching gsd-forensics critical_rules:
- Replace absolute paths with relative paths (strip `$HOME` prefix)
- Remove any API keys, tokens, or credentials found in git diff output
- Truncate large diffs to first 50 lines
These rules are currently absent from silver-forensics's security section.

**Preserve invariants during edits:**
- The GSD-awareness routing table in Step 1b MUST be preserved intact — it is silver-forensics's unique value and has no equivalent in gsd-forensics
- The three-path classification (session-level, task-level, general) MUST be preserved — it is cleaner than gsd-forensics's anomaly-detection approach
- The `ROOT CAUSE: <one sentence> — <path taken> — <confidence>` format MUST be preserved — it is stricter and better than gsd-forensics's prose hypothesis
- The slug-based collision-safe report naming (`YYYY-MM-DD-<slug>.md`) MUST be preserved
- The `## Allowed Commands` security section MUST be preserved
- Do not change the output directory: silver-forensics correctly writes to `docs/silver-forensics/` (not `.planning/forensics/` — these serve different audiences)

Apply only the gaps confirmed by the audit report. If the audit report says a dimension is "Equivalent", skip that fix.
  </action>
  <verify>
    <automated>grep -q "git worktree list" /Users/shafqat/Documents/Projects/silver-bullet/skills/silver-forensics/SKILL.md && grep -q "git diff --stat" /Users/shafqat/Documents/Projects/silver-bullet/skills/silver-forensics/SKILL.md && grep -q "Artifact Completeness" /Users/shafqat/Documents/Projects/silver-bullet/skills/silver-forensics/SKILL.md && grep -q "Confidence:" /Users/shafqat/Documents/Projects/silver-bullet/skills/silver-forensics/SKILL.md && echo "KEY GAP FIXES PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "git worktree list" skills/silver-forensics/SKILL.md` exits 0 (G-05 fixed)
    - `grep -q "git diff --stat" skills/silver-forensics/SKILL.md` exits 0 (G-03 fixed)
    - `grep -q "Artifact Completeness" skills/silver-forensics/SKILL.md` exits 0 (G-07 fixed)
    - `grep -q "Confidence:" skills/silver-forensics/SKILL.md` exits 0 (G-08 fixed)
    - `grep -q "strip.*HOME\|redact\|absolute path" skills/silver-forensics/SKILL.md` exits 0 (G-09 fixed)
    - `grep -q "scope drift\|Scope drift" skills/silver-forensics/SKILL.md` exits 0 (G-06 fixed)
    - GSD-awareness routing table (Step 1b) still present: `grep -q "Step 1b" skills/silver-forensics/SKILL.md` exits 0
    - ROOT CAUSE format still present: `grep -q "ROOT CAUSE:" skills/silver-forensics/SKILL.md` exits 0
    - Security Boundary section still present: `grep -q "## Security Boundary" skills/silver-forensics/SKILL.md` exits 0
  </acceptance_criteria>
  <done>All gaps from the audit report applied to silver-forensics/SKILL.md; invariants preserved</done>
</task>

<task type="auto">
  <name>Task 2: Append Fix Log to audit report</name>
  <read_first>
    - /Users/shafqat/Documents/Projects/silver-bullet/.planning/052-FORENSICS-AUDIT.md (to append, not overwrite)
    - /Users/shafqat/Documents/Projects/silver-bullet/skills/silver-forensics/SKILL.md (post-edit, to verify what was added)
  </read_first>
  <files>.planning/052-FORENSICS-AUDIT.md</files>
  <action>
Append a `## Fix Log` section to `.planning/052-FORENSICS-AUDIT.md` documenting what was changed in `skills/silver-forensics/SKILL.md` for each gap.

Format:

```markdown
---

## Fix Log

Applied fixes from the ## Gaps Found table to `skills/silver-forensics/SKILL.md`.

| Gap ID | Dimension | Change Made | Status |
|--------|-----------|-------------|--------|
| G-01 | 2 | Expanded git log depth to -30; added timestamp format call | FIXED |
| G-02 | 2 | Added file-frequency analysis call (sort/uniq-c) | FIXED |
| ... | ... | ... | ... |

**Invariants preserved:**
- GSD-awareness routing table (Step 1b): intact
- Three-path classification model: intact
- ROOT CAUSE: one-liner format: intact
- Slug-based collision-safe naming: intact
- Allowed Commands security section: intact
- Output directory (docs/silver-forensics/): intact

**FORN-02 status:** All gaps fixed. silver-forensics is functionally equivalent to gsd-forensics across all six dimensions.
```

Use the actual gap IDs from the audit report (G-01, G-02, etc.) and describe the exact change made to SKILL.md for each. If any gap from the audit report was found to be "Equivalent" (no fix needed), record it as "N/A — no gap confirmed."
  </action>
  <verify>
    <automated>grep -q "## Fix Log" /Users/shafqat/Documents/Projects/silver-bullet/.planning/052-FORENSICS-AUDIT.md && grep -q "FORN-02 status" /Users/shafqat/Documents/Projects/silver-bullet/.planning/052-FORENSICS-AUDIT.md && echo "FIX LOG PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Fix Log" .planning/052-FORENSICS-AUDIT.md` exits 0
    - `grep -q "FORN-02 status" .planning/052-FORENSICS-AUDIT.md` exits 0
    - The Fix Log table has one row per gap ID from the ## Gaps Found table
    - Each row has a Status of FIXED or N/A
  </acceptance_criteria>
  <done>Audit report updated with Fix Log; FORN-02 evidence is complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit report → SKILL.md edits | Gap list from Plan 052-01 drives edits to a skill that Claude agents execute |
| SKILL.md → forensics investigation | The edited skill instructs agents to read session logs (UNTRUSTED DATA) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-052-03 | Tampering | skills/silver-forensics/SKILL.md edits | mitigate | Preserve Security Boundary section intact; verify `grep -q "## Security Boundary"` after edit |
| T-052-04 | Information Disclosure | forensics report output | mitigate | G-09 fix adds redaction rules (strip $HOME paths, redact API keys) to SKILL.md so all future reports are redacted |
| T-052-05 | Elevation of Privilege | SKILL.md instruction injection | accept | SKILL.md is a trusted planning artifact within the repo; plugin boundary enforced by dev-cycle-check.sh |
</threat_model>

<verification>
- `grep -q "git worktree list" skills/silver-forensics/SKILL.md` — exits 0
- `grep -q "git diff --stat" skills/silver-forensics/SKILL.md` — exits 0
- `grep -q "Artifact Completeness" skills/silver-forensics/SKILL.md` — exits 0
- `grep -q "Confidence:" skills/silver-forensics/SKILL.md` — exits 0
- `grep -q "scope drift\|Scope drift" skills/silver-forensics/SKILL.md` — exits 0
- `grep -q "## Security Boundary" skills/silver-forensics/SKILL.md` — exits 0 (invariant preserved)
- `grep -q "Step 1b" skills/silver-forensics/SKILL.md` — exits 0 (GSD routing preserved)
- `grep -q "ROOT CAUSE:" skills/silver-forensics/SKILL.md` — exits 0 (format preserved)
- `grep -q "## Fix Log" .planning/052-FORENSICS-AUDIT.md` — exits 0
- `grep -q "FORN-02 status" .planning/052-FORENSICS-AUDIT.md` — exits 0
</verification>

<success_criteria>
All gaps from the audit report are fixed in `skills/silver-forensics/SKILL.md`. The audit report at `.planning/052-FORENSICS-AUDIT.md` contains both the gap analysis and the fix log, satisfying FORN-01 and FORN-02. Phase 54 (silver-scan) can proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/052-silver-forensics-audit/052-02-SUMMARY.md` following the summary template.
</output>
