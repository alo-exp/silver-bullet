---
phase: 20-cross-artifact-consistency
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - skills/silver-feature/SKILL.md
  - skills/silver-release/SKILL.md
  - silver-bullet.md
  - templates/silver-bullet.md.base
autonomous: true
requirements: [ARVW-09e]

must_haves:
  truths:
    - "gsd-complete-milestone is preceded by cross-artifact review gate in silver-feature workflow"
    - "gsd-complete-milestone is preceded by cross-artifact review gate in silver-release workflow"
    - "Milestone completion is blocked if cross-artifact inconsistencies are found"
  artifacts:
    - path: "skills/silver-feature/SKILL.md"
      provides: "Cross-artifact review step before milestone completion"
      contains: "review-cross-artifact"
    - path: "skills/silver-release/SKILL.md"
      provides: "Cross-artifact review step before milestone completion"
      contains: "review-cross-artifact"
    - path: "silver-bullet.md"
      provides: "Documentation of cross-artifact gate"
      contains: "review-cross-artifact"
    - path: "templates/silver-bullet.md.base"
      provides: "Template with cross-artifact gate"
      contains: "review-cross-artifact"
  key_links:
    - from: "skills/silver-feature/SKILL.md"
      to: "skills/review-cross-artifact/SKILL.md"
      via: "Step 17.0b invocation"
      pattern: "review-cross-artifact"
    - from: "skills/silver-release/SKILL.md"
      to: "skills/review-cross-artifact/SKILL.md"
      via: "Pre-milestone step"
      pattern: "review-cross-artifact"
---

<objective>
Wire the cross-artifact consistency reviewer into milestone completion workflows so inconsistencies block shipping.

Purpose: Ensure SPEC↔REQUIREMENTS↔ROADMAP↔DESIGN alignment is validated before any milestone closes.
Output: Updated silver-feature, silver-release, and silver-bullet.md with cross-artifact review gate before gsd-complete-milestone.
</objective>

<execution_context>
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/workflows/execute-plan.md
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cross-artifact-consistency/20-01-SUMMARY.md
@skills/silver-feature/SKILL.md
@skills/silver-release/SKILL.md
@silver-bullet.md
@templates/silver-bullet.md.base

<interfaces>
<!-- From 20-01: the new reviewer skill -->
```
Skill: review-cross-artifact
Invocation: /artifact-reviewer --artifacts .planning/SPEC.md .planning/REQUIREMENTS.md .planning/ROADMAP.md [.planning/DESIGN.md]
Output: PASS or ISSUES_FOUND with XART-F findings
```

<!-- From silver-feature Step 17 (current): -->
```
Step 17.0a: Review UAT.md (existing)
  → artifact-reviewer .planning/UAT.md --reviewer review-uat
Step 17.1: gsd-audit-uat
Step 17.2: gsd-audit-milestone
Step 17.3: gap closure (max 2 iterations)
Step 17.4: gsd-complete-milestone
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire cross-artifact review into silver-feature and silver-release</name>
  <files>skills/silver-feature/SKILL.md, skills/silver-release/SKILL.md</files>
  <read_first>
    - skills/silver-feature/SKILL.md (find Step 17.0a and Step 17 item 4 — insert new step between audit and complete-milestone)
    - skills/silver-release/SKILL.md (find Step 8 — insert cross-artifact review before gsd-complete-milestone)
  </read_first>
  <action>
**silver-feature/SKILL.md changes:**

Insert a new Step 17.0b AFTER Step 17.0a (UAT review) and BEFORE the numbered list (gsd-audit-uat etc.). This follows the existing pattern where 17.0a was inserted as a review gate.

```markdown
### Step 17.0b: Cross-Artifact Consistency Review

Invoke `/artifact-reviewer --reviewer review-cross-artifact` with artifacts:
- `.planning/SPEC.md`
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `.planning/DESIGN.md` (only if it exists)

Do NOT proceed to gsd-audit-uat until cross-artifact review reports clean pass. If ISSUES_FOUND, the orchestrator applies fixes and re-reviews per the review loop. If unresolvable after 5 rounds, STOP and present to the user.

**Why here:** Cross-artifact alignment must be confirmed before milestone audit begins — auditing against misaligned artifacts wastes effort.
```

**silver-release/SKILL.md changes:**

Insert a new Step 7.5 AFTER Step 7 (gsd-ship) and BEFORE Step 8 (gsd-complete-milestone):

```markdown
## Step 7.5: Cross-Artifact Consistency Review

**Only if `.planning/SPEC.md` and `.planning/REQUIREMENTS.md` exist:**

Invoke `/artifact-reviewer --reviewer review-cross-artifact` with:
- `.planning/SPEC.md`
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `.planning/DESIGN.md` (only if it exists)

Do NOT proceed to Step 8 (gsd-complete-milestone) until cross-artifact review reports clean pass. If unresolvable after 5 rounds, STOP and present to the user.
```
  </action>
  <verify>
    <automated>grep -q "review-cross-artifact" skills/silver-feature/SKILL.md && grep -q "review-cross-artifact" skills/silver-release/SKILL.md && grep -q "17.0b\|17\.0b" skills/silver-feature/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "review-cross-artifact" skills/silver-feature/SKILL.md returns match
    - grep "review-cross-artifact" skills/silver-release/SKILL.md returns match
    - grep "17.0b" skills/silver-feature/SKILL.md returns match
    - grep "Cross-Artifact Consistency" skills/silver-feature/SKILL.md returns match
    - grep "Cross-Artifact Consistency" skills/silver-release/SKILL.md returns match
  </acceptance_criteria>
  <done>Both silver-feature and silver-release workflows gate milestone completion behind cross-artifact review</done>
</task>

<task type="auto">
  <name>Task 2: Document cross-artifact gate in silver-bullet.md and template</name>
  <files>silver-bullet.md, templates/silver-bullet.md.base</files>
  <read_first>
    - silver-bullet.md (find the UAT Gate paragraph near line 352 and the review wiring table near line 424 — add cross-artifact gate documentation nearby)
    - templates/silver-bullet.md.base (same sections — must be updated atomically with silver-bullet.md per §3 rules)
  </read_first>
  <action>
**Both files get identical changes** (silver-bullet.md and templates/silver-bullet.md.base must stay in sync):

1. **After the "UAT Gate:" paragraph** (around line 352 in silver-bullet.md), add:

```markdown
**Cross-Artifact Gate:** Before `gsd-complete-milestone`, cross-artifact consistency is validated. `/artifact-reviewer --reviewer review-cross-artifact` checks SPEC↔REQUIREMENTS↔ROADMAP↔DESIGN alignment. Milestone completion is blocked if any ISSUE-level inconsistencies are found (unmapped ACs, orphaned requirements, missing design coverage).
```

2. **In the review wiring table** (around line 424 in silver-bullet.md), add a new row:

```
| Cross-artifact set | SPEC.md, REQUIREMENTS.md, ROADMAP.md, DESIGN.md | /artifact-reviewer --reviewer review-cross-artifact | YES | /silver:feature Step 17.0b, /silver:release Step 7.5 |
```

Apply identical changes to both files. Per the project's §3 rules, silver-bullet.md and templates/silver-bullet.md.base must be updated atomically.
  </action>
  <verify>
    <automated>grep -q "Cross-Artifact Gate" silver-bullet.md && grep -q "Cross-Artifact Gate" templates/silver-bullet.md.base && grep -q "review-cross-artifact" silver-bullet.md && grep -q "review-cross-artifact" templates/silver-bullet.md.base && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Cross-Artifact Gate" silver-bullet.md returns match
    - grep "Cross-Artifact Gate" templates/silver-bullet.md.base returns match
    - grep "review-cross-artifact" silver-bullet.md returns match (in wiring table)
    - grep "review-cross-artifact" templates/silver-bullet.md.base returns match
    - grep "17.0b" silver-bullet.md returns match (step reference in table)
  </acceptance_criteria>
  <done>silver-bullet.md and template both document the cross-artifact gate with wiring table entry, updated atomically</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow step → reviewer invocation | Workflow must pass correct artifact paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | E (Elevation) | milestone completion | mitigate | Cross-artifact review gate blocks gsd-complete-milestone — cannot skip per §3 non-negotiable rules |
| T-20-05 | T (Tampering) | silver-bullet.md sync | mitigate | Both silver-bullet.md and templates/silver-bullet.md.base updated atomically in same task |
</threat_model>

<verification>
- silver-feature Step 17.0b exists and invokes review-cross-artifact before gsd-audit-uat
- silver-release has cross-artifact review step before gsd-complete-milestone
- silver-bullet.md documents the Cross-Artifact Gate
- templates/silver-bullet.md.base matches silver-bullet.md changes
- Review wiring table includes review-cross-artifact row
</verification>

<success_criteria>
Cross-artifact consistency review is wired as a blocking gate before milestone completion in all workflows, documented in silver-bullet.md and its template.
</success_criteria>

<output>
After completion, create `.planning/phases/20-cross-artifact-consistency/20-02-SUMMARY.md`
</output>
