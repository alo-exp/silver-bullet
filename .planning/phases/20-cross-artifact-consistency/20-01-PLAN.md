---
phase: 20-cross-artifact-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/review-cross-artifact/SKILL.md
autonomous: true
requirements: [ARVW-09a, ARVW-09b, ARVW-09c, ARVW-09d]
must_haves:
  truths:
    - "Cross-artifact reviewer accepts a list of artifact paths and returns ISSUE findings for misalignment"
    - "SPEC-to-REQUIREMENTS check detects unmapped ACs and orphaned requirements"
    - "REQUIREMENTS-to-ROADMAP check detects requirements missing from phases and phantom phase requirements"
    - "SPEC-to-DESIGN check runs conditionally (only when DESIGN.md exists) and detects orphaned components or uncovered user stories"
    - "Each consistency check produces separate finding groups with distinct ID prefixes"
  artifacts:
    - path: "skills/review-cross-artifact/SKILL.md"
      provides: "Cross-artifact reviewer skill definition with all QC checks"
      contains: "review-cross-artifact"
  key_links:
    - from: "skills/review-cross-artifact/SKILL.md"
      to: "skills/artifact-reviewer/rules/reviewer-interface.md"
      via: "Loading Rules reference"
      pattern: "reviewer-interface.md"
---

<objective>
Create the cross-artifact consistency reviewer skill that validates alignment across SPEC.md, REQUIREMENTS.md, ROADMAP.md, and DESIGN.md.

Purpose: Prevent shipping misaligned artifacts by detecting unmapped ACs, orphaned requirements, missing design coverage, and phantom phase requirements.
Output: `skills/review-cross-artifact/SKILL.md` — a complete reviewer skill following the artifact-reviewer framework interface.
</objective>

<execution_context>
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/workflows/execute-plan.md
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/artifact-reviewer/SKILL.md
@skills/artifact-reviewer/rules/reviewer-interface.md
@skills/artifact-reviewer/rules/review-loop.md
@skills/review-spec/SKILL.md

<interfaces>
<!-- Reviewer interface contract that this new skill must implement -->

From skills/artifact-reviewer/rules/reviewer-interface.md:
```
Input:
  artifact_path: string     -- absolute or relative path to artifact (REQUIRED)
  source_inputs: string[]   -- paths to source artifacts
  review_context: string    -- optional context
  check_mode: "full" | "structural"

Output:
  status: "PASS" | "ISSUES_FOUND"
  findings: Finding[]

Finding:
  id: string          -- e.g., "XART-F01"
  severity: "ISSUE" | "INFO"
  description: string
  location: string
  suggestion: string
```

From skills/review-spec/SKILL.md (pattern reference):
```
- Loading Rules section referencing reviewer-interface.md and review-loop.md
- Quality Criteria with numbered QC-N sections
- Each QC check tagged as structural or content
- Output Contract section with finding ID prefix
- Reviewer Prohibitions section
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cross-artifact reviewer skill</name>
  <files>skills/review-cross-artifact/SKILL.md</files>
  <read_first>
    - skills/review-spec/SKILL.md (pattern: frontmatter, Loading Rules, QC structure, Output Contract, Prohibitions)
    - skills/artifact-reviewer/rules/reviewer-interface.md (interface contract to implement)
    - skills/review-requirements/SKILL.md (understand how requirements are structured for cross-ref)
    - skills/review-roadmap/SKILL.md (understand roadmap phase/requirement structure)
    - skills/review-design/SKILL.md (understand design component structure)
  </read_first>
  <action>
Create `skills/review-cross-artifact/SKILL.md` following the exact pattern from review-spec/SKILL.md. This is a NEW reviewer skill, not a modification of any existing one.

**Frontmatter** (YAML frontmatter delimiters):

    name: review-cross-artifact
    description: "Cross-artifact consistency reviewer -- validates alignment across SPEC.md, REQUIREMENTS.md, ROADMAP.md, and DESIGN.md. Detects unmapped ACs, orphaned requirements, missing design coverage, and phantom phase requirements."
    argument-hint: "--artifacts <spec-path> <requirements-path> <roadmap-path> [design-path]"

**Key differences from single-artifact reviewers:**
- Input contract accepts MULTIPLE artifact paths via `source_inputs` array (not a single `artifact_path`)
- Use `artifact_path` for the PRIMARY artifact (SPEC.md -- the source of truth) and `source_inputs` for the others
- Actually, to align with the interface: `artifact_path` = first artifact in the list, `source_inputs` = remaining artifacts. But document clearly that this reviewer expects ALL artifacts in the combined list.
- Better approach: use `artifact_path` as a sentinel value (e.g., ".planning/SPEC.md") and pass all artifact paths via `source_inputs`. Document this convention clearly.

**Input section:** Table with:
| Field | Type | Required | Description |
| artifacts | string[] | YES | List of artifact paths to check consistency across. Accepts: SPEC.md, REQUIREMENTS.md, ROADMAP.md, DESIGN.md |
| check_mode | "full" / "structural" | YES | Defaults to "full" |

Explain: The reviewer auto-detects which artifacts are provided by filename pattern matching (same approach as artifact-reviewer's mapping table). At minimum SPEC.md and REQUIREMENTS.md must be present. ROADMAP.md is expected but optional. DESIGN.md is fully optional (ARVW-09d conditional check).

**Quality Criteria -- 3 independent check groups:**

### QC-1: SPEC to REQUIREMENTS Alignment (ARVW-09b) [content]
- Parse all AC IDs from SPEC.md `## Acceptance Criteria` section (pattern: `AC-XX` or similar identifiers)
- Parse all requirement IDs from REQUIREMENTS.md (pattern: `**REQ-XX**:` or `- [x] **XXX-NNx**:`)
- For EACH AC in SPEC: verify at least one requirement references it (by ID or by content mapping). If not, emit ISSUE finding `XART-F01` "AC {id} in SPEC.md has no corresponding requirement in REQUIREMENTS.md"
- For EACH requirement in REQUIREMENTS.md: verify it traces back to a SPEC AC or is explicitly marked as derived. If orphaned, emit ISSUE finding `XART-F02` "Requirement {id} has no source AC in SPEC.md"
- Suggestion for F01: "Add a requirement in REQUIREMENTS.md that implements AC {id}, or update the AC ID reference in an existing requirement"
- Suggestion for F02: "Map requirement {id} to a SPEC.md AC, or add an AC that this requirement implements"

### QC-2: REQUIREMENTS to ROADMAP Alignment (ARVW-09c) [content]
- Parse all requirement IDs from REQUIREMENTS.md
- Parse all phase `Requirements:` lines from ROADMAP.md (pattern: `**Requirements:** [ID1, ID2]` or `**Requirements**: ID1, ID2`)
- For EACH requirement: verify it appears in at least one phase's Requirements line. If not, emit ISSUE finding `XART-F10` "Requirement {id} is not mapped to any ROADMAP phase"
- For EACH requirement ID referenced in a ROADMAP phase: verify it exists in REQUIREMENTS.md. If not, emit ISSUE finding `XART-F11` "Phase {N} references requirement {id} which does not exist in REQUIREMENTS.md"
- Suggestion for F10: "Add requirement {id} to the appropriate phase's Requirements line in ROADMAP.md"
- Suggestion for F11: "Add requirement {id} to REQUIREMENTS.md, or remove the reference from Phase {N} in ROADMAP.md"

### QC-3: SPEC to DESIGN Alignment (ARVW-09d) [content] -- CONDITIONAL
- **Only evaluate when DESIGN.md is provided in the artifacts list.** If DESIGN.md is not provided, skip this entire check and emit INFO finding `XART-I01` "DESIGN.md not provided -- SPEC-to-DESIGN consistency check skipped"
- Parse user stories from SPEC.md `## User Stories` section
- Parse component/module definitions from DESIGN.md (section headers, component names)
- For EACH user story: verify at least one design component addresses it. If not, emit ISSUE finding `XART-F20` "User story '{story}' has no design coverage in DESIGN.md"
- For EACH design component: verify it relates to at least one user story or AC. If orphaned, emit ISSUE finding `XART-F21` "Design component '{component}' has no corresponding user story or AC in SPEC.md"
- Suggestion for F20: "Add a design component or section in DESIGN.md that addresses this user story"
- Suggestion for F21: "Map this design component to a SPEC.md user story, or remove if it is not needed"

**Structural vs Content tagging:**
- All three QC checks are `content`-tagged (they validate cross-reference consistency, not structure)
- In `structural` check_mode, ALL checks are skipped and the reviewer returns PASS with INFO finding `XART-I00` "Cross-artifact consistency checks are content-level only -- structural mode returns automatic PASS"

**Output Contract section:** Finding IDs use prefix `XART-F` for issues, `XART-I` for info. Same schema as reviewer-interface.md.

**Loading Rules section:** Same pattern as review-spec -- must load reviewer-interface.md and review-loop.md.

**Reviewer Prohibitions section:** Same 5 prohibitions as review-spec.
  </action>
  <verify>
    <automated>grep -c "QC-1\|QC-2\|QC-3" skills/review-cross-artifact/SKILL.md | grep -q "3" && grep -q "XART-F01\|XART-F10\|XART-F20" skills/review-cross-artifact/SKILL.md && grep -q "reviewer-interface.md" skills/review-cross-artifact/SKILL.md && grep -q "review-loop.md" skills/review-cross-artifact/SKILL.md && grep -q "DESIGN.md.*optional\|conditional\|CONDITIONAL" skills/review-cross-artifact/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "name: review-cross-artifact" skills/review-cross-artifact/SKILL.md returns match
    - grep "QC-1" skills/review-cross-artifact/SKILL.md returns match (SPEC-to-REQ check)
    - grep "QC-2" skills/review-cross-artifact/SKILL.md returns match (REQ-to-ROADMAP check)
    - grep "QC-3" skills/review-cross-artifact/SKILL.md returns match (SPEC-to-DESIGN check)
    - grep "XART-F01" skills/review-cross-artifact/SKILL.md returns match
    - grep "XART-F10" skills/review-cross-artifact/SKILL.md returns match
    - grep "XART-F20" skills/review-cross-artifact/SKILL.md returns match
    - grep "conditional\|CONDITIONAL\|optional" skills/review-cross-artifact/SKILL.md returns match for DESIGN.md check
    - grep "reviewer-interface.md" skills/review-cross-artifact/SKILL.md returns match
    - grep "review-loop.md" skills/review-cross-artifact/SKILL.md returns match
  </acceptance_criteria>
  <done>skills/review-cross-artifact/SKILL.md exists with 3 QC checks (SPEC-to-REQ, REQ-to-ROADMAP, SPEC-to-DESIGN), XART-F finding prefixes, conditional DESIGN.md check, and proper framework loading rules</done>
</task>

<task type="auto">
  <name>Task 2: Register cross-artifact reviewer in artifact-reviewer mapping</name>
  <files>skills/artifact-reviewer/SKILL.md</files>
  <read_first>
    - skills/artifact-reviewer/SKILL.md (current mapping table to extend)
  </read_first>
  <action>
Add one row to the Artifact-to-Reviewer Mapping table in `skills/artifact-reviewer/SKILL.md`:

| Cross-artifact set | review-cross-artifact | New SB reviewer -- multi-artifact |

This goes after the existing `UAT.md | review-uat` row.

No other changes to the file.
  </action>
  <verify>
    <automated>grep -q "review-cross-artifact" skills/artifact-reviewer/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "review-cross-artifact" skills/artifact-reviewer/SKILL.md returns match
    - grep "Cross-artifact" skills/artifact-reviewer/SKILL.md returns match
  </acceptance_criteria>
  <done>artifact-reviewer SKILL.md mapping table includes the cross-artifact reviewer entry</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artifact files to reviewer | Reviewer reads artifact content which could have malformed markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | T (Tampering) | review-cross-artifact | accept | Reviewer is read-only per interface contract; cannot modify artifacts |
| T-20-02 | D (Denial of Service) | QC checks | mitigate | Safety cap from review-loop.md (5 rounds max) prevents infinite loops |
| T-20-03 | I (Info Disclosure) | Finding output | accept | Findings contain artifact content references -- same trust level as reviewer invoker |
</threat_model>

<verification>
- `skills/review-cross-artifact/SKILL.md` exists and follows reviewer interface contract
- 3 QC checks cover ARVW-09b, ARVW-09c, ARVW-09d respectively
- DESIGN.md check (QC-3) is conditional -- skipped when DESIGN.md not provided
- Finding IDs use XART-F prefix for ISSUE, XART-I for INFO
- artifact-reviewer mapping table includes review-cross-artifact
</verification>

<success_criteria>
Cross-artifact reviewer skill created with 3 independent consistency checks, registered in the artifact-reviewer framework, and ready for wiring into milestone completion.
</success_criteria>

<output>
After completion, create `.planning/phases/20-cross-artifact-consistency/20-01-SUMMARY.md`
</output>
