---
phase: 01-workflow-file-rewrites
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/workflows/devops-cycle.md
autonomous: true
requirements:
  - ORCH-02
  - ORCH-03
  - ORCH-04
  - ORCH-05
  - ORCH-06
  - TRANS-02
  - TRANS-03

must_haves:
  truths:
    - "A GSD-naive user reading devops-cycle.md understands what each GSD step does for infrastructure work without consulting GSD documentation"
    - "Every step in the per-phase loop has a what-it-does summary, what-to-expect section, and what-to-do-if-it-fails recovery"
    - "All 20 guided GSD commands appear at their natural workflow points with context"
    - "Utility commands have a dedicated reference section with when-to-use guidance"
    - "Non-GSD skills and DevOps-specific skills are inserted at correct trigger conditions"
    - "Incident fast path, blast radius, and environment promotion sections are fully explained"
    - "After RELEASE, the workflow offers DevOps-to-Dev transition"
    - "All existing enforcement rules carry forward unchanged"
  artifacts:
    - path: "docs/workflows/devops-cycle.md"
      provides: "Complete orchestration guide for DevOps/infrastructure cycle"
      contains: "## How This Works"
      min_lines: 600
  key_links:
    - from: "docs/workflows/devops-cycle.md"
      to: "silver-bullet.md"
      via: "enforcement rules reference"
      pattern: "NON-NEGOTIABLE"
    - from: "docs/workflows/devops-cycle.md"
      to: ".silver-bullet.json"
      via: "active_workflow transition"
      pattern: "active_workflow"
---

<objective>
Rewrite docs/workflows/devops-cycle.md from a 439-line enforcement checklist into a ~650-750 line comprehensive orchestration guide for infrastructure and DevOps work.

Purpose: Users who have never heard of GSD should be able to follow the entire DevOps/infrastructure cycle with complete understanding of every step. This includes DevOps-specific additions (incident fast path, blast radius analysis, environment promotion) that have no equivalent in the dev cycle.

Output: Rewritten docs/workflows/devops-cycle.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workflow-file-rewrites/01-CONTEXT.md

<!-- Current file being rewritten (MUST read first to understand current structure) -->
@docs/workflows/devops-cycle.md

<!-- GSD workflow references — read each to understand what the step actually does.
     The executor MUST read these to write accurate what/expect/fail sections. -->
@~/.claude/get-shit-done/workflows/new-project.md
@~/.claude/get-shit-done/workflows/new-milestone.md
@~/.claude/get-shit-done/workflows/map-codebase.md
@~/.claude/get-shit-done/workflows/discuss-phase.md
@~/.claude/get-shit-done/workflows/plan-phase.md
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/workflows/verify-work.md
@~/.claude/get-shit-done/workflows/ship.md
@~/.claude/get-shit-done/workflows/autonomous.md
@~/.claude/get-shit-done/workflows/diagnose-issues.md
@~/.claude/get-shit-done/workflows/quick.md
@~/.claude/get-shit-done/workflows/fast.md
@~/.claude/get-shit-done/workflows/next.md
@~/.claude/get-shit-done/workflows/pause-work.md
@~/.claude/get-shit-done/workflows/resume-project.md
@~/.claude/get-shit-done/workflows/progress.md
@~/.claude/get-shit-done/workflows/add-phase.md
@~/.claude/get-shit-done/workflows/insert-phase.md
@~/.claude/get-shit-done/workflows/review.md
@~/.claude/get-shit-done/workflows/audit-milestone.md
@~/.claude/get-shit-done/workflows/complete-milestone.md

<!-- Enforcement rules from silver-bullet.md that must carry forward -->
@silver-bullet.md

<!-- Reference the dev cycle plan so executor understands the parallel structure -->
@.planning/phases/01-workflow-file-rewrites/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite devops-cycle.md as orchestration guide</name>
  <files>docs/workflows/devops-cycle.md</files>
  <action>
Read ALL context files listed above (current workflow + every GSD workflow file + silver-bullet.md). Then rewrite docs/workflows/devops-cycle.md following this exact structure per D-01:

**File structure (sections in order):**

1. **Header + enforcement banner** — Keep the existing ENFORCED banner (lines 1-13 of current file, including the IMPORTANT note about .yml/.yaml files not being exempt). Update if wording needs to reflect orchestration guide language, but preserve ALL enforcement semantics including the YAML non-exemption.

2. **Invocation Methods** — Keep current table (lines 15-20). Note this table references `/blast-radius` and `/devops-quality-gates` instead of `/quality-gates`.

3. **## How This Works** (NEW) — 10-15 line overview explaining: SB orchestrates infrastructure and DevOps work by guiding you through GSD steps adapted for IaC. Each section tells you what happens, what to expect, and what to do if something goes wrong. Includes incident fast path for production emergencies, blast radius analysis before changes, and environment promotion for safe rollouts. No GSD knowledge required.

4. **## Incident Fast Path** — Carry forward current section (lines 28-45) with enhanced explanations per D-07 and D-02. Add:
   - **What it does:** Emergency change process for active production incidents
   - **When to use:** Active incident with confirmed production impact AND change cannot wait
   - **What to expect:** Severity classification, minimal change, post-incident review task
   - `/incident-response` is the FIRST step per D-07
   Keep all 6 fast path steps but add brief explanation for each.

5. **## Session Mode** (STEP 0) — Carry forward current Step 0 logic per D-13. Add brief "what this does" intro. Note: DevOps session mode has fewer pre-answer options than dev cycle (no Design model routing).

6. **## Project Setup** — Rewrite current PROJECT INITIALIZATION with brownfield detection per D-12 (same 4 paths as dev cycle, but with DevOps-specific guidance):
   - Same 4 brownfield detection paths
   - DevOps-specific note: "For DevOps projects, roadmap phases typically map to infrastructure layers: networking -> storage -> compute -> monitoring -> CI/CD"
   - Worktree recommendation: "Strongly recommended for changes touching production resources"

7. **## Per-Phase Loop** — Core of the file. For EACH step, write three sub-sections per D-02:
   - **What it does:** One sentence summary
   - **What to expect:** Artifacts, typical duration, what user sees
   - **If it fails:** Specific recovery per D-08

   Steps within the per-phase loop (preserve current DevOps ordering which DIFFERS from dev cycle):
   - **### Model Routing** — Keep current logic.
   - **### Skill Discovery** — Keep current logic (surfaces `/blast-radius`, `/devops-skill-router`).
   - **### Discuss** (`/gsd:discuss-phase`) — Per D-04. DevOps-specific: include target environments, IaC toolchain, state backend, naming conventions per current file. Per D-07, contextual enrichment via `/devops-skill-router` after capturing decisions. Per D-06, conditional sub-steps: `/system-design` IF new service. Per D-08, if discuss fails: re-run with more specific questions about infra requirements.
   - **### Blast Radius** (`/blast-radius`) — Per D-07, ALWAYS required, comes BEFORE quality gates. Rating gate: LOW/MEDIUM proceed, HIGH needs approval + runbook, CRITICAL is hard stop (CAB review). Explain what blast radius analysis produces and why it matters for infra changes.
   - **### DevOps Quality Gates** (`/devops-quality-gates`) — Per D-07, ALWAYS (7 IaC-adapted dimensions). Per D-08, if gates fail: fix specific dimension, re-run.
   - **### Plan** (`/gsd:plan-phase`) — Both blast radius AND quality gate reports feed in. DevOps-specific wave ordering: networking/IAM -> storage/data -> compute/services -> monitoring -> CI/CD pipeline. Per D-07, contextual enrichment via `/devops-skill-router`. Per D-08, if plan fails: check context, re-discuss if needed.
   - **### Execute** (`/gsd:execute-phase`) — Per D-04, wave-based execution targeting LOWEST environment only. Per D-06, `/test-driven-development` ALWAYS (Terratest/conftest/OPA for Terraform, helm test/BATS for Helm). Per D-07, contextual enrichment via `/devops-skill-router` for vendor-specific IaC skills. Per D-08, if execute fails: use `/gsd:debug`. DevOps-specific: plan/dry-run before apply, verify resource health after apply.
   - **### Verify** (`/gsd:verify-work`) — Infrastructure verification (NOT UAT). DevOps-specific checks: health checks, no configuration drift, monitoring/alerting, rollback tested, runbook updated. Per D-08, if verify fails: invoke `/forensics` FIRST. Per D-07, contextual enrichment for diagnostics.
   - **### Code Review** — Same structure as dev cycle but with IaC-specific review focus: hardcoded values, missing tags, permissive security groups, missing encryption/backups, plan output reviewed. Keep review loop enforcement (2 consecutive approvals).
   - **### Post-Review Execution** — Same as dev cycle.

8. **## Environment Promotion** — Carry forward current section (lines 279-290) with enhanced explanations:
   - **What it does:** Promotes verified infrastructure changes from lower to higher environments
   - **What to expect:** Re-runs execute + verify for each environment tier
   - **If it fails:** Roll back the promotion, diagnose in the lower environment first
   - Key principle: "Never rewrite infrastructure definitions -- only the inputs change"

9. **## Finalization** — Same structure as dev cycle but with DevOps-specific documentation requirements. Each skill gets what/expect/fail. DevOps additions: `docs/Runbooks.md` (one section per phase/component). Per D-07, contextual enrichment for CI/CD pipeline test integration. Per D-06, all finalization skills ALWAYS required.

10. **## Deployment** — DevOps-specific: plan -> review -> apply (never auto-apply to prod), plan output stored as pipeline artifact. Keep CI verification gate and autonomous polling. DevOps additions to deploy-checklist: blast radius reviewed, rollback tested, on-call notified, change window confirmed, dashboards baselining. Add "Production apply" step: one resource group at a time if HIGH blast radius, monitor for 15 min after.

11. **## Ship** — `/gsd:ship`. DevOps-specific: include blast radius ratings and post-apply drift detection results in PR.

12. **## Release** — `/create-release`. Keep autonomous completion cleanup.

13. **## Transition to Development** (NEW per D-10) — After RELEASE:
    - Offer: "Infrastructure deployed. Continue developing features for the next milestone?"
    - If yes: update `active_workflow` in `.silver-bullet.json` to `full-dev-cycle`, start new milestone with `/gsd:new-milestone`
    - Per D-11/TRANS-03: preserve `.planning/` artifacts, `.silver-bullet.json` config, state file, all committed history
    - Explicitly state what is preserved during transition

14. **## Utility Commands** (NEW per D-04/D-05/ORCH-05) — Same utility reference as dev cycle (all 11 utilities) but add DevOps context where relevant:
    - `/gsd:debug` — "For IaC: diagnose Terraform state drift, failed applies, resource conflicts"
    - `/gsd:quick` — "For quick infra fixes: security group rule updates, tag corrections, variable changes"
    - All others: same as dev cycle

15. **## Enforcement Rules** — Carry forward VERBATIM from current file (lines 408-439 including DevOps-specific enforcement: DISCUSS -> BLAST RADIUS -> QUALITY GATES -> PLAN -> EXECUTE -> VERIFY order, .yml/.yaml files are infrastructure code, trivial change non-exemption for YAML/JSON) per D-03. Also carry forward "GSD / Superpowers Ownership Rules" verbatim.

**Per D-14 (autonomous mode):** Throughout the guide, document autonomous mode behaviors inline where they differ from interactive.

**What NOT to include per D-05:** Admin commands are NOT referenced.

**Quality bar:** Same as dev cycle -- a DevOps engineer who has never seen GSD should be able to follow this file top-to-bottom and manage infrastructure through the entire lifecycle.

**Target length:** 650-750 lines. The DevOps cycle has more sections (incident fast path, blast radius, environment promotion) so it will be longer than the dev cycle.
  </action>
  <verify>
    <automated>
# Verify file exists and is in range
LINES=$(wc -l < docs/workflows/devops-cycle.md)
echo "Line count: $LINES"
test "$LINES" -ge 550 && test "$LINES" -le 850 && echo "PASS: line count in range" || echo "FAIL: line count out of range"

# Verify all 20 guided GSD commands appear
for cmd in "gsd:new-project" "gsd:new-milestone" "gsd:map-codebase" "gsd:discuss-phase" "gsd:plan-phase" "gsd:execute-phase" "gsd:verify-work" "gsd:ship" "gsd:complete-milestone" "gsd:autonomous" "gsd:debug" "gsd:quick" "gsd:fast" "gsd:resume" "gsd:pause" "gsd:progress" "gsd:next" "gsd:add-phase" "gsd:insert-phase" "gsd:review" "gsd:audit-milestone"; do
  grep -q "$cmd" docs/workflows/devops-cycle.md && echo "PASS: $cmd found" || echo "FAIL: $cmd missing"
done

# Verify key sections exist (DevOps-specific sections included)
for section in "How This Works" "Incident Fast Path" "Session Mode" "Project Setup" "Per-Phase Loop" "Blast Radius" "DevOps Quality Gates" "Environment Promotion" "Finalization" "Deployment" "Ship" "Release" "Transition to Development" "Utility Commands" "Enforcement Rules"; do
  grep -qi "$section" docs/workflows/devops-cycle.md && echo "PASS: section '$section' found" || echo "FAIL: section '$section' missing"
done

# Verify DevOps-specific skills
for skill in "blast-radius" "devops-quality-gates" "incident-response" "devops-skill-router" "test-driven-development" "forensics" "code-review" "requesting-code-review" "receiving-code-review" "testing-strategy" "tech-debt" "documentation" "finishing-a-development-branch" "deploy-checklist" "create-release"; do
  grep -q "$skill" docs/workflows/devops-cycle.md && echo "PASS: skill '$skill' found" || echo "FAIL: skill '$skill' missing"
done

# Verify error recovery pattern
grep -c "fails\|If it fails\|What to do" docs/workflows/devops-cycle.md | xargs -I{} echo "Error recovery mentions: {}"

# Verify enforcement rules carried forward
grep -q "TWICE IN A ROW" docs/workflows/devops-cycle.md && echo "PASS: review loop enforcement present" || echo "FAIL: review loop enforcement missing"
grep -q "BLAST RADIUS.*QUALITY GATES.*PLAN.*EXECUTE.*VERIFY" docs/workflows/devops-cycle.md && echo "PASS: DevOps step order enforcement present" || echo "FAIL: DevOps step order enforcement missing"
grep -q "yml.*yaml.*infrastructure" docs/workflows/devops-cycle.md && echo "PASS: YAML enforcement present" || echo "FAIL: YAML enforcement missing"

# Verify transition section
grep -q "active_workflow" docs/workflows/devops-cycle.md && echo "PASS: transition logic present" || echo "FAIL: transition logic missing"
grep -q "full-dev-cycle" docs/workflows/devops-cycle.md && echo "PASS: dev cycle reference present" || echo "FAIL: dev cycle reference missing"

# Verify admin commands are NOT present (D-05)
for cmd in "gsd-manager" "gsd-settings" "gsd-stats" "gsd-note" "gsd-add-todo" "gsd-join-discord"; do
  grep -q "$cmd" docs/workflows/devops-cycle.md && echo "FAIL: admin command '$cmd' should NOT appear" || echo "PASS: admin command '$cmd' correctly absent"
done
    </automated>
  </verify>
  <done>
docs/workflows/devops-cycle.md is rewritten as a comprehensive orchestration guide with:
- All 20 guided GSD commands at natural workflow points
- What/expect/fail sections for every per-phase step
- DevOps-specific sections fully explained (incident fast path, blast radius, environment promotion)
- Non-GSD skills and DevOps-specific skills inserted at correct trigger conditions
- Brownfield detection in Project Setup
- DevOps-to-Dev transition section after Release
- Utility Commands reference section with DevOps context
- All enforcement rules carried forward verbatim (including YAML non-exemption)
- 650-750 lines total
- A DevOps engineer new to GSD can follow the entire infrastructure workflow without external docs
  </done>
</task>

</tasks>

<verification>
1. Line count is 550-850 (target 650-750)
2. All 20 guided GSD commands present
3. All required sections exist in correct order (including DevOps-specific)
4. DevOps-specific skills present (blast-radius, devops-quality-gates, incident-response, devops-skill-router)
5. Error recovery instructions present for each per-phase step
6. Enforcement rules section matches original verbatim (including YAML non-exemption)
7. Transition section present with active_workflow logic pointing to full-dev-cycle
8. Environment promotion section fully explained
9. Incident fast path section fully explained
10. No admin commands referenced
</verification>

<success_criteria>
A DevOps engineer who has never heard of GSD can read devops-cycle.md top-to-bottom and:
1. Understand what every step does for infrastructure work
2. Know what to expect at each step
3. Know how to handle production incidents via the fast path
4. Know how to recover when something fails
5. Know when to use utility commands
6. Understand environment promotion from dev through production
7. Understand the transition back to development workflow
All enforcement rules from the original file are preserved unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-file-rewrites/01-02-SUMMARY.md`
</output>
