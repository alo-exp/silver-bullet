---
phase: 01-workflow-file-rewrites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/workflows/full-dev-cycle.md
autonomous: true
requirements:
  - ORCH-01
  - ORCH-03
  - ORCH-04
  - ORCH-05
  - ORCH-06
  - TRANS-01
  - TRANS-03

must_haves:
  truths:
    - "A GSD-naive user reading full-dev-cycle.md understands what each GSD step does without consulting GSD documentation"
    - "Every step in the per-phase loop has a what-it-does summary, what-to-expect section, and what-to-do-if-it-fails recovery"
    - "All 20 guided GSD commands appear at their natural workflow points with context"
    - "Utility commands have a dedicated reference section with when-to-use guidance"
    - "Non-GSD skills are inserted at specific trigger conditions (UI work, architectural decisions, etc.)"
    - "After RELEASE, the workflow detects infrastructure needs and offers dev-to-DevOps transition"
    - "All existing enforcement rules carry forward unchanged"
  artifacts:
    - path: "docs/workflows/full-dev-cycle.md"
      provides: "Complete orchestration guide for full development cycle"
      contains: "## How This Works"
      min_lines: 550
  key_links:
    - from: "docs/workflows/full-dev-cycle.md"
      to: "silver-bullet.md"
      via: "enforcement rules reference"
      pattern: "NON-NEGOTIABLE"
    - from: "docs/workflows/full-dev-cycle.md"
      to: ".silver-bullet.json"
      via: "active_workflow transition"
      pattern: "active_workflow"
---

<objective>
Rewrite docs/workflows/full-dev-cycle.md from a 356-line enforcement checklist into a ~600-700 line comprehensive orchestration guide.

Purpose: Users who have never heard of GSD should be able to follow the full development cycle with complete understanding of every step -- what it does, what to expect, and what to do when things go wrong. This is the primary user-facing workflow document.

Output: Rewritten docs/workflows/full-dev-cycle.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workflow-file-rewrites/01-CONTEXT.md

<!-- Current file being rewritten (MUST read first to understand current structure) -->
@docs/workflows/full-dev-cycle.md

<!-- GSD workflow references — read each to understand what the step actually does.
     The executor MUST read these to write accurate what/expect/fail sections. -->
@~/.claude/get-shit-done/workflows/new-project.md
@~/.claude/get-shit-done/workflows/new-milestone.md
@~/.claude/get-shit-done/workflows/map-codebase.md
@~/.claude/get-shit-done/workflows/discuss-phase.md
@~/.claude/get-shit-done/workflows/plan-phase.md
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/workflows/verify-work.md
@~/.claude/get-shit-done/workflows/ship.md
@~/.claude/get-shit-done/workflows/autonomous.md
@~/.claude/get-shit-done/workflows/diagnose-issues.md
@~/.claude/get-shit-done/workflows/quick.md
@~/.claude/get-shit-done/workflows/fast.md
@~/.claude/get-shit-done/workflows/next.md
@~/.claude/get-shit-done/workflows/pause-work.md
@~/.claude/get-shit-done/workflows/resume-project.md
@~/.claude/get-shit-done/workflows/progress.md
@~/.claude/get-shit-done/workflows/add-phase.md
@~/.claude/get-shit-done/workflows/insert-phase.md
@~/.claude/get-shit-done/workflows/review.md
@~/.claude/get-shit-done/workflows/audit-milestone.md
@~/.claude/get-shit-done/workflows/complete-milestone.md

<!-- Enforcement rules from silver-bullet.md that must carry forward -->
@silver-bullet.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite full-dev-cycle.md as orchestration guide</name>
  <files>docs/workflows/full-dev-cycle.md</files>
  <action>
Read ALL context files listed above (current workflow + every GSD workflow file + silver-bullet.md). Then rewrite docs/workflows/full-dev-cycle.md following this exact structure per D-01:

**File structure (sections in order):**

1. **Header + enforcement banner** — Keep the existing ENFORCED banner (lines 1-9 of current file). Update if wording needs to reflect orchestration guide language, but preserve the enforcement semantics.

2. **Invocation Methods** — Keep current table (lines 12-17).

3. **## How This Works** (NEW) — 10-15 line overview explaining: SB orchestrates the full SDLC by guiding you through GSD steps. Each section below tells you what happens, what to expect, and what to do if something goes wrong. No GSD knowledge required.

4. **## Session Mode** (STEP 0) — Carry forward current Step 0 logic per D-13. Add brief "what this does" intro: "Configures whether Claude pauses at decision points or drives autonomously."

5. **## Project Setup** — Rewrite current PROJECT INITIALIZATION with brownfield detection per D-12:
   - If `.planning/PROJECT.md` exists AND has a completed milestone -> `/gsd:new-milestone`
   - If `.planning/PROJECT.md` exists but no milestone -> resume with `/gsd:next`
   - If existing codebase but no `.planning/` -> `/gsd:map-codebase` THEN `/gsd:new-project`
   - If no codebase -> `/gsd:new-project`
   Each path gets a 1-2 sentence explanation of what happens.

6. **## Per-Phase Loop** — This is the core. For EACH step below, write three sub-sections:
   - **What it does:** One sentence summary (derived from reading the actual GSD workflow file)
   - **What to expect:** What the user will see, what artifacts are produced, how long it typically takes
   - **If it fails:** Specific recovery action per D-08

   Steps within the per-phase loop (preserve current ordering):
   - **### Model Routing** — Keep current logic. Add what/expect.
   - **### Skill Discovery** — Keep current logic. Add what/expect.
   - **### Discuss** (`/gsd:discuss-phase`) — Per D-04, explain that this captures implementation decisions. Per D-06, insert conditional sub-steps: `/design-system` + `/ux-copy` + `/accessibility-review` IF UI work; `/system-design` IF new service. Per D-08, if discuss fails: re-run with more specific questions. Read ~/.claude/get-shit-done/workflows/discuss-phase.md to describe what actually happens (questions asked, CONTEXT.md produced, etc.).
   - **### Quality Gates** (`/quality-gates`) — Per D-06, ALWAYS (hard stop, 8 dimensions). Per D-08, if gates fail: fix specific dimension, re-run.
   - **### Plan** (`/gsd:plan-phase`) — Per D-04, explain research -> plan -> verify. Per D-08, if plan fails: check context, re-discuss if needed. Read plan-phase.md to describe what happens (research, task breakdown, wave assignment, PLAN.md produced).
   - **### Execute** (`/gsd:execute-phase`) — Per D-04, wave-based parallel execution. Per D-06, `/test-driven-development` ALWAYS before implementation. Per D-08, if execute fails: use `/gsd:debug`. Read execute-phase.md to describe (wave dispatch, atomic commits, SUMMARY.md produced).
   - **### Verify** (`/gsd:verify-work`) — Per D-04, goal-backward verification. Per D-08, if verify fails: invoke `/forensics` FIRST, then re-execute or re-plan. Read verify-work.md to describe (VERIFICATION.md, UAT.md produced).
   - **### Code Review** (`/code-review`, `superpowers:code-reviewer`, `/requesting-code-review`, `/receiving-code-review`) — Per D-06, ALWAYS. Keep review loop enforcement (2 consecutive approvals). Per D-08, if review finds issues: post-review plan + execute cycle.
   - **### Post-Review Execution** — Keep current logic (plan + execute for accepted items).

7. **## Finalization** — For each skill (`/testing-strategy`, `/tech-debt`, `/documentation`, `/finishing-a-development-branch`), add what/expect/fail per D-02. Per D-06, these are ALWAYS required. Carry forward the detailed documentation requirements (KNOWLEDGE.md, CHANGELOG.md, session log).

8. **## Deployment** — CI/CD pipeline + `/deploy-checklist`. Add what/expect/fail. Per D-06, `/deploy-checklist` ALWAYS. Keep autonomous mode polling behavior and CI verification gate.

9. **## Ship** — `/gsd:ship`. Add what/expect/fail. Read ship.md to describe (PR creation, phase summaries).

10. **## Release** — `/create-release`. Add what/expect/fail. Per D-06, ALWAYS. Keep autonomous completion cleanup.

11. **## Transition to DevOps** (NEW per D-09) — After RELEASE:
    - Detection triggers: presence of IaC files (*.tf, Dockerfile, k8s manifests), `/deploy-checklist` flagged infra gaps, or user request
    - Offer: "Application shipped. Set up deployment infrastructure? This switches to the DevOps workflow."
    - If yes: update `active_workflow` in `.silver-bullet.json` to `devops-cycle`
    - Per D-11: preserve `.planning/` artifacts, `.silver-bullet.json` config, state file, all committed history
    - Per TRANS-03: explicitly state what is preserved during transition

12. **## Utility Commands** (NEW per D-04/D-05/ORCH-05) — Reference section with when-to-use guidance for each utility:
    - `/gsd:debug` — When execution fails or unexpected behavior occurs
    - `/gsd:quick` — For small, well-defined changes that don't need full cycle
    - `/gsd:fast` — For rapid prototyping within a phase
    - `/gsd:resume-work` (maps to `/gsd:resume-project`) — After session breaks, picks up where you left off
    - `/gsd:pause-work` — Before ending a session, saves state for clean resume
    - `/gsd:progress` — Check milestone progress at any time
    - `/gsd:next` — Auto-advance to next GSD step if unsure
    - `/gsd:add-phase` — Add a phase to the roadmap after initial planning
    - `/gsd:insert-phase` — Insert a phase between existing phases (urgent work)
    - `/gsd:review` — Request an ad-hoc review at any point
    - `/gsd:audit-milestone` — End-of-milestone audit of all work
    Each entry: one sentence what, one sentence when-to-use.

13. **## Enforcement Rules** — Carry forward VERBATIM from current file (lines 315-337 of current file, the "Review Loop Enforcement" and "Enforcement Rules" sections) per D-03. Also carry forward the "GSD / Superpowers Ownership Rules" section verbatim.

**Per D-14 (autonomous mode):** Throughout the guide, document autonomous mode behaviors inline where they differ from interactive. Use the pattern: "**Autonomous mode:** [behavior]" (e.g., "Autonomous mode: stay Sonnet, use defaults, poll CI every 30s").

**What NOT to include per D-05:** Admin commands (gsd-manager, gsd-settings, gsd-stats, gsd-note, gsd-add-todo, gsd-join-discord) are NOT referenced anywhere in the file.

**Quality bar:** A user who has never seen GSD should be able to read this file top-to-bottom and understand the entire development workflow. Test by asking: "Would I know what to do next at every point?" If not, add more context.

**Target length:** 600-700 lines. If significantly shorter, add more user-facing explanations. If significantly longer, tighten without losing clarity.
  </action>
  <verify>
    <automated>
# Verify file exists and is in range
LINES=$(wc -l < docs/workflows/full-dev-cycle.md)
echo "Line count: $LINES"
test "$LINES" -ge 500 && test "$LINES" -le 800 && echo "PASS: line count in range" || echo "FAIL: line count out of range"

# Verify all 20 guided GSD commands appear
for cmd in "gsd:new-project" "gsd:new-milestone" "gsd:map-codebase" "gsd:discuss-phase" "gsd:plan-phase" "gsd:execute-phase" "gsd:verify-work" "gsd:ship" "gsd:complete-milestone" "gsd:autonomous" "gsd:debug" "gsd:quick" "gsd:fast" "gsd:resume" "gsd:pause" "gsd:progress" "gsd:next" "gsd:add-phase" "gsd:insert-phase" "gsd:review" "gsd:audit-milestone"; do
  grep -q "$cmd" docs/workflows/full-dev-cycle.md && echo "PASS: $cmd found" || echo "FAIL: $cmd missing"
done

# Verify key sections exist
for section in "How This Works" "Session Mode" "Project Setup" "Per-Phase Loop" "Finalization" "Deployment" "Ship" "Release" "Transition to DevOps" "Utility Commands" "Enforcement Rules"; do
  grep -qi "$section" docs/workflows/full-dev-cycle.md && echo "PASS: section '$section' found" || echo "FAIL: section '$section' missing"
done

# Verify non-GSD skills appear
for skill in "quality-gates" "design-system" "ux-copy" "accessibility-review" "system-design" "test-driven-development" "forensics" "code-review" "requesting-code-review" "receiving-code-review" "testing-strategy" "tech-debt" "documentation" "finishing-a-development-branch" "deploy-checklist" "create-release"; do
  grep -q "$skill" docs/workflows/full-dev-cycle.md && echo "PASS: skill '$skill' found" || echo "FAIL: skill '$skill' missing"
done

# Verify error recovery pattern appears
grep -c "fails\|If it fails\|What to do" docs/workflows/full-dev-cycle.md | xargs -I{} echo "Error recovery mentions: {}"

# Verify enforcement rules carried forward
grep -q "TWICE IN A ROW" docs/workflows/full-dev-cycle.md && echo "PASS: review loop enforcement present" || echo "FAIL: review loop enforcement missing"
grep -q "NON-NEGOTIABLE" docs/workflows/full-dev-cycle.md || grep -q "GSD steps MUST follow" docs/workflows/full-dev-cycle.md && echo "PASS: enforcement rules present" || echo "FAIL: enforcement rules missing"

# Verify transition section
grep -q "active_workflow" docs/workflows/full-dev-cycle.md && echo "PASS: transition logic present" || echo "FAIL: transition logic missing"

# Verify brownfield detection
grep -q "PROJECT.md.*exists" docs/workflows/full-dev-cycle.md && echo "PASS: brownfield detection present" || echo "FAIL: brownfield detection missing"

# Verify admin commands are NOT present (D-05)
for cmd in "gsd-manager" "gsd-settings" "gsd-stats" "gsd-note" "gsd-add-todo" "gsd-join-discord"; do
  grep -q "$cmd" docs/workflows/full-dev-cycle.md && echo "FAIL: admin command '$cmd' should NOT appear" || echo "PASS: admin command '$cmd' correctly absent"
done
    </automated>
  </verify>
  <done>
docs/workflows/full-dev-cycle.md is rewritten as a comprehensive orchestration guide with:
- All 20 guided GSD commands at natural workflow points
- What/expect/fail sections for every per-phase step
- Non-GSD skills inserted with trigger conditions
- Brownfield detection in Project Setup
- Dev-to-DevOps transition section after Release
- Utility Commands reference section
- All enforcement rules carried forward verbatim
- 600-700 lines total
- A GSD-naive user can follow the entire workflow without external docs
  </done>
</task>

</tasks>

<verification>
1. Line count is 500-800 (target 600-700)
2. All 20 guided GSD commands present
3. All required sections exist in correct order
4. Non-GSD skills present at trigger points
5. Error recovery instructions present for each per-phase step
6. Enforcement rules section matches original verbatim
7. Transition section present with active_workflow logic
8. Brownfield detection covers all 4 paths
9. No admin commands referenced
10. Autonomous mode documented inline throughout
</verification>

<success_criteria>
A user who has never heard of GSD can read full-dev-cycle.md top-to-bottom and:
1. Understand what every step does
2. Know what to expect at each step
3. Know how to recover when something fails
4. Know when to use utility commands
5. Understand the transition to DevOps workflow
All enforcement rules from the original file are preserved unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-file-rewrites/01-01-SUMMARY.md`
</output>
