---
phase: 01-workflow-file-rewrites
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - templates/workflows/full-dev-cycle.md
  - templates/workflows/devops-cycle.md
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02

must_haves:
  truths:
    - "templates/workflows/full-dev-cycle.md is byte-identical to docs/workflows/full-dev-cycle.md"
    - "templates/workflows/devops-cycle.md is byte-identical to docs/workflows/devops-cycle.md"
  artifacts:
    - path: "templates/workflows/full-dev-cycle.md"
      provides: "Template copy of rewritten dev cycle workflow"
    - path: "templates/workflows/devops-cycle.md"
      provides: "Template copy of rewritten DevOps cycle workflow"
  key_links:
    - from: "templates/workflows/full-dev-cycle.md"
      to: "docs/workflows/full-dev-cycle.md"
      via: "byte-identical copy"
      pattern: "identical"
    - from: "templates/workflows/devops-cycle.md"
      to: "docs/workflows/devops-cycle.md"
      via: "byte-identical copy"
      pattern: "identical"
---

<objective>
Copy the rewritten workflow files from docs/workflows/ to templates/workflows/ and verify byte-identical parity.

Purpose: The templates directory serves as the canonical source for new project initialization. Template files must exactly match the active docs so that `sb init` installs the same workflow files. This is a precursor to the full template parity verification in Phase 4 (TMPL-01, TMPL-02).

Output: Updated templates/workflows/full-dev-cycle.md and templates/workflows/devops-cycle.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-workflow-file-rewrites/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy workflow files to templates and verify parity</name>
  <files>templates/workflows/full-dev-cycle.md, templates/workflows/devops-cycle.md</files>
  <action>
1. Copy docs/workflows/full-dev-cycle.md to templates/workflows/full-dev-cycle.md (overwrite existing).
2. Copy docs/workflows/devops-cycle.md to templates/workflows/devops-cycle.md (overwrite existing).
3. Run diff on both pairs to confirm byte-identical.

Use `cp` for the copy to preserve exact content. Do NOT use Read+Write as that can introduce encoding differences.

```bash
cp docs/workflows/full-dev-cycle.md templates/workflows/full-dev-cycle.md
cp docs/workflows/devops-cycle.md templates/workflows/devops-cycle.md
diff docs/workflows/full-dev-cycle.md templates/workflows/full-dev-cycle.md
diff docs/workflows/devops-cycle.md templates/workflows/devops-cycle.md
```

Both diff commands must produce no output (exit code 0).
  </action>
  <verify>
    <automated>
# Verify byte-identical parity
diff docs/workflows/full-dev-cycle.md templates/workflows/full-dev-cycle.md > /dev/null 2>&1 && echo "PASS: full-dev-cycle.md parity" || echo "FAIL: full-dev-cycle.md differs"
diff docs/workflows/devops-cycle.md templates/workflows/devops-cycle.md > /dev/null 2>&1 && echo "PASS: devops-cycle.md parity" || echo "FAIL: devops-cycle.md differs"

# Verify files exist and are non-empty
test -s templates/workflows/full-dev-cycle.md && echo "PASS: full-dev-cycle.md exists and non-empty" || echo "FAIL: full-dev-cycle.md missing or empty"
test -s templates/workflows/devops-cycle.md && echo "PASS: devops-cycle.md exists and non-empty" || echo "FAIL: devops-cycle.md missing or empty"

# Verify line counts match
DOCS_DEV=$(wc -l < docs/workflows/full-dev-cycle.md)
TMPL_DEV=$(wc -l < templates/workflows/full-dev-cycle.md)
test "$DOCS_DEV" -eq "$TMPL_DEV" && echo "PASS: full-dev-cycle line count matches ($DOCS_DEV)" || echo "FAIL: line count mismatch ($DOCS_DEV vs $TMPL_DEV)"

DOCS_OPS=$(wc -l < docs/workflows/devops-cycle.md)
TMPL_OPS=$(wc -l < templates/workflows/devops-cycle.md)
test "$DOCS_OPS" -eq "$TMPL_OPS" && echo "PASS: devops-cycle line count matches ($DOCS_OPS)" || echo "FAIL: line count mismatch ($DOCS_OPS vs $TMPL_OPS)"
    </automated>
  </verify>
  <done>
templates/workflows/full-dev-cycle.md and templates/workflows/devops-cycle.md are byte-identical to their docs/workflows/ counterparts. Both diff commands produce no output.
  </done>
</task>

</tasks>

<verification>
1. `diff docs/workflows/full-dev-cycle.md templates/workflows/full-dev-cycle.md` produces no output
2. `diff docs/workflows/devops-cycle.md templates/workflows/devops-cycle.md` produces no output
3. Both template files exist and are non-empty
</verification>

<success_criteria>
Template parity is achieved: both workflow template files are byte-identical copies of the rewritten docs versions. This ensures `sb init` will install the updated orchestration guides for new projects.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-file-rewrites/01-03-SUMMARY.md`
</output>
