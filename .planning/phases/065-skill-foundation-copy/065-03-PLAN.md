---
phase: 065-skill-foundation-copy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - forge/skills/brainstorming/**
  - forge/skills/dispatching-parallel-agents/**
  - forge/skills/executing-plans/**
  - forge/skills/finishing-a-development-branch/**
  - forge/skills/receiving-code-review/**
  - forge/skills/requesting-code-review/**
  - forge/skills/subagent-driven-development/**
  - forge/skills/systematic-debugging/**
  - forge/skills/test-driven-development/**
  - forge/skills/using-git-worktrees/**
  - forge/skills/using-superpowers/**
  - forge/skills/verification-before-completion/**
  - forge/skills/writing-plans/**
  - forge/skills/writing-skills/**
autonomous: true
requirements:
  - COPY-SP-01
must_haves:
  truths:
    - "All 14 Superpowers skills exist at forge/skills/<name>/SKILL.md with correct name/description frontmatter"
    - "Each Superpowers skill is byte-identical to the source under ~/.claude/plugins/cache/superpowers-marketplace/superpowers/<v>/skills/<name>/SKILL.md"
    - "No name collision between SB skills (Plan 01) and Superpowers skills"
  artifacts:
    - path: "forge/skills/brainstorming/SKILL.md"
      provides: "Superpowers brainstorming skill"
      contains: "name: brainstorming"
    - path: "forge/skills/test-driven-development/SKILL.md"
      provides: "Superpowers TDD skill"
      contains: "name: test-driven-development"
  key_links:
    - from: "~/.claude/plugins/cache/superpowers-marketplace/superpowers/<v>/skills/<name>/SKILL.md"
      to: "forge/skills/<name>/SKILL.md"
      via: "cp"
      pattern: "name:"
---

<objective>
Copy all 14 Superpowers skills from the locally-cached Superpowers plugin into `forge/skills/`. Names do not collide with SB skills (verified by Plan 01's listing — none of the 14 names appear among the 61 SB names except `brainstorming` and `writing-plans`, which DO collide; the Superpowers version is canonical and overwrites the SB ones).

**Important — name collisions resolved by overwrite:** The SB skill set includes its own `brainstorming` and `writing-plans` skills (the "SB orchestrators" that wrap Superpowers behavior). Per the canonical layering, the Superpowers source-of-truth versions of those two skills are the ones we want in `forge/skills/`. This plan overwrites those two SB-copied versions with the Superpowers originals.

Purpose: Bring the 14 reusable Superpowers patterns into Forge's flat skill registry so SB workflow skills can reference them.
Output: 14 directories under `forge/skills/`, each containing the canonical Superpowers SKILL.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locate Superpowers and copy 14 skills</name>
  <read_first>
    - ~/.claude/plugins/cache/superpowers-marketplace/superpowers/ (listing only)
  </read_first>
  <files>forge/skills/{brainstorming,dispatching-parallel-agents,executing-plans,finishing-a-development-branch,receiving-code-review,requesting-code-review,subagent-driven-development,systematic-debugging,test-driven-development,using-git-worktrees,using-superpowers,verification-before-completion,writing-plans,writing-skills}/SKILL.md</files>
  <action>
    Find the latest cached Superpowers version and copy all 14 listed skills.

    Steps:
    1. Find the latest version directory:
       ```bash
       SP_BASE=~/.claude/plugins/cache/superpowers-marketplace/superpowers
       SP_VERSION=$(ls -1 "$SP_BASE" | sort -V | tail -1)
       SP_SKILLS="$SP_BASE/$SP_VERSION/skills"
       [ -d "$SP_SKILLS" ] || { echo "FAIL: Superpowers skills directory not found at $SP_SKILLS"; exit 1; }
       echo "Using Superpowers $SP_VERSION at $SP_SKILLS"
       ```
    2. Define the canonical 14-skill list and copy each one. Note that `brainstorming` and `writing-plans` will OVERWRITE the SB-copied versions from Plan 01 — this is intentional (Superpowers is canonical for these two):
       ```bash
       SKILLS=(brainstorming dispatching-parallel-agents executing-plans finishing-a-development-branch receiving-code-review requesting-code-review subagent-driven-development systematic-debugging test-driven-development using-git-worktrees using-superpowers verification-before-completion writing-plans writing-skills)

       for s in "${SKILLS[@]}"; do
         SRC="$SP_SKILLS/$s/SKILL.md"
         DEST="forge/skills/$s/SKILL.md"
         [ -f "$SRC" ] || { echo "FAIL: source missing: $SRC"; exit 1; }
         mkdir -p "forge/skills/$s"
         cp "$SRC" "$DEST"
       done
       ```
    3. Verify all 14 destinations exist and have correct frontmatter:
       ```bash
       for s in "${SKILLS[@]}"; do
         DEST="forge/skills/$s/SKILL.md"
         [ -f "$DEST" ] || { echo "FAIL: $DEST missing"; exit 1; }
         grep -q "^name: $s" "$DEST" || { echo "FAIL: $DEST has wrong frontmatter name"; exit 1; }
       done
       echo "OK: 14 Superpowers skills copied"
       ```

    Do NOT modify SKILL.md content — Forge's SKILL.md format is identical to Claude Code per ForgeCode docs.
  </action>
  <acceptance_criteria>
    - `test -f forge/skills/brainstorming/SKILL.md`
    - `test -f forge/skills/dispatching-parallel-agents/SKILL.md`
    - `test -f forge/skills/executing-plans/SKILL.md`
    - `test -f forge/skills/finishing-a-development-branch/SKILL.md`
    - `test -f forge/skills/receiving-code-review/SKILL.md`
    - `test -f forge/skills/requesting-code-review/SKILL.md`
    - `test -f forge/skills/subagent-driven-development/SKILL.md`
    - `test -f forge/skills/systematic-debugging/SKILL.md`
    - `test -f forge/skills/test-driven-development/SKILL.md`
    - `test -f forge/skills/using-git-worktrees/SKILL.md`
    - `test -f forge/skills/using-superpowers/SKILL.md`
    - `test -f forge/skills/verification-before-completion/SKILL.md`
    - `test -f forge/skills/writing-plans/SKILL.md`
    - `test -f forge/skills/writing-skills/SKILL.md`
    - `grep -q "^name: test-driven-development" forge/skills/test-driven-development/SKILL.md`
    - `grep -q "^name: brainstorming" forge/skills/brainstorming/SKILL.md`
    - 14 named skill SKILL.md files exist (count matches list)
  </acceptance_criteria>
  <verify>
    <automated>SKILLS=(brainstorming dispatching-parallel-agents executing-plans finishing-a-development-branch receiving-code-review requesting-code-review subagent-driven-development systematic-debugging test-driven-development using-git-worktrees using-superpowers verification-before-completion writing-plans writing-skills); MISSING=0; for s in "${SKILLS[@]}"; do [ -f "forge/skills/$s/SKILL.md" ] || MISSING=$((MISSING+1)); done; [ "$MISSING" = "0" ]</automated>
  </verify>
  <done>
    All 14 Superpowers skills exist under `forge/skills/<name>/SKILL.md` with correct frontmatter, byte-identical to the cached source.
  </done>
</task>

</tasks>

<verification>
  - 14 Superpowers SKILL.md files added (with overwrites for brainstorming + writing-plans)
  - Each frontmatter validates (name field matches directory)
</verification>

<success_criteria>
  - All 14 named skills exist in forge/skills/
  - Total forge/skills/ count after Plan 01 + Plan 03 = 61 + 12 (since brainstorming, writing-plans overwrite) = 73
</success_criteria>

<output>
After completion, create `.planning/phases/065-skill-foundation-copy/065-03-SUMMARY.md`.
</output>
