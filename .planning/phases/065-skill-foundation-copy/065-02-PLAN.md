---
phase: 065-skill-foundation-copy
plan: 02
type: execute
wave: 2
depends_on:
  - "065-01"
files_modified:
  - forge/skills/silver-update/SKILL.md
  - forge/skills/silver-init/SKILL.md
  - forge/skills/silver-migrate/SKILL.md
autonomous: true
requirements:
  - COPY-SB-02
must_haves:
  truths:
    - "silver-update no longer references `claude mcp install` or `installed_plugins.json`"
    - "silver-init no longer drives Claude Code plugin install flows; it instructs running forge-sb-install.sh"
    - "silver-migrate paths point to ~/forge/skills/ and .forge/skills/ rather than ~/.claude/plugins/cache/"
  artifacts:
    - path: "forge/skills/silver-update/SKILL.md"
      provides: "Forge-adapted update skill"
      contains: "forge-sb-install.sh"
    - path: "forge/skills/silver-init/SKILL.md"
      provides: "Forge-adapted init skill"
      contains: "forge-sb-install.sh"
    - path: "forge/skills/silver-migrate/SKILL.md"
      provides: "Forge-adapted migrate skill"
      contains: ".forge/skills"
  key_links:
    - from: "silver-update step 1 (read installed version)"
      to: "Forge install script"
      via: "instruct user to re-run forge-sb-install.sh"
      pattern: "forge-sb-install"
---

<objective>
Adapt the body of the 3 SB skills that contain Claude Code-specific runtime references (`claude mcp install`, `~/.claude/plugins/`, plugin registry JSON) so they work under Forge's runtime. Frontmatter is left unchanged — only body content is edited.

Purpose: These three skills are the only ones with explicit Claude Code-only operational details. All other skills with Skill tool calls or hook references are deferred to Phases 66/67.
Output: Three adapted SKILL.md files referencing `forge-sb-install.sh` and Forge paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@forge-sb-install.sh
@forge/skills/silver-update/SKILL.md
@forge/skills/silver-init/SKILL.md
@forge/skills/silver-migrate/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt silver-update for Forge install flow</name>
  <read_first>
    - forge/skills/silver-update/SKILL.md (the just-copied SB version)
    - forge-sb-install.sh (canonical Forge installer reference)
  </read_first>
  <files>forge/skills/silver-update/SKILL.md</files>
  <action>
    Edit `forge/skills/silver-update/SKILL.md`. Replace Claude Code plugin-registry logic with Forge install logic.

    Required replacements:
    1. Remove the "Read `~/.claude/plugins/installed_plugins.json`" block. Replace with: read the installed version from `~/forge/skills/silver/SKILL.md` frontmatter `version:` field if present, else treat as `0.0.0`. Use:
       ```bash
       grep -E "^version:" ~/forge/skills/silver/SKILL.md 2>/dev/null | head -1 | sed 's/^version:[[:space:]]*//' || echo "0.0.0"
       ```
    2. The "Check latest version from GitHub" curl step stays unchanged (still queries `alo-exp/silver-bullet` releases).
    3. Replace any Claude Desktop / `claude mcp install` / "reinstall via Claude Desktop plugin manager" instructions with:
       ```
       To install or update, run the Forge install script:

           bash forge-sb-install.sh

       Or via curl one-liner (if available):

           curl -fsSL https://raw.githubusercontent.com/alo-exp/silver-bullet/main/forge-sb-install.sh | bash
       ```
    4. Remove all references to `installed_plugins.json` and `silver-bullet@alo-labs` / `silver-bullet@silver-bullet` registry keys.

    Keep the overall step-by-step structure (read installed → check latest → show changelog → install). Frontmatter is unchanged.
  </action>
  <acceptance_criteria>
    - `! grep -q "installed_plugins.json" forge/skills/silver-update/SKILL.md`
    - `! grep -q "claude mcp install" forge/skills/silver-update/SKILL.md`
    - `! grep -q "Claude Desktop plugin manager" forge/skills/silver-update/SKILL.md`
    - `grep -q "forge-sb-install.sh" forge/skills/silver-update/SKILL.md`
    - `grep -q "^name: silver-update" forge/skills/silver-update/SKILL.md` (frontmatter intact)
  </acceptance_criteria>
  <verify>
    <automated>! grep -qE "installed_plugins.json|claude mcp install" forge/skills/silver-update/SKILL.md && grep -q "forge-sb-install.sh" forge/skills/silver-update/SKILL.md</automated>
  </verify>
  <done>
    silver-update points users at `forge-sb-install.sh` for installation and reads version from `~/forge/skills/silver/SKILL.md` rather than the Claude Code plugin registry.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adapt silver-init for Forge runtime</name>
  <read_first>
    - forge/skills/silver-init/SKILL.md (just-copied SB version)
    - forge-sb-install.sh
  </read_first>
  <files>forge/skills/silver-init/SKILL.md</files>
  <action>
    Edit `forge/skills/silver-init/SKILL.md`. Strip Claude Code plugin-install orchestration and replace with Forge install instructions.

    Required changes:
    1. **Plugin-install flows** — locate the section(s) that drive `/plugin install` for Superpowers, Engineering, Design, GSD, MultAI (or any "install plugin X" instruction targeting Claude Desktop). Replace the entire bundle with:
       ```
       ### Install the Forge skill set

       Silver Bullet for Forge ships all dependencies as flat skills in `~/forge/skills/`. Run:

           bash forge-sb-install.sh

       This installs SB, Superpowers, and Anthropic knowledge-work skills in one pass. No plugin manager is involved.
       ```
    2. **Plugin path references** — change `~/.claude/plugins/cache/...` references that point to skill source locations to `~/forge/skills/` and `.forge/skills/` (per-project) where appropriate.
    3. **Session-init Phase −1** — the README/CONTEXT/CLAUDE.md read step stays. Remove only the parts that reach into Claude Code plugin state.
    4. **Plugin-root determination** — the line "PLUGIN_ROOT from this skill file's own path" stays (the skill still locates itself via its own `SKILL.md` location); just update the destination examples to use `~/forge/skills/silver-init/SKILL.md` if any literal example paths exist.
    5. The skill still scaffolds `silver-bullet.md`, `CLAUDE.md`, `.silver-bullet.json`, `.planning/WORKFLOW.md` — keep that scaffolding logic. Only drop the Claude-plugin-manager hooks.

    Frontmatter unchanged.
  </action>
  <acceptance_criteria>
    - `! grep -qE "/plugin install|Claude Desktop plugin" forge/skills/silver-init/SKILL.md`
    - `grep -q "forge-sb-install.sh" forge/skills/silver-init/SKILL.md`
    - `grep -q "^name: silver-init" forge/skills/silver-init/SKILL.md`
    - `! grep -q "claude mcp install" forge/skills/silver-init/SKILL.md`
  </acceptance_criteria>
  <verify>
    <automated>! grep -qE "/plugin install|claude mcp install|Claude Desktop plugin" forge/skills/silver-init/SKILL.md && grep -q "forge-sb-install.sh" forge/skills/silver-init/SKILL.md</automated>
  </verify>
  <done>
    silver-init no longer drives Claude Code plugin installation; it points users at `forge-sb-install.sh` and otherwise scaffolds the same project files.
  </done>
</task>

<task type="auto">
  <name>Task 3: Adapt silver-migrate for Forge paths</name>
  <read_first>
    - forge/skills/silver-migrate/SKILL.md (just-copied SB version)
  </read_first>
  <files>forge/skills/silver-migrate/SKILL.md</files>
  <action>
    Edit `forge/skills/silver-migrate/SKILL.md`. Replace Claude Code plugin paths with Forge paths.

    Required substitutions:
    1. `~/.claude/plugins/cache/alo-labs/silver-bullet/<version>/` → `~/forge/skills/` (user-global skill location under Forge)
    2. `~/.claude/plugins/cache/...` (any other Claude Code plugin cache reference) → `~/forge/skills/`
    3. `.claude/skills/` (per-project skill directory) → `.forge/skills/` (Forge per-project equivalent)
    4. References to "the installed plugin cache" → "the installed Forge skill set"
    5. Remove any `/plugin install` or `claude mcp` invocations; replace with `bash forge-sb-install.sh` if an install action is needed.

    Keep the migration logic structure (detect old version → back up → migrate → confirm). Frontmatter unchanged.
  </action>
  <acceptance_criteria>
    - `! grep -q "~/.claude/plugins/cache" forge/skills/silver-migrate/SKILL.md`
    - `! grep -q "claude mcp" forge/skills/silver-migrate/SKILL.md`
    - `grep -q ".forge/skills" forge/skills/silver-migrate/SKILL.md`
    - `grep -q "^name: silver-migrate" forge/skills/silver-migrate/SKILL.md`
  </acceptance_criteria>
  <verify>
    <automated>! grep -qE "~/.claude/plugins/cache|claude mcp" forge/skills/silver-migrate/SKILL.md && grep -q ".forge/skills" forge/skills/silver-migrate/SKILL.md</automated>
  </verify>
  <done>
    silver-migrate references only Forge paths (`~/forge/skills/`, `.forge/skills/`) and the Forge installer.
  </done>
</task>

</tasks>

<verification>
  - All 3 skills free of Claude Code plugin-registry references
  - All 3 skills mention `forge-sb-install.sh` for install flow (where install is invoked)
  - Frontmatter intact on all 3
</verification>

<success_criteria>
  - `grep -lE "installed_plugins.json|claude mcp install|/plugin install|~/.claude/plugins/cache" forge/skills/silver-{update,init,migrate}/SKILL.md` returns no matches
  - All three skills load successfully under Forge (frontmatter validates)
</success_criteria>

<output>
After completion, create `.planning/phases/065-skill-foundation-copy/065-02-SUMMARY.md`.
</output>
