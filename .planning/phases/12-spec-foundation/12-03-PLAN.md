---
phase: 12-spec-foundation
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - skills/silver/SKILL.md
  - templates/silver-bullet.md.base
autonomous: false
requirements: [ELIC-01, ELIC-05]
user_setup:
  - service: hooks.json
    why: "Self-protection in dev-cycle-check.sh prevents Claude from writing to hooks.json"
    dashboard_config:
      - task: "Add spec-floor-check.sh entry to hooks.json PreToolUse array"
        location: "hooks/hooks.json — see exact JSON block in Task 3"

must_haves:
  truths:
    - "/silver router routes spec-related intents to silver:spec"
    - "silver-bullet.md.base §2 contains spec lifecycle documentation"
    - "hooks.json registers spec-floor-check.sh under PreToolUse/Bash"
  artifacts:
    - path: "skills/silver/SKILL.md"
      provides: "Updated router with silver-spec routes"
      contains: "silver:spec"
    - path: "templates/silver-bullet.md.base"
      provides: "Spec lifecycle in §2"
      contains: "silver:spec"
    - path: "hooks/hooks.json"
      provides: "spec-floor-check.sh registration"
      contains: "spec-floor-check.sh"
  key_links:
    - from: "skills/silver/SKILL.md"
      to: "skills/silver-spec/SKILL.md"
      via: "routing table entry"
      pattern: "silver:spec"
    - from: "hooks/hooks.json"
      to: "hooks/spec-floor-check.sh"
      via: "PreToolUse registration"
      pattern: "spec-floor-check"
---

<objective>
Wire silver-spec into the /silver router, add spec lifecycle to silver-bullet.md.base §2, and register spec-floor-check.sh in hooks.json.

Purpose: Without wiring, the new skill and hook exist but are not discoverable or active.
Output: Updated router, updated §2 docs, registered hook
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-spec-foundation/12-RESEARCH.md
@.planning/phases/12-spec-foundation/12-01-SUMMARY.md
@.planning/phases/12-spec-foundation/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add silver-spec routing to /silver router</name>
  <files>skills/silver/SKILL.md</files>
  <read_first>
    - skills/silver/SKILL.md (full file -- understand current routing table format and where to insert)
    - skills/silver-spec/SKILL.md (confirm skill name and description for routing entry)
  </read_first>
  <action>
Edit `skills/silver/SKILL.md` to add silver-spec routing entries.

In the **full routing table** (the table with "User intent signals | Route to | Notes"), add this row AFTER the "silver:explore" row and BEFORE the "silver:feature" row:

```
| "spec", "requirements", "elicit", "write a spec", "create spec", "define requirements", "what should we build" | `silver:spec` | AI-guided spec elicitation |
```

In the **complexity triage table**, no changes needed -- spec elicitation is not a complexity classification.

In the **multi-signal conflict resolution table**, add:

```
| `silver:spec` + `silver:feature` | `silver:spec` first | Spec before implementation |
```
  </action>
  <verify>
    <automated>grep -c "silver:spec" skills/silver/SKILL.md && grep "spec.*elicit" skills/silver/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `skills/silver/SKILL.md` contains routing entry for `silver:spec` with intent signals including "spec", "requirements", "elicit"
    - Conflict resolution table contains `silver:spec` + `silver:feature` entry with `silver:spec first`
    - No existing routing entries are removed or modified
  </acceptance_criteria>
  <done>silver router routes spec-related intents to silver:spec</done>
</task>

<task type="auto">
  <name>Task 2: Add spec lifecycle to silver-bullet.md.base §2</name>
  <files>templates/silver-bullet.md.base</files>
  <read_first>
    - templates/silver-bullet.md.base (full file -- find §2 section, understand current structure and where to add spec lifecycle)
  </read_first>
  <action>
Edit `templates/silver-bullet.md.base` to add a spec lifecycle subsection within §2 (Workflows).

Find the §2 section (look for `## 2.` heading). Add a new subsection at the END of §2 (before §3 begins):

```markdown
### 2i. Spec Lifecycle

**When to invoke:** Before any feature development begins. `silver:spec` is the entry point for creating or refining a spec.

**Artifacts produced:**
- `.planning/SPEC.md` — Canonical spec with YAML frontmatter (spec-version, status, jira-id, figma-url, source-artifacts) and sections (Overview, User Stories, UX Flows, Acceptance Criteria, Assumptions, Open Questions, Out of Scope, Implementations)
- `.planning/REQUIREMENTS.md` — Derived requirements with REQ-XX and NFR-XX IDs
- `.planning/DESIGN.md` — Design artifact (when Figma or design context provided)

**Spec floor enforcement:**
- `gsd-plan-phase` is hard-blocked by `spec-floor-check.sh` if `.planning/SPEC.md` is missing or lacks `## Overview` and `## Acceptance Criteria` sections
- `gsd-fast` and `gsd-quick` emit a warning but are not blocked — fast path preserves bypass intent
- Run `/silver:spec` to create or update the spec before planning

**Modes:**
- Greenfield: no existing SPEC.md — full Socratic elicitation from scratch
- Augment: existing SPEC.md — refine, extend, increment spec-version

**Assumption tracking:** Every unresolvable ambiguity produces an `[ASSUMPTION: ...]` block in SPEC.md. Assumption density is a quality signal — zero assumptions on a non-trivial feature is suspicious.
```

Determine the correct subsection letter by checking what the last §2 subsection is (e.g., if the last is §2h, use §2i). Adjust the heading letter accordingly.
  </action>
  <verify>
    <automated>grep -c "Spec Lifecycle" templates/silver-bullet.md.base && grep -c "spec-floor-check" templates/silver-bullet.md.base && grep -c "silver:spec" templates/silver-bullet.md.base && grep -c "ASSUMPTION:" templates/silver-bullet.md.base</automated>
  </verify>
  <acceptance_criteria>
    - `templates/silver-bullet.md.base` contains "Spec Lifecycle" subsection within §2
    - Subsection documents: artifacts produced (.planning/SPEC.md, REQUIREMENTS.md, DESIGN.md)
    - Subsection documents: spec floor enforcement (hard block on plan-phase, warning on fast)
    - Subsection documents: greenfield vs augment modes
    - Subsection documents: assumption tracking
    - Subsection references `silver:spec` and `spec-floor-check.sh`
    - No existing §2 subsections are removed or modified
  </acceptance_criteria>
  <done>silver-bullet.md.base §2 documents the spec lifecycle workflow</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Register spec-floor-check.sh in hooks.json</name>
  <what-built>spec-floor-check.sh hook script (from Plan 01) and the router + docs wiring (Tasks 1-2 above)</what-built>
  <action>
The hooks.json file is self-protected by dev-cycle-check.sh -- Claude cannot write to it. The user must add the hook registration manually.

Show the user the exact JSON block to add to `hooks/hooks.json`:

In the `"PreToolUse"` array, add this entry AFTER the existing `ci-status-check.sh` entry (the last PreToolUse entry):

```json
,
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/spec-floor-check.sh\"",
            "async": false
          }
        ]
      }
```

The user should open `hooks/hooks.json` in their editor, find the PreToolUse array, and add this entry at the end of the array (after the ci-status-check.sh block, before the closing `]` of PreToolUse).
  </action>
  <how-to-verify>
    1. Open hooks/hooks.json in your editor
    2. Find the "PreToolUse" array
    3. Add the JSON block shown above after the ci-status-check.sh entry
    4. Save the file
    5. Verify: `cat hooks/hooks.json | jq '.hooks.PreToolUse[-1].hooks[0].command'` should output `"${CLAUDE_PLUGIN_ROOT}/hooks/spec-floor-check.sh"`
    6. Verify JSON is valid: `cat hooks/hooks.json | jq . > /dev/null && echo "Valid JSON"`
  </how-to-verify>
  <resume-signal>Type "done" after adding the hooks.json entry</resume-signal>
  <acceptance_criteria>
    - `hooks/hooks.json` contains `spec-floor-check.sh` in PreToolUse array
    - `cat hooks/hooks.json | jq '.hooks.PreToolUse[] | select(.hooks[].command | contains("spec-floor-check"))' ` returns non-empty output
    - `cat hooks/hooks.json | jq . > /dev/null` exits 0 (valid JSON)
  </acceptance_criteria>
  <done>spec-floor-check.sh is registered in hooks.json and will fire on PreToolUse/Bash events</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /silver router -> skill invocation | Router determines which skill runs |
| hooks.json -> hook execution | Hook registration determines enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering | hooks.json manual edit | mitigate | Task 3 shows exact JSON; user validates with jq before saving |
| T-12-09 | Repudiation | Router misrouting | accept | Router is transparent -- shows routing decision to user before invoking skill |
</threat_model>

<verification>
1. `grep "silver:spec" skills/silver/SKILL.md` returns match
2. `grep "Spec Lifecycle" templates/silver-bullet.md.base` returns match
3. `cat hooks/hooks.json | jq '.hooks.PreToolUse[] | select(.hooks[].command | contains("spec-floor-check"))' | head -1` returns non-empty
</verification>

<success_criteria>
- /silver router includes silver:spec in routing table with correct intent signals
- silver-bullet.md.base §2 documents spec lifecycle
- hooks.json registers spec-floor-check.sh under PreToolUse/Bash
</success_criteria>

<output>
After completion, create `.planning/phases/12-spec-foundation/12-03-SUMMARY.md`
</output>
