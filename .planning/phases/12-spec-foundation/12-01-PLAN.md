---
phase: 12-spec-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/specs/SPEC.md.template
  - templates/specs/DESIGN.md.template
  - templates/specs/REQUIREMENTS.md.template
  - hooks/spec-floor-check.sh
autonomous: true
requirements: [SPEC-01, SPEC-02, SPEC-03, SPEC-05, FLOR-01, FLOR-02, FLOR-03]
must_haves:
  truths:
    - "templates/specs/ directory contains SPEC.md.template, DESIGN.md.template, REQUIREMENTS.md.template"
    - "SPEC.md.template has YAML frontmatter with spec-version, status, jira-id, figma-url, source-artifacts, created, last-updated fields"
    - "SPEC.md.template has all required sections: Overview, User Stories, UX Flows, Acceptance Criteria, Assumptions, Open Questions, Out of Scope, Implementations"
    - "DESIGN.md.template has screen/component/behavior/state definitions"
    - "spec-floor-check.sh hard-blocks gsd-plan-phase when SPEC.md is missing or lacks Overview/Acceptance Criteria"
    - "spec-floor-check.sh emits warning (not block) for gsd-fast/gsd-quick when no spec exists"
    - "spec-floor-check.sh completes in under 10 seconds"
  artifacts:
    - path: "templates/specs/SPEC.md.template"
      provides: "Canonical spec template"
      contains: "spec-version:"
    - path: "templates/specs/DESIGN.md.template"
      provides: "Design artifact template"
      contains: "## Screens"
    - path: "templates/specs/REQUIREMENTS.md.template"
      provides: "Requirements template"
      contains: "## Functional Requirements"
    - path: "hooks/spec-floor-check.sh"
      provides: "Spec floor enforcement hook"
      contains: "emit_block"
  key_links:
    - from: "hooks/spec-floor-check.sh"
      to: ".planning/SPEC.md"
      via: "file existence check + grep for required sections"
      pattern: "grep.*Overview.*Acceptance Criteria"
---

<objective>
Create the three spec template files and the spec-floor-check.sh enforcement hook.

Purpose: These are the foundational artifacts for Phase 12 -- templates define what specs look like, and the hook enforces that specs exist before planning begins.
Output: 3 template files in templates/specs/, 1 hook script in hooks/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-spec-foundation/12-RESEARCH.md

<interfaces>
<!-- Hook boilerplate pattern from existing hooks -->
From hooks/dev-cycle-check.sh (lines 1-44) -- use as exact boilerplate for spec-floor-check.sh:
- shebang: #!/usr/bin/env bash
- set -euo pipefail, trap 'exit 0' ERR, umask 0077
- jq availability check with graceful degradation
- input=$(cat), hook_event extraction via jq
- emit_block() function with PreToolUse permissionDecision:deny output
- cmd extraction via jq, early exit if empty
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spec template files in templates/specs/</name>
  <files>templates/specs/SPEC.md.template, templates/specs/DESIGN.md.template, templates/specs/REQUIREMENTS.md.template</files>
  <read_first>
    - .planning/phases/12-spec-foundation/12-RESEARCH.md (Pattern 5, 6, 7 sections for exact template content)
  </read_first>
  <action>
Create directory `templates/specs/` if it does not exist.

Create `templates/specs/SPEC.md.template` -- the file must contain:

YAML FRONTMATTER (between triple-dash delimiters) with these fields:
  spec-version: 1
  status: Draft
  jira-id: ""
  figma-url: ""
  source-artifacts: []
  created: YYYY-MM-DD
  last-updated: YYYY-MM-DD

Then these sections in order:
  # [Feature Name] -- Spec
  ## Overview -- placeholder: "[2-3 sentence problem statement. Who has the problem. What the problem is.]"
  ## User Stories -- placeholder: "- As a [persona], I want to [action] so that [outcome]."
  ## UX Flows -- placeholder: step-by-step user journey with numbered steps
  ## Acceptance Criteria -- placeholder: "- [ ] [Measurable, testable criterion]" (two items)
  ## Assumptions -- HTML comment explaining purpose, then: "- [ASSUMPTION: {what is assumed} | Status: {Resolved / Follow-up-required} | Owner: {name or TBD}]"
  ## Open Questions -- HTML comment, then: "- [ ] [Question] -- Owner: {name or TBD}"
  ## Out of Scope -- placeholder: "- [Explicit exclusion]"
  ## Implementations -- HTML comment: "Populated automatically by pr-traceability.sh hook post-merge."

Use the EXACT section content shown in 12-RESEARCH.md Pattern 5.

Create `templates/specs/DESIGN.md.template` -- the file must contain:

YAML FRONTMATTER with fields: spec-version: 1, linked-spec: .planning/SPEC.md, figma-url: "", last-updated: YYYY-MM-DD

Then these sections:
  # [Feature Name] -- Design
  ## Screens -- with subsection template: ### [Screen Name] with Purpose/Entry point/Exit points
  ## Components -- with subsection template: ### [Component Name] with Type/State variants/Behavior
  ## Behavior Specifications -- table with Trigger/Condition/System Response columns
  ## State Definitions -- table with State/Description/Visual Indicator columns
  ## Design Tokens (from Figma) -- HTML comment: "Populated by silver-ingest (Phase 13) from Figma MCP. Empty until then."

Use the EXACT section content shown in 12-RESEARCH.md Pattern 6.

Create `templates/specs/REQUIREMENTS.md.template` -- the file must contain:

  # Requirements: [Feature Name]
  **Derived from:** .planning/SPEC.md v{spec-version}
  **Generated:** YYYY-MM-DD
  ## Functional Requirements -- table with ID/Requirement/Acceptance Criterion/Priority columns
  ## Non-Functional Requirements -- table with ID/Requirement/Metric/Priority columns
  ## Out of Scope -- "[Mirror from SPEC.md Out of Scope]"
  ## Open Items -- "[Mirror from SPEC.md Open Questions]"

Use the EXACT section content shown in 12-RESEARCH.md Pattern 7.
  </action>
  <verify>
    <automated>ls templates/specs/SPEC.md.template templates/specs/DESIGN.md.template templates/specs/REQUIREMENTS.md.template && grep -c "spec-version:" templates/specs/SPEC.md.template && grep -c "## Overview" templates/specs/SPEC.md.template && grep -c "## Acceptance Criteria" templates/specs/SPEC.md.template && grep -c "## Assumptions" templates/specs/SPEC.md.template && grep -c "ASSUMPTION:" templates/specs/SPEC.md.template && grep -c "## Screens" templates/specs/DESIGN.md.template && grep -c "## Functional Requirements" templates/specs/REQUIREMENTS.md.template</automated>
  </verify>
  <acceptance_criteria>
    - File `templates/specs/SPEC.md.template` exists and contains `spec-version:` in YAML frontmatter
    - File `templates/specs/SPEC.md.template` contains sections: `## Overview`, `## User Stories`, `## UX Flows`, `## Acceptance Criteria`, `## Assumptions`, `## Open Questions`, `## Out of Scope`, `## Implementations`
    - File `templates/specs/SPEC.md.template` contains `[ASSUMPTION:` block pattern
    - File `templates/specs/DESIGN.md.template` exists and contains `## Screens`, `## Components`, `## Behavior Specifications`, `## State Definitions`
    - File `templates/specs/REQUIREMENTS.md.template` exists and contains `## Functional Requirements`, `## Non-Functional Requirements`
  </acceptance_criteria>
  <done>All three template files exist in templates/specs/ with correct sections and frontmatter</done>
</task>

<task type="auto">
  <name>Task 2: Create spec-floor-check.sh hook</name>
  <files>hooks/spec-floor-check.sh</files>
  <read_first>
    - hooks/dev-cycle-check.sh (lines 1-44 for boilerplate pattern, lines 75-103 for self-protection pattern to understand)
    - hooks/completion-audit.sh (lines 1-44 for alternative boilerplate reference)
    - .planning/phases/12-spec-foundation/12-RESEARCH.md (Pattern 2 section for spec-floor-check logic design)
  </read_first>
  <action>
Create `hooks/spec-floor-check.sh` as a new file (do NOT edit any existing hook files).

The script must:

1. Use the exact boilerplate from dev-cycle-check.sh lines 1-44: shebang, set -euo pipefail, trap 'exit 0' ERR, umask 0077, jq availability check with graceful degradation, input=$(cat), hook_event extraction, emit_block() function, cmd extraction, early exit if cmd is empty.

2. After boilerplate, detect command type using grep with word boundaries:
   - is_plan_phase=true when cmd matches \bgsd-plan-phase\b or \bgsd[- ]plan[- ]phase\b
   - is_fast=true when cmd matches \bgsd-fast\b or \bgsd[- ]fast\b or \bgsd[- ]quick\b
   - Exit 0 immediately if neither matches

3. For gsd-plan-phase (hard block):
   - Set SPEC=".planning/SPEC.md"
   - If SPEC file does not exist: emit_block with message "SPEC FLOOR VIOLATION: .planning/SPEC.md is missing. Run /silver:spec before planning. gsd-plan-phase requires a minimum viable spec."
   - For each required section ("## Overview" and "## Acceptance Criteria"): if section not found via grep -q "^${section}" "$SPEC", emit_block with message "SPEC FLOOR VIOLATION: .planning/SPEC.md is missing required section: ${section}. Run /silver:spec to complete the spec before planning."

4. For gsd-fast/gsd-quick (warning only, never block):
   - If neither .planning/SPEC.md nor .planning/SPEC.fast.md exists: printf a hookSpecificOutput message with text "Warning: SPEC FLOOR ADVISORY: No .planning/SPEC.md found. Fast path proceeding without spec floor. For tracked work, run /silver:spec first."

5. End with exit 0

6. Make the file executable: chmod +x hooks/spec-floor-check.sh
  </action>
  <verify>
    <automated>test -x hooks/spec-floor-check.sh && head -1 hooks/spec-floor-check.sh | grep -q "bash" && grep -c "emit_block" hooks/spec-floor-check.sh && grep -c "gsd-plan-phase" hooks/spec-floor-check.sh && grep -c "gsd-fast" hooks/spec-floor-check.sh && grep -c "SPEC FLOOR VIOLATION" hooks/spec-floor-check.sh && grep -c "SPEC FLOOR ADVISORY" hooks/spec-floor-check.sh && grep -c "permissionDecision" hooks/spec-floor-check.sh</automated>
  </verify>
  <acceptance_criteria>
    - File `hooks/spec-floor-check.sh` exists and is executable (-x)
    - First line is #!/usr/bin/env bash
    - Contains set -euo pipefail and trap 'exit 0' ERR and umask 0077
    - Contains emit_block function with permissionDecision deny output
    - Contains gsd-plan-phase detection with word boundary grep
    - Contains gsd-fast and gsd-quick detection
    - Hard blocks (emit_block) only fire for is_plan_phase == true
    - Warning (printf with ADVISORY) only fires for is_fast == true
    - Checks for .planning/SPEC.md file existence
    - Checks for ## Overview and ## Acceptance Criteria section presence via grep
    - Fast path also checks for .planning/SPEC.fast.md as alternative
    - Script ends with exit 0
  </acceptance_criteria>
  <done>spec-floor-check.sh exists, is executable, hard-blocks gsd-plan-phase without spec, warns on gsd-fast without spec, and follows the exact hook boilerplate pattern from dev-cycle-check.sh</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook stdin -> spec-floor-check.sh | Untrusted JSON from Claude tool system |
| .planning/SPEC.md -> hook | User-created file checked by hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | spec-floor-check.sh | mitigate | Hook self-protection in dev-cycle-check.sh prevents Claude from modifying hook files (already enforced) |
| T-12-02 | Spoofing | .planning/SPEC.md | accept | User could create a minimal SPEC.md with just headers to pass floor check -- acceptable because the spec is a starting point, not a guarantee of quality |
| T-12-03 | Denial of Service | spec-floor-check.sh | mitigate | trap 'exit 0' ERR ensures unexpected failures never block tool calls; no network calls keeps execution under 100ms |
| T-12-04 | Elevation of Privilege | spec-floor-check.sh | mitigate | umask 0077 restricts file permissions; no privileged operations in hook |
</threat_model>

<verification>
1. `ls templates/specs/` shows 3 template files
2. `grep "spec-version:" templates/specs/SPEC.md.template` returns match
3. `grep "## Overview" templates/specs/SPEC.md.template` returns match
4. `grep "## Acceptance Criteria" templates/specs/SPEC.md.template` returns match
5. `grep "ASSUMPTION:" templates/specs/SPEC.md.template` returns match
6. `test -x hooks/spec-floor-check.sh` passes
7. `grep "SPEC FLOOR VIOLATION" hooks/spec-floor-check.sh` returns match
8. `grep "SPEC FLOOR ADVISORY" hooks/spec-floor-check.sh` returns match
</verification>

<success_criteria>
- Three template files exist in templates/specs/ with all required sections and frontmatter
- spec-floor-check.sh is executable and follows exact hook boilerplate
- Hook hard-blocks gsd-plan-phase when SPEC.md missing or lacking required sections
- Hook warns (not blocks) on gsd-fast/gsd-quick path
</success_criteria>

<output>
After completion, create `.planning/phases/12-spec-foundation/12-01-SUMMARY.md`
</output>
