---
phase: 07-close-enforcement-audit-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/forbidden-skill-check.sh
  - hooks/hooks.json
  - hooks/stop-check.sh
  - hooks/completion-audit.sh
  - hooks/ci-status-check.sh
autonomous: true
requirements: [ENF-F03, ENF-F06, ENF-F16, ENF-F19, ENF-F13]
must_haves:
  truths:
    - "PreToolUse/Skill hook denies forbidden skills (executing-plans, subagent-driven-development)"
    - "SubagentStop event triggers stop-check.sh"
    - "stop-check.sh blocks Stop/SubagentStop if quality-gate-stage markers missing when release is in progress"
    - "completion-audit.sh Tier 2 blocks gh pr merge same as gh pr create"
    - "ci-status-check.sh fires on gh pr create, gh pr merge, gh release create"
  artifacts:
    - path: hooks/forbidden-skill-check.sh
      provides: "PreToolUse/Skill blocklist enforcement"
    - path: hooks/hooks.json
      provides: "SubagentStop + PreToolUse/Skill registrations"
    - path: hooks/stop-check.sh
      provides: "quality-gate-stage check on Stop"
    - path: hooks/completion-audit.sh
      provides: "gh pr merge in Tier 2"
    - path: hooks/ci-status-check.sh
      provides: "extended trigger patterns"
  key_links:
    - from: hooks/hooks.json
      to: hooks/forbidden-skill-check.sh
      via: "PreToolUse/Skill registration"
      pattern: "forbidden-skill-check"
    - from: hooks/hooks.json
      to: hooks/stop-check.sh
      via: "SubagentStop registration"
      pattern: "SubagentStop"
---

<objective>
Create the forbidden-skill-check.sh hook, register SubagentStop for stop-check.sh, add quality-gate-stage check to stop-check.sh, add gh pr merge to completion-audit.sh Tier 2, and extend ci-status-check.sh trigger patterns.

Purpose: Close findings F-03, F-06, F-16, F-19, F-13 from the enforcement audit.
Output: 5 updated/new hook scripts + updated hooks.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-close-all-enforcement-audit-gaps-from-enforcement-audit-md-f/07-CONTEXT.md
@hooks/hooks.json
@hooks/stop-check.sh
@hooks/completion-audit.sh
@hooks/ci-status-check.sh
@hooks/dev-cycle-check.sh
@.silver-bullet.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create forbidden-skill-check.sh + register in hooks.json + register SubagentStop</name>
  <files>hooks/forbidden-skill-check.sh, hooks/hooks.json</files>
  <action>
**A) Create `hooks/forbidden-skill-check.sh`** — PreToolUse hook (matcher: Skill).

Follow the exact boilerplate pattern from existing hooks (set -euo pipefail, umask 0077, jq check with warning message, error trap).

Logic:
1. Read JSON from stdin. Extract hook_event_name.
2. Extract skill name: `jq -r '.tool_input.skill // ""'`
3. Strip namespace prefix: `sed 's/^[a-zA-Z0-9_-]*://'`
4. Define hardcoded forbidden list: `executing-plans subagent-driven-development`
5. Also read `.silver-bullet.json` configurable forbidden list: walk up from $PWD to find config, then `jq -r '(.skills.forbidden // []) | .[]'`
6. If stripped skill name matches any forbidden entry, output deny:
   ```
   printf '{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":%s}}' "$json_reason"
   ```
   Reason message: "FORBIDDEN SKILL — [skill_name] is blocked by Silver Bullet. Use /gsd:execute-phase for execution and /gsd:plan-phase for planning. See silver-bullet.md section 6."
7. Otherwise silent exit 0.

Make executable: `chmod +x hooks/forbidden-skill-check.sh`

**B) Update `hooks/hooks.json`:**

1. Add PreToolUse/Skill entry for forbidden-skill-check.sh:
   ```json
   {"matcher":"Skill","hooks":[{"type":"command","command":"\"${CLAUDE_PLUGIN_ROOT}/hooks/forbidden-skill-check.sh\"","async":false}]}
   ```
   Add this to the PreToolUse array BEFORE the existing entries (it should fire first).

2. Add SubagentStop section (new top-level key in "hooks" object, after "Stop"):
   ```json
   "SubagentStop": [{"matcher":".*","hooks":[{"type":"command","command":"\"${CLAUDE_PLUGIN_ROOT}/hooks/stop-check.sh\"","async":false}]}]
   ```
  </action>
  <verify>
    <automated>bash -n hooks/forbidden-skill-check.sh && jq . hooks/hooks.json >/dev/null && jq -e '.hooks.SubagentStop' hooks/hooks.json >/dev/null && jq -e '.hooks.PreToolUse | map(select(.matcher == "Skill")) | length > 0' hooks/hooks.json >/dev/null && echo "PASS"</automated>
  </verify>
  <done>forbidden-skill-check.sh exists and is executable, hooks.json has PreToolUse/Skill and SubagentStop entries, all JSON valid</done>
</task>

<task type="auto">
  <name>Task 2: Extend stop-check.sh (F-16), completion-audit.sh (F-19), ci-status-check.sh (F-13)</name>
  <files>hooks/stop-check.sh, hooks/completion-audit.sh, hooks/ci-status-check.sh</files>
  <action>
**A) stop-check.sh — F-16: Add quality-gate-stage check**

After the existing "Check required skills" section (after the missing skills loop, around line 143), add a quality-gate-stage check block:

1. Determine if release context applies: check if `create-release` is in required_skills list OR if any `quality-gate-stage-*` marker is already present in state_contents.
2. If release context applies, check for all 4 stage markers:
   ```bash
   release_missing=""
   for stage in quality-gate-stage-1 quality-gate-stage-2 quality-gate-stage-3 quality-gate-stage-4; do
     if ! printf '%s\n' "$state_contents" | grep -qx "$stage" 2>/dev/null; then
       release_missing="${release_missing:+$release_missing }$stage"
     fi
   done
   ```
3. If both `missing` (skills) and `release_missing` are non-empty, combine into one block message.
4. If only `release_missing` is non-empty, emit block: "Cannot complete -- quality gate stages incomplete: [list]. Complete all 4 stages before declaring task complete."
5. Keep existing output logic for skills-only missing case unchanged.

**B) completion-audit.sh — F-19: Add `gh pr merge` to Tier 2**

In the command classifier section (around lines 56-67), add a new elif:
```bash
elif printf '%s' "$cmd" | grep -qE '\bgh pr merge\b'; then
  is_completion=true
```
Add this right after the existing `gh pr create` check (line 60).

**C) ci-status-check.sh — F-13: Extend trigger pattern**

Change line 37 from:
```bash
printf '%s' "$cmd" | grep -qE '\bgit (commit|push)\b' || exit 0
```
to:
```bash
printf '%s' "$cmd" | grep -qE '\bgit (commit|push)\b|\bgh pr (create|merge)\b|\bgh release create\b' || exit 0
```
  </action>
  <verify>
    <automated>bash -n hooks/stop-check.sh && bash -n hooks/completion-audit.sh && bash -n hooks/ci-status-check.sh && grep -q 'quality-gate-stage' hooks/stop-check.sh && grep -q 'gh pr merge' hooks/completion-audit.sh && grep -q 'gh pr.*create|merge' hooks/ci-status-check.sh && echo "PASS"</automated>
  </verify>
  <done>stop-check.sh has quality-gate-stage check, completion-audit.sh classifies gh pr merge as Tier 2, ci-status-check.sh fires on gh pr create/merge and gh release create</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill tool input -> forbidden-skill-check.sh | Skill name from Claude's tool invocation |
| State file -> stop-check.sh | Quality gate markers read from user-writable state file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | forbidden-skill-check.sh | mitigate | Strip namespace prefix before matching to prevent bypass via namespace prefixing |
| T-07-02 | Tampering | stop-check.sh quality gate check | accept | State file is already tamper-protected by dev-cycle-check.sh; markers can only be written via whitelisted patterns |
</threat_model>

<verification>
- `bash -n` passes on all modified scripts
- `jq .` validates hooks.json
- hooks.json contains SubagentStop and PreToolUse/Skill entries
- stop-check.sh contains quality-gate-stage logic
- completion-audit.sh classifies `gh pr merge` as completion tier
- ci-status-check.sh matches extended pattern set
</verification>

<success_criteria>
F-03: forbidden-skill-check.sh denies executing-plans and subagent-driven-development
F-06: SubagentStop registered in hooks.json pointing to stop-check.sh
F-16: stop-check.sh checks quality-gate-stage markers when release context detected
F-19: gh pr merge blocked by Tier 2 completion audit
F-13: CI status checked on gh pr create, gh pr merge, gh release create
</success_criteria>

<output>
After completion, create `.planning/phases/07-close-all-enforcement-audit-gaps-from-enforcement-audit-md-f/07-01-SUMMARY.md`
</output>
