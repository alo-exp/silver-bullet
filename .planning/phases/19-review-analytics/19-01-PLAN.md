---
phase: 19-review-analytics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/artifact-reviewer/rules/review-loop.md
  - skills/artifact-reviewer/SKILL.md
autonomous: true
requirements: [ARVW-10a, ARVW-10b, ARVW-10d, ARVW-10e]

must_haves:
  truths:
    - "Every completed review round appends a JSON object to .planning/review-analytics.jsonl"
    - "Each metric record includes artifact_path, round, finding_count, status, depth, check_mode, and duration_seconds"
    - "When review-analytics.jsonl exceeds 1000 lines, oldest entries are archived before appending"
  artifacts:
    - path: "skills/artifact-reviewer/rules/review-loop.md"
      provides: "Metrics emission pseudocode in review loop algorithm"
      contains: "emit_review_metric"
    - path: "skills/artifact-reviewer/SKILL.md"
      provides: "Orchestration step mentioning analytics emission"
      contains: "review-analytics.jsonl"
  key_links:
    - from: "skills/artifact-reviewer/rules/review-loop.md"
      to: ".planning/review-analytics.jsonl"
      via: "emit_review_metric function appends JSON line"
      pattern: "emit_review_metric"
---

<objective>
Add structured metrics emission to the review loop so every review round produces a JSON record in `.planning/review-analytics.jsonl`, and implement file rotation when the file exceeds 1000 lines.

Purpose: Enables data-driven review health visibility — the analytics file is the data source for the silver-review-stats skill (Plan 02).
Output: Updated review-loop.md with metrics emission and rotation logic; updated SKILL.md orchestration steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@skills/artifact-reviewer/rules/review-loop.md
@skills/artifact-reviewer/rules/reviewer-interface.md
@skills/artifact-reviewer/SKILL.md
@.planning/phases/18-configurable-review-depth/18-01-SUMMARY.md

<interfaces>
From skills/artifact-reviewer/rules/review-loop.md (current algorithm pseudocode):
```
consecutive_passes = 0
round = 1
state = load_review_state(artifact_path)
...
depth = resolve_depth(reviewer_skill_name)
required_passes = 2 if depth == "deep" else 1
check_mode = "structural" if depth == "quick" else "full"
while consecutive_passes < required_passes:
  findings = invoke_reviewer(artifact_path, source_inputs, check_mode)
  record_round(artifact_path, round, findings)
  ...
```

From skills/artifact-reviewer/SKILL.md (orchestration steps):
```
1. Resolve artifact path to absolute path
2. Load per-artifact review state
3. Auto-detect or validate reviewer
4. Resolve review depth and run review loop
5. Write REVIEW-ROUNDS.md audit trail
6. On required consecutive clean passes: clear state, report completion
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add metrics emission and rotation to review-loop.md</name>
  <files>skills/artifact-reviewer/rules/review-loop.md</files>
  <read_first>skills/artifact-reviewer/rules/review-loop.md</read_first>
  <action>
Add a new Section 4 titled "## Section 4: Review Analytics (ARVW-10)" to review-loop.md with:

**4a. Metric Record Schema:**
```json
{
  "artifact_path": "/absolute/path/to/artifact.md",
  "artifact_type": "SPEC.md",
  "reviewer": "review-spec",
  "round": 2,
  "finding_count": 3,
  "status": "ISSUES_FOUND",
  "depth": "standard",
  "check_mode": "full",
  "duration_seconds": 45,
  "timestamp": "2026-04-10T12:00:00Z"
}
```

Where `artifact_type` is the filename (basename) of the artifact, and `duration_seconds` is wall-clock seconds for that round.

**4b. `emit_review_metric()` function pseudocode:**
```
emit_review_metric(artifact_path, reviewer, round, finding_count, status, depth, check_mode, duration_seconds):
  analytics_file = ".planning/review-analytics.jsonl"
  
  # Rotate if needed (ARVW-10e)
  rotate_analytics_if_needed(analytics_file)
  
  metric = {
    artifact_path: realpath(artifact_path),
    artifact_type: basename(artifact_path),
    reviewer: reviewer,
    round: round,
    finding_count: finding_count,
    status: status,
    depth: depth,
    check_mode: check_mode,
    duration_seconds: duration_seconds,
    timestamp: current_iso_timestamp()
  }
  
  append_json_line(analytics_file, metric)
```

**4c. `rotate_analytics_if_needed()` function pseudocode:**
```
rotate_analytics_if_needed(analytics_file):
  if not file_exists(analytics_file):
    return
  
  line_count = count_lines(analytics_file)
  if line_count <= 1000:
    return
  
  archive_dir = ".planning/archive"
  mkdir_p(archive_dir)
  archive_name = "review-analytics-{YYYY-MM-DD}.jsonl"
  move(analytics_file, archive_dir + "/" + archive_name)
  # New entries will be appended to a fresh analytics_file
```

**4d. Integration into the main algorithm (Section 1):**

Modify the Section 1 algorithm to:
1. Record `round_start = current_time()` at the top of each while-loop iteration, immediately before `invoke_reviewer()`
2. After the reviewer returns findings and before the if/else on status, compute `duration = current_time() - round_start`
3. Call `emit_review_metric(artifact_path, reviewer_skill_name, round, len(findings.findings), findings.status, depth, check_mode, duration)` after computing duration
4. Add a comment "# ARVW-10a/10d: emit per-round metric" at the emit call site

The algorithm already has `depth`, `check_mode`, `round`, and `findings` in scope — no new variables needed except `round_start` and `duration`.
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/silver-bullet && grep -c "emit_review_metric" skills/artifact-reviewer/rules/review-loop.md | grep -q "[2-9]" && grep -c "rotate_analytics_if_needed" skills/artifact-reviewer/rules/review-loop.md | grep -q "[1-9]" && grep -q "review-analytics.jsonl" skills/artifact-reviewer/rules/review-loop.md && grep -q "duration_seconds" skills/artifact-reviewer/rules/review-loop.md && grep -q "Section 4" skills/artifact-reviewer/rules/review-loop.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "emit_review_metric" review-loop.md returns at least 2 matches (definition + call site)
    - grep "rotate_analytics_if_needed" review-loop.md returns at least 1 match
    - grep "review-analytics.jsonl" review-loop.md returns at least 1 match
    - grep "duration_seconds" review-loop.md returns at least 1 match
    - grep "Section 4" review-loop.md returns 1 match
    - grep "round_start" review-loop.md returns at least 1 match in the Section 1 algorithm
    - grep "1000" review-loop.md returns at least 1 match (rotation threshold)
    - grep "archive" review-loop.md returns at least 1 match
  </acceptance_criteria>
  <done>Section 4 exists with metric schema, emit function, rotation function. Section 1 algorithm calls emit_review_metric with timing instrumentation.</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md orchestration steps for analytics</name>
  <files>skills/artifact-reviewer/SKILL.md</files>
  <read_first>skills/artifact-reviewer/SKILL.md</read_first>
  <action>
Update the "Orchestration Steps" section in SKILL.md to add analytics awareness:

1. Add step 4.5 (between existing steps 4 and 5): "After each review round, emit metrics to `.planning/review-analytics.jsonl` (per rules/review-loop.md Section 4)"
2. Add step 6.5 (after step 6): "Analytics file rotation: if `.planning/review-analytics.jsonl` exceeds 1000 lines, archive to `.planning/archive/review-analytics-{date}.jsonl` before next append"

Also add a new section "## Review Analytics" after the "Review Depth Configuration" section:

```markdown
## Review Analytics

Every review round emits a structured metric to `.planning/review-analytics.jsonl` (JSON Lines format, one object per line). Each record captures:

- `artifact_path`, `artifact_type`, `reviewer` — what was reviewed
- `round`, `finding_count`, `status` — round outcome
- `depth`, `check_mode` — review configuration
- `duration_seconds`, `timestamp` — timing data

When the analytics file exceeds 1000 lines, it is archived to `.planning/archive/review-analytics-{date}.jsonl` and a fresh file is started.

Run `silver-review-stats` to produce summary reports from the analytics data.
```

Add `@skills/artifact-reviewer/rules/review-loop.md` to the "Loading Rules" section's bullet list if not already there (it should already be present — verify, do not duplicate).
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/silver-bullet && grep -q "review-analytics.jsonl" skills/artifact-reviewer/SKILL.md && grep -q "Review Analytics" skills/artifact-reviewer/SKILL.md && grep -q "silver-review-stats" skills/artifact-reviewer/SKILL.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "review-analytics.jsonl" SKILL.md returns at least 1 match
    - grep "Review Analytics" SKILL.md returns 1 match (section header)
    - grep "silver-review-stats" SKILL.md returns at least 1 match
    - grep "1000" SKILL.md returns at least 1 match (rotation threshold)
    - Orchestration steps mention metrics emission
  </acceptance_criteria>
  <done>SKILL.md documents analytics emission in orchestration steps and has a Review Analytics reference section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analytics file writes | Append-only JSONL — no user input crosses into file content beyond artifact paths already trusted by the review loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | T (Tampering) | review-analytics.jsonl | accept | File is advisory (reports only), not a security gate — tampering has no enforcement impact |
| T-19-02 | D (DoS) | analytics file growth | mitigate | Rotation at 1000 lines prevents unbounded growth |
</threat_model>

<verification>
1. review-loop.md Section 4 exists with emit_review_metric and rotate_analytics_if_needed
2. review-loop.md Section 1 algorithm includes round timing and emit call
3. SKILL.md documents analytics emission and references silver-review-stats
</verification>

<success_criteria>
The review loop algorithm includes per-round metric emission with timing instrumentation, JSONL append, and 1000-line rotation — all documented in review-loop.md Section 4 and referenced in SKILL.md.
</success_criteria>

<output>
After completion, create `.planning/phases/19-review-analytics/19-01-SUMMARY.md`
</output>
