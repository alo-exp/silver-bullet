---
phase: 19-review-analytics
plan: "02"
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - skills/silver-review-stats/SKILL.md
autonomous: true
requirements: [ARVW-10c]

must_haves:
  truths:
    - "Running silver-review-stats reads .planning/review-analytics.jsonl and produces a summary table"
    - "Summary shows pass rates, average rounds to clean pass, and common finding categories by artifact type"
  artifacts:
    - path: "skills/silver-review-stats/SKILL.md"
      provides: "silver-review-stats skill definition"
      contains: "review-analytics.jsonl"
  key_links:
    - from: "skills/silver-review-stats/SKILL.md"
      to: ".planning/review-analytics.jsonl"
      via: "reads JSONL file and aggregates metrics"
      pattern: "review-analytics.jsonl"
---

<objective>
Create the silver-review-stats skill that reads `.planning/review-analytics.jsonl` and produces summary reports showing review health metrics by artifact type.

Purpose: Gives users data-driven visibility into review patterns — which artifacts need the most rounds, what pass rates look like, and which finding categories recur.
Output: New skill at skills/silver-review-stats/SKILL.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@skills/artifact-reviewer/rules/review-loop.md
@skills/artifact-reviewer/SKILL.md
@.planning/phases/19-review-analytics/19-01-PLAN.md

<interfaces>
From review-loop.md Section 4 (created in Plan 01), metric record schema:
```json
{
  "artifact_path": "/absolute/path/to/artifact.md",
  "artifact_type": "SPEC.md",
  "reviewer": "review-spec",
  "round": 2,
  "finding_count": 3,
  "status": "ISSUES_FOUND",
  "depth": "standard",
  "check_mode": "full",
  "duration_seconds": 45,
  "timestamp": "2026-04-10T12:00:00Z"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create silver-review-stats SKILL.md</name>
  <files>skills/silver-review-stats/SKILL.md</files>
  <read_first>skills/artifact-reviewer/SKILL.md, skills/artifact-reviewer/rules/review-loop.md</read_first>
  <action>
Create directory `skills/silver-review-stats/` and file `skills/silver-review-stats/SKILL.md` with:

**Frontmatter:**
```yaml
---
name: silver-review-stats
description: "Reads .planning/review-analytics.jsonl and produces summary reports showing pass rates, average rounds, and common findings by artifact type"
argument-hint: "[--since YYYY-MM-DD] [--type artifact-type]"
---
```

**Body:**

```markdown
# silver-review-stats

Produces summary reports from review analytics data. Reads `.planning/review-analytics.jsonl` (and archived files in `.planning/archive/review-analytics-*.jsonl` if `--since` spans archived data) and aggregates metrics by artifact type.

## Usage

```
/silver-review-stats
/silver-review-stats --since 2026-04-01
/silver-review-stats --type SPEC.md
```

## Orchestration Steps

1. Locate `.planning/review-analytics.jsonl` — if missing, display "No review analytics data found. Run artifact reviews to generate data." and stop
2. If `--since` provided, also scan `.planning/archive/review-analytics-*.jsonl` for records after the date
3. Parse each line as JSON, filter by `--since` and/or `--type` if provided
4. Aggregate metrics and display the three report tables below
5. Display total record count and date range at the bottom

## Report Tables

### Table 1: Pass Rates by Artifact Type

| Artifact Type | Total Rounds | Pass | Fail | Pass Rate |
|---------------|-------------|------|------|-----------|
| SPEC.md | 12 | 8 | 4 | 66.7% |
| PLAN.md | 20 | 18 | 2 | 90.0% |

- Group by `artifact_type` field
- Pass = records where `status == "PASS"`, Fail = `status == "ISSUES_FOUND"`
- Pass Rate = Pass / Total Rounds * 100, formatted to 1 decimal

### Table 2: Rounds to Clean Pass by Artifact Type

| Artifact Type | Reviews | Avg Rounds | Min | Max |
|---------------|---------|------------|-----|-----|
| SPEC.md | 4 | 3.0 | 2 | 5 |
| PLAN.md | 8 | 1.5 | 1 | 3 |

- A "review" is a sequence of rounds for the same `artifact_path` (group by artifact_path, then count rounds per review session)
- Avg Rounds = average number of rounds per review session to reach clean pass
- Min/Max = minimum and maximum rounds across sessions

### Table 3: Common Finding Categories by Artifact Type

| Artifact Type | Avg Findings/Round | Total Findings | Most Active Reviewer |
|---------------|-------------------|----------------|---------------------|
| SPEC.md | 2.5 | 30 | review-spec |
| PLAN.md | 0.8 | 16 | gsd-plan-checker |

- Avg Findings/Round = sum(finding_count) / total rounds for that type
- Total Findings = sum(finding_count) for that type
- Most Active Reviewer = reviewer with the most rounds for that artifact type

### Footer

```
---
Total records: {N} | Date range: {earliest timestamp} to {latest timestamp}
Analytics file: .planning/review-analytics.jsonl ({line_count} lines)
```

## Edge Cases

- Empty analytics file: display "No review data recorded yet."
- All PASS: Tables still display with 100% pass rates
- Single round reviews: Avg Rounds = 1.0
- `--type` with no matches: display "No records found for artifact type '{type}'."
```
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/silver-bullet && test -f skills/silver-review-stats/SKILL.md && grep -q "review-analytics.jsonl" skills/silver-review-stats/SKILL.md && grep -q "Pass Rate" skills/silver-review-stats/SKILL.md && grep -q "Avg Rounds" skills/silver-review-stats/SKILL.md && grep -q "artifact_type" skills/silver-review-stats/SKILL.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File skills/silver-review-stats/SKILL.md exists
    - grep "review-analytics.jsonl" SKILL.md returns at least 2 matches
    - grep "Pass Rate" SKILL.md returns at least 1 match
    - grep "Avg Rounds" SKILL.md returns at least 1 match
    - grep "artifact_type" SKILL.md returns at least 1 match
    - grep "archive" SKILL.md returns at least 1 match (handles archived data)
    - Frontmatter contains name: silver-review-stats
    - Three report table sections exist (Table 1, Table 2, Table 3)
  </acceptance_criteria>
  <done>silver-review-stats skill exists with SKILL.md defining three report tables (pass rates, rounds to clean pass, finding categories), handles --since and --type filters, reads both current and archived analytics files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSONL parsing | Reads .planning/review-analytics.jsonl — file is written by the review loop (trusted), not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | I (Info Disclosure) | analytics data | accept | Analytics file contains artifact paths only — no secrets, no PII |
</threat_model>

<verification>
1. skills/silver-review-stats/SKILL.md exists with frontmatter and three report tables
2. Skill references review-analytics.jsonl and handles archived files
3. All three success criteria metrics are covered: pass rates, avg rounds, common findings
</verification>

<success_criteria>
silver-review-stats skill is fully defined with clear orchestration steps, three report tables matching ARVW-10c requirements, and edge case handling.
</success_criteria>

<output>
After completion, create `.planning/phases/19-review-analytics/19-02-SUMMARY.md`
</output>
