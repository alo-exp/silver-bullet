# Phase 38 — Gitignore & Docs Refresh (v0.22.0)

Resolves issues #20 (IGNORE-01) and #23 (DOC-02). Final phase of milestone v0.22.0.

## Tasks

### 1. IGNORE-01 — narrow `.claude/` gitignore (Option A)

Replace blanket `.claude/` rule with runtime-only subpaths:

```
.claude/projects/
.claude/local/
.claude/.silver-bullet/
.claude/settings.local.json
```

Acceptance:
- `git check-ignore .claude/settings.json` → non-zero (tracked)
- `git check-ignore .claude/commands/` → non-zero (tracked)
- `.claude/settings.local.json`, `.claude/worktrees/` … wait: issue #20 allows the committed
  ignore list to be narrowed; committed stale state under `.claude/projects/` etc. removed.

### 2. DOC-02 — public-surface version/count refresh

Current state:
- 41 skills (`ls skills/ | wc -l`)
- 18 hooks (`ls hooks/*.sh | wc -l`)
- 18 FLOWs (per composable-flows architecture)
- Baseline tests: 1112 passed / 13 failed (2/3 suites green)

Updates:
- `README.md` line 11: bump `Current version:` to v0.22.0 with summary covering P34–P38
- `site/index.html` hero version badge: v0.19.1 → v0.22.0
- `site/index.html` meta description/twitter: `39 skills` → `41 skills`
- `site/help/getting-started/index.html`: `39 skills` → `41 skills`; bump version range
- `site/help/concepts/index.html`: `39 skills` → `41 skills`
- (Historical v0.18.1 references in concepts/search.js stay — those describe when features landed)
- `docs/ARCHITECTURE.md`: 7 layers → 10 layers (line 67)

### 3. CHANGELOG.md — fill gap 0.20.8 → 0.22.0

Add consolidated v0.22.0 entry (per milestone closure) covering:

- SEC-01 (P34): Google Chat webhook → env var + secret-scan CI (6cb66c5)
- SEC-02/03/04 (P35): symlink guards, jq payloads, batch fixes (e247ff3)
- HOOK-06/07/08 (P36): stop-check fail-open paths + coverage (4339060)
- CONS-01/02 (P37): broken skill refs + hooks/config schema drift (0b86dc6)
- IGNORE-01 (P38): narrow `.claude/` gitignore (this phase)
- DOC-02 (P38): public surface refresh (this phase)

Resolves #20 and #23. Note: v0.20.9–v0.21.3 gap documented via "Unreleased" consolidation
into v0.22.0 section — this phase explicitly covers v0.22.0 milestone scope per plan.

### 4. Verify

- Run `bash tests/run-all-tests.sh` — expect 1112/13 baseline
- Commit, push, close issues #20 and #23

## Files Touched

- `.gitignore`
- `README.md`
- `site/index.html`
- `site/help/getting-started/index.html`
- `site/help/concepts/index.html`
- `docs/ARCHITECTURE.md`
- `CHANGELOG.md`
- `.planning/phases/38-gitignore-docs-refresh/38-01-PLAN.md` (this file)
