---
plan: 061-01
phase: 061-skill-quality-rename
status: ready
wave: 1
requirements:
  - SKL-01
  - SKL-02
  - SKL-03
  - SKL-04
threat_model: low — skill file edits and minor heading renames, no hook logic changes
must_haves:
  truths:
    - "wc -l skills/silver-add/SKILL.md outputs ≤299"
    - "wc -l skills/silver-rem/SKILL.md outputs ≤299"
    - "grep '### 10[a-e]\\.' silver-bullet.md exits non-zero (0 matches)"
    - "grep -c '### 9[a-e]\\.' silver-bullet.md outputs 5"
    - "bash tests/run-all-tests.sh passes"
  artifacts:
    - path: "skills/silver-add/SKILL.md"
      provides: "Trimmed silver-add skill (SKL-01)"
    - path: "skills/silver-rem/SKILL.md"
      provides: "Trimmed silver-rem skill (SKL-02)"
    - path: "silver-bullet.md"
      provides: "Corrected §9a–9e subsection headings (SKL-04)"
  key_links: []
---

# Plan 061-01: Skill Quality & Rename

## Goal

Trim `skills/silver-add/SKILL.md` and `skills/silver-rem/SKILL.md` to under 300 lines each, rename any remaining `## PATH N` / `PATH-N` patterns to `## FLOW N` / `FLOW-N`, and correct the §10a–10e subsection heading labels in `silver-bullet.md` to `§9a–9e`.

---

## Pre-execution verification

Before editing, confirm line counts:
```bash
wc -l skills/silver-add/SKILL.md skills/silver-rem/SKILL.md
```
Expected: silver-add ~371, silver-rem ~391.

Confirm SKL-03 scope:
```bash
grep -rn "## PATH [0-9]\|PATH-[0-9]" skills/ silver-bullet.md templates/silver-bullet.md.base
```
If no matches: SKL-03 is a no-op (patterns already converted to FLOW).

Confirm SKL-04 scope:
```bash
grep -n "### 10[a-e]\." silver-bullet.md
```
Expected: 5 matches (10a–10e). Templates file should already have 9a–9e.

---

## Task 1 — SKL-03: Verify PATH→FLOW rename (no-op confirmation)

**Requirement:** SKL-03 / GitHub #83

Search for `## PATH N` (uppercase) headings and `PATH-N` inline references across:
- `silver-bullet.md`
- `templates/silver-bullet.md.base`
- all `skills/**/*.md`

Exclusions: filesystem paths, `$PATH`, lowercase prose uses of "path".

**If no matches found:** Log "SKL-03: no uppercase PATH N patterns found — already using FLOW terminology throughout. No changes needed." Proceed to Task 2.

**If matches found:** Rename each occurrence:
- `## PATH N` → `## FLOW N`
- `PATH-N` inline → `FLOW-N`
- Do NOT rename: `## Path N` title-case (diagnosis branches in silver-bugfix, silver-forensics, silver-research — these are branching paths, not composable flows)

---

## Task 2 — SKL-04: Rename §10a–10e → §9a–9e in silver-bullet.md

**Requirement:** SKL-04 / GitHub #59

**Target file:** `silver-bullet.md` only. (`templates/silver-bullet.md.base` already has `### 9a.`–`### 9e.` — no changes needed there.)

**Changes:**
- `### 10a. Routing Preferences` → `### 9a. Routing Preferences`
- `### 10b. Step Skip Preferences` → `### 9b. Step Skip Preferences`
- `### 10c. Tool Preferences` → `### 9c. Tool Preferences`
- `### 10d. MultAI Preferences` → `### 9d. MultAI Preferences`
- `### 10e. Mode Preferences` → `### 9e. Mode Preferences`

The parent `## 10. User Workflow Preferences` heading stays unchanged (it is numbered 10 in silver-bullet.md because `## 9. Pre-Release Quality Gate` occupies §9 — an Ālo-only addition absent from the template). The prose references to `§10` (the whole section) are not changed.

Verify no prose `§10a`–`§10e` cross-references exist (they don't, per pre-run search) — no additional changes needed.

---

## Task 3 — SKL-01: Trim silver-add/SKILL.md to under 300 lines

**Requirement:** SKL-01 / GitHub #61

**Current:** ~371 lines. **Target:** ≤299 lines. **Must cut:** ≥72 lines. **Constraint:** no functional content removed.

### Trimming strategy (in order of impact):

**A. Step 5d — file header templates (~20 lines saved)**
The step shows full markdown code blocks for both ISSUES.md and BACKLOG.md headers. Replace both code blocks with concise prose descriptions:
- Remove the 8-line ISSUES.md header code block; replace with: `If not exists, create with a "# Issues" title line, one-sentence sequential-ID note, and a horizontal rule.`
- Remove the 8-line BACKLOG.md header code block; replace with: `If not exists, create with a "# Backlog" title line, one-sentence sequential-ID note, and a horizontal rule.`

**B. Step 4e — retry logic description (~7 lines saved)**
The retry-on-rate-limit description (8 lines of prose) can be condensed. Replace with:
```
Rate-limit retry: if any gh command fails with "rate limit"/"403"/"429", wait 60 s and retry; then 120 s; then 240 s. After 3 failures, record the issue as created and skip board placement with a warning. Proceed to Step 6.
```

**C. Step 4d — redundant output messages (~5 lines saved)**
Remove the two `Output: "..."` display lines from the cache-present and cache-absent branches. These add runtime verbosity with no decision value.

**D. Security Boundary section (~4 lines saved)**
Condense from 8 lines to 4: keep the three rules (session logs as untrusted, jq for JSON, description via jq) but drop the explanatory elaboration on each.

**E. Allowed Commands section (~4 lines saved)**
Merge the `grep -oE`, `sort -n`, `tail -1` items into one line. Remove `mktemp`, `mv` (atomic config write is implied by jq construction). Drop the closing explanatory sentence.

**F. Step 3 classification rubric (~5 lines saved)**
Merge "Default when ambiguous" and "Minimum bar" paragraphs. Remove the three-item "Do not file" list (keep as inline note: "Do not file transient exploration notes, one-line TODOs, or items already addressed").

**G. Edge Cases section (~8 lines saved)**
- Merge the "gh not authenticated" and "gh missing project scope" cases (both stop with an instruction).
- Merge "Target file absent on first write" and "`docs/issues/` directory absent" (Step 5a/5d already covers both).
- Drop the "Rate limit exhausted after retries" edge case (it duplicates Step 4e's handling note).

**H. Step 5e — appendable entry template code block (~8 lines saved)**
The 12-line markdown code block showing the exact entry format to append (with `FILED_ID`, `ITEM_TITLE`, `ITEM_LABEL`, etc.) can be replaced with a 2-line prose description of the format. The executor can derive the format from the field names. Replace the fenced code block with: "Append a `### FILED_ID — ITEM_TITLE` block containing Type, Filed date, Source, Status, and description fields, followed by `---`."

**I. Step 4b/4c prose header lines (~4 lines saved)**
Step 4b has "Run (idempotent — safe to call every invocation):" before the bash block — this comment belongs in the code, not as a standalone prose line. Remove it (1 line). Step 4c has three prose-label lines: "Derive owner/repo:", "Construct issue body using jq (never string interpolation):", "Create issue:" — each precedes a self-explanatory code block. Remove all three (3 lines).

**J. Step 5c sequential ID derivation (~3 lines saved)**
The "For issues:" and "For backlog:" prose labels before each bash block are redundant — the code is self-explanatory. Remove both prose label lines and the blank line separating them.

**Line-count accounting:** A + B + C + D + E + F + G + H + I + J = ~20 + 7 + 2 + 4 + 4 + 5 + 8 + 8 + 4 + 3 = **65 lines**. At 371 current lines: 371 − 65 = 306. Apply additional general prose tightening in Steps 1–2 and Step 7 (~7 lines) to reach ≤299.

After all cuts, run: `wc -l skills/silver-add/SKILL.md` — must be ≤299.

---

## Task 4 — SKL-02: Trim silver-rem/SKILL.md to under 300 lines

**Requirement:** SKL-02 / GitHub #62

**Current:** ~392 lines. **Target:** ≤299 lines. **Must cut:** ≥93 lines. **Constraint:** no functional content removed.

### Trimming strategy (in order of impact):

**A. Step 5 overflow — duplicated header templates (~30 lines saved)**
The size-cap overflow block (lines ~168–216) contains verbatim copies of both the knowledge and lessons file header templates. These are identical to what appears 40 lines earlier. Replace the embedded templates with prose references:

Inside the knowledge overflow branch, replace the `cat > "$TARGET" << EOF ... EOF` block (~17 lines) with:
```bash
# Re-create using the same knowledge header template as above (project name + MONTH + five category headings).
```
Inside the lessons overflow branch, replace the `cat > "$TARGET" << EOF ... EOF` block (~11 lines) with:
```bash
# Re-create using the same lessons header template as above (period + MONTH).
```
Keep the surrounding control flow (`if IS_NEW_FILE`, `PROJECT_NAME=`, `mkdir -p`) intact.

**B. Step 6 — merge parallel awk blocks (~12 lines saved)**
The knowledge and lessons awk patterns are identical except for the variable name (`CATEGORY` vs `CATEGORY_TAG` / `HEADING`). Merge the commentary: keep both code blocks but remove the repeated prose paragraphs ("For knowledge entries — CATEGORY is one of..." and "For lessons entries — CATEGORY_TAG is in..."). Replace with a single-sentence preamble before both blocks.

**C. Step 7 — condense INDEX.md update blocks (~10 lines saved)**
The knowledge awk (18 lines) and lessons awk (7 lines) are functionally necessary but the surrounding prose repeats what the code says. Remove the `Display:` lines from each branch and tighten the prose descriptions to one sentence per branch.

**D. Step 8 — condense session log recording (~5 lines saved)**
The two-branch session log append can be described in 8 lines instead of 13. Merge the "If contains ## Items Filed" and "If does NOT contain" branches into a single code comment pattern.

**E. Security Boundary section (~4 lines saved)**
Mirror the silver-add condensation: 3 rules in 4 lines (not 6).

**F. Allowed Commands section (~5 lines saved)**
Merge `printf`, `cat`, `>>` into one entry. Remove `wc -l` (implied by the 300-line cap). Tighten formatting.

**G. Step 2 classification (~4 lines saved)**
Remove "Display: 'Classifying as: [...]'" line (informational, no decision value). Merge the "Default when ambiguous" note inline.

**H. Edge Cases section (~8 lines saved)**
Merge related cases: "Monthly file does not exist" + "Category heading absent" + "IS_NEW_FILE=false" can share 2 lines each instead of 3.

**I. Step 5 size-cap wrapper scaffold (~14 lines saved)**
The size-cap overflow block's outer control-flow scaffold (the `if IS_NEW_FILE = false; then` wrapper with `LINE_COUNT=$(wc -l)`, the conditional `if [ "$LINE_COUNT" -ge 300 ]`, the `TARGET="${TARGET%.md}-b.md"`, the `IS_NEW_FILE=false`, and the nested `IS_NEW_FILE=true` subcheck) is ~14 lines of boilerplate that duplicates logic visible in strategy A. Replace the entire scaffold with prose:

> "**Size cap (existing files only):** If the target file has ≥300 lines, redirect TARGET to the next-suffix file (e.g., `YYYY-MM-b.md`). If that suffix file is new (does not exist), create it with the appropriate header template for the type (see Step 5 header above). Then proceed to Step 6 using the updated TARGET."

Keep only the `wc -l` command as a one-liner example: `` LINE_COUNT=$(wc -l < "$TARGET") ``.

**Line-count accounting:** A + B + C + D + E + F + G + H + I = ~28 + 12 + 10 + 5 + 4 + 5 + 4 + 8 + 14 = **90 lines**. At 391 current lines: 391 − 90 = 301. Apply ~3 lines of additional general prose tightening in Steps 1 or 4 to reach ≤299.

After all cuts, run: `wc -l skills/silver-rem/SKILL.md` — must be ≤299.

---

## Task 5 — Run test suite and commit

```bash
bash tests/run-all-tests.sh
```

All tests must pass (no regressions — skill edits do not affect hook tests, but run the full suite as a gate).

Commit all changes in a single atomic commit:
```
chore(skills): trim silver-add/silver-rem to <300 lines; fix §10a→9a subsection labels

SKL-01: silver-add/SKILL.md trimmed from 371→<300 lines (prose tightened, no functional content removed)
SKL-02: silver-rem/SKILL.md trimmed from 392→<300 lines (overflow template duplication removed)
SKL-03: verified no ## PATH N patterns exist — already using FLOW terminology throughout
SKL-04: silver-bullet.md ### 10a–10e renamed to ### 9a–9e (templates/silver-bullet.md.base already correct)

Closes #61, #62, #83, #59
```

---

## Acceptance Criteria

- [ ] `wc -l skills/silver-add/SKILL.md` outputs ≤299
- [ ] `wc -l skills/silver-rem/SKILL.md` outputs ≤299
- [ ] All silver-add Steps 1–7 + Edge Cases present (no steps dropped)
- [ ] All silver-rem Steps 1–9 + Edge Cases present (no steps dropped)
- [ ] `grep "### 10[a-e]\." silver-bullet.md` → 0 matches
- [ ] `grep "### 9[a-e]\." silver-bullet.md` → 5 matches
- [ ] `grep "### 9[a-e]\." templates/silver-bullet.md.base` → 5 matches (unchanged)
- [ ] `bash tests/run-all-tests.sh` → all tests pass
- [ ] Single commit with all 4 SKL changes, closes all 4 issues
