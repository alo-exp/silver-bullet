---
phase: 065-sb-core-skills-port
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - forge/skills/silver-release/SKILL.md
  - forge/skills/silver-migrate/SKILL.md
  - forge/skills/silver-update/SKILL.md
  - forge/skills/silver-scan/SKILL.md
autonomous: true
requirements:
  - PORT-SB-06
  - PORT-SB-07
  - PORT-SB-08
  - PORT-SB-09

must_haves:
  truths:
    - "forge/skills/silver-release/SKILL.md exists with Forge frontmatter and full milestone release workflow"
    - "forge/skills/silver-migrate/SKILL.md exists with Forge frontmatter and WORKFLOW.md migration steps"
    - "forge/skills/silver-update/SKILL.md exists with Forge frontmatter and update steps adapted for Forge"
    - "forge/skills/silver-scan/SKILL.md exists with Forge frontmatter and full session log scan logic"
    - "No file contains Skill() calls, AskUserQuestion, hook references, or silver-bullet.md §N references"
  artifacts:
    - path: "forge/skills/silver-release/SKILL.md"
      provides: "Milestone release orchestration"
      contains: "id: silver-release"
    - path: "forge/skills/silver-migrate/SKILL.md"
      provides: "Composable flows migration"
      contains: "id: silver-migrate"
    - path: "forge/skills/silver-update/SKILL.md"
      provides: "Silver Bullet Forge update"
      contains: "id: silver-update"
    - path: "forge/skills/silver-scan/SKILL.md"
      provides: "Retrospective session log scan"
      contains: "id: silver-scan"
  key_links:
    - from: "forge/skills/silver-release/SKILL.md"
      to: "silver-quality-gates skill"
      via: "trigger phrase instruction"
      pattern: "run the silver-quality-gates skill"
    - from: "forge/skills/silver-update/SKILL.md"
      to: "forge-sb-install.sh"
      via: "bash command"
      pattern: "forge-sb-install.sh"
---

<objective>
Port 4 SB release and lifecycle skills to Forge SKILL.md format.

Purpose: Forge users need access to the full release workflow (silver-release), workflow migration (silver-migrate), tool update (silver-update), and retrospective scan (silver-scan). These handle the end-of-milestone and maintenance lifecycle operations.

Output: 4 SKILL.md files under forge/skills/ with valid Forge frontmatter and complete workflow instructions adapted for direct Forge agent execution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Forge Skill Format (MANDATORY)

Every ported skill MUST use exactly this frontmatter:

```yaml
---
id: skill-name
title: Human Readable Title
description: One sentence description for Forge skill picker
trigger:
  - "trigger phrase 1"
  - "trigger phrase 2"
---
```

Body: Plain markdown instructions addressed directly to the Forge agent.

## Adaptations Required for ALL Skills

1. Remove `Skill(skill="...")` calls — replace with "run the X skill"
2. Remove `AskUserQuestion` — replace with "ask the user: [question]\nWait for their response."
3. Remove hook references (completion-audit, stop-check, record-skill, etc.)
4. Remove `silver-bullet.md §N` references — replace with inline instruction text
5. Remove `${PLUGIN_ROOT}` path resolution — Forge skills are self-contained
6. Preserve all substantive workflow steps, bash commands, and decision logic

## Key silver-update Adaptation

The source `skills/silver-update/SKILL.md` installs via `claude mcp install silver-bullet@alo-labs`. For Forge, adapt Step 5 to instead run `bash forge-sb-install.sh` (re-runs the installer script to get the latest skill files from the GitHub source). The version check steps (Steps 1-4) are preserved as-is — they still read GitHub releases to show what changed.

## Source Files

- `skills/silver-release/SKILL.md` → `forge/skills/silver-release/SKILL.md` (PORT-SB-06)
- `skills/silver-migrate/SKILL.md` → `forge/skills/silver-migrate/SKILL.md` (PORT-SB-07)
- `skills/silver-update/SKILL.md` → `forge/skills/silver-update/SKILL.md` (PORT-SB-08)
- `skills/silver-scan/SKILL.md` → `forge/skills/silver-scan/SKILL.md` (PORT-SB-09)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port silver-release and silver-migrate to Forge format</name>
  <files>forge/skills/silver-release/SKILL.md, forge/skills/silver-migrate/SKILL.md</files>
  <action>
Read source files `skills/silver-release/SKILL.md` and `skills/silver-migrate/SKILL.md`.

Create `forge/skills/silver-release/SKILL.md`:
- Frontmatter: id=silver-release, title="Silver Release — Milestone Release Workflow", description="SB-orchestrated milestone release: quality gates → audit → gap closure → docs → release notes → ship → complete milestone", triggers: "silver release", "release milestone", "publish release", "cut a release", "ship to users", "go live", "tag version"
- Remove §10 preference loading grep at top
- Replace `Skill(skill="silver:silver-quality-gates", ...)` in Step 0 with "run the silver-quality-gates skill"
- Replace `Skill(skill="gsd-audit-uat", ...)` in Step 1 with "run the gsd-verify skill (UAT mode)"
- Replace `Skill(skill="gsd-audit-milestone", ...)` in Step 2 with "run the gsd-progress skill to audit milestone completion"
- Replace `Skill(skill="silver:security", ...)` in Step 2a with "run the security quality gates dimension"
- Replace `Skill(skill="gsd-plan-milestone-gaps", ...)` in Step 2b gap loop with "run the gsd-plan skill for each gap phase"
- Replace `Skill(skill="silver:feature", ...)` in Step 2b with "run the silver-feature skill for each gap phase"
- Replace `Skill(skill="gsd-docs-update", ...)` in Step 3a with "run the gsd-validate skill to verify documentation accuracy"
- Replace `Skill(skill="/documentation", ...)` in Step 3b with "run the documentation skill"
- Replace `Skill(skill="gsd-milestone-summary", ...)` in Step 4 with "run the gsd-progress skill to generate milestone narrative"
- Replace `Skill(skill="gsd-pr-branch", ...)` in Step 5 with "run the gsd-ship skill with PR branch option"
- Replace `/artifact-reviewer --reviewer review-cross-artifact ...` in Step 6 with "run the artifact-reviewer skill with reviewer=review-cross-artifact on the listed artifacts"
- Replace `Skill(skill="/deploy-checklist", ...)` in Step 6b with "run the deploy-checklist skill"
- Replace `Skill(skill="gsd-ship", ...)` in Step 7 with "run the gsd-ship skill"
- Replace `Skill(skill="gsd-complete-milestone", ...)` in Step 8 with "run the gsd-complete-milestone skill"
- Replace `Skill(skill="silver:silver-create-release", ...)` in Step 9 with "run the silver-create-release skill"
- Replace `design:design-handoff` and `design:design-system` Skill calls in FLOW DESIGN HANDOFF with "run the design-handoff skill" and "run the design-system skill"
- Replace AskUserQuestion in Step 5 (PR branch question) with "ask the user: 'Would you like a clean PR branch (strips .planning/ commits)? A. Yes — run gsd-pr-branch  B. No — release as-is'\nWait for their response."
- Replace AskUserQuestion in Step 2b gap loop limit with an inline question block
- Remove Step-Skip Protocol reference to `silver-bullet.md §10b` — replace with "note the skip preference and proceed"
- Remove Composition Proposal "Auto-Confirm in Autonomous Mode" §10e reference — replace with "In non-interactive mode, auto-confirm the composition proposal"
- Remove "Create WORKFLOW.md" Step 5 reference to `templates/workflow.md.base` — replace with "If .planning/WORKFLOW.md does not exist, create it with: Intent, Composed timestamp, Composer: silver-release, Mode: interactive, and the flow chain"
- Preserve: all step logic, bash detection commands, gap-closure loop iteration tracking, post-release items summary (Step 9b) including all bash commands for session log scanning

Create `forge/skills/silver-migrate/SKILL.md`:
- Frontmatter: id=silver-migrate, title="Silver Migrate — Composable Flows Migration", description="Migrates a pre-v0.20.0 mid-milestone project to composable flows by generating WORKFLOW.md from current artifact state", triggers: "silver migrate", "migrate workflow", "composable flows migration", "generate workflow.md"
- Replace `${PLUGIN_ROOT}/templates/workflow.md.base` reference with "forge/AGENTS.md.template" for context on template format; for the actual workflow.md content, generate it directly from the spec in this skill (or read from `templates/workflow.md.base` if present)
- Note: The migration skill reads STATE.md and scans artifact files — these are project filesystem operations that work identically in Forge
- Replace Step 4 "Ask the user to confirm before writing" — present the content, then "ask the user: 'Does this migration look correct? Reply yes to confirm, or describe any changes needed.'\nWait for their response before writing WORKFLOW.md."
- Preserve: all 17 flow artifact-to-completion mapping table, Step 2 composition determination logic, Step 3 WORKFLOW.md content generation structure, Step 5 write and commit
- Remove "Plugin root" determination section — Forge skills don't use PLUGIN_ROOT; reference template structure inline
  </action>
  <verify>
    <automated>grep -l "^id: silver-release" forge/skills/silver-release/SKILL.md && grep -l "^id: silver-migrate" forge/skills/silver-migrate/SKILL.md && ! grep -r "AskUserQuestion\|Skill(skill=\|silver-bullet\.md §" forge/skills/silver-release/SKILL.md forge/skills/silver-migrate/SKILL.md</automated>
  </verify>
  <done>Both files exist with valid Forge frontmatter, no Claude Code-specific tool references, and all substantive workflow steps preserved</done>
</task>

<task type="auto">
  <name>Task 2: Port silver-update and silver-scan to Forge format</name>
  <files>forge/skills/silver-update/SKILL.md, forge/skills/silver-scan/SKILL.md</files>
  <action>
Read source files `skills/silver-update/SKILL.md` and `skills/silver-scan/SKILL.md`.

Create `forge/skills/silver-update/SKILL.md`:
- Frontmatter: id=silver-update, title="Silver Update — Update Silver Bullet for Forge", description="Checks GitHub for the latest Silver Bullet release, shows changelog since installed version, and updates skill files via forge-sb-install.sh", triggers: "silver update", "update silver bullet", "update skills", "upgrade silver bullet"
- Steps 1-4 (version check, compare, changelog fetch) are PRESERVED as-is — these bash commands work identically in Forge
- Step 4 AskUserQuestion "Proceed with update to vA.B.C?" → replace with "ask the user: 'Proceed with update to vA.B.C? A. Yes, update now  B. No, cancel'\nIf the user cancels, stop."
- Step 5 (Install): Replace `claude mcp install silver-bullet@alo-labs` with:
  ```
  Run forge-sb-install.sh to install the latest skill files:
  ```bash
  curl -fsSL https://raw.githubusercontent.com/alo-exp/silver-bullet/main/forge-sb-install.sh | bash
  ```
  If the command fails (non-zero exit), display the error and stop without proceeding to cleanup.
  ```
- Step 6 (Remove stale legacy entries): Remove entirely — no ~/.claude/plugins/cache/ in Forge; replace with: "Step 6: Verify installation: run `ls ~/forge/skills/ | wc -l` and confirm the skill count increased."
- Step 7 (Display result): Update the result message — change "Restart Claude Desktop" to "Your Forge skills are now updated. The new skill versions are immediately active."
- Remove `~/.claude/plugins/installed_plugins.json` reads in Step 1 — replace with: "Step 1: Check current version from forge-sb-install.sh log or the silver-bullet GitHub releases page. If no version tracking is available locally, treat installed version as unknown and proceed to Step 2."
- Preserve: semver validation logic (Step 2 validation block), version comparison logic (Step 3), changelog fetch (Step 4), final result banner format

Create `forge/skills/silver-scan/SKILL.md`:
- Frontmatter: id=silver-scan, title="Silver Scan — Retrospective Session Log Scan", description="Scans all project session logs to surface unaddressed deferred items and unrecorded knowledge/lessons insights, with human Y/n gating before filing", triggers: "silver scan", "scan sessions", "retrospective scan", "scan session logs", "find deferred items"
- Preserve the Security Boundary section verbatim (UNTRUSTED DATA handling, path validation, fixed-string grep)
- Preserve the Allowed Commands section verbatim
- Preserve all 9 Steps with their full bash commands and logic
- Replace `AskUserQuestion` in Step 6 (deferred item Y/n) with "ask the user: 'File this item? [Y to file via silver-add / n to skip]'\nIf Y: run the silver-add skill passing ITEM_TITLE + ITEM_CONTEXT as the description.\nIf n: skip."
- Replace `AskUserQuestion` in Step 8 (knowledge/lessons Y/n) with "ask the user: 'Record this insight? [Y to record via silver-rem / n to skip]'\nIf Y: run the silver-rem skill passing the full insight text.\nIf n: skip."
- Replace `Skill(skill="/silver-add", ...)` in Step 6 with "run the silver-add skill"
- Replace `Skill(skill="/silver-rem", ...)` in Step 8 with "run the silver-rem skill"
- Preserve: Step 1 project root detection, Step 2 session enumeration, Step 3 deferred-item signal extraction (structured sections + keyword grep), Step 4 cross-reference (git log, CHANGELOG, GitHub issues, local tracker), Step 5 candidate cap (20), Step 6 presentation loop, Step 7 knowledge/lessons scan, Step 8 presentation loop, Step 9 summary format, all edge cases
  </action>
  <verify>
    <automated>grep -l "^id: silver-update" forge/skills/silver-update/SKILL.md && grep -l "^id: silver-scan" forge/skills/silver-scan/SKILL.md && ! grep -r "AskUserQuestion\|Skill(skill=\|installed_plugins\.json\|claude mcp install" forge/skills/silver-update/SKILL.md forge/skills/silver-scan/SKILL.md</automated>
  </verify>
  <done>Both files exist with valid Forge frontmatter; silver-update instructs re-running forge-sb-install.sh; silver-scan preserves full security boundary and all 9 steps; no Claude Code-specific tool references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session log content → silver-scan | Session logs are UNTRUSTED DATA — security boundary preserved from source |
| git commit messages → silver-create-release | Commit subjects are UNTRUSTED DATA — backtick sanitization preserved from source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-065-04 | Tampering | silver-scan session log path validation | mitigate | Preserve verbatim: "paths must match docs/sessions/[^/]+\.md; reject any path containing .. or absolute path components" |
| T-065-05 | Information Disclosure | silver-release Step 9b session log scan | accept | Session logs are local project files; bash commands read them as data only |
| T-065-06 | Spoofing | silver-update version string from GitHub | mitigate | Preserve semver validation: `^[0-9]+\.[0-9]+\.[0-9]+$` check before using LATEST in any command |
</threat_model>

<verification>
After task completion, verify:
1. `ls forge/skills/silver-release/SKILL.md forge/skills/silver-migrate/SKILL.md forge/skills/silver-update/SKILL.md forge/skills/silver-scan/SKILL.md` — all 4 files exist
2. `grep "forge-sb-install" forge/skills/silver-update/SKILL.md` — update adapted for Forge
3. `grep "UNTRUSTED DATA" forge/skills/silver-scan/SKILL.md` — security boundary preserved
4. `grep -c "^trigger:" forge/skills/silver-release/SKILL.md forge/skills/silver-migrate/SKILL.md forge/skills/silver-update/SKILL.md forge/skills/silver-scan/SKILL.md` — each shows 1 (trigger array present)
5. No file contains `AskUserQuestion`, `Skill(skill=`, `installed_plugins.json`, or `claude mcp install`
</verification>

<success_criteria>
- 4 Forge SKILL.md files exist under forge/skills/ for PORT-SB-06 through PORT-SB-09
- Each file has Forge frontmatter: id, title, description, trigger[]
- silver-update instructs re-running forge-sb-install.sh instead of claude mcp install
- silver-scan security boundary (UNTRUSTED DATA, path validation, fixed-string grep) preserved verbatim
- silver-release full release chain (Steps 0-9b) preserved with all skill invocations converted to trigger-based
- No Claude Code-specific tool references anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/065-sb-core-skills-port/065-02-SUMMARY.md`
</output>
