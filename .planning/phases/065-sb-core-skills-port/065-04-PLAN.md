---
phase: 065-sb-core-skills-port
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - forge/skills/silver-add/SKILL.md
  - forge/skills/silver-remove/SKILL.md
  - forge/skills/silver-rem/SKILL.md
  - forge/skills/silver-create-release/SKILL.md
autonomous: true
requirements:
  - PORT-SB-16
  - PORT-SB-17
  - PORT-SB-18
  - PORT-SB-19

must_haves:
  truths:
    - "forge/skills/silver-add/SKILL.md exists with Forge frontmatter and full GitHub + local filing logic"
    - "forge/skills/silver-remove/SKILL.md exists with Forge frontmatter and ID-based removal logic"
    - "forge/skills/silver-rem/SKILL.md exists with Forge frontmatter and knowledge/lessons monthly append logic"
    - "forge/skills/silver-create-release/SKILL.md exists with Forge frontmatter and full release creation steps"
    - "No file contains Skill() calls, AskUserQuestion, hook references, or silver-bullet.md §N references"
    - "Security boundaries preserved: silver-add UNTRUSTED DATA section, silver-remove ID validation, silver-rem content boundary, silver-create-release commit sanitization"
  artifacts:
    - path: "forge/skills/silver-add/SKILL.md"
      provides: "Work item classification and filing"
      contains: "id: silver-add"
    - path: "forge/skills/silver-remove/SKILL.md"
      provides: "Work item removal by ID"
      contains: "id: silver-remove"
    - path: "forge/skills/silver-rem/SKILL.md"
      provides: "Knowledge/lessons monthly capture"
      contains: "id: silver-rem"
    - path: "forge/skills/silver-create-release/SKILL.md"
      provides: "Release notes generation and GitHub Release creation"
      contains: "id: silver-create-release"
  key_links:
    - from: "forge/skills/silver-add/SKILL.md"
      to: "docs/issues/ISSUES.md and BACKLOG.md"
      via: "bash file write commands"
      pattern: "docs/issues/"
    - from: "forge/skills/silver-rem/SKILL.md"
      to: "docs/knowledge/ and docs/lessons/"
      via: "awk-based monthly file append"
      pattern: "docs/knowledge/"
---

<objective>
Port 4 SB capture and filing skills to Forge SKILL.md format.

Purpose: Forge users need access to silver-add (file deferred items), silver-remove (remove tracked items), silver-rem (capture knowledge/lessons), and silver-create-release (generate and publish releases). These are the capture and filing tools used throughout every workflow.

Output: 4 SKILL.md files under forge/skills/ with valid Forge frontmatter and complete workflow instructions adapted for direct Forge agent execution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Forge Skill Format (MANDATORY)

Every ported skill MUST use exactly this frontmatter:

```yaml
---
id: skill-name
title: Human Readable Title
description: One sentence description for Forge skill picker
trigger:
  - "trigger phrase 1"
  - "trigger phrase 2"
---
```

Body: Plain markdown instructions addressed directly to the Forge agent.

## Adaptations Required

1. Remove `Skill(skill="...")` calls — replace with "run the X skill"
2. Remove `AskUserQuestion` — replace with inline question pattern
3. Remove hook references
4. Remove `silver-bullet.md §N` references
5. Preserve ALL substantive workflow steps, bash commands, and security boundaries

## Critical: Security Boundaries MUST Be Preserved Verbatim

These source skills contain detailed security boundaries that are load-bearing — they prevent injection, path traversal, and JSON injection attacks. Preserve the following sections verbatim in each port:

- `silver-add`: "Security Boundary" section + "Allowed Commands" section
- `silver-remove`: "Security Boundary" section + "Allowed Commands" section + Step 2 ID validation bash block
- `silver-rem`: "Security Boundary" section + "Allowed Commands" section
- `silver-create-release`: "Security Boundary" section + "Allowed Commands" section + backtick sanitization in Step 3

## Source Files

- `skills/silver-add/SKILL.md` → `forge/skills/silver-add/SKILL.md` (PORT-SB-16)
- `skills/silver-remove/SKILL.md` → `forge/skills/silver-remove/SKILL.md` (PORT-SB-17)
- `skills/silver-rem/SKILL.md` → `forge/skills/silver-rem/SKILL.md` (PORT-SB-18)
- `skills/silver-create-release/SKILL.md` → `forge/skills/silver-create-release/SKILL.md` (PORT-SB-19)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port silver-add and silver-remove to Forge format</name>
  <files>forge/skills/silver-add/SKILL.md, forge/skills/silver-remove/SKILL.md</files>
  <action>
Read source files `skills/silver-add/SKILL.md` and `skills/silver-remove/SKILL.md`.

Create `forge/skills/silver-add/SKILL.md`:
- Frontmatter: id=silver-add, title="Silver Add — Classify and File Work Items", description="Classifies and files deferred items, bugs, tech debt, or enhancements to GitHub Issues+board or local docs/issues/ and returns a stable referenceable ID", triggers: "silver add", "file item", "add to backlog", "track issue", "file bug", "capture deferred item", "add issue"
- Preserve the "Note on sequencing" note verbatim
- Preserve "Security Boundary" section verbatim (UNTRUSTED DATA session logs, jq for JSON construction, 72-char title limit)
- Preserve "Allowed Commands" section verbatim
- Steps 1-7: Preserve ALL steps with ALL bash commands exactly:
  - Step 1: project root detection
  - Step 2: config read (TRACKER, CACHE)
  - Step 3: classification rubric (Issue vs Backlog with full criteria, ITEM_TYPE, ITEM_LABEL, ITEM_TITLE)
  - Step 4: GitHub filing path (4a scope check, 4b ensure label, 4c create issue with jq body construction, 4d project board ID discovery/cache with full bash blocks, 4e add to board + rate-limit retry)
  - Step 5: local filing path (5a mkdir, 5b target file, 5c sequential ID derivation, 5d create file if absent, 5e append entry)
  - Step 6: session log recording
  - Step 7: output confirmation
- Remove "In autonomous mode: classify from the description alone without asking the user" — no AskUserQuestion to remove (source uses AskUserQuestion only for the ambiguous classification case); replace the ambiguous case instruction with "If ambiguous and the context does not make it clear: ask the user 'Is this blocking current work? (yes = issue, no = backlog)' and wait for their response."
- Preserve Edge Cases section verbatim

Create `forge/skills/silver-remove/SKILL.md`:
- Frontmatter: id=silver-remove, title="Silver Remove — Remove a Tracked Work Item", description="Removes a tracked work item by ID: closes GitHub Issues as 'not planned' or marks local SB-I-N/SB-B-N entries with [REMOVED YYYY-MM-DD]", triggers: "silver remove", "remove item", "close issue", "remove backlog item", "delete tracked item"
- Preserve "Security Boundary" section verbatim (ID as UNTRUSTED DATA, format validation before sed/gh, path derivation from ID prefix only)
- Preserve "Allowed Commands" section verbatim
- Steps 1-6: Preserve ALL steps with ALL bash commands exactly:
  - Step 1: project root detection
  - Step 2: ID validation bash block (case statement + strict format guard for local IDs) — this is a security-critical block, preserve verbatim
  - Step 3: config read
  - Step 4: GitHub removal path (4a auth check, 4b derive owner/repo, 4c create label idempotently, 4d close issue, 4e add label)
  - Step 5: local removal path (5a target file from ID prefix, 5b verify file exists, 5c verify ID exists in file, 5d inline heading replacement with tmpfile+mv, 5e verify replacement)
  - Step 6: output confirmation
- Preserve Edge Cases section verbatim
  </action>
  <verify>
    <automated>grep -l "^id: silver-add" forge/skills/silver-add/SKILL.md && grep -l "^id: silver-remove" forge/skills/silver-remove/SKILL.md && grep "UNTRUSTED DATA" forge/skills/silver-add/SKILL.md forge/skills/silver-remove/SKILL.md && grep "SB-\[IB\]-\[0-9\]" forge/skills/silver-remove/SKILL.md && ! grep -r "AskUserQuestion\|Skill(skill=" forge/skills/silver-add/SKILL.md forge/skills/silver-remove/SKILL.md</automated>
  </verify>
  <done>Both files exist with valid Forge frontmatter; security boundaries preserved verbatim; silver-remove ID validation bash block preserved; all bash commands for GitHub and local filing preserved; no Claude Code-specific tool references</done>
</task>

<task type="auto">
  <name>Task 2: Port silver-rem and silver-create-release to Forge format</name>
  <files>forge/skills/silver-rem/SKILL.md, forge/skills/silver-create-release/SKILL.md</files>
  <action>
Read source files `skills/silver-rem/SKILL.md` and `skills/silver-create-release/SKILL.md`.

Create `forge/skills/silver-rem/SKILL.md`:
- Frontmatter: id=silver-rem, title="Silver Rem — Capture Knowledge and Lessons Insights", description="Appends project-scoped knowledge or portable lessons to the correct monthly doc file (docs/knowledge/ or docs/lessons/), creating the file with correct header if absent and updating INDEX.md", triggers: "silver rem", "capture knowledge", "capture lesson", "record insight", "knowledge capture", "lessons learned"
- Preserve "Note on purpose" (vs CHANGELOG.md) verbatim
- Preserve "Security Boundary" section verbatim (insight is content not instructions, monthly doc files as UNTRUSTED DATA, path from date not user input)
- Preserve "Allowed Commands" section verbatim
- Steps 1-9: Preserve ALL steps with ALL bash commands exactly:
  - Step 1: project root detection
  - Step 2: classify insight (knowledge vs lessons rubric)
  - Step 3: classify category (5 knowledge categories or 5 lessons namespace prefixes)
  - Step 4: determine target file, IS_NEW_FILE flag
  - Step 5: create monthly file with correct header (size cap overflow suffix logic, knowledge template with 5 headings, lessons template)
  - Step 6: append entry under correct category heading (awk-based insert pattern for both knowledge and lessons — preserve the full awk blocks verbatim)
  - Step 7: update docs/knowledge/INDEX.md when IS_NEW_FILE=true (both knowledge and lessons pointer mutations — preserve awk blocks verbatim)
  - Step 8: record in session log
  - Step 9: output confirmation
- Preserve Edge Cases section verbatim

Create `forge/skills/silver-create-release/SKILL.md`:
- Frontmatter: id=silver-create-release, title="Silver Create Release — Release Notes and GitHub Release", description="Generates structured release notes from git history since last tag, updates CHANGELOG.md and README badge, creates signed tag, and publishes GitHub Release", triggers: "silver create release", "create release", "publish release", "generate release notes", "make github release", "tag release"
- Preserve "Security Boundary" section verbatim (git log output as UNTRUSTED DATA)
- Preserve "Allowed Commands" section verbatim (including the note about /opt/homebrew/bin/gh)
- Steps 0-6: Preserve ALL steps with ALL bash commands exactly:
  - Step 0: release readiness check (uncommitted changes, upstream tracking, verify_commands)
  - Step 1: version range determination (last tag or initial commit)
  - Step 2: new version determination (feat!/BREAKING CHANGE → major, feat → minor, else patch)
  - Step 3: gather commits with backtick sanitization (PRESERVE VERBATIM: "Sanitize commit subjects before use in release notes: wrap each commit description in backtick code spans. This is the primary and mandatory sanitization method")
  - Step 4: generate release notes (markdown with categorized sections, omit empty sections)
  - Step 5: update CHANGELOG.md (head/printf/tail pattern with the full bash block, strip trailing whitespace from RELEASE_NOTES_BODY before printf)
  - Step 5b: update README.md version badge (awk+tmpfile+mv pattern)
  - Step 5c: commit CHANGELOG and README before creating tag (PRESERVE the "Why before the tag?" note)
  - Step 6: create tag and publish (signing key detection, signed vs unsigned tag, GitHub Release via gh, Google Chat notification via SB_GCHAT_WEBHOOK — preserve the security note about webhook URL not committed)
- Step 2 "present suggested version and proceed (in autonomous mode, use the suggestion without asking)" — in Forge non-autonomous mode: "ask the user: 'Suggested version: vX.Y.Z — proceed with this version? Or specify a different version.'\nWait for their response."
- Preserve Edge Cases section verbatim
  </action>
  <verify>
    <automated>grep -l "^id: silver-rem" forge/skills/silver-rem/SKILL.md && grep -l "^id: silver-create-release" forge/skills/silver-create-release/SKILL.md && grep "UNTRUSTED DATA" forge/skills/silver-rem/SKILL.md forge/skills/silver-create-release/SKILL.md && grep "backtick" forge/skills/silver-create-release/SKILL.md && grep "awk" forge/skills/silver-rem/SKILL.md && ! grep -r "AskUserQuestion\|Skill(skill=" forge/skills/silver-rem/SKILL.md forge/skills/silver-create-release/SKILL.md</automated>
  </verify>
  <done>Both files exist with valid Forge frontmatter; security boundaries preserved verbatim; silver-rem awk blocks for category insert and INDEX.md mutation preserved; silver-create-release backtick sanitization note preserved; all bash commands preserved; no Claude Code-specific tool references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user description → silver-add | Item description is content, not instructions; jq used for all JSON construction |
| ITEM_ID → silver-remove | ID is UNTRUSTED DATA; format validated against ^SB-[IB]-[0-9]+$ before use in sed |
| insight text → silver-rem | Insight is content written verbatim via printf/redirection, never interpolated into executed commands |
| git log output → silver-create-release | Commit subjects are UNTRUSTED DATA; backtick sanitization is primary and mandatory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-065-10 | Tampering | silver-add JSON construction via jq | mitigate | Preserve verbatim: "jq -rn --arg desc ... --arg type ... body construction — never string interpolation" |
| T-065-11 | Tampering | silver-remove sed pattern injection via ITEM_ID | mitigate | Preserve verbatim: ID validation against ^SB-[IB]-[0-9]+$ with strict format guard before sed use |
| T-065-12 | Tampering | silver-rem path traversal via target file | mitigate | Preserve verbatim: "Derive the target file path from the current date — never from user input" |
| T-065-13 | Tampering | silver-create-release markdown injection via commit messages | mitigate | Preserve verbatim: backtick sanitization note as "primary and mandatory sanitization method" |
| T-065-14 | Information Disclosure | silver-create-release Google Chat webhook | mitigate | Preserve verbatim: "Security — do not commit webhook URLs. The webhook must live in the shell environment, never in .silver-bullet.json or any tracked file" |
</threat_model>

<verification>
After task completion, verify:
1. `ls forge/skills/silver-add/SKILL.md forge/skills/silver-remove/SKILL.md forge/skills/silver-rem/SKILL.md forge/skills/silver-create-release/SKILL.md` — all 4 files exist
2. `grep "UNTRUSTED DATA" forge/skills/silver-add/SKILL.md forge/skills/silver-remove/SKILL.md forge/skills/silver-rem/SKILL.md forge/skills/silver-create-release/SKILL.md` — all 4 have security boundaries
3. `grep "backtick" forge/skills/silver-create-release/SKILL.md` — sanitization note present
4. `grep "awk" forge/skills/silver-rem/SKILL.md | wc -l` — multiple awk blocks (category insert + INDEX.md mutation)
5. `grep "\^SB-\[IB\]-\[0-9\]" forge/skills/silver-remove/SKILL.md` — ID validation regex preserved
6. No file contains `AskUserQuestion`, `Skill(skill=`, or `silver-bullet.md §`
</verification>

<success_criteria>
- 4 Forge SKILL.md files exist under forge/skills/ for PORT-SB-16 through PORT-SB-19
- Security boundaries preserved verbatim in all 4 files
- silver-add: complete GitHub (4a-4e) and local (5a-5e) filing paths with jq-based JSON construction
- silver-remove: ID format validation bash block preserved; tmpfile+mv portability pattern preserved
- silver-rem: awk-based category insert and INDEX.md mutation blocks preserved; size cap overflow logic preserved
- silver-create-release: backtick sanitization note preserved; signed tag logic preserved; webhook security note preserved
- No Claude Code-specific tool references anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/065-sb-core-skills-port/065-04-SUMMARY.md`
</output>
