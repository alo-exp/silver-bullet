# Clarify Brief — router_subagent_surfaces_85bf9f09

> **The architecture spec supersedes contradicting clarify Qs.** Canonical text: [`.planning/router_subagent_surfaces_85bf9f09.plan.md`](.planning/router_subagent_surfaces_85bf9f09.plan.md) (byte-identical with [`~/.cursor/plans/router_subagent_surfaces_85bf9f09.plan.md`](/Users/shafqat/.cursor/plans/router_subagent_surfaces_85bf9f09.plan.md)). History below is retained and marked superseded. **2026-08-16 SUPERSEDE (round-25)** on SHA `701cc66466830161476597af35606168377acb2437088fb86787eec0f6fcc884` (both plan copies byte-identical): GPT Max H-1 WS1 owns `scripts/lib/apo_delegate_catalog.py` + `scripts/lib/apo_multi_ai_catalog.py` (`/silver:*` / `$silver:*` → `/sb:` / `$sb:`) + Opus Extra High re-verify B-1 catalog is generated (`generate-apo-catalog.py` + parity gate; stop hand-edit SOT) + H-1 one `ART-AGENT-DELEGATE` (hyphen) + M-1 OpenCode/Pi `artifact_refs` = `ART-AGENT-DELEGATE`. Schema unchanged; no second AF/WF; `nested_executor` lock-only; public `/sb` only (no dual `/silver` window); catalog ids `WF-SILVER-*` may remain; `tests/run-all-tests.sh` remains the release gate and is **named** in WS1. Max **not** re-launched. Prior same-day Round-24 ACCEPT on SHA `1096479cc1deba1b902ca501e8d7b7b1c0c1ba5f23510f8776c098f6913002d5` (both plan copies byte-identical): GPT Extra High re-verify H-1 nested-Task `worktree_cwd` declare/stamp/compare (mismatch → row 4; consume never ran must not bypass) + H-2 Doctor vs inspect-only (Init/Doctor/SessionStart MAY write HNEST-01/HINST-01; inspect-only = unrelated IDE prefs) + M-1 GST tombstones beyond N-1 (historical day files that still exist, or durable tombstone index; pin `VAL/TST-RFL-621`) + Opus Extra High re-verify H-3 generated APO artifacts are WS1-owned + H-4 post-exec `silver:` literals in generator + parity tests + M-3 delegation step↔flow reciprocity. Schema unchanged; no second AF/WF; `nested_executor` lock-only; public `/sb` only (no dual `/silver` window); `tests/run-all-tests.sh` remains the release gate and is **named** in WS1. Extra High Max **not** started. Prior same-day Round-23 ACCEPT on SHA `c82e1b3bccc5625e9cb4fe2cc7518a577fdd69af225c062c08b6cd6d5ac7a472` (both plan copies byte-identical): GPT Extra High H-1 `worktree_cwd` declare-at-admit / stamp-at-consume (envelope metadata; not inner-prompt bytes; not hashed into `prompt_hash`; mismatch vs admit or vs hashed `scope_bounds`/WBS tree → row 4) + Opus Extra High B-1 alias-map/hardcoded-check ownership + H-1 OpenCode/Pi attach to shipped AF + H-2 `nested_executor` lock-only + M-1 row 19 computed-vs-declared + M-2 `WF-AGENT-DELEGATE-ENTRY.owning_skill`. Schema unchanged; no second agent-delegate AF/WF; public `/sb:agent-*` in-plan Executor mint stays; HINST 3+2 stays; `prompt_hash` inner-only. Extra High Max **not** started. Prior same-day Round-22 ACCEPT on SHA `67eff63eb035db3586cb0b0faeb55088b05dd86ea71651773b488b47c23efd75` (both plan copies byte-identical): cold `/sb:agent-*` **dispatches shipped** `WF-AGENT-DELEGATE-ENTRY` / `AF-AGENT-DELEGATE` / `VL-AF-AGENT-DELEGATE` / `ART-AGENT-DELEGATE` / `FS-DELEGATE-*` (do **not** add a second wrap; `sb:agent-wrap` is an alias); `nested_executor` is the five `/sb:agent-*` **leaf** class; WS1 completeness scoped to records this ship changes; `router-coverage` keep; retain `WF-SILVER-ROUTER` / `AF-ROUTE`; public `/sb`; Val **step** (not column); `prompt_hash` binds inner prompt bytes only. Extra High not started. Prior same-day Round-20 ACCEPT 2026-08-16 (Extra High CLEAN on SHA `b673de8c…40c18`, M-A–M-C: row 42 spawn target includes non-parent; `WF-SILVER-NEW-WORKFLOW` Advisor-compose-gated; WS2 retires queue-builder in source + `sync-templates.sh` mirror + installed copy). Prior same-day Round-19 ACCEPT 2026-08-16 (Extra High re-verify on SHA `65fde3d6…c7fd8c`, H-1–H-2 / M-1–M-2: authoring session is a Job with Advisor compose; rows 11/12 carved for 41/42; Codex `unbounded` consumer semantics; replacement-ladder probe). Prior same-day Round-18 ACCEPT 2026-08-16 (`/silver:new-workflow` **output compliance** — generator emits spec-compliant WFs; retract “Advisor invoke only” as the primary lock). Prior same-day Round-17 ACCEPT (Extra High re-verify H-1–H-5 / M-1–M-4). Prior same-day Round-16 ACCEPT 2026-08-16 (retract catalog-only agent-* invent ban; agent-* Executor in-plan WF mint; HINST-01 three-host install ensure + OpenCode/Pi instruction bundle). Prior same-day Round-15 ACCEPT (role-gated WF mint/invoke; Extra High H-1–H-5 / M-1–M-5 retained + nested-WF maximum dynamicity + host-settings lock revoked). **User spec 2026-08-16** (plan-time Validation-loop + Ver/Val loop names + Val-fail Advisor re-plan; **later same day:** composition-Val vs user intent after `/sb` mint + fresh AF/WF synthesis + PUB-01/H1 binding + Board of Advisors + host built-in defaults + Board members in parallel; **later same day:** Global Status team dashboard — Job = user intent; `.sb/status/STATUS-YYMMDD.md` on `origin/main`; does not replace local ASCII WBS viz; **later same day:** trivial/Q&A classification at `/sb` resolve — reuse catalog `sb:fast` (`WF-SILVER-FAST`) / `AF-FAST-PATH`; remint must not enter synthesize; **later same day SUPERSEDE:** FAST/trivial **exempt** from quality-order roles except Orchestrator classify + Authorizer-admitted FAST Executor/Q&A leaf; **not a Job**; **must not** appear on GST-01; **later same day:** current-month files are a **context-load cap** (Graphify searches all months; INDEX + this month is the fallback hot set; older `YYYY-MM.md` not discarded); FAST thin capture after the FAST leaf (AM opted in → `memory_save` then classify/promote; AM not opted in → `kl_write_am_skipped`; durable promote to K/L; not a second Job); AM is capture buffer, K/L is git SoT (promote AM→K/L; no dual-write; `synergy_max` AM auto-commit is not the K/L path); **later same day SUPERSEDE:** AM-first fail-closed `kl_write` provenance (`memory_save` first, same durable text, then classify, then promote; AM opted-in failure / missing `am_captured` → `blocked_knowledge_postwrite`, do not write K/L anyway; AM not opted in → `kl_write_am_skipped`; no direct K/L Write except via this leaf); team Graphify fan-out (do **not** reload K/L into each clone's agentmemory; optional thin pointer only); **later same day SUPERSEDE (Opus High ladder-3 ACCEPT):** classified-trivial **reuses** locked IDs but **this ship amends** APO `WF-SILVER-FAST` / `AF-FAST-PATH` / `PP-SB-STARTUP-FAST` so generation cannot emit old V-loop/composition obligations for classified-trivial (live composition is **`AF-FAST-PATH` only**; catalog extra AFs must not run; `PP-SB-STARTUP-FAST` is not the Q&A classifier); GST push failure **degrades** (`gst_stale`; Job continues; row 34 dashboard-only / not a Job hard-stop; dashboard freshness is not a `scope_complete` gate; round-6 merge-by `(instance_id, job_id)` is superseded); **later same day SUPERSEDE (Opus Extra High ladder-3 ACCEPT):** classified-trivial exemption is enforced in `scripts/check-apo-invariants.py` **and** the generators (WS1); `docs/apo-catalog.schema.json` **unchanged**; must-not-run extra AFs include `AF-EXECUTE`; overlay also skips emit/enforce of `prerequisites` / `exit_condition` / `flow_steps` V-loops (`FS-SILVER_*` / `VL-FS-SILVER_FAST`) / `execution.join_condition` / `artifacts`/`evidence_refs` as required V-evidence / `capability_class: bounded_fast_path` as a quality-order trigger; `PP-SB-STARTUP-FAST` rewrite `override_rules[0]` to prefer `WF-SILVER-FEATURE` for 3-day greenfield / durable edits; FAST operator surfaces include the thin-capture deny-all node; `VAL/TST-RFL-621` owns the protected-`main` / no-push-rights `gst_stale` + Job-continues fixture; Board of one: unifier leaf still launches (identity-preserving / no-op unify; not last-write-wins))) is listed in the superseded banner and wins over contradicting text below.
>
> **Superseded (do not implement these answers):**
> - **Q4** dual-prefix during migrate — no public dual `/silver` window; bootstrap is `bash scripts/sb-migrate-from-silver.sh` plus plugin postinstall, not only `/sb:migrate`.
> - **Q5** day-1 Cursor+Codex+Claude hosts — MVP is Cursor host adapter; Codex/Claude/OpenCode host adapters after MVP.
> - **Q7** hard-cut delete public RFL — keep `sb:review-fix-ladder` until Iterate exists (Verifier+Process-final-Val path or thin alias).
> - **Q9c** trust path `host/org/repo` as CAS — Authorizer trust is `~/.silver-bullet/authorizer-trust/<repo-id>/`; host is metadata, not a second CAS.
> - **Q11** ship-everything / pending lock — MVP slice as named in the spec.
> - **Q12** I-loop two consecutive clean — spec-wins: I has no self-attested two-clean; two-clean applies to A (including Process-scope A), Verification (including Process-scope Verification), composition Validation-loop (vs user intent), plan-time Validation-loop (vs work spec), and Process-final Val (when product Val runs).
> - **Q14** AF Val after merge — AF does Verification, not product Val; product Val is Process-final only. **2026-08-16:** plan-time Validation-loop still runs after each Advisor plan vs the work spec (not AF/Workflow product Val).
> - **Q18** `val_*` on ordinary AF/Workflow SM — canonical order plan-time Val → I → A → Verification at AF/Workflow; after the top Workflow join: Process-synthesis I → Process-scope A/Verification (mandatory) → Process-final Val. Ordinary AF/Workflow SM still has no product `val_*`.
> - **Q21** “Val always after V” at AF (answer B) — product Val is Process-final only; AF/Workflow run Verification. **2026-08-16 SUPERSEDE:** plan-time Validation-loop after Advisor plan vs work spec is now required (distinct from product Val).
> - **Q22** AF + Workflow + Process all mandatory product Val — only Process runs product Val. **2026-08-16 SUPERSEDE:** plan-time Validation-loop (plan vs work spec) is additional and is not AF/Workflow product Val.
> - **Note 15** path-as-CAS / `remote_id_sha256` suffix — trust path remains `~/.silver-bullet/authorizer-trust/<repo-id>/`.
> - Universal Advisor “Validation-loop mandatory at AF, Workflow, and Process”.
> - Toolstack “Five-tool is Cursor opt-in” / Graphify miss not blocked on other hosts — opt-in then mandatory on every selected runtime after init probe.
> - Round-5 “Orchestrator session is the only Task/Agent syscall” — nested Task is allowed only for descendants whose work-spec the ancestor already persisted before this child's Task started; parent-proxy for new projector writes at any remaining depth, and always at remaining depth 0.
> - Recommended-defaults “Advisor strictly stronger than that scope’s executor” — **SUPERSEDED / non-binding.** Architecture spec wins: same `{ runtime, model, effort }` across Advisor and Executor is allowed; row 14 is retired (Init/Doctor warn only, never identity-equality hard-stop).
> - Round-5 “merge then AF Val” — do not un-merge; product Val again only at Process-final; Orchestrator+Advisor map the fail receipt (Validator does not walk WBS). **2026-08-16 SUPERSEDE:** Val-fail is not fail-receipt-only / no re-plan. A Validation-loop starts with the Advisor, who re-plans to address the gaps, then plan-time Validation-loop, then implementation as the quality order requires. Keep Val-fail `launch_id` / occurrence / 9a–9c.
> - **2026-08-16 user spec (plan-time Validation-loop + loop names + Val-fail re-plan).** Spec-wins: after each Advisor plan, Validator runs a plan-time Validation-loop (Val-loop) vs the **work spec** (gap analysis, flaws, enhancement feedback → Advisor revises; two-clean spirit until the plan satisfies the work spec; Executor I proceeds only on the validated plan). Process-final Validation still checks the **entire output vs original user intent**, once at roll-up. If it does not satisfy original intent, a Validation-loop starts with the Advisor (re-plan → plan-time Validation-loop → implementation as the existing quality order). Verifier’s loop = **Verification-loop (Ver-loop)** — never a bare “V-loop” in new/edited text. Validator’s loop = **Validation-loop (Val-loop)**. Advisor A-loop Mentorship after I stays. No A on ESC-02 steps 2–3. Same model across roles remains allowed (row 14 retired). Keep `process_v_verified` (not `process_v_two_clean`). Process-final Val states remain `val_running` / `val_two_clean` / `val_validated`. Plan-time Val states: `plan_val_running` / `plan_val_two_clean` / `plan_val_verified`.
> - **2026-08-16 SUPERSEDE (composition-Val + fresh AF/WF synthesis).** Spec-wins: wrapping user-intent Workflow still mints at `/sb` Process resolve/wrap (do **not** catalog the six-role loop as AF/WF; no `AF-meta-six-role`). After mint (static catalog WF **or** dynamic composition of existing AFs), composition Validation-loop checks whether that WF will satisfy **user intent** — distinct from plan-time Val (plan vs work spec) and Process-final Val (output vs original intent). Fail returns to `/sb` (Orchestrator) remint, **not** Executor. If catalog + pre-existing AF composition cannot cover intent at 100%, `/sb` **must** synthesize new AFs/WFs (new + pre-existing) under 100% process-administered coverage; **forbidden** to execute user-intent work with no AF coverage. Remint order: reuse catalog → compose existing AFs → synthesize. Loop until `comp_val_verified`, then Advisor → plan-time Val → I (`plan_val_verified`). Bound: `comp_val_round` ceiling 8 / `launch_intent` deadline → row 13 `blocked_validation_state`, not ESC-02 (no new sibling row). Orchestrator owns remint; Advisor may draft AF/WF definitions; Validator approves composition vs intent; Authorizer admits synthesis/planning/exec spawns, does not mint WFs, is not Approver. Remint **may rewrite WBS before Executor spawn**; this supersedes any reading that WBS-from-intent at Process resolve is frozen before composition-Val. Executor still only after `plan_val_verified`. Local synthesize path is in this plan (install-global `~/.silver-bullet/flows/` or project-level `$primary_checkout/.silver-bullet/flows/`; prefer generic). Unsigned/untrusted import fail-closed. Global AI Flows marketplace is later version, not MVP-block. Projector-attested receipts, consume journal, and `plan_val_verified` on mid-flight replacement remain.
> - **2026-08-16 SUPERSEDE (B1 PUB-01 + H1 VALP-01/CORR-17 + Board of Advisors + host defaults).** Spec-wins: synthesized AFs/WFs publish only via Authorizer-admitted `hooks/lib/sb-flow-publisher.sh` (same helper class as `wbs-projector.sh`; Advisor drafts; Orchestrator does not Write AF/WF files; children Write `.silver-bullet/` only via named helpers; stage→validate→promote; register `sb:<route>` / deny-graph / in-session Cursor overlay; collision different-hash → row 1; before Executor I; parent≠implementer; no second Process; no uncovered tasks). `comp_val_verified` binds `original_intent_hash` + `candidate_workflow_hash` + ordered AF/definition closure + `composition_generation`; remint fences prior-generation clean composition receipts (CORR-17). Advisor preference key is a Board of one or more members; each does Advisor duties; members MUST launch and work in parallel (Authorizer admits each; Task-capable ancestor starts concurrently; `advisor_board_unify` after all member outputs); Process consumes one named `advisor_board_unify` result (not last-write-wins; bounded conflict → `blocked_plan_of_action_review` row 6, not retired row 14); each member Authorizer-admitted deny-all Advisor leaf; no recursive I/A/Verification/Val on Board internals. First `/sb:init` or any later init if prefs missing: user picks agent+model+effort per preference-key role (`host_native` or `/sb:agent-claude` / `/sb:agent-codex` / `/sb:agent-opencode`). Authorizer is not a preference key. Codex/Claude/Cursor host built-in defaults are not mandates (Codex: Orchestrator GPT-5.6 Luna XHigh manual session, Executor GPT-5.6 Luna Max, Advisor Board GPT-5.6 Sol High + Opus 5 High `/sb:agent-claude`, Verifier Opus 5 High `/sb:agent-claude`, Validator Opus 5 XHigh `/sb:agent-claude`; Claude: Orchestrator Opus 5 Med, Executor Opus 5 High, Advisor Board GPT-5.6 Sol XHigh `/sb:agent-codex` + Opus 5 XHigh `/sb:agent-claude`, Verifier GPT-5.6 Sol High `/sb:agent-codex`, Validator GPT-5.6 Sol Max `/sb:agent-codex`; Cursor: Orchestrator Composer 2.5, Executor Grok 4.6 High, Advisor Board GPT-5.6 Sol Max `/sb:agent-codex` + Opus 5 High `/sb:agent-claude`, Verifier Gemini 3.7 Flash, Validator Opus 5 XHigh `/sb:agent-claude`). OpenCode has no baked Board table; init picker still applies. Same model across roles remains allowed. Keep: no A on ESC-02; row 14 retired; `process_v_verified`; Authorizer not Approver; no bare V-loop; no `AF-meta-six-role`; wrap at `/sb`.
> - **2026-08-16 SUPERSEDE (Board of Advisors parallel members).** Spec-wins: Board of Advisors members MUST launch and work in parallel, not sequentially. Authorizer admits each member spawn; a Task-capable ancestor starts them concurrently (same spirit as simultaneous Executors on disjoint work — here the work is the same Advisor packet, independent attempts). `advisor_board_unify` runs **after** all members have produced their Advisor outputs (or failed/timed out under existing bounds). Forbidden: round-robin / one-after-another Board turns as the default. If a member fails to start, fail-closed per existing spawn blockers; do not serialize the rest as a substitute for parallelism. Do not merge Authorizer with Validator. Do not reopen ESC-02, row 14, `process_v_verified`, Approver rename, or `AF-meta-six-role`.
> - **2026-08-16 SUPERSEDE (trivial/Q&A — no fresh AF/WF synthesis).** Spec-wins: at `/sb` resolve, classify **trivial** when the intent is Q&A, one-shot explain, or otherwise has no durable product edit. Reuse shipped catalog wrapping Workflow `sb:fast` (`WF-SILVER-FAST`) owning shipped catalog AF `AF-FAST-PATH` (public IDs `sb` / `sb:` / `/sb` only; do not invent `/silver` as a live window; do not mint a new `AF-qa`). Wrapping Workflow still exists (AF never sits directly under Process). Remint **must not** enter synthesize (step 3) for classified-trivial. parent≠implementer: Orchestrator does not implement; trivial I is `AF-FAST-PATH`'s Executor or a deny-all Q&A leaf under that AF — not a new synthesized skill. Non-trivial work is unchanged: catalog → compose existing AFs → synthesize; still forbidden to execute with no AF coverage. This supersedes any reading that 100% AF coverage + “must synthesize if catalog cannot cover” mints new AFs/WFs for trivial Q&A. Keep: no A on ESC-02; Authorizer not Approver; no `AF-meta-six-role`; wrap at `/sb`; BOA parallel for **non-trivial** Advisor. **Composition-Val accepts / Job still exists / GST still records** clauses in this bullet are **superseded** by the next same-day lock.
> - **2026-08-16 SUPERSEDE (FAST quality-order exemption + Job/GST exclusion).** Spec-wins: classified-trivial / `sb:fast` / `WF-SILVER-FAST` / `AF-FAST-PATH` (the whole `sb:fast` surface) is **exempt** from the six-role quality order. **Must not** run Advisor / Board of Advisors, composition-Val (no Validator spawn; canned cover is by classification, not a Val-loop), plan-time Validation-loop, A-loop Mentorship, Verification-loop, or Process-final Validation-loop. **Not a Job** and **must not** appear on Global Status (GST-01): no `original_intent_hash` Job mint; no GST projector write. Job remains user intent for **non-trivial** `/sb` work only. **Still required:** Orchestrator classifies; parent≠implementer (Orchestrator does not answer/implement); one Authorizer-admitted `AF-FAST-PATH` Executor or deny-all Q&A leaf. If the work is not actually trivial (durable product edit, multi-file change, etc.): **fail-closed reclassify** as non-trivial and enter the full mint → composition-Val → … quality order (that reclassified work **is** a Job and **does** hit GST). Do not silently stay on FAST. Keep: ESC-02 no A on non-trivial escalation; Authorizer not Approver; no `AF-meta-six-role`; wrap at `/sb`; BOA parallel for non-trivial Advisor; no synthesize on trivial.
> - **2026-08-16 SUPERSEDE (current-month context-load cap + FAST thin capture + AM→K/L promote).** Spec-wins: "current month files" is a **context-load cap**, not "older months do not count." Graphify-first when five-tool is opted in: one query covering `docs/knowledge/`, `docs/learnings/`, **and** `.agentmemory/memory/` when agentmemory is opted in; Graphify searches **all** months; a Graphify miss is `blocked_knowledge_preread`. INDEX fallback **only** when five-tool is not opted in: load `INDEX.md` (catalog of **all** months) **plus current month files** as the default hot set; older `YYYY-MM.md` are **not discarded** — pull on demand when INDEX or the question points at them; do not dump every historical month into the pre-read context. FAST exemption from quality-order/Job/GST does **not** mean throw away the insight: after the FAST leaf answers, Authorizer admits a **thin capture** deny-all leaf (same family as `knowledge_postwrite`, not a second Job): AM opted in → `memory_save` then classify/promote; AM not opted in → `kl_write_am_skipped`; durable insights promote into `docs/knowledge/YYYY-MM.md` or `docs/learnings/YYYY-MM.md`; ephemeral stays AM-only with `kl_post_write_no_insights` / equivalent FAST no-insight receipt; must not mint `original_intent_hash`, write GST, or run Advisor/Board/Val/Ver/A. AM is the **capture buffer**; K/L dirs are the **git source of truth**. `knowledge_postwrite` (Jobs) and FAST thin-capture **read AM candidates and export/promote** classified new insights into those dirs — **not** committing raw `.agentmemory/memory/` dumps as the team knowledge product. **Do not dual-write** the same durable insight as both a git-tracked AM markdown file and a K/L file. Graphify retrieve: K/L dirs primary for Job pre-read; AM remains in the same query for unpromoted session recall. `synergy_max` auto-commit of `.agentmemory/memory/` is **not** the K/L path (off-by-default / not required for this control plane). Existing already-tracked AM files are out of scope. KLW-01 still owns Job post-Val durable docs: the leaf promotes from AM → K/L (or `kl_post_write_no_insights`). Orchestrator does not author insight text. Keep: FAST not a Job; FAST not on GST; FAST exempt from six-role quality order; ESC-02 no A; Authorizer not Approver; `process_v_verified`; wrap at `/sb`.
> - **2026-08-16 SUPERSEDE (AM-first fail-closed K/L + team Graphify fan-out).** Spec-wins: `knowledge_postwrite` and FAST thin-capture MUST `memory_save` the insight **first** (the same durable text that would have been the K/L entry, not a weaker summary), then classify, then promote into `docs/knowledge/YYYY-MM.md` or `docs/learnings/YYYY-MM.md` (or `kl_post_write_no_insights` / FAST no-insight if ephemeral). Every `kl_write` receipt and every K/L monthly entry cites `am_id` (or content hash of the AM record); `kl_write` without AM provenance is invalid. When agentmemory is opted in for that runtime: AM save failure or missing `am_captured` → `blocked_knowledge_postwrite` (do **not** write K/L anyway). When AM is not opted in: K/L write may proceed; receipt is `kl_write_am_skipped` (INDEX fallback already covers retrieve; do not silently pretend AM captured). Orchestrator, ordinary Executor, and parent must not Write `docs/knowledge/` or `docs/learnings/` except via this leaf (direct write → `blocked_knowledge_postwrite`). Team fan-in: each person's local AM captures; classified durable insights promote into committed K/L files (git); unpromoted episodic AM stays local and is **not** the team store. Fan-out retrieve: everyone `git pull`s K/L; everyone's Graphify indexes `docs/knowledge/` + `docs/learnings/` (`graphify update` after pull / session start / Job pre-read). **Do not ingest K/L back into each clone's agentmemory** (optional thin pointer only, e.g. last commit SHA — not a copy of monthly files into AM). Teammates without AM still get team K/L via Graphify / INDEX; teammates with AM still retrieve team K/L via Graphify, not AM search. Keep: current-month is a context-load cap; FAST thin capture; AM is capture buffer; K/L is git SoT; `synergy_max` AM auto-commit is not the K/L path; FAST not a Job; FAST not on GST; FAST exempt from six-role quality order; ESC-02 no A; Authorizer not Approver; `process_v_verified`; wrap at `/sb`.
> - **2026-08-16 SUPERSEDE (Opus High ladder-3 ACCEPT — FAST catalog overlay).** Spec-wins: keep locked public IDs `sb:fast` / `WF-SILVER-FAST` / `AF-FAST-PATH`. Do **not** mint `AF-qa`. The *vehicle* is the gap: shipped `docs/apo-catalog.json` `WF-SILVER-FAST` `composition_tree` includes `AF-QUALITY-GATE` / `AF-PLAN` / `AF-VALIDATE` / `AF-EXECUTE` / `AF-VERIFY`; `AF-FAST-PATH` has `VL-AF-FAST-PATH`; `PP-SB-STARTUP-FAST` prefers that WF for 3-day greenfield (durable edits). **This ship amends** those APO records so generation cannot emit the old V-loop/composition obligations for classified-trivial. For classified-trivial, live composition is **`AF-FAST-PATH` only** (Authorizer-admitted Executor or deny-all Q&A leaf + FAST thin capture). Catalog extra AFs (`AF-PLAN`, `AF-VALIDATE`, `AF-VERIFY`, `AF-QUALITY-GATE`, `AF-EXECUTE`) **must not run**. WS1: APO is generation source **except** this classified-trivial overlay/amendment; generated denies/composition must not re-impose six-role gates on `sb:fast`. `PP-SB-STARTUP-FAST` must **not** map 3-day greenfield / durable product edits onto classified-trivial `sb:fast` (rewrite `override_rules[0]` so 3-day greenfield / durable edits prefer catalog `WF-SILVER-FEATURE`; fail-closed reclassify if `WF-SILVER-FAST` is selected for durable edits). Catalog JSON amendment lands with this architecture ship (plan is the spec; `docs/apo-catalog.schema.json` unchanged; exemption in checker + generators, not a surgical comment/flag). Keep: FAST not a Job; FAST not on GST; FAST exempt from six-role quality order; thin capture; no synthesize on trivial; wrap at `/sb`. **Round-12 further SUPERSEDES the schema/comment/OR clauses of this bullet** (see next).
> - **2026-08-16 SUPERSEDE (Opus High ladder-3 ACCEPT — GST degrade, not halt).** Spec-wins: GST is a team dashboard, not a delivery gate. Protected `main` / no push rights / exhaustion: stamp `gst_stale` (or equivalent) on the local WBS; **Job continues**. Do **not** classify `blocked_global_status_push` as a job-stopping row (row 34 is dashboard-only / non-classifying, like retired row 14). Local ASCII viz still updates. Resume of dashboard freshness is operator path-exception / push rights — **not** a gate on `scope_complete`. Round-6 lock "merge this instance's Job rows by `(instance_id, job_id)`" is **superseded** (plan semantic row CAS is `(instance_id, gst_row_id)` with `gst_row_id = H(original_intent_hash, process_id)`). Keep: FAST not a Job; FAST not on GST; WBS-01 distinct from GST-01; never force-push.
> - **2026-08-16 SUPERSEDE (Opus Extra High ladder-3 ACCEPT — catalog exemption mechanism + FAST overlay completeness).** Spec-wins: **B1** — stop “surgical comment/flag” in `docs/apo-catalog.json`. `$defs.atomic_flow` / `workflow` / `process_pack` have `additionalProperties: false`; `$defs.v_loop` is required so `VL-AF-FAST-PATH` cannot be deleted. Schema **unchanged** (`docs/apo-catalog.schema.json` named in WS1 as unchanged). Classified-trivial exemption is enforced in `scripts/check-apo-invariants.py` **and** the generators (WS1): they must **not** emit old V-loop/composition obligations for classified-trivial even though the JSON still carries required `v_loop` / `VL-AF-FAST-PATH`. Pruning generated composition for classified-trivial to `AF-FAST-PATH` only remains. Do **not** bulk-rewrite `docs/apo-catalog.json` except a schema-legal one-line change if required. **H-A** — add `AF-EXECUTE` to every must-not-run enumeration that lists `AF-PLAN` / `AF-VALIDATE` / `AF-VERIFY` / `AF-QUALITY-GATE`. Live classified-trivial composition remains **`AF-FAST-PATH` only**; `AF-EXECUTE` must not run (it would re-impose a populated `v_loop`). **H-B** — for classified-trivial, generators/checker also must **not** emit/enforce: `prerequisites` (upstream V-gates); `exit_condition` (step V-loops + flow V-gate + evidence); `flow_steps` V-loops (`FS-SILVER_*` / `VL-FS-SILVER_FAST`) — Leaf Step has no Step-level V/Val **wins** on this path; `execution.join_condition` (join after step V-loops); `artifacts` / `evidence_refs` as required V-evidence; `capability_class: bounded_fast_path` as a quality-order trigger. **M-A** — drop the unreachable “stays bounded-fast-path product work via `WF-SILVER-FAST`” alternative. **Only** retarget: rewrite `override_rules[0]` so 3-day greenfield / durable edits prefer catalog `WF-SILVER-FEATURE`. `WF-SILVER-FAST` selected for durable edits still fail-closed reclassifies. **M-B** — FAST Executor/Q&A leaf viz/prose **includes** the thin-capture deny-all node (M3 terminal; required WBS content — omit → `blocked_progress_viz`). **M-C** — protected-`main` / no-push-rights `gst_stale` + Job-continues fixture is owned by **`VAL/TST-RFL-621`** (GST-01 family). **M-D** — unifier leaf **still launches** for a Board of one (identity-preserving / no-op unify still Authorizer-admitted after the single member's output) so output-receipt identity (`input-set hash` + unifier `launch_id` / occurrence) can form. Not last-write-wins. Orchestrator still does not implement unify. Keep REJECT untouched. **Round-13 further SUPERSEDES leftover “amends those APO records” JSON-edit residue and adds H-1–H-5 / M-1–M-5** (see next).
> - **2026-08-16 SUPERSEDE (round-36 — Opus Extra High re-verify M-1a ACCEPT on SHA `71427c3dda42824c3dc59d04fa500f62c904c169bf782dc853a37815106f8c54`).** Spec-wins: **M-1a** land the third limb **in the canonical cells**: row 40 (~L669) trigger includes **mid-I new PUB-01 definition / new catalog WF record** (keep uncited / new product scope limbs; even when a `plan_node_id` is cited and there is no new product scope); row 37 (~L666) carve-out **excludes** that mid-I new-catalog-WF case so it cannot classify as row 37. L112/L118 two-limb lock unchanged. L859/L626 not churned. Keep REJECT untouched. Extra High Max **not** re-launched. GPT Max already CLEAN — not reopened.
> - **2026-08-16 SUPERSEDE (round-35 — Opus Extra High re-verify M-1/M-2 + nit ACCEPT on SHA `fe219ffeffd1bdff4a16debccb2a598f81e26176fdcc905d20af3c92a51f8b2b`).** Spec-wins: **M-1** Proposed-architecture L112/L118 (and twins) restated as L251 **two-limb** Executor mint — legal **iff** (a) Work Plan–cited WF/AF **or** (b) a **pre-existing catalog** WF that supports that cited node; drop “new or pre-existing” as equally legal at mint; L112 + L118 row-40 includes **mid-I new PUB-01 definition / new catalog WF record**, matching L185/L251/L253/L265/L669/L737. **M-2** second snapshot GC trigger — collect when **either** (1) `launch_id` is CAS-provably superseded **or** (2) that launch’s durable `scope_complete` / `completion_receipt_id` is CAS-recorded; still-current + not-complete retain; missing still-current incomplete snapshot remains row 4; CORR-17 fence **holds**; do **not** revert supersession-GC; do **not** use fence-release / child-terminality / pid liveness. **n-1** `VAL/TST-RFL-626` pins fifo/socket/device, dangling symlink, symlink loop → row 4. Keep REJECT untouched. Extra High Max **not** re-launched. GPT Max already CLEAN — not reopened.
> - **2026-08-16 SUPERSEDE (round-26 — GPT Max re-verify B-1/H-1/M-1/M-2 + Opus Extra High re-verify B-1/H-1/H-2/M-1 ACCEPT on SHA `701cc66466830161476597af35606168377acb2437088fb86787eec0f6fcc884`).** Spec-wins: **GPT Max B-1** Executor mid-I `wf_mint` may invoke/instantiate a Work Plan–cited WF/AF or a **pre-existing catalog** WF that supports that cited node; creating a **new PUB-01 definition / new catalog WF record** mid-I is **out of plan** → row 40 → Advisor re-compose + composition-Val + plan-time Val **re-bind** the closure before Executor resumes. Do not run an unbound new definition during I. PUB-01 / composition-Val remain **before Executor I** for the bound set; WFM-01 / `VAL/TST-RFL-625` owns the fixture. **GPT Max H-1** Executor **may** invent/instantiate **in-plan** nested WFs (plan-cited or pre-existing catalog) and **must not** invent new product-scope / new PUB-01 definitions (Advisor). Blanket “forbids invent a new WF” is wrong. **GPT Max M-1** FAST thin-capture tests **must not** require `memory_save` when AM is not opted in (AM opted in → `memory_save` then classify/promote; AM not opted in → `kl_write_am_skipped`). **GPT Max M-2** `PP-SB-DEFAULT` / `generate-apo-catalog.py` must not forbid PUB-01 project/global synthesized flows as illegal extra-catalog AFs (emit into catalog/lock or a named overlay, **or** carve PUB-01 synthesized flows; one explicit generator + WS1 sentence; do not drop PUB-01). **Opus B-1 / H-2** WS1 ordered steps: (1) **back-port** the committed catalog’s 3 semantic divergences into generator source (do **not** regen first); (2) then regen `docs/apo-catalog.json`; (3) then `--check` / parity gate. Named: `check-apo-invariants.py worker-template-parity`, `AF-MULTI-AI-TASK.execution.worker_template`. Intended truth for `worker_template`: `templates/orchestrator-workers/MULTI-AI-TASK.md`. Fix `ATOMIC_SPECS` and/or `merge_multi_ai_catalog()` so regen **sets** that templates/ path and does **not** emit `SKILL.md`. Python builders are SOT **after** committed un-regenerable state is back-ported. WS1 must **not** mandate a regen that fails `worker-template-parity`. **Opus H-1** In `apo_delegate_catalog.py`: **remove** the strip of `AF-AGENT-DELEGATE` from `FS-SILVER_AGENT_OPENCODE` / `FS-SILVER_AGENT_PI` `reusable_by_flows`; add those steps to `DELEGATE_FLOW_STEP_ORDER` **and** `DELEGATE_STEP_DEFS` in the **same** step (`build_delegate_flow_steps` indexes `DELEGATE_STEP_DEFS[step_id]`; adding ORDER without dict entries is `KeyError`). Name `DELEGATE_STEP_DEFS` and `build_delegate_flow_steps` / `merge_delegate_catalog` overwrites (~186/212) — **not** generic `build_flow_steps` in `generate-apo-catalog.py`. Intended `skill` / `purpose` / `classification` **mirror** `FS-SILVER_AGENT_{CODEX,CURSOR,CLAUDE}`. `SKILL_TO_FLOW` must **agree** with merge (no map-then-detach). Name `DELEGATE_STEP_DEFS` and `build_delegate_flow_steps` in WS1. Round-24 M-3 reciprocity is satisfiable **only after** this. **Opus M-1** There are **zero** `VL-FS-DELEGATE-*` top-level records. Patch the **inline** `AF-AGENT-DELEGATE.v_loop.verification.artifact_refs` to `["ART-AGENT-DELEGATE"]` (not the top-level `v_loops[]` stub). Sibling comparison = already-attached cursor/codex/claude **step** V-loops / `ART-AGENT-DELEGATE`. Schema unchanged. No second AF. `nested_executor` lock-only. Public `/sb` only. `tests/run-all-tests.sh` remains the release gate. Keep REJECT untouched.
> - **2026-08-16 SUPERSEDE (round-25 — GPT Max H-1 + Opus Extra High re-verify B-1/H-1/M-1 ACCEPT on SHA `701cc66466830161476597af35606168377acb2437088fb86787eec0f6fcc884`).** Spec-wins: **GPT Max H-1** WS1 **owns** `scripts/lib/apo_delegate_catalog.py` and `scripts/lib/apo_multi_ai_catalog.py` as named source surfaces (same regenerate-mirrors-only-through-the-named-command discipline); those libs still emit `/silver:*` and `$silver:*` and **must** retarget to public `/sb:` / `$sb:` (or the post-rename token the rest of generation uses). **B-1** `docs/apo-catalog.json` is **emitted** by `scripts/generate-apo-catalog.py` (`build_catalog()` overwrite) — stop treating it as a hand-edit SOT; all catalog mutations this ship needs live in generator source (`SKILL_TO_FLOW`, `ROUTERS`, `ATOMIC_SPECS`, `build_workflows()`, `merge_delegate_catalog()`, `PROCESS_PACK_DEFS`, plus the two libs). WS1 **names** `generate-apo-catalog.py` as regenerate command for the catalog itself (not only `generate-apo-artifacts.py`). Add a **parity gate**: `--check` and/or a test that `docs/apo-catalog.json` matches generator output (today `tests/scripts/test-apo-catalog-sot.sh` does **not**). Uncommitted generator output is not authority **after** regen; the **source** is the Python builders. `SKILL_TO_FLOW` / `ROUTERS = {"silver", ...}` **must** accept post-rename `sb` / `sb-init` / `sb-new-workflow` / `sb-agent-*` or the first regen **aborts**. `WF-SILVER-ROUTER.triggers` must not mint public `"/silver"`; public trigger is `/sb` (catalog id may stay `WF-SILVER-ROUTER`). **H-1** Drop/stop emitting duplicate `ART-AGENT_DELEGATE` (underscore); `VL-AF-AGENT-DELEGATE.verification.artifact_refs` must be `ART-AGENT-DELEGATE`; invariant `v_loop.verification.artifact_refs` ⇄ AF `artifacts` for this AF (`check-apo-invariants.py` must see it); fix in `ATOMIC_SPECS` / `merge_delegate_catalog()`. **M-1** `FS-SILVER_AGENT_OPENCODE` / `FS-SILVER_AGENT_PI` `verification.artifact_refs` must be `["ART-AGENT-DELEGATE"]`; catalog path strings into dirs this ship renames are generator-owned and heal on regen if B-1 regen is required. Schema unchanged. No second AF/WF. `nested_executor` lock-only. Public `/sb` only. `tests/run-all-tests.sh` remains the release gate. Keep REJECT untouched.
> - **2026-08-16 SUPERSEDE (round-24 — GPT Extra High + Opus Extra High re-verify ACCEPT on SHA `1096479cc1deba1b902ca501e8d7b7b1c0c1ba5f23510f8776c098f6913002d5`).** Spec-wins: **H-1** Nested-Task of pre-persisted descendants (no parent-proxy consume) **declares/stamps/compares** `worktree_cwd` against hashed `scope_bounds`/WBS tree; mismatch → row 4; stale/tampered cwd must not bypass row 4 because consume never ran. Round-23 consume-path `worktree_cwd` stays. **H-2** Init / Doctor / SessionStart **MAY write** HNEST-01 max-nesting knobs and HINST-01 install-ensure (idempotent; skip if already at max / already installed). “Inspect-only” / do-not-write applies to **unrelated** IDE prefs (model, telemetry, permissions, maxTurns, experimental teams) — not those two mandated writes. Do not revoke the user lock that SB configures max nested-subagent support. **M-1** Completed/Blocked GST tombstones must not expire after two UTC rollovers. Projector consults **historical day files that still exist** (or a durable tombstone index) for that `gst_row_id`, not only current + previous day. Stale Active retry must not resurrect a terminal row. Pin in `VAL/TST-RFL-621`. GST degrade 34+35 stays. **H-3** After any catalog / `migration_map` / `owning_skills` / `runtime_queue_tokens` edit this ship makes, WS1 **must** regenerate and keep green: `scripts/generate-apo-artifacts.py` (and `--check` / `derived-views`), `docs/composable-flows-contracts.md`, `docs/workflow-composition-matrix.md`, `docs/generated/atomic-flow-index.json`, `tests/scripts/test-apo-derived-views.sh`, `tests/scripts/test-apo-composition-sot.sh`. Unnamed mirrors going stale is a WS1 miss. **H-4** WS1 (and WS6 if instruction text) **must** retarget hardcoded `silver:` public routes in `POST_EXEC_SEQUENCING_LINES` (or equivalent) in `scripts/generate-apo-artifacts.py` and in `tests/scripts/test-instruction-flow-parity.sh` (P0-4 three-way), `tests/scripts/test-silver-router-flow-contracts.sh`, `tests/scripts/test-apo-catalog-sot.sh`. Generated docs must not advertise `/silver` public routes. Catalog ids `WF-SILVER-*` may remain. Public prefix is `/sb`. **M-3** `AF-AGENT-DELEGATE.flow_steps` ⇄ `reusable_by_flows` on `FS-SILVER_AGENT_OPENCODE` / `FS-SILVER_AGENT_PI` (and other delegation steps) must agree; add/require an invariant in `check-apo-invariants.py` for this AF. `tests/run-all-tests.sh` remains the release gate and is **named** in WS1. Schema unchanged. No second AF/WF. `nested_executor` lock-only. Keep REJECT untouched.
> - **2026-08-16 SUPERSEDE (round-21 — Max CLEAN ACCEPT on SHA `5eba7e86…95216`, M-1–M-2 / L-1).** Spec-wins: **M-1** Public identifier is `/sb:new-workflow` (skill/command/help/test slug `sb-new-workflow`). `/silver:new-workflow` / `silver-new-workflow` are historical only (same gloss as `skills/sb/` vs `skills/silver/`). WS2 ships `skills/sb-new-workflow/SKILL.md` (historical `skills/silver-new-workflow/SKILL.md`) via the ship-wide `silver`→`sb` rename — no dual `/silver` window. Catalog id `WF-SILVER-NEW-WORKFLOW` stays. Authoring generator + authoring Job + Advisor compose stay; this **implements** the public-IDs-`sb`-only lock for this route, it does **not** reopen KEEP REJECT. **M-2** Row 37 is non-Advisor mint without Authorizer admit / role permission that is **not** the out-of-plan Executor case (uncited `plan_node_id` / new product scope **stays row 40**) and **not** Orchestrator (**stays row 39**). WFM-01 three fixtures remain distinct. **L-1** Delete the adjacent verbatim duplicate of the Codex `unbounded` parenthetical in the frontmatter capability-contract todo. Keep REJECT untouched.
> - **2026-08-16 SUPERSEDE (round-20 — Extra High CLEAN ACCEPT on SHA `b673de8c…40c18`, M-A–M-C).** Spec-wins: **M-A** Row 42 **includes** a non-parent **spawn target** (e.g. parent Cursor, target Codex). “Sibling-host install fail is a doctor warning” means a **third** present host that is **neither** parent **nor** spawn target. A spawn-target install fail stays row 42 (not a warning, not row 22). Init/Doctor sibling-host warn remains Init-time only (no spawn target yet). **M-B** Resolve of `WF-SILVER-NEW-WORKFLOW` is **Advisor-compose-gated** (work-spec + Advisor invoke). Keep `type: precomposed`; that type does **not** mean skip-Advisor for this route. Catalog dispatch of this already-existing id without Advisor is row 39. **M-C** Retire the queue-builder in **source** [`templates/orchestrator-workers/NEW-WORKFLOW.md`](templates/orchestrator-workers/NEW-WORKFLOW.md); generated mirror [`plugins/silver-bullet/templates/orchestrator-workers/NEW-WORKFLOW.md`](plugins/silver-bullet/templates/orchestrator-workers/NEW-WORKFLOW.md) via `scripts/sync-templates.sh`; installed [`.silver-bullet/orchestrator-workers/NEW-WORKFLOW.md`](.silver-bullet/orchestrator-workers/NEW-WORKFLOW.md) follows install. Do not edit only the generated or installed copy. Output-compliance generator bar unchanged; authoring session remains a Job with Advisor compose. Keep REJECT untouched.
> - **2026-08-16 SUPERSEDE (round-19 — Extra High re-verify ACCEPT on SHA `65fde3d6…c7fd8c`, H-1–H-2 / M-1–M-2).** Spec-wins: **H-1** `/silver:new-workflow` **authoring session is a Job** and **must** use the Job quality order (Orchestrator work-spec + Advisor compose + composition-Val + plan-time Val; Executor executes the authoring plan). Queue-builder composition is retired for this session (Orchestrator-composed Work Plan → row 39). Authoring Job **does** mint `original_intent_hash`, take a GST row, emit WBS. **`WF-SILVER-NEW-WORKFLOW`’s own catalog record is in scope** (must become NW-capable / spec-compliant). WFM-01 / `VAL/TST-RFL-625` golden fixture proves the **session** path (Advisor-composed authoring Job, not Orchestrator queue-builder) **and** the emitted WF. WS2: `.silver-bullet/orchestrator-workers/NEW-WORKFLOW.md` becomes work-spec + Advisor invoke (not a queue-builder mint path). Output-compliance generator bar unchanged; do **not** recast the skill as “only invokes Advisor.” **H-2** Rows 11/12 carved so HINST-01 install/reference failures classify 41/42, not `blocked_executor_unavailable` / `blocked_verification_unavailable`. Instruction-only runtimes excluded from closest-replacement when the recorded tuple was an installable host; do not silently downgrade to the instruction-bundle path. **M-1** Codex `remaining_depth` sentinel `unbounded` is **not** compared as integer 0. Integer `> 0` / `== 0` apply only when numeric (Cursor/Claude). Depth-exhaustion parent-proxy on Codex = host refuses further nest (spawn/nest API error / capability), stamp `host_nest_refused`, then parent-proxy. Consumers MUST accept the non-integer sentinel. `VAL/TST-RFL-623` refuse-then-proxy fixture. **M-2** Replacement-ladder probe: (1) installable host → HINST-01 install-ensure + adapter capability (row 20) + five-tool init probe if opted in; (2) instruction-only candidate must **not** replace an installable-host tuple; (3) when five-tool is not opted in, probe = HINST-01 + adapter capability only. Keep REJECT untouched.
> - **2026-08-16 SUPERSEDE (round-18 — `/silver:new-workflow` output compliance; plan SHA `65fde3d6…c7fd8c`).** Spec-wins: `/silver:new-workflow` is a **workflow authoring generator**. Emitted WFs must match this plan’s spec (NW-capable composition; role-gate runtime contract; catalog-legal `v_loop` / `additionalProperties: false`; FAST vs Job artifact shape; launch-envelope stamps; agent-wrap family if requested; host/install docs). **Retract** treating the skill primarily as “a user command that only invokes Advisor instead of Orchestrator mint.” The authoring session is a Job that may use Advisor internally to formulate the definition; Orchestrator still does not `wf_mint` at runtime. Extra High H-1–H-5 / M-1–M-4 remain. Keep REJECT untouched.
> - **2026-08-16 SUPERSEDE (round-17 — Extra High re-verify ACCEPT, H-1–H-5 / M-1–M-4).** Spec-wins: **H-1** `/sb:agent-*` shared-state is catalog **dispatch** of `sb:agent-wrap` / `AF-agent-delegate` (Advisor-first only if Job with no validated plan; classified-trivial cold Q&A skips Advisor; in-flight nested dispatch not Orchestrator `wf_mint`). **H-2** HINST-01 B4: parent-proxy is **not an install substitute**; §Dispatch parent-proxy still applies at remaining_depth 0 **and** in-flight new projector write at remaining_depth > 0. **H-3** Row 22 exclusion extends to rows 11–21 **and 36–42**; row 20 carved so 41/42 classify. **H-4** `VAL/TST-RFL-625` / WFM-01 owns role-gate blockers 37/39/40 + in-plan happy path + mid-AF-step insert. **H-5** Instruction-only `/sb:agent-opencode` / `/sb:agent-pi` are five-tool probe-exempt (ancestor parent-proxy satisfies). **M-1** Depth unit = hops below main (Cursor 2, Claude 3). **M-2** Codex `remaining_depth: unbounded`. **M-3** Row 37 any non-Advisor unauthorized mint (Orchestrator stays 39). **M-4** HNEST-01/HINST-01 risks: vendor doc drift; cross-host `~/.codex`/`~/.claude` writes; instruction-only no PreToolUse/PostToolUse — expected writes reconciled via parent-proxy after the fact. Keep REJECT untouched.
> - **2026-08-16 SUPERSEDE (round-16 — retract catalog-only agent-* invent ban + HINST-01).** Spec-wins: **Retract** “`/sb:agent-*` may dispatch catalog AF/WF ids but may not invent a new WF.” Every `/sb:agent-*` (Cursor, Codex, Claude, OpenCode, Pi) is an Executor-shaped leaf that **may invent a new Workflow in-plan** (cited `plan_node_id` / WBS id); out-of-plan → `blocked_executor_wf_out_of_plan` → Advisor. Catalog wrap `sb:agent-wrap` / `AF-agent-delegate` is the dispatch envelope, not a ban on nested invent inside it. Orchestrator/`/sb` still does **not** compose Work Plans or mint WFs (`blocked_orchestrator_wf_mint`). FAST remains classify + catalog dispatch, not WF mint. **HINST-01 / `VAL/TST-RFL-624`:** same Init/Doctor pass as HNEST-01 ensures SB on present Cursor/Codex/Claude via `scripts/install-{cursor,codex,claude}.sh` to this repo `package.json` version (detect: CLI or `~/.cursor` / `~/.codex` / `~/.claude`; sibling absent → warn). `/sb:agent-cursor|codex|claude` verify-then-install-then-spawn (native SB; parent-proxy only at `remaining_depth` 0). OpenCode/Pi: no plugin install (OpenCode plugins are JS/TS; Pi is `skills/silver-agent-pi` CLI wrap with no SB hook surface); instruction bundle points at parent-or-Cursor→Codex→Claude install dir plus `$primary_checkout/silver-bullet.md` / `skills/` / `contracts/`; parent-proxy writes work-spec/WBS/GST/AM/`graphify update`/WF mint records; `blocked_sb_host_missing` if no reference dir; `blocked_sb_host_install` if an installable present host cannot be repaired. Keep Extra High ACCEPT, nested-WF dynamicity, role-gated mint, Orchestrator-does-not-compose, HNEST-01, FAST classify-not-mint.
> - **2026-08-16 SUPERSEDE (round-14 — item 7 narrowed + host max-nesting Init/Doctor; Extra High ACCEPT retained).** Spec-wins: **Initial user-intent Process wrap** still mints at `/sb` Process resolve/wrap. **Nested / opportunistic Workflow mint or invoke does NOT return to Orchestrator or `/sb`.** Any Executor may `wf_mint` / `wf_invoke` a newly created or pre-existing Workflow at any point (Authorizer-admitted; not a second Process; AF never under Process; FAST does not synthesize NWs; WBS must show inserted NWs; GST nested WF is Job-scoped). Previous “do not write Cursor/Codex/Claude max-nesting settings; Init/Doctor does not probe those knobs” is **revoked**. On first `/sb:init` or any next SessionStart/init if unset, SB configures the host for documented max nested-subagent support (`VAL/TST-RFL-623` / HNEST-01). **Cursor:** official max **2 Task hops** below the main agent (Cursor 2.5; https://cursor.com/docs/subagents; no writable knob; adapt via `remaining_depth`). **Codex:** no documented official nesting-depth number (https://developers.openai.com/codex/multi-agent; may set `agents.enabled = true`; do not write schema `max_depth`; adapt via `remaining_depth`). **Claude:** documented default **3 subagent layers** below main via `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` (https://code.claude.com/docs/en/env-vars; write `3` if unset/below; no published ceiling above 3). Keep Extra High H-1–H-5 / M-1–M-5. Keep REJECT otherwise untouched.
> - **2026-08-16 SUPERSEDE (Opus Extra High ladder-3 ACCEPT, real xhigh, round-13).** Spec-wins: **H-1** — row 35 same GST degrade as row 34: missing git identity stamps `gst_stale`; **Job continues**; row 35 dashboard-only / non-classifying; identity fixture in `VAL/TST-RFL-621`. **H-2** — add `.sb/` to every sparse-checkout / split-brain / merge-oracle / WBS-01 / launch-template ledger-omit enumeration; extra-tree discriminator (row 33 vs 4/8) treats `.sb/` as ledger-omit. **H-3** — classified-trivial is not a Job; Job step 1 / `blocked_knowledge_preread` / `pre_read_pending` are Job-scoped; FAST WBS is classify + wrap + FAST leaf + thin-capture only (no pre-read node); Graphify miss on FAST is not row 8; five-tool still applies if a FAST leaf would edit (it must not); reclassified durable work **does** run step 1 as a Job. **H-4** — FAST leaf hang/die/wrong-answer → Authorizer-admitted **one** re-dispatch of the same FAST leaf; second failure → `blocked_fast_leaf` (FAST-scoped, not a Job, not GST; stops FAST `scope_complete` / user return, not GST); do **not** use ESC-02 / Advisor/Val/Ver; thin-capture AM-save failure stays `blocked_knowledge_postwrite` but FAST-scoped. **H-5** — WS1 **must** add `AF-agent-delegate` / wrapping WF `sb:agent-wrap` to `docs/apo-catalog.json` (schema-legal required `v_loop` `VL-AF-agent-delegate`; class `nested_executor`; public routes `sb:agent-wrap` / `sb:agent-delegate`). **M-1** — classified-trivial exemption is generators + `check-apo-invariants.py`; do **not** JSON-edit `AF-FAST-PATH`/`WF-SILVER-FAST`; FAST collision change lives in generator `PROCESS_PACK_DEFS` (`PP-SB-STARTUP-FAST.override_rules[0]` prefer `WF-SILVER-FEATURE` for 3-day greenfield), then regen — do **not** hand-edit `docs/apo-catalog.json` for that field. **M-2** — GST UTC rollover: Active rows carry forward; Completed/Blocked stay on the day they terminated; tombstone consulted across current **and previous** day file (`VAL/TST-RFL-621`). **M-3** — GST helper write order: (1) if operator primary is git main-worktree, commit/push there; (2) else fetch+commit on `origin/main` without a second worktree that materializes ledger-omit dirs; forbid a dedicated main worktree that copies `.planning/` / `.sb/` / etc. **M-4** — `[skip ci]` / `paths-ignore` scoped to **push** heartbeats on `main`; do not suppress `pull_request` checks. **M-5** — CAS `git_user_id` still prefers email if set (identity stability); published dashboard MUST NOT write raw `user.email` into `.sb/status/**` (use `user.name` or a short hash of email; helper refuse-write if payload would contain a raw email). Keep REJECT untouched.
> - Round-4 / note 19 `blocked_replan_budget` as job bound — replaced by the finite 4-step ladder.
> - RFL note 10 leaf Step then AF Val.
> - RFL note 12 Process-synthesis owns Process I of child work — packet-local composition/findings only.
> - Next-step “pending user lock” / plan `## Pending user lock`.

**UTC file stamp:** 2026-07-17T14:37:57Z (session local 2026-07-18)
**Clarify run:** 2026-07-17T14:37:57Z (interactive `/silver:clarify`, Grok 4.5 High)  
**Round-5 alignment:** 2026-08-14 — historical. **Round-1 fix 2026-08-14:** architecture spec supersedes the contradicting Qs listed in the banner (round-4 executor-draft P-loop remains superseded; Q4 public IDs remain `sb:<route>`). **Round-2 fix 2026-08-14:** Q18 `val_*` on ordinary AF/Workflow SM superseded; note 15 `remote_id_sha256` path suffix superseded. **Round-10 fix 2026-08-14:** Q12 I-loop two consecutive clean superseded — I has no self-attested two-clean. **Composer-Medium RFL fix 2026-08-14:** Process-scope A/V and Process-synthesis I are mandatory after the top Workflow join before Process-final Val (ordinary-delivery steps 9a–9c); VLP-01/ILP-01/ESC-02/`step_yield`/overlap live E2E aligned. **Kimi-K3-High parent-side fix 2026-08-14:** live canonical-order chains (this banner, Universal Advisor L64, Validation-loop Order) include Process-synthesis I and Process-scope A/V after the **top** Workflow join only. **GPT-5.6 Sol High ladder-2 2026-08-15:** recommended-defaults “Advisor strictly stronger…” marked superseded/non-binding; plan same-tuple rule unchanged. **GPT-5.6 Sol Extra High ladder-2 rerun 2026-08-15:** parent-proxy `failed`→`resumed`; POA-01 plan-revision binding on `launch_intent`; Process-scope A/V dirty → `process_repair_pending` + 9a–9c rerun (distinct from Val-fail; no A on ESC-02); KLW-01 post-write deny-all Advisor `knowledge_postwrite` leaf + admission + receipt. **GPT-5.6 Sol Max ladder-2 2026-08-15:** parent-proxy hash-bound `prepared` payload and `prepared`→`consumed` recovery; overlap-worktree `git merge --no-ff --no-commit` and clean index on every join including the first; row 1 excludes LPS-01 launch-input mismatches; obligation IDs `VAL-RFL-604` / `VAL-RFL-900` / `TST-RFL-900`. **User spec 2026-08-16:** plan-time Validation-loop vs work spec after each Advisor plan; Verification-loop (Ver-loop) / Validation-loop (Val-loop) names; Process-final Val-fail starts a Validation-loop with Advisor re-plan (supersedes fail-receipt-only / no re-plan); A-loop Mentorship after I stays; no A on ESC-02 steps 2–3. **Same day later:** composition-Val vs user intent after `/sb` mint; remint/synthesize until `comp_val_verified`; WBS rewrite before Executor spawn; row 13 bounds composition-Val; no `AF-meta-six-role`. **Same day later still:** PUB-01 flow-publisher (B1) + `comp_val_verified` identity binding (H1/CORR-17) + Board of Advisors (`advisor_board_unify`) + Codex/Claude/Cursor host built-in defaults (not mandates); first `/sb:init` or later init if prefs missing; Authorizer not a preference key. **Same day later (Board parallel):** Board of Advisors members MUST launch and work in parallel (Authorizer admits each; Task-capable ancestor starts concurrently; `advisor_board_unify` after all member outputs or fail/timeout; forbidden round-robin / one-after-another as default; member start-fail is fail-closed — do not serialize remaining members). **Same day later (trivial/Q&A):** at `/sb` resolve classify trivial (Q&A / one-shot explain / no durable product edit); reuse catalog `sb:fast` (`WF-SILVER-FAST`) / `AF-FAST-PATH`; remint must not enter synthesize; parent≠implementer. **Same day later SUPERSEDE (FAST exemption):** FAST/trivial **exempt** from quality-order roles except Orchestrator classify + Authorizer-admitted FAST Executor/Q&A leaf; **not a Job**; **must not** appear on GST-01; fail-closed reclassify if not actually trivial. **Same day later SUPERSEDE (K/L + FAST thin capture + AM promote):** current-month files are a context-load cap; FAST thin capture after the FAST leaf; AM capture buffer → K/L git SoT (no dual-write). **Same day later SUPERSEDE (AM-first fail-closed + team Graphify fan-out):** AM-first `kl_write` provenance; AM opted-in failure → `blocked_knowledge_postwrite`; AM not opted in → `kl_write_am_skipped`; do not reload K/L into each clone's agentmemory. **Same day later SUPERSEDE (Opus High ladder-3 ACCEPT):** classified-trivial catalog overlay (`AF-FAST-PATH` only; amend APO so generation cannot emit old V-loop/composition obligations; `PP-SB-STARTUP-FAST` is not the Q&A classifier); GST degrade-not-halt (`gst_stale`; row 34 dashboard-only; round-6 `(instance_id, job_id)` merge-by superseded). **Same day later SUPERSEDE (Opus Extra High ladder-3 ACCEPT):** checker+generators exemption with `docs/apo-catalog.schema.json` unchanged; must-not-run includes `AF-EXECUTE`; overlay field list; `PP-SB-STARTUP-FAST` `override_rules[0]` prefers `WF-SILVER-FEATURE`; FAST viz includes thin-capture deny-all; `VAL/TST-RFL-621` owns `gst_stale` fixture; Board of one unifier still launches. **Same day later SUPERSEDE (Opus Extra High ladder-3 ACCEPT, real xhigh, round-13):** row 35 GST degrade; `.sb/` ledger-omit everywhere; FAST not Job step-1; `blocked_fast_leaf`; WS1 adds `AF-agent-delegate` / `sb:agent-wrap`; no JSON-edit of `AF-FAST-PATH`/`WF-SILVER-FAST`; GST UTC rollover; GST helper write order; push-only `[skip ci]`/`paths-ignore`; no raw email on published dashboard.  
**Source plan:** `.planning/router_subagent_surfaces_85bf9f09.plan.md`  
**Cursor mirror:** `~/.cursor/plans/router_subagent_surfaces_85bf9f09.plan.md` (byte-identical after incorporation)  
**Plan SHA-256 (both copies, after round-36 ACCEPT):** `9c9aa7d93a6fa7dd701e18528dd3d8f422cff0da47902aac4b8ac8c76ce21b06`
**Prior frozen SHA-256 (round-35 final, invalidated):** `71427c3dda42824c3dc59d04fa500f62c904c169bf782dc853a37815106f8c54`
**Prior frozen SHA-256 (round-34 final, invalidated):** `fe219ffeffd1bdff4a16debccb2a598f81e26176fdcc905d20af3c92a51f8b2b`
**Prior frozen SHA-256 (round-33 final, invalidated):** `ebd7ad9e499c34e669f8317fcc25715bdfab12a4ef5ed97ac14df6152575ae5e`
**Prior frozen SHA-256 (round-32 ACCEPT):** `3af884ef7892cc93ff5d69023632cf299ec18e87ce77d30860d205b694f317a4`
**Prior frozen SHA-256 (round-31 final, invalidated):** `a8e7a463bb1bde980ed173b9ddd32e95accf0b6902d6ae92348145f1cffad9ca`
**Prior frozen SHA-256 (round-31 mid-write GPT-only, invalidated):** `a007c83a645ab6d96846de07b3a22b8233ecbafd6699e83b233f6c3b7b48fe97`
**Prior frozen SHA-256 (round-31 mid-write seen by Opus, invalidated):** `b062dc1cbe92aaf9acf027804dd0719963551553d0c98da284bfa677c9a76a6b`
**Prior frozen SHA-256 (round-30, invalidated):** `c1868fa31a9e424997ae9994376bac5d27e3f6886f74509c4f2717b21f36a93e`
**Prior frozen SHA-256 (round-29 final, invalidated):** `9a173a53f04eec56bc139d1e1ae67f7cdc3c0530a9860f71e6253a67e346e7be`
**Prior frozen SHA-256 (round-29 mid-write, invalidated):** `caa36067e2ed6e893b46209a8bec9ee99d8ef78015efb65f8b967a1190934418`
**Prior frozen SHA-256 (round-28 final, invalidated):** `aa3677a5531797f59465a1370b1909fb5c696a42067546b3b4f99f3ac4c5b30c`
**Prior frozen SHA-256 (round-27, invalidated):** `60cf413ddac0cbcb80073e776dd0f6d9d56302002d3a2019a682fbb5060410de`
**Prior frozen SHA-256 (round-26, invalidated):** `ac500b960f2ade792b4cc97f542986e39582bae9a295e8be8a9cca6f2955974b`
**Prior frozen SHA-256 (round-25, invalidated):** `701cc66466830161476597af35606168377acb2437088fb86787eec0f6fcc884`
**Prior frozen SHA-256 (round-24, invalidated):** `1096479cc1deba1b902ca501e8d7b7b1c0c1ba5f23510f8776c098f6913002d5`
**Prior frozen SHA-256 (round-23, invalidated):** `c82e1b3bccc5625e9cb4fe2cc7518a577fdd69af225c062c08b6cd6d5ac7a472`
**Prior frozen SHA-256 (round-22, invalidated):** `67eff63eb035db3586cb0b0faeb55088b05dd86ea71651773b488b47c23efd75`
**Prior frozen SHA-256 (round-21, invalidated):** `c9511f2daa336ef34f30271348085c885e19903eb8243ddb53832980279aaddf`
**Prior frozen SHA-256 (round-20, invalidated):** `5eba7e86c040c4965b43446a4454bde8064904a9e52c0f69d2a80f542f795216`
**Prior frozen SHA-256 (round-19, invalidated):** `b673de8cc11bb46dfb12ed8bd48fa1d9ecc642f9e4ea4b9047d46b8e9c240c18`
**Prior frozen SHA-256 (round-16, invalidated):** `d9d452eee1d4ed2307cdc9c195d42afaf03889ab18923e52cc48ca8e72bdd172`
**Prior frozen SHA-256 (round-14, invalidated):** `18ac07bbc763241d023681a14aab1261d6b1d7be13f9e364bff5caba8c4614b4`
**Prior frozen SHA-256 (round-13, invalidated):** `4f772f9f618ae42aa2ecd573c2c5a813af8d22c29f719f97a5f12106efee2d1d`
**Prior frozen SHA-256 (round-12, invalidated):** `0289df0c7d8e8982ecef041d5e9da7092b5a6a67b94863297c74c276785ec2af`  
**Prior frozen SHA-256 (round-11, invalidated):** `bebcfa9165afd7bbfef934ca8093bbd567f9c569d49c1f1d980298fb182d1b0d`  
**Prior frozen SHA-256 (invalidated):** `db8cec806a33cfea991316c3ba9034281a229efe7424194625a51ce9527c06ed`  
**Do not overwrite:** `.planning/CLARIFY.md` (multi-AI deep-research brief — unrelated)

---

## Problem Statement

Silver Bullet needs a Process-first `/sb` router architecture with Authorizer-admitted / Orchestrator-spawned hierarchical execution, nested quality loops (**`/sb` mint wrapping WF → composition-Val vs user intent → remint/synthesize until `comp_val_verified` (classified-trivial reuses `sb:fast` / `AF-FAST-PATH` and skips synthesize) → Advisor plan → plan-time Validation-loop vs work spec → I → A → Verification** at AF and Workflow; after the **top** Workflow join: Process-synthesis I → Process-scope A/Verification → **Process-final Val vs original user intent**), Levels 0–3 defect recovery (post-MVP with Iterate), optional Iterate Ladder (post-MVP), and universal migration — clarified interactively before implementation. Public routes are `sb:<route>`. Product name `silver-bullet.md` / `~/.silver-bullet/` may remain.

---

## Recorded Q&A

| Q | Decision |
|---|----------|
| Q1 | Plan-scoped clarify brief + Decision Addendum in plan; do **not** overwrite multi-AI `.planning/CLARIFY.md` |
| Q2 | Diff-merge repo vs Cursor first (richer Cursor base + clarify wins); then keep both byte-identical |
| Q3 | GLM 5.2 is **not** barred; allow in Planning/Validation defaults; remove blanket bar |
| Q4 | **All AFs and all Workflows** are `sb:<route>` (amend “exactly 18”); ordered membership from full APO catalog. Public cutover is `/sb` only. **SUPERSEDED (dual-prefix):** no public dual `/silver` window; bootstrap is `bash scripts/sb-migrate-from-silver.sh` plus plugin postinstall, not only `/sb:migrate`. |
| Q5 | **SUPERSEDED (day-1 Codex+Claude).** Architecture: MVP = Cursor host adapter; Codex/Claude/OpenCode host adapters after MVP; `sb:agent-*` rename in the MVP ship. Historical: Day-1 hosts intent Cursor + Codex + Claude Code; OpenCode deferred. |
| Q6 | `critical_policy` only from in-repo reviewed hash-bound SB policies |
| Q7 | **SUPERSEDED (hard-cut delete public RFL).** Architecture: keep public `sb:review-fix-ladder` until Iterate exists (Verifier+Process-final-Val path or thin alias). Historical: hard cut RFL retirement. |
| Q8 | **Unlimited** Process-authorized Workflow nesting |
| Q9a | Rename **Broker → Authorizer** |
| Q9b | Runtime-home key storage |
| Q9c | **SUPERSEDED (path-as-CAS).** Architecture: Authorizer trust is `~/.silver-bullet/authorizer-trust/<repo-id>/`; host is metadata, not a second CAS. Historical: filesystem-safe `host/org/repo` from git remote. |
| Q10 | **Superseded** by universal Advisor A-loop |
| Q10′ | A-loop **two consecutive clean**; findings → I → re-A before V; V never starts with open Advisor findings |
| Q11 | **SUPERSEDED (ship-everything / pending lock).** Architecture MVP = Cursor host + six-role control plane + `/sb` + Task/work-spec + WBS projector + overlap worktrees + Advisor-first + process-final Val + existing `sb:agent-*` rename + bootstrap migrate (ILM-01) + live E2E. |

---

## Universal Advisor / A-loop

Canonical order: `pre-read Knowledge/Learnings → /sb Process resolve mint wrapping WF → composition Validation-loop vs user intent (remint/synthesize until comp_val_verified; fail returns to Orchestrator not Executor; remint may rewrite WBS before Executor spawn) → Advisor planning (Advisor produces plan of action) → plan-time Validation-loop vs work spec (Val-loop; Advisor revises until the plan satisfies the work spec) → one-way plan handoff to Executor → I-loop(s) → A-loop Mentorship after I → (return to I if needed) → Verification-loop (Ver-loop) → (merge code if extra host_native worktree) → top Workflow join → Process-synthesis I → Process-scope A two-clean → Process-scope Verification two-clean → Process-final Validation-loop vs original user intent → (on fail: Validation-loop starts with Advisor re-plan, then plan-time Validation-loop, then implementation as this order) → post-verify Knowledge/Learnings write (Authorizer-admitted deny-all Advisor `knowledge_postwrite` leaf) → return to parent`. AF and Workflow stop at Verification. Inner nested Workflow joins are not Process-scope. Ordinary AF/Workflow SM has no product `val_*` states. Six-role quality order is not an AF/WF (no `AF-meta-six-role`). Executor I still only after `plan_val_verified`.

Round-4 executor-draft **P-loop** (`poa_draft` / executor drafts then Advisor reviews) is **superseded**. Executor never plans.

- Advisor is fundamental and universal (not Marketing-only).
- Enables lower-cost executors + higher-cost Advisors.
- Verifier = strict spec check (never fixes). Advisor = review **and Mentor**. Authorizer = hook/admission TCB (not bound to Verifier LLM; not a preference key).
- **Validation-loop (Val-loop)** = Validator-role fit-for-purpose judgment (≠ Verification-loop / Ver-loop, ≠ Advisor). **2026-08-16 SUPERSEDE:** plan-time Validation-loop after each Advisor plan vs the **work spec** is required (two-clean spirit; Advisor revises from gap/flaw/enhancement feedback until the plan satisfies the work spec). Process-final Val remains entire output vs original user intent, once at roll-up. On Process-final fail, a Validation-loop starts with the Advisor (re-plan, then plan-time Validation-loop, then implementation as the quality order) — this supersedes fail-receipt-only / no re-plan; keep `launch_id` / occurrence / 9a–9c. Product Val is still not mandatory at AF/Workflow. **2026-08-16 SUPERSEDE (composition-Val):** after `/sb` mints a wrapping WF, composition-Val vs **user intent** is required before Advisor; fail → Orchestrator remint (catalog → compose existing AFs → synthesize) until `comp_val_verified`; remint may rewrite WBS before Executor spawn; Executor still only after `plan_val_verified`. Historical: always after V including after empty V-attest; mandatory at AF, Workflow, and Process; two-clean; fail receipt with no Advisor re-plan; WBS-from-intent at Process resolve frozen before composition-Val.
- Mentorship: project → `docs/knowledge/`; portable → `docs/learnings/` (git SoT via monthly K/L). agentmemory is the capture buffer (`memory_save`); `knowledge_postwrite` / FAST thin-capture MUST `memory_save` first (same durable text), then classify, then promote (AM opted-in failure → `blocked_knowledge_postwrite`; AM not opted in → `kl_write_am_skipped`; `kl_write` cites `am_id`; do not dual-write). Team retrieve is Graphify after `git pull` (do **not** reload K/L into each clone's AM).
- **Sidekick:** absorb host-as-advisor/mentor + AGENTS mentoring semantics; ignore / out of MVP: external-agent harness / DLGT / L3 take-over.
- Marketing specialty folded into universal A-loop for v1.

### Toolstack (five-tool routed)

| Step | Graphify | agentmemory | Context Mode | LeanCTX | RTK |
|------|----------|-------------|--------------|--------|-----|
| Pre-read | query covering K/L dirs (primary) + `.agentmemory/memory/` (unpromoted); **all** months; team K/L via Graphify after `git pull` (do **not** ingest K/L into AM) | capture buffer (`memory_save`); unpromoted recall — not a second git knowledge tree and not the team store; no K/L→AM reload | INDEX fallback: `INDEX.md` + current-month hot set; older months on demand, not discarded | large-file read if needed | n/a |
| I / A / V / Val | query before edits; update after code | save decisions/defects/receipts (capture buffer, not K/L git SoT) | analysis of diffs/tests | large-file read | shell compression when opted in |
| Post-verify K/L write | update after K/L doc edits; fan-out index after pull / session start | capture buffer; `memory_save` **first** then classify then promote AM → K/L with `am_id` provenance; AM opted-in failure → `blocked_knowledge_postwrite`; AM not opted in → `kl_write_am_skipped`; no dual-write; `synergy_max` AM auto-commit is not the K/L path; do **not** reload K/L into AM | analysis assist | not for durable write | n/a |

Synergy: save via agentmemory, retrieve via Graphify. Do not use `lctx_remember` or `lctx_graph` for code.
Alumnium is out of scope for this architecture ship (UI/browser evidence remains a separate opted-in surface).
**SUPERSEDED (Cursor-only five-tool).** Architecture: five-tool is opt-in, then mandatory on every **selected** runtime after an init probe passes (do not record a runtime if Graphify/etc. cannot run there yet; porting is in-scope for that selection). Historical: Cursor opt-in only; Codex/Claude INDEX/fallback; Graphify failure on those hosts not `blocked_knowledge_preread`.

---

## Recommended defaults (logged, not re-litigated)

- A-loop orthogonal to Levels 0–3.
- **SUPERSEDED / non-binding:** Advisor strictly stronger than that scope’s executor. Architecture spec wins: same `{ runtime, model, effort }` across Advisor and Executor is allowed; row 14 is retired (Init/Doctor warn only, never identity-equality hard-stop). See plan Roles and model preferences.
- Sidekick L3 host take-over out of scope.

---

## Next step

Round-2 Q12–Q22 remain historical except where the architecture banner supersedes them (Q12 I-loop two-clean; Q14/Q18/Q21/Q22 Val scope; Q11 packaging; note 15 trust path). Plan + Cursor mirror must stay byte-identical. **Do not implement product code from superseded Qs.** Skip `/sb:context` unless brownfield framing is needed.

---

## Artifacts

| Artifact | Path |
|----------|------|
| This brief | `.planning/router_subagent_surfaces_85bf9f09-CLARIFY-260717-143757.md` |
| Repo plan | `.planning/router_subagent_surfaces_85bf9f09.plan.md` |
| Cursor mirror | `~/.cursor/plans/router_subagent_surfaces_85bf9f09.plan.md` |

---

## Plan appendix (Q12–Q22)

These deepen the plan. Q1–Q3 and Q6 / Q8–Q10′ / Q13 / Q15–Q17 / Q19–Q20 remain as noted; **Q4 dual-prefix, Q5 hosts, Q7 RFL delete, Q9c trust path, Q11 packaging, Q12 I-loop two-clean, Q14/Q18/Q21/Q22 Val scope, note 15 path suffix** are **SUPERSEDED** by the architecture spec (banner). Authoritative detail remains in `.planning/router_subagent_surfaces_85bf9f09.plan.md` (byte-identical with `~/.cursor/plans/router_subagent_surfaces_85bf9f09.plan.md`).

| Q | Decision | Status |
|---|----------|--------|
| Q12 | I-loop requires two consecutive clean outcomes (same two-clean family as A-loop) | **SUPERSEDED** — spec-wins: I has no self-attested two-clean; two-clean applies to A, Verification, composition Validation-loop, plan-time Validation-loop, and Process-final Val (when product Val runs). Historical: I-loop two consecutive clean. |
| Q13 | V-loop requires two consecutive clean; ordinary SM uses `v_running`/`v_two_clean`/`v_verified` (`v_verified` = two-clean terminal) | **Ratified** (round-2). **2026-08-16:** product name is Verification-loop (Ver-loop); SM tokens unchanged. |
| Q14 | AF leaf Step runs A-loop two-clean once before AF V; that receipt satisfies the AF A-gate (no duplicate AF A); **no Step-level V**; work stays in worktree until AF V; then merge if overlap. **SUPERSEDED (AF Val mandatory):** AF does Verification not product Val; product Val is Process-final only. **2026-08-16:** plan-time Validation-loop vs work spec still runs after Advisor plan. | **SUPERSEDED in part** (AF Val) |
| Q15 | Nine `fitness_charter` fields + four canonical Iterate rung IDs as in plan Locked decisions | **Ratified** (round-2) |
| Q16 | `contracts/iterate-ladder-contract.lock.json` is binding/fence authority for Iterate | **Ratified** (round-2) |
| Q17 | Exact six migration ingress states: `freeze_new_source` → `project_pre_freeze_events` → `seal_drain_watermark` → `drain_old_epoch` → `producer_stopped` → `cutover` | **Ratified** (round-2, answer **A**) |
| Q18 | **SUPERSEDED (`val_*` on ordinary AF/Workflow SM).** Canonical order is mint → composition-Val vs user intent → Advisor → plan-time Val → I → A → Verification at AF and Workflow; product Val is Process-final only (separate last step after Workflow join). Ordinary AF/Workflow SM: `pre_read_pending` → composition-Val `comp_val_*` at Process resolve (not AF/Workflow product `val_*`) → `advisor_planning` / plan-time `plan_val_*` / `plan_handed_off` → `i_*` → `a_*` → `v_*` → (merge if extra host_native worktree) → join. Process-final: `val_*` → `kl_post_write_pending` → `scope_complete`. **2026-08-16 SUPERSEDE:** plan-time Validation-loop vs work spec is required; Process-final Val-fail starts Advisor re-plan (not fail-receipt-only). **2026-08-16 SUPERSEDE (composition-Val):** composition-Val vs user intent after `/sb` mint; remint may rewrite WBS before Executor spawn; Executor still only after `plan_val_verified`. Historical: `val_*` on the ordinary AF/Workflow machine. Historical `p_*`/`poa_*` names retired with executor-draft P-loop. | **SUPERSEDED in part** (`val_*` on AF/Workflow) |
| Q19 | Iterate work states are orthogonal to `authority_status` | **Ratified** (round-2) |
| Q20 | `awaiting_revalidation` (in-rung) ≠ `awaiting_baseline_revalidation` (baseline-stale) | **Ratified** (round-2) |
| Q21 | AF Validation-loop (≠ V, ≠ A); **"Val always after V"** → **B**: `I → A → V → (merge if overlap) → Val`; Validator-role owner; two-clean; Val still required after empty V-attest | **SUPERSEDED** — Process-final product Val only; AF/Workflow run Verification. **2026-08-16 SUPERSEDE:** plan-time Validation-loop vs work spec after Advisor plan is required (not AF product Val). |
| Q22 | AF **and** Workflow **and** Process all **must** run Validation-loop, always after V | **SUPERSEDED** — only Process runs product Val. **2026-08-16:** plan-time Validation-loop (plan vs work spec) is additional. |

---

## Clarify Decision Addendum (round-2 — 2026-07-20) — COMPLETE

Interactive `/silver:clarify` round-2 (Grok 4.5 High). All blockers answered; incorporated into plan + mirror.

### Ratified

- **Q12** — **SUPERSEDED (I-loop two consecutive clean).** Spec-wins: I has no self-attested two-clean. Two-clean applies to A, Verification, composition Validation-loop, plan-time Validation-loop, and Process-final Val. Historical: I-loop two consecutive clean (same two-clean family as A-loop)
- **Q13** — V-loop two consecutive clean; SM `v_running` / `v_two_clean` / `v_verified`. **2026-08-16:** product name is Verification-loop (Ver-loop); SM tokens unchanged.
- **Q14** — AF leaf Step A-loop two-clean once before AF V; receipt satisfies AF A-gate (no duplicate AF A); then AF V. **SUPERSEDED:** then AF Val mandatory
- **Q15** — Nine `fitness_charter` fields + four canonical Iterate rung IDs
- **Q16** — `contracts/iterate-ladder-contract.lock.json` is Iterate binding/fence authority
- **Q17** — Answer **A**: exact six ordered migration ingress states
- **Q18** — **SUPERSEDED (`val_*` on ordinary AF/Workflow SM).** Canonical I → A → V at AF/Workflow; Process-final Val after Workflow join. Historical round-2 text put `val_*` on the ordinary machine; P-loop `p_*`/`poa_*` inserted by round-4 then superseded by Advisor-first `advisor_planning`/`plan_handed_off`
- **Q19** — Iterate work states orthogonal to `authority_status`
- **Q20** — `awaiting_revalidation` (in-rung) ≠ `awaiting_baseline_revalidation` (baseline-stale)
- **Q21** — Answer **B** / **"Val always after V"**: **SUPERSEDED** by Process-final product Val only (historical: order `I → A → V → Val` at AF). **2026-08-16 SUPERSEDE:** plan-time Validation-loop after Advisor plan vs work spec is now required (distinct from product Val).
- **Q22** — Answer **B**: **SUPERSEDED** (historical: AF + Workflow + Process all mandatory Validation-loop). **2026-08-16:** plan-time Validation-loop (plan vs work spec) is additional; product Val remains Process-final only.

### Validation-loop

| Axis | Spec |
|------|------|
| Distinction | Validation-loop (Val-loop) ≠ Verification-loop (Ver-loop; historical “V-loop”) ≠ Advisor (A-loop) |
| Meaning | Fit-for-purpose / right-thing judgment (Planning/Validation tier) |
| Order | **`/sb` mint wrapping WF → composition-Val vs user intent → remint/synthesize until `comp_val_verified` → Advisor plan → plan-time Val → I → A → Verification** at AF and Workflow. After the **top** Workflow join: Process-synthesis I → Process-scope A/Verification (mandatory) → **Process-final Val**. Chain: `/sb` mint → composition-Val vs user intent → `Advisor plan → plan-time Validation-loop vs work spec → I → A → Verification → (merge code if extra host_native worktree) → top Workflow join → Process-synthesis I → Process-scope A two-clean → Process-scope Verification two-clean → Process-final Val vs original user intent`. Remint may rewrite WBS before Executor spawn; Executor still only after `plan_val_verified`. On Process-final fail: Validation-loop starts with Advisor re-plan, then plan-time Validation-loop, then implementation as this order. Inner nested Workflow joins stop at Verification. **SUPERSEDED:** always after V at every scope; `val_*` on ordinary AF/Workflow SM; Validation only once at roll-up with no plan-time Validator; fail-receipt-only / no re-plan; WBS-from-intent frozen before composition-Val; six-role loop as AF/WF |
| Scopes | Product Val: Process-final only. Composition-Val: after each `/sb` mint vs user intent (not AF/Workflow product Val). Plan-time Val: after each Advisor plan vs the work spec (not AF/Workflow product Val). Historical: AF, Workflow, and Process — all mandatory product Val |
| Owner | Validator role (not Verifier; not Advisor; not Authorizer). Orchestrator owns remint. Advisor may draft new AF/WF definitions. Authorizer admits synthesis/planning/exec spawns and does not mint WFs. Advisor revises the plan from plan-time Val feedback and re-plans on Process-final Val-fail |
| Two-clean | Yes for A, Verification, composition Validation-loop, plan-time Validation-loop, and Process-final Val (when product Val runs). I has no self-attested two-clean |
| Dirty | Return to I; re-satisfy A if needed; re-Verification if contract affected; product Val again only at Process-final (after Advisor re-plan + plan-time Validation-loop + ordinary quality order); KL post-write only after Process-final Val two-clean |

### Still open

None for round-2.

**Do not** overwrite multi-AI `.planning/CLARIFY.md`. Repo plan and Cursor mirror must remain **byte-identical**.

---

## Clarify Decision Addendum (round-3 — 2026-07-20) — COMPLETE

Interactive requirements lock (Grok 4.5 High). Incorporated into plan + mirror.

### Ratified

- **Launch prompt + work spec (mandatory admission gate)** — Every host subagent launch must include a prompt-engineered launch prompt **and** a well-specified work spec with clear **output** and **outcome** requirements. Fail-closed without them: blocker `blocked_launch_prompt_spec` (no spawn / no lease/capability/channel).
- **Work-spec exactly these fields** — `goal_outcome`, `required_outputs`, `acceptance_criteria`, `scope_bounds`, `context_refs` (Knowledge/Learnings/pre-read + packet/ancestry refs). Unknown properties fail closed (`additionalProperties: false`). Prompt binds work-spec hash, WBS path, role, denies, callback/return contract.
- **ASCII WBS progress visualization (mandatory UX)** — On every governing-scope step transition and every user-facing status: path `Process > Workflow > AF > Step` (optional ` > Skill`), markers `[x]` complete / `[>]` current / `[ ]` pending / `[!]` blocked. Missing viz → `blocked_progress_viz`.
- Traceability: **LPS-01** (`VAL/TST-RFL-616`), **WBS-01** (`VAL/TST-RFL-617`).

### Still open

None for round-3.

**Do not** overwrite multi-AI `.planning/CLARIFY.md`. Repo plan and Cursor mirror must remain **byte-identical**.

---

## Clarify Decision Addendum (round-4 — 2026-08-12) — SUPERSEDED

**Naming:** the pre-implementation plan-of-action Advisor gate is **Advisor-first one-way planning** (not an executor-draft P-loop). Round-4 text below is historical and **must not** be treated as current spec.

Interactive requirements lock. Incorporated into plan + Cursor mirror, then **superseded 2026-08-14**.

### Historical (do not implement)

1. ~~P-loop / Planning Loop: every worker/executor drafts a durable plan of action, submits it to a stronger-tier Advisor~~ → **replaced:** Advisor owns planning; Executor never drafts; one-way handoff; `advisor_planning` / `plan_handed_off`.
2. On-demand Advisor consult during `i_running` remains (advice only). Material plan change = **Advisor receipt**, not Executor self-assessment; **SUPERSEDED:** `blocked_replan_budget` as job bound — replaced by the finite 4-step ladder.
3. ~~Ordinary SM: `poa_draft`/`poa_advisor_review`/`poa_satisfied`~~ → **replaced:** `advisor_planning`/`plan_handed_off`.
4. Traceability **POA-01** / `VAL/TST-RFL-618` **retained** with renamed semantics.

**Do not** overwrite multi-AI `.planning/CLARIFY.md`. Repo plan and Cursor mirror must remain **byte-identical**.

## Clarify Decision Addendum (round-5 — 2026-08-14) — SUPERSEDED IN PART (architecture spec wins)

Aligns this brief with an earlier plan draft. **SUPERSEDED** where it contradicts the architecture spec banner (including Q18 `val_*` on ordinary AF/Workflow SM).

- Spawn: **SUPERSEDED** “Orchestrator session is the only Task/Agent syscall”. Architecture: hooks never invoke Task; nested Task is allowed only for descendants whose work-spec the ancestor already persisted before this child's Task started; root Orchestrator is always Task-capable; parent-proxy for new projector writes at any remaining depth, and always at remaining depth 0.
- Effort: `low|medium|high|xhigh`; Executor = highest available on host; do not collapse xhigh; Max is not a Cursor Task effort.
- Merge: after AF Verification two-clean, before **Process-final** Val (code-only). **SUPERSEDED:** AF Val. Val-fail: do not un-merge; Validator fail receipt; Orchestrator+Advisor map to WBS. **2026-08-16 SUPERSEDE:** not Executor I+V only / fail-receipt-only. A Validation-loop starts with the Advisor (re-plan → plan-time Validation-loop → implementation as the quality order); product Val again only at Process-final; keep `launch_id` / occurrence / 9a–9c.
- MVP slice supersedes Q11 “ship everything together”.
- `{ runtime, model, effort }`; Authorizer not a preference key; in-repo slug catalog; **SUPERSEDED:** five-tool Cursor-opt-in — opt-in then mandatory on every selected runtime after init probe.
- `silver`→`sb` is in the architecture ship; Q4 = `sb:<route>`; **SUPERSEDED:** dual-prefix migrate window; bootstrap is `scripts/sb-migrate-from-silver.sh`.
- Full one-line checklist: architecture spec Document control (no `## Pending user lock`).

## RFL incorporate notes (2026-08-12)

Clarifying elaborations recorded into the plan during adversarial RFL (do not reopen these Qs), **as amended by the 2026-08-14 draft** (Advisor-first; no executor-draft P-loop):

1. **Advisor-plan scope:** Applies to **ordinary-delivery** implementation workers/executors only (Workflow/AF/Step/Process-synthesis). Deny-all leaf control-plane roles (`advisor`, `verifier`, `validator`, `defect_escalation`) are exempt from plan-of-action handoff. **Iterate rung implementers** use charter + baseline admission (not ordinary `advisor_planning`). Historical “P-loop” language is retired.
2. **Migration Val receipts:** Legacy RFL maps into I/A/V/**Validation-loop** records; missing Val history uses migration-only `val_loop_not_applicable` (never satisfies live Val gates).
3. **Levels 0–3 repair return:** Successful repair → original owner **I → A → V** (re-A before V), not V-only. Ordinary repair never enters Iterate `awaiting_baseline_revalidation`; Iterate repair does.
4. **Authorizer path fallback:** **SUPERSEDED (path-as-CAS).** Architecture: `~/.silver-bullet/authorizer-trust/<repo-id>/` with host as metadata. Historical injective `host/org/repo/<remote_id_sha256>` (full 64-hex digest; no truncated prefix) from canonical remote bytes; `local/default/<repo_dir_sha256>` when git remote absent/unparseable; trust lookup verifies stored canonical identity.
5. **KLW post-write:** Insight write **or** durable `kl_post_write_no_insights` satisfies KLW-01. Owner is the Authorizer-admitted deny-all Advisor `knowledge_postwrite` leaf (ordinary Executor / Orchestrator / parent must not Write K/L except via this leaf). **Same leaf, ordered effects:** `memory_save` **first** (same durable text that would have been the K/L entry), then classify, then promote into `docs/knowledge/` / `docs/learnings/`. Every `kl_write` and K/L monthly entry cites `am_id` (or AM content hash); `kl_write` without AM provenance is invalid. AM opted-in save failure / missing `am_captured` → `blocked_knowledge_postwrite` (do not write K/L anyway). AM not opted in → `kl_write_am_skipped`. AM is capture buffer; K/L is git SoT; do not dual-write; `synergy_max` AM auto-commit is not the K/L path. FAST thin capture is the same family after the FAST leaf (not a Job). Team fan-out is Graphify after `git pull` (do **not** reload K/L into each clone's AM).
6. **LPS schema artifact:** `contracts/work-spec.schema.json` is a Row-1 reviewed source; explicit `VAL/TST-RFL-612`–`618` obligation paragraphs required.

7. **Work-spec immutability:** Task five-field hash immutable per `launch_id`; Advisor plan artifact may be replaced under the same `launch_id` with an Advisor receipt. Replacement is revision-bound: monotonic `plan_revision` + plan-artifact hash on `launch_intent` / continuation resume / callback acceptance; replacement fences the prior revision. Launch-payload CAS key remains `(prompt_hash, work_spec_hash)`. New `launch_id` only if the Task itself (goal/scope/acceptance) changes.
8. **Control-plane quality-loop exemption:** deny-all leaf roles exempt from Advisor-plan **and** recursive I/A/V/Val; role receipt then terminate.
9. **LPS host delimitation:** Cursor single-string `Task.prompt` uses `<<<SB_LAUNCH_PROMPT>>>` / `<<<SB_WORK_SPEC_JSON>>>` / `<<<SB_END>>>` envelope.
10. **Leaf Step handoff:** leaf Step terminates quality loops at `a_two_clean`; parent AF owns V → (merge if overlap). **SUPERSEDED:** AF Val → K/L post-write. No Step-level V. Val is Process-final only.

11. **Discriminated callbacks (Sol High):** ordinary producers bind `launch_id + scope_execution_id + execution_attempt_id` (`producer_kind=ordinary_delivery`); only `producer_kind=iterate_attempt` binds Iterate contract-binding/rung/`attempt_id`. **Callback fence is generic** for both kinds. Early-callback dedupe CAS key is immutable `(project_id, source_operation_id)` only — token/generation/epoch/channel/seq are validated values/indexes, not key components (Sol XHigh c1).
12. **Process-synthesis executor:** **SUPERSEDED (owns I of child work).** Architecture: Process-synthesis I is packet-local composition/findings only; product implementation is Workflow/AF Executors. Historical: Authorizer-admitted Process-scope synthesis/executor owns Process Advisor-plan→I→A→V→Val after top Workflow returns; parent orchestrator never implements (Orchestrator session does spawn).
13. **Step→Advisor request edges:** implementation Step/executor (or AF on behalf of Step) may request Advisor planning and in-I consult; Authorizer-only admission; Orchestrator session spawns; §6 deny-generation must allow these edges.
14. **Active ordinary RFL migration:** non-Iterate re-admit path with `blocked_legacy_rfl_readmit`; never uses Iterate baseline states; Iterate authority only via fresh post-migration activation.
15. **Injective Authorizer trust identity:** **SUPERSEDED (path-as-CAS / `remote_id_sha256` suffix).** Trust path remains `~/.silver-bullet/authorizer-trust/<repo-id>/`. Historical: canonical remote bytes + **full** `remote_id_sha256` path suffix (no truncated prefix); trust lookup verifies stored canonical identity before key use (Sol XHigh c1).
16. **Discriminated Levels 0–3 repair (Sol High c3):** ordinary repair re-enters ordinary SM (never `awaiting_baseline_revalidation`); Iterate repair uses baseline-revalidation path. Level-1 is a separate repair leaf, not the Verification-loop Verifier.
17. **Active RFL → live pre-read/Advisor-plan:** post-migration prospective ordinary I edits require fresh `pre_read_pending → advisor_planning`; historical mapped evidence never satisfies live gates.
18. **Iterate Advisor-plan exemption:** rung planning gate is charter + baseline admission/revalidation — not ordinary `advisor_planning`.
19. **Plan freshness:** material plan-of-action change = Advisor receipt that ordered steps changed; plan artifact may be replaced under the same `launch_id` with revision binding (`plan_revision` + plan-artifact hash on `launch_intent` / resume / callback acceptance; replacement fences the prior revision). **SUPERSEDED:** `blocked_replan_budget` as job-stop — finite 4-step ladder. New `launch_id` only if the Task five-field hash changes.
20. **Process-repair delegation (Sol XHigh c2/c3):** ancestry-preserving Authorizer-admitted path `process_repair_pending` → `process_repair_delegated` → reopen deepest leaf through declared owner chain only; invalidate ancestor A/V/KL evidence; keep already-merged sibling AFs; bottom-up joins + I+V (A judges I-clean) before Process-final Val; forbid direct nested-AF/Workflow→Process callback; Process-synthesis never mutates AF/Work-Skill artifacts. Process-scope A dirty / V findings use this same `process_repair_pending` path (not return to Process-synthesis for implementation) then re-run 9a–9c after rejoin; distinct from Process-final Val-fail 9a–9c; do not add A to ESC-02. **SUPERSEDED:** re-run Val at every repair hop.
21. **Blocker total precedence:** complete ordered mutually exclusive table over canonical `blocked_*` IDs (now including `blocked_replan_budget` and `blocked_executor_unavailable`; `blocked_triage_unresolved` retired as Val-triage) with disjoint predicates and explicit resume targets.

22. **Non-material RFL polish (Opus High/XHigh + earlier-rung nits, 2026-08-12):** occurrence-ordinal cross-refs use § anchors (not brittle line numbers); `drain_only` named in ingress substate whitelist; `launch_intent` enumerates Authorizer-minted `scope_execution_id`/`execution_attempt_id`; dependency-matrix rows 2–4 name WBS/LPS/POA acceptance; ESC-01 repair-rejoin ordinal fixture; uniform `revalidation_cycle_id` `trigger_kind` 3-tuple; TRUST-01 local-fallback realpath/symlink fixture + migrate re-bind; LPS envelope escaping + RFC 8785 JCS hash equality; labelled leaf-Step `a_two_clean → step_yield` terminal; ordinary budget/oscillation note (no Iterate-style ladder-conflict counterpart); Process-synthesis Workhorse tier; A-loop Mentor continuity + knowledge-candidate buffering; rows 6↔12 phase labels + overlap fixture; callback gap-vs-unresolved tiebreaker; I-loop dirty-round disposition mapping; `final-validation` = `val_validated` synonym; mandatory A/Val "floor" wording.

23. **GPT-5.6 Sol Extra High ladder-2 rerun (2026-08-15):** (H1) parent-proxy failure is total: `launched`→`failed` with `failure_receipt_id` then `failed`→`resumed` (idempotent; distinct from locked success `completed`→`resumed`); failed-without-resumed is CORR-17. (H2) POA-01 plan replacement binds monotonic `plan_revision` + plan-artifact hash + prior-revision fence on `launch_intent` / continuation resume / callback acceptance; launch-payload CAS key remains `(prompt_hash, work_spec_hash)` (do not reopen launch-CAS-keys-as-current-product). (H3) Process-scope A dirty / V findings enter `process_repair_pending` then 9a–9c rerun after owner-chain rejoin; distinct from Val-fail 9a–9c; no A on ESC-02. (M1) KLW-01 post-write owner is Authorizer-admitted deny-all Advisor `knowledge_postwrite` leaf (`kl_write` or `kl_post_write_no_insights`); not ordinary Executor; not parent-authored insight text.

---

## Clarify Decision Addendum (round-6 — 2026-08-16) — SUPERSEDED IN PART

Interactive requirements lock for **Global Status** (team dashboard). User supplied a dense requirement. Per `/silver:clarify` decision taxonomy (`decision_class: blocking` only), no user question was required. Fail-closed defaults below are **clarify locks** (logged, not silent invention). Incorporated into both plan copies. **SUPERSEDED IN PART (round-11 ACCEPT):** merge-by `(instance_id, job_id)` as semantic row CAS; treating `blocked_global_status_push` as a Job hard-stop. **Do not** reopen ESC-02 no A; row 14 retired; Authorizer not Approver; no bare V-loop; no `AF-meta-six-role`; wrap at `/sb`; BOA parallel; composition-Val + synthesize for **non-trivial**; classified-trivial does not synthesize; Board of Advisors.

### Problem statement

Long-running SB work is visible to the **operator** as local ASCII WBS viz in the host agent. **Managers and teammates** on the same repo have no shared, at-a-glance view of which Jobs are running, on which instance, at which workflow step. Local viz is not that dashboard.

### Current context

WBS-01 already mandates projector-only ASCII WBS (`Process > Workflow > AF > Step`) on operator status surfaces (`blocked_progress_viz` if omitted). WBS lives under `$primary_checkout/.planning/` and is not pushed as a team dashboard. CI (`.github/workflows/ci.yml`) is `on: [push, pull_request]` with **no** `paths-ignore` and **no** existing `[skip ci]` pattern. `.sb/` does not exist in-repo yet.

### PM framing

- **Who:** managers and teammates watching a shared project; distinct from the operator at the host session.
- **Job to be done:** see all in-flight user intents across every SB instance of this repo, in real time, without opening each operator's agent UI.
- **Value:** one markdown file on `main` is cloneable, reviewable, and linkable; no extra product UI in MVP.
- **Success:** a teammate pulling `origin/main` can name every active Job, which Git-identity SB owns it, and which workflow step it is on.

### Options considered

1. **Reuse WBS as the team file** — reject: WBS is a local control-plane ledger (packets, CAS, receipts); not a manager document; extra worktrees omit `.planning/`.
2. **Host-session ASCII viz only** — reject: user explicitly distinguished Global Status from operator viz.
3. **Per-instance status branches / force-push to a status ref** — reject: user required `main`; force-push is unsafe with concurrent instances.
4. **Recommended:** per-UTC-day markdown on `origin/main` at `.sb/status/STATUS-YYMMDD.md`, written by a named Orchestrator helper, merged by CAS/retry.

### Recommendation (user-locked)

1. **Job = user intent.** One Job is one original user intent captured at `/sb` Process resolve (`original_intent_hash`). A Job is **not** a Process packet, **not** a `launch_id`, **not** a Task/work-spec five-field hash. Process is the control-plane execution of that Job. Remint/composition-Val stays the same Job. A new `/sb` resolve for a different intent mints a new Job. Child `launch_id`s are occurrences under the Job.
2. **Global Status path:** repo-root [`.sb/status/STATUS-YYMMDD.md`](.sb/status/STATUS-YYMMDD.md) (user wrote `/.sb/status/STATUS-YYMMDD.md`). `YYMMDD` is **UTC**. One file per UTC day; prior days remain as history (no auto-delete).
3. **Audience:** managers/teammates. **Does not replace** local ASCII WBS viz (WBS-01 / `blocked_progress_viz` unchanged).
4. **Who writes:** Task-capable Orchestrator session invokes named helper `hooks/lib/global-status-projector.sh` (same helper class as `wbs-projector.sh`). Parent≠implementer: Orchestrator does not raw-Edit the file. **Executor / Advisor / Verifier / Validator must not write or push status.**
5. **When:** real-time on governing-scope step change (same moments the WBS projector would refresh operator viz), plus Job start, Job complete, and Job block.
6. **Identity:** each instance is **`{git_user_id}'s SB`**. `git_user_id` = `git config --get user.email` if set, else `user.name`. `instance_id` = SHA-256 of canonical remote bytes + `git_user_id` (full 64-hex; no truncated prefix).
7. **Push target:** **`origin/main` only.** Status commits must not land on the operator's feature-branch index. Helper uses git main / a dedicated main worktree / fetch+commit on `main`; never mixes status into extra-tree product branches. Extra `host_native` worktrees **omit** `.sb/` (ledger-omit).
8. **All SB instances** on that repo update the **same** day's file.

### At-a-glance file contract (locked)

Markdown, human-readable. Required sections:

1. **Header** — UTC date, `generated_at` (ISO-8601 UTC), repo identity (canonical remote), schema/version note.
2. **Instances** — each `{git_user_id}'s SB` with `instance_id` short display, host/runtime (no secrets), `last_heartbeat`.
3. **Jobs (active)** — table: `job_id` (short of `original_intent_hash`), one-line intent summary, instance display name, `process_id`, public route (`sb:<route>`), current path `Process > Workflow > AF > Step` (optional ` > Skill`), quality-loop phase (`comp_val_*` / `advisor_planning` / `plan_val_*` / `i_running` / `a_*` / `v_*` / Process-scope / `val_*` / etc.), started_at, updated_at.
4. **Blocked** — same columns plus canonical `blocked_*` id.
5. **Completed today** (UTC) — job_id, intent summary, instance, completed_at.

Forbidden in the file: secrets, tokens, env values, private keys, prompt bodies, work-spec full text, PII beyond the Git user id already used as instance identity, raw file contents.

### Clarify locks (fail-closed defaults — not silent invention)

| Lock | Default | Why not blocking |
|---|---|---|
| Concurrent `main` pushes | Compare-and-swap: fetch `origin/main`, merge this instance's Job **occurrence** rows by `(instance_id, gst_row_id)` where `gst_row_id = H(original_intent_hash, process_id)`, commit, push. On non-fast-forward, retry (ceiling 8). **Never force-push.** Exhaustion stamps `gst_stale` on the local WBS; **Job continues** (row 34 is dashboard-only / non-classifying). **SUPERSEDED:** merge-by `(instance_id, job_id)` as semantic row CAS. | User required all instances on one file; CAS is the only safe merge. |
| Skip CI | Status-only commits **must** include `[skip ci]` in the message **and** CI `paths-ignore: ['.sb/status/**']` (belt and suspenders). Plan had no skip-ci pattern; this is the lock. | Prevents dashboard heartbeats from retriggering full CI. |
| No secrets/PII | Helper refuse-write if payload would include secrets; redact intent to one line. | Safety; not a product fork. |
| Job vs Process vs `launch_id` | As recommendation (1). | User said Job = user intent. |
| Writer | Named helper, Orchestrator-only, not Executor. | Parent≠implementer already locked. |
| Relationship to WBS | Global Status is a **projection** of Jobs from WBS/packets; WBS remains the live execution ledger. GST-01 does not satisfy WBS-01. | Complements, does not replace. |
| Protected `main` / no push rights | Stamp `gst_stale` (or equivalent) on the local WBS; **Job continues**. Row 34 `blocked_global_status_push` is dashboard-only / non-classifying (not a Job hard-stop). Local ASCII viz **still** updates. Resume of dashboard freshness: restore push rights / branch protection exception for `.sb/status/**` or retry CAS — **not** a gate on `scope_complete`. Delivery does not pretend the team dashboard is current. **SUPERSEDED:** treating row 34 as a job-stopping classifier. | User required real-time push to `main`; GST is a dashboard, not a delivery gate. |
| Git identity missing | Both `user.email` and `user.name` empty → `blocked_global_status_identity` (row 35). | Fail-closed identity. |
| Timezone / retention | UTC day file; keep historical `STATUS-*.md`. | Matches other SB UTC artifacts. |
| MVP | GST-01 is in the Cursor MVP slice (same ship as WBS projector). | Dashboard is the stated need for long-running workflows. |

### Assumptions to validate later

- Branch-protection rules may need a path exception or a bot identity for `.sb/status/**`; product fail-closed is row 34 until that exists.
- High-frequency step changes may produce many status commits; `[skip ci]` + `paths-ignore` is the mitigation (not a batching redesign unless later evidence shows `main` history pain).

### Unresolved questions

None blocking. No user question this round.

### Next SB lifecycle step

`silver:plan` incorporation into both byte-identical plan copies (this session). Do not start GPT Max / Opus. Stay on `main`. Do not commit.

**Do not** overwrite multi-AI `.planning/CLARIFY.md`. Repo plan and Cursor mirror must remain **byte-identical**.

---

## Clarify Decision Addendum (round-7 — 2026-08-16) — SUPERSEDED IN PART

Interactive requirements lock for **trivial / Q&A coverage**. User-directed close of a spec gap: 100% AF coverage + “must synthesize if catalog cannot cover” would mint new AFs/WFs for trivial Q&A. That is overkill. Per `/silver:clarify` decision taxonomy (`decision_class: blocking` only), no user question was required. Fail-closed defaults below are **clarify locks** (logged, not silent invention). Incorporated into both plan copies.

**Do not** reopen ESC-02 no A; Authorizer not Approver; no `AF-meta-six-role`; wrap at `/sb`; BOA parallel for non-trivial Advisor.

**Superseded by round-8 (same day):** composition-Val accepts canned cover; Job still exists; Global Status still records it. Classification rule, catalog IDs, no-synthesize, and parent≠implementer remain.

### Classification rule (locked)

At `/sb` resolve, classify **trivial** when the intent is Q&A, one-shot explain, or otherwise has no durable product edit.

### Catalog IDs (locked — shipped, not invented)

| Layer | Public / catalog id |
|---|---|
| Wrapping Workflow | `sb:fast` (`WF-SILVER-FAST`; APO slug `silver-fast`; public IDs `sb` / `sb:` / `/sb` only — do not invent `/silver` as a live window) |
| Covering Atomic Flow | `AF-FAST-PATH` (shipped catalog AF). Do not mint a new `AF-qa`. |

Wrapping Workflow still exists (AF never sits directly under Process).

### Composition-Val and remint (historical — Job/GST/composition-Val-accepts superseded by round-8)

- ~~Composition-Val **accepts** that canned trivial AF as covering intent (`comp_val_verified`).~~ **Superseded:** no composition-Val spawn; cover is by classification.
- Remint **must not** enter synthesize (step 3) for classified-trivial. *(still locked)*
- ~~**Job still exists.** **Global Status still records it.**~~ **Superseded:** not a Job; no GST.
- parent≠implementer: Orchestrator does not implement; trivial I is `AF-FAST-PATH`'s Executor or a deny-all Q&A leaf under that AF — not a new synthesized skill. *(still locked)*
- Non-trivial work is unchanged: catalog → compose existing AFs → synthesize; still forbidden to execute with no AF coverage. *(still locked)*

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start GPT Max / Opus.

---

## Clarify Decision Addendum (round-8 — 2026-08-16) — COMPLETE

Interactive requirements lock for **FAST quality-order exemption** and **Job/GST exclusion**. User-directed: classified-trivial / `sb:fast` must not run the six-role quality order, is not a Job, and must not appear on GST-01. Per `/silver:clarify` decision taxonomy (`decision_class: blocking` only), no user question was required. Fail-closed defaults below are **clarify locks** (logged, not silent invention). Incorporated into both plan copies.

**Do not** reopen ESC-02 no A on non-trivial escalation; Authorizer not Approver; no `AF-meta-six-role`; wrap at `/sb`; BOA parallel for non-trivial Advisor; no synthesize on trivial.

### Quality-order exemption (locked)

Classified-trivial (`sb:fast` / `WF-SILVER-FAST` / `AF-FAST-PATH`; the whole `sb:fast` surface) **must not** run:

- Advisor / Board of Advisors
- composition-Val (no Validator spawn; canned cover is by classification, not a Val-loop)
- plan-time Validation-loop
- A-loop Mentorship
- Verification-loop
- Process-final Validation-loop

### Job / GST exclusion (locked)

Classified-trivial / `sb:fast` is **not a Job** and **must not** appear on Global Status (GST-01). No `original_intent_hash` Job mint. No GST projector write. Job remains user intent for **non-trivial** `/sb` work only.

### Still required (locked)

- Orchestrator classifies trivial and wraps `sb:fast`
- parent≠implementer: Orchestrator does **not** answer/implement
- one Authorizer-admitted Executor (or deny-all Q&A leaf) under `AF-FAST-PATH`

### Fail-closed reclassify (locked)

If the work is not actually trivial (durable product edit, multi-file change, etc.): **fail-closed reclassify** as non-trivial and enter the full mint → composition-Val → Advisor → plan-time Val → I → A → Verification / Process-final Val quality order. That reclassified work **is** a Job and **does** hit GST. Do not silently stay on FAST.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start GPT Max / Opus.

---

## Clarify Decision Addendum (round-9 — 2026-08-16) — SUPERSEDED IN PART

Interactive requirements lock for **current-month context-load cap**, **FAST thin capture**, and **AM→K/L promote**. User-directed; per `/silver:clarify` decision taxonomy (`decision_class: blocking` only), no user question was required. Fail-closed defaults below are **clarify locks** (logged, not silent invention). Incorporated into both plan copies (byte-identical).

**Superseded in part by round-10 (same day):** AM-first fail-closed `kl_write` provenance (`memory_save` first, same durable text, then classify, then promote; AM opted-in failure → `blocked_knowledge_postwrite`; AM not opted in → `kl_write_am_skipped`) and team Graphify fan-out (do **not** reload K/L into each clone's agentmemory). Current-month context-load cap remains.

**Plan SHA-256 (both copies):** `6bd715cd81abbfa367b81674624d55eabea02f7a2e44bf58fe499c9791dd6a52`  
**Invalidated prior SHA-256:** `276806b3091dea955ae6aeecf06562d4f9b7b8ceb105580238e0550d252aaae8`

**Do not** reopen ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job; FAST not on GST; FAST exempt from six-role quality order; wrap at `/sb`; BOA parallel for non-trivial Advisor; no synthesize on trivial; no `AF-meta-six-role`.

### Current-month context-load cap (locked)

"Current month files" is a **context-load cap**, not "older months do not count."

- Graphify-first when five-tool is opted in for that runtime: one query covering `docs/knowledge/`, `docs/learnings/`, **and** `.agentmemory/memory/` when agentmemory is opted in. Graphify searches **all** months. A Graphify miss is `blocked_knowledge_preread`.
- INDEX fallback **only** when five-tool is not opted in: load `INDEX.md` (catalog of **all** months) **plus current month files** as the default hot set. Older `YYYY-MM.md` are **not discarded**; pull on demand when INDEX or the question points at them. Do not dump every historical month into the pre-read context. Old monthly files are never modified.

### FAST thin capture (locked)

Exemption from quality-order/Job/GST does **not** mean throw away the insight. After the FAST leaf answers, Authorizer admits a **thin capture** deny-all leaf (same family as `knowledge_postwrite`, not a second Job):

- Always `memory_save` / agentmemory capture of the Q&A insight candidate.
- If classified **durable** (project-scoped or portable learning, not ephemeral session noise): promote/write into `docs/knowledge/YYYY-MM.md` or `docs/learnings/YYYY-MM.md` via the K/L mechanism.
- If classified **ephemeral** (operational chatter, no reusable insight): AM-only; durable `kl_post_write_no_insights` / equivalent FAST no-insight receipt is enough.
- Must not mint `original_intent_hash`, must not write GST, must not run Advisor/Board/Val/Ver/A.

FAST still skips Job steps 2–11 but **does** run this thin capture after the FAST leaf.

### AM → K/L promote (locked)

AM and K/L are the **same insights at two stages**, not two equal git knowledge trees.

- **Capture buffer:** agentmemory only (`memory_save`). Session/episodic/noisy/operational notes stay here.
- **Git source of truth:** `docs/knowledge/` (project) and `docs/learnings/` (portable) via the existing K/L monthly mechanism. `knowledge_postwrite` (Jobs) and FAST thin-capture **read AM candidates and export/promote** classified new insights into those dirs. That is the "export" — **not** committing raw `.agentmemory/memory/` dumps as the team knowledge product.
- **Do not dual-write** the same durable insight as both a git-tracked AM markdown file and a K/L file.
- Graphify retrieve: K/L dirs are primary for Job pre-read; AM remains in the same Graphify query for unpromoted session recall.
- `synergy_max` auto-commit of `.agentmemory/memory/` is **not** the K/L path (off-by-default / not required for this control plane). Existing already-tracked AM files are out of scope.
- KLW-01 still owns Job post-Val durable docs: the leaf promotes from AM → K/L (or `kl_post_write_no_insights`). Orchestrator does not author insight text.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start GPT Max / Opus.

---

## Clarify Decision Addendum (round-10 — 2026-08-16) — COMPLETE

Interactive requirements lock for **AM-first fail-closed K/L provenance** and **team Graphify fan-out (no K/L→AM reload)**. User-directed; per `/silver:clarify` decision taxonomy (`decision_class: blocking` only), no user question was required. Fail-closed defaults below are **clarify locks** (logged, not silent invention). Incorporated into both plan copies (byte-identical).

**Plan SHA-256 (both copies):** `db8cec806a33cfea991316c3ba9034281a229efe7424194625a51ce9527c06ed`  
**Invalidated prior SHA-256:** `6bd715cd81abbfa367b81674624d55eabea02f7a2e44bf58fe499c9791dd6a52`

**Do not** reopen ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job; FAST not on GST; FAST exempt from six-role quality order; wrap at `/sb`; BOA parallel for non-trivial Advisor; no synthesize on trivial; no `AF-meta-six-role`; current-month is a context-load cap; AM is capture buffer; K/L dirs are git SoT; `synergy_max` AM auto-commit is not the K/L path.

### AM-first fail-closed `kl_write` provenance (locked)

K/L that currently gets captured **must still land in agentmemory mechanically**, not by hoping the agent also saved AM.

- **Same leaf, ordered effects.** The deny-all Advisor `knowledge_postwrite` (Jobs) and FAST thin-capture leaf MUST `memory_save` the insight **first** (the same durable text that would have been the K/L entry, not a weaker summary), then classify, then promote into `docs/knowledge/YYYY-MM.md` or `docs/learnings/YYYY-MM.md` (or `kl_post_write_no_insights` / FAST no-insight if ephemeral).
- **Provenance.** Every `kl_write` receipt and every K/L monthly entry cites `am_id` (or content hash of the AM record). `kl_write` without AM provenance is invalid.
- **Fail-closed when agentmemory is opted in** for that runtime: AM save failure or missing `am_captured` → `blocked_knowledge_postwrite` (do **not** write K/L anyway). That is how “current K/L still gets into AM” is ensured.
- **When AM is not opted in:** K/L write may proceed (tool not present); receipt is `kl_write_am_skipped` so the skip is visible. INDEX fallback already covers retrieve. Do not silently pretend AM captured.
- **No direct K/L authorship.** Orchestrator, ordinary Executor, and parent must not Write `docs/knowledge/` or `docs/learnings/` except via this leaf. Direct K/L write without the leaf is `blocked_knowledge_postwrite`.

### Team Graphify fan-out (locked)

- **Fan-in:** each person's local AM captures; classified durable insights promote into committed K/L files (git). Unpromoted episodic AM stays local and is **not** the team store.
- **Fan-out retrieve:** everyone `git pull`s K/L; everyone's Graphify indexes `docs/knowledge/` + `docs/learnings/` (`graphify update` after pull / session start / Job pre-read). That is how teammates see each other's K/L.
- **Do not ingest K/L back into each clone's agentmemory.** Full-text reload recreates the dual store, mixes personal episodic AM with team curated K/L, and blows proactive-injection tokens. Graphify **is** the shared retrieve layer (existing synergy: save via AM, retrieve via Graphify).
- Optional thin pointer only (not full text): session-start may note K/L is Graphify-indexed (e.g. last commit SHA). Not a copy of monthly files into AM.
- Teammates without AM still get team K/L via Graphify / INDEX. Teammates with AM still retrieve team K/L via Graphify, not via AM search.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start GPT Max / Opus Extra High / Max.

---

## Clarify Decision Addendum (round-11 — 2026-08-16) — Opus High ladder-3 ACCEPT

Interactive lock incorporating all six Opus 5 High ladder-3 findings (0 blockers; parent verified none wrong). Plan SHA-256 (both copies, byte-identical): `bebcfa9165afd7bbfef934ca8093bbd567f9c569d49c1f1d980298fb182d1b0d`. Prior frozen SHA invalidated: `db8cec806a33cfea991316c3ba9034281a229efe7424194625a51ce9527c06ed`. Extra High / Max **not** started.

**KEEP REJECT (untouched):** ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST **not a Job** / not GST / exempt from six-role quality order; wrap at `/sb`; BOA parallel; no `AF-meta-six-role`; no synthesize on trivial; **do not mint `AF-qa`**; reuse IDs `sb:fast` / `WF-SILVER-FAST` / `AF-FAST-PATH`; AM-first `kl_write`; current-month load cap; team Graphify fan-out (no K/L→AM reload); AM capture buffer / K/L git SoT.

### H1 — FAST catalog vehicle vs exemption (accepted)

Keep the locked IDs. The *vehicle* is the gap. Classified-trivial **reuses** those public IDs but **this ship amends** the APO records so generation cannot emit the old V-loop/composition obligations for classified-trivial. For classified-trivial, live composition is **`AF-FAST-PATH` only** (Authorizer-admitted Executor or deny-all Q&A leaf + FAST thin capture). Catalog extra AFs (`AF-PLAN`, `AF-VALIDATE`, `AF-VERIFY`, `AF-QUALITY-GATE`, `AF-EXECUTE`) **must not run**. WS1: APO is generation source **except** this overlay/amendment; generated denies/composition must not re-impose six-role gates on `sb:fast`. `PP-SB-STARTUP-FAST` must **not** map 3-day greenfield / durable product edits onto classified-trivial `sb:fast`. Fail-closed reclassify if `WF-SILVER-FAST` is selected for durable edits. Do **not** mint `AF-qa`. Do **not** reopen FAST-not-a-Job / no GST / quality-order exemption / thin capture.

### H2 — GST push failure must not hard-stop Jobs (accepted)

**Lock degrade, not halt.** GST is a team dashboard, not a delivery gate. Protected `main` / no push rights / exhaustion: stamp `gst_stale` (or equivalent) on the local WBS; **Job continues**. Row 34 is dashboard-only / non-classifying (like retired row 14). Local ASCII viz still updates. Resume of dashboard freshness is operator path-exception / push rights — **not** a gate on `scope_complete`. Round-6 merge-by `(instance_id, job_id)` is superseded (semantic CAS remains `(instance_id, gst_row_id)`).

### M1–M4 (accepted)

- **M1:** Row 6 trigger includes ABU-01 unresolved Board conflict; resume includes re-unify (`advisor_board_unify` / `advisor_board_unifier`), not retired row 14.
- **M2:** Parent-proxy consume is the Task-capable **Orchestrator** ancestor (root in MVP). Nested Task-capable Executor must **not** consume parent-proxy / invoke the projector.
- **M3:** Classified-trivial ledger terminal = FAST leaf complete **plus** thin-capture receipt (`kl_write` / `kl_post_write_no_insights` / FAST no-insight / `kl_write_am_skipped` as applicable). Not Process-final Val. Then `scope_complete`.
- **M4:** Document-integrity clause covers TOC headings at the body level (`##` or `###`). TOC `###` entries (Board of Advisors, Global Status) must not be required as `##`. `### Document integrity` is a checklist subsection, not a TOC entry.

### Next SB lifecycle step

Stay on `main`. Do not commit. Extra High completed; do not start Opus Max.

---

## Clarify Decision Addendum (round-12 — 2026-08-16) — Opus Extra High ladder-3 ACCEPT

Interactive lock incorporating all seven Opus 5 Extra High ladder-3 findings (1 blocker, 2 Highs, 4 Mediums; parent verified none wrong). Plan SHA-256 (both copies, byte-identical): `0289df0c7d8e8982ecef041d5e9da7092b5a6a67b94863297c74c276785ec2af`. Prior frozen SHA invalidated: `bebcfa9165afd7bbfef934ca8093bbd567f9c569d49c1f1d980298fb182d1b0d`. Max **not** started. No Fast.

**KEEP REJECT (untouched):** ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST **not a Job** / not GST / exempt from six-role quality order; wrap at `/sb`; BOA parallel; no `AF-meta-six-role`; no synthesize on trivial; **do not mint `AF-qa`**; reuse IDs `sb:fast` / `WF-SILVER-FAST` / `AF-FAST-PATH`; AM-first `kl_write`; current-month load cap; team Graphify fan-out (no K/L→AM reload); AM capture buffer / K/L git SoT; H2 GST **degrade** (`gst_stale`, Job continues); High ACCEPT H1 overlay intent (AF-FAST-PATH only for classified-trivial).

### B1 — Catalog amendment mechanism (accepted)

Stop “surgical comment/flag” in `docs/apo-catalog.json`. `$defs.atomic_flow` / `workflow` / `process_pack` have `additionalProperties: false`; `$defs.v_loop` is required so `VL-AF-FAST-PATH` cannot be deleted. **Lock:** schema **unchanged**. Classified-trivial exemption is enforced in `scripts/check-apo-invariants.py` **and** the generators (WS1): they must **not** emit old V-loop/composition obligations for classified-trivial even though the JSON still carries required `v_loop` / `VL-AF-FAST-PATH`. Name `docs/apo-catalog.schema.json` in the owning work stream (WS1) as **unchanged** (exemption lives in checker + generators, not extra JSON properties). Pruning generated composition for classified-trivial to `AF-FAST-PATH` only remains.

### H-A — Add `AF-EXECUTE` to every must-not-run list (accepted)

Shipped `WF-SILVER-FAST.composition_tree` includes `AF-EXECUTE`. Add `AF-EXECUTE` to **every** enumeration that lists `AF-PLAN`, `AF-VALIDATE`, `AF-VERIFY`, `AF-QUALITY-GATE`. Live classified-trivial composition remains **`AF-FAST-PATH` only**. `AF-EXECUTE` must not run (it would re-impose a populated `v_loop`).

### H-B — Overlay the rest of the AF-FAST-PATH record (accepted)

Amendment is not only `composition_tree` + `VL-AF-FAST-PATH`. For classified-trivial, generators/checker also must **not** emit/enforce: `prerequisites` (upstream V-gates); `exit_condition` (step V-loops + flow V-gate + evidence); `flow_steps` V-loops (`FS-SILVER_*` / `VL-FS-SILVER_FAST`) — Leaf Step has no Step-level V/Val **wins** on this path; `execution.join_condition` (join after step V-loops); `artifacts` / `evidence_refs` as required V-evidence; `capability_class: bounded_fast_path` as a quality-order trigger.

### M-A — `PP-SB-STARTUP-FAST` (accepted)

Drop the unreachable “stays bounded-fast-path product work **via WF-SILVER-FAST**” alternative. **Only** retarget: rewrite `override_rules[0]` so 3-day greenfield / durable edits prefer catalog **`WF-SILVER-FEATURE`**. `WF-SILVER-FAST` selected for durable edits still fail-closed reclassifies.

### M-B — FAST operator surfaces (accepted)

The FAST Executor/Q&A leaf viz/prose **includes** the thin-capture deny-all node (M3 terminal). Align mermaid + enumerations. Thin capture is required WBS content (omit → `blocked_progress_viz`).

### M-C — GST degrade fixture owner (accepted)

Add the protected-`main` / no-push-rights `gst_stale` + Job-continues fixture to **`VAL/TST-RFL-621`** (GST-01 family). Not a new sibling ID.

### M-D — Board of one (accepted)

Unifier leaf **still launches** for a Board of one (identity-preserving / no-op unify still Authorizer-admitted after the single member's output) so output-receipt identity (`input-set hash` + unifier `launch_id` / occurrence) can form. Not last-write-wins. Orchestrator still does not implement unify.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Opus Max. No Fast.

---

## Clarify Decision Addendum (round-13 — 2026-08-16) — Opus Extra High ladder-3 ACCEPT (real xhigh)

Interactive lock incorporating all ten Opus 5 Extra High ladder-3 findings from real Extra High [`sb-opus-5-xhigh`](ae7acbea-65c5-4331-a0a9-7a19964c42e0) (0 blockers, 5 Highs, 5 Mediums; parent verified none wrong). Plan SHA-256 (both copies, byte-identical): `4f772f9f618ae42aa2ecd573c2c5a813af8d22c29f719f97a5f12106efee2d1d`. Prior frozen SHA invalidated: `0289df0c7d8e8982ecef041d5e9da7092b5a6a67b94863297c74c276785ec2af`. Max **not** started. No Fast.

**KEEP REJECT (untouched):** ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST **not a Job** / not GST / exempt from six-role quality order; wrap at `/sb`; BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first; current-month cap; team Graphify fan-out; GST **degrade** (`gst_stale`, Job continues, row 34 dashboard-only); B1 schema unchanged / exemption in checker+generators; AF-FAST-PATH only for classified-trivial; `AF-EXECUTE` must not run on FAST; PP-SB-STARTUP-FAST → `WF-SILVER-FEATURE`; Board-of-one unifier still launches.

### H-1 — Row 35 must not hard-stop Jobs (accepted)

Same GST degrade as row 34. Missing git identity: stamp `gst_stale` (or equivalent); **Job continues**. Row 35 is dashboard-only / non-classifying. Add identity fixture to `VAL/TST-RFL-621`.

### H-2 — `.sb/` ledger-omit must propagate (accepted)

Add `.sb/` to **every** sparse-checkout / split-brain / merge-oracle / WBS-01 / launch-template ledger-omit enumeration that listed only `.planning/`, `graphify-out/`, `.agentmemory/`, project `.silver-bullet/`. Extra-tree discriminator (row 33 vs 4/8) treats `.sb/` as ledger-omit.

### H-3 — FAST does not run Job step-1 K/L pre-read (accepted)

Classified-trivial is **not a Job**. Job step 1 / `blocked_knowledge_preread` / `pre_read_pending` are **Job-scoped**. FAST WBS: classify + wrap + FAST leaf + thin-capture only (no pre-read node). Five-tool still applies if a FAST leaf would edit (it must not); Graphify miss on FAST is not row 8. Reclassified durable work **does** run step 1 as a Job.

### H-4 — FAST failure terminal (accepted)

Do **not** use ESC-02 / Advisor/Val/Ver. FAST leaf hang/die/wrong-answer → Authorizer-admitted **one** re-dispatch of the same FAST leaf; second failure → user-visible `blocked_fast_leaf` (row 36; FAST-scoped — not a Job, not GST). Thin-capture AM-save failure stays `blocked_knowledge_postwrite` but FAST-scoped (does not invent a Job). Stops FAST `scope_complete` / user return, not GST.

### H-5 — `AF-agent-delegate` / `sb:agent-wrap` in APO (accepted)

WS1 **must** add these records to `docs/apo-catalog.json` (schema-legal: required `v_loop` `VL-AF-agent-delegate`). Class `nested_executor` as already specified (not a second public Process; wrapping WF `sb:agent-wrap` / catalog id `WF-SB-AGENT-WRAP` owning AF `AF-agent-delegate`; public routes `sb:agent-wrap` / `sb:agent-delegate`). Enumerate route ids + `VL-*` in WS1. Do not bulk-rewrite the whole catalog.

### M-1 — Drop leftover “amends those APO records” (accepted)

Classified-trivial exemption is generators + `check-apo-invariants.py`; do **not** instruct JSON edits to `AF-FAST-PATH`/`WF-SILVER-FAST` records. The FAST collision change lives in generator `PROCESS_PACK_DEFS` (`PP-SB-STARTUP-FAST.override_rules[0]` prefer `WF-SILVER-FEATURE` for 3-day greenfield), then regen. Do **not** hand-edit `docs/apo-catalog.json` for that field.

### M-2 — GST UTC rollover (accepted)

In-flight **Active** rows carry forward into the next UTC day file; **Completed/Blocked** stay on the day they terminated; tombstone for `gst_row_id` is consulted across the current **and previous** day file so a stale retry after midnight cannot rewind to Active. Pin in `VAL/TST-RFL-621`.

### M-3 — GST helper write strategy order (accepted)

Ordered: (1) if operator primary **is** git main-worktree, commit/push there; (2) else `fetch` + commit on `origin/main` **without** creating a second worktree that materializes ledger-omit dirs. **Forbid** a “dedicated main worktree” that copies `.planning/` / `.sb/` / etc. Dashboard must not be able to reach `blocked_corrupt_state` via split-brain.

### M-4 — `paths-ignore` / `[skip ci]` (accepted)

Scope to **push** heartbeats on `main` (status-only commits). Do **not** suppress `pull_request` checks via workflow-level `paths-ignore` that would leave a status-only PR with no checks.

### M-5 — Do not publish raw email (accepted)

CAS `git_user_id` still prefers email if set (identity stability). **Published** dashboard display MUST NOT write raw `user.email` into `.sb/status/**` on `origin/main`. Use `user.name` if set, else a short hash of email for `{…}'s SB` display. Helper refuse-write if payload would contain a raw email.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Opus Max. No Fast.

---

## Clarify Decision Addendum (round-14 — 2026-08-16) — Extra High ACCEPT retained + two user locks

Interactive lock covering (1) all ten Opus 5 Extra High ladder-3 findings (H-1–H-5 / M-1–M-5, already landed) and (2) two user-directed plan locks. Plan SHA-256 (both copies, byte-identical): `18ac07bbc763241d023681a14aab1261d6b1d7be13f9e364bff5caba8c4614b4`. Prior frozen SHA invalidated: `4f772f9f618ae42aa2ecd573c2c5a813af8d22c29f719f97a5f12106efee2d1d`. Max **not** started. No Fast.

**KEEP REJECT (unchanged except the two user overrides below):** ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / not GST / quality-order exemption; BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first; current-month cap; team Graphify fan-out; GST degrade; B1 schema unchanged; AF-FAST-PATH only for classified-trivial; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier still launches.

### User override A — item 7 narrowed (accepted)

Initial **user-intent Process wrap** still mints at `/sb` Process resolve/wrap. **Nested / opportunistic Workflow mint or invoke does NOT return to Orchestrator or `/sb`.** Any Executor may `wf_mint` / `wf_invoke` a newly created or pre-existing Workflow at any point (Authorizer-admitted; not a second Process). Physical rails kept: inner NW joins stop at Verification; AF never under Process (`blocked_af_under_process`); FAST does not synthesize NWs; WBS must show inserted NWs; GST nested WF is Job-scoped; host `remaining_depth` / parent-proxy still apply. Executor mint without Authorizer admit → `blocked_wf_mint_unauthorized`. Cold `/sb:agent-*` wrap at Orchestrator remains for that family only.

### User override B — host settings lock revoked (accepted)

Previous “do not write Cursor/Codex/Claude max-nesting settings; Init/Doctor does not probe those knobs” is **revoked**. On first `/sb:init` or any next SessionStart/init if unset, SB configures the host for documented max nested-subagent support (`VAL/TST-RFL-623` / HNEST-01; idempotent; no unrelated knobs).

| Host | Official max | Config SB writes | Source (fetched 2026-08-16) | Residual |
|---|---|---|---|---|
| Cursor | **2 Task hops** below main (3 agent layers) | None (hard product limit) | https://cursor.com/docs/subagents (Cursor 2.5) | No writable knob; adapt via `remaining_depth` |
| Codex | **No documented numeric max** | `agents.enabled = true` if unset/false; do **not** write `max_depth` | https://developers.openai.com/codex/multi-agent | Adapt via `remaining_depth` |
| Claude | **3 subagent layers** below main (documented default) | `env.CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` = `3` if unset/below | https://code.claude.com/docs/en/env-vars | No published ceiling above 3; SB writes 3 |

### Extra High ACCEPT (retained)

H-1–H-5 and M-1–M-5 remain as in the round-13 addendum.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Opus Max. No Fast.

---

## Clarify Decision Addendum (round-15 — 2026-08-16) — role-gated WF mint (historical; SHA not frozen)

Interactive lock: Advisors compose Work Plans (may create new WFs). Executors `wf_mint` / `wf_invoke` only in-plan (cited `plan_node_id` / WBS id). Orchestrator/`/sb` does not compose or invent WFs (`blocked_orchestrator_wf_mint`). FAST classify + catalog dispatch is not WF mint. Item 7 “wrapping WF mints at `/sb` Process resolve/wrap” superseded: Advisor composes the root WF. Copies were not `cp`’d in that pass; SHA not frozen. Superseded in part by round-16 (agent-* invent ban retracted; HINST-01).

---

## Clarify Decision Addendum (round-16 — 2026-08-16) — agent-* Executor mint + HINST-01

Interactive lock (parent-accepted). Plan SHA-256 (both copies, byte-identical): `d9d452eee1d4ed2307cdc9c195d42afaf03889ab18923e52cc48ca8e72bdd172`. Prior frozen SHA invalidated: `18ac07bbc763241d023681a14aab1261d6b1d7be13f9e364bff5caba8c4614b4`. Max **not** started. No Fast. Stayed on `main`.

**KEEP (unchanged):** Extra High ACCEPT H-1–H-5 / M-1–M-5; nested-WF maximum dynamicity; Advisor/Executor role-gated mint; Orchestrator-does-not-compose; HNEST-01 three-host max-nesting Init; FAST classify-not-mint.

**KEEP REJECT (unchanged):** ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / not GST / quality-order exemption; BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first; current-month cap; team Graphify fan-out; GST degrade; B1 schema unchanged; AF-FAST-PATH only for classified-trivial; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier still launches.

### User override A — retract catalog-only invent ban (accepted)

**Retract** “`/sb:agent-*` may dispatch catalog AF/WF ids but may not invent a new WF.”

Every `/sb:agent-*` (Cursor, Codex, Claude, OpenCode, Pi) is an **Executor for Workflow mint**. The leaf **may invent a new Workflow** the same way any other Executor-shaped subagent may: **only to support the Advisor-formulated, Validator-validated Work Plan** (cited `plan_node_id` / WBS id). Out-of-plan → `blocked_executor_wf_out_of_plan` → Advisor. Orchestrator/`/sb` still does **not** compose Work Plans or mint WFs; it **dispatches** the agent-* Executor. Catalog wrap `sb:agent-wrap` / `AF-agent-delegate` is the **dispatch envelope**, not a ban on inventing nested WFs inside that envelope. agent-* nested Executors do **not** become Advisors; a new Work Plan is reclassify to Advisor — even on Cursor-with-SB.

### User override B — three-host SB install vs OpenCode/Pi instruction-only (accepted)

`VAL/TST-RFL-624` / HINST-01. **Same Init/Doctor pass as HNEST-01.**

| Lock | Spec |
|---|---|
| When | First `/sb:init` on any supported parent, **and** later SessionStart/Doctor if a present host is missing SB or down-level vs this repo `package.json` version. Idempotent. No unrelated IDE prefs. |
| Detect | Present if CLI **or** config dir: `cursor`/`~/.cursor`, `codex`/`~/.codex`, `claude`/`~/.claude`. Absent both → `host_absent` warn, do not fail parent Job. |
| Install scripts | `scripts/install-cursor.sh` (+ `install-cursor-sb-agents.sh --fix`); `scripts/install-codex.sh`; `scripts/install-claude.sh`. No `install-opencode.sh` / `install-pi.sh`. |
| Cursor installed means | `~/.cursor/plugins/cache/alo-labs/silver-bullet/<version>/` + `current`; hooks via `cursor-hook-bridge.sh`; `installed_plugins.json` `silver-bullet@alo-labs`; local plugin link; `commands/` + `skill-source/`; `~/.cursor/agents/` |
| Codex installed means | `~/.codex/plugins/cache/alo-labs-codex/silver-bullet/current`; plugin `silver-bullet@alo-labs-codex`; Codex `AGENTS.md` / skill-source |
| Claude installed means | `~/.claude/plugins/cache/alo-labs/silver-bullet/<version>/` + `current`; `installed_plugins.json` `silver-bullet@alo-labs`; hooks in `~/.claude/settings.json`; `agents/claude/` |
| Cross-host | Any supported parent ensures the other two when present. Sibling fail = warn + continue. Parent unrepairable → `blocked_sb_host_install`. |
| Spawn Cursor/Codex/Claude | Verify-then-install-then-spawn. Native SB. Parent-proxy **only** at `remaining_depth` 0, not as a substitute for missing SB. |
| OpenCode/Pi | Instruction bundle. Preference: parent if Cursor/Codex/Claude, else Cursor → Codex → Claude. Named dirs + `$primary_checkout/silver-bullet.md` / `skills/` / `contracts/`. Honest: no hooks/MCP. Parent-proxy writes work-spec/WBS/GST/AM/`graphify update`/WF mint records. No reference dir → `blocked_sb_host_missing`. |
| Why no install | OpenCode plugins are JS/TS (`~/.config/opencode/`); SB ships bash hook plugins only. Pi is `skills/silver-agent-pi` CLI wrap ([earendil-works/pi](https://github.com/earendil-works/pi)); no SB plugin surface. After-MVP OpenCode-as-parent-host ≠ nested `/sb:agent-opencode`. |

Rows **41–42:** `blocked_sb_host_missing`, `blocked_sb_host_install`.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Opus Max. No Fast.

---

## Clarify Decision Addendum (round-19 — 2026-08-16) — Extra High re-verify ACCEPT (H-1–H-2 / M-1–M-2)

Interactive lock (parent-accepted; Extra High [`sb-opus-5-xhigh`](bc5b2af0-21d0-4a36-bd30-1dbd4b71eafc) on SHA `65fde3d6f4521e7a8aed89ceb07efc6bcfdbf1b5518e3d1ce0e921c43ac7fd8c`; 0 blockers, 2 Highs, 2 Mediums; parent verified none wrong). Prior frozen SHA invalidated after this ACCEPT. Max **not** started. No Fast. Stayed on `main`.

**KEEP (unchanged):** `/silver:new-workflow` is a **workflow authoring generator**; emitted-WF spec compliance is the bar; do **not** recast the skill as “only invokes Advisor.” Output-compliance lock stays. Extra High prior H-1–H-5 / M-1–M-4 remain except as this round amends.

**KEEP REJECT (unchanged):** ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / not GST / quality-order exemption; BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first; current-month cap; team Graphify fan-out; GST degrade; B1 schema unchanged; AF-FAST-PATH only for classified-trivial; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier still launches; generator bar intact.

### H-1 — Authoring session role contract (accepted)

The `/silver:new-workflow` **authoring session is a Job**. Orchestrator writes the work-spec and invokes Advisor. Advisor formulates the Work Plan that **authors** the new WF definition. Validator validates. Executor executes the authoring plan. Orchestrator **queue-builder composition is retired** for this session (no Orchestrator-composed Work Plan → would be row 39). The session **must** use the Job quality order (Advisor compose + composition-Val + plan-time Val). Authoring Job **does** mint `original_intent_hash`, take a GST row, emit WBS. **`WF-SILVER-NEW-WORKFLOW`’s own catalog record is in scope:** it must become NW-capable / spec-compliant (not remain AF-only pre-spec) — same generator bar as the artifacts it emits. WFM-01 / `VAL/TST-RFL-625` golden fixture must also prove the **session** path (Advisor-composed authoring Job, not Orchestrator queue-builder), not only the emitted WF. WS2: `.silver-bullet/orchestrator-workers/NEW-WORKFLOW.md` must stop being a queue-builder mint path; it becomes work-spec + Advisor invoke for this Job.

### H-2 — Rows 41/42 vs 11/12 (accepted)

Carve rows 11/12 so HINST-01 install/reference failures classify **41/42**, not `blocked_executor_unavailable` / `blocked_verification_unavailable`. Exclude instruction-only runtimes (OpenCode/Pi) from the closest-replacement set when the recorded tuple was an **installable** host (Cursor/Codex/Claude). Do not silently downgrade to the instruction-bundle path.

### M-1 — Codex `unbounded` consumer semantics (accepted)

Codex `remaining_depth` sentinel `unbounded` is **not** compared as integer 0. Depth-exhaustion parent-proxy on Codex = **host refuses further nest**, then parent-proxy. Refusal-detection is the host spawn error / capability refuse; stamp ledger field `host_nest_refused`. `VAL/TST-RFL-623` fixture for refuse-then-proxy. Consumers must accept the non-integer sentinel. Integer `> 0` / `== 0` rules apply only when remaining_depth is numeric (Cursor/Claude).

### M-2 — Replacement-ladder probe (accepted)

“Probe the new runtime first” means: (1) installable host → HINST-01 install-ensure + adapter capability (row 20) + five-tool init probe if opted in; (2) instruction-only candidate must **not** replace an installable-host tuple (H-2); (3) when five-tool is not opted in, probe = HINST-01 + adapter capability only.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Opus Max. No Fast.

---

## Clarify Decision Addendum (round-21 — 2026-08-16) — Max CLEAN ACCEPT (M-1–M-2 / L-1)

Interactive lock (parent-accepted; Max [`sb-opus-5-max`](511a9a49-a7a9-434d-8760-9a7cde177082) on SHA `5eba7e86c040c4965b43446a4454bde8064904a9e52c0f69d2a80f542f795216`; 0 blockers, 0 Highs, 2 Mediums, 1 Low; parent verified none wrong). Prior frozen SHA invalidated after this ACCEPT. Max **not** re-started (already CLEAN). Ladder 4 **not** started. No Fast. Stayed on `main`.

**KEEP (unchanged):** `/sb:new-workflow` is a **workflow authoring generator**; emitted-WF spec compliance is the bar; authoring **session** is a Job with Advisor compose (queue-builder retired). Extra High CLEAN M-A–M-C remain. This round **implements** the public-IDs-`sb`-only lock for this route; it does **not** reopen KEEP REJECT.

**KEEP REJECT (unchanged):** ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose (no `/sb` WF mint); BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first; current-month cap; GST 34+35 degrade; B1 schema unchanged; AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter; HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; `/sb:new-workflow` authoring **generator** (emitted WFs spec-compliant) **and** authoring **session** is a Job with Advisor compose (queue-builder retired; this route not skip-Advisor catalog dispatch). Public IDs are `sb` / `sb:` / `/sb` only.

### M-1 — Public route is `/sb:new-workflow` (accepted)

Public identifier: `/sb:new-workflow` (skill/command/help/test slug `sb-new-workflow`). `/silver:new-workflow` and `silver-new-workflow` are historical only, same gloss as `skills/sb/` vs `skills/silver/`. WS2 file list: [`skills/sb-new-workflow/SKILL.md`](skills/sb-new-workflow/SKILL.md) (historical `skills/silver-new-workflow/SKILL.md`); command stub `plugins/silver-bullet/commands/sb-new-workflow.md`; help `site/help/workflows/sb-new-workflow.html`; tests `tests/scripts/test-sb-new-workflow.sh`. Implementation still migrates the existing skill via the ship-wide `silver`→`sb` rename — do **not** invent a dual `/silver` window. Catalog id `WF-SILVER-NEW-WORKFLOW` stays. Authoring generator + authoring Job + Advisor compose stay.

### M-2 — Rows 37 vs 40 first-match (accepted)

Executor out-of-plan / uncited `plan_node_id` **stays row 40** (`blocked_executor_wf_out_of_plan`). Row 37 is non-Advisor mint **without Authorizer admit / role permission** that is **not** the out-of-plan Executor case (and not Orchestrator — **stays row 39**). WFM-01 three fixtures remain distinct.

### L-1 — Duplicate parenthetical at capability-contract todo (accepted)

Delete the adjacent verbatim duplicate of the Codex `unbounded` parenthetical in the frontmatter todo. Normative body text was not duplicated.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Max (already CLEAN). Do not start ladder 4 (parent will). No Fast.

---

## Clarify Decision Addendum (round-23 — 2026-08-16) — GPT Extra High H-1 + Opus Extra High B-1/H-1/H-2/M-1/M-2 ACCEPT

Interactive lock (parent-accepted; GPT Extra High [`sb-gpt-5-6-sol-xhigh`](01f3b506-66f1-471a-a4b4-3560dca0b1d3) NOT CLEAN 0/1/0 + Opus Extra High [`sb-opus-5-xhigh`](053395ca-1db5-45b2-9aab-a9769c550a80) NOT CLEAN 1/2/2 on SHA `67eff63eb035db3586cb0b0faeb55088b05dd86ea71651773b488b47c23efd75`; parent: none wrong; incorporate all). New SHA `c82e1b3bccc5625e9cb4fe2cc7518a577fdd69af225c062c08b6cd6d5ac7a472`. Prior frozen SHA invalidated after this ACCEPT. Max **not** started. No Fast. Stayed on `main`. No commit.

**KEEP REJECT (unchanged):** ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose (no `/sb` WF mint); BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first; current-month cap; GST 34+35 degrade; B1 schema unchanged (`docs/apo-catalog.schema.json`); AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter; HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; no second agent-delegate AF/WF; `prompt_hash` binds inner prompt bytes only. Public IDs are `sb` / `sb:` / `/sb` only.

### GPT Extra High H-1 — `worktree_cwd` declare-at-admit / stamp-at-consume (accepted)

Same pattern as `remaining_depth`: `worktree_cwd` is **envelope metadata**, not inner-prompt bytes (stays out of `prompt_hash`). Authorizer-admitted `launch_intent` **declares** `worktree_cwd` (absolute path / tree id matching hashed work-spec `scope_bounds` / WBS tree assignment). Ancestor **stamps** envelope `worktree_cwd` at consume to the **declared** value. Mismatch vs admit **or** vs hashed `scope_bounds`/WBS tree → row 4 `blocked_launch_prompt_spec` (extended beyond missing-only). Stale/incorrect cwd must **not** retain valid hashes + admission.

### B-1 — Rename must own alias map + hardcoded checks (accepted)

Ship-wide `silver`→`sb` **directory** rename is not public-route-only. WS2 + WS1 as needed **own**: rekey `docs/apo-catalog.json` `migration_map.skill_to_entity` for every skill directory this ship renames; update `tests/scripts/test-canonical-alias-mapping.sh` / `canonical_alias_mapping`; `agent-delegation-contract` **keep the check**, **update expected skill names** to post-rename ids; `runtime_queue_tokens` and `hooks/workflow-chain-guard.sh` / `triple_alignment` **must retarget** for slugs this ship renames. Not a bulk rewrite of all 72 `silver-*` keys unless those dirs are renamed this ship. WS1 “keep agent-delegation-contract” + “do not bulk-rewrite catalog” must not forbid those mapping/check string updates.

### H-1 — OpenCode/Pi must attach to shipped AF (accepted)

Named field changes **only** on existing records (not new AF/WF): `WF-AGENT-DELEGATE-ENTRY.triggers` include `agent-opencode` and `agent-pi`; `AF-AGENT-DELEGATE.owning_skills` include the OpenCode and Pi agent skills; attach `FS-SILVER_AGENT_OPENCODE` and `FS-SILVER_AGENT_PI` to `AF-AGENT-DELEGATE.flow_steps`. HINST-01 still: no SB plugin on OpenCode/Pi.

### H-2 — `nested_executor` is lock-only (accepted)

Strike “still catalog `nested_executor`”. The class lives in `contracts/apo-hierarchy.lock.json` / `public-workflow-routes.lock.json` — **not** a catalog JSON field (schema unchanged). Leaves `/sb:agent-*` are lock class `nested_executor`; `WF-AGENT-DELEGATE-ENTRY` / `AF-AGENT-DELEGATE` stay Workflow/AF types.

### M-1 — Row 19 must compare computed vs declared (accepted)

Stamp ≠ tautology. **Actual** post-spawn `remaining_depth` is **computed** from host nesting (hops-below-main after spawn, or Codex refuse → `host_nest_refused`). Consume (and the nested-Task path that has no consume) **compares computed vs declared**; mismatch → row 19. Nested-Task of pre-persisted descendants must stamp/compare at launch.

### M-2 — `WF-AGENT-DELEGATE-ENTRY.owning_skill` (accepted)

In-scope this ship. Bind to a real skill directory (`silver-agent-worker` or the renamed `sb-agent-*` worker skill) **and** a `skill_to_entity` key. No dangling `silver-agent`.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Max. No Fast.

---

## Clarify Decision Addendum (round-24 — 2026-08-16) — GPT Extra High H-1/H-2/M-1 + Opus Extra High H-3/H-4/M-3 ACCEPT

Interactive lock (parent-accepted; GPT Extra High [`9bf8e875-25ca-4e93-a939-05ccffb2b065`](9bf8e875-25ca-4e93-a939-05ccffb2b065) NOT CLEAN 0/2/1 + Opus Extra High [`180607f2-a384-4749-ae93-b6a798c079a8`](180607f2-a384-4749-ae93-b6a798c079a8) NOT CLEAN 0/2/1 on SHA `c82e1b3bccc5625e9cb4fe2cc7518a577fdd69af225c062c08b6cd6d5ac7a472`; parent: ACCEPT all six). New SHA `1096479cc1deba1b902ca501e8d7b7b1c0c1ba5f23510f8776c098f6913002d5`. Prior frozen SHA invalidated after this ACCEPT. Max **not** started. No Fast. Stayed on `main`. No commit.

**KEEP REJECT (unchanged):** ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose (no `/sb` WF mint); BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first; current-month cap; GST 34+35 degrade; B1 schema unchanged (`docs/apo-catalog.schema.json`); AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter; HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; no second agent-delegate AF/WF; `nested_executor` lock-only; `prompt_hash` binds inner prompt bytes only; public IDs are `sb` / `sb:` / `/sb` only (no dual `/silver` window). Round-23 `worktree_cwd` on **consume** stays (this High extends the **nested-Task** path). HNEST/HINST writes on Init/Doctor stay. `tests/run-all-tests.sh` remains the release gate — H-3/H-4 make that gate **named** in WS1.

### GPT Extra High H-1 — Nested-Task must bind `worktree_cwd` (accepted)

The nested-Task path (pre-persisted descendants, no parent-proxy consume) already compares/stamps `remaining_depth`. It **must also** declare/stamp/compare `worktree_cwd` against hashed `scope_bounds`/WBS tree. Mismatch → row 4. Stale/tampered cwd must not bypass row 4 because consume never ran.

### GPT Extra High H-2 — Doctor vs inspect-only (accepted)

Init / Doctor / SessionStart **MAY write** HNEST-01 max-nesting knobs and HINST-01 install-ensure (idempotent; skip if already at max / already installed). “Inspect-only” / do-not-write applies to **unrelated** IDE prefs (model, telemetry, permissions, maxTurns, experimental teams, etc.) — not those two mandated writes. Row 1 / Doctor language that says Doctor never writes is scoped. Do not revoke the user lock that SB configures max nested-subagent support.

### GPT Extra High M-1 — GST tombstones beyond N-1 (accepted)

Completed/Blocked tombstones must not expire after two UTC rollovers. Projector consults **historical day files that still exist** (or a durable tombstone index) for that `gst_row_id`, not only current + previous day. Stale Active retry must not resurrect a terminal row. Pin in `VAL/TST-RFL-621`.

### Opus Extra High H-3 — Generated APO artifacts are WS1-owned (accepted)

After any catalog / `migration_map` / `owning_skills` / `runtime_queue_tokens` edit this ship makes, WS1 **must** regenerate and keep green: `scripts/generate-apo-artifacts.py` (and `--check` / `derived-views`), `docs/composable-flows-contracts.md`, `docs/workflow-composition-matrix.md`, `docs/generated/atomic-flow-index.json`, `tests/scripts/test-apo-derived-views.sh`, `tests/scripts/test-apo-composition-sot.sh`. Name these in the WS1 row so “changes only named source surfaces, regenerates mirrors only through the named command” **includes** this generator. Unnamed mirrors going stale is a WS1 miss.

### Opus Extra High H-4 — Post-exec `silver:` literals in generator + parity tests (accepted)

WS1 (and WS6 if instruction text) **must** retarget hardcoded `silver:` public routes in `POST_EXEC_SEQUENCING_LINES` (or equivalent) in `scripts/generate-apo-artifacts.py`, `tests/scripts/test-instruction-flow-parity.sh` (P0-4 three-way: `silver-bullet.md` / `templates/silver-bullet.md.base` / `docs/composable-flows-contracts.md`), `tests/scripts/test-silver-router-flow-contracts.sh`, `tests/scripts/test-apo-catalog-sot.sh`. Generated docs must not advertise `/silver` public routes. Catalog ids like `WF-SILVER-*` may remain. Public prefix is `/sb`.

### Opus Extra High M-3 — Delegation step↔flow reciprocity (accepted)

WS1: `AF-AGENT-DELEGATE.flow_steps` ⇄ `reusable_by_flows` on `FS-SILVER_AGENT_OPENCODE` / `FS-SILVER_AGENT_PI` (and other delegation steps) must agree. Add/require an invariant in `check-apo-invariants.py` for this AF. H-1’s `flow_steps` add is not enough if the index is derived only from `reusable_by_flows`.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Max. No Fast.

---

## Clarify Decision Addendum (round-25 — 2026-08-16) — GPT Max H-1 + Opus Extra High re-verify B-1/H-1/M-1 ACCEPT

Interactive lock (parent-accepted; GPT Max [`744ad445-82ae-41ba-8d48-92968867546c`](744ad445-82ae-41ba-8d48-92968867546c) NOT CLEAN 0/1/0 + Opus Extra High re-verify [`85f74b97-1f2a-48a1-b661-33d5c59187c5`](85f74b97-1f2a-48a1-b661-33d5c59187c5) NOT CLEAN 1/1/1 on SHA `1096479cc1deba1b902ca501e8d7b7b1c0c1ba5f23510f8776c098f6913002d5`; parent: ACCEPT GPT Max H-1 and Opus B-1/H-1/M-1). Round-24 landings confirmed. New SHA `701cc66466830161476597af35606168377acb2437088fb86787eec0f6fcc884`. Prior frozen SHA invalidated after this ACCEPT. Max **not** re-launched. No Fast. Stayed on `main`. No commit.

**KEEP REJECT (unchanged):** ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose (no `/sb` WF mint); BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first; current-month cap; GST 34+35 degrade; B1 schema unchanged (`docs/apo-catalog.schema.json`); AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter; HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; no second agent-delegate AF/WF; `nested_executor` lock-only; `prompt_hash` binds inner prompt bytes only; public IDs are `sb` / `sb:` / `/sb` only (no dual `/silver` window). Catalog ids `WF-SILVER-*` may remain. `tests/run-all-tests.sh` remains the release gate and is **named** in WS1. Round-24 H-3/H-4/M-3 (generated artifacts + post-exec `silver:` + flow_steps reciprocity) stay.

### GPT Max H-1 — WS1 owns catalog libs (`/silver:*` → `/sb:`) (accepted)

Add to WS1 named source surfaces (same “regenerate mirrors only through the named command” discipline):

- [`scripts/lib/apo_delegate_catalog.py`](scripts/lib/apo_delegate_catalog.py)
- [`scripts/lib/apo_multi_ai_catalog.py`](scripts/lib/apo_multi_ai_catalog.py)

These still emit `/silver:*` and `$silver:*`. They **must** retarget to public `/sb:` / `$sb:` (or the post-rename token the rest of generation uses) so generated artifacts do not advertise `/silver` public routes. Do not bulk-rewrite the whole catalog. `scripts/validate-host-agnostic-core.sh` lists `apo_delegate_catalog.py`. `tests/scripts/test-instruction-flow-parity.sh`, `tests/scripts/test-silver-router-flow-contracts.sh`, and `tests/scripts/test-apo-catalog-sot.sh` remain the named public-route parity tests (H-4); they do not currently assert those lib purpose strings.

### Opus Extra High B-1 — Catalog is generated; WS1 owns generator + parity (accepted)

`docs/apo-catalog.json` is **emitted** by `scripts/generate-apo-catalog.py` (`build_catalog()` overwrite). Plan must **stop treating it as a hand-edit SOT** for this ship’s field changes.

- All catalog mutations this ship needs (OpenCode/Pi triggers, owning_skills, flow_steps, owning_skill, `skill_to_entity`, FAST `PP-SB-STARTUP-FAST.override_rules[0]`, public trigger `/sb` not `/silver`) live in **generator source**: `SKILL_TO_FLOW`, `ROUTERS`, `ATOMIC_SPECS`, `build_workflows()`, `merge_delegate_catalog()`, `PROCESS_PACK_DEFS`, plus `apo_delegate_catalog.py` / `apo_multi_ai_catalog.py`.
- WS1 **names** `generate-apo-catalog.py` as regenerate command for the catalog itself (not only `generate-apo-artifacts.py`).
- Add a **parity gate**: `--check` and/or a test that `docs/apo-catalog.json` matches generator output (today `tests/scripts/test-apo-catalog-sot.sh` does **not**). Uncommitted generator output is not authority **after** regen; the **source** is the Python builders.
- `SKILL_TO_FLOW` / `ROUTERS = {"silver", ...}` **must** accept post-rename `sb` / `sb-init` / `sb-new-workflow` / `sb-agent-*` or the first regen **aborts**. Name those maps as WS1/WS2 rename surfaces.
- `WF-SILVER-ROUTER.triggers` must not mint public `"/silver"`; public trigger is `/sb` (catalog id may stay `WF-SILVER-ROUTER`).

### Opus Extra High H-1 — One ART-AGENT-DELEGATE (hyphen); V-loop matches AF.artifacts (accepted)

Drop/stop emitting duplicate `ART-AGENT_DELEGATE` (underscore). `VL-AF-AGENT-DELEGATE.verification.artifact_refs` must be `ART-AGENT-DELEGATE` (same as `AF-AGENT-DELEGATE.artifacts` and sibling `VL-FS-DELEGATE-*`). WS1: invariant `v_loop.verification.artifact_refs` ⇄ AF `artifacts` for this AF; `check-apo-invariants.py` must see it (today it only checks registration). Fix in `ATOMIC_SPECS` / `merge_delegate_catalog()` so regen cannot reintroduce the collision. `tests/scripts/test-agent-delegation-catalog-contract.sh` already pins hyphen `ART-AGENT-DELEGATE`.

### Opus Extra High M-1 — OpenCode/Pi step evidence is ART-AGENT-DELEGATE (accepted)

`FS-SILVER_AGENT_OPENCODE` / `FS-SILVER_AGENT_PI` `verification.artifact_refs` must be `["ART-AGENT-DELEGATE"]` like cursor/codex/claude — not `skills/.../SKILL.md`. Catalog path strings into dirs this ship renames are **generator-owned** (`f"skills/{name}/SKILL.md"`) and heal on regen **if B-1 regen is required**. Name those `artifact_refs` / `artifact_or_tool_ref` fields as in-scope for the rename.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Max. No Fast.

---

## Clarify Decision Addendum (round-26 — 2026-08-16) — GPT Max re-verify B-1/H-1/M-1/M-2 + Opus Extra High re-verify B-1/H-1/H-2/M-1 ACCEPT

Interactive lock (parent-accepted; GPT Max re-verify [`4b5a10ff-b0d9-4933-9246-693ae35d2308`](4b5a10ff-b0d9-4933-9246-693ae35d2308) NOT CLEAN 1/1/2 + Opus Extra High re-verify [`74e66df1-00b5-4c83-9555-6c5e9b5b9575`](74e66df1-00b5-4c83-9555-6c5e9b5b9575) NOT CLEAN 1/2/1 on SHA `701cc66466830161476597af35606168377acb2437088fb86787eec0f6fcc884`; parent: ACCEPT all eight). Round-25 landings confirmed. New SHA `ac500b960f2ade792b4cc97f542986e39582bae9a295e8be8a9cca6f2955974b`. Prior frozen SHA invalidated after this ACCEPT. Max **not** re-launched. No Fast. Stayed on `main`. No commit.

**KEEP REJECT (unchanged):** ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose (no `/sb` WF mint); BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first (`memory_save` when AM opted in; `kl_write_am_skipped` when not); current-month cap; GST 34+35 degrade; B1 schema unchanged (`docs/apo-catalog.schema.json`); AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter (plan-cited or pre-existing catalog; **not** new PUB-01 mid-I); HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; no second agent-delegate AF/WF; `nested_executor` lock-only; `prompt_hash` binds inner prompt bytes only; public IDs are `sb` / `sb:` / `/sb` only (no dual `/silver` window). Catalog is **generated**; Python builders are SOT **after** committed un-regenerable state is back-ported. Catalog ids `WF-SILVER-*` may remain. `tests/run-all-tests.sh` remains the release gate — WS1 must not mandate a regen that fails `worker-template-parity`. PUB-01 synthesized flows remain required. Round-25 H-1/B-1/H-1/M-1 (catalog libs + generated catalog + hyphen ART + OpenCode/Pi evidence) stay.

### GPT Max B-1 — Mid-I `wf_mint` vs PUB-01 closure (accepted)

Executor mid-execution `wf_mint` may **invoke/instantiate**:

- a Work Plan–cited WF/AF (`plan_node_id` / WBS), or
- a **pre-existing catalog** WF that supports that cited node.

Creating a **new PUB-01 definition / new catalog WF record** mid-I is **out of plan** → row 40 → Advisor re-compose + composition-Val + plan-time Val **re-bind** the closure before Executor resumes. Do not run an unbound new definition during I. PUB-01 / composition-Val remain **before Executor I** for the bound set; the revalidation transition is the only way to enlarge it. WFM-01 / `VAL/TST-RFL-625` owns the fixture.

### GPT Max H-1 — Role table vs in-plan invent (accepted)

Executor **may** invent/instantiate **in-plan** nested WFs (plan-cited or pre-existing catalog). Executor **must not** invent new product-scope / new PUB-01 definitions (Advisor). Blanket “forbids invent a new WF” is wrong.

### GPT Max M-1 — FAST AM skip (accepted)

FAST thin-capture tests must **not** require `memory_save` when agentmemory is not opted in. Canonical: AM opted in → `memory_save` then classify/promote; AM not opted in → `kl_write_am_skipped`. Align VAL/TST FAST fixtures.

### GPT Max M-2 — `PP-SB-DEFAULT` vs PUB-01 locals (accepted)

`PP-SB-DEFAULT` / `generate-apo-catalog.py` must not forbid PUB-01 **project/global synthesized** flows as illegal extra-catalog AFs. Either: synthesized PUB-01 flows are **emitted into** catalog/lock (or a named overlay the checker accepts), **or** the default pack’s “no local AFs outside catalog” carves PUB-01 synthesized flows. One explicit generator + WS1 sentence. Do not drop PUB-01.

### Opus Extra High B-1 / H-2 — Reconcile **before** regen/`--check` (accepted)

WS1 ordered steps:

1. **Back-port** the committed catalog’s 3 semantic divergences into generator source (do not regen first).
2. Then regen `docs/apo-catalog.json`.
3. Then `--check` / parity gate.

Named: `check-apo-invariants.py worker-template-parity`, `AF-MULTI-AI-TASK.execution.worker_template`.

**Intended truth for `worker_template`:** `templates/orchestrator-workers/MULTI-AI-TASK.md` (committed + parity). Fix [`ATOMIC_SPECS`](scripts/generate-apo-catalog.py) (today `skills/silver-multi-ai-task/SKILL.md`) and/or [`merge_multi_ai_catalog()`](scripts/lib/apo_multi_ai_catalog.py) so regen **sets** `worker_template` to that templates/ path and does **not** emit `SKILL.md`. Round-24 H-3 artifact regen then stays consistent (`atomic-flow-index.json`, `composable-flows-contracts.md`).

Do not leave “regen then --check” as the first action while source still disagrees with committed.

### Opus Extra High H-1 — Stop stripping OpenCode/Pi from the delegate AF (accepted)

In [`apo_delegate_catalog.py`](scripts/lib/apo_delegate_catalog.py):

- **Remove** the strip of `AF-AGENT-DELEGATE` from `FS-SILVER_AGENT_OPENCODE` / `FS-SILVER_AGENT_PI` `reusable_by_flows`.
- Add those steps to `DELEGATE_FLOW_STEP_ORDER` **and** `DELEGATE_STEP_DEFS` in the **same** step (`build_delegate_flow_steps` indexes `DELEGATE_STEP_DEFS[step_id]`; adding ORDER without dict entries is `KeyError`).
- Name `DELEGATE_STEP_DEFS` and `build_delegate_flow_steps` / `merge_delegate_catalog` overwrites (~186/212) — **not** generic `build_flow_steps` in `generate-apo-catalog.py`.
- Intended `skill` / `purpose` / `classification` for those two records **mirror** `FS-SILVER_AGENT_{CODEX,CURSOR,CLAUDE}` (same classification family; skill = opencode/pi agent skill; purpose = catalog-backed step in `AF-AGENT-DELEGATE`).
- `SKILL_TO_FLOW` mapping to the AF must **agree** with merge (no map-then-detach).

Name `DELEGATE_STEP_DEFS` and `build_delegate_flow_steps` in WS1. Round-24 M-3 reciprocity is satisfiable **only after** this.

### Opus Extra High M-1 — Patch the **inline** AF V-loop (accepted)

There are **zero** `VL-FS-DELEGATE-*` top-level records. The underscore collision is `AF-AGENT-DELEGATE.v_loop.verification.artifact_refs = ["ART-AGENT_DELEGATE"]` (**inline** on the AF). Top-level `v_loops[]` stub has no `artifact_refs` — do **not** tell WS1 to patch that stub. Set inline `artifact_refs` to `["ART-AGENT-DELEGATE"]`. Sibling comparison = the already-attached cursor/codex/claude **step** V-loops / `ART-AGENT-DELEGATE`, not `VL-FS-DELEGATE-*`.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Max. No Fast.

---

## Clarify Decision Addendum (round-27 — 2026-08-16) — GPT Max leftover H-1/H-2 + Opus Extra High re-verify B-1/B-2/H-1/M-1 ACCEPT

Interactive lock (parent-accepted; GPT Max re-verify [`368dc0a1-4e7b-4662-b0f8-f2e398ca76dc`](368dc0a1-4e7b-4662-b0f8-f2e398ca76dc) NOT CLEAN 0/2/0 + Opus Extra High re-verify [`942927ab-f98e-4101-aafa-17c7be8b417b`](942927ab-f98e-4101-aafa-17c7be8b417b) NOT CLEAN 2/1/1 on SHA `ac500b960f2ade792b4cc97f542986e39582bae9a295e8be8a9cca6f2955974b`; parent: ACCEPT GPT leftover Highs as unpropagated round-26 residue, not new product, and ACCEPT Opus B-1 / B-2 / H-1 / M-1). Round-26 landings confirmed. Reviewer sandboxed the generator. New SHA `60cf413ddac0cbcb80073e776dd0f6d9d56302002d3a2019a682fbb5060410de`. Prior frozen SHA invalidated after this ACCEPT. Max **not** re-launched. No Fast. Stayed on `main`. No commit.

**KEEP REJECT (unchanged):** ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose (no `/sb` WF mint); BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first (`memory_save` when AM opted in; `kl_write_am_skipped` when not); current-month cap; GST 34+35 degrade; B1 schema unchanged (`docs/apo-catalog.schema.json`); AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter (plan-cited or pre-existing catalog; **not** new PUB-01 mid-I); HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; no second agent-delegate AF/WF; `nested_executor` lock-only; `prompt_hash` binds inner prompt bytes only; public IDs are `sb` / `sb:` / `/sb` only (no dual `/silver` window). Catalog is **generated**; back-port then regen. Catalog ids `WF-SILVER-*` **workflow** ids may remain — that does **not** freeze `FS-*` step ids derived from skill directory names. `tests/run-all-tests.sh` remains the release gate — WS1 must not mandate a regen that fails `worker-template-parity`. PUB-01 synthesized flows remain required. Row 40 → Advisor re-compose + composition-Val + plan-time Val re-bind. In-plan Executor mint stays. Round-26 landings stay.

### GPT Max H-1 — FAST not always `memory_save` (accepted)

Every remaining “always `memory_save`” / thin-capture test mandate matches round-26: AM opted in → `memory_save` then classify/promote; AM not opted in → `kl_write_am_skipped`. FAST tests must not require `memory_save` when AM is not opted in.

### GPT Max H-2 — Row 40 recovery + admission binding (accepted)

Row 40 recovery is **Advisor re-compose + composition-Val + plan-time Val** (not Advisor + plan-time Val only). Executor admission of the replacement revision is **bound** to current `definition_closure_hash` / `composition_generation` (or equivalent named stamps). A resume that does not carry the re-bound closure is not admitted. Surgical at row 40, WFM-01, and out-of-plan mint resume sentences.

### Opus Extra High B-1 — Name `DELEGATE_STEP_DEFS`; adding to ORDER is not enough (accepted)

`build_delegate_flow_steps()` indexes `DELEGATE_STEP_DEFS[step_id]`. Adding `FS-SILVER_AGENT_OPENCODE` / `FS-SILVER_AGENT_PI` to `DELEGATE_FLOW_STEP_ORDER` without dict entries is `KeyError`. WS1 must:

- Name `DELEGATE_STEP_DEFS` (and `build_delegate_flow_steps` / `merge_delegate_catalog` overwrites at ~186/212 — **not** generic `build_flow_steps` in `generate-apo-catalog.py`).
- Specify intended `skill` / `purpose` / `classification` for those two records **mirroring** `FS-SILVER_AGENT_{CODEX,CURSOR,CLAUDE}` (same classification family, skill = opencode/pi agent skill, purpose = catalog-backed step in `AF-AGENT-DELEGATE`).
- Add those `DELEGATE_STEP_DEFS` entries **and** ORDER in the same step.

### Opus Extra High B-2 — Rename and FS-* ids must move together (accepted)

`build_flow_steps` derives `FS-` + `name.upper().replace("-","_")` from skill **directory** names. WS2 `silver-agent-*` → `sb-agent-*` therefore forks twins unless hardcoded delegate tables retarget in the **same** commit.

Lock:

- `DELEGATE_FLOW_STEP_ORDER`, `DELEGATE_STEP_DEFS`, `AF-AGENT-DELEGATE.owning_skills`, and the `"silver-agent-worker"` filter (~`generate-apo-catalog.py`:550) retarget to post-rename `sb-agent-*` / `FS-SB_AGENT_*` **with** the directory rename.
- After rename: **one** id set only (no `FS-SILVER_AGENT_*` + `FS-SB_AGENT_*` twins). No `FS-SB_AGENT_WORKER` leak.
- Reciprocity / no-dangling-owning_skill use the **new** ids.
- Explicit: `WF-SILVER-*` may remain does **not** apply to derived `FS-*` ids.
- Generic `build_flow_steps` derivation vs hardcoded `DELEGATE_*` tables must not both emit.

### Opus Extra High H-1 — Name `PRECOMPOSED` (accepted)

`classify_skill` / `PRECOMPOSED` currently contains `silver-new-workflow`. After rename, `FS-SB_NEW_WORKFLOW.classification` becomes `flow-step-skill` instead of `precomposed-workflow`. WS2/WS1: add `sb-new-workflow` to `PRECOMPOSED` (drop or keep historical silver key). Add a check or named assertion so this classification cannot silently downgrade (parity of committed vs generator is insufficient if both are wrong).

### Opus Extra High M-1 — Kill leftover “catalog JSON edit” for FAST (accepted)

All remaining “only intended FAST **catalog JSON edit** is `PP-SB-STARTUP-FAST.override_rules[0]`” sentences now read: change lives in generator `PROCESS_PACK_DEFS`, then regen. Do not hand-edit `docs/apo-catalog.json` for that field. Index/target value stay (prefer `WF-SILVER-FEATURE` for 3-day greenfield).

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Max. No Fast.

---

## Clarify Decision Addendum (round-28 — 2026-08-16) — GPT Max H-1/H-2/H-3 + Opus Extra High H-1/H-2/M-1 ACCEPT

Interactive lock (parent-accepted; GPT Max re-verify [`9886413d-5f6c-4b07-80fe-07337d0afd44`](9886413d-5f6c-4b07-80fe-07337d0afd44) NOT CLEAN 0/3/0 on SHA `60cf413ddac0cbcb80073e776dd0f6d9d56302002d3a2019a682fbb5060410de`; Opus Extra High re-verify [`fa369f39-73a1-402d-8635-37d12fa2576d`](fa369f39-73a1-402d-8635-37d12fa2576d) official verdict **HASH MISMATCH** — plan moved mid-review to ~`5463fc19…`; Round-27 landings PASS; parent: ACCEPT GPT H-1/H-2/H-3 and Opus H-1/H-2/M-1). Reviewer sandboxed `build_catalog()` — measurements treated as fact. Mid-write SHA `5463fc192eed0cc601d1406536f628ad7e8f8c0b3e182f7a57c5d249b21c6e27` is **not** the finished freeze. Round-28 **final** SHA (both copies byte-identical): `aa3677a5531797f59465a1370b1909fb5c696a42067546b3b4f99f3ac4c5b30c`. Prior frozen SHA `60cf413ddac0cbcb80073e776dd0f6d9d56302002d3a2019a682fbb5060410de` invalidated after this ACCEPT. Max **not** re-launched. No Fast. Stayed on `main`. No commit.

**KEEP REJECT (unchanged):** ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose (no `/sb` WF mint); BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first (`memory_save` when AM opted in; `kl_write_am_skipped` when not); current-month cap; GST 34+35 degrade; B1 schema unchanged (`docs/apo-catalog.schema.json`); AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter (plan-cited or pre-existing catalog; **not** new PUB-01 mid-I); HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; no second agent-delegate AF/WF; `nested_executor` lock-only; **`prompt_hash` binds inner prompt bytes only** (do **not** put mutable prompt bodies into `prompt_hash`); `worktree_cwd` / `remaining_depth` are envelope/jsonl metadata, not `prompt_hash` inputs; public IDs are `sb` / `sb:` / `/sb` only (no dual `/silver` window). Catalog **id** `WF-SILVER-ROUTER` may remain. Schema unchanged. No second AF. Catalog is **generated**; back-port then regen. **Exactly one** `ART-AGENT-DELEGATE` record. Catalog ids `WF-SILVER-*` **workflow** ids may remain — that does **not** freeze `FS-*` step ids derived from skill directory names. `tests/run-all-tests.sh` remains the release gate — WS1 must not mandate a regen that fails `worker-template-parity`. PUB-01 synthesized flows remain required. Row 40 → Advisor re-compose + composition-Val + plan-time Val re-bind. In-plan Executor mint stays. Round-27 landings stay. GPT Max identity-binding Highs stay.

### GPT Max H-1 — `launch_intent` must carry closure identity (accepted)

Authorizer-admitted `launch_intent` **requires** `definition_closure_hash` and `composition_generation` (same stamps row 40 recovery already names). Omit → not admitted (`blocked_launch_prompt_spec`, row 4). Surgical at ~L433, ~L592, and the admission envelope list.

### GPT Max H-2 — Closure hash is recursive over nested WFs (accepted)

`definition_closure_hash` covers the **root WF + every recursively referenced Workflow definition + their AFs**, not only root WF + AFs. Nested WF mint of a pre-existing catalog WF is already in the closure; Advisor re-compose that adds a WF **changes** the hash (row 40 path).

### GPT Max H-3 — `context_refs` must be snapshotted or hashed (accepted)

Mutable `context_refs` **content** cannot change under an unchanged work-spec / launch identity. Hash the job-relevant K/L / work-spec-cited ref bytes into a sibling `context_refs_hash` on `launch_intent` (do **not** hash live agentmemory dumps) **and** snapshot those refs at admit; consume / nested-Task compare. Stale/mutated ref content with the same identity fail-closes as row 4 `blocked_launch_prompt_spec`.

### Opus Extra High H-1 — One artifact record; name the real emitters (accepted)

Do **not** only change `ATOMIC_SPECS` slug `AGENT_DELEGATE` → `AGENT-DELEGATE`: that plus `merge_delegate_catalog()` append yields **two records with the same id** and conflicting `path_pattern` (`.planning/apo/agent-delegate.*` vs `.planning/agent-<host>/<task-id>/*`). 16 invariant checks still PASS; no uniqueness test.

WS1 must name:

- `build_catalog()` artifacts comprehension (`f"ART-{slug}"`)
- `build_atomic_flows()` `artifact_refs`
- `merge_delegate_catalog()` append (~line 190)

**Exactly one** `ART-AGENT-DELEGATE` survives (hyphen; host-path pattern is the intended truth unless documented otherwise). Add `check-apo-invariants.py` **artifact-id uniqueness**. Inline AF `v_loop.artifact_refs` stays `["ART-AGENT-DELEGATE"]`.

### Opus Extra High H-2 — `WF-SILVER-ROUTER` slug + owning_skill (accepted)

Plan already retargets **triggers** to `/sb` and allows the catalog **id** to stay. Also retarget **`slug`** and **`owning_skill`** off `"silver"` to the renamed `skills/sb` skill (`sb`). A post-rename dangling `owning_skill: "silver"` is **in scope** (ship-introduced). `check-apo-invariants.py` currently does not inspect `owning_skill` — add or name a check for this record (or all workflows this ship retargets). Sibling `WF-SILVER-NEW-WORKFLOW` already has to-be slug/owning_skill — same discipline.

### Opus Extra High M-1 — `PP-SB-STARTUP-FAST.workflow_refs` order (accepted)

`override_rules[0]` prefers `WF-SILVER-FEATURE`, but `workflow_refs` still lists `WF-SILVER-FAST` first. Reorder (FEATURE first, or FAST not first for durable/greenfield ranking) in `PROCESS_PACK_DEFS` so the pack does not advertise FAST as the first recommendation. Fail-closed reclassify stays.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Max. No Fast.

---

## Clarify Decision Addendum (round-29 — 2026-08-16) — GPT Max H-1/H-2/H-3/M-1 + Opus Extra High B-1/H-1/M-1 ACCEPT

Interactive lock (parent-accepted; GPT Max re-verify [`bea156bd-36ca-4c24-bd70-4a891bc77031`](bea156bd-36ca-4c24-bd70-4a891bc77031) NOT CLEAN 0 blockers / 3 Highs / 1 Medium on SHA `aa3677a5531797f59465a1370b1909fb5c696a42067546b3b4f99f3ac4c5b30c`; **and** Opus Extra High re-verify [`8ed1bc81-2e08-48d8-9032-a0bc239fb5a6`](8ed1bc81-2e08-48d8-9032-a0bc239fb5a6) HASH MISMATCH mid-write `caa36067…` with B-1 / H-1 / M-1 surviving on the new bytes; Round-28 landings **PASS**; parent: ACCEPT GPT four + Opus three). Mid-write SHA `caa36067e2ed6e893b46209a8bec9ee99d8ef78015efb65f8b967a1190934418` (GPT Highs/M-1 only) is **not** the finished freeze. Round-29 **final** SHA `9a173a53f04eec56bc139d1e1ae67f7cdc3c0530a9860f71e6253a67e346e7be` (both plan copies byte-identical). Prior frozen SHA `aa3677a5531797f59465a1370b1909fb5c696a42067546b3b4f99f3ac4c5b30c` (round-28 final) invalidated after this ACCEPT. Max **not** re-launched. No Fast. Stayed on `main`. No commit.

**KEEP REJECT (unchanged):** Unlimited **tree** nesting of NWs (user lock). Recursive **cycles** are not that lock. In-plan Executor mint. `prompt_hash` binds inner prompt bytes only. Public IDs are `sb` / `sb:` / `/sb` only (no dual `/silver` window). ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose (no `/sb` WF mint); BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first (`memory_save` when AM opted in; `kl_write_am_skipped` when not); current-month cap; GST 34+35 degrade; B1 schema unchanged (`docs/apo-catalog.schema.json`); AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter (plan-cited or pre-existing catalog; **not** new PUB-01 mid-I); HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; no second agent-delegate AF/WF; `nested_executor` lock-only; `worktree_cwd` / `remaining_depth` are envelope/jsonl metadata, not `prompt_hash` inputs. Catalog **id** `WF-SILVER-ROUTER` may remain. Schema unchanged. No second AF. Catalog is **generated**; back-port then regen. **Exactly one** `ART-AGENT-DELEGATE` record. Catalog ids `WF-SILVER-*` **workflow** ids may remain — that does **not** freeze `FS-*` step ids derived from skill directory names. `tests/run-all-tests.sh` remains the release gate — WS1 must not mandate a regen that fails `worker-template-parity`. PUB-01 synthesized flows remain required. Row 40 → Advisor re-compose + composition-Val + plan-time Val re-bind. Round-28 landings stay.

### GPT Max H-1 — Cycle rejection (accepted)

Unlimited nesting is a **tree**. Self/mutual WF cycles fail-closed as `blocked_corrupt_state` (row 1). The `definition_closure_hash` walk MUST terminate (visited-set). Composition-Val / Advisor mint that would introduce a cycle is rejected before Executor I. Recursive cycles are **not** the unlimited-tree user lock.

### GPT Max H-2 — Hash correctness not just presence (accepted)

Admission **recomputes** the current recursive closure and **compares** to `launch_intent.definition_closure_hash` and `composition_generation`. Presence-only is insufficient. Stale/wrong hash or drifted generation → **row 4** (omit **or mismatch**).

### GPT Max H-3 — Immutable `context_refs` snapshot as child read source (accepted)

At admit: copy job-relevant K/L / work-spec-cited ref **bytes** to `$primary_checkout/.planning/packets/<launch_id>/context-refs-snapshot/`. The child **reads the snapshot**, not live files. `context_refs_hash` = SHA-256 of the specified canonical encoding (files only; UTF-8 NFC POSIX relative paths sorted lexicographically; for each path emit `UTF-8(path) || 0x00 || uint64_be(byte_length) || file_bytes`). Consume / nested-Task compare hash **and** that the snapshot still exists. TOCTOU on live docs after admit is not a valid read. Not live AM dumps.

### GPT Max M-1 — Clarify SHA ledger (accepted)

Header ledger no longer shows round-26 as current. Round-28 **final** SHA `aa3677a5531797f59465a1370b1909fb5c696a42067546b3b4f99f3ac4c5b30c` is recorded on the round-28 block and in the invalidated-prior list. `caa36067e2ed6e893b46209a8bec9ee99d8ef78015efb65f8b967a1190934418` is round-29 GPT-only **mid-write**, not the finished freeze. This freeze’s SHA is round-29 **final** `9a173a53f04eec56bc139d1e1ae67f7cdc3c0530a9860f71e6253a67e346e7be`. Round-27 `60cf413d…` is also in the invalidated-prior list so round-28’s actual freeze is not missing.

### Opus Extra High B-1 — Lock files need an emitter + input table (accepted)

`contracts/` does **not** exist. `generate-apo-artifacts.py` does **not** emit `apo-hierarchy.lock.json` / `public-workflow-routes.lock.json`. Catalog cannot carry `nested_executor` (`additionalProperties: false` unchanged). WS1 **creates** those two lock files via a **named generator** (new script or named function — not “the catalog emits locks”). Input for `nested_executor` is a **hand-authored table** in generator source (or a committed seed JSON the generator reads) listing the five `/sb:agent-*` leaves as lock class `nested_executor` — **not** a catalog extra property. Commit the generated locks; content-hash parity; CI drift fails. First ship: generated files **are** the baseline (no prior hash). Name `tests/scripts/test-router-contract-locks.sh` (**create it**). “Automated old/new lock-hash evidence” = this test + committed locks.

### Opus Extra High H-1 — Composition remint mints a new Executor `launch_id` (accepted)

Row-40 / Advisor re-compose **changes composition**, so Executor resume is **not** put-if-absent on the old `(prompt_hash, work_spec_hash)` only. Composition remint **mints a new `launch_id`** for the Executor replacement revision (same exception class as Val-fail 9a–9c / Process-scope dirty). Admission payload **includes** `definition_closure_hash` + `composition_generation`. Conflicting payload on the **old** `launch_id` stays blocked (CORR-17 fence). The new `launch_id` carries the re-bound closure. Do not ack generation-N as a duplicate of generation-N+1.

### Opus Extra High M-1 — `context_refs` snapshot has a WS + VAL/TST (accepted)

WS3 owns `hooks/lib/orchestrator-admission.sh` writing `$primary_checkout/.planning/packets/<launch_id>/context-refs-snapshot/`. Add `VAL/TST-RFL-626` (extend LPS-01) for hash + snapshot existence. Snapshot is **cited job-relevant refs only**, not whole `docs/knowledge/` / `docs/learnings/` trees. GC = packet lifecycle (same as other packet files).

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Max. No Fast.

---

## Clarify Decision Addendum (round-30 — 2026-08-16) — GPT Max H-1/H-2/H-3/M-1 + Opus Extra High B-1/H-1/H-2/H-3/M-1/M-2 ACCEPT

Interactive lock (parent-accepted; GPT Max re-verify [`74906da9-cc19-403d-b036-a6eae3e725de`](74906da9-cc19-403d-b036-a6eae3e725de) NOT CLEAN 0 blockers / 3 Highs / 1 Medium on SHA `9a173a53f04eec56bc139d1e1ae67f7cdc3c0530a9860f71e6253a67e346e7be`; **and** Opus Extra High re-verify [`99a0dd63-44bb-4e09-b2e7-6ffd2be3e1f4`](99a0dd63-44bb-4e09-b2e7-6ffd2be3e1f4) NOT CLEAN 1 Blocker / 3 Highs / 2 Mediums on the same SHA; Round-29 landings **PASS**; parent: ACCEPT GPT four + Opus six). One SHA for GPT Max + Opus Extra High together. Round-30 SHA `c1868fa31a9e424997ae9994376bac5d27e3f6886f74509c4f2717b21f36a93e` (both plan copies byte-identical). Prior frozen SHA `9a173a53f04eec56bc139d1e1ae67f7cdc3c0530a9860f71e6253a67e346e7be` (round-29 final) invalidated after this ACCEPT. Max **not** re-launched. Extra High **not** re-launched. No Fast. Stayed on `main`. No commit.

**KEEP REJECT (unchanged):** `hooks/lib/wbs-projector.sh` is the **exclusive** packet writer — do **not** add `orchestrator-admission.sh` as a second packet writer. Unlimited **tree** nesting of NWs (user lock); recursive **cycles** fail-closed. In-plan Executor mint. Row 40 remint **mints a new `launch_id`**. Snapshot is the child read source (cooperative bind). Public IDs are `sb` / `sb:` / `/sb` only (no dual `/silver` window). Catalog is **generated**. `nested_executor` lock-only. B1 schema unchanged (`docs/apo-catalog.schema.json`). `prompt_hash` binds inner prompt bytes only. ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose (no `/sb` WF mint); BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first (`memory_save` when AM opted in; `kl_write_am_skipped` when not); current-month cap; GST 34+35 degrade; AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter (plan-cited or pre-existing catalog; **not** new PUB-01 mid-I); HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; no second agent-delegate AF/WF; `worktree_cwd` / `remaining_depth` are envelope/jsonl metadata, not `prompt_hash` inputs. Catalog **id** `WF-SILVER-ROUTER` may remain. **Exactly one** `ART-AGENT-DELEGATE` record. `tests/run-all-tests.sh` remains the release gate. PUB-01 synthesized flows remain required. Round-29 landings stay.

### GPT Max H-1 / Opus Extra High B-1 — Projector writes the snapshot (accepted)

`hooks/lib/orchestrator-admission.sh` must **not** write packet files itself. It **requests** `hooks/lib/wbs-projector.sh` (sole allowlisted packet writer) to persist `$primary_checkout/.planning/packets/<launch_id>/context-refs-snapshot/`. WS3: admission helper + projector; no second writer. Amend every “sole writer” / allowlist / “sole writers unchanged” / row-1 helper-write contradiction (plan ~L48/L173/L429/L457/L542/L612/L705/L729/L738/L762/L764/L856). Do **not** carve admission as an extra allowlisted packet writer.

### GPT Max H-2 — Tri-color / active-stack cycle detection (accepted)

A visited-set **terminates** but cannot tell a **back-edge cycle** from **legal shared-node DAG reuse** (diamond). Require DFS **recursion-stack / tri-color**. Fixtures: self-cycle FAIL, mutual-cycle FAIL, shared-DAG (two parents one child WF) PASS. `blocked_corrupt_state` on cycles. Pin `VAL/TST-RFL-615`.

### GPT Max H-3 — Remint revokes old Executor authority (accepted)

New `launch_id` is not enough. **Before** admitting the replacement: revoke the old `launch_id`’s Authorizer-bound lease, capabilities, callbacks, and expected writes/effects (same bind-at-launch model). Conflicting reuse of the old id stays blocked (CORR-17). A still-running old Executor after remint is `blocked_corrupt_state` (row 1). *[superseded round-33: limb (b) = observable post-revoke effects only; live-but-fenced is not row 1]*

### GPT Max M-1 — TST-RFL-626 negative fixture (accepted)

`VAL/TST-RFL-626` must also prove a child **cannot** consume mutable live `context_refs` after admission (only the snapshot). Hash/existence alone is insufficient. Combined with Opus H-2: the fixture proves the bound prompt/receipt cites snapshot paths **and not** live `context_refs` paths — not a path-jail on Read.

### Opus Extra High H-1 — `context_refs_hash` stamp vs compare (accepted)

- **Launcher** may omit `context_refs_hash` on `launch_intent`.
- **At admit**, projector copies live `context_refs` into the snapshot, then stamps `context_refs_hash` as SHA-256 of that snapshot’s canonical encoding.
- Admission **does not** row-4 on pre-admit live-doc drift. Drift in the author→admit window is **refresh**: re-copy + re-stamp, then proceed.
- Row-4 `omitted or mismatched` applies **after** stamp: consume / nested launch must present the stamped hash and it must match a **recompute of the durable snapshot** (independent source = snapshot bytes, not the live files). Missing snapshot or hash≠snapshot → row 4.
- Do **not** make admission hash-then-compare the snapshot it just wrote as a self-satisfying check, and do **not** require the launcher to pre-hash live files.

### Opus Extra High H-2 — live-file read is cooperative, not a physical jail (accepted)

Cursor Task cannot PreToolUse-jail reads (same limit as L239 writes). Downgrade “child reading live files instead of that snapshot” from a claimed physical row-4 rail to a **cooperative child obligation**: child prompt/receipt **binds** snapshot paths; Verification-loop is the detection surface. Do not claim an unimplementable read jail.

### Opus Extra High H-3 — name the lock generator; fix L175 (accepted)

Lock emitter is `scripts/generate-router-contract-locks.py` (or a named function in that script). Add it to WS1 **named source surfaces** and to the L746 named regen command. Proposed architecture ~L175: **that** script emits `contracts/public-workflow-routes.lock.json` and `contracts/apo-hierarchy.lock.json` — not “Generators” / catalog builders / `generate-apo-artifacts.py`. Hand-authored `nested_executor` table stays hand-authored; generator does not invent that table. `tests/scripts/test-router-contract-locks.sh` remains create-it + committed-locks baseline.

### Opus Extra High M-1 — snapshot encoding: regular files only (accepted)

Admit copy **resolves nothing as a preserved symlink**. Copy **regular file bytes** only (follow symlink once to regular-file contents, then store a regular file in the snapshot — no symlink left in the snapshot tree). Non-regular entries (dirs already skipped; fifo/socket/device; dangling symlink; symlink loops) → fail-close row 4 / `blocked_corrupt_state` as appropriate. Hash covers the stored regular-file bytes, never a link string.

### Opus Extra High M-2 — snapshot GC tied to resumability (accepted)

Do **not** leave “GC = packet lifecycle” undefined. Snapshots **survive while `launch_id` is resumable** (parent-proxy consult continuation, ESC-02 re-dispatch, `plan_revision` under the same id). GC only after fence release **and** child terminality for that `launch_id`. A missing snapshot for a still-resumable id is row 4 / corrupt, not successful GC.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Max. Do not re-launch Extra High from this worker. No Fast.

---

## Clarify Decision Addendum (round-31 — 2026-08-16) — GPT Max High + Opus Extra High H-1/H-2 ACCEPT

Interactive lock (parent-accepted; GPT Max re-verify [`fb24bc26-df23-496d-8fc3-9027d95966c1`](fb24bc26-df23-496d-8fc3-9027d95966c1) NOT CLEAN 1 High (remint landing FAIL) on SHA `c1868fa31a9e424997ae9994376bac5d27e3f6886f74509c4f2717b21f36a93e`; parent: **ACCEPT that High.** **And** Opus Extra High re-verify [`a5d48a6d-3f9e-40fc-8799-b21d315c3acd`](a5d48a6d-3f9e-40fc-8799-b21d315c3acd) HASH MISMATCH mid-write `b062dc1c…` / `a007c83a…` with 2 Highs re-located at L120/L122; parent: **ACCEPT both.**). Round-31 **final** SHA `a8e7a463bb1bde980ed173b9ddd32e95accf0b6902d6ae92348145f1cffad9ca` (both plan copies byte-identical). Prior frozen SHA `c1868fa31a9e424997ae9994376bac5d27e3f6886f74509c4f2717b21f36a93e` (round-30) invalidated after this ACCEPT. Mid-write SHAs `b062dc1cbe92aaf9acf027804dd0719963551553d0c98da284bfa677c9a76a6b` and `a007c83a645ab6d96846de07b3a22b8233ecbafd6699e83b233f6c3b7b48fe97` are **not** the finished freeze. Max **not** re-launched. Extra High **not** re-launched. No Fast. Stayed on `main`. No commit.

**KEEP REJECT (unchanged):** `hooks/lib/wbs-projector.sh` is the **exclusive** packet writer — do **not** add `orchestrator-admission.sh` as a second packet writer. Unlimited **tree** nesting of NWs (user lock); recursive **cycles** fail-closed (DFS **tri-color / recursion-stack**, not visited-set). In-plan Executor mint. Row 40 remint **mints a new `launch_id`**. Snapshot is the child read source (cooperative bind). Launcher **may omit** `context_refs_hash` (stamp at admit). Public IDs are `sb` / `sb:` / `/sb` only (no dual `/silver` window). Catalog is **generated**. `nested_executor` lock-only. B1 schema unchanged (`docs/apo-catalog.schema.json`). `prompt_hash` binds inner prompt bytes only. ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose (no `/sb` WF mint); BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first (`memory_save` when AM opted in; `kl_write_am_skipped` when not); current-month cap; GST 34+35 degrade; AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter (plan-cited or pre-existing catalog; **not** new PUB-01 mid-I); HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; no second agent-delegate AF/WF; `worktree_cwd` / `remaining_depth` are envelope/jsonl metadata, not `prompt_hash` inputs. Catalog **id** `WF-SILVER-ROUTER` may remain. **Exactly one** `ART-AGENT-DELEGATE` record. `tests/run-all-tests.sh` remains the release gate. PUB-01 synthesized flows remain required. Round-30 landings stay.

### GPT Max High — Canonical row 1 classifies still-running old Executor after successful revocation (accepted) *[superseded round-33: limb (b) = observable post-revoke effects only; live-but-fenced is not row 1]*

The ordered canonical blocker table (row 1 `blocked_corrupt_state`) independently matches:

- **(a)** failure to complete revocation of the old `launch_id`'s Authorizer-bound lease / capabilities / callbacks / expected writes/effects **before** the replacement `launch_id` is admitted;
- **(b)** any evidence that the old Executor remains running after remint (process/session still live, still attempting writes/callbacks/effects) **regardless of whether revocation succeeded** — same as other helper-write / sole-writer violations. *[superseded round-33: limb (b) = observable post-revoke effects only; live-but-fenced is not row 1]*

This is **not** a new Job/GST/FAST exception. Do **not** invent a new row. Do **not** route it to row 4 `blocked_launch_prompt_spec`. Pin both cases in `VAL/TST-RFL-625` / WFM-01.

### Opus Extra High H-1 — L122 visited-set MUST → tri-color (accepted)

Proposed architecture L122 (and any remaining visited-set-as-the-algorithm `MUST`) no longer says the `definition_closure_hash` walk **MUST terminate via a visited-set**. The walk is DFS **recursion-stack / tri-color**. Visited-set alone is **not** sufficient. Keep: self-cycle FAIL, mutual-cycle FAIL, shared-DAG PASS; `VAL/TST-RFL-615`; `blocked_corrupt_state` on cycles. Historical round-29 visited-set quote in the Revised ledger stays history.

### Opus Extra High H-2 — L120 generated-template `context_refs_hash` omit (accepted)

`/sb:new-workflow` generated-template / authoring contract: `context_refs_hash` **may be omitted** on `launch_intent`; omit is **not** row 4 at submit. Projector stamps at admit. Row-4 mismatch applies **after** admit stamp vs recompute of **snapshot bytes**. Do not require templates to pre-hash live files. Child-reads-the-snapshot obligation stays (cooperative, not a physical jail).

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Max. Do not re-launch Extra High from this worker. No Fast.

---

## Clarify Decision Addendum (round-32 — 2026-08-16) — Opus Extra High M-1/M-2 + 3 nits ACCEPT

Interactive lock (parent-accepted; Opus Extra High re-verify [`d7a996e8-b213-4be9-8af6-514fac53ce0a`](d7a996e8-b213-4be9-8af6-514fac53ce0a) NOT CLEAN 0 Blockers / 0 Highs / 2 Mediums / 3 nits on SHA `a8e7a463bb1bde980ed173b9ddd32e95accf0b6902d6ae92348145f1cffad9ca`; Round-30/31 landings **PASS**; GPT Max already **CLEAN** on this SHA — findings not reopened; parent: **ACCEPT both Mediums and the three nits.**). Round-32 **final** SHA `3af884ef7892cc93ff5d69023632cf299ec18e87ce77d30860d205b694f317a4` (both plan copies byte-identical). Prior frozen SHA `a8e7a463bb1bde980ed173b9ddd32e95accf0b6902d6ae92348145f1cffad9ca` (round-31 final) invalidated after this ACCEPT. Max **not** re-launched. Extra High **not** re-launched. No Fast. Stayed on `main`. No commit.

**KEEP REJECT (unchanged):** `hooks/lib/wbs-projector.sh` is the **exclusive** packet writer — do **not** add `orchestrator-admission.sh` as a second packet writer. Unlimited **tree** nesting of NWs (user lock); recursive **cycles** fail-closed (DFS **tri-color / recursion-stack**, not visited-set). In-plan Executor mint. Row 40 remint **mints a new `launch_id`**. Snapshot is the child read source (cooperative bind). Launcher **may omit** `context_refs_hash` (stamp at admit). Public IDs are `sb` / `sb:` / `/sb` only (no dual `/silver` window). Catalog is **generated**. `nested_executor` lock-only. B1 schema unchanged (`docs/apo-catalog.schema.json`). `prompt_hash` binds inner prompt bytes only. ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose (no `/sb` WF mint); BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first (`memory_save` when AM opted in; `kl_write_am_skipped` when not); current-month cap; GST 34+35 degrade; AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter (plan-cited or pre-existing catalog; **not** new PUB-01 mid-I); HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; no second agent-delegate AF/WF; `worktree_cwd` / `remaining_depth` are envelope/jsonl metadata, not `prompt_hash` inputs. Catalog **id** `WF-SILVER-ROUTER` may remain. **Exactly one** `ART-AGENT-DELEGATE` record. `tests/run-all-tests.sh` remains the release gate. PUB-01 synthesized flows remain required. Round-31 landings stay. **L598 stands:** timeout, disconnect, missing process, or lease silence **cannot** prove abandonment. OFF-01 durable stopped acknowledgments remain **post-MVP**. Do not require process-death as an MVP oracle.

### Opus Extra High M-1 — Row 1 limb (b) is observable effects, not "still live" (accepted)

Revocation revokes **authority**, not the process. Limb (b) fires **only** on **observable post-revoke effects**: write/callback/effect attempts after remint that hit the CORR-17 fence (or equivalent attested receipt). **"Process/session still live" alone is not a row-1 match.** Harmless live-but-fenced old Executor is the expected remint window — not row-1 quarantine. Do **not** use timeout/disconnect/missing process/lease silence as abandonment proof (KEEP REJECT L598). Do **not** pull OFF-01 into MVP. Canonical row-1 classifier + `VAL/TST-RFL-625` / WFM-01 wording: tests **must not** treat "pid still exists" as FAIL.

### Opus Extra High M-2 — Row 1 remediation cell (accepted)

Canonical remediation cell (not only in-body prose) now states an exit for each match class:

- **Cycle class:** reject the draft; Advisor remint/recompose (L122/L727) — not "repair of corrupt store".
- **Revoke-before-admit failure:** do not admit the replacement until revoke succeeds, or fail-close without admitting (CORR-17).
- **Observable stale-Executor effects after remint:** CORR-17 fence holds; replacement `launch_id` proceeds; do **not** require killing the old process at MVP.
- **Corrupt store / helper-write / sole-writer / CAS / split-brain:** quarantine + reviewed repair.
- **`sb:<route>` identity collision:** remint may pick a non-colliding route id.

### Nits (accepted)

1. One canonical citation form: `VAL/TST-RFL-604`.
2. `context_refs_hash` has the same explicit "not a `prompt_hash` input" carve-out as `worktree_cwd` / `remaining_depth` (parity; inner-only lock unchanged).
3. VALP-01 traceability includes the cycle fixtures its testing row already pins (`VAL/TST-RFL-615` self/mutual/shared-DAG).

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Max. Do not re-launch Extra High from this worker. No Fast.

---

## Clarify Decision Addendum (round-33 — 2026-08-16) — Opus Extra High H-1 + nit n-1 ACCEPT

Interactive lock (parent-accepted; Opus Extra High re-verify [`aea56ad4-ca9e-46d6-9899-18b58346b5cd`](aea56ad4-ca9e-46d6-9899-18b58346b5cd) NOT CLEAN 1 High / 1 nit on SHA `3af884ef7892cc93ff5d69023632cf299ec18e87ce77d30860d205b694f317a4`; M-2 and nits N-1/N-2/N-3 **PASS**; M-1 **PARTIAL**; GPT Max already **CLEAN** on this SHA — findings not reopened; parent: **ACCEPT the High and the nit.**). Round-33 **final** SHA `ebd7ad9e499c34e669f8317fcc25715bdfab12a4ef5ed97ac14df6152575ae5e` (both plan copies byte-identical). Prior frozen SHA `3af884ef7892cc93ff5d69023632cf299ec18e87ce77d30860d205b694f317a4` (round-32 final) invalidated after this ACCEPT. Max **not** re-launched. Extra High **not** re-launched. No Fast. Stayed on `main`. No commit.

**KEEP REJECT (unchanged):** `hooks/lib/wbs-projector.sh` is the **exclusive** packet writer — do **not** add `orchestrator-admission.sh` as a second packet writer. Unlimited **tree** nesting of NWs (user lock); recursive **cycles** fail-closed (DFS **tri-color / recursion-stack**, not visited-set). In-plan Executor mint. Row 40 remint **mints a new `launch_id`**. Snapshot is the child read source (cooperative bind). Launcher **may omit** `context_refs_hash` (stamp at admit). Public IDs are `sb` / `sb:` / `/sb` only (no dual `/silver` window). Catalog is **generated**. `nested_executor` lock-only. B1 schema unchanged (`docs/apo-catalog.schema.json`). `prompt_hash` binds inner prompt bytes only. ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose (no `/sb` WF mint); BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first (`memory_save` when AM opted in; `kl_write_am_skipped` when not); current-month cap; GST 34+35 degrade; AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter (plan-cited or pre-existing catalog; **not** new PUB-01 mid-I); HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; no second agent-delegate AF/WF; `worktree_cwd` / `remaining_depth` are envelope/jsonl metadata, not `prompt_hash` inputs. Catalog **id** `WF-SILVER-ROUTER` may remain. **Exactly one** `ART-AGENT-DELEGATE` record. `tests/run-all-tests.sh` remains the release gate. PUB-01 synthesized flows remain required. Round-32 landings stay. **L598 stands:** timeout, disconnect, missing process, or lease silence **cannot** prove abandonment. OFF-01 durable stopped acknowledgments remain **post-MVP**. Do not require process-death as an MVP oracle. **Limb (b) is observable post-revoke effects only** — "process/session still live" alone is **not** row 1.

### Opus Extra High H-1 — Sweep live-spec restatements of un-narrowed limb (b) (accepted)

Canonical row-1 cell (L630) and fixtures (L737, L859) already had the narrowing. Four live-spec statements still asserted the rejected trigger. L251 / L253 / L265 / L669 now **cite row 1**: limb (b) = **observable post-revoke effects** only (CORR-17 fence / attested receipt). A live-but-fenced old Executor is **not** row 1. L251 (WFM-01 / `VAL/TST-RFL-625` fixture ownership) must not build a liveness/pid oracle. L669 row 40 must not contradict row 1 about liveness. Do not rewrite KEEP REJECT. Do not change the canonical cell.

### nit n-1 — Supersession pointers in append-only logs (accepted)

Plan revision cell (round-31 limb (b); round-30 H-3) and CLARIFY addenda (round-30 H-3 ~L1102; round-31 heading ~L1144 and limb (b) ~L1149) keep historical text. Recency governs. **Do not rewrite history.** Each of those entries now carries: *superseded round-33: limb (b) = observable post-revoke effects only; live-but-fenced is not row 1.*

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not start Max. Do not re-launch Extra High from this worker. No Fast.

---

## Clarify Decision Addendum (round-34 — 2026-08-16) — Opus Max H-1/M-1/M-2 + nit n-1 ACCEPT

Interactive lock (parent-accepted; Opus Max [`0723e455-5bf0-432f-9b26-7afe619b7db8`](0723e455-5bf0-432f-9b26-7afe619b7db8) NOT CLEAN 1 High / 2 Mediums / 1 nit / 0 Blockers on SHA `ebd7ad9e499c34e669f8317fcc25715bdfab12a4ef5ed97ac14df6152575ae5e`; Extra High and GPT Max already **CLEAN** on this SHA — findings not reopened except where this Max High/Medium proved a landing incomplete; parent: **ACCEPT H-1, M-1, M-2, and the specified nit.**). Round-34 **final** SHA `fe219ffeffd1bdff4a16debccb2a598f81e26176fdcc905d20af3c92a51f8b2b` (both plan copies byte-identical). Prior frozen SHA `ebd7ad9e499c34e669f8317fcc25715bdfab12a4ef5ed97ac14df6152575ae5e` (round-33 final) invalidated after this ACCEPT. Extra High **not** re-launched. Max **not** re-launched. No Fast. Stayed on `main`. No commit.

**KEEP REJECT (unchanged):** `hooks/lib/wbs-projector.sh` is the **exclusive** packet writer — do **not** add `orchestrator-admission.sh` as a second packet writer. Unlimited **tree** nesting of NWs (user lock); recursive **cycles** fail-closed (DFS **tri-color / recursion-stack**, not visited-set). In-plan Executor mint (Executor may mint/invoke WFs **only** to support a validated Work Plan / cited `plan_node_id`). Row 40 remint **mints a new `launch_id`**. Snapshot is the child read source (cooperative bind). Launcher **may omit** `context_refs_hash` (stamp at admit). Public IDs are `sb` / `sb:` / `/sb` only (no dual `/silver` window). Catalog is **generated**. `nested_executor` lock-only. B1 schema unchanged (`docs/apo-catalog.schema.json`). `prompt_hash` binds inner prompt bytes only. ESC-02 no A; Authorizer not Approver; `process_v_verified`; FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose (no `/sb` WF mint); BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first (`memory_save` when AM opted in; `kl_write_am_skipped` when not); current-month cap; GST 34+35 degrade; AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter (plan-cited or pre-existing catalog; **not** new PUB-01 mid-I); HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; no second agent-delegate AF/WF; `worktree_cwd` / `remaining_depth` are envelope/jsonl metadata, not `prompt_hash` inputs. Catalog **id** `WF-SILVER-ROUTER` may remain. **Exactly one** `ART-AGENT-DELEGATE` record. `tests/run-all-tests.sh` remains the release gate. PUB-01 synthesized flows remain required. Round-33 landings stay. **L598 stands:** timeout, disconnect, missing process, or lease silence **cannot** prove abandonment. OFF-01 durable stopped acknowledgments remain **post-MVP**. Do not require process-death as an MVP oracle. Do **not** invent a new MVP process-death / fence-release oracle. **Limb (b) is observable post-revoke effects only** — "process/session still live" / pid-exists alone is **not** row 1 / not FAIL.

### Opus Max H-1 — snapshot GC gated on CAS-provable supersession (accepted)

Drop “fence release **and** child terminality” as the MVP GC trigger (unreachable: fence release is undefined for a launch-id fence; child terminality is unprovable at MVP under L598 / pid-exists / OFF-01 post-MVP). Live-spec restatements at L263 / L433 / L592 / L728 / L738 now: snapshots **survive while `launch_id` is still-current** (not CAS-provably superseded) — parent-proxy consult continuation, ESC-02 re-dispatch, `plan_revision` under the **same** id. GC / drop snapshot retention when that `launch_id` is **CAS-provably superseded** (replacement `launch_id` admitted; CORR-17 fence on the old id **holds**). Collect **because** superseded, not because the fence released. Do **not** wait for fence release or process-death. Missing snapshot for a still-current id remains row 4 / corrupt. `VAL/TST-RFL-626` owns still-current-id retain / CAS-supersession GC. Historical round-30 M-2 changelog keeps its text with a round-34 supersession pointer.

### Opus Max M-1 — L511 diagram must say in-plan (accepted)

L511 WBS/parallelism Executor mint edge now matches L156: `in-plan wf_mint / wf_invoke` into “Authorizer-admitted **in-plan** nested WF”. Orchestrator/`/sb` still do not mint WFs. Prose gates at L112 / L118 / L122 / row 40 unchanged.

### Opus Max M-2 — one row for snapshot special-file failures (accepted)

Non-regular snapshot entries at admit (fifo/socket/device, dangling symlink, symlink loop) classify **exactly** to row 4 `blocked_launch_prompt_spec` (cannot form a valid snapshot / invalid `context_refs` for this launch). Not row 1. Not `blocked_corrupt_state`. L263 encoding rule, L630 row-1 LPS-01 exclusion, and L633 row-4 trigger all pin this single class. No “as appropriate” dual citation remains in live spec.

### nit n-1 — L470 WBS example “inserted in-plan NW” (accepted)

Local two-word edit on the ASCII WBS sample so the inserted-NW label matches L122 (“Executor-inserted **in-plan** NWs”). Extra High’s two already-non-blocking cosmetic nits were not churned.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not re-launch Extra High. Do not re-launch Max. No Fast.

## Clarify Decision Addendum (round-35 — 2026-08-16) — Opus Extra High M-1/M-2 + nit ACCEPT

Interactive lock (parent-accepted; Opus Extra High [`4d27c5bd-ddeb-4850-9b83-5ed01c5f503d`](4d27c5bd-ddeb-4850-9b83-5ed01c5f503d) NOT CLEAN 0 Blockers / 0 Highs / 2 Mediums / 1 nit on SHA `fe219ffeffd1bdff4a16debccb2a598f81e26176fdcc905d20af3c92a51f8b2b`; all four round-34 landings PASS; round-33 limb (b) 4/4 PASS; GPT Max already **CLEAN** on this SHA — not reopened; parent: **ACCEPT both Mediums and the nit.**). Round-35 **final** SHA `71427c3dda42824c3dc59d04fa500f62c904c169bf782dc853a37815106f8c54` (both plan copies byte-identical). Prior frozen SHA `fe219ffeffd1bdff4a16debccb2a598f81e26176fdcc905d20af3c92a51f8b2b` (round-34 final) invalidated after this ACCEPT. Extra High **not** re-launched. Max **not** re-launched. No Fast. Stayed on `main`. No commit.

**KEEP REJECT (unchanged):** Exclusive `hooks/lib/wbs-projector.sh` writer — do **not** add `orchestrator-admission.sh` as a second packet writer. Unlimited **tree** nesting of NWs; recursive **cycles** fail-closed (DFS **tri-color / recursion-stack**). **In-plan Executor mint** = L251 **two limbs**: (a) Work Plan–cited WF/AF **or** (b) a **pre-existing** catalog WF that supports that cited node. **New PUB-01 definition mid-I → row 40 + Advisor re-bind.** Orchestrator/`/sb` do not mint WFs. Remint **new `launch_id`**. Public `/sb`. Catalog generated. `nested_executor` lock-only. B1 schema unchanged (`docs/apo-catalog.schema.json`). Authorizer not Approver. ESC-02 no A. Launcher may omit `context_refs_hash`. **L598** no abandonment-by-silence. OFF-01 post-MVP. Limb (b) observable post-revoke only; pid-exists is not FAIL. **No** new process-death / fence-release / child-terminality oracle. Round-34 GC on **CAS-provable supersession** stays; this round **adds** a second collection trigger from an **existing** durable terminal — do not revert supersession-GC. Snapshot is the child read source (cooperative bind). `prompt_hash` binds inner prompt bytes only. FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose; BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first (`memory_save` when AM opted in; `kl_write_am_skipped` when not); current-month cap; GST 34+35 degrade; AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter (plan-cited or pre-existing catalog; **not** new PUB-01 mid-I); HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; no second agent-delegate AF/WF; `worktree_cwd` / `remaining_depth` are envelope/jsonl metadata, not `prompt_hash` inputs. Catalog **id** `WF-SILVER-ROUTER` may remain. **Exactly one** `ART-AGENT-DELEGATE` record. `tests/run-all-tests.sh` remains the release gate. PUB-01 synthesized flows remain required. Round-34 landings stay.

### Opus Extra High M-1 — L112/L118 two-limb lock + row 40 mid-I trigger (accepted)

L112 (and twin summaries including the `/sb:agent-*` L118 invent clause and Proposed-architecture row-40 trigger list at L122) now match L251: Executor `wf_mint` / `wf_invoke` is legal **iff** it **invokes/instantiates** (a) a Work Plan–cited WF/AF **or** (b) a **pre-existing catalog** WF that supports that cited node. Drop “new or pre-existing” as if both were equally legal at Executor mint time. L112 + L118 row-40 trigger includes **mid-I new PUB-01 definition / new catalog WF record** (not only uncited / new product scope), matching L185/L251/L253/L265/L669/L737. Row table L669 and fixture L737 already read correctly and were not churned. *superseded round-36: L669 canonical row-40 cell and L666 row-37 carve-out now enumerate the third limb (mid-I new PUB-01 / new catalog WF record); round-35 left the table unchurned. L737 already had it.*

### Opus Extra High M-2 — second GC trigger from existing durable terminal (accepted)

Supersession-only GC never collected a launch that succeeds (never reminted). Live-spec restatements at L263 / L433 / L592 / L728 / L738 / L762 now: collect snapshot when **either** (1) `launch_id` is CAS-provably superseded **or** (2) that launch’s durable `scope_complete` / Authorizer-acked `completion_receipt_id` is CAS-recorded. Still-current + not-complete retain (consult / ESC-02 / same-id `plan_revision`). Missing snapshot for a still-current incomplete id remains row 4. CORR-17 fence still **holds**. Do **not** use fence-release, child-terminality, or pid liveness. Round-34 supersession-GC **stays**; this is a second trigger, not a revert.

### nit — `VAL/TST-RFL-626` pins non-regular snapshot-entry fixtures (accepted)

Local to L738 / testing row: fifo FAIL, socket FAIL, device FAIL, dangling symlink FAIL, symlink loop FAIL → row 4 `blocked_launch_prompt_spec` (not row 1), analogous to `VAL/TST-RFL-615` cycle pins.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not re-launch Extra High. Do not re-launch Max. No Fast.

---

## Clarify Decision Addendum (round-36 — 2026-08-16) — Opus Extra High M-1a ACCEPT

Interactive lock (parent-accepted; Opus Extra High [`68a06c8f-8909-46ae-b692-0b46d3757304`](68a06c8f-8909-46ae-b692-0b46d3757304) NOT CLEAN 0 Blockers / 0 Highs / 1 Medium on SHA `71427c3dda42824c3dc59d04fa500f62c904c169bf782dc853a37815106f8c54`; round-35 M-1 prose/fixtures PASS except the **canonical rows table**; GPT Max already **CLEAN** on this SHA — not reopened; parent: **ACCEPT the Medium.**). Round-36 **final** SHA `9c9aa7d93a6fa7dd701e18528dd3d8f422cff0da47902aac4b8ac8c76ce21b06` (both plan copies byte-identical). Prior frozen SHA `71427c3dda42824c3dc59d04fa500f62c904c169bf782dc853a37815106f8c54` (round-35 final) invalidated after this ACCEPT. Extra High **not** re-launched. Max **not** re-launched. No Fast. Stayed on `main`. No commit.

**KEEP REJECT (unchanged):** Two-limb in-plan mint: (a) Work Plan–cited WF/AF **or** (b) a **pre-existing** catalog WF that supports that cited node. **Mid-I new PUB-01 definition / new catalog WF record → row 40, not row 37**, even when a `plan_node_id` is cited and there is no new product scope. Advisor re-bind. Exclusive `hooks/lib/wbs-projector.sh` writer — do **not** add `orchestrator-admission.sh` as a second packet writer. Unlimited **tree** nesting of NWs; recursive **cycles** fail-closed (DFS **tri-color / recursion-stack**). Remint **mints a new `launch_id`**. Public `/sb`. Catalog generated. `nested_executor` lock-only. B1 schema unchanged (`docs/apo-catalog.schema.json`). Authorizer not Approver. ESC-02 no A. **L598** no abandonment-by-silence. OFF-01 post-MVP. Limb (b) observable post-revoke only; pid-exists is not FAIL. **No** new process-death / fence-release / child-terminality oracle. Launcher may omit `context_refs_hash`. `prompt_hash` binds inner prompt bytes only. FAST not a Job / classify-not-mint; Orchestrator work-spec + Advisor compose; BOA parallel; no `AF-meta-six-role`; no `AF-qa`; AM-first (`memory_save` when AM opted in; `kl_write_am_skipped` when not); current-month cap; GST 34+35 degrade; AF-FAST-PATH only; PP-SB-STARTUP-FAST → WF-SILVER-FEATURE; Board-of-one unifier; agent-* in-plan Executor minter (plan-cited or pre-existing catalog; **not** new PUB-01 mid-I); HINST 3+2; HNEST Cursor 2 hops / Codex unbounded refuse-then-proxy / Claude 3; no second agent-delegate AF/WF; `worktree_cwd` / `remaining_depth` are envelope/jsonl metadata, not `prompt_hash` inputs. Catalog **id** `WF-SILVER-ROUTER` may remain. **Exactly one** `ART-AGENT-DELEGATE` record. `tests/run-all-tests.sh` remains the release gate. PUB-01 synthesized flows remain required. Round-35 landings stay (two-limb L112/L118; second snapshot GC trigger; VAL/TST-RFL-626 special-file fixtures).

### Opus Extra High M-1a — canonical rows 37/40 third limb (accepted)

The rows table defined row 40 as only two limbs (uncited `plan_node_id` / WBS id **or** new product scope) and row 37’s carve-out defined “the out-of-plan Executor case” as that same closed pair. An Executor that mints a **new catalog WF record for a node the plan does cite**, with no new product scope, matched neither row-40 limb → table would emit **row 37**, which L737 forbids (“stays row 40, **not row 37**”). Landed **in the canonical cells** (not only prose):

- **Row 40 (~L669):** trigger includes **mid-I new PUB-01 definition / new catalog WF record**, matching L185/L251/L253/L265/L737. Existing uncited / new product scope limbs kept.
- **Row 37 (~L666):** carve-out excludes that mid-I new-catalog-WF case so it cannot classify as row 37. “Out-of-plan Executor” is not a closed pair of (uncited OR new scope).

L859/L626 not churned (already consistent). L112/L118 two-limb lock not rewritten. KEEP REJECT not reopened.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not re-launch Extra High. Do not re-launch Max. No Fast.

---

## Clarify Decision Addendum (round-36 dismiss — 2026-08-16) — GPT High M-1 + Extra High M-1 REJECT (no plan edit)

Interactive lock (parent-accepted; GPT-5.6 Sol High Codex re-verify NOT CLEAN 0 Blockers / 0 Highs / 1 Medium **and** GPT-5.6 Sol Extra High Codex re-verify NOT CLEAN 0 Blockers / 0 Highs / 1 Medium on freeze SHA `9c9aa7d93a6fa7dd701e18528dd3d8f422cff0da47902aac4b8ac8c76ce21b06`; Opus High **CLEAN**; GPT Max, Opus Extra High, Opus Max already **CLEAN** on this SHA — not reopened). Parent: **REJECT both Mediums. Do not edit the plan.** Freeze **unchanged** `9c9aa7d93a6fa7dd701e18528dd3d8f422cff0da47902aac4b8ac8c76ce21b06` (both plan copies byte-identical). Treat GPT High **and** Extra High as **CLEAN for ladder purposes** after this dismiss. Extra High **not** re-launched. Max **not** re-launched. No Fast. Stayed on `main`. No commit. Not round-37 (no ACCEPT / no SHA churn).

**KEEP REJECT (unchanged):** Exclusive `hooks/lib/wbs-projector.sh` writer — do **not** add `orchestrator-admission.sh` as a second packet writer. Unlimited **tree** nesting of NWs; recursive **cycles** fail-closed (DFS **tri-color / recursion-stack**). **In-plan Executor mint** = L251 **two limbs**: (a) Work Plan–cited WF/AF **or** (b) a **pre-existing** catalog WF that supports that cited node. **Mid-I new PUB-01 definition / new catalog WF record → row 40, not row 37**, even when a `plan_node_id` is cited and there is no new product scope. **L598** no abandonment-by-silence. OFF-01 post-MVP. Limb (b) observable post-revoke only; pid-exists is not FAIL. Round-36 landings stay.

### GPT High M-1 — Document control Round-35 / omitted Round-36 SHA — REJECT

Provenance-only; recency in CLARIFY / [RFL-LADDER-4-START](rfl-router-subagent-surfaces-85bf9f09-20260812/RFL-LADDER-4-START.md) governs. Plan L80 `Revised` still begins Round-35 **final** and omits this freeze SHA; that is ledger lag, not an architecture miss. Landing it would churn the freeze and invalidate every CLEAN on `9c9aa7d9`. **Do not** change L80 / Document control.

### Extra High M-1 — CORR-11 “before Advisor” vs body L249→L255 — REJECT

CORR-11 (L823) is a stale traceability phrase, not a live-spec order. Audience: the body is the spec. Live order is Advisor **compose** root WF (L249) → composition-Val (L255) → Advisor **planning** (L275). CORR-11 already checks a **minted WF** (compose has run) and then distinguishes plan-time Val vs the Advisor plan; “before Advisor” is shorthand for before Advisor **planning**, not before compose. An implementer following the body would not invert the order. Do **not** rewrite L823. `VAL/TST-RFL-111` follow the body.

### Next SB lifecycle step

Stay on `main`. Do not commit. Do not re-launch Extra High. Do not re-launch Max. No Fast. Ladder-4 OpenCode `qwen3.8-max` **SKIPPED** this freeze (two 2h Go runs, durable `Endpoint is unavailable`, no `review.md` VERDICT; user skip — no third launch); DeepSeek V4 Pro and MiniMax M3 remain CLEAN on `9c9aa7d9…`.
















