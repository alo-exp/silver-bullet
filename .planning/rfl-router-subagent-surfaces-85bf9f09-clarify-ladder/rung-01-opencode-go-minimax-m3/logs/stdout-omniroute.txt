All acceptance criteria satisfied. Summary:

## Result

**No clarifications applied. No AskQuestion emitted. Both freeze copies already byte-identical.**

The freeze plan is fully locked:
- **Q1 (FAST/classified-trivial/`/sb:improve`)** — locked: FAST = classified-trivial, runs Executor → Verifier → Validator; `/sb:improve` is always a Job. Encoded in `KR-fast-overlay` + §6 Clarify Q1.
- **Q2 (workstream ownership)** — locked (A): WS1 emit / WS4 runtime / WS7 docs. Encoded in §5.3 and §6 Clarify Q2.
- **Q3 (deep research)** — locked: `WF-SILVER-DEEP-RESEARCH-MULTI-A` → **`WF-DEEP-RESEARCH`**; public `/sb:deep-research`; current impl as `/sb:legacy-dr`; no `/sb:multi-ai-task` alias. Encoded in §6 Clarify Q3.
- **Omni absorption** — composed (no new A/B/C); origin SHA `745c7f4166f70dff9181d7c8a639eb2e3519eedeb25487dda2f97e84425c2c26` in WS6 + LS-agent-pin.
- **KEEP REJECT** — closed (no reopen).
- **33/33 YAML todos** — all `status: pending`.

## Acceptance criteria — all green

- ✅ Every applied clarification listed (none applied; table documents the check)
- ✅ No KEEP REJECT reopen
- ✅ YAML todos remain `pending` (33/33)
- ✅ Both freeze copies byte-identical: SHA `cca61544e25d60e8f645910b55f456db35663a76440349c12de19895cfa726f2` on both `.planning/router_subagent_surfaces_85bf9f09.plan.md` and `~/.cursor/plans/router_subagent_surfaces_85bf9f09.plan.md`

## Constraints — all respected

- Planning-only. Public `/sb` only. No product hooks/skills/tests/workstreams implemented.
- FAST not a Job; not a legal `/sb:ladder|parallel <route>`.
- One-level compose (ladder XOR parallel).
- Authorizer not a pref key. No `sb:agent-wrap`. No `/sb:multi-ai-task`.
- Omni absorbed (SHA cited verbatim).
- Branch: `main`. No checkout/switch. No commit. No push.

## File written

`.planning/rfl-router-subagent-surfaces-85bf9f09-clarify-ladder/rung-01-opencode-go-minimax-m3/clarifications.md` (93 lines / 8.7 KB) — documents the already-locked inputs, the "none applied" table with rationale per check, the "none required" AskQuestion section, the acceptance-criteria check-off, the constraints check-off, and a note that the `Error: Unknown provider "opencode-go"` in `logs/stderr.txt` is a host-toolstack gap (not a freeze-plan gap) and is out of scope for this clarify run.
