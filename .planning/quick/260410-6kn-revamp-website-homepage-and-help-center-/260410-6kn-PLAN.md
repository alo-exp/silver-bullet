---
quick_id: 260410-6kn
description: "Revamp website homepage and Help Center to reflect all v0.14.0-v0.16.0 features"
task_count: 2
---

# Quick Task: Revamp Website for v0.14.0-v0.16.0 Features

## Task 1: Update site/index.html homepage

**Files:** `site/index.html`

**Action:**

Update the homepage to reflect v0.14.0-v0.16.0 capabilities. Specific changes:

1. **Meta tags** (lines 11-18): Update `<meta name="description">` and OG/Twitter descriptions to mention spec creation, artifact reviewers, review analytics, and cross-artifact consistency.

2. **Hero pills** (lines 786-789): Update from current counts. The project now has 39 skills (not just plugins). Update pills to reflect:
   - `39 Skills` (count from `skills/` directory)
   - `7 Orchestration Workflows` (unchanged)
   - `20 + 24 Steps` (unchanged)
   - `10 Enforcement Layers` (unchanged)

3. **Solution section pills** (lines 842-845): Same update as hero pills.

4. **Add a NEW section after the Solution section (after line 875) called "Spec-Driven Development"** with 4 feature cards:
   - **Spec Creation** (`silver:spec`): AI-driven spec elicitation with canonical SPEC.md format, structured requirements, acceptance criteria, and assumption tracking.
   - **External Ingestion** (`silver:ingest`): Ingest specs from JIRA, Figma, Google Docs via MCP connectors. No custom API code -- all access delegated to MCP.
   - **Pre-Build Validation** (`silver:validate`): Validate specs before planning. Spec floor enforcement, PR-to-spec traceability, UAT pipeline gate.
   - **Multi-Repo Coordination**: Cross-repo spec fetch via GitHub CLI. Read-only cache to `.planning/SPEC.main.md`. No git submodules.

5. **Add a NEW section after "Spec-Driven Development" called "Artifact Review System"** with 4 feature cards:
   - **8 Artifact Reviewers**: SPEC, DESIGN, REQUIREMENTS, ROADMAP, CONTEXT, RESEARCH, INGESTION_MANIFEST, UAT -- each with domain-specific QC checks.
   - **2-Consecutive-Clean-Pass Enforcement**: Every artifact must pass two consecutive clean reviews before approval. State tracked per artifact with SHA256-keyed JSON.
   - **Configurable Review Depth**: Configure deep/standard/quick per artifact type in `.planning/config.json`. Standard is default for backward compatibility.
   - **Cross-Artifact Consistency**: Validates SPEC-to-REQUIREMENTS-to-ROADMAP-to-DESIGN alignment. Wired into milestone completion and release workflow.

6. **Add a NEW section after "Artifact Review System" called "Review Analytics"** with 3 feature cards:
   - **JSON Lines Metrics**: Every review round emits structured metrics. Pass rates, rounds to clean pass, finding categories by artifact type.
   - **silver-review-stats**: Summary reports from review metrics. Three report tables covering pass rates, rounds to clean, and finding categories.
   - **Verification-Before-Completion**: Enforcement in review loop ensures verification completes before any completion claim.

7. **Update the "Best Community Skills" card** (line 855-856): Add mention of 39 skills including 8 artifact reviewers.

8. **SENTINEL section**: Add a brief mention of SENTINEL security hardening (shell injection fixes, markdown injection prevention) in the Security quality gate card (line 1301) or as part of enforcement layers.

Use the same HTML patterns, CSS classes, and structure as existing sections (feature-grid, feature-card, section-label, section-title, section-desc, fade-in, etc.). Match the existing visual style exactly.

For each new section, follow this template:
```html
<section id="{id}" style="background:var(--section-alt)">
  <div class="container text-center">
    <div class="section-label">{Label}</div>
    <h2 class="section-title">{Title}</h2>
    <p class="section-desc centered">{Description}</p>
    <div class="feature-grid mt-4 fade-in">
      <!-- feature-card elements -->
    </div>
  </div>
</section>
```

Alternate `style="background:var(--section-alt)"` on sections to maintain the existing visual rhythm.

**Done:** Homepage reflects all v0.14.0-v0.16.0 features. Meta descriptions updated. Three new sections (Spec-Driven Development, Artifact Review System, Review Analytics) present with correct feature descriptions. Skill count shows 39. No broken HTML or missing closing tags.

---

## Task 2: Update Help Center pages and search index

**Files:** `site/help/getting-started/index.html`, `site/help/concepts/index.html`, `site/help/reference/index.html`, `site/help/dev-workflow/index.html`, `site/help/workflows/index.html`, `site/help/search.js`

**Action:**

Update Help Center pages to document v0.14.0-v0.16.0 features:

### A. `site/help/getting-started/index.html`
- Update "What Silver Bullet does" to mention spec creation, artifact reviewers, review analytics, cross-artifact consistency alongside existing description.
- Update any skill/plugin counts to 39 skills.
- Add mention of `silver:spec`, `silver:ingest`, `silver:validate` as available commands.

### B. `site/help/concepts/index.html`
- Add new concept section for **Spec-Driven Development**: Explain SPEC.md canonical format, spec floor enforcement, ingestion from external sources via MCP, pre-build validation.
- Add new concept section for **Artifact Review System**: Explain the 8 reviewer skills, 2-consecutive-clean-pass enforcement, review state tracking, configurable depth (deep/standard/quick).
- Add new concept section for **Review Analytics**: JSON Lines metrics emission, silver-review-stats reports, verification-before-completion enforcement.
- Add new concept section for **Cross-Artifact Consistency**: SPEC-to-REQUIREMENTS-to-ROADMAP-to-DESIGN alignment validation, wired into milestone completion.
- Update the existing "Skills" concept to mention the 8 new reviewer skills and 3 new workflow skills (silver-spec, silver-ingest, silver-validate) for a total of 39 skills.

### C. `site/help/reference/index.html`
- Add `silver:spec` command reference: Spec creation with canonical SPEC.md format.
- Add `silver:ingest` command reference: External artifact ingestion (JIRA, Figma, Google Docs via MCP).
- Add `silver:validate` command reference: Pre-build spec validation, spec floor check.
- Add `silver-review-stats` command reference: Review analytics summary reports.
- Add any new hooks to the hooks reference: `spec-floor-check.sh`, `pr-traceability.sh`, `uat-gate.sh`.

### D. `site/help/dev-workflow/index.html`
- Add steps for artifact review gates in the dev workflow (Step 17.0a artifact review gates, Step 17.0b cross-artifact alignment -- inserted before UAT audit).
- Add step for cross-artifact consistency check (Step 7.5 in release workflow).
- Mention configurable review depth option.

### E. `site/help/workflows/index.html`
- Update the workflows overview if it references skill counts or version numbers.

### F. `site/help/search.js`
- Add search index entries for ALL new features. Each entry needs: page, url, anchor, title, text. Add entries for:
  - Spec creation (silver:spec)
  - External ingestion (silver:ingest, JIRA, Figma, Google Docs, MCP)
  - Pre-build validation (silver:validate)
  - Spec floor enforcement
  - PR-to-spec traceability
  - UAT pipeline gate
  - Multi-repo spec coordination
  - Artifact reviewer framework (2-consecutive-clean-pass)
  - 8 new reviewer skills (SPEC, DESIGN, REQUIREMENTS, ROADMAP, CONTEXT, RESEARCH, INGESTION_MANIFEST, UAT)
  - Configurable review depth (deep/standard/quick)
  - Review analytics (JSON Lines, silver-review-stats)
  - Cross-artifact consistency (SPEC-REQUIREMENTS-ROADMAP-DESIGN alignment)
  - Verification-before-completion enforcement
  - SENTINEL security hardening

Follow the exact format of existing entries in search.js. Group new entries under clear comment headers (e.g., `// -- SPEC-DRIVEN DEVELOPMENT`, `// -- ARTIFACT REVIEW SYSTEM`, `// -- REVIEW ANALYTICS`).

Match the HTML style and structure of existing help pages exactly. Use the same CSS classes, heading levels, and section patterns already present.

**Done:** All Help Center pages updated with v0.14.0-v0.16.0 content. search.js contains entries for all new features. No broken HTML. Search index entries follow existing format with page, url, anchor, title, text fields.
