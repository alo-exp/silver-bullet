---
phase: quick
plan: 260413-p4t
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/live/helpers.sh
  - tests/live/test-live-doc-scheme.sh
  - tests/live/run-live-tests.sh
autonomous: true
must_haves:
  truths:
    - "Live tests exercise all 6 documentation scheme scenarios via real claude CLI"
    - "New assertion helpers (assert_file_contains, assert_file_not_contains, assert_file_modified) work correctly"
    - "Doc scheme test suite is registered in run-live-tests.sh"
  artifacts:
    - path: "tests/live/test-live-doc-scheme.sh"
      provides: "6 live AI test scenarios for documentation scheme"
    - path: "tests/live/helpers.sh"
      provides: "New assertion helpers for file content and mtime checks"
    - path: "tests/live/run-live-tests.sh"
      provides: "Updated runner including doc-scheme suite"
---

<objective>
Create a live AI E2E test file that verifies Silver Bullet's documentation scheme artifacts are correctly created and updated by real Claude invocations.

Purpose: Validate that silver-init scaffolds knowledge/lessons files, finalization appends to them, CHANGELOG gets prepended, INDEX.md updates, lessons stay portable, and monthly boundaries are respected.
Output: `tests/live/test-live-doc-scheme.sh` with 6 scenarios, plus new assertion helpers in `helpers.sh`.
</objective>

<context>
@tests/live/helpers.sh
@tests/live/test-live-enforcement.sh
@tests/live/run-live-tests.sh
@templates/knowledge/INDEX.md.base
@templates/knowledge/YYYY-MM.md.base
@templates/lessons/YYYY-MM.md.base
@docs/project-documentation-scheme.md
@skills/silver-init/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add file content assertion helpers to helpers.sh</name>
  <files>tests/live/helpers.sh</files>
  <action>
Append three new assertion functions to tests/live/helpers.sh, following the exact pattern of existing assert_* functions (same label/PASS/FAIL formatting, same PASS/FAIL counter increment):

1. `assert_file_contains label filepath pattern` — uses `grep -iE "$pattern" "$filepath"` to check file content. PASS if match found, FAIL with file snippet if not.

2. `assert_file_not_contains label filepath pattern` — inverse of above. PASS if pattern NOT found, FAIL if found.

3. `assert_file_modified label filepath reference_time` — compares file mtime against a reference epoch timestamp. Uses `stat -f %m "$filepath"` (macOS) to get mtime. PASS if file mtime > reference_time, FAIL otherwise.

Also add a helper function `capture_mtime filepath` that prints the epoch mtime of a file (using `stat -f %m`), for use before invoke_claude to capture reference timestamps.
  </action>
  <verify>
    <automated>bash -c 'source /Users/shafqat/Documents/Projects/silver-bullet/tests/live/helpers.sh && type assert_file_contains && type assert_file_not_contains && type assert_file_modified && type capture_mtime && echo "ALL HELPERS DEFINED"'</automated>
  </verify>
  <done>Four new helper functions exist in helpers.sh, follow same PASS/FAIL pattern as existing assertions</done>
</task>

<task type="auto">
  <name>Task 2: Create test-live-doc-scheme.sh with 6 scenarios</name>
  <files>tests/live/test-live-doc-scheme.sh</files>
  <action>
Create `tests/live/test-live-doc-scheme.sh` following the exact structure of `test-live-enforcement.sh` (shebang, set -euo pipefail, source helpers.sh, echo header, scenarios, print_results).

Use `invoke_claude_permissive` for all scenarios (these tests need file write permissions).

Each scenario follows: echo header, live_setup, pre-seed workspace, invoke_claude, assertions, live_teardown.

**Workspace seeding helper** — define a function `seed_doc_scheme` at top of file that:
- Creates dirs: `docs/knowledge/`, `docs/lessons/`
- Copies template files from `$SB_ROOT/templates/knowledge/INDEX.md.base` to `$WORK_DIR/docs/knowledge/INDEX.md` (replacing `{{GIT_REPO}}` with "https://github.com/test/test.git")
- Copies `$SB_ROOT/templates/knowledge/YYYY-MM.md.base` to `$WORK_DIR/docs/knowledge/2026-04.md` (replacing `{{PROJECT_NAME}}` with "live-test" and `{{YYYY-MM}}` with "2026-04")
- Copies `$SB_ROOT/templates/lessons/YYYY-MM.md.base` to `$WORK_DIR/docs/lessons/2026-04.md` (replacing `{{YYYY-MM}}` with "2026-04")
- Commits all with `git -C "$WORK_DIR" add -A && git -C "$WORK_DIR" commit -q -m "seed docs"`

**S1: silver-init scaffolds all documentation scheme files**
- live_setup (creates fresh workspace with git + src)
- Prompt: "Run silver-init to initialize Silver Bullet for this project. The project name is 'live-test' and the git repo is 'https://github.com/test/test.git'. Accept all defaults."
- Assertions:
  - assert_file_exists "S1: knowledge INDEX exists" "$WORK_DIR/docs/knowledge/INDEX.md"
  - assert_file_exists "S1: knowledge monthly exists" "$WORK_DIR/docs/knowledge/2026-04.md"
  - assert_file_exists "S1: lessons monthly exists" "$WORK_DIR/docs/lessons/2026-04.md"
  - assert_file_exists "S1: config exists" "$WORK_DIR/.silver-bullet.json"
  - assert_file_contains "S1: INDEX references current month" "$WORK_DIR/docs/knowledge/INDEX.md" "2026-04|knowledge|lessons"
  - assert_file_contains "S1: knowledge has section headers" "$WORK_DIR/docs/knowledge/2026-04.md" "Architecture Patterns|Known Gotchas"
- live_teardown

**S2: Finalization step appends to knowledge/lessons**
- live_setup, then call seed_doc_scheme
- Capture mtime of both files: `k_mtime=$(capture_mtime "$WORK_DIR/docs/knowledge/2026-04.md")` and similar for lessons
- sleep 2 (ensure mtime difference)
- Prompt: "We just completed implementing a caching feature using Redis. Update docs/knowledge/2026-04.md and docs/lessons/2026-04.md with what we learned. Add to knowledge: an architecture pattern about cache-aside strategy, and a gotcha about Redis connection pooling. Add to lessons: a portable lesson about cache invalidation strategies under practice:architecture. Do not mention any project-specific names in the lessons file."
- Assertions:
  - assert_file_modified "S2: knowledge file modified" "$WORK_DIR/docs/knowledge/2026-04.md" "$k_mtime"
  - assert_file_modified "S2: lessons file modified" "$WORK_DIR/docs/lessons/2026-04.md" "$l_mtime"
  - assert_file_contains "S2: knowledge mentions caching" "$WORK_DIR/docs/knowledge/2026-04.md" "cache|Redis|caching"
  - assert_file_contains "S2: lessons mentions cache" "$WORK_DIR/docs/lessons/2026-04.md" "cache|invalidation"
- live_teardown

**S3: CHANGELOG.md gets prepended with correct task entry**
- live_setup, then call seed_doc_scheme
- Create an existing CHANGELOG.md with one entry:
  ```
  # Changelog

  ## 2026-04-10 — initial-setup
  - **What:** Project scaffolding
  - **Commits:** def5678
  - **Skills:** quality-gates
  ```
- Commit it
- Prompt: "Prepend a new CHANGELOG entry to docs/CHANGELOG.md for today's work. Task slug: redis-cache. What was done: Added Redis cache-aside layer for API responses. Commits: abc1234. Skills run: quality-gates, code-review. Knowledge updated: Architecture Patterns. Lessons updated: stack, practice."
- Assertions:
  - assert_file_contains "S3: new entry has slug" "$WORK_DIR/docs/CHANGELOG.md" "redis-cache"
  - assert_file_contains "S3: new entry has date" "$WORK_DIR/docs/CHANGELOG.md" "2026-04"
  - assert_file_contains "S3: entry has Knowledge ref" "$WORK_DIR/docs/CHANGELOG.md" "Knowledge|knowledge"
  - assert_file_contains "S3: entry has Lessons ref" "$WORK_DIR/docs/CHANGELOG.md" "Lessons|lessons"
  - assert_file_contains "S3: old entry preserved" "$WORK_DIR/docs/CHANGELOG.md" "initial-setup"
- live_teardown

**S4: knowledge/INDEX.md updates when new doc created**
- live_setup, then call seed_doc_scheme
- Create `$WORK_DIR/docs/SECURITY.md` with "# Security" content, commit
- Prompt: "A new SECURITY.md was created at docs/SECURITY.md. Update docs/knowledge/INDEX.md to include SECURITY.md in the index table. Keep all existing entries."
- Assertions:
  - assert_file_contains "S4: INDEX has SECURITY" "$WORK_DIR/docs/knowledge/INDEX.md" "SECURITY"
  - assert_file_contains "S4: INDEX still has Architecture" "$WORK_DIR/docs/knowledge/INDEX.md" "Architecture|ARCHITECTURE"
- live_teardown

**S5: Non-redundancy — lessons must be portable**
- live_setup, then call seed_doc_scheme
- Prompt: "Add a lesson to docs/lessons/2026-04.md about what we learned from implementing a Git hook-based enforcement system. The lesson should be portable — it must NOT mention any specific project names, tool names, or hook script names. Write it as a general practice lesson about pre-commit hook enforcement patterns."
- Assertions:
  - assert_file_contains "S5: lesson content added" "$WORK_DIR/docs/lessons/2026-04.md" "hook|enforcement|pre-commit"
  - assert_file_not_contains "S5: no silver-bullet mention" "$WORK_DIR/docs/lessons/2026-04.md" "silver-bullet|silver bullet"
  - assert_file_not_contains "S5: no specific hook names" "$WORK_DIR/docs/lessons/2026-04.md" "spec-floor-check|dev-cycle-check|completion-audit"
- live_teardown

**S6: Monthly file boundary — new month gets fresh file**
- live_setup, then call seed_doc_scheme
- Also create `$WORK_DIR/docs/knowledge/2026-03.md` with some frozen content ("## Architecture Patterns\n\n2026-03-15 — Old pattern about something"), commit
- Capture mtime: `old_mtime=$(capture_mtime "$WORK_DIR/docs/knowledge/2026-03.md")`
- sleep 2
- Prompt: "It is now April 2026. The March knowledge file docs/knowledge/2026-03.md is frozen and must not be modified. Verify that docs/knowledge/2026-04.md exists (it should from scaffolding). Add an architecture pattern about hook-based enforcement to docs/knowledge/2026-04.md. Do NOT modify docs/knowledge/2026-03.md."
- Assertions:
  - assert_file_exists "S6: April file exists" "$WORK_DIR/docs/knowledge/2026-04.md"
  - assert_file_contains "S6: April has new content" "$WORK_DIR/docs/knowledge/2026-04.md" "hook|enforcement"
  - assert_file_contains "S6: April has section headers" "$WORK_DIR/docs/knowledge/2026-04.md" "Architecture Patterns"
  - Use a direct check: compare mtime of 2026-03.md to old_mtime. If they differ, FAIL. Implement as: `new_mtime=$(capture_mtime "$WORK_DIR/docs/knowledge/2026-03.md"); if [[ "$new_mtime" -le "$old_mtime" ]]; then PASS; else FAIL; fi` — wrap in the standard assert format with label "S6: March file unchanged".
- live_teardown

Make the file executable: `chmod +x tests/live/test-live-doc-scheme.sh`
  </action>
  <verify>
    <automated>bash -c 'bash -n /Users/shafqat/Documents/Projects/silver-bullet/tests/live/test-live-doc-scheme.sh && echo "SYNTAX OK" && grep -c "^echo.*---.*S[1-6]" /Users/shafqat/Documents/Projects/silver-bullet/tests/live/test-live-doc-scheme.sh'</automated>
  </verify>
  <done>test-live-doc-scheme.sh exists, passes syntax check, contains all 6 scenarios (S1-S6), uses helpers.sh assertions, follows exact pattern of test-live-enforcement.sh</done>
</task>

<task type="auto">
  <name>Task 3: Register doc-scheme suite in run-live-tests.sh</name>
  <files>tests/live/run-live-tests.sh</files>
  <action>
Add a new `run_suite` line to `tests/live/run-live-tests.sh` after the existing three suites:

```bash
run_suite "Doc Scheme" "$SCRIPT_DIR/test-live-doc-scheme.sh"
```

Update the cost estimate comment from "$0.08-$0.40" to "$0.10-$0.60" to account for the 6 additional Claude invocations.
  </action>
  <verify>
    <automated>grep -c "doc-scheme\|Doc Scheme" /Users/shafqat/Documents/Projects/silver-bullet/tests/live/run-live-tests.sh</automated>
  </verify>
  <done>run-live-tests.sh includes Doc Scheme suite, cost estimate updated</done>
</task>

</tasks>

<verification>
1. `bash -n tests/live/test-live-doc-scheme.sh` — syntax valid
2. `grep -c "live_setup" tests/live/test-live-doc-scheme.sh` — returns 6 (one per scenario)
3. `grep -c "live_teardown" tests/live/test-live-doc-scheme.sh` — returns 6
4. `grep "Doc Scheme" tests/live/run-live-tests.sh` — suite registered
5. `grep "assert_file_contains\|assert_file_not_contains\|assert_file_modified" tests/live/helpers.sh` — new helpers present
</verification>

<success_criteria>
- test-live-doc-scheme.sh has 6 scenarios covering: init scaffolding, finalization append, CHANGELOG prepend, INDEX update, portability validation, monthly boundary
- All scenarios use invoke_claude_permissive and follow helpers.sh patterns exactly
- New assertion helpers (assert_file_contains, assert_file_not_contains, assert_file_modified, capture_mtime) added to helpers.sh
- Doc Scheme suite registered in run-live-tests.sh
- All files pass bash -n syntax check
</success_criteria>

<output>
After completion, create `.planning/quick/260413-p4t-create-and-run-live-ai-tests-for-sb-docu/260413-p4t-SUMMARY.md`
</output>
