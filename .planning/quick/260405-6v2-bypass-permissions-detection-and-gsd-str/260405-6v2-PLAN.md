---
phase: quick
plan: 260405-6v2
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/silver-bullet.md.base
  - silver-bullet.md
  - templates/workflows/full-dev-cycle.md
  - docs/workflows/full-dev-cycle.md
  - .planning/config.json
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [R1]

must_haves:
  truths:
    - "When bypass-permissions is active, session auto-sets autonomous mode without asking"
    - "All confirmation-asking behaviors are suppressed in bypass mode"
    - "Template and live files stay in sync for both section 4 and workflow Step 0"
    - ".planning/config.json exists with valid GSD structure"
    - "PROJECT.md has Core Value, Constraints, and Decisions sections"
    - "REQUIREMENTS.md has requirement IDs and coverage table"
    - "ROADMAP.md has Success Criteria per phase"
  artifacts:
    - path: "templates/silver-bullet.md.base"
      provides: "Bypass-permissions detection in section 4"
      contains: "bypass.permissions"
    - path: "templates/workflows/full-dev-cycle.md"
      provides: "Bypass-permissions detection in Step 0"
      contains: "bypass.permissions"
    - path: ".planning/config.json"
      provides: "GSD configuration"
    - path: ".planning/PROJECT.md"
      provides: "Expanded project definition"
      contains: "Core Value"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceable requirements with IDs"
      contains: "Coverage"
    - path: ".planning/ROADMAP.md"
      provides: "Phase success criteria"
      contains: "Success Criteria"
  key_links:
    - from: "templates/silver-bullet.md.base"
      to: "silver-bullet.md"
      via: "template instantiation"
      pattern: "bypass.permissions"
    - from: "templates/workflows/full-dev-cycle.md"
      to: "docs/workflows/full-dev-cycle.md"
      via: "template instantiation"
      pattern: "bypass.permissions"
---

<objective>
Add bypass-permissions auto-detection to Silver Bullet's session mode (section 4 and workflow Step 0), and fix GSD planning file structure gaps.

Purpose: (1) When users enable Claude Code's "Bypass permissions" toggle, SB should not ask interactive/autonomous — just auto-set autonomous and suppress all confirmation prompts. (2) Bring .planning/ files up to GSD standards so workflows don't break on missing config.json or incomplete metadata.

Output: Updated templates + live files for bypass detection; complete .planning/ structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@silver-bullet.md (section 4)
@templates/silver-bullet.md.base (section 4)
@docs/workflows/full-dev-cycle.md (Step 0)
@templates/workflows/full-dev-cycle.md (Step 0)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bypass-permissions detection to section 4 and workflow Step 0</name>
  <files>templates/silver-bullet.md.base, silver-bullet.md, templates/workflows/full-dev-cycle.md, docs/workflows/full-dev-cycle.md</files>
  <action>
In templates/silver-bullet.md.base section "## 4. Session Mode", add a new paragraph BEFORE the "At the start of every session" prompt block:

```
**Bypass-permissions detection:** If the session is running with Claude Code's
"Bypass permissions" toggle enabled (i.e., all tool calls are auto-accepted without
user confirmation prompts), skip the interactive/autonomous question entirely.
Auto-set autonomous mode immediately:
\```bash
echo "autonomous" > ~/.claude/.silver-bullet/mode
\```
Log: "Autonomous mode auto-set: bypass-permissions detected".
Also suppress ALL other confirmation-asking behaviors for the remainder of the session
(e.g., "Proceed? yes/no", phase gate approvals, model routing questions in section 5).
Use defaults for any skipped questions. Log each suppressed question under
"Autonomous decisions" with note "(bypass-permissions)".
```

Apply the IDENTICAL change to the live silver-bullet.md file (same section 4 location).

In templates/workflows/full-dev-cycle.md STEP 0, add the same bypass-permissions paragraph BEFORE the "Ask:" prompt. Also add after the existing fallback line:

```
**Bypass-permissions shortcut:** If bypass-permissions is detected (all tool calls
auto-accepted), skip the mode question AND the pre-answers follow-up. Auto-set
autonomous mode, use all defaults (Sonnet, main, isolated). Log:
"Step 0 skipped: bypass-permissions detected, autonomous mode with defaults".
```

Apply the IDENTICAL change to docs/workflows/full-dev-cycle.md.

IMPORTANT: Keep template placeholders (like {{PROJECT_NAME}}, {{ACTIVE_WORKFLOW}}) intact in template files. The live files should have the actual values.
  </action>
  <verify>
    <automated>grep -c "bypass.permissions" templates/silver-bullet.md.base silver-bullet.md templates/workflows/full-dev-cycle.md docs/workflows/full-dev-cycle.md | grep -v ":0$" | wc -l | xargs test 4 -eq</automated>
  </verify>
  <done>All four files contain bypass-permissions detection. Template and live files are in sync. No existing content removed or broken.</done>
</task>

<task type="auto">
  <name>Task 2: Fix GSD planning file structure</name>
  <files>.planning/config.json, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
1. Create .planning/config.json with GSD-standard structure:
```json
{
  "project_name": "silver-bullet",
  "version": "0.7.0",
  "planner_model": "claude-opus-4-6",
  "researcher_model": "claude-opus-4-6",
  "checker_model": "claude-sonnet-4-6",
  "commit_docs": true,
  "research_enabled": true
}
```

2. Expand .planning/PROJECT.md — keep existing Vision and Stack, add:
- **Core Value:** Single enforced workflow that eliminates the gap between "what AI should do" and "what AI actually does" — 7 compliance layers, zero single-point-of-bypass.
- **Constraints:** Must not modify third-party plugin files (GSD, Superpowers, etc.). All enforcement must survive context window resets. User CLAUDE.md instructions always override SB defaults.
- **Decisions:** (1) silver-bullet.md.base is the single source of truth for enforcement sections 0-9. (2) CLAUDE.md.base reduced to minimal scaffold referencing silver-bullet.md. (3) Conflict detection runs interactively during setup. (4) Update mode overwrites silver-bullet.md without confirmation.

3. Expand .planning/REQUIREMENTS.md — keep R1 content but:
- Add requirement IDs with prefix format (e.g., SB-R1)
- Add status column (SB-R1: COMPLETE)
- Add a Coverage Table section at the bottom mapping requirement IDs to phases/plans

4. Expand .planning/ROADMAP.md — keep existing Phase 1 content but:
- Add "**Success Criteria:**" under Phase 1 with 3-4 measurable criteria (e.g., "silver-bullet.md exists at project root with all 10 sections", "CLAUDE.md references silver-bullet.md", "Update mode overwrites only silver-bullet.md", "Conflict detection resolves contradictions interactively")
- Mark Phase 1 plan as complete: `- [x] 01-PLAN.md`
  </action>
  <verify>
    <automated>test -f .planning/config.json && node -e "JSON.parse(require('fs').readFileSync('.planning/config.json','utf8'))" && grep -q "Core Value" .planning/PROJECT.md && grep -q "SB-R1" .planning/REQUIREMENTS.md && grep -q "Coverage" .planning/REQUIREMENTS.md && grep -q "Success Criteria" .planning/ROADMAP.md && echo "PASS"</automated>
  </verify>
  <done>config.json exists and is valid JSON. PROJECT.md has Core Value, Constraints, Decisions. REQUIREMENTS.md has SB-prefixed IDs and coverage table. ROADMAP.md has success criteria per phase.</done>
</task>

</tasks>

<verification>
- All four bypass-permissions files contain the detection logic
- Template files preserve placeholders; live files have actual values
- .planning/config.json parses as valid JSON
- All .planning/ markdown files have the required GSD sections
</verification>

<success_criteria>
- Bypass-permissions detection present in silver-bullet.md section 4, its template, workflow Step 0, and its template
- .planning/config.json exists with project metadata
- PROJECT.md, REQUIREMENTS.md, ROADMAP.md all expanded to GSD standards
</success_criteria>

<output>
After completion, create `.planning/quick/260405-6v2-bypass-permissions-detection-and-gsd-str/260405-6v2-SUMMARY.md`
</output>
