---
phase: 260407-224
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/silver-init/SKILL.md
  - SENTINEL-audit-using-silver-bullet.md
  - README.md
  - silver-bullet.md
  - context.md
  - CONTRIBUTING.md
  - CHANGELOG.md
  - templates/silver-bullet.md.base
  - templates/silver-bullet.config.json.default
  - .silver-bullet.json
  - site/index.html
  - site/help/getting-started/index.html
  - site/help/concepts/index.html
  - site/help/reference/index.html
  - site/help/troubleshooting/index.html
  - site/help/search.js
  - hooks/dev-cycle-check.sh
  - docs/enforcement-techniques/claude.md
  - docs/Testing-Strategy-and-Plan.md
  - docs/Architecture-and-Design.md
  - docs/SENTINEL-audit-silver-bullet-v0.8.0-pass2.md
  - docs/SENTINEL-audit-silver-bullet-v0.8.0.md
  - docs/SENTINEL-audit-silver-bullet-v0.7.0.md
  - docs/specs/2026-04-02-gap-narrowing-plan.md
  - tests/e2e-smoke-test.md
  - tests/test-app/README.md
  - tests/test-app/silver-bullet.md
  - tests/test-app/.silver-bullet.json
autonomous: true
must_haves:
  truths:
    - "/silver:init is the only way to invoke the setup skill (no /using-silver-bullet references remain in live code or docs)"
    - "Skill directory is skills/silver-init/ with frontmatter name: silver:init"
    - "All tests pass after rename"
  artifacts:
    - path: "skills/silver-init/SKILL.md"
      provides: "Renamed skill with name: silver:init"
      contains: "name: silver:init"
    - path: "SENTINEL-audit-silver-init.md"
      provides: "Renamed audit file"
  key_links:
    - from: "silver-bullet.md"
      to: "skills/silver-init/SKILL.md"
      via: "/silver:init reference"
      pattern: "silver:init"
---

<objective>
Rename the /using-silver-bullet skill to /silver:init across the entire project.

Purpose: Shorter, namespaced skill name following Claude Code conventions.
Output: All references updated, directory renamed, tests passing.
</objective>

<context>
@skills/using-silver-bullet/SKILL.md
@README.md
@silver-bullet.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename skill directory and update skill file + core config</name>
  <files>
    skills/silver-init/SKILL.md
    SENTINEL-audit-silver-init.md
    .silver-bullet.json
    templates/silver-bullet.config.json.default
    templates/silver-bullet.md.base
    hooks/dev-cycle-check.sh
  </files>
  <action>
1. Rename directory: `git mv skills/using-silver-bullet skills/silver-init`
2. In skills/silver-init/SKILL.md:
   - Change frontmatter `name: using-silver-bullet` to `name: silver:init`
   - Replace ALL occurrences of `/using-silver-bullet` with `/silver:init`
   - Replace ALL occurrences of `using-silver-bullet` (non-slash path references like "skills/using-silver-bullet") with `silver-init` or `silver:init` as appropriate (directory paths use `silver-init`, skill invocations use `silver:init`)
   - Update the Plugin root comment: `${PLUGIN_ROOT}/skills/silver-init/SKILL.md`
3. Rename: `git mv SENTINEL-audit-using-silver-bullet.md SENTINEL-audit-silver-init.md`
   - Inside that file, replace all `using-silver-bullet` with `silver:init` or `silver-init` as appropriate
4. In .silver-bullet.json: replace `using-silver-bullet` with `silver-init` or `silver:init`
5. In templates/silver-bullet.config.json.default: replace `using-silver-bullet` with `silver-init` or `silver:init`
6. In templates/silver-bullet.md.base: replace `/using-silver-bullet` with `/silver:init`
7. In hooks/dev-cycle-check.sh: replace `using-silver-bullet` with `silver-init` or `silver:init` as appropriate

Naming rule: slash-invocations become `/silver:init`, filesystem paths become `silver-init`, skill name in frontmatter/config becomes `silver:init`.
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/silver-bullet && test -d skills/silver-init && grep -q "name: silver:init" skills/silver-init/SKILL.md && ! grep -r "using-silver-bullet" skills/silver-init/ .silver-bullet.json templates/ hooks/dev-cycle-check.sh SENTINEL-audit-silver-init.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Skill directory is skills/silver-init/, frontmatter says name: silver:init, no using-silver-bullet references in core files.</done>
</task>

<task type="auto">
  <name>Task 2: Update all docs, site, README, and test references</name>
  <files>
    README.md
    silver-bullet.md
    context.md
    CONTRIBUTING.md
    CHANGELOG.md
    site/index.html
    site/help/getting-started/index.html
    site/help/concepts/index.html
    site/help/reference/index.html
    site/help/troubleshooting/index.html
    site/help/search.js
    docs/enforcement-techniques/claude.md
    docs/Testing-Strategy-and-Plan.md
    docs/Architecture-and-Design.md
    docs/SENTINEL-audit-silver-bullet-v0.8.0-pass2.md
    docs/SENTINEL-audit-silver-bullet-v0.8.0.md
    docs/SENTINEL-audit-silver-bullet-v0.7.0.md
    docs/specs/2026-04-02-gap-narrowing-plan.md
    tests/e2e-smoke-test.md
    tests/test-app/README.md
    tests/test-app/silver-bullet.md
    tests/test-app/.silver-bullet.json
  </files>
  <action>
For every file listed above, perform find-and-replace:
- `/using-silver-bullet` → `/silver:init` (skill invocation references)
- `skills/using-silver-bullet` → `skills/silver-init` (filesystem path references)
- `using-silver-bullet` (standalone, e.g. in skill name contexts) → `silver:init` or `silver-init` depending on context (skill name = `silver:init`, file/dir path = `silver-init`)

For CHANGELOG.md specifically: add a new entry at the top under the current version section:
```
- **Renamed** `/using-silver-bullet` skill to `/silver:init` — shorter namespaced name
```

For .planning/ files (06-02-SUMMARY.md, 06-02-PLAN.md, 06-CONTEXT.md, 02-01-PLAN.md, 01-SUMMARY.md, 01-PLAN.md, 02-CONTEXT.md): these are historical planning artifacts. Do NOT update them — they reflect the state at the time they were written.
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/silver-bullet && HITS=$(grep -r "using-silver-bullet" README.md silver-bullet.md context.md CONTRIBUTING.md site/ docs/ tests/ 2>/dev/null | grep -v ".planning/" | wc -l | tr -d ' ') && echo "Remaining references: $HITS" && test "$HITS" -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Zero references to using-silver-bullet in any live (non-.planning/) file. All docs, site pages, and test files use /silver:init.</done>
</task>

<task type="auto">
  <name>Task 3: Run tests and final sweep</name>
  <files></files>
  <action>
1. Run the full test suite: `npm test` (or whatever test command is configured)
2. Do a final grep for any remaining `using-silver-bullet` references in tracked files (excluding .planning/ and .git/): `grep -r "using-silver-bullet" --include="*.md" --include="*.json" --include="*.js" --include="*.sh" --include="*.html" . | grep -v ".planning/" | grep -v ".git/" | grep -v node_modules`
3. If any references found, fix them.
4. Verify the skill can be found by Claude Code: confirm skills/silver-init/SKILL.md exists with correct frontmatter.
  </action>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/silver-bullet && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass. No using-silver-bullet references remain in any live project file.</done>
</task>

</tasks>

<verification>
1. `grep -r "using-silver-bullet" . --include="*.md" --include="*.json" --include="*.js" --include="*.sh" --include="*.html" | grep -v .planning/ | grep -v .git/ | grep -v node_modules` returns empty
2. `skills/silver-init/SKILL.md` exists with `name: silver:init` in frontmatter
3. `npm test` passes
4. `SENTINEL-audit-silver-init.md` exists, old file gone
</verification>

<success_criteria>
- Zero remaining references to "using-silver-bullet" in live project files (excluding .planning/ historical archives)
- Skill directory at skills/silver-init/ with correct frontmatter name: silver:init
- All tests pass
- CHANGELOG updated with rename note
</success_criteria>

<output>
Commit with message: "rename(skill): /using-silver-bullet → /silver:init across entire project"
</output>
