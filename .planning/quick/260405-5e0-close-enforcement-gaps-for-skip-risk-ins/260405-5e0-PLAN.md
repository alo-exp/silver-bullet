---
phase: quick-260405-5e0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/dev-cycle-check.sh
  - hooks/compliance-status.sh
  - templates/silver-bullet.md.base
  - templates/silver-bullet.config.json.default
  - .silver-bullet.json
autonomous: true
requirements: [ENF-01, ENF-02, ENF-03, ENF-04, ENF-05]

must_haves:
  truths:
    - "Stage B in dev-cycle-check.sh blocks source edits (blockToolUse:true) when code-review is missing"
    - "compliance-status.sh displays current session mode and GSD ownership reminder"
    - "silver-bullet.md.base contains anti-skip reinforcement text for every skippable section"
    - "quality-gates appears in required_deploy in both config files"
    - "GSD state file marker instructions exist in silver-bullet.md.base"
  artifacts:
    - path: "hooks/dev-cycle-check.sh"
      provides: "Stage B blocker upgrade"
      contains: "blockToolUse.*true"
    - path: "hooks/compliance-status.sh"
      provides: "Mode and ownership display"
      contains: "mode"
    - path: "templates/silver-bullet.md.base"
      provides: "Anti-skip reinforcement and GSD markers"
      contains: "you are violating"
    - path: ".silver-bullet.json"
      provides: "quality-gates in required_deploy"
      contains: "quality-gates"
    - path: "templates/silver-bullet.config.json.default"
      provides: "quality-gates in required_deploy template"
      contains: "quality-gates"
  key_links:
    - from: "templates/silver-bullet.md.base"
      to: "hooks/dev-cycle-check.sh"
      via: "anti-skip text references hook behavior"
      pattern: "dev-cycle-check"
    - from: "hooks/compliance-status.sh"
      to: "~/.claude/.silver-bullet/mode"
      via: "reads mode file"
      pattern: "mode"
---

<objective>
Close enforcement gaps for skip-risk instructions across hooks, templates, and config.

Purpose: Strengthen Silver Bullet enforcement so Claude cannot rationalize skipping workflow steps. Stage B becomes a blocker (not just a reminder), compliance status shows mode/ownership context, anti-skip text makes violations observable, and quality-gates is added to required_deploy.

Output: Updated hooks, templates, and config files with tighter enforcement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@hooks/dev-cycle-check.sh
@hooks/compliance-status.sh
@hooks/completion-audit.sh
@templates/silver-bullet.md.base
@.silver-bullet.json
@templates/silver-bullet.config.json.default
@silver-bullet.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Stage B to blocker and enhance compliance-status with mode/ownership</name>
  <files>hooks/dev-cycle-check.sh, hooks/compliance-status.sh</files>
  <action>
**dev-cycle-check.sh — Stage B blocker upgrade (lines 203-208):**

Replace the current Stage B block (the `if ! has_skill "code-review"` section that outputs a reminder message) with a blocker that matches Stage A's pattern. The new Stage B must:
- Output `blockToolUse: true` in the JSON (same pattern as Stage A on line 185)
- Message: "BLOCKED — Code review required before further source edits. Planning is complete but you must run /code-review before editing source code. Non-source operations (reading, commits, skill invocations) are still allowed."
- Keep the existing phase-skip detection block above it unchanged
- Keep Stage C and Stage D unchanged

**compliance-status.sh — Add mode and ownership display:**

After reading state_file (around line 82), add a section that:
1. Reads the mode file: `mode_file="${HOME}/.claude/.silver-bullet/mode"` and reads its content (default "interactive" if missing)
2. Appends mode to the output message: add `| Mode: ${mode}` after the existing step count
3. Appends a GSD ownership reminder: add `| GSD owns execution` to the message

The final message format should be:
`Silver Bullet: N steps | Mode: interactive | GSD owns execution | PLANNING X/Y | REVIEW X/Y | FINALIZATION X/Y | RELEASE X/Y | Next: /skill`

Keep the early-exit path (no state file, line 91-98) consistent — add mode reading before it so both paths show mode.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/shafqat/Documents/Projects/silver-bullet && grep -q "blockToolUse" hooks/dev-cycle-check.sh && grep -c "blockToolUse" hooks/dev-cycle-check.sh | grep -q "^[23]$" && grep -q "mode" hooks/compliance-status.sh && grep -q "GSD owns execution" hooks/compliance-status.sh && echo "PASS"'</automated>
  </verify>
  <done>Stage B outputs blockToolUse:true for source edits when code-review missing. compliance-status.sh shows mode and GSD ownership on every tool use.</done>
</task>

<task type="auto">
  <name>Task 2: Add anti-skip reinforcement to silver-bullet.md.base and GSD state markers</name>
  <files>templates/silver-bullet.md.base</files>
  <action>
Add anti-skip reinforcement blocks to silver-bullet.md.base. For each skippable section, add a clearly marked "Anti-Skip" subsection at the end with explicit violation conditions and observable evidence tests.

**Section 0 (Session Startup):** After step 4, add:
```
> **Anti-Skip:** You are violating this rule if you begin work without reading docs/ or skip /compact. Evidence: no Read tool calls for docs/ files in session start.
```

**Section 2 (Active Workflow):** After the skill-not-found rule, add:
```
> **Anti-Skip:** You are violating this rule if you start a non-trivial task without a Read call to the active workflow file. The compliance-status hook will show your progress — if it shows 0 steps, you have not read the workflow.
```

**Section 3 (Non-Negotiable Rules):** After the existing rules list, add:
```
> **Anti-Skip:** You are violating this rule if:
> - You produce source code without a skill invocation recorded in the state file (dev-cycle-check.sh will block you)
> - You claim "I already covered X" instead of invoking the skill (record-skill.sh tracks invocations, not claims)
> - You skip /gsd:verify-work at the end (completion-audit.sh will block your commit/push)
> - You proceed past a review loop with fewer than 2 consecutive approvals
```

**Section 4 (Session Mode):** After the autonomous mode rules, add:
```
> **Anti-Skip:** You are violating this rule if the mode file (~/.claude/.silver-bullet/mode) does not exist when you begin work. The compliance-status hook displays mode on every tool call — if it shows "unknown", you skipped this step.
```

**Section 5 (Model Routing):** After autonomous mode routing, add:
```
> **Anti-Skip:** You are violating this rule if you enter Planning or Design phases without offering the Opus upgrade. Evidence: no model switch prompt in conversation before /gsd:discuss-phase or design skill invocation.
```

**Section 6 (GSD Ownership):** After the hard rules, add:
```
> **Anti-Skip:** You are violating this rule if you use superpowers:executing-plans or superpowers:subagent-driven-development for project execution. The compliance-status hook shows "GSD owns execution" as a constant reminder.
```

**Section 9 (Pre-Release Quality Gate):** After the Pre-Release Gate Enforcement subsection, add:
```
> **Anti-Skip:** You are violating this rule if you attempt /create-release without all four quality-gate-stage-N markers in the state file. completion-audit.sh will block the release. Each stage requires explicit /superpowers:verification-before-completion invocation — the marker alone is insufficient.
```

**GSD State File Markers:** Add a new subsection after Section 3 (before Section 3a), titled "3b. GSD Command Tracking":
```
## 3b. GSD Command Tracking

After completing each GSD command, record a marker in the state file:

\`\`\`bash
echo "gsd-discuss" >> ~/.claude/.silver-bullet/state    # after /gsd:discuss-phase
echo "gsd-plan" >> ~/.claude/.silver-bullet/state       # after /gsd:plan-phase
echo "gsd-execute" >> ~/.claude/.silver-bullet/state    # after /gsd:execute-phase
echo "gsd-verify" >> ~/.claude/.silver-bullet/state     # after /gsd:verify-work
\`\`\`

These markers enable dev-cycle-check.sh and completion-audit.sh to verify
GSD commands were actually invoked, not just claimed. The same accepted
fragility as quality-gate-stage-N markers applies: markers depend on
Claude compliance, which is reinforced by the anti-skip text throughout
this file.

> **Anti-Skip:** You are violating this rule if you complete a GSD command without writing its marker. Future enforcement hooks will check for these markers.
```
  </action>
  <verify>
    <automated>bash -c 'cd /Users/shafqat/Documents/Projects/silver-bullet && grep -c "Anti-Skip" templates/silver-bullet.md.base | xargs test 8 -le && grep -q "gsd-discuss" templates/silver-bullet.md.base && grep -q "gsd-verify" templates/silver-bullet.md.base && grep -q "you are violating" templates/silver-bullet.md.base && echo "PASS"'</automated>
  </verify>
  <done>Every skippable section (0,2,3,4,5,6,9) has anti-skip text with violation conditions and observable evidence. Section 3b exists with GSD state file marker instructions.</done>
</task>

<task type="auto">
  <name>Task 3: Add quality-gates to required_deploy in config files</name>
  <files>.silver-bullet.json, templates/silver-bullet.config.json.default</files>
  <action>
In both `.silver-bullet.json` and `templates/silver-bullet.config.json.default`:

Add `"quality-gates"` as the FIRST entry in the `skills.required_deploy` array. Currently the array starts with `"code-review"`. After the change it should be:
```json
"required_deploy": [
  "quality-gates",
  "code-review", "requesting-code-review", "receiving-code-review",
  "testing-strategy", "documentation",
  "finishing-a-development-branch", "deploy-checklist",
  "create-release",
  "verification-before-completion"
]
```

This ensures completion-audit.sh checks that quality-gates was invoked before allowing commit/push/deploy, closing the gap where planning could be skipped but deploy still allowed.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/shafqat/Documents/Projects/silver-bullet && jq -e ".skills.required_deploy[0] == \"quality-gates\"" .silver-bullet.json && jq -e ".skills.required_deploy[0] == \"quality-gates\"" templates/silver-bullet.config.json.default && echo "PASS"'</automated>
  </verify>
  <done>quality-gates is the first entry in required_deploy in both config files. completion-audit.sh will now block deploy if quality-gates was not invoked.</done>
</task>

</tasks>

<verification>
Run all three verify commands in sequence. All must output PASS.
Additionally, confirm silver-bullet.md.base has no broken markdown by checking section headers are intact:
```bash
grep -c "^## " templates/silver-bullet.md.base
```
Should return 11 or more (original 10 sections + new 3b).
</verification>

<success_criteria>
- Stage B in dev-cycle-check.sh blocks source edits with blockToolUse:true
- compliance-status.sh shows mode and GSD ownership on every tool call
- All skippable sections in silver-bullet.md.base have anti-skip reinforcement
- GSD command tracking section (3b) exists with marker instructions
- quality-gates is in required_deploy in both config files
- No existing hook behavior broken (Stage A, C, D unchanged)
</success_criteria>

<output>
After completion, create `.planning/quick/260405-5e0-close-enforcement-gaps-for-skip-risk-ins/260405-5e0-SUMMARY.md`
</output>
