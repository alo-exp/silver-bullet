
## HEAD (no branch)
PLAN bytes=36750 sha=1c25c33cd16f957a8752dafd30a290e10e36e070a8db51c96889b85aae2f3e09
ART bytes=35739 sha=2feac1066947b07e99ea2fbcc3b594f046bf7fa89dd7b9b648fa1a480846db3b identical=False
53: - **Session continuity ⇒ interactive (auto only).** If keeping a live session with the external agent will improve the outcome, and the request is `auto`, interactive is **required** (classifier must 
73: - **D1 — Default mode = `auto` (not a fixed NI or TUI default).** Resolver order: **explicit pin > process-alive or continue/coach (D3) > classifier > non-interactive.** If `--interaction-mode interac
74: - **D2 — Canonical interaction flag `--interaction-mode auto|interactive|non-interactive`.** Default `auto`. **Do not reuse `--mode`:** live `--mode` remains **permission** `permissive|strict` on ever
75: - **D3 — Session continuity requires interactive (auto only).** D3 **live session** means §4.1 (1) or (2) only — **not** (3). Signals: (1) OS child still running; (2) reusable conversation id **and** 
87: - **D8 — Optional human attach** (`--attach` / `SB_AGENT_MODE_ATTACH=1`): applies only when **resolved** mode is interactive; same PTY; parent still owns PASS/FAIL. **Not a mode pin** (I-20): `--inter
113:   tok -->|no 3 resume-token only or absent| clf
130: Session stick is the §4.1 three-way split, **not** “any live `session.json`”. (1) process-alive and (2) resume-token + continue/coach or in-wave Cursor → interactive; (3) resume-token only after termi
138: - Session liveness is **split** (I-18): (1) **OS child still running** → D3 interactive; (2) **reusable conversation id** + explicit continue/coach utterance **or** in-wave Cursor follow-up → interact
147: - `--no-escalate` disables D4 **and** disables this in-flight-escalate force-interactive (tests/CI). It does **not** ignore a currently **process-alive** in-TTL session (D3 (1)) or continue/coach on a
152: - No process-alive in-TTL session; no continue/coach; leftover resume-token after terminal result is not D3.
254:   [--work-dir <path>] [--log <path>] [--attach]
272: --attach                                              # human attach if resolved interactive; NOT an auto pin (I-20)
280: Env (CLI `--interaction-mode` wins when present; leftover concrete env is **not** a pin — I-21): `SB_AGENT_INTERACTION_MODE`, `SB_AGENT_MODE_ATTACH=1`, `SB_AGENT_NO_ESCALATE=1`, `SB_AGENT_ALLOW_MODE_F
294: | `--interaction-mode non-interactive` + `--attach` / `--control-dir` / `--max-turns` | Interactive-only flags on NI |
295: | `--interaction-mode auto` (or omitted) + `--attach` / `--control-dir` | **Not a pin** (classifier still runs). If classified interactive: attach/control-dir valid (D8). If classified NI: fail `mode-
296: | Inherited `SB_AGENT_INTERACTION_MODE=interactive\|non-interactive` without argv `--interaction-mode` / alias / legacy pin | Leftover env pin; fail `mode-conflict` (`leftover-env-pin`) (I-21) |
375: 11. **Tests** — classifier fixtures (continue → interactive, bounded implement+test → NI, leftover conversation id after terminal `result.md` with no continue → NI not D3 (I-18), explicit pin wins ove
400: - Leftover conversation id after terminal `result.md` (no continue utterance) classifies fresh / NI — not D3 (I-18).
401: - `--interaction-mode auto --attach` does not skip the classifier; classified NI fails `mode-conflict` (`attach-on-ni`) (I-20).
402: - Inherited concrete `SB_AGENT_INTERACTION_MODE` without argv `--interaction-mode` fails `mode-conflict` (`leftover-env-pin`) (I-21).