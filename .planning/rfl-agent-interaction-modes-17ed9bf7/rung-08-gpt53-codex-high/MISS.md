# Rungs 8–9 miss — GPT-5.3 Codex High / Extra High

`codex exec -m gpt-5.3-codex -c model_reasoning_effort=high` → **401 Unauthorized** (no bearer). Extra High not attempted (same auth). No remap to Grok.
