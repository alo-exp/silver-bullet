# Slug probe — DeepSeek V4 Pro Max

```
opencode run --dir <repo> -m opencode-go/deepseek-v4-pro --variant max --auto "Reply with the single word PONG and stop."
```

Exit: 0  
Stdout: `PONG`  
Banner: `build · deepseek-v4-pro`
