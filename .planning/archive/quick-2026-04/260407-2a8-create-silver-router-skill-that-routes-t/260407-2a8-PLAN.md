---
phase: quick
plan: 260407-2a8
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/silver/SKILL.md
  - README.md
  - site/help/search.js
  - CHANGELOG.md
autonomous: true
must_haves:
  truths:
    - "/silver routes SB skill keywords to the correct SB skill"
    - "/silver routes GSD-related text to /gsd:do with original arguments"
    - "/silver shows a routing banner with input, destination, and reason"
    - "/silver asks a clarifying question when input is ambiguous"
  artifacts:
    - path: "skills/silver/SKILL.md"
      provides: "Pure dispatcher skill routing to SB and GSD skills"
      contains: "name: silver"
    - path: "README.md"
      provides: "Updated Built-in Skills table with /silver entry"
      contains: "/silver"
    - path: "site/help/search.js"
      provides: "Search index entry for /silver"
      contains: "silver"
    - path: "CHANGELOG.md"
      provides: "Unreleased entry for /silver"
      contains: "/silver"
  key_links:
    - from: "skills/silver/SKILL.md"
      to: "/gsd:do"
      via: "delegation for GSD-related input"
      pattern: "gsd:do"
---

<objective>
Create `/silver` — a pure dispatcher skill that routes freeform natural language to the best Silver Bullet or GSD skill.

Purpose: Single entry point for users who don't know which specific skill to invoke.
Output: skills/silver/SKILL.md + README/search/changelog updates.
</objective>

<execution_context>
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/workflows/execute-plan.md
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/templates/summary.md
</execution_context>

<context>
@skills/silver-init/SKILL.md
@README.md
@site/help/search.js
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /silver dispatcher skill</name>
  <files>skills/silver/SKILL.md</files>
  <action>
Create `skills/silver/SKILL.md` with frontmatter `name: silver` and `description: Routes freeform text to the best Silver Bullet or GSD skill`.

The skill must be a PURE DISPATCHER — it never does work itself. Structure:

1. **Routing Banner** — For every invocation, display a banner:
   ```
   --- /silver router ---
   Input:      {user's freeform text}
   Routing to: {chosen skill}
   Reason:     {one-line explanation}
   ---
   ```

2. **SB Routing Table** — Match input keywords/intent to SB skills:
   - `/silver:init` — triggers: setup, initialize, onboarding, install, scaffold, "silver bullet init", configure project
   - `/quality-gates` — triggers: quality review, ilities, architecture review, quality dimensions, code quality check
   - `/blast-radius` — triggers: risk assessment, infrastructure change impact, blast radius, change scope, rollback plan
   - `/devops-quality-gates` — triggers: IaC quality, DevOps quality review, infrastructure quality, terraform quality
   - `/forensics` — triggers: debugging, incident investigation, root cause, session failed, stalled, wrong output
   - `/create-release` — triggers: release, publish, changelog, version bump, tag, ship release, github release
   - `/devops-skill-router` — triggers: DevOps toolchain, which IaC skill, terraform vs pulumi, infrastructure tool

3. **GSD Delegation** — For GSD-related input (plan, execute, discuss, debug, new project, ship, verify, next, research, do, status, forensics workflow issues, roadmap), delegate to `/gsd:do` passing the original user text as arguments. Do NOT attempt to route to individual GSD commands — let `/gsd:do` handle that.

   GSD trigger keywords: plan, execute, discuss, debug, new project, ship, verify, next, research, status, roadmap, phase, milestone, implement, build, code, develop, fix bug, refactor, test, deploy, PR, pull request, commit.

4. **Ambiguous Input** — If input does not clearly match any SB skill or GSD category, ask a single clarifying question presenting the top 2-3 most likely destinations with one-line descriptions, then route based on the answer.

5. **Routing Priority** — Check SB routing table first (exact domain matches). If no SB match, check GSD triggers. If neither, ask for clarification.

The skill should invoke the target skill using the Skill tool (for SB skills) or by telling the user to run the command (for GSD commands via `/gsd:do`). For GSD delegation, invoke `/gsd:do` via the Skill tool passing the original text.
  </action>
  <verify>
    <automated>test -f skills/silver/SKILL.md && grep -q "name: silver" skills/silver/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>skills/silver/SKILL.md exists with frontmatter name: silver, contains routing table for all 7 SB skills, GSD delegation to /gsd:do, ambiguity handling, and routing banner format.</done>
</task>

<task type="auto">
  <name>Task 2: Update README, search index, and changelog</name>
  <files>README.md, site/help/search.js, CHANGELOG.md</files>
  <action>
**README.md** — In the "Built-in Silver Bullet Skills" table (line ~190), add `/silver` as the FIRST row after the header (it is the main entry point):
```
| `/silver` | Main entry point — routes freeform text to the best SB or GSD skill |
```

**site/help/search.js** — Add a new entry to the IDX array for the /silver skill. Follow the existing entry format (object with page, url, anchor, title, text fields). Use page "Skills", url "/help/skills/", anchor "silver", title "/silver — Skill Router". Text should describe that it routes freeform natural language to the best Silver Bullet skill or GSD command, listing the 7 SB skills it can route to plus GSD delegation.

**CHANGELOG.md** — Under `## [Unreleased]`, add:
```
- **Added** `/silver` dispatcher skill — routes freeform text to the best SB or GSD skill
```

Then run the test suite to confirm nothing is broken:
```bash
bash tests/run-all-tests.sh
```
All 187+ tests must pass.
  </action>
  <verify>
    <automated>grep -q "/silver" README.md && grep -q "silver" site/help/search.js && grep -q "/silver" CHANGELOG.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README skills table includes /silver as first entry, search.js has /silver indexed, CHANGELOG [Unreleased] mentions /silver, all tests pass.</done>
</task>

</tasks>

<verification>
1. `skills/silver/SKILL.md` exists with correct frontmatter and routing logic
2. README Built-in Skills table lists `/silver`
3. `site/help/search.js` includes `/silver` entry
4. CHANGELOG [Unreleased] has `/silver` entry
5. `bash tests/run-all-tests.sh` passes 187+ tests
</verification>

<success_criteria>
- /silver skill file created as pure dispatcher with routing table, GSD delegation, ambiguity handling
- All documentation touchpoints updated (README, search, changelog)
- Test suite green
</success_criteria>

<output>
After completion, create `.planning/quick/260407-2a8-create-silver-router-skill-that-routes-t/260407-2a8-SUMMARY.md`
</output>
