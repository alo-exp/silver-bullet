# Quick Task 260405-5e0: Close enforcement gaps for skip-risk instructions - Context

**Gathered:** 2026-04-04
**Status:** Ready for planning

<domain>
## Task Boundary

Close enforcement gaps for skip-risk instructions — upgrade dev-cycle-check.sh Stage B to blocker, enhance compliance-status.sh with mode/ownership display, add anti-skip reinforcement to silver-bullet.md.base, add quality-gates to required_deploy, add GSD state file markers.

</domain>

<decisions>
## Implementation Decisions

### Blocker Severity
- Stage B upgraded to **block source edits only** (Edit/Write/Bash touching source files)
- Non-source operations (reading, commits, skill invocations) still allowed
- Matches existing Stage A pattern for consistency

### GSD Command Tracking
- Use **state file markers** — silver-bullet.md will instruct Claude to write markers after each GSD command (similar to quality-gate-stage-N pattern)
- Markers: `gsd-discuss`, `gsd-plan`, `gsd-execute`, `gsd-verify` written to state file
- dev-cycle-check.sh and completion-audit.sh can then check for these markers
- Accepted fragility: markers depend on Claude compliance (same as quality-gate-stage-N)

### Anti-Skip Text Scope
- **Comprehensive** — add explicit "you are violating this rule if..." conditions
- Include observable evidence tests (e.g., "if mode file doesn't exist, you skipped §4")
- Cross-reference hooks that would detect violations
- Cover every skippable instruction in §0-§9

</decisions>

<specifics>
## Specific Ideas

- Stage B blocker reuses the same `blockToolUse: true` JSON output pattern as Stage A
- GSD markers use the same `echo "marker" >> state_file` pattern as quality-gate stages
- Anti-rationalization text should reference specific hook behaviors so Claude knows its compliance is observable
- compliance-status.sh already runs on every tool call — ideal place for mode/ownership warnings

</specifics>
