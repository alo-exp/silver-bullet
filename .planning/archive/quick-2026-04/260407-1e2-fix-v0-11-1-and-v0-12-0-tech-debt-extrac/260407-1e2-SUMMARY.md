---
phase: quick-260407-1e2
plan: 01
subsystem: hooks
tags: [refactor, tests, security, config]
key_files:
  created:
    - hooks/lib/required-skills.sh
  modified:
    - hooks/stop-check.sh
    - hooks/completion-audit.sh
    - hooks/prompt-reminder.sh
    - hooks/forbidden-skill-check.sh
    - templates/silver-bullet.config.json.default
    - tests/hooks/test-prompt-reminder.sh
    - tests/hooks/test-forbidden-skill-check.sh
decisions:
  - teardown() in test-prompt-reminder.sh must clean up trivial file to prevent test-ordering interference
  - config-list forbidden skills now get a generic project-config message distinct from the GSD-specific hardcoded message
metrics:
  duration: "~25 minutes"
  completed: "2026-04-07"
  tasks: 2
  files: 8
---

# Phase quick-260407-1e2 Plan 01: Tech Debt Extraction Summary

**One-liner:** Extracted duplicated DEFAULT_REQUIRED to a shared lib sourced by all 3 enforcement hooks, added 3 new security-coverage unit tests, fixed forbidden-skill message distinction, added config_version field, and refactored stop-check.sh stage-check to a function.

## Tasks Completed

### Task 1: Extract DEFAULT_REQUIRED to shared lib and refactor hooks

- Created `hooks/lib/required-skills.sh` with `DEFAULT_REQUIRED` and `DEVOPS_DEFAULT_REQUIRED` as the single source of truth.
- Updated `hooks/stop-check.sh`: replaced inline DEFAULT_REQUIRED assignment with source-from-lib + fallback pattern; replaced inline devops substitution with `DEVOPS_DEFAULT_REQUIRED`; extracted quality-gate-stage check into `check_quality_gate_stages()` function (TD-04).
- Updated `hooks/completion-audit.sh`: same source-from-lib + fallback pattern for both DEFAULT_REQUIRED and DEVOPS_DEFAULT_REQUIRED.
- Updated `hooks/prompt-reminder.sh`: same source-from-lib + fallback; sets `required_skills="$DEFAULT_REQUIRED"` when config is empty; added S-3 performance comment above grep loop.
- All 9 existing stop-check tests and 7 existing prompt-reminder tests continued passing.

**Commit:** 13a693f — `refactor(hooks): extract DEFAULT_REQUIRED to shared lib, stage-check to function (TD-01, TD-04)`

### Task 2: Tests, messages, config_version, invariant comment

- **test-prompt-reminder.sh:** Added git init to `setup()` for branch detection support; fixed `teardown()` to clean trivial file (preventing test-ordering bug); added Test 6 (main-branch filter, TD-02) and Test 7 (path traversal defense, TD-06).
- **test-forbidden-skill-check.sh:** Added Test 6 (double-namespace bypass, TD-03) verifying `outer:inner:executing-plans` is blocked.
- **hooks/forbidden-skill-check.sh:** Changed config-list forbidden skill message to generic project-config wording (TD-07); added S-1 invariant comment and early-exit for empty skill_name after namespace stripping.
- **templates/silver-bullet.config.json.default:** Added `"config_version": "0.11.0"` as first key (TD-05).
- Total tests: 187 passed, 0 failed (up from 184).

**Commit:** b1e848c — `test(hooks): add double-namespace, main-branch, path-traversal tests; fix forbidden-skill messages; add config_version (TD-02,03,05,06,07,S-1)`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed trivial-file cleanup in test teardown**

- **Found during:** Task 2, Test 6 debugging
- **Issue:** `teardown()` in test-prompt-reminder.sh did not clean up the trivial file created in Test 5. This caused Test 6 to find the trivial file still present, making the hook exit silently (trivial bypass), which produced empty output instead of "Missing:".
- **Fix:** Added `rm -f "${SB_TEST_DIR}/trivial-test-${TEST_RUN_ID}"` to `teardown()`.
- **Files modified:** tests/hooks/test-prompt-reminder.sh
- **Commit:** b1e848c (included in Task 2 commit)

## Test Results

```
TOTAL: 187 passed, 0 failed (3/3 suites green)
```

- stop-check: 9 passed
- prompt-reminder: 10 passed (7 existing + 2 new: Tests 6 and 7)
- forbidden-skill-check: 6 passed (5 existing + 1 new: Test 6)
- Integration / scenario suites: 54 passed
- Other unit suites: 108 passed

## Known Stubs

None.

## Threat Flags

None — no new network endpoints, auth paths, or trust boundary changes introduced.

## Self-Check: PASSED

- hooks/lib/required-skills.sh: FOUND
- hooks/stop-check.sh: FOUND (contains check_quality_gate_stages function)
- hooks/completion-audit.sh: FOUND (sources lib)
- hooks/prompt-reminder.sh: FOUND (sources lib, S-3 comment present)
- hooks/forbidden-skill-check.sh: FOUND (invariant comment, generic config message)
- templates/silver-bullet.config.json.default: FOUND (contains config_version)
- tests/hooks/test-prompt-reminder.sh: FOUND (Tests 6 and 7 present)
- tests/hooks/test-forbidden-skill-check.sh: FOUND (Test 6 present)
- Commit 13a693f: FOUND
- Commit b1e848c: FOUND
