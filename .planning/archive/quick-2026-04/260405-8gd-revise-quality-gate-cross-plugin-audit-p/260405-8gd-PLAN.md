---
phase: quick
plan: 260405-8gd
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/silver-bullet.md.base
  - hooks/completion-audit.sh
autonomous: true
requirements: [QG-CROSSPLUG, QG-REORDER]

must_haves:
  truths:
    - "Stage 2 Big-Picture Audit includes a 5th dimension checking all 4 dependency plugin skills for contradictions with Silver Bullet"
    - "Stage order is: 1-Code Review, 2-Big Picture, 3-Content Refresh, 4-SENTINEL"
    - "completion-audit.sh message string reflects new stage order"
  artifacts:
    - path: "templates/silver-bullet.md.base"
      provides: "Updated §9 with 5th dimension and reordered stages"
      contains: "Cross-plugin consistency"
    - path: "hooks/completion-audit.sh"
      provides: "Updated stage name order in block message"
      contains: "Content Refresh, SENTINEL"
  key_links:
    - from: "templates/silver-bullet.md.base"
      to: "hooks/completion-audit.sh"
      via: "stage marker numbers stay 1-4"
      pattern: "quality-gate-stage-[1-4]"
---

<objective>
Revise §9 quality gate in the silver-bullet.md.base template: (1) add a 5th audit dimension to Stage 2 for cross-plugin consistency checking, (2) reorder stages so Content Refresh is Stage 3 and SENTINEL is Stage 4 (security last, since earlier stages change code). Update completion-audit.sh message to reflect new order.

Purpose: Earlier stages may introduce code changes, so security audit should run last. Cross-plugin consistency catches contradictions between SB and its dependency plugins.
Output: Updated template and hook.
</objective>

<execution_context>
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/workflows/execute-plan.md
@${SB_RUNTIME_HOME_ROOT}/get-shit-done/templates/summary.md
</execution_context>

<context>
@templates/silver-bullet.md.base (lines 259-347 — §9 quality gate)
@hooks/completion-audit.sh (line 186 — stage name message)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update §9 in template — add 5th dimension and reorder stages</name>
  <files>templates/silver-bullet.md.base</files>
  <action>
Edit lines 259-347 of templates/silver-bullet.md.base (the §9 quality gate section). Two changes:

**Change A — Add 5th dimension to Stage 2 (Big-Picture Consistency Audit):**

In the Stage 2 numbered list item 1, after the existing 4 dimensions, add a 5th bullet:

```
   - Cross-plugin consistency (read 100% of skill content from all 4 dependency plugins — GSD: ${SB_RUNTIME_HOME_ROOT}/get-shit-done/ workflows/references/templates; Superpowers: ${SB_RUNTIME_HOME_ROOT}/plugins/cache/*/superpowers/*/skills/*/SKILL.md; Engineering: ${SB_RUNTIME_HOME_ROOT}/plugins/cache/*/knowledge-work-plugins/*/engineering/skills/*/SKILL.md; Design: ${SB_RUNTIME_HOME_ROOT}/plugins/cache/*/knowledge-work-plugins/*/design/skills/*/SKILL.md — check for contradictions, conflicts, inconsistencies, or redundancies between Silver Bullet instructions and upstream plugin skills)
```

Update the dimension count from "four dimensions" to "five dimensions" in the instruction text on line 285 ("Dispatch parallel Explore agents across four dimensions" -> "five dimensions").

**Change B — Reorder Stage 3 and Stage 4:**

Swap the current Stage 3 (SENTINEL) and Stage 4 (Content Refresh) so:
- Stage 3 becomes Public-Facing Content Refresh (currently Stage 4 content, but marker becomes quality-gate-stage-3)
- Stage 4 becomes Security Audit / SENTINEL (currently Stage 3 content, but marker becomes quality-gate-stage-4)

Keep all internal content of each stage intact — just swap their positions and update:
- Heading numbers: `### Stage 3 —` and `### Stage 4 —`
- Marker echo commands: stage-3 records `quality-gate-stage-3`, stage-4 records `quality-gate-stage-4`
- Step numbering within each stage stays the same

In the Pre-Release Gate Enforcement bullet list, update the labels:
- Stage 3: `quality-gate-stage-3` — was SENTINEL, now Content Refresh
- Stage 4: `quality-gate-stage-4` — was Content Refresh, now SENTINEL

Do NOT change anything about Stage 1 or Stage 2 other than the 5th dimension addition.
  </action>
  <verify>
    <automated>grep -c "Cross-plugin consistency" templates/silver-bullet.md.base | grep -q "1" && grep "Stage 3" templates/silver-bullet.md.base | head -1 | grep -q "Content Refresh" && grep "Stage 4" templates/silver-bullet.md.base | head -1 | grep -q "SENTINEL\|Security" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Stage 2 has 5th dimension for cross-plugin consistency. Stage 3 is Content Refresh. Stage 4 is SENTINEL. All markers use correct stage numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Update completion-audit.sh message string</name>
  <files>hooks/completion-audit.sh</files>
  <action>
Edit line 186 of hooks/completion-audit.sh. Change the stage name order in the message string from:

"Code Review Triad, Big-Picture Audit, SENTINEL, Content Refresh"

to:

"Code Review Triad, Big-Picture Audit, Content Refresh, SENTINEL"

This is the only change needed in this file. The marker checking logic (quality-gate-stage-1 through quality-gate-stage-4) stays exactly the same.
  </action>
  <verify>
    <automated>grep "Content Refresh, SENTINEL" hooks/completion-audit.sh | grep -q "Content Refresh, SENTINEL" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>completion-audit.sh message reflects new stage order: Code Review Triad, Big-Picture Audit, Content Refresh, SENTINEL.</done>
</task>

</tasks>

<verification>
1. `grep -A2 "Stage 3" templates/silver-bullet.md.base` shows Content Refresh
2. `grep -A2 "Stage 4" templates/silver-bullet.md.base` shows SENTINEL/Security
3. `grep "five dimensions" templates/silver-bullet.md.base` returns a match
4. `grep "Cross-plugin consistency" templates/silver-bullet.md.base` returns a match
5. `grep "Content Refresh, SENTINEL" hooks/completion-audit.sh` returns a match
6. All 4 `quality-gate-stage-N` markers still present in template
</verification>

<success_criteria>
- §9 Stage 2 has 5 audit dimensions including cross-plugin consistency with all 4 dependency plugin paths
- Stages reordered: 1-Code Review, 2-Big Picture, 3-Content Refresh, 4-SENTINEL
- completion-audit.sh message reflects new order
- No changes to marker names or hook logic
</success_criteria>

<output>
After completion, create `.planning/quick/260405-8gd-revise-quality-gate-cross-plugin-audit-p/260405-8gd-SUMMARY.md`
</output>
