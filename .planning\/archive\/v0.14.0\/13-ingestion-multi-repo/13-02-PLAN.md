---
phase: 13-ingestion-multi-repo
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - skills/silver/SKILL.md
  - templates/silver-bullet.md.base
autonomous: true
requirements: [REPO-02, REPO-03, REPO-04]

must_haves:
  truths:
    - "Mobile repo session is blocked when pinned spec-version mismatches main repo version"
    - "Non-mobile-exclusive requirements are spec'd in main repo first per documented convention"
    - "Mobile-exclusive requirements follow standard SB process with no special handling"
    - "silver-ingest is routable via /silver router"
  artifacts:
    - path: "skills/silver/SKILL.md"
      provides: "Router entry for silver-ingest"
      contains: "silver-ingest"
    - path: "templates/silver-bullet.md.base"
      provides: "Session-start version validation instruction and MCP prerequisites"
      contains: "SPEC.main.md"
  key_links:
    - from: "templates/silver-bullet.md.base"
      to: "skills/silver-ingest/SKILL.md"
      via: "§0 references silver-ingest for spec refresh on version mismatch"
      pattern: "silver.ingest"
---

<objective>
Wire silver-ingest into the SB ecosystem: add /silver router entry, add session-start version validation for multi-repo (REPO-02), document cross-repo workflow conventions (REPO-03/REPO-04), and add MCP prerequisite documentation to silver-bullet.md.

Purpose: Make silver-ingest discoverable and enforce cross-repo spec version consistency.
Output: Updated skills/silver/SKILL.md, updated templates/silver-bullet.md.base
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ingestion-multi-repo/13-RESEARCH.md
@skills/silver-ingest/SKILL.md
@skills/silver/SKILL.md
@templates/silver-bullet.md.base

<interfaces>
<!-- From 13-01 output -->
skills/silver-ingest/SKILL.md exists with name: silver-ingest, argument-hint, full step sequence

<!-- From existing router -->
skills/silver/SKILL.md contains routing table mapping user intents to silver:* skills
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add silver-ingest to /silver router</name>
  <files>skills/silver/SKILL.md</files>
  <read_first>
    - skills/silver/SKILL.md (current routing table format and existing entries)
    - skills/silver-ingest/SKILL.md (name and argument-hint for routing)
  </read_first>
  <action>
Add a routing entry for silver-ingest in skills/silver/SKILL.md. Follow the exact pattern of existing entries.

Add to the routing table:
- Intent patterns: "ingest", "import", "jira", "figma", "pull ticket", "cross-repo", "fetch spec from"
- Routes to: `silver:ingest`
- Description: "Ingest external artifacts (JIRA, Figma, Google Docs) into SPEC.md + DESIGN.md, or fetch cross-repo spec"

Also add silver-ingest to the skill inventory/listing section if one exists.
  </action>
  <verify>
    <automated>grep -q "silver.ingest\|silver-ingest" skills/silver/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ingest" skills/silver/SKILL.md (routing entry exists)
    - grep -q "jira\|JIRA" skills/silver/SKILL.md (JIRA intent pattern)
    - grep -q "figma\|Figma" skills/silver/SKILL.md (Figma intent pattern)
  </acceptance_criteria>
  <done>silver-ingest is discoverable via /silver router with appropriate intent patterns for JIRA, Figma, cross-repo, and general ingestion requests.</done>
</task>

<task type="auto">
  <name>Task 2: Add session-start version validation and MCP prerequisites to silver-bullet.md.base</name>
  <files>templates/silver-bullet.md.base</files>
  <read_first>
    - templates/silver-bullet.md.base (find §0 Session Start section and identify insertion point; find existing prerequisites or MCP documentation sections)
    - .planning/phases/13-ingestion-multi-repo/13-RESEARCH.md (Pattern 4: REPO-02 version validation, MCP prerequisite documentation guidance)
  </read_first>
  <action>
**Part A: Add REPO-02 version validation to §0 Session Start.**

Find the `## 0. Session Start` section in templates/silver-bullet.md.base. Add a subsection (after existing session-start checks) titled `### 0.x Multi-Repo Spec Validation` (use the next available sub-number):

```markdown
### 0.x Multi-Repo Spec Validation

If `.planning/SPEC.main.md` exists (indicating a satellite/mobile repo linked to a main repo spec):

1. Read `spec-version:` from `.planning/SPEC.main.md` frontmatter
2. Read the `<!-- READ-ONLY: fetched from {source-url} -->` header to get the source repo URL
3. Fetch the current main repo SPEC.md version:
   ```bash
   /opt/homebrew/bin/gh api repos/{owner}/{repo}/contents/.planning/SPEC.md --jq '.content' | base64 -d | grep -m1 '^spec-version:' | awk '{print $2}'
   ```
4. Compare versions:
   - **Match:** Display "Spec version v{N} -- in sync with main repo." and continue.
   - **Mismatch:** BLOCK the session. Display:
     ```
     SPEC VERSION MISMATCH
     Local (SPEC.main.md):  v{local}
     Remote ({repo}):       v{remote}

     Run /silver:ingest --source-url {repo-url} to refresh before proceeding.
     ```
     Do NOT proceed with any implementation work until the user refreshes.

If `.planning/SPEC.main.md` does not exist, skip this check silently.
```

**Part B: Add MCP Prerequisites documentation.**

Find the appropriate section in silver-bullet.md.base for prerequisites or configuration. Add a subsection documenting MCP connector requirements for ingestion features:

```markdown
### MCP Connector Prerequisites (for silver-ingest)

silver-ingest delegates all external data access to MCP connectors. Configure these in your Claude environment before using ingestion features:

| Connector | Required For | Configuration |
|-----------|-------------|---------------|
| Atlassian MCP | JIRA ticket + Confluence ingestion | API token auth via `/v1/mcp` streamable HTTP endpoint. SSE transport deprecated after 2026-06-30. |
| Figma MCP (official remote) | Design context extraction | Connect via Figma account. Free during beta. |
| Google Drive MCP | Google Docs/Slides extraction | Community MCP or Google Workspace CLI with OAuth. |

If a connector is not configured, silver-ingest continues with `[ARTIFACT MISSING]` blocks -- it never hard-blocks on a missing connector.
```

**Part C: Add REPO-03 and REPO-04 cross-repo workflow conventions.**

In the same prerequisites area or a new subsection near the multi-repo validation:

```markdown
### Cross-Repo Spec Workflow

- **Main repo specs first (REPO-03):** Non-mobile-exclusive requirements are implementation-spec'd in the main repo first. The main repo spec is then fetched as input for satellite repo sessions via `silver-ingest --source-url`.
- **Mobile-exclusive (REPO-04):** Requirements unique to the mobile/satellite repo follow the standard SB process entirely within the satellite repo -- no main repo dependency.
- **Do not edit SPEC.main.md directly.** It is a read-only cache of the main repo spec. To update, re-run `/silver:ingest --source-url {repo-url}`.
```
  </action>
  <verify>
    <automated>grep -q "SPEC.main.md" templates/silver-bullet.md.base && grep -q "SPEC VERSION MISMATCH\|spec-version" templates/silver-bullet.md.base && grep -q "MCP Connector Prerequisites\|Atlassian MCP" templates/silver-bullet.md.base && grep -q "REPO-03\|main repo.*first\|Non-mobile-exclusive" templates/silver-bullet.md.base && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "SPEC.main.md" templates/silver-bullet.md.base (REPO-02 validation references the file)
    - grep -q "MISMATCH\|mismatch" templates/silver-bullet.md.base (REPO-02 block on version mismatch)
    - grep -q "silver.ingest --source-url\|silver:ingest --source-url" templates/silver-bullet.md.base (REPO-02 refresh instruction)
    - grep -q "Atlassian MCP\|atlassian" templates/silver-bullet.md.base (MCP prereqs documented)
    - grep -q "SSE.*deprecated\|streamable HTTP\|/v1/mcp" templates/silver-bullet.md.base (Atlassian SSE deprecation noted)
    - grep -q "main repo.*first\|Non-mobile-exclusive\|REPO-03" templates/silver-bullet.md.base (REPO-03 convention)
    - grep -q "mobile-exclusive\|REPO-04\|standard SB process" templates/silver-bullet.md.base (REPO-04 convention)
    - grep -q "READ-ONLY\|read-only cache" templates/silver-bullet.md.base (SPEC.main.md is read-only)
  </acceptance_criteria>
  <done>silver-bullet.md.base has session-start version validation that blocks on spec-version mismatch (REPO-02), MCP prerequisite documentation, and cross-repo workflow conventions (REPO-03, REPO-04). The file also documents the Atlassian SSE deprecation timeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gh CLI -> version check | Remote API call during session start to validate spec version |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Denial of Service | §0 version check network failure | mitigate | If gh CLI call fails (no network, auth expired), display warning "Could not verify spec version -- proceed with caution" rather than blocking. The check is best-effort in §0. |
| T-13-07 | Tampering | SPEC.main.md local edit | mitigate | READ-ONLY header + §0 instruction explicitly forbids editing + version validation detects drift on next session start |
</threat_model>

<verification>
- skills/silver/SKILL.md contains silver-ingest routing entry
- templates/silver-bullet.md.base contains §0 version validation for SPEC.main.md
- templates/silver-bullet.md.base contains MCP prerequisite table
- templates/silver-bullet.md.base contains cross-repo workflow conventions
</verification>

<success_criteria>
silver-ingest is wired into the SB ecosystem: routable via /silver, session-start enforcement for multi-repo version pinning, MCP prerequisites documented, cross-repo conventions established. All REPO-02, REPO-03, REPO-04 requirements addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ingestion-multi-repo/13-02-SUMMARY.md`
</output>
