# Scenario: `silver-handoff`

Use when the current SB session should be wrapped up into a reusable project-level handoff prompt for the next agent or future session.

Example trigger: "Wrap up this session so another AI can continue safely."
