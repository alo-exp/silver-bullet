# silver-multi-ai-task scenario

Given a bounded task brief and declared model pools, dispatch the selected
lanes, reconcile their outputs, and verify the indexed artifacts and evidence
before returning control to the parent workflow.
