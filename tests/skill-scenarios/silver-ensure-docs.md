# Scenario: `silver-ensure-docs`

Use when the todo app's governed docs need bootstrapping, semantic freshness review, doc-scheme reconciliation, or recovery after a docs gate fails.

Example trigger: "Ensure the docs scheme is up to date before we ship."
