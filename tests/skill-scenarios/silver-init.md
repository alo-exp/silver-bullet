# Silver Init Skill Scenario

## Skill: silver-init
## Context: Project initialization

### Scenario: Initialize New Project

**Trigger:** "Start a new Node.js project"

**Workflow:**
1. Scaffold → npm init, git init
2. Structure → folder layout
3. Configure → package.json, .gitignore
4. Template → starter files
