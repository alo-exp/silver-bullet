# Project: [PROJECT NAME — customize this]
# Generated by forge-sb-install.sh | Customize before first use
#
# This file extends the global Silver Bullet AGENTS.md (~/forge/AGENTS.md) with
# project-specific conventions. Forge merges both files into the system prompt.
# Project rules take priority over global ones for any direct conflict.

## Project Conventions

Language: [Rust / TypeScript / Python / ...]
Framework: [...]
Test runner: [cargo test / npm test / pytest / ...]
Linter: [clippy / eslint / ruff / ...]
Formatter: [rustfmt / prettier / black / ...]
Package manager: [cargo / npm / pip / poetry / ...]

## GSD Phase Cursor

Current phase: [check .planning/STATE.md and fill in]
Active milestone: [from .planning/PROJECT.md]
Active plan: [path to current PLAN.md or "(none)"]

## Project-Specific Rules

[Add project non-negotiables here. These override global SB defaults when in conflict.]

Examples:
- Never run tests with `--no-fail-fast` (masks cascading failures)
- Always run `<lint command>` before committing
- DCO sign-off required on every commit: `Signed-off-by: <name> <email>`
- Branch naming: `feat/<phase>-<description>`, `fix/<description>`
- Commit format: `<type>(<scope>): <description>` — types: feat, fix, refactor, test, docs, chore

## Project-Specific Skill Routing

[Override or extend global skill routing for this project. Examples:]

| User says | Apply skill |
|---|---|
| "deploy to staging" | (custom: project-staging-deploy) |
| "run smoke tests" | (custom: project-smoke-tests) |

## Custom Hook-Agent Configuration

[If this project uses different required-skill lists, document them here. The
default required-skill lists live in .planning/config.json under "skills".]

For example, this project might require additional skills before PR:
- `engineering-deploy-checklist` for production-bound changes
- `design-accessibility-review` for UI changes
- `silver-blast-radius` for cross-cutting changes

The `forge-pre-pr-audit` agent reads these from `.planning/config.json` and
enforces them.

## Project Stack Specifics

[Project-specific guardrails here. Examples:]

- Database migrations: never run destructive migrations without `--accept-data-loss` flag review
- Schema changes require both `prisma db push` AND `prisma generate`
- Production builds must include source maps disabled
- E2E tests must run against a containerised database, not a mocked one

## Forge Output Format (optional)

[Some teams want a consistent end-of-task summary. Uncomment to enforce:]

```
# After every task, end the response with:
# STATUS: SUCCESS | PARTIAL | FAILED
# FILES_CHANGED: <list>
# ASSUMPTIONS: <any assumptions made>
# REQ-IDS_ADDRESSED: <list of REQ-IDs from REQUIREMENTS.md>
```

## Task Patterns

[Recurring task types and how to handle them — populate from learnings over time.]

## Forge Corrections

[Specific corrections for this codebase — populate when mistakes are caught.]

## Notes

- Silver Bullet for Forge global rules: see `~/forge/AGENTS.md`
- All ~106 SB skills auto-load from `~/forge/skills/`
- All ~41 hook-equivalent + GSD subagent custom agents auto-load from `~/forge/agents/`
- Project-level overrides go in `.forge/skills/` and `.forge/agents/`
