# Decision — FAST quality-order exemption + Job/GST exclusion (2026-08-16)

Locked into router_subagent_surfaces_85bf9f09 plan (clarify round-8). Did not stop for user questions. Stay on `main`. No commit.

## FAST skips

- Advisor / Board of Advisors
- composition-Val (no Validator spawn; cover is by classification, not a Val-loop)
- plan-time Validation-loop
- A-loop Mentorship
- Verification-loop
- Process-final Validation-loop
- Job mint (`original_intent_hash`)
- GST-01 projector write (must not appear on Global Status)

## FAST keeps

- Orchestrator classifies trivial and wraps `sb:fast` (`WF-SILVER-FAST` / `AF-FAST-PATH`)
- parent≠implementer (Orchestrator does not answer/implement)
- one Authorizer-admitted `AF-FAST-PATH` Executor or deny-all Q&A leaf
- no synthesize on trivial

## Fail-closed

If not actually trivial (durable product edit, multi-file change): reclassify as non-trivial and enter full mint → composition-Val → … quality order. That work **is** a Job and **does** hit GST.

## Locked otherwise

ESC-02 no A on non-trivial escalation; Authorizer not Approver; no `AF-meta-six-role`; wrap at `/sb`; BOA parallel for non-trivial Advisor.

Clarify: `.planning/router_subagent_surfaces_85bf9f09-CLARIFY-260717-143757.md` (round-8)
Plan SHA-256: `d7565d5695651ccd90696711561f1d61004006bf0c6b41cd536779b03f01268b`
