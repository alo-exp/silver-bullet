# Decision — prompt+spec launch gate + ASCII WBS progress (2026-07-20)

Locked into router_subagent_surfaces_85bf9f09 plan (Grok 4.5 High):
1. Every host subagent launch requires prompt-engineered prompt + work spec (goal_outcome, required_outputs, acceptance_criteria, scope_bounds, context_refs); fail-closed blocked_launch_prompt_spec.
2. ASCII WBS progress viz on step transitions / user-facing status; path Process > Workflow > AF > Step; markers [x]/[>]/[ ]; fail-closed blocked_progress_viz.
Traceability LPS-01 / WBS-01 (VAL/TST-RFL-616/617). Clarify round-3 addendum appended.
