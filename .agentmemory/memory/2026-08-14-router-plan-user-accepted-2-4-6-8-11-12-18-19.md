# router-plan-user-accepted-2-4-6-8-11-12-18-19

Decision (2026-08-14): Applied user ratifications to both byte-identical copies of `router_subagent_surfaces_85bf9f09.plan.md` (repo `.planning/` + `~/.cursor/plans/`) on git `main`. No commit. No product code.

User-accepted (list still pending formal lock): #2 effort (`max` valid where documented; Executor = highest available for that runtime; Cursor Max Mode ≠ thinking-effort), #4 WBS single-writer, #6 Authorizer uses Verifier model+effort (same weights, different job/tools; TCB keys stay in hooks), #8 no dual `sb`/`silver` prefix, #11 in-repo slug catalog, #12 five-tool opt-in then mandatory on every selected runtime, #18 `sb:agent-*` already exist (rename including opencode; host adapters may still sequence), #19 no Sidekick (bounded replan stays).

Open: #7 Pi mimo-v2.5 pin vs honor user model+effort.
