# RFL rung-07 Pi Claude Opus 5 High — review pass 9 (2026-08-31)

RFL rung-07 Pi Claude Opus 5 High review pass 9 on `.planning/spec_template_world_class.plan.md`.
SHA `892b263d530f867b21c36426e6b1e7917690aafd1d95bef2d3d92cb951addde4` verified on BOTH twins (freeze + `phases/01-world-class-spec/PLAN.md`), 726 lines.
Verdict **NOT CLEAN**. 11 residuals `R7i-F01`..`R7i-F11` (1 HIGH / 3 MED / 4 LOW / 3 nit). Written to `review-rerun-9.md` only. Freeze not mutated; prior review files untouched.

- **R7i-F01 HIGH:** Clause-(c) version-cell re-anchor (R7h-F03, L73) has no reachable target. Offered target A = "the retained migration-record entry" but that record is non-canonical / not-parsed-by-any-QC / never-an-input (L313/L457/L593) and is not a staged-SPEC heading, so `scan-section-slug` unique-heading match fails ⇒ the very `REQ-F71` the rule promises to avoid. Offered target B = "surviving canonical row" is a silent semantic repoint (`v3`→`v1`) with no row-identity rule, contradicting "silent repoint FAIL". Only fail-before-write/ASK remains, deadlocking the pinned L602 malformed-prior **PASS-install** fixture in brief-less/CI (ASK = fail-before-write per R7c-F01). Ask: named `change-row-identity` (summary-cell text under `decision-row-identity` normalization + original integer); delete the migration-record pointer; PASS branch = identity match, FAIL fixture for migrated-out no-match.
- **R7i-F02 MED:** `ASM-nn` is now a **mandatory** clause-(a) SCAN anchor (R7g-F07/R7h-F06, pinned PASS at L437) but L217/L426 QC-13 still says "`ASM-nn` remains optional" — no exact-width check, **no duplicate-full-ID FAIL** (two `ASM-01` legal ⇒ ambiguous clause-(a) resolution with no FAIL rule), not in the L217 catalog-ID list so SPEC `id-tombstones` cannot hold it (clause (a)'s "not tombstoned" undefined), and no producer authorized to add `ASM-nn` though R7h-F06 triggers on "gains `ASM-nn`".
- **R7i-F03 MED:** QC-10's "non-placeholder summary" (L426) can FAIL the freeze's own mandated no-structural-change sentence (L182, R7g-F10/R7h-F11) which L602 pins as PASS. No whitelist; token `no structural changes` absent from the L437 QC-string assert list ⇒ compiler must emit a string the reviewer may reject.
- **R7i-F04 MED:** QC-10 summary provenance is written as a **reviewer** rule (L426) though reviewers cannot see the brief and `change-summary` has no SPEC YAML projection. The adjacent QC-11 row on the same line carries the repair ("Provenance is a compiler obligation… Reviewers read SPEC YAML, not the brief"); QC-10 does not. Same class as R7-F06 / R7b-F04.
- **R7i-F05 LOW:** `review-cross-artifact` (L428) imports only the `<line-or-id>` half of SCAN resolution; `scan-section-slug` `<section>` normalization + unique-heading/ambiguous-slug FAIL is bound at L73/L293/L427 but not L428, though XART must "resolve every `SCAN:` atom … before the eligible-set join" (R7d-F05/R7e-F01). The L434 `rg` spans all three files so a match elsewhere satisfies it.
- **R7i-F06 LOW:** L427 still defines clause (b) as "for **ID-less sections**" — too broad (`## Overview` is ID-less but FAILs, R7g-F05) and too narrow (mixed `## Assumptions` is not ID-less yet is `bNN`-citable, R7h-F01; `#b02` pinned PASS at L437). L293/L428 already use the closed form; L427 is the lone residual.
- **R7i-F07 LOW:** Wave 2 `rg` alternation (L434) omits `decision-row-identity` and any Assumptions/`ASM-nn`/per-entry token though L437 asserts both on the same three skill files. Same omission class as R7b-F11 / R7c-F10 / R7d-F06 / R7f-F08 / R7h-F08.
- **R7i-F08 LOW:** Wave 3 `- contains` QC-10 bullet (L500) omits the named no-structural-change clause, the "delta set excluding the version entry" trigger, the closed `<reason>` enum, and the `N` binding — i.e. the compiler string contract does not pin the branch that keeps brief-less augment 2/3/4b off ASK (the R7f-F01 HIGH).
- **R7i-F09 nit:** `decision-log` Default class cell (L197) reads `**conditionally-required** (R7c-F15/R7h-F10)` — not enum-only per L209; every other pack row's class cell is bare. Residual of R7h-F10.
- **R7i-F10 nit:** Clause (c) `v<integer>` lacks a canonical decimal form (`v01` and `v1` both resolve ⇒ Step 8 re-anchor rewrite has no canonical output) and lacks a dead-value rule for `v0`/non-positive, unlike fully-bounded `b00` / `>99`.
- **R7i-F11 nit:** R7h-F05 Assumptions entry grammar has PASS-side fixtures only (`#ASM-01`, `#b02`) plus the per-entry-MUST negative; nothing pins the "continuation, nested, and non-conforming lines do not count" half, so a mis-counting implementation shifts every ordinal base that R7h-F01 stability depends on.

**R7h-F01..F11 APPLY confirmed landed** (evidence table in the review). R7g/R7f/R7e/R7d/R7c/R7b/R7/R6b–R6n spot-checked, unweakened. Spec-floor untightened. KEEP REJECT upheld (two files; Clarify does not write SPEC; ingest stays; migration record explicitly not a third canonical doc — R7i-F01 proposes deleting an unreachable pointer to it, not parsing it). `R7b-F17` REJECT not reopened.

Role review-only: no triage, no APPLY, no fix, no branch switch, no commit, no verify launch, no ladder advance, no `--record-rung-review-outcome`. Reviewer = Claude High Pi (`claude/claude-opus-5-high`, OmniRoute); Verify/Triage = Composer 2.5; Fix/APPLY = Grok 4.6 High. Policy F streak was 0 entering pass 9; ACCEPT+APPLY resets it again.
