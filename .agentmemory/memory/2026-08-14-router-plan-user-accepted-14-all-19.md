# router-plan-user-accepted-14-all-19

Decision (2026-08-14): User accepted checklist **item 14**. Applied to both byte-identical copies of `router_subagent_surfaces_85bf9f09.plan.md` (repo `.planning/` + `~/.cursor/plans/`) on git `main`. No commit. No product code.

Item 14 (kept plain-language): Task saved when work starts; if steps need to change a lot, Advisor writes the new steps; Executor must not silently change steps.

Items 1 and 17 were already user-accepted from the prior edit.

**All 19 user-accepted**, pending a single formal “lock all.” No remaining “draft pending understanding” items.
