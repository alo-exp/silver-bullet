---
name: "sb-execute"
title: "SB: Sb Execute"
description: This skill executes an SB PLAN.md in controlled waves with TDD, evidence capture, and summary artifacts.
argument-hint: <plan path or execution scope>
---

Invoke the Silver Bullet `sb:execute` workflow for this request. Follow the composable flow contracts in `docs/composable-flows-contracts.md` and record required skill markers through the host runtime-native skill invocation channel. If the runtime-native skill invocation channel cannot resolve this route by name, read the full instructions from `skill-source/sb-execute/SILVER_SOURCE` under the Silver Bullet plugin install root.
